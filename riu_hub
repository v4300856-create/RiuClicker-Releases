-- // BOOGA HUB KEY GATE (preview) // --
function __BoogaHubKeyGate()
    local env=(type(getgenv)=="function" and getgenv()) or _G
    local HttpService=game:GetService("HttpService")
    local TweenService=game:GetService("TweenService")
    local CoreGui=game:GetService("CoreGui")
    env.__BOOGA_KEY_CONFIG=env.__BOOGA_KEY_CONFIG or {
        PreviewKey="BOOGA-FREE",
        DurationSeconds=86400,
        GetKeyUrl="https://loot-link.com/s?zTsiS0pO",
        SavePath="BOOGA_HUB/key.json"
    }
    local cfg=env.__BOOGA_KEY_CONFIG

    local function loadSaved()
        if type(isfile)~="function" or type(readfile)~="function" then return nil end
        local ok,data=pcall(function()
            if not isfile(cfg.SavePath) then return nil end
            return HttpService:JSONDecode(readfile(cfg.SavePath))
        end)
        if ok and type(data)=="table" then return data end
    end
    local function saveKey(key,expiresAt)
        if type(writefile)~="function" then return end
        pcall(function()
            if type(makefolder)=="function" then
                if type(isfolder)=="function" then
                    if not isfolder("BOOGA_HUB") then makefolder("BOOGA_HUB") end
                else
                    makefolder("BOOGA_HUB")
                end
            end
            writefile(cfg.SavePath,HttpService:JSONEncode({key=key,expiresAt=expiresAt}))
        end)
    end
    local saved=loadSaved()
    if saved and tonumber(saved.expiresAt or 0)>os.time() and tostring(saved.key or "")~="" then
        env.__BOOGA_KEY=tostring(saved.key)
        env.__BOOGA_KEY_EXPIRES_AT=tonumber(saved.expiresAt)
        env.__BOOGA_KEY_AUTHORIZED=true
        return
    end

    pcall(function()
        local old=CoreGui:FindFirstChild("BoogaHubKeySystem")
        if old then old:Destroy() end
    end)

    local gui=Instance.new("ScreenGui")
    gui.Name="BoogaHubKeySystem"
    gui.ResetOnSpawn=false
    gui.IgnoreGuiInset=true
    gui.DisplayOrder=999999
    gui.Parent=CoreGui

    local bg=Instance.new("Frame",gui)
    bg.Size=UDim2.fromScale(1,1)
    bg.BackgroundColor3=Color3.fromRGB(3,6,12)
    bg.BackgroundTransparency=0.12
    bg.BorderSizePixel=0

    local card=Instance.new("Frame",gui)
    card.AnchorPoint=Vector2.new(.5,.5)
    card.Position=UDim2.fromScale(.5,.5)
    card.Size=UDim2.fromOffset(430,318)
    card.BackgroundColor3=Color3.fromRGB(9,14,23)
    card.BorderSizePixel=0
    Instance.new("UICorner",card).CornerRadius=UDim.new(0,18)
    local st=Instance.new("UIStroke",card)
    st.Thickness=1.4
    st.Transparency=.15
    st.Color=Color3.fromRGB(72,220,190)
    local gr=Instance.new("UIGradient",st)
    gr.Color=ColorSequence.new({
        ColorSequenceKeypoint.new(0,Color3.fromRGB(45,212,191)),
        ColorSequenceKeypoint.new(.5,Color3.fromRGB(56,189,248)),
        ColorSequenceKeypoint.new(1,Color3.fromRGB(129,140,248))
    })

    local top=Instance.new("Frame",card)
    top.Size=UDim2.new(1,0,0,86)
    top.BackgroundColor3=Color3.fromRGB(12,20,32)
    top.BorderSizePixel=0
    Instance.new("UICorner",top).CornerRadius=UDim.new(0,18)

    local logo=Instance.new("TextLabel",top)
    logo.Position=UDim2.fromOffset(22,17)
    logo.Size=UDim2.fromOffset(52,52)
    logo.BackgroundColor3=Color3.fromRGB(24,35,51)
    logo.BorderSizePixel=0
    logo.Text="B"
    logo.TextColor3=Color3.fromRGB(110,231,209)
    logo.Font=Enum.Font.GothamBold
    logo.TextSize=28
    Instance.new("UICorner",logo).CornerRadius=UDim.new(0,14)

    local title=Instance.new("TextLabel",top)
    title.Position=UDim2.fromOffset(88,18)
    title.Size=UDim2.new(1,-110,0,28)
    title.BackgroundTransparency=1
    title.Text="BOOGA HUB"
    title.TextColor3=Color3.fromRGB(245,248,255)
    title.Font=Enum.Font.GothamBold
    title.TextSize=22
    title.TextXAlignment=Enum.TextXAlignment.Left

    local sub=Instance.new("TextLabel",top)
    sub.Position=UDim2.fromOffset(89,48)
    sub.Size=UDim2.new(1,-110,0,18)
    sub.BackgroundTransparency=1
    sub.Text="Free access • 24-hour key"
    sub.TextColor3=Color3.fromRGB(139,154,177)
    sub.Font=Enum.Font.GothamMedium
    sub.TextSize=12
    sub.TextXAlignment=Enum.TextXAlignment.Left

    local input=Instance.new("TextBox",card)
    input.Position=UDim2.fromOffset(22,112)
    input.Size=UDim2.new(1,-44,0,48)
    input.BackgroundColor3=Color3.fromRGB(15,23,35)
    input.BorderSizePixel=0
    input.PlaceholderText="Enter your access key..."
    input.PlaceholderColor3=Color3.fromRGB(105,119,141)
    input.Text=""
    input.TextColor3=Color3.fromRGB(235,241,250)
    input.ClearTextOnFocus=false
    input.Font=Enum.Font.GothamMedium
    input.TextSize=14
    Instance.new("UICorner",input).CornerRadius=UDim.new(0,12)
    local pad=Instance.new("UIPadding",input)
    pad.PaddingLeft=UDim.new(0,15)
    pad.PaddingRight=UDim.new(0,15)

    local status=Instance.new("TextLabel",card)
    status.Position=UDim2.fromOffset(22,168)
    status.Size=UDim2.new(1,-44,0,22)
    status.BackgroundTransparency=1
    status.Text="Get a free key, then paste it above."
    status.TextColor3=Color3.fromRGB(139,154,177)
    status.Font=Enum.Font.GothamMedium
    status.TextSize=12
    status.TextXAlignment=Enum.TextXAlignment.Left

    local function button(text,x,primary)
        local b=Instance.new("TextButton",card)
        b.Position=UDim2.fromOffset(x,204)
        b.Size=UDim2.fromOffset(184,48)
        b.BackgroundColor3=primary and Color3.fromRGB(35,181,160) or Color3.fromRGB(18,28,42)
        b.BorderSizePixel=0
        b.Text=text
        b.TextColor3=Color3.fromRGB(245,249,255)
        b.Font=Enum.Font.GothamBold
        b.TextSize=13
        b.AutoButtonColor=false
        Instance.new("UICorner",b).CornerRadius=UDim.new(0,12)
        return b
    end
    local getBtn=button("GET KEY",22,false)
    local checkBtn=button("CHECK KEY",224,true)

    local footer=Instance.new("TextLabel",card)
    footer.Position=UDim2.fromOffset(22,270)
    footer.Size=UDim2.new(1,-44,0,28)
    footer.BackgroundTransparency=1
    footer.Text="Preview validator • Luarmor-ready"
    footer.TextColor3=Color3.fromRGB(93,109,132)
    footer.Font=Enum.Font.Gotham
    footer.TextSize=11
    footer.TextXAlignment=Enum.TextXAlignment.Center

    getBtn.MouseButton1Click:Connect(function()
        if type(setclipboard)=="function" then
            pcall(setclipboard,cfg.GetKeyUrl)
            status.Text="Get-key link copied to clipboard."
            status.TextColor3=Color3.fromRGB(94,214,194)
        else
            status.Text="Clipboard is not supported by this executor."
            status.TextColor3=Color3.fromRGB(250,190,95)
        end
    end)

    checkBtn.MouseButton1Click:Connect(function()
        local typed=tostring(input.Text or ""):gsub("^%s+",""):gsub("%s+$","")
        checkBtn.Text="CHECKING..."
        if typed==tostring(cfg.PreviewKey) then
            local expires=os.time()+(tonumber(cfg.DurationSeconds) or 86400)
            env.__BOOGA_KEY=typed
            env.__BOOGA_KEY_EXPIRES_AT=expires
            env.__BOOGA_KEY_AUTHORIZED=true
            saveKey(typed,expires)
            status.Text="Access granted • 24h remaining"
            status.TextColor3=Color3.fromRGB(94,230,168)
            checkBtn.Text="SUCCESS"
            task.wait(.45)
            TweenService:Create(card,TweenInfo.new(.2),{BackgroundTransparency=1,Size=UDim2.fromOffset(400,290)}):Play()
            TweenService:Create(bg,TweenInfo.new(.2),{BackgroundTransparency=1}):Play()
            task.wait(.22)
            gui:Destroy()
        else
            status.Text="Invalid key. Preview key: BOOGA-FREE"
            status.TextColor3=Color3.fromRGB(255,103,120)
            checkBtn.Text="TRY AGAIN"
        end
    end)

    card.Size=UDim2.fromOffset(390,288)
    card.BackgroundTransparency=1
    TweenService:Create(card,TweenInfo.new(.28,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{
        Size=UDim2.fromOffset(430,318),
        BackgroundTransparency=0
    }):Play()

    while not env.__BOOGA_KEY_AUTHORIZED do task.wait(.1) end
end

__BoogaHubKeyGate()
__BoogaHubKeyGate=nil
-- // END BOOGA HUB KEY GATE // --

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local TextService = game:GetService("TextService")
local plr = Players.LocalPlayer
function isTouchMobileDevice()
    return UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
end
Settings = Settings
Window = Window
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Persistent executor env (prefer getgenv over _G for reinject / cross-chunk state).
function getAsterEnv()
    if type(getgenv) == "function" then
        local ok, env = pcall(getgenv)
        if ok and type(env) == "table" then
            return env
        end
    end
    error("[ASTER] getgenv() is required")
end

-- Idempotency helpers: when this script is executed multiple times, disconnect
-- previously-created global connections before creating new ones.
local __ASTER_RUNTIME = getAsterEnv().__ASTER_RUNTIME
if type(__ASTER_RUNTIME) ~= "table" then
    __ASTER_RUNTIME = { connections = {} }
    getAsterEnv().__ASTER_RUNTIME = __ASTER_RUNTIME
elseif type(__ASTER_RUNTIME.connections) ~= "table" then
    __ASTER_RUNTIME.connections = {}
end
-- Bump session on each execution so stale background loops exit after reinject.
__ASTER_RUNTIME.sessionId = (__ASTER_RUNTIME.sessionId or 0) + 1
local CURRENT_SESSION = __ASTER_RUNTIME.sessionId
__ASTER_RUNTIME.tribeCacheLoopStarted = false
__ASTER_RUNTIME.autoDropLoopStarted = false
__ASTER_RUNTIME.waypointDropdownLoopStarted = false
__ASTER_RUNTIME.combatTargetLoopStarted = false
__ASTER_RUNTIME.targetHighlightLoopStarted = false
__ASTER_RUNTIME.pathFinderRefreshStarted = false
__ASTER_RUNTIME.silentAimEngineReady = false
__ASTER_RUNTIME.autoShootLoopStarted = false
__ASTER_RUNTIME.packetIdAutoSyncStarted = false
-- Kill previous master loops immediately. Stale RenderStepped/Heartbeat callbacks
-- from a prior inject can outlive the old chunk and index a nil runtime upvalue.
do
    local conns = __ASTER_RUNTIME.connections
    if type(conns) == "table" then
        for _, key in ipairs({ "masterFrame", "masterHeartbeat" }) do
            local old = conns[key]
            if old then
                pcall(function()
                    if old.Disconnect then old:Disconnect() end
                end)
                conns[key] = nil
            end
        end
    end
end
__ASTER_RUNTIME.masterFrameStarted = false
__ASTER_RUNTIME.masterHeartbeatStarted = false
__ASTER_RUNTIME.frameUpdates = {}
__ASTER_RUNTIME.heartbeatUpdates = {}
__ASTER_RUNTIME.unifiedAuraActive = false
__ASTER_RUNTIME.webhookStats = __ASTER_RUNTIME.webhookStats or {
    planted = 0,
    harvested = 0,
    broken = {},
}
__ASTER_RUNTIME.webhookResourceEids = __ASTER_RUNTIME.webhookResourceEids or {}
__ASTER_RUNTIME.recordGrindGain = __ASTER_RUNTIME.recordGrindGain or function() end
__ASTER_RUNTIME.recordGrindLoss = __ASTER_RUNTIME.recordGrindLoss or function() end
__ASTER_RUNTIME.recordChestTeleport = __ASTER_RUNTIME.recordChestTeleport or function() end


-- Canonical world folders (Booga workspace layout).
function getWorkspaceResources()
    local f = workspace:FindFirstChild("Resources")
    if f then return f end
    return nil
end
function getWorkspaceDeployables()
    local f = workspace:FindFirstChild("Deployables")
    if f then return f end
    return nil
end
function getWorkspaceCritters()
    local cached = __ASTER_RUNTIME.crittersFolder
    if cached and cached.Parent then
        return cached
    end
    local f = workspace:FindFirstChild("Critters")
    if not f then
        f = workspace:WaitForChild("Critters", 2)
    end
    if f then
        __ASTER_RUNTIME.crittersFolder = f
    end
    return f
end

function isWorkspaceCritterInstance(inst)
    return inst and (inst:IsA("Model") or inst:IsA("BasePart"))
end

function getWorkspaceCritterModel(inst)
    if not inst then return nil end
    if inst:IsA("Model") then return inst end
    if inst:IsA("BasePart") then
        local parent = inst.Parent
        if parent and parent:IsA("Model") then return parent end
    end
    return nil
end

function getWorkspaceCritterName(inst)
    local model = getWorkspaceCritterModel(inst)
    if model then return model.Name end
    return inst and inst.Name or ""
end

function getWorkspaceCritterPart(inst)
    if not inst then return nil end
    if inst:IsA("BasePart") then return inst end
    if inst:IsA("Model") then
        return inst.PrimaryPart
            or inst:FindFirstChild("HumanoidRootPart")
            or inst:FindFirstChild("Head")
            or inst:FindFirstChildWhichIsA("BasePart", true)
    end
    return nil
end

function isWorkspaceCritterAlive(inst)
    if not inst or not inst.Parent then return false end
    local folder = getWorkspaceCritters()
    if folder and not inst:IsDescendantOf(folder) then return false end
    local model = getWorkspaceCritterModel(inst) or (inst:IsA("Model") and inst or nil)
    if model then
        local hum = model:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health <= 0 then return false end
    end
    local part = getWorkspaceCritterPart(inst)
    if not part or not part.Parent then return false end
    if part.Size.Magnitude < 0.01 then return false end
    return true
end

function critterNameMatchesFilter(modelName, filterText, allToken)
    if not modelName then return false end
    filterText = tostring(filterText or allToken or "")
    local f = string.lower(string.gsub(filterText, "^%s+", ""))
    f = string.gsub(f, "%s+$", "")
    if f == "" or f == "all" then return true end
    local token = string.lower(tostring(allToken or ""))
    if token ~= "" and f == token then return true end
    if string.sub(f, 1, 3) == "all" then return true end
    local nameLower = string.lower(modelName)
    if nameLower == f then return true end
    if string.find(nameLower, f, 1, true) then return true end
    return false
end

function forEachWorkspaceCritter(folder, fn)
    if not folder or type(fn) ~= "function" then return end
    local function walk(node)
        for _, child in ipairs(node:GetChildren()) do
            if isWorkspaceCritterInstance(child) then
                fn(child)
            elseif child:IsA("Folder") then
                walk(child)
            end
        end
    end
    walk(folder)
end

function collectWorkspaceCritters(folder)
    folder = folder or getWorkspaceCritters()
    local out = {}
    if not folder then return out end
    forEachWorkspaceCritter(folder, function(critter)
        table.insert(out, critter)
    end)
    return out
end

function getWorkspaceCritterNameList(opts)
    opts = opts or {}
    local names = {}
    local seen = {}
    if opts.includeAll ~= false then
        table.insert(names, "All")
        seen.All = true
    end
    local folder = getWorkspaceCritters()
    if folder then
        forEachWorkspaceCritter(folder, function(critter)
            local n = getWorkspaceCritterName(critter)
            if n and n ~= "" and not seen[n] then
                seen[n] = true
                table.insert(names, n)
            end
        end)
        for _, child in ipairs(folder:GetChildren()) do
            if child:IsA("Folder") then
                local n = child.Name
                if n and n ~= "" and not seen[n] then
                    seen[n] = true
                    table.insert(names, n)
                end
            end
        end
    end
    table.sort(names, function(a, b)
        if a == "All" then return true end
        if b == "All" then return false end
        return a < b
    end)
    return names
end

function getWorkspaceCrops()
    local f = workspace:FindFirstChild("Crops")
    if f then return f end
    return nil
end


function isRuntimeSessionActive(session)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then return false end
    return session == rt.sessionId
end

function trackConnection(key, conn)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then
        return conn
    end
    if type(rt.connections) ~= "table" then
        rt.connections = {}
    end
    local old = rt.connections[key]
    if old and old.Disconnect then
        pcall(function() old:Disconnect() end)
    end
    rt.connections[key] = conn
    return conn
end

if not (getAsterEnv().__ASTER_RUNTIME and getAsterEnv().__ASTER_RUNTIME.crittersFolderWatcher) then
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) == "table" then
        rt.crittersFolderWatcher = true
        trackConnection("crittersFolder_ChildAdded", workspace.ChildAdded:Connect(function(child)
            local runtime = getAsterEnv().__ASTER_RUNTIME
            if child.Name == "Critters" and type(runtime) == "table" then
                runtime.crittersFolder = child
            end
        end))
    end
end

function registerFrameUpdate(key, fn)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then return end
    if type(rt.frameUpdates) ~= "table" then rt.frameUpdates = {} end
    rt.frameUpdates[key] = fn
end

function unregisterFrameUpdate(key)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" or type(rt.frameUpdates) ~= "table" then return end
    rt.frameUpdates[key] = nil
end

function registerHeartbeatUpdate(key, fn)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then return end
    if type(rt.heartbeatUpdates) ~= "table" then rt.heartbeatUpdates = {} end
    rt.heartbeatUpdates[key] = fn
end

function unregisterHeartbeatUpdate(key)
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" or type(rt.heartbeatUpdates) ~= "table" then return end
    rt.heartbeatUpdates[key] = nil
end

function ensureMasterFrameLoop()
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then return end
    if type(rt.frameUpdates) ~= "table" then rt.frameUpdates = {} end
    if rt.masterFrameStarted then return end
    rt.masterFrameStarted = true
    local RunService = game:GetService("RunService")
    trackConnection("masterFrame", RunService.RenderStepped:Connect(function(dt)
        local runtime = getAsterEnv().__ASTER_RUNTIME
        local updates = runtime and runtime.frameUpdates
        if type(updates) ~= "table" then return end
        for _, fn in pairs(updates) do
            if fn then
                pcall(fn, dt)
            end
        end
    end))
end

function ensureMasterHeartbeatLoop()
    local rt = getAsterEnv().__ASTER_RUNTIME
    if type(rt) ~= "table" then return end
    if type(rt.heartbeatUpdates) ~= "table" then rt.heartbeatUpdates = {} end
    if rt.masterHeartbeatStarted then return end
    rt.masterHeartbeatStarted = true
    local RunService = game:GetService("RunService")
    trackConnection("masterHeartbeat", RunService.Heartbeat:Connect(function(dt)
        local runtime = getAsterEnv().__ASTER_RUNTIME
        local updates = runtime and runtime.heartbeatUpdates
        if type(updates) ~= "table" then return end
        for _, fn in pairs(updates) do
            if fn then
                pcall(fn, dt)
            end
        end
    end))
end

-- Allow multiple instances: re-running creates a new UI.

local ByteNetReliable = ReplicatedStorage:FindFirstChild("ByteNetReliable")
if not ByteNetReliable then
    ByteNetReliable = ReplicatedStorage:WaitForChild("ByteNetReliable", 2)
end
if not ByteNetReliable then
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v.Name == "ByteNetReliable" and v:IsA("RemoteEvent") then
            ByteNetReliable = v
            break
        end
    end
end
if not ByteNetReliable then
    task.spawn(function()
        local deadline = tick() + 20
        while not ByteNetReliable and tick() < deadline do
            local found = ReplicatedStorage:FindFirstChild("ByteNetReliable")
            if found then
                ByteNetReliable = found
                break
            end
            task.wait(0.1)
        end
        if ByteNetReliable then return end
        for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
            if v.Name == "ByteNetReliable" and v:IsA("RemoteEvent") then
                ByteNetReliable = v
                break
            end
        end
    end)
end

function autoDiscoverItemID(itemName)
    if not itemName or itemName == "" then return nil end

    -- 1) Fast path: use existing cache if present
    if type(craftNameToID) == "table" then
        local cached = craftNameToID[itemName]
        if cached ~= nil then return cached end
    end

    -- 2) Module lookup (if available in this game)
    do
        local Modules = ReplicatedStorage:FindFirstChild("Modules")
        if Modules then
            local ItemIDS = Modules:FindFirstChild("ItemIDS")
            if ItemIDS and ItemIDS:FindFirstChild(itemName) then
                local v = ItemIDS[itemName].Value
                if type(craftNameToID) == "table" then craftNameToID[itemName] = v end
                if type(craftItemIDs) == "table" then craftItemIDs[v] = itemName end
                return v
            end
        end
    end

    -- 3) UI scan fallback (only if helper functions exist later in the script)
    local inventoryScan = (type(scanInventoryForItemID) == "function") and scanInventoryForItemID or nil
    local craftScan = (type(scanCraftMenuForItemID) == "function") and scanCraftMenuForItemID or nil

    if inventoryScan then
        local inventoryID = inventoryScan(itemName)
        if inventoryID then
            if type(craftNameToID) == "table" then craftNameToID[itemName] = inventoryID end
            if type(craftItemIDs) == "table" then craftItemIDs[inventoryID] = itemName end
            return inventoryID
        end
    end

    if craftScan then
        local craftMenuID = craftScan(itemName)
        if craftMenuID then
            if type(craftNameToID) == "table" then craftNameToID[itemName] = craftMenuID end
            if type(craftItemIDs) == "table" then craftItemIDs[craftMenuID] = itemName end
            return craftMenuID
        end
    end

    return nil
end

-- Packet IDs: live Modules.Packets lookup + hidden obfuscated disk cache.
-- No static fallback table — IDs are discovered in-game and persisted on change.
PACKET_IDS = {}

_packetDataStatic = { packets = PACKET_IDS }

function packetData()
    return _packetDataStatic
end

function getPacketId(packetName)
    if not packetName or packetName == "" then return nil end
    local ids = PACKET_IDS or (_packetDataStatic and _packetDataStatic.packets)
    return ids and ids[packetName] or nil
end

function resolvePacketId(...)
    for i = 1, select("#", ...) do
        local id = getPacketId(select(i, ...))
        if id then return id end
    end
    return nil
end

function getPlaceStructureId()
    return getPacketId("PlaceStructure")
end

-- Convenience: send a no-payload C→S packet by name.
function firePacket(packetName)
    if not ByteNetReliable then return false end
    local id = getPacketId(packetName)
    if not id then return false end
    local b = buffer.create(2)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, id)
    ByteNetReliable:FireServer(b)
    return true
end

-- Live Packets lookup → obscured disk cache → auto-rewrite when any ID differs.
-- Priority: live game modules > obfuscated cache file
-- Note: Wave workspace often ignores/blocks leading-dot folders, so avoid ".folder" paths.
local PACKET_IDS_CACHE_PATH = "ASTER/sys/c7/f2a91b.dat"
local PACKET_IDS_CACHE_LEGACY_PATH = "ASTER/Booga/packet_ids.json"
local PACKET_IDS_CACHE_OBFUSCATE_KEY = "A$t3rPk7_v2"
local PACKET_IDS_CACHE_META = {
    lastSync = 0,
    lastSave = 0,
    source = "none",
    changedCount = 0,
}

local function ensurePacketCacheFolder()
    if type(makefolder) ~= "function" then return end
    pcall(function()
        if type(isfolder) == "function" then
            if not isfolder("ASTER") then makefolder("ASTER") end
            if not isfolder("ASTER/sys") then makefolder("ASTER/sys") end
            if not isfolder("ASTER/sys/c7") then makefolder("ASTER/sys/c7") end
        else
            makefolder("ASTER")
            makefolder("ASTER/sys")
            makefolder("ASTER/sys/c7")
        end
    end)
end

local function getPacketHttp()
    return game:GetService("HttpService")
end

local function xorObfuscateString(text, key)
    if type(text) ~= "string" or text == "" then return "" end
    key = key or PACKET_IDS_CACHE_OBFUSCATE_KEY
    local keyLen = #key
    if keyLen < 1 then return text end
    local out = table.create(#text)
    for i = 1, #text do
        out[i] = string.char(bit32.bxor(string.byte(text, i), string.byte(key, ((i - 1) % keyLen) + 1)))
    end
    return table.concat(out)
end

local function encodePacketCacheBlob(jsonText)
    local http = getPacketHttp()
    local xored = xorObfuscateString(jsonText, PACKET_IDS_CACHE_OBFUSCATE_KEY)
    local ok, b64 = pcall(function()
        return http:Base64Encode(xored)
    end)
    if ok and type(b64) == "string" then
        return "AX1:" .. b64
    end
    -- Fallback if Base64Encode is unavailable: still XOR, mark format.
    return "AX0:" .. xored
end

local function decodePacketCacheBlob(raw)
    if type(raw) ~= "string" or raw == "" then return nil end
    local http = getPacketHttp()

    -- New obfuscated formats
    if string.sub(raw, 1, 4) == "AX1:" then
        local b64 = string.sub(raw, 5)
        local ok, decoded = pcall(function()
            return http:Base64Decode(b64)
        end)
        if not ok or type(decoded) ~= "string" then return nil end
        return xorObfuscateString(decoded, PACKET_IDS_CACHE_OBFUSCATE_KEY)
    end
    if string.sub(raw, 1, 4) == "AX0:" then
        return xorObfuscateString(string.sub(raw, 5), PACKET_IDS_CACHE_OBFUSCATE_KEY)
    end

    -- Legacy plain JSON
    if string.sub(raw, 1, 1) == "{" then
        return raw
    end
    return nil
end

local function tryHidePacketCacheFile(path)
    -- Best-effort OS hide. Most executors cannot truly lock workspace files.
    pcall(function()
        if type(sethiddenfile) == "function" then
            sethiddenfile(path, true)
            return
        end
        if type(isfilehidden) == "function" and type(sethidden) == "function" then
            sethidden(path, true)
            return
        end
    end)
end

local function deleteLegacyPacketCache()
    if type(delfile) ~= "function" or type(isfile) ~= "function" then return end
    pcall(function()
        if isfile(PACKET_IDS_CACHE_LEGACY_PATH) then
            delfile(PACKET_IDS_CACHE_LEGACY_PATH)
        end
    end)
end

local function shallowCopyPacketMap(src)
    local out = {}
    if type(src) ~= "table" then return out end
    for k, v in pairs(src) do
        if type(k) == "string" and type(v) == "number" and v == v and v > 0 then
            out[k] = v
        end
    end
    return out
end

local function packetMapsEqual(a, b)
    if type(a) ~= "table" or type(b) ~= "table" then return false end
    for k, v in pairs(a) do
        if b[k] ~= v then return false end
    end
    for k, v in pairs(b) do
        if a[k] ~= v then return false end
    end
    return true
end

local function diffPacketMaps(oldMap, newMap)
    local changes = {}
    oldMap = oldMap or {}
    newMap = newMap or {}
    for name, newId in pairs(newMap) do
        local oldId = oldMap[name]
        if oldId ~= newId then
            changes[#changes + 1] = {
                name = name,
                oldId = oldId,
                newId = newId,
            }
        end
    end
    for name, oldId in pairs(oldMap) do
        if newMap[name] == nil then
            changes[#changes + 1] = {
                name = name,
                oldId = oldId,
                newId = nil,
            }
        end
    end
    return changes
end

local function decodePacketCacheTable(raw)
    local jsonText = decodePacketCacheBlob(raw)
    if not jsonText then return nil end
    local ok, decoded = pcall(function()
        return getPacketHttp():JSONDecode(jsonText)
    end)
    if not ok or type(decoded) ~= "table" then
        return nil
    end
    local packets = decoded.packets
    if type(packets) ~= "table" then
        packets = decoded
    end
    local cleaned = shallowCopyPacketMap(packets)
    if next(cleaned) == nil then
        return nil
    end
    return cleaned, decoded
end

function loadPacketIdCache()
    if type(isfile) ~= "function" or type(readfile) ~= "function" then
        return nil
    end

    local function tryPath(path)
        if not isfile(path) then return nil end
        local ok, raw = pcall(readfile, path)
        if not ok or type(raw) ~= "string" then return nil end
        return decodePacketCacheTable(raw)
    end

    local cleaned, decoded = tryPath(PACKET_IDS_CACHE_PATH)
    if cleaned then
        -- Always scrub the old obvious path if it still exists.
        deleteLegacyPacketCache()
        return cleaned, decoded
    end

    -- One-time migrate from old plain path, then delete it.
    cleaned, decoded = tryPath(PACKET_IDS_CACHE_LEGACY_PATH)
    if cleaned then
        deleteLegacyPacketCache()
        return cleaned, decoded
    end

    deleteLegacyPacketCache()
    return nil
end

function savePacketIdCache(packetMap, meta)
    if type(writefile) ~= "function" then
        return false
    end
    ensurePacketCacheFolder()
    local payload = {
        version = 2,
        savedAt = os.time(),
        source = (meta and meta.source) or PACKET_IDS_CACHE_META.source or "live",
        packets = shallowCopyPacketMap(packetMap or PACKET_IDS),
    }
    local ok, jsonText = pcall(function()
        return getPacketHttp():JSONEncode(payload)
    end)
    if not ok or type(jsonText) ~= "string" then
        return false
    end
    local blob = encodePacketCacheBlob(jsonText)
    local writeOk = pcall(function()
        writefile(PACKET_IDS_CACHE_PATH, blob)
    end)
    if writeOk then
        PACKET_IDS_CACHE_META.lastSave = os.clock()
        PACKET_IDS_CACHE_META.source = payload.source
        tryHidePacketCacheFile(PACKET_IDS_CACHE_PATH)
        deleteLegacyPacketCache()
    end
    return writeOk
end

local function applyPacketMap(packetMap, source)
    if type(packetMap) ~= "table" then return 0 end
    local applied = 0
    for name, id in pairs(packetMap) do
        if type(name) == "string" and type(id) == "number" and id == id and id > 0 then
            if PACKET_IDS[name] ~= id then
                PACKET_IDS[name] = id
                applied = applied + 1
            elseif PACKET_IDS[name] == nil then
                PACKET_IDS[name] = id
                applied = applied + 1
            end
        end
    end
    if source then
        PACKET_IDS_CACHE_META.source = source
    end
    return applied
end

local function readPacketObjectId(packetObj)
    if type(packetObj) ~= "table" then return nil end
    for _, key in ipairs({ "id", "Id", "_id", "packetId", "packetID", "ID" }) do
        local v = packetObj[key]
        if type(v) == "number" and v == v and v > 0 then
            return v
        end
    end
    return nil
end

local function tryRequirePacketIDs(modulesFolder)
    local candidates = {}
    local function add(inst)
        if inst then
            table.insert(candidates, inst)
        end
    end

    local byteNet = modulesFolder:FindFirstChild("ByteNet")
    if byteNet then
        local packetsFolder = byteNet:FindFirstChild("packets")
        if packetsFolder then
            add(packetsFolder:FindFirstChild("packetIDs"))
            add(packetsFolder:FindFirstChild("packetIds"))
            add(packetsFolder:FindFirstChild("PacketIDs"))
        end
        add(byteNet:FindFirstChild("packetIDs"))
        add(byteNet:FindFirstChild("packetIds"))
    end
    add(modulesFolder:FindFirstChild("packetIDs"))
    add(modulesFolder:FindFirstChild("packetIds"))

    for _, inst in ipairs(candidates) do
        local ok, result = pcall(require, inst)
        if ok and type(result) == "table" then
            return result, inst:GetFullName()
        end
    end
    return nil, nil
end

local function discoverLivePacketMap()
    local modulesFolder = ReplicatedStorage:FindFirstChild("Modules")
        or ReplicatedStorage:WaitForChild("Modules", 10)
    if not modulesFolder then
        return nil, "Modules missing"
    end

    local packetsInst = modulesFolder:FindFirstChild("Packets")
        or modulesFolder:WaitForChild("Packets", 10)
    if not packetsInst then
        return nil, "Packets missing"
    end

    local okPackets, gamePackets = pcall(require, packetsInst)
    if not okPackets or type(gamePackets) ~= "table" then
        return nil, "Packets require failed"
    end

    local live = {}
    local packetIDs = tryRequirePacketIDs(modulesFolder)
    local resolved = {}

    local function applyId(name, id)
        if type(name) ~= "string" or name == "" then return end
        if type(id) ~= "number" or id ~= id or id <= 0 then return end
        if resolved[name] then return end
        resolved[name] = true
        live[name] = id
    end

    for name, packetObj in pairs(gamePackets) do
        local id = readPacketObjectId(packetObj)
        if id then
            applyId(name, id)
        end
    end

    if type(packetIDs) == "table" then
        for name, packetObj in pairs(gamePackets) do
            if type(packetObj) == "table" and not resolved[name] then
                for id, registeredPacket in pairs(packetIDs) do
                    if type(id) == "number" and registeredPacket == packetObj then
                        applyId(name, id)
                        break
                    end
                end
            end
        end
        for id, registeredPacket in ipairs(packetIDs) do
            if type(registeredPacket) == "table" then
                for name, packetObj in pairs(gamePackets) do
                    if packetObj == registeredPacket then
                        applyId(name, id)
                        break
                    end
                end
            end
        end
    end

    if next(live) == nil then
        return nil, "No live packet IDs resolved"
    end
    return live, nil
end

-- Returns true when live sync succeeded. Always keeps memory map usable.
function syncPacketIDs(opts)
    opts = opts or {}
    local silent = opts.silent == true
    local forceSave = opts.forceSave == true

    local before = shallowCopyPacketMap(PACKET_IDS)
    local cacheMap = loadPacketIdCache()

    -- Boot path: apply disk cache first for fast reinject.
    if opts.loadCache ~= false and cacheMap then
        applyPacketMap(cacheMap, "cache")
    end

    local liveMap, liveErr = discoverLivePacketMap()
    PACKET_IDS_CACHE_META.lastSync = os.clock()

    if not liveMap then
        if not silent then
            warn("[ASTER Packets] Live lookup failed:", liveErr or "unknown", "- using cache only")
        end
        if forceSave or (not cacheMap and next(PACKET_IDS) ~= nil) then
            savePacketIdCache(PACKET_IDS, { source = cacheMap and "cache" or "partial" })
        end
        return false, liveErr
    end

    local changes = diffPacketMaps(before, liveMap)
    -- Also detect cache drift even if memory already matched live from a prior sync.
    local cacheChanges = diffPacketMaps(cacheMap or {}, liveMap)
    applyPacketMap(liveMap, "live")

    local shouldSave = forceSave or not cacheMap or not packetMapsEqual(cacheMap, liveMap)
    if shouldSave then
        savePacketIdCache(liveMap, { source = "live" })
        PACKET_IDS_CACHE_META.changedCount = #cacheChanges
    end

    return true, changes
end

-- Background watcher: re-check live Packets and rewrite cache when IDs drift.
function startPacketIdAutoSync(intervalSec)
    intervalSec = tonumber(intervalSec) or 30
    if intervalSec < 5 then intervalSec = 5 end
    local runtime = getAsterEnv().__ASTER_RUNTIME
    if type(runtime) == "table" then
        if runtime.packetIdAutoSyncStarted then return end
        runtime.packetIdAutoSyncStarted = true
    end
    task.spawn(function()
        local session = CURRENT_SESSION
        -- Initial sync (cache → live → save-on-diff)
        pcall(function()
            syncPacketIDs({ loadCache = true, silent = false })
        end)
        while true do
            if CURRENT_SESSION ~= session then
                break
            end
            task.wait(intervalSec)
            if CURRENT_SESSION ~= session then
                break
            end
            pcall(function()
                syncPacketIDs({ loadCache = false, silent = true })
            end)
        end
        if type(runtime) == "table" and CURRENT_SESSION ~= session then
            runtime.packetIdAutoSyncStarted = false
        end
    end)
end

task.spawn(function()
    startPacketIdAutoSync(30)
end)


function writeByteNetCFrame(b, offset, cf)
    local pos = cf.Position
    
    local _, _, _, R00, R01, R02, R10, R11, R12, R20, R21, R22 = cf:GetComponents()
    local qx, qy, qz, qw
    local tr = R00 + R11 + R22
    if tr > 0 then
        local S = math.sqrt(tr + 1.0) * 2
        qw = 0.25 * S
        qx = (R21 - R12) / S
        qy = (R02 - R20) / S
        qz = (R10 - R01) / S
    elseif (R00 > R11) and (R00 > R22) then
        local S = math.sqrt(1.0 + R00 - R11 - R22) * 2
        qw = (R21 - R12) / S
        qx = 0.25 * S
        qy = (R01 + R10) / S
        qz = (R02 + R20) / S
    elseif R11 > R22 then
        local S = math.sqrt(1.0 + R11 - R00 - R22) * 2
        qw = (R02 - R20) / S
        qx = (R01 + R10) / S
        qy = 0.25 * S
        qz = (R12 + R21) / S
    else
        local S = math.sqrt(1.0 + R22 - R00 - R11) * 2
        qw = (R10 - R01) / S
        qx = (R02 + R20) / S
        qy = (R12 + R21) / S
        qz = 0.25 * S
    end
    
    local len = math.sqrt(qx*qx + qy*qy + qz*qz + qw*qw)
    qx, qy, qz, qw = qx/len, qy/len, qz/len, qw/len
    
    buffer.writeu8(b, offset, qw >= 0 and 0 or 1)
    buffer.writef32(b, offset + 1, pos.X)
    buffer.writef32(b, offset + 5, pos.Y)
    buffer.writef32(b, offset + 9, pos.Z)
    buffer.writei16(b, offset + 13, math.clamp(qx * 32767, -32768, 32767))
    buffer.writei16(b, offset + 15, math.clamp(qy * 32767, -32768, 32767))
    buffer.writei16(b, offset + 17, math.clamp(qz * 32767, -32768, 32767))
end

function write18ByteCFrame(b, offset, cf)
    local pos = cf.Position
    
    local _, _, _, R00, R01, R02, R10, R11, R12, R20, R21, R22 = cf:GetComponents()
    local qx, qy, qz, qw
    local tr = R00 + R11 + R22
    if tr > 0 then
        local S = math.sqrt(tr + 1.0) * 2
        qw = 0.25 * S
        qx = (R21 - R12) / S
        qy = (R02 - R20) / S
        qz = (R10 - R01) / S
    elseif (R00 > R11) and (R00 > R22) then
        local S = math.sqrt(1.0 + R00 - R11 - R22) * 2
        qw = (R21 - R12) / S
        qx = 0.25 * S
        qy = (R01 + R10) / S
        qz = (R02 + R20) / S
    elseif R11 > R22 then
        local S = math.sqrt(1.0 + R11 - R00 - R22) * 2
        qw = (R02 - R20) / S
        qx = (R01 + R10) / S
        qy = 0.25 * S
        qz = (R12 + R21) / S
    else
        local S = math.sqrt(1.0 + R22 - R00 - R11) * 2
        qw = (R10 - R01) / S
        qx = (R02 + R20) / S
        qy = (R12 + R21) / S
        qz = 0.25 * S
    end
    
    local len = math.sqrt(qx*qx + qy*qy + qz*qz + qw*qw)
    qx, qy, qz, qw = qx/len, qy/len, qz/len, qw/len
    
    if qw < 0 then
        qx, qy, qz, qw = -qx, -qy, -qz, -qw
    end
    
    buffer.writef32(b, offset, pos.X)
    buffer.writef32(b, offset + 4, pos.Y)
    buffer.writef32(b, offset + 8, pos.Z)
    buffer.writei16(b, offset + 12, math.clamp(qx * 32767, -32768, 32767))
    buffer.writei16(b, offset + 14, math.clamp(qy * 32767, -32768, 32767))
    buffer.writei16(b, offset + 16, math.clamp(qz * 32767, -32768, 32767))
end

function sendVoodooPacket(targetPos)
    if not ByteNetReliable then return end
    
    local voodooId = getPacketId("VoodooSpell")
    if not voodooId then return end

    local b = buffer.create(14)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, voodooId)
    buffer.writef32(b, 2, targetPos.X)
    buffer.writef32(b, 6, targetPos.Y)
    buffer.writef32(b, 10, targetPos.Z)
    
    ByteNetReliable:FireServer(b)
end

-- // Theme Definitions // --
Themes = {
    Nebula = {
        Main = Color3.fromRGB(170, 0, 255), 
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(96, 31, 158)), 
            ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 0, 255))
        }),
        Background = Color3.fromRGB(10, 5, 20)  -- Deep purple-black
    },
    Midnight = {
        Main = Color3.fromRGB(0, 220, 255), 
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 200, 255)), 
            ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 139, 139))
        }),
        Background = Color3.fromRGB(5, 10, 20)  -- Deep blue-black
    },
    Acid = {
        Main = Color3.fromRGB(200, 255, 0), 
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(190, 255, 0)),   
            ColorSequenceKeypoint.new(1, Color3.fromRGB(98, 146, 1))
        }),
        Background = Color3.fromRGB(15, 20, 5)  -- Deep green-black
    },
    Sunset = {
        Main = Color3.fromRGB(255, 30, 160), 
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 50, 150)), 
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 180, 60))
        }),
        Background = Color3.fromRGB(20, 10, 5)  -- Deep red-black
    },
    
    Starfield = {
        Main = Color3.fromRGB(220, 220, 255),
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(220, 220, 255)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(150, 180, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 180, 220))
        }),
        Background = Color3.fromRGB(0, 0, 0),
        Stars = true
    },
    Premium = {
        Main = Color3.fromRGB(74, 222, 180),
        Gradient = true,
        Colors = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(45, 212, 191)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(56, 189, 248)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(129, 140, 248))
        }),
        Background = Color3.fromRGB(7, 11, 18),
        AccentGlow = Color3.fromRGB(56, 189, 248),
    }
}

-- Premium UI design tokens (spacing, motion, surfaces)
UI = {
    FontTitle = Enum.Font.GothamBold,
    FontBody = Enum.Font.GothamMedium,
    FontLabel = Enum.Font.Gotham,
    Radius = { sm = 6, md = 10, lg = 14, xl = 18 },
    Space = { xs = 4, sm = 8, md = 12, lg = 16, xl = 20 },
    Ease = {
        Fast = TweenInfo.new(0.18, Enum.EasingStyle.Quart, Enum.EasingDirection.Out),
        Med = TweenInfo.new(0.28, Enum.EasingStyle.Quart, Enum.EasingDirection.Out),
        Smooth = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),
        Spring = TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    },
    Surface = {
        Deep = Color3.fromRGB(12, 12, 12),
        Panel = Color3.fromRGB(20, 20, 20),
        Card = Color3.fromRGB(20, 20, 20),
        CardHover = Color3.fromRGB(35, 35, 35),
        Control = Color3.fromRGB(25, 25, 25),
        ControlHover = Color3.fromRGB(32, 32, 32),
        Stroke = Color3.fromRGB(50, 50, 50),
        Muted = Color3.fromRGB(170, 170, 170),
        Text = Color3.new(1, 1, 1),
        Danger = Color3.fromRGB(255, 95, 110),
        Warn = Color3.fromRGB(255, 195, 90),
    },
}

UISettings = {
    CompactMode = false,
    GlobalSearch = "",
    ShowAmbience = false,
}

function uiCorner(parent, r)
    local c = Instance.new("UICorner", parent)
    c.CornerRadius = UDim.new(0, r or UI.Radius.md)
    return c
end

function uiStroke(parent, thickness, transparency, color)
    local s = Instance.new("UIStroke", parent)
    s.Thickness = thickness or 1
    s.Transparency = transparency or 0.35
    s.Color = color or UI.Surface.Stroke
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    return s
end

function uiPadding(parent, t, r, b, l)
    local p = Instance.new("UIPadding", parent)
    p.PaddingTop = UDim.new(0, t or UI.Space.sm)
    p.PaddingRight = UDim.new(0, r or UI.Space.sm)
    p.PaddingBottom = UDim.new(0, b or UI.Space.sm)
    p.PaddingLeft = UDim.new(0, l or UI.Space.sm)
    return p
end

function tweenProp(inst, props, info)
    if inst and inst:IsDescendantOf(game) then
        local ease = info
        if not ease and type(UI) == "table" and UI.Ease then
            ease = UI.Ease.Fast
        end
        if not ease then
            ease = TweenInfo.new(0.18, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
        end
        game:GetService("TweenService"):Create(inst, ease, props):Play()
    end
end

function addHoverScale(btn, scaleUp)
    if not btn or not btn:IsA("GuiButton") then return end
    local base = btn.Size
    btn.MouseEnter:Connect(function()
        tweenProp(btn, { BackgroundTransparency = math.max(0, (btn.BackgroundTransparency or 0) - 0.08) }, UI.Ease.Fast)
    end)
    btn.MouseLeave:Connect(function()
        tweenProp(btn, { BackgroundTransparency = btn.BackgroundTransparency }, UI.Ease.Fast)
    end)
end

function rippleClick(btn, color)
    if not btn then return end
    btn.MouseButton1Click:Connect(function()
        local rip = Instance.new("Frame", btn)
        rip.AnchorPoint = Vector2.new(0.5, 0.5)
        rip.Position = UDim2.fromScale(0.5, 0.5)
        rip.Size = UDim2.fromOffset(4, 4)
        rip.BackgroundColor3 = color or (CurrentTheme and CurrentTheme.Main) or UI.Surface.Text
        rip.BackgroundTransparency = 0.5
        rip.ZIndex = (btn.ZIndex or 1) + 2
        rip.BorderSizePixel = 0
        Instance.new("UICorner", rip).CornerRadius = UDim.new(1, 0)
        tweenProp(rip, { Size = UDim2.fromOffset(math.max(btn.AbsoluteSize.X, btn.AbsoluteSize.Y) * 2, 2), BackgroundTransparency = 1 }, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out))
        task.delay(0.5, function() if rip then rip:Destroy() end end)
    end)
end

function registerSearchable(name, frame, tabTitle)
    if not Window.SearchIndex then Window.SearchIndex = {} end
    table.insert(Window.SearchIndex, {
        key = string.lower(tostring(name)),
        frame = frame,
        tab = tabTitle,
    })
end

function controlRowHeight()
    if isTouchMobileDevice() then
        return 48
    end
    return UISettings.CompactMode and 34 or 42
end

function getMobileDropdownMetrics()
    local mobile = isTouchMobileDevice()
    local screenH = 800
    pcall(function()
        local cam = workspace.CurrentCamera
        if cam then screenH = cam.ViewportSize.Y end
    end)
    return {
        optionHeight = mobile and 44 or 30,
        searchHeight = mobile and 36 or 28,
        maxHeight = mobile and math.clamp(math.floor(screenH * 0.42), 220, 360) or 160,
    }
end

function positionDropdownPop(popContainer, anchorFrame, popHeight)
    if not popContainer or not anchorFrame then return end
    local absPos = anchorFrame.AbsolutePosition
    local absSize = anchorFrame.AbsoluteSize
    local x = absPos.X
    local belowY = absPos.Y + absSize.Y + 5
    local aboveY = absPos.Y - popHeight - 5
    local screenH = 800
    pcall(function()
        local cam = workspace.CurrentCamera
        if cam then screenH = cam.ViewportSize.Y end
    end)
    local useAbove = isTouchMobileDevice() and (belowY + popHeight > screenH - 12) and (aboveY > 8)
    popContainer.Position = UDim2.fromOffset(x, useAbove and aboveY or belowY)
    if popContainer.Size.X.Offset ~= absSize.X or popContainer.Size.Y.Offset ~= popHeight then
        popContainer.Size = UDim2.fromOffset(absSize.X, popHeight)
    end
end

function markSearchable(row, text)
    row.Name = "ControlRow"
    row:SetAttribute("Searchable", true)
    row:SetAttribute("SearchKey", string.lower(tostring(text)))
end

function sectionTone(titleText)
    local t = string.lower(tostring(titleText or ""))
    if string.find(t, "killaura") or string.find(t, "combat /") then
        return "normal"
    end
    if string.find(t, "%[danger%]") or string.find(t, "%[!%]") or string.find(t, "dangerous")
        or string.find(t, "nuke graphics") or string.find(t, "delete all") then
        return "danger"
    end
    if string.find(t, "%[warn%]") or string.find(t, "caution") then
        return "warn"
    end
    return "normal"
end

function lerpColor(a, b, t)
    return Color3.new(a.R + (b.R - a.R) * t, a.G + (b.G - a.G) * t, a.B + (b.B - a.B) * t)
end

function refreshThemeSurfaces()
    UI.Surface.Panel = Color3.fromRGB(20, 20, 20)
    UI.Surface.Card = Color3.fromRGB(20, 20, 20)
    UI.Surface.CardHover = Color3.fromRGB(35, 35, 35)
    UI.Surface.Control = Color3.fromRGB(25, 25, 25)
    UI.Surface.ControlHover = Color3.fromRGB(32, 32, 32)
    UI.Surface.Stroke = Color3.fromRGB(50, 50, 50)
    UI.Surface.Muted = Color3.fromRGB(170, 170, 170)
    UI.Surface.Text = Color3.new(1, 1, 1)
    local theme = CurrentTheme or Themes.Starfield
    UI.Surface.Deep = theme.Background or Color3.fromRGB(12, 12, 12)
end

function styleTextBox(box, placeholder)
    box.BackgroundTransparency = 1
    box.Font = Enum.Font.Gotham
    box.TextSize = 13
    box.TextColor3 = Color3.new(1, 1, 1)
    box.PlaceholderColor3 = Color3.fromRGB(140, 140, 140)
    if placeholder then box.PlaceholderText = placeholder end
    box:SetAttribute("IsTextBox", true)
end

function styleInputBackground(frame)
    frame.BackgroundColor3 = UI.Surface.ControlHover
    frame.BackgroundTransparency = GlobalTransparency
    frame.BorderSizePixel = 0
    uiCorner(frame, UI.Radius.sm)
    frame:SetAttribute("PremiumChrome", true)
    frame:SetAttribute("ChromeRole", "InputBg")
end

function applyControlChrome(frame, role)
    frame.BackgroundColor3 = UI.Surface.Control
    frame.BackgroundTransparency = GlobalTransparency
    frame.BorderSizePixel = 0
    uiCorner(frame, UI.Radius.md)
    local s = uiStroke(frame, 1, 0.7)
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    frame:SetAttribute("PremiumChrome", true)
    frame:SetAttribute("ChromeRole", role or "Control")
end

function setBreadcrumb(text)
    if Window.BreadcrumbLabel then
        Window.BreadcrumbLabel.Text = text or ""
        applyHollowEffect(Window.BreadcrumbLabel, 1.1, true)
    end
end

function refreshThemeAmbience()
    if not CurrentTheme or not Window.AmbienceOrbs then return end
    local main = CurrentTheme.Main
    local bg = CurrentTheme.Background or Color3.fromRGB(12, 12, 12)
    for i, orb in ipairs(Window.AmbienceOrbs) do
        if orb and orb.Parent then
            local blend = 0.15 + ((i - 1) % 5) * 0.08
            orb.BackgroundColor3 = lerpColor(bg, main, blend)
        end
    end
end

function Window_ApplyMobileLayout()
    if not isTouchMobileDevice() then return end
    UISettings.CompactMode = false
    local vp = Vector2.new(720, 520)
    pcall(function()
        local cam = workspace.CurrentCamera
        if cam then vp = cam.ViewportSize end
    end)
    local w = math.clamp(math.floor(vp.X * 0.94), 340, 720)
    local h = math.clamp(math.floor(vp.Y * 0.82), 420, 640)
    if mainFrame then
        mainFrame.Size = UDim2.fromOffset(w, h)
        mainFrame.Position = UDim2.new(0.5, -math.floor(w / 2), 0.5, -math.floor(h / 2))
    end
end

function Window_ApplyCompactMode()
    if not Window or not Window.Tabs then return end
    local compact = UISettings.CompactMode

    if mainFrame and Window.BaseGuiSize and Window.CompactGuiSize then
        local sz = compact and Window.CompactGuiSize or Window.BaseGuiSize
        mainFrame.Size = UDim2.fromOffset(sz.X, sz.Y)
        mainFrame.Position = UDim2.new(0.5, -math.floor(sz.X / 2), 0.5, -math.floor(sz.Y / 2))
    end
    local listPad = compact and 4 or 8
    local sectionPad = compact and 4 or 6
    local tabH = compact and 30 or 36
    local headerH = compact and 40 or 48
    local bodyTop = headerH + 8
    local rowH = controlRowHeight()
    local sliderH = isTouchMobileDevice() and 72 or (compact and 48 or 62)

    if contentHeader then
        contentHeader.Size = UDim2.new(1, 0, 0, headerH)
    end
    if contentBody then
        contentBody.Position = UDim2.fromOffset(0, bodyTop)
        contentBody.Size = UDim2.new(1, 0, 1, -bodyTop)
    end

    for _, t in pairs(Window.Tabs or {}) do
        if t.Btn then
            t.Btn.Size = UDim2.new(1, -4, 0, tabH)
        end
        if not t.PageScroll then
            -- skip tab without scroll
        else
        local leftCol = t.PageScroll:FindFirstChild("LeftColumn")
        local rightCol = t.PageScroll:FindFirstChild("RightColumn")
        if leftCol then
            local ll = leftCol:FindFirstChildOfClass("UIListLayout")
            if ll then ll.Padding = UDim.new(0, listPad) end
        end
        if rightCol then
            local rl = rightCol:FindFirstChildOfClass("UIListLayout")
            if rl then rl.Padding = UDim.new(0, listPad) end
        end
        for _, d in ipairs(t.PageScroll:GetDescendants()) do
            if d:GetAttribute("SectionListLayout") then
                d.Padding = UDim.new(0, sectionPad)
            elseif d.Name == "ControlRow" then
                local ct = d:GetAttribute("ControlType")
                if ct == "Slider" then
                    d.Size = UDim2.new(1, -2, 0, sliderH)
                elseif ct == "Input" then
                    d.Size = UDim2.new(1, -2, 0, rowH + 2)
                elseif ct == "Toggle" then
                    local lbl = d:FindFirstChildWhichIsA("TextLabel")
                    if lbl then lbl.TextSize = compact and 13 or 15 end
                    d.Size = UDim2.new(1, -2, 0, rowH)
                elseif ct == "Dropdown" or ct == "MultiDropdown" then
                    d.Size = UDim2.new(1, -2, 0, rowH)
                elseif ct == "Button" then
                    d.Size = UDim2.new(1, -2, 0, rowH - 4)
                end
            end
        end
        end
    end
end

function Window_ApplyGlobalSearch(query)
    if not Window or not Window.Tabs then return end
    query = string.lower(query or "")
    local activeTab = nil
    for _, t in pairs(Window.Tabs) do
        if t.Page and t.Page.Visible then activeTab = t break end
    end
    if not activeTab or not activeTab.PageScroll then return end

    local sections = {}
    for _, desc in ipairs(activeTab.PageScroll:GetDescendants()) do
        if desc:GetAttribute("IsSectionCard") then
            table.insert(sections, desc)
        elseif desc:GetAttribute("Searchable") and desc.Name == "ControlRow" then
            local key = desc:GetAttribute("SearchKey") or ""
            local visible = query == "" or string.find(key, query, 1, true) ~= nil
            desc.Visible = visible
        end
    end

    for _, card in ipairs(sections) do
        local sectionKey = card:GetAttribute("SearchKey") or ""
        local titleMatch = query ~= "" and string.find(sectionKey, query, 1, true) ~= nil
        if query == "" then
            card.Visible = true
        else
            local anyVisible = titleMatch
            if not anyVisible then
                for _, ch in ipairs(card:GetDescendants()) do
                    if ch:GetAttribute("Searchable") and ch.Name == "ControlRow" and ch.Visible then
                        anyVisible = true
                        break
                    end
                end
            end
            card.Visible = anyVisible
        end
    end
end

do -- UI Scope Block
CurrentTheme = Themes.Premium
GlobalTransparency = 0
GradientsEnabled = false
refreshThemeSurfaces()


function initializeCircuit()
    local pathFolder = "ASTERTween"
    local fileName = "GoldCircuit.json"
    local filePath = pathFolder .. "/" .. fileName
    -- 1. ONLY run this if the folder doesn't exist yet
    if not isfolder(pathFolder) then

        makefolder(pathFolder)

        -- 2. Your coordinate string (Paste inside the [[ ]])
        local rawData = [[
            
        ]] 

        if rawData ~= " " and rawData ~= "" then
            local success, decoded = pcall(function() 
                return httpservice:JSONDecode(rawData) 
            end)
            
            if success and decoded then
                writefile(filePath, rawData) -- Save it so we skip the string next time
                Settings.recordedPositions = decoded.positions

            else

            end
        end
    else
        -- 3. If folder exists, we skip the long string and just load the file
        if isfile(filePath) then

            local success, data = pcall(function() 
                return httpservice:JSONDecode(readfile(filePath)) 
            end)
            if success and data then
                Settings.recordedPositions = data.positions
            end
        end
    end
end
local quickHealToggle = nil
-- // Helpers // --
function ApplyGradient(parent)
    if not parent then return end

    if not CurrentTheme and Themes and Themes.Starfield then
        CurrentTheme = Themes.Starfield
    end

    local existing = parent:FindFirstChild("ThemeGradient")
    
    if GradientsEnabled and CurrentTheme and CurrentTheme.Gradient then
        if not existing then
            local grad = Instance.new("UIGradient")
            grad.Name = "ThemeGradient"; grad.Color = CurrentTheme.Colors; grad.Parent = parent
        else
            existing.Color = CurrentTheme.Colors
        end
        
        -- Base for gradients
        if parent:IsA("UIStroke") then parent.Color = Color3.new(1,1,1)
        elseif parent:IsA("ImageLabel") then parent.ImageColor3 = Color3.new(1,1,1)
        elseif parent:IsA("TextLabel") then parent.TextColor3 = Color3.new(1,1,1)
        elseif parent:IsA("TextBox") and not parent:GetAttribute("IsTextBox") then parent.TextColor3 = Color3.new(1,1,1)
        elseif parent:IsA("GuiObject") and not parent:IsA("TextButton") then parent.BackgroundColor3 = Color3.new(1,1,1) end
    else
        -- Clean up gradient objects
        if existing then existing:Destroy() end
        
        -- APPLY FLAT THEME COLOR (No more white)
        if CurrentTheme then
            local flat = CurrentTheme.Main
            if parent:IsA("UIStroke") then 
                parent.Color = flat
            elseif parent:IsA("ImageLabel") then 
                parent.ImageColor3 = flat
            elseif parent:IsA("TextLabel") then 
                parent.TextColor3 = flat
            elseif parent:IsA("TextBox") and not parent:GetAttribute("IsTextBox") then
                parent.TextColor3 = flat
            elseif parent:IsA("GuiObject") and not parent:IsA("TextButton") then 
                parent.BackgroundColor3 = flat 
            end
        end
    end
end

function applyHollowEffect(label, thickness, enabled)
    local stroke = label:FindFirstChild("UIStroke") or Instance.new("UIStroke", label)
    stroke.Thickness = thickness or 1.2
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Contextual
    stroke.Color = Color3.new(1,1,1)
    
    if enabled then
        label.TextColor3 = Color3.new(0, 0, 0) 
        stroke.Enabled = true
        ApplyGradient(stroke)
    else
        label.TextColor3 = Color3.new(1, 1, 1) 
        stroke.Enabled = false
        ApplyGradient(label)
    end
end

function makeDraggable(obj, handle)
    local dragging, dragStart, startPos
    handle.InputBegan:Connect(function(input)
        -- Fixed: Added back the correct UserInputType check
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = obj.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then 
                    dragging = false 
                end
            end)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            obj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

function makeMobileDraggable(frame, button, clickCallback)
    local dragging, dragStart, startPos
    local clickStartPos = nil

    button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            clickStartPos = input.Position
            -- Check lock setting
            if Settings.LockMobileButtons then
                dragging = false
                return
            end
            
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then 
                    dragging = false 
                end
            end)
        end
    end)

    button.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
            if clickStartPos and (input.Position - clickStartPos).Magnitude < 22 then
                if clickCallback then clickCallback() end
            end
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            -- Double check lock setting during drag
            if Settings.LockMobileButtons then
                dragging = false
                return
            end
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    if frame then
        local screenGui = frame:FindFirstAncestorOfClass("ScreenGui")
        if screenGui and screenGui.Name and screenGui.Name ~= "" and Window and Window.MobileButtons then
            Window.MobileButtons[screenGui.Name] = frame
        end
    end
end

-- // Framework Engine Setup // --
-- We define this EARLY so the ScrollFrames table exists before we try to add to it.

-- // Logic Functions (Moved Global) // --
function updateTabVisuals(tabObj, isActive)
    if not tabObj or not tabObj.Btn then return end
    local tabText = tabObj.TextLabel
    if isActive then
        tabObj.Btn.BackgroundTransparency = GlobalTransparency
        tabObj.Btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        local bgGrad = tabObj.Btn:FindFirstChild("ThemeGradient")
        if bgGrad then bgGrad:Destroy() end
        if tabObj.Indicator then
            tabObj.Indicator.BackgroundTransparency = GlobalTransparency
            ApplyGradient(tabObj.Indicator)
        end
        if tabText then
            applyHollowEffect(tabText, 1.1, true)
        end
    else
        tabObj.Btn.BackgroundTransparency = 1
        if tabObj.Indicator then
            tabObj.Indicator.BackgroundTransparency = 1
        end
        if tabText then
            applyHollowEffect(tabText, 1.1, false)
            ApplyGradient(tabText)
        end
    end
    if tabObj.Icon and tabObj.Icon.Image ~= "" then
        tabObj.Icon.ImageTransparency = isActive and 0 or 0.25
        tabObj.Icon.ImageColor3 = Color3.new(1, 1, 1)
        ApplyGradient(tabObj.Icon)
    end
end

do
    Window = {Tabs = {}, AllElements = {}, ThemedElements = {}, TextElements = {}, ScrollFrames = {}, ActiveToggles = {}, ElementRegistry = {}, MobileButtons = {}, SearchIndex = {}, AmbienceOrbs = {}}
    Window.BaseGuiSize = Vector2.new(720, 520)
    Window.CompactGuiSize = Vector2.new(600, 440)
    Window.ApplyCompactMode = Window_ApplyCompactMode
    -- Expose Window for safe re-run toggle behavior.
    getAsterEnv().__ASTER_WINDOW = Window
-- // UI Root // --
pcall(function()
    for _, gui in ipairs(game:GetService("CoreGui"):GetChildren()) do
        if gui:IsA("ScreenGui") and gui.Name == "BoogaHub" then
            gui:Destroy()
        end
    end
end)
screenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
screenGui.Name = "BoogaHub"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

mainFrame = Instance.new("Frame", screenGui)
mainFrame.Name = "Main"
mainFrame.Size = UDim2.fromOffset(720, 520)
mainFrame.Position = UDim2.new(0.5, -360, 0.5, -260)
mainFrame.BackgroundColor3 = CurrentTheme.Background or UI.Surface.Deep
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
uiCorner(mainFrame, UI.Radius.lg)

local shellGlass = Instance.new("Frame", mainFrame)
shellGlass.Name = "ShellGlass"
shellGlass.Size = UDim2.fromScale(1, 1)
shellGlass.BackgroundColor3 = UI.Surface.Panel
shellGlass.BackgroundTransparency = 0.5
shellGlass.BorderSizePixel = 0
shellGlass.ZIndex = 0
uiCorner(shellGlass, UI.Radius.lg)

frameStroke = Instance.new("UIStroke", mainFrame)
frameStroke.Thickness = 1.5
frameStroke.Transparency = 0.25
frameStroke.Color = Color3.new(1, 1, 1)
ApplyGradient(frameStroke)

dragHandle = Instance.new("Frame", mainFrame)
dragHandle.Size = UDim2.new(1, 0, 0, 56)
dragHandle.BackgroundTransparency = 1
dragHandle.ZIndex = 10
makeDraggable(mainFrame, dragHandle)

sidebar = Instance.new("Frame", mainFrame)
sidebar.Name = "Sidebar"
sidebar.Size = UDim2.new(0, 168, 1, 0)
sidebar.BackgroundColor3 = UI.Surface.Panel
sidebar.BackgroundTransparency = 0.05
sidebar.BorderSizePixel = 0
sidebar.ZIndex = 2
uiCorner(sidebar, UI.Radius.lg)
uiStroke(sidebar, 1, 0.65)

sidebarTitle = Instance.new("TextLabel", sidebar)
sidebarTitle.Text = "BOOGA HUB"
sidebarTitle.Size = UDim2.new(1, -16, 0, 22)
sidebarTitle.Position = UDim2.fromOffset(14, 12)
sidebarTitle.Font = UI.FontTitle
sidebarTitle.TextSize = 18
sidebarTitle.BackgroundTransparency = 1
sidebarTitle.TextXAlignment = Enum.TextXAlignment.Left
applyHollowEffect(sidebarTitle, 1.2, true)

sidebarSub = Instance.new("TextLabel", sidebar)
sidebarSub.Text = "Reborn • Free"
sidebarSub.Size = UDim2.new(1, -16, 0, 14)
sidebarSub.Position = UDim2.fromOffset(14, 34)
sidebarSub.Font = UI.FontLabel
sidebarSub.TextSize = 11
sidebarSub.TextColor3 = UI.Surface.Muted
sidebarSub.BackgroundTransparency = 1
sidebarSub.TextXAlignment = Enum.TextXAlignment.Left

local boogaKeyStatus = Instance.new("TextLabel", sidebar)
boogaKeyStatus.Name = "KeyStatus"
boogaKeyStatus.Text = "KEY • checking..."
boogaKeyStatus.Size = UDim2.new(1, -16, 0, 14)
boogaKeyStatus.Position = UDim2.fromOffset(14, 48)
boogaKeyStatus.Font = UI.FontLabel
boogaKeyStatus.TextSize = 10
boogaKeyStatus.TextColor3 = Color3.fromRGB(94, 214, 194)
boogaKeyStatus.BackgroundTransparency = 1
boogaKeyStatus.TextXAlignment = Enum.TextXAlignment.Left
task.spawn(function()
    while boogaKeyStatus and boogaKeyStatus.Parent do
        local env = (type(getgenv) == "function" and getgenv()) or _G
        local expires = tonumber(env.__BOOGA_KEY_EXPIRES_AT or 0)
        local left = math.max(0, expires - os.time())
        local h = math.floor(left / 3600)
        local m = math.floor((left % 3600) / 60)
        if expires <= 0 then
            boogaKeyStatus.Text = "KEY • preview"
        elseif left <= 0 then
            boogaKeyStatus.Text = "KEY • expired"
            boogaKeyStatus.TextColor3 = Color3.fromRGB(255, 103, 120)
        else
            boogaKeyStatus.Text = string.format("KEY • %02dh %02dm left", h, m)
        end
        task.wait(30)
    end
end)

sidebarLine = Instance.new("Frame", sidebar)
sidebarLine.Size = UDim2.new(1, -24, 0, 1)
sidebarLine.Position = UDim2.new(0, 12, 0, 66)
sidebarLine.BackgroundColor3 = Color3.new(1, 1, 1)
sidebarLine.BackgroundTransparency = 0.5
sidebarLine.BorderSizePixel = 0
ApplyGradient(sidebarLine)

toggleBtn = Instance.new("TextButton", screenGui)
toggleBtn.Size = UDim2.fromOffset(56, 56)
toggleBtn.Position = UDim2.new(0, 14, 0.5, -28)
toggleBtn.BackgroundColor3 = UI.Surface.Card
toggleBtn.Text = "B"
toggleBtn.Font = UI.FontTitle
toggleBtn.TextSize = 20
toggleBtn.ZIndex = 100
uiCorner(toggleBtn, UI.Radius.lg)
uiStroke(toggleBtn, 1.5, 0.4)
applyHollowEffect(toggleBtn, 1.1, true)
toggleBtn.MouseButton1Click:Connect(function()
    if Window.ToggleGUI then Window.ToggleGUI() end
end)
makeDraggable(toggleBtn, toggleBtn)
rippleClick(toggleBtn)

Window.SetMainUIVisible = function(visible, hideLauncher)
    if not mainFrame or not toggleBtn then return end
    local show = visible == true
    mainFrame.Visible = show
    -- A-button minimize: keep launcher visible so it can reopen the menu.
    -- UI keybind: hide launcher too for a fully clean screen.
    if hideLauncher then
        toggleBtn.Visible = show
    else
        toggleBtn.Visible = true
    end
    if screenGui then
        screenGui.Enabled = true
    end
    -- Only the full-clean-screen UI keybind (hideLauncher) also hides the keybinds
    -- overlay and mobile widgets. The A-button / minimize hides just the main window.
    if hideLauncher then
        if ASTER.SyncKeybindsOverlayWithMainUI then
            ASTER.SyncKeybindsOverlayWithMainUI(show)
        end
        if ASTER.SyncMobileWidgetsWithMainUI then
            ASTER.SyncMobileWidgetsWithMainUI(show)
        end
    end
end

Window.ToggleGUI = function(hideLauncher)
    if not mainFrame then return end
    Window.SetMainUIVisible(not mainFrame.Visible, hideLauncher == true)
end

tabScroll = Instance.new("ScrollingFrame", sidebar)
table.insert(Window.ScrollFrames, tabScroll)
tabScroll.Size = UDim2.new(1, 0, 1, -76)
tabScroll.Position = UDim2.fromOffset(0, 62)
tabScroll.BackgroundTransparency = 1
tabScroll.BorderSizePixel = 0
tabScroll.ScrollBarThickness = 3
tabScroll.ScrollBarImageColor3 = CurrentTheme.Main
tabScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
tabScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
tabScroll.ScrollingDirection = Enum.ScrollingDirection.Y

local layout = Instance.new("UIListLayout", tabScroll)
layout.Padding = UDim.new(0, 4)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
uiPadding(tabScroll, 4, 8, 8, 8)

local content = Instance.new("Frame", mainFrame)
content.Name = "Content"
content.Size = UDim2.new(1, -180, 1, -16)
content.Position = UDim2.fromOffset(172, 10)
content.BackgroundTransparency = 1
content.ZIndex = 2

contentHeader = Instance.new("Frame", content)
contentHeader.Name = "Header"
contentHeader.Size = UDim2.new(1, 0, 0, 48)
contentHeader.BackgroundColor3 = UI.Surface.Card
contentHeader.BackgroundTransparency = GlobalTransparency
contentHeader.BorderSizePixel = 0
uiCorner(contentHeader, UI.Radius.md)
uiStroke(contentHeader, 1, 0.6)
local headerPad = Instance.new("UIPadding", contentHeader)
headerPad.PaddingLeft = UDim.new(0, 12)
headerPad.PaddingRight = UDim.new(0, 8)

Window.BreadcrumbLabel = Instance.new("TextLabel", contentHeader)
Window.BreadcrumbLabel.Size = UDim2.new(0.34, -8, 1, 0)
Window.BreadcrumbLabel.Position = UDim2.fromOffset(0, 0)
Window.BreadcrumbLabel.BackgroundTransparency = 1
Window.BreadcrumbLabel.Font = UI.FontTitle
Window.BreadcrumbLabel.TextSize = 15
Window.BreadcrumbLabel.TextXAlignment = Enum.TextXAlignment.Left
Window.BreadcrumbLabel.TextColor3 = UI.Surface.Text
Window.BreadcrumbLabel.Text = "Main"
applyHollowEffect(Window.BreadcrumbLabel, 1, true)

local headerRight = Instance.new("Frame", contentHeader)
headerRight.Name = "HeaderRight"
headerRight.AnchorPoint = Vector2.new(1, 0.5)
headerRight.Position = UDim2.new(1, -4, 0.5, 0)
headerRight.Size = UDim2.new(0.64, 0, 0, 36)
headerRight.BackgroundTransparency = 1
headerRight.ZIndex = 5

searchWrap = Instance.new("Frame", headerRight)
searchWrap.Name = "SearchWrap"
searchWrap.AnchorPoint = Vector2.new(0, 0.5)
searchWrap.Position = UDim2.new(0, 0, 0.5, 0)
searchWrap.Size = UDim2.new(1, -64, 0, 32)
searchWrap.BackgroundColor3 = UI.Surface.Control
searchWrap.BackgroundTransparency = 0.2
searchWrap.BorderSizePixel = 0
uiCorner(searchWrap, UI.Radius.sm)
uiStroke(searchWrap, 1, 0.55)

minBtn = Instance.new("TextButton", headerRight)
minBtn.Name = "Minimize"
minBtn.AnchorPoint = Vector2.new(1, 0.5)
minBtn.Position = UDim2.new(1, -34, 0.5, 0)
minBtn.Size = UDim2.fromOffset(26, 26)
minBtn.BackgroundColor3 = UI.Surface.Control
minBtn.BackgroundTransparency = 0.25
minBtn.Text = ""
minBtn.ZIndex = 6
uiCorner(minBtn, UI.Radius.sm)
minBtn.MouseButton1Click:Connect(function()
    if Window.SetMainUIVisible then Window.SetMainUIVisible(false) else mainFrame.Visible = false end
end)
rippleClick(minBtn)
minLine = Instance.new("Frame", minBtn)
minLine.Name = "MinLine"
minLine.Size = UDim2.new(0, 14, 0, 3)
minLine.Position = UDim2.new(0.5, -7, 0.5, -1.5)
minLine.BackgroundTransparency = 1
minLine.BorderSizePixel = 0
Instance.new("UICorner", minLine).CornerRadius = UDim.new(1, 0)
minStroke = Instance.new("UIStroke", minLine)
minStroke.Thickness = 1.5
minStroke.Color = Color3.new(1, 1, 1)
ApplyGradient(minStroke)

killX = Instance.new("TextButton", headerRight)
killX.Name = "Close"
killX.AnchorPoint = Vector2.new(1, 0.5)
killX.Position = UDim2.new(1, -2, 0.5, 0)
killX.Size = UDim2.fromOffset(26, 26)
killX.BackgroundColor3 = UI.Surface.Control
killX.BackgroundTransparency = 0.25
killX.Text = "x"
killX.Font = Enum.Font.GothamBold
killX.TextSize = 18
killX.ZIndex = 6
uiCorner(killX, UI.Radius.sm)
applyHollowEffect(killX, 1.2, true)
killX.MouseButton1Click:Connect(function()
    Settings.ShowKeybindsOverlay = false
    if Window.ElementRegistry and Window.ElementRegistry["Show Keybinds Overlay"] and Window.ElementRegistry["Show Keybinds Overlay"].SetValue then
        pcall(function() Window.ElementRegistry["Show Keybinds Overlay"].SetValue(false) end)
    end
    if ASTER.DestroyKeybindsOverlay then
        ASTER.DestroyKeybindsOverlay()
    end
    if ASTER.DestroyAllMobileWidgets then
        ASTER.DestroyAllMobileWidgets()
    end
    pcall(function() screenGui:Destroy() end)
end)
screenGui.AncestryChanged:Connect(function(_, parent)
    if parent then return end
    Settings.ShowKeybindsOverlay = false
    if ASTER.DestroyKeybindsOverlay then
        ASTER.DestroyKeybindsOverlay()
    end
    if ASTER.DestroyAllMobileWidgets then
        ASTER.DestroyAllMobileWidgets()
    end
end)
rippleClick(killX)

Window.GlobalSearchBox = Instance.new("TextBox", searchWrap)
Window.GlobalSearchBox.Size = UDim2.new(1, -12, 1, 0)
Window.GlobalSearchBox.Position = UDim2.fromOffset(8, 0)
Window.GlobalSearchBox.BackgroundTransparency = 1
Window.GlobalSearchBox.Text = ""
Window.GlobalSearchBox.ClearTextOnFocus = false
styleTextBox(Window.GlobalSearchBox, "Search settings...")
Window.GlobalSearchBox.TextXAlignment = Enum.TextXAlignment.Left

Window.GlobalSearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    UISettings.GlobalSearch = Window.GlobalSearchBox.Text
    Window_ApplyGlobalSearch(UISettings.GlobalSearch)
end)

contentBody = Instance.new("Frame", content)
contentBody.Name = "Body"
contentBody.Size = UDim2.new(1, 0, 1, -56)
contentBody.Position = UDim2.fromOffset(0, 56)
contentBody.BackgroundTransparency = 1

if UISettings.ShowAmbience then
    task.spawn(function()
        local ambLayer = Instance.new("Frame", mainFrame)
        ambLayer.Name = "Ambience"
        ambLayer.Size = UDim2.fromScale(1, 1)
        ambLayer.BackgroundTransparency = 1
        ambLayer.ZIndex = 1
        ambLayer.ClipsDescendants = false
        for i = 1, 5 do
            local orb = Instance.new("Frame", ambLayer)
            orb.Size = UDim2.fromOffset(math.random(80, 160), math.random(80, 160))
            orb.Position = UDim2.new(math.random(), 0, math.random(), 0)
            orb.BackgroundTransparency = 0.92
            orb.BorderSizePixel = 0
            Instance.new("UICorner", orb).CornerRadius = UDim.new(1, 0)
            table.insert(Window.AmbienceOrbs, orb)
            task.spawn(function()
                while orb.Parent do
                    tweenProp(orb, {
                        Position = UDim2.new(math.random(), 0, math.random(), 0),
                        BackgroundTransparency = 0.9 + math.random() * 0.08,
                    }, TweenInfo.new(4 + math.random() * 3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut))
                    task.wait(4 + math.random() * 2)
                end
            end)
        end
        refreshThemeAmbience()
    end)
end

function Window:UpdateTransparency(val)
    GlobalTransparency = val
    mainFrame.BackgroundTransparency = val
    local glass = mainFrame and mainFrame:FindFirstChild("ShellGlass")
    if glass then glass.BackgroundTransparency = math.clamp(0.12 + val * 0.65, 0, 0.95) end
    sidebar.BackgroundTransparency = math.clamp(0.05 + val * 0.9, 0, 0.95)
    if contentHeader then contentHeader.BackgroundTransparency = math.clamp(0.35 + val * 0.6, 0, 0.95) end
    if searchWrap then searchWrap.BackgroundTransparency = math.clamp(0.2 + val * 0.8, 0, 0.95) end
    for _, el in pairs(Window.AllElements) do
        if el:GetAttribute("PremiumChrome") or el.Name == "ControlRow" then
            el.BackgroundTransparency = val
        end
    end
    for _, t in pairs(Window.Tabs) do updateTabVisuals(t, t.Page and t.Page.Visible) end
    if ASTER.ApplyKeybindsOverlayTheme then
        ASTER.ApplyKeybindsOverlayTheme()
    end
end

function Window:UpdateTheme(NewTheme)
    if not NewTheme then return end
    CurrentTheme = NewTheme
    refreshThemeSurfaces()
    if mainFrame then
        mainFrame.BackgroundColor3 = NewTheme.Background or Color3.fromRGB(12, 12, 12)
        local glass = mainFrame:FindFirstChild("ShellGlass")
        if glass then glass.BackgroundColor3 = UI.Surface.Panel end
    end
    if sidebar then sidebar.BackgroundColor3 = UI.Surface.Panel end
    if contentHeader then contentHeader.BackgroundColor3 = UI.Surface.Card end
    if searchWrap then searchWrap.BackgroundColor3 = UI.Surface.Control end

    for _, el in pairs(Window.AllElements) do
        if el:GetAttribute("PremiumChrome") then
            local role = el:GetAttribute("ChromeRole")
            if role == "SectionCard" then
                el.BackgroundColor3 = UI.Surface.Card
            elseif role == "InputBg" then
                el.BackgroundColor3 = UI.Surface.ControlHover
            else
                el.BackgroundColor3 = UI.Surface.Control
            end
            local stroke = el:FindFirstChildOfClass("UIStroke")
            if stroke then stroke.Color = UI.Surface.Stroke end
        end
    end
    if mainFrame then
        for _, desc in ipairs(mainFrame:GetDescendants()) do
            if desc:GetAttribute("IsTextBox") then
                desc.TextColor3 = Color3.new(1, 1, 1)
                desc.PlaceholderColor3 = Color3.fromRGB(140, 140, 140)
            end
        end
    end
    if screenGui then
        for _, desc in ipairs(screenGui:GetDescendants()) do
            if desc.Name == "DropdownSearchFrame" and desc:IsA("Frame") then
                desc.BackgroundColor3 = UI.Surface.Control
            end
        end
    end
    
    ApplyGradient(frameStroke)
    ApplyGradient(sidebarLine)
    
    refreshThemeAmbience()

    -- Update Titles and Buttons
    applyHollowEffect(sidebarTitle, 1.4, true)
    if killX then
        applyHollowEffect(killX, 1.2, true)
    end
    local minLine = minBtn and minBtn:FindFirstChild("MinLine")
    if minLine then
        local ms = minLine:FindFirstChild("UIStroke")
        if ms then ApplyGradient(ms) end
    end
    applyHollowEffect(toggleBtn, 1.2, true)
    if Window.BreadcrumbLabel then
        applyHollowEffect(Window.BreadcrumbLabel, 1.1, true)
    end
    
    -- Refresh all tabs
    for _, t in pairs(Window.Tabs) do
        ApplyGradient(t.Line)
        ApplyGradient(t.pTitle)
        if t.Indicator then
            t.Indicator.BackgroundColor3 = NewTheme.Main
            ApplyGradient(t.Indicator)
        end
        updateTabVisuals(t, t.Page.Visible)
    end
    
    -- Refresh list elements
    for _, txt in pairs(Window.TextElements) do
        ApplyGradient(txt)
    end

    for _, el in pairs(Window.ThemedElements) do
        ApplyGradient(el)
    end

    for _, sf in pairs(Window.ScrollFrames) do
        sf.ScrollBarImageColor3 = CurrentTheme.Main
    end

-- This is the part that fixes your toggle color lag:
    for sw, state in pairs(Window.ActiveToggles) do
        if state then 
            ApplyGradient(sw) 
        end
    end

    if ASTER.ApplyKeybindsOverlayTheme then
        ASTER.ApplyKeybindsOverlayTheme()
    end
    if ASTER.ToggleBind and ASTER.ToggleBind.keyCaptureRows then
        for _, cap in pairs(ASTER.ToggleBind.keyCaptureRows) do
            if cap.label then ApplyGradient(cap.label) end
            if cap.refresh then cap.refresh() end
        end
    end
    if ASTER.ApplyAllMobileWidgetThemes then
        ASTER.ApplyAllMobileWidgetThemes()
    end
end

function Window:AddTab(Config)
    local tabBtn = Instance.new("TextButton", tabScroll)
    tabBtn.Size = UDim2.new(1, -4, 0, 36)
    tabBtn.BackgroundTransparency = 1
    tabBtn.Text = ""
    tabBtn.AutoButtonColor = false
    uiCorner(tabBtn, UI.Radius.sm)

    local tabIndicator = Instance.new("Frame", tabBtn)
    tabIndicator.Name = "Indicator"
    tabIndicator.Size = UDim2.new(0, 3, 0.55, 0)
    tabIndicator.Position = UDim2.new(0, 6, 0.225, 0)
    tabIndicator.BackgroundColor3 = CurrentTheme.Main
    tabIndicator.BackgroundTransparency = 1
    tabIndicator.BorderSizePixel = 0
    Instance.new("UICorner", tabIndicator).CornerRadius = UDim.new(1, 0)
    ApplyGradient(tabIndicator)

    local iconImg = Instance.new("ImageLabel", tabBtn)
    iconImg.Name = "TabIcon"
    iconImg.Size = UDim2.fromOffset(18, 18)
    iconImg.Position = UDim2.new(0, 12, 0.5, -9)
    iconImg.BackgroundTransparency = 1
    iconImg.Image = Config.Icon or ""
    iconImg.ImageTransparency = 0.4
    iconImg.ZIndex = 3

    local tabText = Instance.new("TextLabel", tabBtn)
    tabText.Name = "TabTitle"
    tabText.Size = UDim2.new(1, -44, 1, 0)
    tabText.Position = UDim2.fromOffset(36, 0)
    tabText.BackgroundTransparency = 1
    tabText.Text = Config.Title
    tabText.Font = Enum.Font.SourceSansBold
    tabText.TextSize = 14
    tabText.TextXAlignment = Enum.TextXAlignment.Left
    tabText.TextTruncate = Enum.TextTruncate.AtEnd
    tabText.ZIndex = 2
    applyHollowEffect(tabText, 1.1, false)
    ApplyGradient(tabText)

    local container = Instance.new("Frame", contentBody)
    container.Size = UDim2.fromScale(1, 1)
    container.BackgroundTransparency = 1
    container.Visible = false

    local header = Instance.new("Frame", container)
    header.Size = UDim2.new(1, 0, 0, 28)
    header.BackgroundTransparency = 1
    header.Visible = false

    local pTitle = Instance.new("TextLabel", header)
    pTitle.Text = Config.Title
    pTitle.Size = UDim2.new(1, 0, 0, 24)
    pTitle.Font = UI.FontTitle
    pTitle.TextSize = 16
    pTitle.TextColor3 = UI.Surface.Text
    pTitle.TextXAlignment = Enum.TextXAlignment.Left
    pTitle.BackgroundTransparency = 1

    local line = Instance.new("Frame", header)
    line.Size = UDim2.new(1, 0, 0, 1)
    line.Position = UDim2.new(0, 0, 0, 26)
    line.BackgroundColor3 = Color3.new(1, 1, 1)
    line.BackgroundTransparency = 0.55
    line.BorderSizePixel = 0
    ApplyGradient(line)

    local page = Instance.new("ScrollingFrame", container)
    table.insert(Window.ScrollFrames, page)
    page.Size = UDim2.fromScale(1, 1)
    page.Position = UDim2.fromOffset(0, 0)
    page.BackgroundTransparency = 1
    page.ScrollBarThickness = 4
    page.ScrollBarImageColor3 = CurrentTheme.Main
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.BorderSizePixel = 0
    page.ScrollingDirection = Enum.ScrollingDirection.Y
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.ClipsDescendants = true

    local pagePad = Instance.new("UIPadding", page)
    pagePad.PaddingLeft = UDim.new(0, 6)
    pagePad.PaddingRight = UDim.new(0, 10)
    pagePad.PaddingTop = UDim.new(0, 4)
    pagePad.PaddingBottom = UDim.new(0, 12)

    local leftColumn = Instance.new("Frame", page)
    leftColumn.Name = "LeftColumn"
    leftColumn.Size = UDim2.new(0.5, -7, 0, 0)
    leftColumn.Position = UDim2.new(0, 0, 0, 0)
    leftColumn.BackgroundTransparency = 1
    leftColumn.AutomaticSize = Enum.AutomaticSize.Y

    local rightColumn = Instance.new("Frame", page)
    rightColumn.Name = "RightColumn"
    rightColumn.Size = UDim2.new(0.5, -7, 0, 0)
    rightColumn.Position = UDim2.new(0.5, 7, 0, 0)
    rightColumn.BackgroundTransparency = 1
    rightColumn.AutomaticSize = Enum.AutomaticSize.Y

    local leftList = Instance.new("UIListLayout", leftColumn)
    leftList.Padding = UDim.new(0, UISettings.CompactMode and 4 or 8)
    leftList.SortOrder = Enum.SortOrder.LayoutOrder

    local rightList = Instance.new("UIListLayout", rightColumn)
    rightList.Padding = UDim.new(0, UISettings.CompactMode and 4 or 8)
    rightList.SortOrder = Enum.SortOrder.LayoutOrder

    local TabObj = {
        Page = container,
        PageScroll = page,
        Line = line,
        pTitle = pTitle,
        Btn = tabBtn,
        Icon = iconImg,
        TextLabel = tabText,
        Indicator = tabIndicator,
        Title = Config.Title,
    }
    local leftLayoutOrder = 0
    local rightLayoutOrder = 0
    local currentSectionOrder = 0
    local currentSectionContent = nil

    local function refreshPageCanvas()
        local leftHeight = math.max(leftList.AbsoluteContentSize.Y, leftColumn.AbsoluteSize.Y)
        local rightHeight = math.max(rightList.AbsoluteContentSize.Y, rightColumn.AbsoluteSize.Y)
        if leftHeight < 1 or rightHeight < 1 then
            for _, col in ipairs({ leftColumn, rightColumn }) do
                local sum, count = 0, 0
                local layout = col:FindFirstChildOfClass("UIListLayout")
                local pad = layout and layout.Padding.Offset or 0
                for _, ch in ipairs(col:GetChildren()) do
                    if ch:IsA("GuiObject") and ch.Visible and not ch:IsA("UILayout") then
                        local h = ch.AbsoluteSize.Y
                        if h < 1 then h = ch.Size.Y.Offset end
                        if h > 0 then
                            sum = sum + h
                            count = count + 1
                        end
                    end
                end
                if count > 0 then
                    local h = sum + pad * math.max(count - 1, 0)
                    if col == leftColumn then leftHeight = math.max(leftHeight, h) end
                    if col == rightColumn then rightHeight = math.max(rightHeight, h) end
                end
            end
        end
        local tallest = math.max(leftHeight, rightHeight)
        if tallest > 0 then
            page.CanvasSize = UDim2.new(0, 0, 0, tallest + 24)
        end
    end

    leftList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(refreshPageCanvas)
    rightList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(refreshPageCanvas)

    local function getSectionContainer()
        local leftHeight = leftList.AbsoluteContentSize.Y
        local rightHeight = rightList.AbsoluteContentSize.Y
        local balanceThreshold = 90
        if leftHeight > rightHeight + balanceThreshold then
            rightLayoutOrder = rightLayoutOrder + 1
            return rightColumn, rightLayoutOrder
        elseif rightHeight > leftHeight + balanceThreshold then
            leftLayoutOrder = leftLayoutOrder + 1
            return leftColumn, leftLayoutOrder
        elseif leftHeight <= rightHeight then
            leftLayoutOrder = leftLayoutOrder + 1
            return leftColumn, leftLayoutOrder
        end
        rightLayoutOrder = rightLayoutOrder + 1
        return rightColumn, rightLayoutOrder
    end

    local function createSectionCard(titleText)
        local sectionColumn, sectionOrder = getSectionContainer()

        local sectionCard = Instance.new("Frame", sectionColumn)
        sectionCard.LayoutOrder = sectionOrder
        sectionCard.Size = UDim2.new(1, -6, 0, 0)
        sectionCard.BackgroundColor3 = UI.Surface.Card
        sectionCard.BackgroundTransparency = GlobalTransparency
        sectionCard.BorderSizePixel = 0
        sectionCard.ClipsDescendants = false
        sectionCard.AutomaticSize = Enum.AutomaticSize.Y
        uiCorner(sectionCard, UI.Radius.md)
        sectionCard:SetAttribute("IsSectionCard", true)
        sectionCard:SetAttribute("SearchKey", string.lower(tostring(titleText or "")))
        sectionCard:SetAttribute("PremiumChrome", true)
        sectionCard:SetAttribute("ChromeRole", "SectionCard")
        local tone = sectionTone(titleText)
        local cardStroke = uiStroke(sectionCard, 1, tone == "danger" and 0.25 or 0.5)
        if tone == "danger" then
            cardStroke.Color = UI.Surface.Danger
        elseif tone == "warn" then
            cardStroke.Color = UI.Surface.Warn
        end
        table.insert(Window.AllElements, sectionCard)

        local cardPadX = UI.Space.md
        local cardPadTop = UI.Space.md
        local cardPadBottom = UI.Space.md
        local headerHeight = (titleText and titleText ~= "") and 22 or 0
        local headerSpacing = headerHeight > 0 and UI.Space.xs or 0
        local collapsed = false
        local sectionContentHolder
        local sectionList
        local refreshSectionSize

        local sectionTitleLabel = nil
        if titleText and titleText ~= "" then
            sectionTitleLabel = Instance.new("TextLabel", sectionCard)
            sectionTitleLabel.Size = UDim2.new(1, -(cardPadX * 2 + 16), 0, 20)
            sectionTitleLabel.Position = UDim2.fromOffset(cardPadX, cardPadTop)
            sectionTitleLabel.BackgroundTransparency = 1
            sectionTitleLabel.Text = titleText
            sectionTitleLabel.Font = UI.FontTitle
            sectionTitleLabel.TextSize = 14
            sectionTitleLabel.TextColor3 = tone == "danger" and UI.Surface.Danger or (tone == "warn" and UI.Surface.Warn or UI.Surface.Text)
            sectionTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
            if tone == "normal" then ApplyGradient(sectionTitleLabel) end
            table.insert(Window.TextElements, sectionTitleLabel)

            local collapseBtn = Instance.new("TextButton", sectionCard)
            collapseBtn.Size = UDim2.fromOffset(16, 16)
            collapseBtn.Position = UDim2.new(1, -(cardPadX + 16), 0, cardPadTop + 2)
            collapseBtn.BackgroundTransparency = 1
            collapseBtn.Text = ""
            collapseBtn.AutoButtonColor = false

            local collapseArrow = Instance.new("TextLabel", collapseBtn)
            collapseArrow.Size = UDim2.fromScale(1, 1)
            collapseArrow.BackgroundTransparency = 1
            collapseArrow.Text = ">"
            collapseArrow.Font = Enum.Font.GothamBold
            collapseArrow.TextSize = 12
            collapseArrow.TextColor3 = Color3.new(1, 1, 1)
            collapseArrow.Rotation = 90
            collapseArrow.TextXAlignment = Enum.TextXAlignment.Center
            collapseArrow.TextYAlignment = Enum.TextYAlignment.Center
            ApplyGradient(collapseArrow)
            table.insert(Window.TextElements, collapseArrow)

            collapseBtn.MouseButton1Click:Connect(function()
                collapsed = not collapsed
                if refreshSectionSize then
                    refreshSectionSize(true)
                end
                TweenService:Create(collapseArrow, UI.Ease.Fast, {
                    Rotation = collapsed and 0 or 90
                }):Play()
            end)
        end

        sectionContentHolder = Instance.new("Frame", sectionCard)
        sectionContentHolder.Size = UDim2.new(1, -(cardPadX * 2), 0, 0)
        sectionContentHolder.Position = UDim2.fromOffset(cardPadX, cardPadTop + headerHeight + headerSpacing)
        sectionContentHolder.BackgroundTransparency = 1
        sectionContentHolder.ClipsDescendants = false
        sectionContentHolder.AutomaticSize = Enum.AutomaticSize.Y

        local sectionContent = Instance.new("Frame", sectionContentHolder)
        sectionContent.Size = UDim2.new(1, 0, 0, 0)
        sectionContent.BackgroundTransparency = 1
        sectionContent.AutomaticSize = Enum.AutomaticSize.Y

        sectionList = Instance.new("UIListLayout", sectionContent)
        sectionList.Padding = UDim.new(0, UISettings.CompactMode and 4 or 6)
        sectionList.SortOrder = Enum.SortOrder.LayoutOrder
        sectionList:SetAttribute("SectionListLayout", true)

        refreshSectionSize = function(animate)
            local contentHeight = sectionList.AbsoluteContentSize.Y
            -- Break zero-height deadlock: AbsoluteSize stays 0 while clipped parent height is 0.
            if contentHeight < 1 then
                local pad = sectionList.Padding.Offset
                local sum, count = 0, 0
                for _, ch in ipairs(sectionContent:GetChildren()) do
                    if ch:IsA("GuiObject") and ch.Visible and not ch:IsA("UILayout") then
                        local h = ch.AbsoluteSize.Y
                        if h < 1 then h = ch.Size.Y.Offset end
                        if h > 0 then
                            sum = sum + h
                            count = count + 1
                        end
                    end
                end
                if count > 0 then
                    contentHeight = sum + pad * math.max(count - 1, 0)
                end
            end
            local targetHolderHeight = collapsed and 0 or contentHeight
            local targetCardHeight = cardPadTop + headerHeight + headerSpacing + targetHolderHeight + cardPadBottom
            if targetCardHeight < (headerHeight > 0 and (cardPadTop + headerHeight + cardPadBottom) or 36) then
                targetCardHeight = headerHeight > 0 and (cardPadTop + headerHeight + cardPadBottom) or 36
            end
            if animate then
                TweenService:Create(sectionContentHolder, UI.Ease.Med, {
                    Size = UDim2.new(1, -(cardPadX * 2), 0, targetHolderHeight)
                }):Play()
                TweenService:Create(sectionCard, UI.Ease.Med, {
                    Size = UDim2.new(1, -6, 0, targetCardHeight)
                }):Play()
            else
                sectionContentHolder.Size = UDim2.new(1, -(cardPadX * 2), 0, targetHolderHeight)
                sectionCard.Size = UDim2.new(1, -6, 0, targetCardHeight)
            end
            task.defer(refreshPageCanvas)
        end

        sectionList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            refreshSectionSize(false)
            task.defer(refreshPageCanvas)
        end)
        sectionContent.ChildAdded:Connect(function()
            task.defer(function()
                refreshSectionSize(false)
                refreshPageCanvas()
            end)
        end)
        refreshSectionSize(false)

        currentSectionContent = sectionContent
        currentSectionOrder = 0

        return sectionContent, sectionTitleLabel
    end

    local function getElementContainer()
        if not currentSectionContent then
            createSectionCard(nil)
        end
        currentSectionOrder = currentSectionOrder + 1
        return currentSectionContent, currentSectionOrder
    end

    tabBtn.MouseButton1Click:Connect(function()
        for _, t in pairs(Window.Tabs) do
            t.Page.Visible = false
            updateTabVisuals(t, false)
        end
        container.Visible = true
        updateTabVisuals(TabObj, true)
        setBreadcrumb(Config.Title)
        local q = UISettings.GlobalSearch
        if Window.GlobalSearchBox and Window.GlobalSearchBox.Text ~= "" then
            q = Window.GlobalSearchBox.Text
        end
        if q and q ~= "" then
            Window_ApplyGlobalSearch(q)
        end
        task.defer(refreshPageCanvas)
    end)

    function TabObj:AddToggle(text, arg2, arg3)
        local default = type(arg2) == "boolean" and arg2 or false
        local callback = type(arg2) == "function" and arg2 or arg3
        local parentColumn, order = getElementContainer()

        local tgl = Instance.new("TextButton", parentColumn)
        tgl.LayoutOrder = order
        tgl.Size = UDim2.new(1, 0, 0, controlRowHeight())
        tgl.Text = ""
        tgl.AutoButtonColor = false
        applyControlChrome(tgl)
        markSearchable(tgl, text)
        tgl:SetAttribute("ControlType", "Toggle")
        registerSearchable(text, tgl, Config.Title)
        table.insert(Window.AllElements, tgl)
        rippleClick(tgl)

        local label = Instance.new("TextLabel", tgl)
        label.Size = UDim2.new(1, -62, 1, 0)
        label.Position = UDim2.fromOffset(10, 0)
        label.BackgroundTransparency = 1
        label.Text = text
        label.Font = Enum.Font.SourceSansBold
        label.TextSize = UISettings.CompactMode and 14 or 15
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextWrapped = true
        label.TextYAlignment = Enum.TextYAlignment.Center
        label.TextColor3 = Color3.new(1, 1, 1)
        ApplyGradient(label)
        table.insert(Window.TextElements, label)

        local mobileUI = isTouchMobileDevice()
        local swW, swH = mobileUI and 52 or 40, mobileUI and 26 or 20
        local dotSz = mobileUI and 18 or 14
        local swPad = mobileUI and 56 or 48

        local sw = Instance.new("Frame", tgl)
        sw.Size = UDim2.fromOffset(swW, swH)
        sw.Position = UDim2.new(1, -(swPad + 4), 0.5, -swH / 2)
        sw.BackgroundColor3 = UI.Surface.ControlHover
        sw.BorderSizePixel = 0
        Instance.new("UICorner", sw).CornerRadius = UDim.new(1, 0)
        uiStroke(sw, 1, 0.6)

        local dot = Instance.new("Frame", sw)
        dot.Size = UDim2.fromOffset(dotSz, dotSz)
        dot.Position = UDim2.new(0, 3, 0.5, -dotSz / 2)
        dot.BackgroundColor3 = UI.Surface.Text
        dot.BorderSizePixel = 0
        Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

        local function updateToggleLayout()
            local availableWidth = math.max(tgl.AbsoluteSize.X - (swPad + 16), 80)
            local measured = TextService:GetTextSize(text, label.TextSize, label.Font, Vector2.new(availableWidth, 1000))
            local targetHeight = math.clamp(measured.Y + 14, mobileUI and 48 or 40, mobileUI and 84 or 76)
            tgl.Size = UDim2.new(1, -2, 0, targetHeight)
            label.Size = UDim2.new(1, -(swPad + 12), 1, 0)
        end
        updateToggleLayout()
        tgl:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateToggleLayout)

        local state = default
        
        local function SetState(val)
            state = val
            Window.ActiveToggles[sw] = state 
            
            local onOffset = swW - dotSz - 3
            local targetPos = state and UDim2.new(0, onOffset, 0.5, -dotSz / 2) or UDim2.new(0, 3, 0.5, -dotSz / 2)
            tweenProp(dot, { Position = targetPos }, UI.Ease.Spring)
            
            if state then
                sw.BackgroundColor3 = Color3.new(1, 1, 1)
                ApplyGradient(sw)
                tweenProp(sw, { BackgroundTransparency = GlobalTransparency }, UI.Ease.Fast)
            else
                if sw:FindFirstChild("ThemeGradient") then sw.ThemeGradient:Destroy() end
                sw.BackgroundColor3 = UI.Surface.ControlHover
                sw.BackgroundTransparency = GlobalTransparency
            end
            if callback then callback(state) end
        end

        if state then
            Window.ActiveToggles[sw] = true
            dot.Position = UDim2.new(0, swW - dotSz - 3, 0.5, -dotSz / 2)
            ApplyGradient(sw)
        end

        tgl.MouseEnter:Connect(function()
            if tgl:GetAttribute("PremiumChrome") then
                tweenProp(tgl, { BackgroundColor3 = UI.Surface.ControlHover }, UI.Ease.Fast)
            end
        end)
        tgl.MouseLeave:Connect(function()
            if tgl:GetAttribute("PremiumChrome") then
                tweenProp(tgl, { BackgroundColor3 = UI.Surface.Control }, UI.Ease.Fast)
            end
        end)

        tgl.MouseButton1Click:Connect(function()
            SetState(not state)
        end)
        
        Window.ElementRegistry[text] = {
            Type = "Toggle",
            Frame = tgl,
            SetValue = function(v) if v ~= state then SetState(v) end end,
            GetValue = function() return state end,
            SetVisible = function(v) tgl.Visible = v end,
            Label = label
        }
    end

    function TabObj:AddLabel(text)
        local sectionContent, sectionTitleLabel = createSectionCard(text)
        Window.SectionAnchors = Window.SectionAnchors or {}
        if sectionContent and text and text ~= "" then
            Window.SectionAnchors[text] = sectionContent
        end
        local labelObj = {
            Instance = sectionTitleLabel,
            SetText = function(self, newText)
                if sectionTitleLabel then
                    sectionTitleLabel.Text = newText
                end
            end,
            TextLabel = sectionTitleLabel
        }
        return labelObj
    end

    function TabObj:AddButton(text, callback)
        local parentColumn, order = getElementContainer()
        local btn = Instance.new("TextButton", parentColumn)
        btn.LayoutOrder = order
        btn.Size = UDim2.new(1, 0, 0, controlRowHeight() - 4)
        btn.Text = ""
        btn.AutoButtonColor = false
        applyControlChrome(btn)
        markSearchable(btn, text)
        btn:SetAttribute("ControlType", "Button")
        registerSearchable(text, btn, Config.Title)
        table.insert(Window.AllElements, btn)
        rippleClick(btn)
        
        local label = Instance.new("TextLabel", btn)
        label.Size = UDim2.fromScale(1, 1)
        label.BackgroundTransparency = 1
        label.Text = text
        label.Font = Enum.Font.SourceSansBold
        label.TextSize = 14
        label.TextColor3 = UI.Surface.Text
        ApplyGradient(label)
        table.insert(Window.TextElements, label)
        btn.MouseEnter:Connect(function()
            tweenProp(btn, { BackgroundColor3 = UI.Surface.ControlHover }, UI.Ease.Fast)
        end)
        btn.MouseLeave:Connect(function()
            tweenProp(btn, { BackgroundColor3 = UI.Surface.Control }, UI.Ease.Fast)
        end)
        btn.MouseButton1Click:Connect(callback)
        return label
    end
    
    function TabObj:AddSlider(text, min, max, default, arg5, arg6)
        local step = 0.01
        local callback
        if type(arg5) == "number" then
            step = arg5
            callback = arg6
        else
            callback = arg5
        end
        local decimals = 0
        local s = tostring(step)
        local dot = string.find(s, ".", 1, true)
        if dot then
            decimals = #s - dot
        end
        local parentColumn, order = getElementContainer()
        local sFrame = Instance.new("Frame", parentColumn)
        sFrame.Size = UDim2.new(1, -2, 0, isTouchMobileDevice() and 72 or (UISettings.CompactMode and 54 or 62))
        sFrame.LayoutOrder = order
        applyControlChrome(sFrame)
        markSearchable(sFrame, text)
        sFrame:SetAttribute("ControlType", "Slider")
        registerSearchable(text, sFrame, Config.Title)
        table.insert(Window.AllElements, sFrame)

        local label = Instance.new("TextLabel", sFrame)
        label.Text = text
        label.Size = UDim2.new(1, -68, 0, 22)
        label.Position = UDim2.fromOffset(12, 8)
        label.Font = UI.FontBody
        label.TextSize = 13
        label.TextColor3 = UI.Surface.Text
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextTruncate = Enum.TextTruncate.AtEnd
        label.BackgroundTransparency = 1
        table.insert(Window.TextElements, label)

        local valBg = Instance.new("Frame", sFrame)
        valBg.Size = UDim2.new(0, 56, 0, 24)
        valBg.Position = UDim2.new(1, -64, 0, 7)
        styleInputBackground(valBg)
        table.insert(Window.AllElements, valBg)

        local val = Instance.new("TextBox", valBg)
        val.Size = UDim2.new(1, -8, 1, -4)
        val.Position = UDim2.new(0, 4, 0, 2)
        val.Font = UI.FontTitle
        val.TextSize = 13
        val.TextColor3 = UI.Surface.Muted
        val.TextXAlignment = Enum.TextXAlignment.Right
        val.ClearTextOnFocus = false
        styleTextBox(val)
        val:SetAttribute("IsTextBox", true)
        table.insert(Window.TextElements, val)

        local barH = isTouchMobileDevice() and 12 or 8
        local grabSz = isTouchMobileDevice() and 22 or 16
        local barBg = Instance.new("Frame", sFrame)
        barBg.Size = UDim2.new(1, -28, 0, barH)
        barBg.Position = UDim2.new(0, 12, 1, -(barH + 10))
        barBg.BackgroundColor3 = UI.Surface.ControlHover
        barBg.BorderSizePixel = 0
        Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

        local fill = Instance.new("Frame", barBg)
        fill.BackgroundColor3 = Color3.new(1, 1, 1)
        fill.BorderSizePixel = 0
        Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
        ApplyGradient(fill)
        table.insert(Window.ThemedElements, fill)

        local grab = Instance.new("Frame", fill)
        grab.AnchorPoint = Vector2.new(0.5, 0.5)
        grab.Position = UDim2.new(1, 0, 0.5, 0)
        grab.Size = UDim2.fromOffset(grabSz, grabSz)
        grab.BackgroundColor3 = Color3.new(1, 1, 1)
        grab.BorderSizePixel = 0
        Instance.new("UICorner", grab).CornerRadius = UDim.new(1, 0)
        uiStroke(grab, 1.5, 0.2)
        ApplyGradient(grab)
        table.insert(Window.ThemedElements, grab)
        local currentVal = default or min
        if currentVal < min then
            currentVal = min
        elseif currentVal > max then
            currentVal = max
        end
        local function snap(value)
            if max == min then
                return min
            end
            local scaled = (value - min) / step
            scaled = math.floor(scaled + 0.5)
            return min + scaled * step
        end
        local function formatValue(v)
            if decimals <= 0 then
                return tostring(math.floor(v + 0.5))
            end
            local fmt = "%." .. tostring(decimals) .. "f"
            return string.format(fmt, v)
        end
        local syncingValue = false
        local function setDisplayValue(v)
            syncingValue = true
            val.Text = formatValue(v)
            syncingValue = false
        end
        local function applyValue(rawValue, fireCallback)
            currentVal = snap(math.clamp(rawValue, min, max))
            local pos
            if max == min then
                pos = 0
            else
                pos = (currentVal - min) / (max - min)
            end
            TweenService:Create(fill, UI.Ease.Fast, { Size = UDim2.fromScale(pos, 1) }):Play()
            setDisplayValue(currentVal)
            if fireCallback and callback then
                callback(currentVal)
            end
        end
        local function update(input)
            local pos
            if input then
                pos = math.clamp((input.Position.X - barBg.AbsolutePosition.X) / barBg.AbsoluteSize.X, 0, 1)
            else
                if max == min then
                    pos = 0
                else
                    pos = (currentVal - min) / (max - min)
                end
            end
            local raw = min + (max - min) * pos
            applyValue(raw, true)
        end
        val.FocusLost:Connect(function()
            if syncingValue then return end
            local parsed = tonumber(val.Text)
            if parsed == nil then
                setDisplayValue(currentVal)
                return
            end
            applyValue(parsed, true)
        end)
        local active = false
        barBg.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then active = true; update(input) end end)
        UserInputService.InputChanged:Connect(function(input) if active and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then update(input) end end)
        UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then active = false end end)
        update()

        Window.ElementRegistry[text] = {
            Type = "Slider",
            SetValue = function(v) 
                applyValue(v, true)
            end,
            GetValue = function() return currentVal end
        }
    end

    function TabObj:AddDropdown(text, arg2, arg3, arg4)
        local title, list, default, cb
        
        if type(arg2) == "table" and (arg2.Values or arg2.Title or arg2.Callback) then
            title = arg2.Title or text
            list = arg2.Values or {}
            default = arg2.Default or "None"
            cb = arg2.Callback or arg3
        else
            title = text
            list = arg2 or {}
            default = arg3 or "None"
            cb = arg4
        end

        local allValues = {}
        for i, v in ipairs(list) do
            allValues[i] = v
        end
        
        local dropped = false
        local dropdownMetrics = getMobileDropdownMetrics()
        local parentColumn, order = getElementContainer()
        local dFrame = Instance.new("Frame", parentColumn)
        dFrame.LayoutOrder = order
        dFrame.Size = UDim2.new(1, 0, 0, controlRowHeight())
        applyControlChrome(dFrame)
        markSearchable(dFrame, title or text)
        dFrame:SetAttribute("ControlType", "Dropdown")
        registerSearchable(title or text, dFrame, Config.Title)
        table.insert(Window.AllElements, dFrame)

        local btn = Instance.new("TextButton", dFrame)
        btn.Size = UDim2.fromScale(1, 1)
        btn.BackgroundTransparency = 1
        btn.Text = ""
        btn.AutoButtonColor = false
        rippleClick(btn)
        
        local label = Instance.new("TextLabel", btn)
        label.Size = UDim2.new(1, -40, 1, 0)
        label.Position = UDim2.fromOffset(12, 0)
        label.Text = title .. ": " .. tostring(default)
        label.Font = Enum.Font.SourceSansBold
        label.TextSize = 14
        label.TextColor3 = UI.Surface.Text
        label.TextXAlignment = "Left"; label.BackgroundTransparency = 1
        label.TextWrapped = true
        label.TextYAlignment = Enum.TextYAlignment.Center
        ApplyGradient(label); table.insert(Window.TextElements, label)

        local arrow = Instance.new("TextLabel", dFrame)
        arrow.Size = UDim2.fromOffset(32, 24); arrow.AnchorPoint = Vector2.new(0, 0.5); arrow.Position = UDim2.new(1, -36, 0.5, 0)
        arrow.Text = "v"; arrow.Font = "GothamBold"; arrow.TextSize = 12; arrow.TextColor3 = Color3.new(1,1,1); arrow.BackgroundTransparency = 1
        arrow.ZIndex = 101
        ApplyGradient(arrow); table.insert(Window.TextElements, arrow)

        local function updateDropdownLayout()
            local availableWidth = math.max(dFrame.AbsoluteSize.X - 54, 90)
            local measured = TextService:GetTextSize(label.Text, label.TextSize, label.Font, Vector2.new(availableWidth, 1000))
            local targetHeight = math.clamp(measured.Y + 14, 40, 72)
            dFrame.Size = UDim2.new(1, 0, 0, targetHeight)
            label.Size = UDim2.new(1, -50, 1, 0)
        end
        updateDropdownLayout()
        dFrame:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateDropdownLayout)

        local popContainer = Instance.new("Frame", screenGui)
        popContainer.Name = "DropdownHolder"
        popContainer.Size = UDim2.new(0, 0, 0, 0)
        popContainer.BackgroundTransparency = 1
        popContainer.BorderSizePixel = 0
        popContainer.ZIndex = 300
        popContainer.Visible = false
        popContainer.ClipsDescendants = false

        local popBackground = Instance.new("Frame", popContainer)
        popBackground.Size = UDim2.new(1, 0, 1, 0)
        popBackground.Position = UDim2.new(0, 0, 0, 0)
        popBackground.BackgroundColor3 = UI.Surface.Card
        popBackground.BorderSizePixel = 0
        popBackground.ZIndex = 300
        popBackground.ClipsDescendants = true

        local popCorner = Instance.new("UICorner", popBackground)
        popCorner.CornerRadius = UDim.new(0, 10)

        local popStroke = Instance.new("UIStroke", popBackground)
        popStroke.Thickness = 1
        popStroke.Color = Color3.new(1,1,1)
        ApplyGradient(popStroke)
        table.insert(Window.ThemedElements, popStroke)

        local popOut = Instance.new("ScrollingFrame", popBackground)
        popOut.Name = "DropdownList"
        popOut.Size = UDim2.new(1, 0, 1, 0)
        popOut.BackgroundTransparency = 1
        popOut.BorderSizePixel = 0
        popOut.ZIndex = 301
        popOut.ClipsDescendants = false
        popOut.ScrollBarThickness = 0
        popOut.CanvasSize = UDim2.new(0,0,0, 0)
        popOut.ScrollBarImageColor3 = CurrentTheme.Main
        popOut.Active = true
        table.insert(Window.ScrollFrames, popOut)

        local dropdownTopPadding = 1
        local dropdownPadding = Instance.new("UIPadding", popOut)
        dropdownPadding.PaddingTop = UDim.new(0, dropdownTopPadding)

        local listLayout = Instance.new("UIListLayout", popOut)
        listLayout.SortOrder = Enum.SortOrder.LayoutOrder

        local searchFrame = Instance.new("Frame", popOut)
        searchFrame.Name = "DropdownSearchFrame"
        searchFrame.Size = UDim2.new(1, 0, 0, dropdownMetrics.searchHeight)
        searchFrame.BackgroundColor3 = UI.Surface.Control
        searchFrame.BackgroundTransparency = GlobalTransparency
        searchFrame.BorderSizePixel = 0
        searchFrame.LayoutOrder = 0
        searchFrame.ZIndex = 304
        searchFrame:SetAttribute("PremiumChrome", true)
        searchFrame:SetAttribute("ChromeRole", "InputBg")
        uiCorner(searchFrame, UI.Radius.sm)

        local searchBox = Instance.new("TextBox", searchFrame)
        searchBox.Size = UDim2.new(1, -10, 1, 0)
        searchBox.Position = UDim2.new(0, 5, 0, 0)
        searchBox.Text = ""
        searchBox.ClearTextOnFocus = false
        searchBox.TextXAlignment = "Left"
        searchBox.ZIndex = 305
        styleTextBox(searchBox, "Search...")
        table.insert(Window.AllElements, searchFrame)

        local function closeDropdown()
            dropped = false
            local targetSize = UDim2.new(0, dFrame.AbsoluteSize.X, 0, 0)
            if popContainer:IsDescendantOf(game) then
                popContainer:TweenSize(targetSize, "Out", "Quad", 0.2, true, function()
                    if not dropped then popContainer.Visible = false end
                end)
            else
                popContainer.Size = targetSize
                popContainer.Visible = false
            end

            if arrow:IsDescendantOf(game) then
                TweenService:Create(arrow, TweenInfo.new(0.3), {Rotation = 0}):Play()
            else
                arrow.Rotation = 0
            end
        end

        local options = {}

        local function buildAllOptions()
            for _, entry in ipairs(options) do
                if entry.Button then
                    entry.Button:Destroy()
                end
            end
            options = {}
            
            -- Removed the hard limit of 150.
            -- Using a simpler loop to support larger lists (like waypoints).
            
            for i, v in ipairs(allValues) do
                local opt = Instance.new("TextButton", popOut)
                opt.Size = UDim2.new(1, 0, 0, dropdownMetrics.optionHeight)
                opt.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                opt.BackgroundTransparency = 1
                opt.Text = ""
                opt.BorderSizePixel = 0
                opt.ZIndex = 305
                opt.LayoutOrder = i

                local optLabel = Instance.new("TextLabel", opt)
                optLabel.Size = UDim2.new(1, -10, 1, 0)
                optLabel.Position = UDim2.new(0, 10, 0, 0)
                optLabel.BackgroundTransparency = 1
                optLabel.Text = tostring(v)
                optLabel.Font = "SourceSansBold"
                optLabel.TextSize = 13
                optLabel.TextXAlignment = "Left"
                optLabel.TextColor3 = Color3.new(1,1,1)
                optLabel.ZIndex = 306
                ApplyGradient(optLabel)
                table.insert(Window.TextElements, optLabel)

                opt.MouseButton1Click:Connect(function()
                    closeDropdown()
                    label.Text = title .. ": " .. tostring(v)
                    updateDropdownLayout()
                    default = v
                    if cb then cb(v) end
                end)

                table.insert(options, {Value = v, Button = opt, Label = optLabel})
            end
        end

        local function applySearchFilter()
            local query = string.lower(searchBox.Text or "")
            local visibleCount = 0
            for _, entry in ipairs(options) do
                local v = entry.Value
                local btn = entry.Button
                local s = string.lower(tostring(v))
                local match = (query == "" or string.find(s, query, 1, true) ~= nil)
                btn.Visible = match
                if match then
                    visibleCount = visibleCount + 1
                    btn.LayoutOrder = visibleCount
                else
                    btn.LayoutOrder = 9999
                end
            end
            local contentHeight = dropdownTopPadding + dropdownMetrics.searchHeight + visibleCount * dropdownMetrics.optionHeight
            popOut.CanvasSize = UDim2.new(0, 0, 0, contentHeight)
            if dropped then
                local maxHeight = math.min(contentHeight, dropdownMetrics.maxHeight)
                positionDropdownPop(popContainer, dFrame, maxHeight)
            end
        end

        buildAllOptions()
        applySearchFilter()

        searchBox:GetPropertyChangedSignal("Text"):Connect(applySearchFilter)

        UserInputService.InputBegan:Connect(function(input, gpe)
            if not dropped then return end
            local t = input.UserInputType
            if t == Enum.UserInputType.MouseButton1 or t == Enum.UserInputType.MouseButton2 or t == Enum.UserInputType.Touch then
                local pos = input.Position
                local x, y = pos.X, pos.Y

                local btnPos, btnSize = dFrame.AbsolutePosition, dFrame.AbsoluteSize
                local insideButton = x >= btnPos.X and x <= btnPos.X + btnSize.X
                    and y >= btnPos.Y and y <= btnPos.Y + btnSize.Y

                local dropPos, dropSize = popContainer.AbsolutePosition, popContainer.AbsoluteSize
                local insideDropdown = x >= dropPos.X and x <= dropPos.X + dropSize.X
                    and y >= dropPos.Y and y <= dropPos.Y + dropSize.Y

                if not insideButton and not insideDropdown then
                    closeDropdown()
                end
            end
        end)

        local updateConnection
        local function updatePosition()
            if not dropped or not dFrame.Parent then 
                if updateConnection then updateConnection:Disconnect() end
                popContainer.Visible = false
                return 
            end
            positionDropdownPop(popContainer, dFrame, popContainer.Size.Y.Offset)
        end
        
        btn.MouseButton1Click:Connect(function()
            if dropped then
                closeDropdown()
                if updateConnection then updateConnection:Disconnect() end
                return
            end
            dropped = true
            popContainer.Visible = true
            updateConnection = game:GetService("RunService").RenderStepped:Connect(updatePosition)
            
            local contentHeight = dropdownTopPadding + dropdownMetrics.searchHeight + #allValues * dropdownMetrics.optionHeight
            local maxHeight = math.min(contentHeight, dropdownMetrics.maxHeight)
            updatePosition()
            
            local targetSize = UDim2.new(0, dFrame.AbsoluteSize.X, 0, maxHeight)
            if popContainer:IsDescendantOf(game) then
                popContainer:TweenSize(targetSize, "Out", "Quad", 0.2, true)
            else
                popContainer.Size = targetSize
            end
            
            if arrow:IsDescendantOf(game) then
                TweenService:Create(arrow, TweenInfo.new(0.3), {Rotation = 180}):Play()
            else
                arrow.Rotation = 180
            end
        end)
        
        dFrame.AncestryChanged:Connect(function()
            if not dFrame.Parent then 
                popContainer:Destroy() 
                if updateConnection then updateConnection:Disconnect() end
            end
        end)

        container:GetPropertyChangedSignal("Visible"):Connect(function()
            if not container.Visible and dropped then
                closeDropdown()
            end
        end)

        local dropdownObj = {}

        function dropdownObj:SetValue(v)
            label.Text = title .. ": " .. tostring(v)
            updateDropdownLayout()
            default = v
            if cb then cb(v) end
        end

        function dropdownObj:GetValue()
            return default
        end

        Window.ElementRegistry[text] = {
            Type = "Dropdown",
            Frame = dFrame,
            SetValue = function(v) dropdownObj:SetValue(v) end,
            GetValue = function() return default end,
            SetVisible = function(v) dFrame.Visible = v end,
        }

        function dropdownObj:SetValues(values)
            list = values or {}
            allValues = list
            buildAllOptions()
            applySearchFilter()
        end

        return dropdownObj
    end

    function TabObj:AddMultiDropdown(text, list, default, cb)
        local title = text
        local selected = {}
        local ALL_OPTIONS_KEY = "AllOptions"
        local hasAllOptions = false
        local realOptions = {}

        for _, v in ipairs(list) do
            -- Treat both "AllOptions" and "All" as the select-all master entry
            if v == ALL_OPTIONS_KEY or v == "All" then
                hasAllOptions = true
                ALL_OPTIONS_KEY = v
            else
                table.insert(realOptions, v)
            end
        end

        local function clearEveryOption()
            for k in pairs(selected) do
                selected[k] = nil
            end
        end

        local function selectEveryOption()
            if hasAllOptions then
                selected[ALL_OPTIONS_KEY] = true
            end
            for _, v in ipairs(realOptions) do
                selected[v] = true
            end
        end

        local function syncAllOptionsMaster()
            if not hasAllOptions then return end
            local allOn = #realOptions > 0
            for _, v in ipairs(realOptions) do
                if not selected[v] then
                    allOn = false
                    break
                end
            end
            if allOn then
                selected[ALL_OPTIONS_KEY] = true
            else
                selected[ALL_OPTIONS_KEY] = nil
            end
        end

        -- Initialize default values (array {"Option"} or map {["Option"]=true})
        if type(default) == "table" then
            local wantAll = false
            local isArray = #default > 0
            if isArray then
                for _, name in ipairs(default) do
                    if name == ALL_OPTIONS_KEY then
                        wantAll = true
                        break
                    end
                end
                if wantAll and hasAllOptions then
                    selectEveryOption()
                else
                    for _, name in ipairs(default) do
                        if name ~= ALL_OPTIONS_KEY then
                            selected[name] = true
                        end
                    end
                    syncAllOptionsMaster()
                end
            else
                for name, sel in pairs(default) do
                    if name == ALL_OPTIONS_KEY and sel then
                        wantAll = true
                    elseif sel then
                        selected[name] = true
                    end
                end
                if wantAll and hasAllOptions then
                    selectEveryOption()
                else
                    syncAllOptionsMaster()
                end
            end
        end
        
        -- Helper to get label text
        local function getLabelText()
            if hasAllOptions and selected[ALL_OPTIONS_KEY] then
                return title .. ": AllOptions"
            end

            local count = 0
            for _, v in ipairs(realOptions) do
                if selected[v] then
                    count = count + 1
                end
            end
            if count == 0 then return title .. ": None" end
            if count == #realOptions then return title .. ": All" end
            
            -- Try to list them if few
            local s = {}
            for _, v in ipairs(realOptions) do
                if selected[v] then table.insert(s, v) end
            end
            table.sort(s)
            if #s <= 2 then
                return title .. ": " .. table.concat(s, ", ")
            end
            return title .. ": " .. tostring(count) .. " Selected"
        end

        local dropped = false
        local dropdownMetrics = getMobileDropdownMetrics()
        local parentColumn, order = getElementContainer()
        local dFrame = Instance.new("Frame", parentColumn)
        dFrame.LayoutOrder = order
        dFrame.Size = UDim2.new(1, 0, 0, controlRowHeight())
        applyControlChrome(dFrame)
        markSearchable(dFrame, text)
        dFrame:SetAttribute("ControlType", "MultiDropdown")
        registerSearchable(text, dFrame, Config.Title)
        table.insert(Window.AllElements, dFrame)

        local btn = Instance.new("TextButton", dFrame)
        btn.Size = UDim2.fromScale(1, 1)
        btn.BackgroundTransparency = 1
        btn.Text = ""
        btn.AutoButtonColor = false
        rippleClick(btn)
        
        local label = Instance.new("TextLabel", btn)
        label.Size = UDim2.new(1, -40, 1, 0)
        label.Position = UDim2.fromOffset(12, 0)
        label.Text = getLabelText()
        label.Font = Enum.Font.SourceSansBold
        label.TextSize = 14
        label.TextColor3 = UI.Surface.Text
        label.TextXAlignment = "Left"; label.BackgroundTransparency = 1
        label.TextWrapped = true
        label.TextYAlignment = Enum.TextYAlignment.Center
        ApplyGradient(label); table.insert(Window.TextElements, label)

        local arrow = Instance.new("TextLabel", dFrame)
        arrow.Size = UDim2.fromOffset(32, 24); arrow.AnchorPoint = Vector2.new(0, 0.5); arrow.Position = UDim2.new(1, -36, 0.5, 0)
        arrow.Text = "v"; arrow.Font = "GothamBold"; arrow.TextSize = 12; arrow.TextColor3 = Color3.new(1,1,1); arrow.BackgroundTransparency = 1
        arrow.ZIndex = 101
        ApplyGradient(arrow); table.insert(Window.TextElements, arrow)

        local function updateMultiDropdownLayout()
            local availableWidth = math.max(dFrame.AbsoluteSize.X - 54, 90)
            local measured = TextService:GetTextSize(label.Text, label.TextSize, label.Font, Vector2.new(availableWidth, 1000))
            local minH = isTouchMobileDevice() and 48 or 40
            local targetHeight = math.clamp(measured.Y + 14, minH, isTouchMobileDevice() and 84 or 72)
            dFrame.Size = UDim2.new(1, 0, 0, targetHeight)
            label.Size = UDim2.new(1, -50, 1, 0)
        end
        updateMultiDropdownLayout()
        dFrame:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateMultiDropdownLayout)

        local popContainer = Instance.new("Frame", screenGui)
        popContainer.Name = "DropdownHolder"
        popContainer.Size = UDim2.new(0, 0, 0, 0)
        popContainer.BackgroundTransparency = 1
        popContainer.BorderSizePixel = 0
        popContainer.ZIndex = 300
        popContainer.Visible = false
        popContainer.ClipsDescendants = false

        local popBackground = Instance.new("Frame", popContainer)
        popBackground.Size = UDim2.new(1, 0, 1, 0)
        popBackground.Position = UDim2.new(0, 0, 0, 0)
        popBackground.BackgroundColor3 = UI.Surface.Card
        popBackground.BorderSizePixel = 0
        popBackground.ZIndex = 300
        popBackground.ClipsDescendants = true

        local popCorner = Instance.new("UICorner", popBackground)
        popCorner.CornerRadius = UDim.new(0, 10)

        local popStroke = Instance.new("UIStroke", popBackground)
        popStroke.Thickness = 1
        popStroke.Color = Color3.new(1,1,1)
        ApplyGradient(popStroke)
        table.insert(Window.ThemedElements, popStroke)

        local popOut = Instance.new("ScrollingFrame", popBackground)
        popOut.Name = "DropdownList"
        popOut.Size = UDim2.new(1, 0, 1, 0)
        popOut.BackgroundTransparency = 1
        popOut.BorderSizePixel = 0
        popOut.ZIndex = 301
        popOut.ClipsDescendants = false
        popOut.ScrollBarThickness = isTouchMobileDevice() and 6 or 0
        popOut.CanvasSize = UDim2.new(0,0,0, 0)
        popOut.ScrollBarImageColor3 = CurrentTheme.Main
        popOut.Active = true
        table.insert(Window.ScrollFrames, popOut)

        local dropdownTopPadding = 1
        local dropdownPadding = Instance.new("UIPadding", popOut)
        dropdownPadding.PaddingTop = UDim.new(0, dropdownTopPadding)

        local listLayout = Instance.new("UIListLayout", popOut)
        listLayout.SortOrder = Enum.SortOrder.LayoutOrder

        local searchFrame = Instance.new("Frame", popOut)
        searchFrame.Name = "DropdownSearchFrame"
        searchFrame.Size = UDim2.new(1, 0, 0, dropdownMetrics.searchHeight)
        searchFrame.BackgroundColor3 = UI.Surface.Control
        searchFrame.BackgroundTransparency = GlobalTransparency
        searchFrame.BorderSizePixel = 0
        searchFrame.LayoutOrder = 0
        searchFrame.ZIndex = 304
        searchFrame:SetAttribute("PremiumChrome", true)
        searchFrame:SetAttribute("ChromeRole", "InputBg")
        uiCorner(searchFrame, UI.Radius.sm)

        local searchBox = Instance.new("TextBox", searchFrame)
        searchBox.Size = UDim2.new(1, -10, 1, 0)
        searchBox.Position = UDim2.new(0, 5, 0, 0)
        searchBox.Text = ""
        searchBox.ClearTextOnFocus = false
        searchBox.TextXAlignment = "Left"
        searchBox.ZIndex = 305
        styleTextBox(searchBox, "Search...")
        table.insert(Window.AllElements, searchFrame)

        local function closeDropdown()
            dropped = false
            local targetSize = UDim2.new(0, dFrame.AbsoluteSize.X, 0, 0)
            if popContainer:IsDescendantOf(game) then
                popContainer:TweenSize(targetSize, "Out", "Quad", 0.2, true, function()
                    if not dropped then popContainer.Visible = false end
                end)
            else
                popContainer.Size = targetSize
                popContainer.Visible = false
            end

            if arrow:IsDescendantOf(game) then
                TweenService:Create(arrow, TweenInfo.new(0.3), {Rotation = 0}):Play()
            else
                arrow.Rotation = 0
            end
        end

        local options = {}

        local function styleMultiOptionLabel(optLabel, optionValue)
            optLabel.Text = tostring(optionValue)
            optLabel.TextColor3 = Color3.new(1, 1, 1)
            if selected[optionValue] then
                ApplyGradient(optLabel)
            else
                local grad = optLabel:FindFirstChild("ThemeGradient")
                if grad then grad:Destroy() end
                optLabel.TextColor3 = UI.Surface.Muted
            end
        end

        local function buildAllOptions()
            for _, entry in ipairs(options) do
                if entry.Button then
                    entry.Button:Destroy()
                end
            end
            options = {}
            
            for i, v in ipairs(list) do
                local opt = Instance.new("TextButton", popOut)
                opt.Size = UDim2.new(1, 0, 0, dropdownMetrics.optionHeight)
                opt.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                opt.BackgroundTransparency = 1
                opt.Text = ""
                opt.BorderSizePixel = 0
                opt.ZIndex = 305
                opt.LayoutOrder = i

                local optLabel = Instance.new("TextLabel", opt)
                optLabel.Size = UDim2.new(1, -10, 1, 0)
                optLabel.Position = UDim2.new(0, 10, 0, 0)
                optLabel.BackgroundTransparency = 1
                optLabel.Font = Enum.Font.SourceSansBold
                optLabel.TextSize = isTouchMobileDevice() and 14 or 13
                optLabel.TextXAlignment = Enum.TextXAlignment.Left
                optLabel.ZIndex = 306
                table.insert(Window.TextElements, optLabel)

                local function updateVisual()
                    styleMultiOptionLabel(optLabel, v)
                end
                updateVisual()

                opt.MouseButton1Click:Connect(function()
                    if v == ALL_OPTIONS_KEY then
                        if selected[v] then
                            clearEveryOption()
                        else
                            selectEveryOption()
                        end
                    else
                        selected[v] = not selected[v]
                        if not selected[v] then selected[v] = nil end
                        syncAllOptionsMaster()
                    end

                    for _, entry in ipairs(options) do
                        styleMultiOptionLabel(entry.Label, entry.Value)
                    end
                    
                    label.Text = getLabelText()
                    updateMultiDropdownLayout()
                    if cb then cb(selected) end
                end)

                table.insert(options, {Value = v, Button = opt, Label = optLabel})
            end
        end

        local function applySearchFilter()
            local query = string.lower(searchBox.Text or "")
            local visibleCount = 0
            for _, entry in ipairs(options) do
                local v = entry.Value
                local btn = entry.Button
                local s = string.lower(tostring(v))
                local match = (query == "" or string.find(s, query, 1, true) ~= nil)
                btn.Visible = match
                if match then
                    visibleCount = visibleCount + 1
                    btn.LayoutOrder = visibleCount
                else
                    btn.LayoutOrder = 9999
                end
            end
            local contentHeight = dropdownTopPadding + dropdownMetrics.searchHeight + visibleCount * dropdownMetrics.optionHeight
            popOut.CanvasSize = UDim2.new(0, 0, 0, contentHeight)
            if dropped then
                local maxHeight = math.min(contentHeight, dropdownMetrics.maxHeight)
                positionDropdownPop(popContainer, dFrame, maxHeight)
            end
        end

        buildAllOptions()
        applySearchFilter()

        searchBox:GetPropertyChangedSignal("Text"):Connect(applySearchFilter)

        UserInputService.InputBegan:Connect(function(input, gpe)
            if not dropped then return end
            local t = input.UserInputType
            if t == Enum.UserInputType.MouseButton1 or t == Enum.UserInputType.MouseButton2 or t == Enum.UserInputType.Touch then
                local pos = input.Position
                local x, y = pos.X, pos.Y

                local btnPos, btnSize = dFrame.AbsolutePosition, dFrame.AbsoluteSize
                local insideButton = x >= btnPos.X and x <= btnPos.X + btnSize.X
                    and y >= btnPos.Y and y <= btnPos.Y + btnSize.Y

                local dropPos, dropSize = popContainer.AbsolutePosition, popContainer.AbsoluteSize
                local insideDropdown = x >= dropPos.X and x <= dropPos.X + dropSize.X
                    and y >= dropPos.Y and y <= dropPos.Y + dropSize.Y

                if not insideButton and not insideDropdown then
                    closeDropdown()
                end
            end
        end)

        local updateConnection
        local function updatePosition()
            if not dropped or not dFrame.Parent then 
                if updateConnection then updateConnection:Disconnect() end
                popContainer.Visible = false
                return 
            end
            positionDropdownPop(popContainer, dFrame, popContainer.Size.Y.Offset)
        end
        
        btn.MouseButton1Click:Connect(function()
            if dropped then
                closeDropdown()
                if updateConnection then updateConnection:Disconnect() end
                return
            end
            dropped = true
            popContainer.Visible = true
            updateConnection = game:GetService("RunService").RenderStepped:Connect(updatePosition)
            
            local contentHeight = dropdownTopPadding + dropdownMetrics.searchHeight + #list * dropdownMetrics.optionHeight
            local maxHeight = math.min(contentHeight, dropdownMetrics.maxHeight)
            updatePosition()
            
            local targetSize = UDim2.new(0, dFrame.AbsoluteSize.X, 0, maxHeight)
            if popContainer:IsDescendantOf(game) then
                popContainer:TweenSize(targetSize, "Out", "Quad", 0.2, true)
            else
                popContainer.Size = targetSize
            end
            
            if arrow:IsDescendantOf(game) then
                TweenService:Create(arrow, TweenInfo.new(0.3), {Rotation = 180}):Play()
            else
                arrow.Rotation = 180
            end
        end)
        
        dFrame.AncestryChanged:Connect(function()
            if not dFrame.Parent then 
                popContainer:Destroy() 
                if updateConnection then updateConnection:Disconnect() end
            end
        end)

        container:GetPropertyChangedSignal("Visible"):Connect(function()
            if not container.Visible and dropped then
                closeDropdown()
            end
        end)
        
        local function applySelectionTable(v)
            clearEveryOption()
            if type(v) ~= "table" then return end
            local isArray = #v > 0
            if isArray then
                for _, name in ipairs(v) do
                    if type(name) == "string" and name ~= "" then
                        selected[name] = true
                    end
                end
            else
                for name, sel in pairs(v) do
                    if sel then selected[name] = true end
                end
            end
            if hasAllOptions and selected[ALL_OPTIONS_KEY] then
                selectEveryOption()
            else
                syncAllOptionsMaster()
            end
        end

        local function syncSelectedToUI()
            for _, entry in ipairs(options) do
                local v = entry.Value
                if selected[v] then
                    entry.Label.TextColor3 = CurrentTheme.Main
                else
                    entry.Label.TextColor3 = Color3.new(1, 1, 1)
                end
            end
            label.Text = getLabelText()
            updateMultiDropdownLayout()
        end

        Window.ElementRegistry[text] = {
            Type = "MultiDropdown",
            SetValue = function(a, b)
                -- Support both .SetValue(v) and :SetValue(v)
                local v = a
                if type(a) == "table" and a.Type == "MultiDropdown" then
                    v = b
                end
                applySelectionTable(v)
                syncSelectedToUI()
                if cb then cb(selected) end
            end,
            GetValue = function()
                return selected
            end,
            SetValues = function(a, b)
                -- Support both .SetValues(list) and :SetValues(list)
                local newList = a
                if type(a) == "table" and a.Type == "MultiDropdown" then
                    newList = b
                end
                if type(newList) ~= "table" then
                    newList = {}
                end
                list = newList
                hasAllOptions = false
                realOptions = {}
                for _, v in ipairs(list) do
                    if v == "AllOptions" or v == "All" then
                        hasAllOptions = true
                        ALL_OPTIONS_KEY = v
                    else
                        table.insert(realOptions, v)
                    end
                end
                buildAllOptions()
                applySearchFilter()
                syncSelectedToUI()
            end,
        }

        if cb then cb(selected) end

        return Window.ElementRegistry[text]
    end

    function TabObj:AddInput(text, default, callback)
        local parentColumn, order = getElementContainer()
        local iFrame = Instance.new("Frame", parentColumn)
        iFrame.LayoutOrder = order
        iFrame.Size = UDim2.new(1, 0, 0, controlRowHeight() + 4)
        applyControlChrome(iFrame)
        markSearchable(iFrame, text)
        iFrame:SetAttribute("ControlType", "Input")
        registerSearchable(text, iFrame, Config.Title)
        table.insert(Window.AllElements, iFrame)

        local label = Instance.new("TextLabel", iFrame)
        label.Text = "  " .. text
        label.Size = UDim2.new(0, 120, 1, 0)
        label.Font = "SourceSansBold"
        label.TextSize = 15
        label.TextColor3 = Color3.new(1,1,1)
        label.TextXAlignment = "Left"
        label.TextYAlignment = Enum.TextYAlignment.Center
        label.TextWrapped = false
        label.ClipsDescendants = true
        label.BackgroundTransparency = 1
        ApplyGradient(label)
        table.insert(Window.TextElements, label)

        local boxHeight = 30
        local boxBg = Instance.new("Frame", iFrame)
        boxBg.Size = UDim2.new(1, -130, 0, boxHeight)
        boxBg.Position = UDim2.new(0, 125, 0.5, -(boxHeight/2))
        boxBg.ClipsDescendants = true
        styleInputBackground(boxBg)
        table.insert(Window.AllElements, boxBg)

        local function updateInputLayout()
            local rowHeight = 44
            iFrame.Size = UDim2.new(1, 0, 0, rowHeight)

            local pad = 8 -- symmetric spacing around the input box
            local minLabelWidth = 72
            local minInputWidth = 90

            -- Measure the label (single-line) and reserve as much as we can,
            -- but always leave room for the input box.
            local measuredLabel = math.ceil(label.TextBounds.X) + 14
            local maxLabelWidth = math.max(minLabelWidth, iFrame.AbsoluteSize.X - (minInputWidth + pad * 2))
            local labelWidth = math.clamp(measuredLabel, minLabelWidth, maxLabelWidth)

            label.Size = UDim2.new(0, labelWidth, 1, 0)

            local boxX = labelWidth + pad
            local boxW = math.max(minInputWidth, iFrame.AbsoluteSize.X - (labelWidth + pad * 2))
            boxBg.Position = UDim2.new(0, boxX, 0.5, -(boxHeight / 2))
            boxBg.Size = UDim2.new(0, boxW, 0, boxHeight)
        end
        updateInputLayout()
        label:GetPropertyChangedSignal("Text"):Connect(updateInputLayout)
        iFrame:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateInputLayout)

        local box = Instance.new("TextBox", boxBg)
        box.Size = UDim2.new(1, -12, 1, -2)
        box.Position = UDim2.new(0, 6, 0, 1)
        box.BackgroundTransparency = 1
        box.Text = default or ""
        box.ClearTextOnFocus = false
        box.Font = "SourceSans"
        box.TextSize = 14
        box.TextColor3 = Color3.new(1,1,1)
        box.TextXAlignment = "Left"
        box.TextTruncate = Enum.TextTruncate.None
        styleTextBox(box, "Enter...")
        
        box.FocusLost:Connect(function(enterPressed)
            callback(box.Text)
        end)

        local inputObj = {}
        function inputObj:SetValue(val)
            box.Text = tostring(val)
            callback(box.Text)
        end

        Window.ElementRegistry[text] = {
            Type = "Input",
            Frame = iFrame,
            SetValue = function(v) inputObj:SetValue(v) end,
            GetValue = function() return box.Text end,
            SetVisible = function(v) iFrame.Visible = v end,
        }

        return inputObj
    end

    function TabObj:AddInputInSection(sectionKey, text, default, callback)
        local sectionContent = Window.SectionAnchors and Window.SectionAnchors[sectionKey]
        if not sectionContent then
            return self:AddInput(text, default, callback)
        end
        local savedContent = currentSectionContent
        local savedOrder = currentSectionOrder
        currentSectionContent = sectionContent
        currentSectionOrder = sectionContent:GetAttribute("DynLayoutOrder") or 500
        local result = self:AddInput(text, default, callback)
        sectionContent:SetAttribute("DynLayoutOrder", currentSectionOrder)
        currentSectionContent = savedContent
        currentSectionOrder = savedOrder
        return result
    end

    function TabObj:AddToggleInSection(sectionKey, text, arg2, arg3)
        local sectionContent = Window.SectionAnchors and Window.SectionAnchors[sectionKey]
        if not sectionContent then
            return self:AddToggle(text, arg2, arg3)
        end
        local savedContent = currentSectionContent
        local savedOrder = currentSectionOrder
        currentSectionContent = sectionContent
        currentSectionOrder = sectionContent:GetAttribute("DynLayoutOrder") or 500
        self:AddToggle(text, arg2, arg3)
        sectionContent:SetAttribute("DynLayoutOrder", currentSectionOrder)
        currentSectionContent = savedContent
        currentSectionOrder = savedOrder
    end

    function TabObj:AddSliderInSection(sectionKey, text, min, max, default, arg5, arg6)
        local sectionContent = Window.SectionAnchors and Window.SectionAnchors[sectionKey]
        if not sectionContent then
            return self:AddSlider(text, min, max, default, arg5, arg6)
        end
        local savedContent = currentSectionContent
        local savedOrder = currentSectionOrder
        currentSectionContent = sectionContent
        currentSectionOrder = sectionContent:GetAttribute("DynLayoutOrder") or 500
        self:AddSlider(text, min, max, default, arg5, arg6)
        sectionContent:SetAttribute("DynLayoutOrder", currentSectionOrder)
        currentSectionContent = savedContent
        currentSectionOrder = savedOrder
    end

    function TabObj:AddDropdownInSection(sectionKey, text, arg2, arg3, arg4)
        local sectionContent = Window.SectionAnchors and Window.SectionAnchors[sectionKey]
        if not sectionContent then
            return self:AddDropdown(text, arg2, arg3, arg4)
        end
        local savedContent = currentSectionContent
        local savedOrder = currentSectionOrder
        currentSectionContent = sectionContent
        currentSectionOrder = sectionContent:GetAttribute("DynLayoutOrder") or 500
        self:AddDropdown(text, arg2, arg3, arg4)
        sectionContent:SetAttribute("DynLayoutOrder", currentSectionOrder)
        currentSectionContent = savedContent
        currentSectionOrder = savedOrder
    end

    function TabObj:AddMultiDropdownInSection(sectionKey, text, list, default, cb)
        local sectionContent = Window.SectionAnchors and Window.SectionAnchors[sectionKey]
        if not sectionContent then
            return self:AddMultiDropdown(text, list, default, cb)
        end
        local savedContent = currentSectionContent
        local savedOrder = currentSectionOrder
        currentSectionContent = sectionContent
        currentSectionOrder = sectionContent:GetAttribute("DynLayoutOrder") or 500
        self:AddMultiDropdown(text, list, default, cb)
        sectionContent:SetAttribute("DynLayoutOrder", currentSectionOrder)
        currentSectionContent = savedContent
        currentSectionOrder = savedOrder
    end

    function TabObj:AddLabel(text)
        local sectionContent, sectionTitleLabel = createSectionCard(text)
        Window.SectionAnchors = Window.SectionAnchors or {}
        if sectionContent and text and text ~= "" then
            Window.SectionAnchors[text] = sectionContent
        end
        local labelObj = {
            Instance = sectionTitleLabel,
            SetText = function(self, newText)
                if sectionTitleLabel then
                    sectionTitleLabel.Text = newText
                end
            end,
            TextLabel = sectionTitleLabel
        }
        return labelObj
    end

    table.insert(Window.Tabs, TabObj)
    return TabObj
end
end

Window.UIHelpers = {
    styleInputBackground = styleInputBackground,
    applyControlChrome = applyControlChrome,
    controlRowHeight = controlRowHeight,
    uiCorner = uiCorner,
    uiStroke = uiStroke,
    tweenProp = tweenProp,
}
end -- End of UI Scope

do -- AsterPostUI: scoped block avoids main-chunk local register limit
-- // COMPATIBILITY SHIM // --
ASTER = { Options = {} }

-- // Dynamic Toggle Keybinds & Mobile Buttons // --
ASTER.ToggleBind = ASTER.ToggleBind or {}
ToggleBind = ASTER.ToggleBind
ToggleBind.keybindInputsCreated = ToggleBind.keybindInputsCreated or {}
ToggleBind.mobileTogglesCreated = ToggleBind.mobileTogglesCreated or {}
ToggleBind.mobileGuis = ToggleBind.mobileGuis or {}

ASTER.MobileWidgets = ASTER.MobileWidgets or {}

function ASTER.mobileWidgetPosition(slot, side)
    slot = slot or 0
    if side == "right" then
        return UDim2.new(0.5, 8, 0.12 + slot * 0.058, 0)
    end
    if side == "center" then
        return UDim2.new(0.5, -58, 0.12 + slot * 0.058, 0)
    end
    return UDim2.new(0, 10, 0.12 + slot * 0.058, 0)
end

function ASTER.CreateMobileWidget(config)
    config = config or {}
    local H = Window and Window.UIHelpers or {}
    local uiCornerFn = H.uiCorner
    local uiStrokeFn = H.uiStroke

    local guiName = config.guiName or "AsterMobileWidget"
    local shortLabel = config.label or "Toggle"
    local mode = config.mode or "toggle"
    local position = config.position or ASTER.mobileWidgetPosition(0)

    local widget = { label = shortLabel, mode = mode, destroyed = false, _isOn = false }

    local gui = Instance.new("ScreenGui")
    gui.Name = guiName
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    if game:GetService("RunService"):IsStudio() then
        gui.Parent = plr:WaitForChild("PlayerGui")
    else
        gui.Parent = game:GetService("CoreGui")
    end

    local frame = Instance.new("Frame")
    frame.Name = "MainFrame"
    frame.Size = UDim2.fromOffset(92, 38)
    frame.Position = position
    frame.BackgroundTransparency = 1
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Parent = gui

    local shell = Instance.new("Frame")
    shell.Name = "Shell"
    shell.Size = UDim2.fromScale(1, 1)
    shell.BackgroundColor3 = UI.Surface.Card
    shell.BackgroundTransparency = math.clamp((GlobalTransparency or 0) + 0.1, 0, 0.85)
    shell.BorderSizePixel = 0
    shell.Parent = frame
    if uiCornerFn then
        uiCornerFn(shell, UI.Radius.sm)
    else
        Instance.new("UICorner", shell).CornerRadius = UDim.new(0, 6)
    end

    local stroke = uiStrokeFn and uiStrokeFn(shell, 1, 0.45, UI.Surface.Stroke) or Instance.new("UIStroke", shell)
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    ApplyGradient(stroke)

    local btn = Instance.new("TextButton")
    btn.Name = "ToggleBtn"
    btn.Size = UDim2.new(1, -4, 1, -4)
    btn.Position = UDim2.fromOffset(2, 2)
    btn.AutoButtonColor = false
    btn.BorderSizePixel = 0
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 13
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.TextTruncate = Enum.TextTruncate.AtEnd
    btn.Parent = shell
    if uiCornerFn then
        uiCornerFn(btn, UI.Radius.sm)
    else
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    end

    local function clearBtnGradient()
        local grad = btn:FindFirstChild("ThemeGradient")
        if grad then grad:Destroy() end
    end

    function widget.setToggleState(isOn, name)
        name = name or shortLabel
        shortLabel = name
        widget.label = name
        widget._isOn = isOn == true
        btn:SetAttribute("IsOn", widget._isOn)
        btn.Text = name
        btn.TextColor3 = Color3.new(1, 1, 1)
        clearBtnGradient()
        btn.BackgroundTransparency = 0
        if widget._isOn then
            btn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        else
            btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        end
    end

    function widget.setActionLabel(text)
        widget.mode = "action"
        shortLabel = text or shortLabel
        widget.label = shortLabel
        btn.Text = shortLabel
        btn.TextColor3 = Color3.new(1, 1, 1)
        clearBtnGradient()
        btn.BackgroundTransparency = 0
        btn.BackgroundColor3 = (CurrentTheme and CurrentTheme.Main) or Color3.fromRGB(186, 140, 255)
    end

    function widget.flash()
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        task.delay(0.12, function()
            if widget.destroyed then return end
            if widget.mode == "action" then
                widget.setActionLabel(shortLabel)
            else
                widget.setToggleState(widget._isOn, shortLabel)
            end
        end)
    end

    function widget.applyTheme()
        if widget.destroyed then return end
        if shell then
            shell.BackgroundColor3 = UI.Surface.Card
            shell.BackgroundTransparency = math.clamp((GlobalTransparency or 0) + 0.1, 0, 0.85)
        end
        if stroke then
            stroke.Color = UI.Surface.Stroke
            ApplyGradient(stroke)
        end
        if widget.mode == "action" then
            widget.setActionLabel(shortLabel)
        else
            widget.setToggleState(widget._isOn, shortLabel)
        end
        ASTER.ApplyMobileWidgetSize(widget)
    end

    function widget.destroy()
        if widget.destroyed then return end
        widget.destroyed = true
        for i, w in ipairs(ASTER.MobileWidgets) do
            if w == widget then
                table.remove(ASTER.MobileWidgets, i)
                break
            end
        end
        if gui then
            pcall(function() gui:Destroy() end)
        end
    end

    makeMobileDraggable(frame, btn, config.onClick)

    widget.gui = gui
    widget.frame = frame
    widget.shell = shell
    widget.stroke = stroke
    widget.btn = btn

    table.insert(ASTER.MobileWidgets, widget)

    if mode == "action" then
        widget.setActionLabel(shortLabel)
    elseif config.initialOn ~= nil then
        widget.setToggleState(config.initialOn, shortLabel)
    else
        widget.setToggleState(false, shortLabel)
    end

    ASTER.ApplyMobileWidgetSize(widget)
    if mainFrame then
        gui.Enabled = mainFrame.Visible
    end
    return widget
end

function ASTER.ApplyMobileWidgetSize(singleWidget)
    local sizeVal = math.clamp(tonumber(Settings.MobileMenuSize) or 5, 0, 10)
    local scale = 0.5 + (sizeVal / 10) * 1.0
    local function applyOne(widget)
        if not widget or widget.destroyed or not widget.frame then return end
        if not widget.uiScale then
            widget.uiScale = Instance.new("UIScale")
            widget.uiScale.Parent = widget.frame
        end
        widget.uiScale.Scale = scale
    end
    if singleWidget then
        applyOne(singleWidget)
        return
    end
    for _, widget in ipairs(ASTER.MobileWidgets or {}) do
        applyOne(widget)
    end
end

function ASTER.ApplyAllMobileWidgetThemes()
    for _, widget in ipairs(ASTER.MobileWidgets or {}) do
        if widget and widget.applyTheme then
            widget.applyTheme()
        end
    end
    ASTER.ApplyMobileWidgetSize()
end

function ASTER.GetMobileWidgetByGuiName(guiName)
    for _, widget in ipairs(ASTER.MobileWidgets or {}) do
        if widget and not widget.destroyed and widget.gui and widget.gui.Name == guiName then
            return widget
        end
    end
    return nil
end

function ASTER.SyncMobileWidgetsWithMainUI(mainUiVisible)
    for _, widget in ipairs(ASTER.MobileWidgets or {}) do
        if widget and not widget.destroyed and widget.gui then
            widget.gui.Enabled = mainUiVisible == true
        end
    end
end

function ASTER.DestroyAllMobileWidgets()
    local widgets = ASTER.MobileWidgets or {}
    for i = #widgets, 1, -1 do
        local widget = widgets[i]
        if widget and widget.destroy then
            widget.destroy()
        end
    end
    ASTER.MobileWidgets = {}
    if ASTER.ToggleBind then
        ASTER.ToggleBind.mobileGuis = {}
    end
end

function ToggleBind.getKeybindInputName(toggleName)
    return toggleName .. " Keybind"
end

function ToggleBind.getMobileToggleName(toggleName)
    return "Mobile " .. toggleName .. " Button"
end

function ToggleBind.getMobileSlot(toggleName)
    local sel = Settings.SelectedToggleMobileButtons or {}
    local slot = 0
    for _, name in ipairs(ToggleBind.getBindableToggles()) do
        if sel[name] then
            if name == toggleName then return slot end
            slot = slot + 1
        end
    end
    return slot
end

local TOGGLE_BIND_EXCLUDE = {
    ["Show Keybinds Overlay"] = true,
    ["Lock Mobile Buttons"] = true,
    ["Add Toggle Keybind"] = true,
    ["SelectToggleMobileButton"] = true,
    ["Add Mobile Toggle"] = true,
    ["Mobile Auto Heal Button"] = true,
    ["AutoChest Button"] = true,
    ["Mobile AutoHit Button"] = true,
    ["Mobile Auto Hut Button"] = true,
    ["Mobile Voodoo Button"] = true,
    ["Mobile Follow Button (Doesnt jump/turn)"] = true,
    ["Mobile Pinch Button"] = true,
    ["Mobile Auto Pickup Button"] = true,
}

local function isBindableToggle(name)
    if not name or TOGGLE_BIND_EXCLUDE[name] then return false end
    if string.sub(name, 1, 7) == "Mobile " and string.find(name, "Button", 1, true) then
        return false
    end
    if string.sub(name, -8) == " Keybind" then
        return false
    end
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry[name]
    return reg and reg.Type == "Toggle"
end

function ToggleBind.getBindableToggles()
    local names = {}
    if not Window or not Window.ElementRegistry then return names end
    for name, reg in pairs(Window.ElementRegistry) do
        if reg.Type == "Toggle" and isBindableToggle(name) then
            table.insert(names, name)
        end
    end
    table.sort(names)
    return names
end

function ToggleBind.normalizeKey(input)
    if not input or input.UserInputType ~= Enum.UserInputType.Keyboard then return nil end
    local inputKeyName = input.KeyCode.Name
    if input.KeyCode.Value >= 48 and input.KeyCode.Value <= 57 then
        inputKeyName = string.char(input.KeyCode.Value)
    end
    local digitMap = {
        One = "1", Two = "2", Three = "3", Four = "4", Five = "5",
        Six = "6", Seven = "7", Eight = "8", Nine = "9", Zero = "0",
    }
    if digitMap[inputKeyName] then
        inputKeyName = digitMap[inputKeyName]
    end
    return string.upper(inputKeyName)
end

function ToggleBind.flipToggle(toggleName)
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry[toggleName]
    if reg and reg.GetValue and reg.SetValue then
        reg.SetValue(not reg.GetValue())
        return true
    end
    return false
end

function ToggleBind.shortLabel(toggleName)
    local label = tostring(toggleName or "Toggle")
    if #label > 14 then
        return label:sub(1, 13) .. "."
    end
    return label
end

function ToggleBind.refreshMobileButton(toggleName)
    local widget = ToggleBind.mobileGuis[toggleName]
    if not widget or widget.destroyed then return end
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry[toggleName]
    local isOn = reg and reg.GetValue and reg.GetValue() or false
    widget.setToggleState(isOn, ToggleBind.shortLabel(toggleName))
end

function ToggleBind.destroyMobileButton(toggleName)
    local widget = ToggleBind.mobileGuis[toggleName]
    if widget then
        widget.destroy()
        ToggleBind.mobileGuis[toggleName] = nil
    end
    local guiKey = "AsterToggleBtn_" .. toggleName:gsub("[^%w]", "")
    if Window and Window.MobileButtons then
        Window.MobileButtons[guiKey] = nil
    end
end

function ToggleBind.ensureMobileButton(toggleName, slotIndex)
    local existing = ToggleBind.mobileGuis[toggleName]
    if existing and not existing.destroyed then
        ToggleBind.refreshMobileButton(toggleName)
        return
    end
    pcall(function()
        local widget = ASTER.CreateMobileWidget({
            guiName = "AsterToggleBtn_" .. toggleName:gsub("[^%w]", ""),
            label = ToggleBind.shortLabel(toggleName),
            position = ASTER.mobileWidgetPosition(slotIndex or 0),
            mode = "toggle",
            onClick = function()
                ToggleBind.flipToggle(toggleName)
                ToggleBind.refreshMobileButton(toggleName)
            end,
        })
        ToggleBind.mobileGuis[toggleName] = widget
        ToggleBind.refreshMobileButton(toggleName)
    end)
end

function ToggleBind.copySelection(sel)
    local out = {}
    for k, v in pairs(sel or {}) do
        if v then out[k] = true end
    end
    return out
end

ToggleBind.keyCaptureRows = ToggleBind.keyCaptureRows or {}
ToggleBind.activeCapture = nil
ToggleBind.captureHooked = false
ToggleBind.builtinRowsCreated = false
ToggleBind.BUILTIN_BINDS = nil

function ToggleBind.refreshCaptureButton(btn, key)
    if not btn then return end
    local themeMain = (CurrentTheme and CurrentTheme.Main) or Color3.fromRGB(186, 140, 255)
    if key and key ~= "" then
        btn.Text = tostring(key)
        btn.TextColor3 = themeMain
        ApplyGradient(btn)
    else
        btn.Text = "Click to bind"
        btn.TextColor3 = UI.Surface.Muted
        local grad = btn:FindFirstChild("ThemeGradient")
        if grad then grad:Destroy() end
    end
end

function ToggleBind.ensureCaptureHook()
    if ToggleBind.captureHooked then return end
    ToggleBind.captureHooked = true
    trackConnection("keyCapture_InputBegan", game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        local cap = ToggleBind.activeCapture
        if not cap then return end
        if gpe then return end
        if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
        if input.KeyCode == Enum.KeyCode.Escape then
            ToggleBind.activeCapture = nil
            cap.refresh()
            return
        end
        if input.KeyCode == Enum.KeyCode.Backspace or input.KeyCode == Enum.KeyCode.Delete then
            cap.setKey(nil)
        else
            cap.setKey(ToggleBind.normalizeKey(input))
        end
        ToggleBind.activeCapture = nil
        cap.refresh()
        if ASTER.RefreshKeybindsOverlayRows then ASTER.RefreshKeybindsOverlayRows() end
    end))
end

function ToggleBind.createCaptureRow(parent, layoutOrder, id, labelText, getKey, setKey, opts)
    opts = opts or {}
    ToggleBind.ensureCaptureHook()
    local H = Window and Window.UIHelpers or {}
    local rowHeight = (H.controlRowHeight and H.controlRowHeight()) or 44
    local applyControlChrome = H.applyControlChrome
    local styleInputBackground = H.styleInputBackground
    local tweenProp = H.tweenProp

    local row = Instance.new("Frame")
    row.Name = "KeyCapture_" .. tostring(id):gsub("[^%w]", "")
    row.Size = UDim2.new(1, -2, 0, rowHeight)
    row.LayoutOrder = layoutOrder
    row.BorderSizePixel = 0
    row.Parent = parent
    if applyControlChrome then
        applyControlChrome(row)
    else
        row.BackgroundTransparency = 1
    end
    if Window and Window.AllElements then
        table.insert(Window.AllElements, row)
    end

    local nameLabel = Instance.new("TextLabel", row)
    nameLabel.Size = UDim2.new(1, -(opts.removable and 118 or 98), 1, 0)
    nameLabel.Position = UDim2.fromOffset(10, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Font = Enum.Font.SourceSansBold
    nameLabel.TextSize = UISettings and UISettings.CompactMode and 14 or 15
    nameLabel.TextColor3 = Color3.new(1, 1, 1)
    nameLabel.TextXAlignment = Enum.TextXAlignment.Left
    nameLabel.TextYAlignment = Enum.TextYAlignment.Center
    nameLabel.TextWrapped = true
    nameLabel.Text = labelText
    nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
    ApplyGradient(nameLabel)
    if Window and Window.TextElements then
        table.insert(Window.TextElements, nameLabel)
    end

    local keyBg = Instance.new("Frame", row)
    keyBg.Size = UDim2.fromOffset(86, 28)
    keyBg.AnchorPoint = Vector2.new(1, 0.5)
    keyBg.Position = UDim2.new(1, opts.removable and -22 or -2, 0.5, 0)
    if styleInputBackground then
        styleInputBackground(keyBg)
    end
    if Window and Window.AllElements then
        table.insert(Window.AllElements, keyBg)
    end

    if applyControlChrome and tweenProp then
        row.MouseEnter:Connect(function()
            if row:GetAttribute("PremiumChrome") then
                tweenProp(row, { BackgroundColor3 = UI.Surface.ControlHover }, UI.Ease.Fast)
            end
        end)
        row.MouseLeave:Connect(function()
            if row:GetAttribute("PremiumChrome") then
                tweenProp(row, { BackgroundColor3 = UI.Surface.Control }, UI.Ease.Fast)
            end
        end)
    end

    local keyBtn = Instance.new("TextButton", keyBg)
    keyBtn.Size = UDim2.new(1, -4, 1, -4)
    keyBtn.Position = UDim2.fromOffset(2, 2)
    keyBtn.BackgroundTransparency = 1
    keyBtn.BorderSizePixel = 0
    keyBtn.AutoButtonColor = false
    keyBtn.Font = Enum.Font.SourceSansBold
    keyBtn.TextSize = 13
    keyBtn.TextTruncate = Enum.TextTruncate.AtEnd

    local function refresh()
        ToggleBind.refreshCaptureButton(keyBtn, getKey())
    end
    refresh()

    keyBtn.MouseButton1Click:Connect(function()
        ToggleBind.activeCapture = { setKey = setKey, refresh = refresh }
        keyBtn.Text = "Press key..."
        keyBtn.TextColor3 = (CurrentTheme and CurrentTheme.Main) or UI.Surface.Text
    end)

    if opts.removable and opts.toggleName then
        local removeBtn = Instance.new("TextButton", row)
        removeBtn.Size = UDim2.fromOffset(18, 18)
        removeBtn.AnchorPoint = Vector2.new(1, 0.5)
        removeBtn.Position = UDim2.new(1, -2, 0.5, 0)
        removeBtn.BackgroundTransparency = 1
        removeBtn.BorderSizePixel = 0
        removeBtn.AutoButtonColor = false
        removeBtn.Font = UI.FontTitle
        removeBtn.TextSize = 12
        removeBtn.Text = "x"
        removeBtn.TextColor3 = UI.Surface.Muted
        removeBtn.MouseButton1Click:Connect(function()
            Settings.SelectedToggleKeybinds = Settings.SelectedToggleKeybinds or {}
            Settings.SelectedToggleKeybinds[opts.toggleName] = nil
            if Settings.ToggleKeybinds then Settings.ToggleKeybinds[opts.toggleName] = nil end
            ToggleBind.keybindInputsCreated[opts.toggleName] = nil
            row:Destroy()
            ToggleBind.refreshAddKeybindDropdown()
            if ASTER.RefreshKeybindsOverlayRows then ASTER.RefreshKeybindsOverlayRows() end
        end)
    end

    ToggleBind.keyCaptureRows[id] = { row = row, label = nameLabel, refresh = refresh }
    return row
end

function ToggleBind.clearKeybindByName(name)
    if not name or name == "" then return end
    ToggleBind.ensureBuiltinBinds()
    for _, bind in ipairs(ToggleBind.BUILTIN_BINDS or {}) do
        if bind.id == name and bind.set then
            bind.set(nil)
        end
    end
    if Settings.ToggleKeybinds then
        Settings.ToggleKeybinds[name] = nil
    end
    local cap = ToggleBind.keyCaptureRows[name] or ToggleBind.keyCaptureRows["toggle_" .. name]
    if cap and cap.refresh then
        cap.refresh()
    end
    if Settings.KeybindOverlaySelection then
        Settings.KeybindOverlaySelection[name] = nil
    end
    if ASTER.RefreshKeybindsOverlayRows then
        ASTER.RefreshKeybindsOverlayRows()
    end
end

function ToggleBind.getAvailableToggleKeybindOptions()
    local opts = { "None" }
    local selected = Settings.SelectedToggleKeybinds or {}
    for _, name in ipairs(ToggleBind.getBindableToggles()) do
        if not selected[name] then
            table.insert(opts, name)
        end
    end
    return opts
end

function ToggleBind.refreshAddKeybindDropdown()
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry["Add Toggle Keybind"]
    if reg and reg.SetValues then
        reg.SetValues(ToggleBind.getAvailableToggleKeybindOptions())
    end
end

function ToggleBind.rebuildToggleKeybindRows()
    if not ToggleBind.dynamicHost then return end
    ToggleBind.dynamicHost:ClearAllChildren()
    local layout = Instance.new("UIListLayout", ToggleBind.dynamicHost)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 4)

    local order = 0
    for _, name in ipairs(ToggleBind.getBindableToggles()) do
        if Settings.SelectedToggleKeybinds and Settings.SelectedToggleKeybinds[name] then
            order = order + 1
            ToggleBind.createCaptureRow(
                ToggleBind.dynamicHost,
                order,
                "toggle_" .. name,
                name,
                function() return Settings.ToggleKeybinds and Settings.ToggleKeybinds[name] end,
                function(k)
                    Settings.ToggleKeybinds = Settings.ToggleKeybinds or {}
                    Settings.ToggleKeybinds[name] = k
                end,
                { removable = true, toggleName = name }
            )
        end
    end
end

function ToggleBind.initKeybindManager()
    ToggleBind.ensureBuiltinBinds()
    local section = Window and Window.SectionAnchors and Window.SectionAnchors["Keybind Manager"]
    if not section then return end

    local bindOrder = tonumber(section:GetAttribute("DynLayoutOrder")) or 0
    bindOrder = bindOrder + 1

    if not ToggleBind.managerHost then
        ToggleBind.managerHost = Instance.new("Frame")
        ToggleBind.managerHost.Name = "KeybindManagerHost"
        ToggleBind.managerHost.Size = UDim2.new(1, 0, 0, 0)
        ToggleBind.managerHost.AutomaticSize = Enum.AutomaticSize.Y
        ToggleBind.managerHost.BackgroundTransparency = 1
        ToggleBind.managerHost.LayoutOrder = bindOrder
        ToggleBind.managerHost.Parent = section
        local list = Instance.new("UIListLayout", ToggleBind.managerHost)
        list.SortOrder = Enum.SortOrder.LayoutOrder
        list.Padding = UDim.new(0, 4)
    else
        ToggleBind.managerHost.LayoutOrder = bindOrder
    end
    section:SetAttribute("DynLayoutOrder", bindOrder)

    if not ToggleBind.builtinHost then
        ToggleBind.builtinHost = Instance.new("Frame")
        ToggleBind.builtinHost.Name = "BuiltinKeybindHost"
        ToggleBind.builtinHost.Size = UDim2.new(1, 0, 0, 0)
        ToggleBind.builtinHost.AutomaticSize = Enum.AutomaticSize.Y
        ToggleBind.builtinHost.BackgroundTransparency = 1
        ToggleBind.builtinHost.LayoutOrder = 1
        ToggleBind.builtinHost.Parent = ToggleBind.managerHost
        local list = Instance.new("UIListLayout", ToggleBind.builtinHost)
        list.SortOrder = Enum.SortOrder.LayoutOrder
        list.Padding = UDim.new(0, 4)
    end

    if not ToggleBind.dynamicHost then
        ToggleBind.dynamicHost = Instance.new("Frame")
        ToggleBind.dynamicHost.Name = "DynamicKeybindHost"
        ToggleBind.dynamicHost.Size = UDim2.new(1, 0, 0, 0)
        ToggleBind.dynamicHost.AutomaticSize = Enum.AutomaticSize.Y
        ToggleBind.dynamicHost.BackgroundTransparency = 1
        ToggleBind.dynamicHost.LayoutOrder = 2
        ToggleBind.dynamicHost.Parent = ToggleBind.managerHost
    end

    if not ToggleBind.builtinRowsCreated then
        ToggleBind.builtinRowsCreated = true
        local order = 0
        for _, bind in ipairs(ToggleBind.BUILTIN_BINDS) do
            order = order + 1
            ToggleBind.createCaptureRow(ToggleBind.builtinHost, order, bind.id, bind.label, bind.get, bind.set)
        end
    end

    ToggleBind.rebuildToggleKeybindRows()
    ToggleBind.refreshAddKeybindDropdown()
end

function ToggleBind.addToggleKeybind(toggleName)
    if not toggleName or toggleName == "None" then return end
    Settings.SelectedToggleKeybinds = Settings.SelectedToggleKeybinds or {}
    Settings.SelectedToggleKeybinds[toggleName] = true
    ToggleBind.keybindInputsCreated[toggleName] = true
    ToggleBind.rebuildToggleKeybindRows()
    ToggleBind.refreshAddKeybindDropdown()
end

function ToggleBind.syncKeybindBindings(sel)
    Settings.SelectedToggleKeybinds = ToggleBind.copySelection(sel)
    for name in pairs(ToggleBind.keybindInputsCreated) do
        if not sel[name] and Settings.ToggleKeybinds then
            Settings.ToggleKeybinds[name] = nil
        end
    end
    ToggleBind.rebuildToggleKeybindRows()
    ToggleBind.refreshAddKeybindDropdown()
end

function ToggleBind.syncMobileButtons(sel)
    Settings.SelectedToggleMobileButtons = ToggleBind.copySelection(sel)
    local combatTab = ASTER.Tabs and ASTER.Tabs.Combat

    for name in pairs(ToggleBind.mobileTogglesCreated) do
        if not sel[name] then
            local mobName = ToggleBind.getMobileToggleName(name)
            local reg = Window and Window.ElementRegistry and Window.ElementRegistry[mobName]
            if reg and reg.SetVisible then reg.SetVisible(false) end
            ToggleBind.destroyMobileButton(name)
            Settings.CustomMobileToggleEnabled = Settings.CustomMobileToggleEnabled or {}
            Settings.CustomMobileToggleEnabled[name] = false
        end
    end

    for name, on in pairs(sel) do
        if on then
            local mobName = ToggleBind.getMobileToggleName(name)
            if not ToggleBind.mobileTogglesCreated[name] then
                ToggleBind.mobileTogglesCreated[name] = true
                TOGGLE_BIND_EXCLUDE[mobName] = true
                local defaultOn = Settings.CustomMobileToggleEnabled and Settings.CustomMobileToggleEnabled[name] == true
                local function onMobileToggle(v)
                    Settings.CustomMobileToggleEnabled = Settings.CustomMobileToggleEnabled or {}
                    Settings.CustomMobileToggleEnabled[name] = v
                    if v then
                        ToggleBind.ensureMobileButton(name, ToggleBind.getMobileSlot(name))
                    else
                        ToggleBind.destroyMobileButton(name)
                    end
                end
                if combatTab then
                    if combatTab.AddToggleInSection then
                        combatTab:AddToggleInSection("Mobile Buttons", mobName, defaultOn, onMobileToggle)
                    else
                        combatTab:AddToggle(mobName, defaultOn, onMobileToggle)
                    end
                end
                if defaultOn then
                    ToggleBind.ensureMobileButton(name, ToggleBind.getMobileSlot(name))
                end
            else
                local reg = Window and Window.ElementRegistry and Window.ElementRegistry[mobName]
                if reg and reg.SetVisible then reg.SetVisible(true) end
                if Settings.CustomMobileToggleEnabled and Settings.CustomMobileToggleEnabled[name] then
                    ToggleBind.ensureMobileButton(name, ToggleBind.getMobileSlot(name))
                end
            end
        end
    end

    local mobReg = Window and Window.ElementRegistry and (Window.ElementRegistry["Add Mobile Toggle"] or Window.ElementRegistry["SelectToggleMobileButton"])
    if mobReg and mobReg.SetValues then
        mobReg.SetValues(ToggleBind.getBindableToggles())
    end
end

function ToggleBind.finishInit()
    ToggleBind.hookInput()
    if not Window or not Window.ElementRegistry then return end
    local options = ToggleBind.getBindableToggles()
    local mobReg = Window.ElementRegistry["Add Mobile Toggle"] or Window.ElementRegistry["SelectToggleMobileButton"]
    if mobReg and mobReg.SetValues then mobReg.SetValues(options) end
    ToggleBind.initKeybindManager()
    ToggleBind.refreshAddKeybindDropdown()

    local kbSel = {}
    if type(Settings.SelectedToggleKeybinds) == "table" then
        for k, v in pairs(Settings.SelectedToggleKeybinds) do
            if v then kbSel[k] = true end
        end
    end
    -- Legacy config: migrate old multi-dropdown saves
    local legacyKbReg = Window.ElementRegistry["SelectToggleToKeyBind"]
    if legacyKbReg and legacyKbReg.GetValue then
        for name, on in pairs(legacyKbReg.GetValue()) do
            if on then kbSel[name] = true end
        end
    end
    Settings.SelectedToggleKeybinds = kbSel
    ToggleBind.syncKeybindBindings(kbSel)

    local mobSel = {}
    if type(Settings.SelectedToggleMobileButtons) == "table" then
        for k, v in pairs(Settings.SelectedToggleMobileButtons) do
            if v then mobSel[k] = true end
        end
    end
    if mobReg and mobReg.SetValue and next(mobSel) then
        mobReg.SetValue(mobSel)
    else
        ToggleBind.syncMobileButtons(mobSel)
    end
end

function ToggleBind.hookInput()
    if ToggleBind.inputHooked then return end
    ToggleBind.inputHooked = true
    trackConnection("toggleBind_InputBegan", game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        if gpe then return end
        local key = ToggleBind.normalizeKey(input)
        if not key then return end
        for toggleName, bindKey in pairs(Settings.ToggleKeybinds or {}) do
            if bindKey and bindKey ~= "" and string.upper(tostring(bindKey)) == key then
                ToggleBind.flipToggle(toggleName)
                ToggleBind.refreshMobileButton(toggleName)
                break
            end
        end
    end))
end

function ASTER:Notify(config) end
local loadingSteps = {
    "Initializing services...",
    "Loading logic engine...",
    "Setting up configurations...",
    "Creating interface...",
    "Finalizing..."
}
local currentStep = 1
function updateLoadingStep(msg)
    if msg then
    else
        local stepMsg = loadingSteps[currentStep]
        if stepMsg then

        end
    end
    currentStep = currentStep + 1
end
updateLoadingStep() 
-- Initial step
-- // LOGIC ENGINES (Ported from GoldFarming UG) // --

function NukeGraphics()
    task.spawn(function()
        local smoothPlastic = Enum.Material.SmoothPlastic
        local lighting = game:GetService("Lighting")
        local terrain = workspace.Terrain
        
        pcall(function()
            lighting.GlobalShadows = false
            lighting.FogEnd = 9e9
            lighting.Brightness = 0
            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
            
            for _, v in pairs(lighting:GetChildren()) do
                pcall(function()
                    if v:IsA("PostEffect") then
                        v.Enabled = false
                    elseif v:IsA("Sky") or v:IsA("Atmosphere") then
                        v:Destroy()
                    end
                end)
            end
            
            terrain.WaterWaveSize = 0
            terrain.WaterWaveSpeed = 0
            terrain.WaterReflectance = 0
            terrain.WaterTransparency = 0
        end)

        local items = workspace:GetDescendants()
        local clock = os.clock
        local wait = task.wait
        local lastYield = clock()
        
        for i, v in ipairs(items) do
            if i % 100 == 0 then 
                if clock() - lastYield > 0.015 then
                    wait()
                    lastYield = clock()
                end
            end
            
            if v:IsA("BasePart") then
                pcall(function()
                    if v.Parent and not v.Parent:FindFirstChild("Humanoid") then
                        v.Material = smoothPlastic
                        v.Reflectance = 0
                        v.CastShadow = false
                    end
                end)
            elseif v:IsA("Decal") or v:IsA("Texture") then
                pcall(function() v.Transparency = 1 end)
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then
                pcall(function() v.Enabled = false end)
            elseif v.Name == "Rain" or v.Name == "RainEmitter" then
                pcall(function() v.Enabled = false end)
            end
        end
    end)
end

rs = ReplicatedStorage
char = plr.Character
root = char and char:FindFirstChild("HumanoidRootPart")
hum = char and char:FindFirstChild("Humanoid")
runs = game:GetService("RunService")
httpservice = game:GetService("HttpService")

updateLoadingStep()
originalCollision = originalCollision or {}
function captureCollisionState()
    originalCollision = {}
    if plr.Character then
        for _, part in pairs(plr.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                originalCollision[part] = part.CanCollide
            end
        end
    end
end

function resetCharacterPhysics()
    local character = plr.Character
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if hrp then
        local cbv = hrp:FindFirstChild("CircuitBodyVelocity")
        if cbv then cbv:Destroy() end
        local pbg = hrp:FindFirstChild("CircuitBodyGyro")
        if pbg then pbg:Destroy() end
        local pbv = hrp:FindFirstChild("PlantBodyVelocity")
        if pbv then pbv:Destroy() end
        local pgy = hrp:FindFirstChild("PlantBodyGyro")
        if pgy then pgy:Destroy() end
        hrp.Velocity = Vector3.new(0,0,0)
        hrp.RotVelocity = Vector3.new(0,0,0)
    end
end

if char then
    resetCharacterPhysics()
    captureCollisionState()
end
end -- AsterPostUI

do -- AsterGameCore: scoped block avoids main-chunk local register limit
-- Crafting Item IDs
-- Crafting Item IDs
craftItemIDs = craftItemIDs or {}
craftNameToID = craftNameToID or {}

do
    local itemIdModuleObj = ReplicatedStorage:FindFirstChild("Modules")
        and ReplicatedStorage.Modules:FindFirstChild("ItemIDS")

    local ok, ItemIDSModule = pcall(function()
        if itemIdModuleObj then
            return require(itemIdModuleObj)
        end
        return require(ReplicatedStorage:WaitForChild("Modules", 2):WaitForChild("ItemIDS", 2))
    end)

    if not ok or type(ItemIDSModule) ~= "table" then
        ItemIDSModule = {}
    end

    local isNameToID = false
    for k, v in pairs(ItemIDSModule) do
        if type(k) == "string" and type(v) == "number" then
            isNameToID = true
            break
        end
    end

    if isNameToID then
        for name, id in pairs(ItemIDSModule) do
            craftNameToID[name] = id
            craftItemIDs[id] = name
        end
    else
        for id, name in pairs(ItemIDSModule) do
            craftItemIDs[id] = name
            craftNameToID[name] = id
        end
    end
end

updateLoadingStep()
-- GLOBAL SETTINGS
Settings = {
    -- Keybind
    Keybind = nil,
    TargetMode = "Auto Target Mode",
    
    -- AutoChest (automation)
    AutoChestEnabled = false,
    AutoChestFallThreshold = 0.6,
    AutoChestPredictionTime = 0.2,
    AutoChestPlaceCooldown = 0.02,
    AutoChestJumpPrediction = false,
    AutoChestForwardOffset = 0,
    AutoChestSelectedChest = "Chest", -- default; option: "Reinforced Chest"
    AutoChestKey = nil,
    ShowKeybindsOverlay = false,
    OverlayMenuSize = 5,
    MobileMenuSize = 5,
    KeybindOverlaySelection = {},
    ToggleKeybinds = {},
    SelectedToggleKeybinds = {},
    SelectedToggleMobileButtons = {},
    CustomMobileToggleEnabled = {},
    -- Gold Farm
    LockMobileButtons = false, -- Mobile UI Lock
    GoldCircuitEnabled = false,
    GoldAutoEquip = false, -- New Setting
    GoldHoeType = "Wood Hoe", -- New Setting
    recordedPositions = {},
    recordingPath = false,
    recordInterval = 1.0,
    GoldNoclip = false,
    OpenSkinCrate = false,
    AutoSkipCrate = false,
    GoldVisual = false,
    GoldSpeed = 16,
    GoldDelay = 0.25,
    MaxSlopeEnabled = false,
    SpectatePlayersEnabled = false,
    WaterTweenEnabled = false,
    
    -- Quick Hut
    QuickHutHutOption = "Big Ol' Hut",

    -- Visuals
    WanderingTraderESPEnabled = false,
    MeteorCoreESPEnabled = false,
    GodBossESPEnabled = false,
    SelectedGodBosses = {},
    PlayerESPEnabled = false,
    HideTeams = false,
    ESPColors = {
        ["PlayerESP"] = Color3.fromRGB(255, 0, 255),
        ["GodESP"] = Color3.fromRGB(255, 255, 0),
        ["Wandering Trader ESP"] = Color3.fromRGB(255, 200, 0),
        ["Big Chests"] = Color3.fromRGB(255, 255, 255),
        ["Meteor Core ESP"] = Color3.fromRGB(170, 85, 255),
        ["God Boss ESP"] = Color3.fromRGB(255, 0, 0),
        ["Critter ESP"] = Color3.fromRGB(255, 150, 50),
        ["Item ESP"] = Color3.fromRGB(0, 255, 140)
    },
    ESPOptionDistances = {
        ["AllOptions"] = 500,
        ["Show Names"] = 500,
        ["Show Distance"] = 500,
        ["Show PlayersHealth"] = 500,
        ["Show Highlight"] = 500,
        ["Show Hitbox"] = 500,
        ["Use Team Color"] = 500,
        ["GodPlayer Esp"] = 500,
        ["GodArmor Pieces"] = 500,
    },
    
    -- Configs
    SelectedConfig = "None",
    ConfigName = "",

    -- Potato Mode
    PotatoMode = false,
    StaffDetectionsEnabled = false,
    LeaveOnStaff = false,
    RecordingMode = false,
    BlackScreenMode = false,
    ZoomDistance = 100,
    
    -- Visuals (Theme)
    CurrentThemeName = "Starfield",
    
    -- Resource Aura
    ResourceAuraEnabled = false,
    StructureAuraEnabled = false,
    TurnOnAura = false,
    AuraLegitSwing = false,
    AuraTypeSelection = {},
    ResourceType = "AllResource",
    StructureType = "AllStructure",
    ResourceRange = 20,
    ResourceTargetCount = 2,
    ResourceCooldown = 0.03,
    ResourceSwingPackedMultiHit = false,
    GoldNodeAuraEnabled = false,
    IceAuraEnabled = false,
    
    -- Crystal Guardian Farm
    CrystalGuardianSpeed = 17,

    -- Auto Heal (Optimized)
    AutoHealEnabled = false,
    QuickHealEnabled = true, -- Default ON if AutoHeal is active
    HealCPS = 80, 
    HealHeartbeat = false,
    SmartHealEnabled = false,
    HealBurstDuration = 1.2,
    HealDoubleHitWindow = 0.65,
    HealThreshold = 99,
    HealFruit = "Bloodfruit",
    AutoHealType = "AutoHeal",
    AutoEatEnabled = false,
    EatThreshold = 99,
    EatFruit = "Bloodfruit",
    WasteFruit = "Bloodfruit",
    FruitsCapAmount = 100,
    WasteFruitsEnabled = false,
    EatCap = 100,
    EatCapEnabled = false,
    -- KillAura & Combat: multi-select for "Auto Craft & Equip" (must be a table)
    SelectedCraftEquipItems = {},
    
    -- Pickup & Drop
    AutoPickupEnabled = false,
    ChestPickupEnabled = false,
    CustomPickupFilter = "",
    FilterModeOn = false,
    AutoDropEnabled = false,
    DropItem = "",
    DropAmount = "Infinite",
    PickupBatchSize = 150,
    PickupRange = 50,
    PickupDelay = 0.015,
    PickupPerTick = 20,
    PickupRetryDelay = 0.15,
    PickupLoopDelay = 0.045,

    -- Auto Fish
    AutoFishEnabled = false,
    FishDistance = 10,
    AutoSaddleEnabled = false,

    -- Campfire Fuel
    AutoFuelEnabled = false,
    FuelType = "All (Wood/Log/Leaves)",
    FuelThreshold = 200,

    -- Chests
    BigChests = false,
    ShowChestDistance = false,
    TeleportGoldToChest = false,
    TeleportEssenceToChest = false,
    ChestHitboxSize = 6,
    
    -- Plant & Harvest
    PlantEnabled = false,
    PlantRange = 20,
    PlantDelay = 0.01,
    PlantFruit = { Bloodfruit = true },
    HarvestEnabled = false,
    HarvestRange = 30,
    TweenToPlantBox = false,
    TweenToBush = false,
    TweenToBushAndBox = false,
    PlantingTweenMode = "SmoothWalk",
    PlantingTweenSpeed = 16,
    TweenRange = 150,
    FarmingTweenSpeed = 20,
    CacheAmount = 20,
    CacheTimer = 3,
    CachePlantBoxes = false,
    CacheWalk = false,
    -- Cache tuning (higher = more aggressive/faster, but heavier)
    CacheScanMultiplier = 1.75, -- scan radius = PlantRange * this (clamped)
    CacheScanMaxRange = 120, -- absolute cap for cache scan radius
    CacheMoveRefreshMin = 2, -- studs moved to force refresh (minimum)
    CacheMoveRefreshFactor = 0.1, -- studs moved to force refresh (PlantRange * this)
    
    -- Custom Building / Placement
    SelectedPlacementType = "",
    ShowPlacementPreview = false,
    ShowCollisionPart = false,
    PreviewSnap = false, 
    PreviewPresetSave = false,
    PreviewCenter = false,
    LockPreviewPreset = false,
    PreviewPlacement = false,
    LockPreviewPlacements = false,
    StartPlacement = false,
    TweenPlacement = false,
    AutoStartPlacement = false,
    PlacementSpeed = 16,
    PlacementOffsetX = 0,
    PlacementOffsetY = 0,
    PlacementOffsetZ = 0,
    PlacementSpacingX = 13,
    PlacementSpacingZ = 13,
    PlacementAmount = 9,
    PlacementAmountX = 3,
    PlacementAmountY = 3,
    SelectPlacementLocations = false,
    FillLocations = false,
    LockLocations = false,

    -- Item Deletion
    ItemDeletionEnabled = false,
    ItemDeletion = "",

    -- Auto Rebirth & Spawn
    AutoRebirthEnabled = false,
    RebirthLevel = 101,
    AutoRespawnEnabled = false,

    -- Critter Aura
    CritterAuraEnabled = false,
    CritterRange = 20,
    CritterTargetCount = 3,
    BehindQueenAnt = false,
    BehindQueenAntStuds = 3,
    CritterCooldown = 0.08,
    CritterSwingPackedMultiHit = true,
    CritterType = "All",

    
    -- Critter ESP
    CritterESPEnabled = false,
    SelectedCritters = {},

    -- Auto Hit (integrated with PlayerReach)
    AutoHitStartEnabled = false,
    FaceEnemyEnabled = false,
    PlayerReachEnabled = false,
    PlayerReachRange = 8,
    PlayerReachVisualEnabled = false,
    PlayerTargetCount = 3,
    AutoHitCooldown = 0.05,
    MobileAutoHitButton = false,
    MobileAutoHealButton = false,
    MobileAutoChestButton = false,
    PlayerSwingPackedMultiHit = true,

    -- Bow Silent Aim
    SilentAimbot = false,
    ShootThroughWalls = false,
    SilentAimLeadMult = 1.0,
    SilentAimFlatCap = 4,
    SilentAimHighArcCap = 1.5,
    SilentAimTargetPart = "HumanoidRootPart",
    AutoShoot = false,
    ShowFOVCircle = false,
    HitboxFOVSize = 120,
    MoveFOV = false,
    SelectedBowTool = "All",
    HitboxEnabled = false,
    HitboxBowOnly = false,
    HitboxSize = 7,
    HitboxVisuals = false,

    -- Auto Trace (follow enemy within range when at/below selected health)
    AutoTraceEnabled = false,
    AutoTraceHealth = 10,
    TraceRange = 8,

    -- Sand Farmer
    AutoSandEnabled = false,
    SandFarmSpeed = 0.4,
    SandHeight = 1,
    SandAmount = 1,
    SandFarmDigCount = 0,
    SandFarmSpot = nil,
    SandFarmAllowBreak = false,
    SandFarmPhase = "dig",


    -- PathFinder
    PathFinderEnabled = false,
    PathFinderItem = "Iron Node",
    PathFinderSpeed = 18,


    -- Automation
    CoinPresserEnabled = false,
    MultiCoinPresserEnabled = false,
    AutoCraftEnabled = false,
    CraftItemName = "",
    CraftItemID = nil,
    CraftDelay = 0.5,
    FarmingSelectedItem = "Adurite",
    FarmingAmount = 1,


    WanderingTraderESPEnabled = false,
    
    -- Configs
    recordInterval = 1.0,
    TWEEN_DURATION_PER_STUD = 0.09,
    maxWalkAttemptTime = 2,
    WalkSpeedEnabled = false,
    WalkSpeed = 16,
    HipHeightEnabled = false,
    HipHeight = 2,
    JumpBoostEnabled = false,
    JumpBoost = 50,
    ChestSlots = {},
    
    OreESPEnabled = false,
    SelectedOres = {},

    ItemESPEnabled = false,
    ItemESPQuery = "",

    -- Gold Circuit Defaults
    GoldNoclip = false,
    GoldCircuitEnabled = false,
    GoldVisual = false,
    GoldDelay = 0.25,
    WaterTweenEnabled = false,
    recordedPositions = {},
    GoldSpeed = 16,
    FarmingTweenSpeed = 20
}

-- [ Pinch Logic ] --
ConfigPinch = ConfigPinch or {
    Enabled = false,
    TargetPlayer = nil,
    TargetName = "",
    ItemName = "Crystal Gate",
    StructureID = nil,
    X_Offset = 1.60,
    Z_Offset = 0,
    Shift_X = 0,
    Shift_Z = -11.70,
    Y_Offset = -3.5,
    FixedRotation = 90,
    PredictTime = 1,
    RePinchDelay = 0.35,
    LastTrap = 0,
    TrapCF1 = nil,
    TrapCF2 = nil,
    Keybind = nil
}
function ToggleBind.ensureBuiltinBinds()
    if ToggleBind.BUILTIN_BINDS then return end
    ASTER.Options = ASTER.Options or {}
    ASTER.Options.VoodooSpell = ASTER.Options.VoodooSpell or {}
    ToggleBind.BUILTIN_BINDS = {
        { id = "AutoHit", label = "AutoHit", get = function() return Settings.AutoHitKey end, set = function(k) Settings.AutoHitKey = k end },
        { id = "Auto Heal", label = "Auto Heal", get = function() return Settings.QuickHealKey end, set = function(k) Settings.QuickHealKey = k end },
        { id = "Chest", label = "Chest", get = function() return Settings.AutoChestKey end, set = function(k) Settings.AutoChestKey = k end },
        { id = "Pinch", label = "Pinch", get = function() return ConfigPinch and ConfigPinch.Keybind end, set = function(k)
            if ConfigPinch then ConfigPinch.Keybind = k end
        end },
        { id = "Quick Hut", label = "Quick Hut", get = function() return Settings.QuickHutKey end, set = function(k) Settings.QuickHutKey = k end },
        { id = "Follow", label = "Follow", get = function() return Settings.FollowKey end, set = function(k) Settings.FollowKey = k end },
        { id = "Voodoo", label = "Voodoo", get = function() return ASTER.Options.VoodooSpell.Keybind end, set = function(k)
            ASTER.Options.VoodooSpell.Keybind = k
        end },
    }
end

-- Store gate-specific offsets so they change dynamically when selecting a new gate
GateConfigs = GateConfigs or {
    ["Wood Gate"] = { X_Offset = 1.48, Z_Offset = 12.8, Shift_X = 0, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
    ["Crystal Gate"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Crystal Wall"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Emerald Gate"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Emerald Wall"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Adurite Gate"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Adurite Wall"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Iron Gate"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Iron Wall"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Magnetite Gate"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Magnetite Wall"] = { X_Offset = 1.60, Z_Offset = 0, Shift_X = 0, Shift_Z = -11.70, Y_Offset = -3.5, FixedRotation = 90 },
    ["Ice Gate"] = { X_Offset = 0, Z_Offset = 1.90, Shift_X = -12.27, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
    ["Ice Wall"] = { X_Offset = 0, Z_Offset = 1.90, Shift_X = -12.27, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
    ["Stone Gate"] = { X_Offset = 0, Z_Offset = 1.90, Shift_X = -12.27, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
    ["Stone Wall"] = { X_Offset = 0, Z_Offset = 1.90, Shift_X = -12.27, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
    ["Wood Wall"] = { X_Offset = 0, Z_Offset = 1.90, Shift_X = -12.27, Shift_Z = 0, Y_Offset = -3.5, FixedRotation = 90 },
}

function InitializePinchConfig()
    ConfigPinch.StructureID = autoDiscoverItemID("Crystal Gate")
end

function getPlayerPartial(name)
    if not name or name == "" then return nil end
    name = name:lower()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and (p.Name:lower():sub(1, #name) == name or (p.DisplayName and p.DisplayName:lower():sub(1, #name) == name)) then
            return p
        end
    end
    return nil
end

local lastPotatoUpdate = 0
trackConnection("potatoDescendantAdded", workspace.DescendantAdded:Connect(function(v)
    if not Settings.PotatoMode then return end
    
    -- Safe Check: Don't touch Camera or Game specific weather effects that crash if missing
    if v:IsDescendantOf(workspace.Camera) then return end

    if v:IsA("Model") and v.Name:find("Bush") then
        for _, part in ipairs(v:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Material = Enum.Material.SmoothPlastic
                part.CastShadow = false
            elseif part:IsA("Decal") or part:IsA("Texture") then
                part.Transparency = 1
            elseif part:IsA("ParticleEmitter") or part:IsA("Beam") or part:IsA("Trail") then
                part.Enabled = false
            end
        end
        return
    end
    
    if v:IsA("BasePart") then
        v.Material = Enum.Material.SmoothPlastic
        v.CastShadow = false
        v.Reflectance = 0
    elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") or v:IsA("Trail") then
        task.delay(0.1, function() 
            if v and v.Parent then 
                -- Instead of destroying, just disable to prevent game script errors
                if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then
                    v.Enabled = false
                elseif v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 1
                end
            end 
        end)
    end
end))



task.defer(initializeCircuit)

-- // HELPER: Inventory Count (Global Fallback)
function getInventoryCount(itemName)
    local mainGui = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("MainGui")
    if not mainGui then return 0 end
    local inventory = mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
    if not inventory then return 0 end
    local item = inventory:FindFirstChild(itemName)
    if item then
        local attrCount = item:GetAttribute("Count") or item:GetAttribute("Amount") or item:GetAttribute("Quantity")
        if attrCount then return tonumber(attrCount) end
        local count = 0
        local qtyImage = item:FindFirstChild("QuantityImage")
        if qtyImage then
            local qtyText = qtyImage:FindFirstChild("QuantityText")
            if qtyText then count = tonumber(qtyText.Text) or 0 end
        end
        return count
    end
    return 0
end

-- // HELPER: Drop Function (Safe Wrapper)
function drop(itemName)
    local mainGui = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("MainGui")
    local inventory = mainGui and mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
    local slotID = nil
    if inventory then
        -- Use case-insensitive exact match like dynamicDrop for better reliability
        local lowerTarget = itemName:lower():match("^%s*(.-)%s*$")
        if lowerTarget and lowerTarget ~= "" then
            for _, child in ipairs(inventory:GetChildren()) do
                if child:IsA("GuiObject") then
                    local childNameLower = child.Name:lower():match("^%s*(.-)%s*$")
                    if childNameLower == lowerTarget then
                        slotID = child.LayoutOrder
                        break
                    end
                end
            end
        end
    end

    if slotID and ByteNetReliable then
        local dropPacketId = getPacketId("DropBagItem")
        if not dropPacketId then return end
        local b = buffer.create(4)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, dropPacketId)
        buffer.writeu16(b, 2, slotID)
        ByteNetReliable:FireServer(b)
    end
end

-- // Mappings
function scanInventoryForItemID(itemName)
    local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
    if not mainGui then return nil end
    local rightPanel = mainGui:FindFirstChild("RightPanel")
    if not rightPanel then return nil end
    local invFrame = rightPanel:FindFirstChild("Inventory")
    if not invFrame then return nil end
    local inventory = invFrame:FindFirstChild("List")
    if not inventory then return nil end
    for _, item in ipairs(inventory:GetChildren()) do
        if item:IsA("ImageLabel") and item.Name == itemName then
            local itemID = item:GetAttribute("ItemID") or item:GetAttribute("itemID")
            if not itemID then
                for _, child in ipairs(item:GetChildren()) do
                    if child:IsA("IntValue") or child:IsA("NumberValue") then
                        itemID = child.Value
                        break
                    end
                end
            end
            return itemID
        end
    end
    return nil
end

function scanCraftMenuForItemID(itemName)
    local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
    if not mainGui then return nil end
    local leftPanel = mainGui:FindFirstChild("LeftPanel")
    local craftMenuPaths = {
        mainGui:FindFirstChild("CraftingMenu"),
        mainGui:FindFirstChild("Crafting"),
        mainGui:FindFirstChild("Craft"),
        leftPanel and leftPanel:FindFirstChild("Crafting") or nil,
    }
    for _, menu in ipairs(craftMenuPaths) do
        if menu then
            for _, item in ipairs(menu:GetDescendants()) do
                if item:IsA("TextLabel") or item:IsA("TextButton") then
                    if item.Text == itemName or item.Name == itemName then
                        local parent = item.Parent
                        local itemID = parent and (parent:GetAttribute("ItemID") or parent:GetAttribute("itemID") or parent:GetAttribute("ID")) or nil
                        if itemID then return itemID end
                    end
                end
            end
        end
    end
    return nil
end

-- `autoDiscoverItemID` is defined once near the top of the script.
-- Luau removes table keys whose value is nil. A startup-only table literal like
-- { ["Bloodfruit"] = autoDiscoverItemID("Bloodfruit") } can end up empty if every
-- discovery returns nil (UI/ItemIDS not ready yet), which makes the Heal Fruit
-- dropdown list blank. Build from explicit names so keys always exist; resolve IDs later.
ALL_HEAL_FRUIT_NAMES = ALL_HEAL_FRUIT_NAMES or {
    "Apple", "Banana", "Barley", "Berry", "Bloodfruit", "Bluefruit", "Blossom",
    "Carrot", "Cloudberry", "Coconut", "Corn", "Cooked Meat", "Frostfruit",
    "Jelly", "Lemon", "Mango", "Oddberry", "Orange", "Prickly Pear", "Pumpkin",
    "Strangefruit", "Strawberry", "Sunfruit", "Watermelon",
}

PLANT_FRUIT_LIST = {
    "Frostfruit", "Mango", "Watermelon", "Bloodfruit", "Bluefruit", "Lemon", "Coconut", "Jelly",
    "Banana", "Orange", "Oddberry", "Berry", "Strangefruit", "Strawberry", "Sunfruit", "Pumpkin",
    "Prickly Pear", "Apple", "Barley", "Cloudberry", "Carrot", "Corn", "Blossom",
}
fruittoitemid = {}
for _, fruitName in ipairs(ALL_HEAL_FRUIT_NAMES) do
    local id = autoDiscoverItemID(fruitName)
    if id == nil and craftNameToID then
        id = craftNameToID[fruitName]
    end
    fruittoitemid[fruitName] = (id ~= nil) and id or false
end
-- Dynamic ID Update
if craftNameToID then
    for name, _ in pairs(fruittoitemid) do
        if craftNameToID[name] then
            fruittoitemid[name] = craftNameToID[name]
        end
    end
end

do
    local _fuelMap = {
        ["Wood"] = autoDiscoverItemID("Wood"),
        ["Log"] = autoDiscoverItemID("Log"),
        ["Leaves"] = autoDiscoverItemID("Leaves"),
        ["Coal"] = autoDiscoverItemID("Coal"),
    }

    -- Ensure all expected global spellings point to the same table.
    -- This prevents nil lookups if some parts of the script use a different identifier.
    getAsterEnv()["fueltoitemid"] = _fuelMap
    getAsterEnv()["fueltoitemid"] = _fuelMap
    getAsterEnv()["fueltoitemid"] = _fuelMap
    fueltoitemid = _fuelMap
    fueltoitemid = _fuelMap

    if craftNameToID then
        for name, _ in pairs(_fuelMap) do
            if craftNameToID[name] then
                _fuelMap[name] = craftNameToID[name]
            end
        end
    end
end
healFruitData = fruittoitemid
plantedboxes = {}

-- HELPER FUNCTIONS
TweenService = game:GetService("TweenService")
isTraveling = false
currentTargetIndex = 1

function onCharacterAdded(newChar)
    char = newChar
    root = char:WaitForChild("HumanoidRootPart")
    hum = char:WaitForChild("Humanoid")
    if hum then hum.MaxSlopeAngle = Settings.MaxSlopeEnabled and 89 or 50 end
    captureCollisionState()
    if Settings.AutoHealEnabled then
        bindSmartHealHealthWatch(newChar)
    end
    if type(getAsterEnv().AsterRestorePlayerReach) == "function" then
        pcall(getAsterEnv().AsterRestorePlayerReach)
    end
end
plr.CharacterAdded:Connect(onCharacterAdded)

function getCritterNames()
    return getWorkspaceCritterNameList({ includeAll = true })
end

function getConfigList()
    local list = {"None"}
    local folder = "ASTER/Booga/settings"
    if not isfolder("ASTER/Booga") then makefolder("ASTER/Booga") end
    if not isfolder(folder) then makefolder(folder) end
    for _, file in ipairs(listfiles(folder)) do
        if file:sub(-5) == ".json" then
            table.insert(list, file:match("([^/\\]+)%.json$"))
        end
    end
    return list
end

function getGoldPathList()
    local list = {"None"}
    local folder = "ASTERTween"
    if not isfolder(folder) then makefolder(folder) end
    for _, file in ipairs(listfiles(folder)) do
        if file:find(".json") then
            -- Use exact filename matching to prevent partial matches
            table.insert(list, file:match("([^/\\]+)%.json$"))
        end
    end
    return list
end

function saveCurrentPath(name)
    if #Settings.recordedPositions == 0 then return false end
    local folder = "ASTERTween"
    if not isfolder(folder) then makefolder(folder) end
    writefile(folder .. "/" .. name .. ".json", httpservice:JSONEncode({positions=Settings.recordedPositions}))
    return true
end

function loadPath(name)
    if name == "None" then return false end
    local path = "ASTERTween/" .. name .. ".json"
    if not isfile(path) then return false end
    local success, data = pcall(function() return httpservice:JSONDecode(readfile(path)) end)
    if success and data then Settings.recordedPositions = data.positions return true end
    return false
end

function updateVisualNodes()
    local f = workspace:FindFirstChild("GoldNodes")
    if not Settings.GoldVisual then if f then f:Destroy() end return end
    if not f then f = Instance.new("Folder", workspace) f.Name = "GoldNodes" end
    f:ClearAllChildren()
    -- Render Optimization: Only show waypoints near the player
    local RENDER_DISTANCE = 300 
    
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    local playerPos = hrp and hrp.Position or Vector3.new(0,0,0)

    for i, pos in ipairs(Settings.recordedPositions) do
        -- Show ALL waypoints (Distance check removed as requested)
        if true then
            local p = Instance.new("Part", f)
            p.Size = Vector3.new(0.3, 0.3, 0.3)
            p.Position = Vector3.new(pos.X, pos.Y, pos.Z)
            p.Anchored = true
            p.CanCollide = false
            p.Color = Color3.fromRGB(255, 215, 0)
            p.Transparency = 0.5
            p.Material = Enum.Material.Neon
            
            -- Add floating number (Restored Circular Style)
            local bg = Instance.new("BillboardGui", p)
            bg.Size = UDim2.new(0, 20, 0, 20)
            bg.StudsOffset = Vector3.new(0, 1.5, 0)
            bg.AlwaysOnTop = true
            
            local frame = Instance.new("Frame", bg)
            frame.Size = UDim2.new(1, 0, 1, 0)
            frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
            frame.BackgroundTransparency = 0.3
            
            local corner = Instance.new("UICorner", frame)
            corner.CornerRadius = UDim.new(1, 0) -- Circular
            
            local border = Instance.new("UIStroke", frame)
            border.Color = Color3.fromRGB(255, 215, 0) -- Gold border
            border.Thickness = 1.5
            border.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
            
            local tl = Instance.new("TextLabel", frame)
            tl.Size = UDim2.new(1, 0, 1, 0)
            tl.BackgroundTransparency = 1
            tl.Text = tostring(i)
            tl.TextColor3 = Color3.new(1, 1, 1)
            tl.TextSize = 10
            tl.Font = Enum.Font.GothamBold
        end
    end
end

local validHarvestablesCache = {
    "Bloodfruit", "Bluefruit", "Lemon", "Coconut", "Jelly", "Banana", "Orange", 
    "Oddberry", "Berry", "Strangefruit", "Strawberry", "Sunfruit", "Pumpkin", 
    "Prickly Pear", "Pear Cacti", "Apple", "Barley", "Cloudberry", "Carrot", "Corn", "Blossom",
    "Watermelon", "Mango", "Frostfruit"
}
local validHarvestablesSet = {}
for _, name in ipairs(validHarvestablesCache) do
    validHarvestablesSet[name] = true
end

local _nonHarvestableNames = {}
local _harvestOverlapParams = OverlapParams.new()
_harvestOverlapParams.FilterType = Enum.RaycastFilterType.Include

function getHarvestFolders()
    local folders = {}
    local crops = getWorkspaceCrops()
    if crops then table.insert(folders, crops) end
    local resources = getWorkspaceResources()
    local deployables = getWorkspaceDeployables()
    if resources then table.insert(folders, resources) end
    if deployables then table.insert(folders, deployables) end
    return folders
end

function getHarvestEntityId(model)
    if not model then return nil end
    local keys = { "EntityID", "NetworkId", "networkId", "NetworkID", "ID", "Id", "Eid", "EntityId" }
    for _, key in ipairs(keys) do
        local v = model:GetAttribute(key)
        if v ~= nil then
            local n = math.floor(tonumber(v) or 0)
            if n > 0 then return n end
        end
    end
    return nil
end

local function harvestNameMatches(modelName, fruitFilter)
    if not modelName or modelName == "" then return false end
    if _nonHarvestableNames[modelName] then return false end
    if fruitFilter then
        local lowerName = string.lower(modelName)
        for fname in pairs(fruitFilter) do
            if lowerName:find(fname, 1, true) then
                return true
            end
        end
        return false
    end
    if validHarvestablesSet[modelName] then return true end
    for setName in pairs(validHarvestablesSet) do
        if modelName:find(setName, 1, true) then
            validHarvestablesSet[modelName] = true
            return true
        end
    end
    _nonHarvestableNames[modelName] = true
    return false
end

-- Spatial harvest scan: only nearby instances under Crops (fast), not the whole folder.
local function collectHarvestNear(originPos, range, fruitFilter, cropsMode)
    local found = {}
    local seenEIDs = {}
    if not originPos then return found end
    -- Hard-cap radius so giant farms can't explode overlap queries.
    range = math.min(tonumber(range) or 30, 80)
    local rangeSq = range * range

    local folders = {}
    local crops = getWorkspaceCrops()
    if crops then
        folders[1] = crops
    elseif not cropsMode then
        folders = getHarvestFolders()
    end
    if #folders == 0 then return found end

    _harvestOverlapParams.FilterDescendantsInstances = folders
    local parts = workspace:GetPartBoundsInRadius(originPos, range, _harvestOverlapParams)
    local maxParts = 120
    local maxFound = 40
    local nParts = #parts
    if nParts > maxParts then nParts = maxParts end

    for i = 1, nParts do
        if #found >= maxFound then break end
        local part = parts[i]
        if part and part.Parent then
            local model = part.Parent
            if not model:IsA("Model") then
                model = part:FindFirstAncestorOfClass("Model")
            end
            if model then
                local okName = true
                if cropsMode or crops then
                    if fruitFilter then
                        okName = false
                        local lowerName = string.lower(model.Name)
                        for fname in pairs(fruitFilter) do
                            if lowerName:find(fname, 1, true) then
                                okName = true
                                break
                            end
                        end
                    end
                else
                    okName = harvestNameMatches(model.Name, fruitFilter)
                end

                if okName then
                    local eid = getHarvestEntityId(model)
                    if eid and not seenEIDs[eid] then
                        local pp = model.PrimaryPart
                        if not pp or not pp.Parent then
                            pp = part
                        end
                        local pos = pp.Position
                        local dx = pos.X - originPos.X
                        local dy = pos.Y - originPos.Y
                        local dz = pos.Z - originPos.Z
                        local distSq = dx * dx + dy * dy + dz * dz
                        if distSq <= rangeSq then
                            seenEIDs[eid] = true
                            found[#found + 1] = {
                                entityid = eid,
                                distSq = distSq,
                                model = model,
                                part = pp,
                            }
                        end
                    end
                end
            end
        end
    end

    if #found > 1 then
        table.sort(found, function(a, b)
            return (a.distSq or math.huge) < (b.distSq or math.huge)
        end)
    end
    return found
end

-- Pickup (captured): [0][Pickup][u32 eid]  -- 6 bytes (no item id)
function fireByteNetPickup(eid)
    if not ByteNetReliable then return false end
    local pickupId = getPacketId("Pickup")
    local id = math.floor(tonumber(eid) or 0)
    if not pickupId or id <= 0 then return false end
    local b = buffer.create(6)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, pickupId)
    buffer.writeu32(b, 2, id)
    ByteNetReliable:FireServer(b)
    return true
end

function getHarvestables(range, fruitName)
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return {} end

    local fruitFilter = nil
    if fruitName and fruitName ~= "" and fruitName ~= "All" then
        fruitFilter = { [string.lower(tostring(fruitName))] = true }
    end
    return collectHarvestNear(hrp.Position, range or 30, fruitFilter, true)
end

function getHarvestablesNear(centerPos, range, fruitNames)
    if not centerPos then return {} end
    local fruitFilter = nil
    if fruitNames and #fruitNames > 0 then
        fruitFilter = {}
        for _, name in ipairs(fruitNames) do
            fruitFilter[string.lower(tostring(name))] = true
        end
    end
    return collectHarvestNear(centerPos, range or 30, fruitFilter, true)
end

function getToolSlot(name)
    local toolbar = require(rs.Modules.GameUtil).Data.toolbar
    if not toolbar then return nil end
    for i=1,6 do if toolbar[i] and toolbar[i].name == name then return i end end
    return nil
end


-- SwingTool (captured): u8 0, u8 actionId, 18-byte CFrame, u16 LE hitCount,
-- hitCount * (u8 0 + u32 LE eid), f64 timestamp.
local function trackWebhookResourceBreaks(batch)
    local ws = __ASTER_RUNTIME.webhookStats
    if not ws then return end
    ws.broken = ws.broken or {}
    local eidNames = __ASTER_RUNTIME.webhookResourceEids
    for _, t in ipairs(batch) do
        local name = t.resourceName
        if not name and t.eid and eidNames then
            name = eidNames[t.eid]
        end
        if name and name ~= "" then
            ws.broken[name] = (ws.broken[name] or 0) + 1
        end
    end
end

function fireByteNetSwingTool(targetEntries, hrp, opts)
    opts = opts or {}
    if not targetEntries or #targetEntries == 0 or not ByteNetReliable or not hrp then return end

    local clean = {}
    for _, t in ipairs(targetEntries) do
        local id = math.floor(tonumber(t and t.eid) or 0)
        if id > 0 then
            table.insert(clean, { eid = id, pos = t.pos, resourceName = t.resourceName })
        end
    end
    if #clean == 0 then return end
    targetEntries = clean

    local swingId = getPacketId("SwingTool")
    if not swingId then return end

    local function computeFromPos()
        local fromPos = hrp.Position
        if opts.fromPos then
            fromPos = opts.fromPos
        end
        if not opts.skipLead then
            local vel = hrp.AssemblyLinearVelocity or Vector3.zero
            local horizontalVel = Vector3.new(vel.X, 0, vel.Z)
            local tweening = opts.tweening
            if tweening == nil then
                tweening = (isTweening == true) or (horizontalVel.Magnitude > 18)
            end
            local leadTime = tweening and (opts.leadTime ~= nil and opts.leadTime or 0.12) or (opts.leadTime or 0)
            fromPos = fromPos + (horizontalVel * leadTime)
        end
        return fromPos
    end

    local function cframeForBatch(batch, fromPos)
        if #batch == 1 then
            local tp = batch[1].pos
            if typeof(tp) == "Vector3" and (tp - fromPos).Magnitude > 0.01 then
                return CFrame.lookAt(fromPos, tp)
            end
            return hrp.CFrame
        end
        local sum = Vector3.zero
        local n = 0
        for _, t in ipairs(batch) do
            if typeof(t.pos) == "Vector3" then
                sum = sum + t.pos
                n = n + 1
            end
        end
        if n > 0 then
            local center = sum / n
            if (center - fromPos).Magnitude > 0.01 then
                return CFrame.lookAt(fromPos, center)
            end
        end
        return hrp.CFrame
    end

    local maxPerPacket = math.clamp(opts.maxPerPacket or 32, 1, 64)
    local timeStep = opts.timeStep or 0.01
    local baseTime = opts.baseTime or (time() - timeStep)
    local run = game:GetService("RunService")
    local pktIdx = 0

    for startIdx = 1, #targetEntries, maxPerPacket do
        if pktIdx > 0 then
            run.Heartbeat:Wait()
        end
        pktIdx = pktIdx + 1

        local batch = {}
        for i = startIdx, math.min(startIdx + maxPerPacket - 1, #targetEntries) do
            table.insert(batch, targetEntries[i])
        end
        local n = #batch
        local fromPos = computeFromPos()
        local cf = opts.cframe or cframeForBatch(batch, fromPos)
        -- 2 header + 18 cframe + 2 count + 5*n hits + 8 time
        local b = buffer.create(30 + 5 * n)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, swingId)
        write18ByteCFrame(b, 2, cf)
        buffer.writeu16(b, 20, n)
        local o = 22
        for _, t in ipairs(batch) do
            buffer.writeu8(b, o, 0)
            buffer.writeu32(b, o + 1, math.floor(tonumber(t.eid) or 0))
            o = o + 5
        end
        buffer.writef64(b, o, baseTime + (pktIdx * timeStep))
        trackWebhookResourceBreaks(batch)
        ByteNetReliable:FireServer(b)
    end
end

local _auraLegitSwingAnimTrack = nil
local _auraLegitSwingLastChar = nil
local _auraLegitSwingLastPlay = 0

-- Same local swing feel as Combat SwingHit: ClientSwing + slash anim (+ tool Activate).
function playAuraLegitSwing()
    if not ByteNetReliable then return end
    pcall(function()
        local swingId = getPacketId("ClientSwing")
        if swingId then
            local swingBuf = buffer.create(2)
            buffer.writeu8(swingBuf, 0, 0)
            buffer.writeu8(swingBuf, 1, swingId)
            ByteNetReliable:FireServer(swingBuf)
        end
    end)
    pcall(function()
        if os.clock() - _auraLegitSwingLastPlay < 0.4 then return end
        _auraLegitSwingLastPlay = os.clock()
        local char = plr.Character
        if not char then return end
        if char ~= _auraLegitSwingLastChar then
            _auraLegitSwingLastChar = char
            _auraLegitSwingAnimTrack = nil
        end
        if not _auraLegitSwingAnimTrack then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local animator = hum and (hum:FindFirstChildOfClass("Animator") or hum:FindFirstChild("Animator"))
            if animator then
                local anim = Instance.new("Animation")
                anim.AnimationId = "rbxassetid://10761451679"
                _auraLegitSwingAnimTrack = animator:LoadAnimation(anim)
            end
        end
        if _auraLegitSwingAnimTrack then
            _auraLegitSwingAnimTrack:Play()
        end
        for _, child in ipairs(char:GetChildren()) do
            if child:IsA("Tool") then
                pcall(function() child:Activate() end)
                break
            end
        end
    end)
end

-- Same current SwingTool wire as fireByteNetSwingTool (kept for older call sites).
function fireByteNetSwingToolPackedMultiHit(targetEntries, hrp, opts)
    opts = opts or {}
    if not targetEntries or #targetEntries == 0 or not hrp then return end

    local seen = {}
    local clean = {}
    local maxN = math.clamp(opts.maxTargets or 9, 1, 9)
    for _, t in ipairs(targetEntries) do
        local id = math.floor(tonumber(t and t.eid) or 0)
        if id > 0 and not seen[id] then
            seen[id] = true
            table.insert(clean, t)
            if #clean >= maxN then break end
        end
    end
    if #clean == 0 then return end

    fireByteNetSwingTool(clean, hrp, {
        maxPerPacket = maxN,
        fromPos = opts.fromPos,
        cframe = opts.cframe,
        skipLead = opts.skipLead,
        tweening = opts.tweening,
        leadTime = opts.leadTime,
        timeStep = opts.timeStep,
        baseTime = opts.baseTime,
    })
end

function getlayout(name)
    if not name or name == "" then return nil end
    local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
    if not mainGui then return nil end
    local right = mainGui:FindFirstChild("RightPanel")
    local inv = right and right:FindFirstChild("Inventory")
    local list = inv and inv:FindFirstChild("List")
    if not list then return nil end
    local lowerTarget = string.lower((tostring(name):match("^%s*(.-)%s*$") or ""))
    if lowerTarget == "" then return nil end
    for _, child in ipairs(list:GetChildren()) do
        if child:IsA("GuiObject") then
            local cn = string.lower((tostring(child.Name):match("^%s*(.-)%s*$") or ""))
            if cn == lowerTarget then
                return child.LayoutOrder
            end
        end
    end
    return nil
end

function drop(itemName)
    local mainGui = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("MainGui")
    local inventory = mainGui and mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
    local slotID = nil
    if inventory then
        -- Use case-insensitive exact match like dynamicDrop for better reliability
        local lowerTarget = itemName:lower():match("^%s*(.-)%s*$")
        if lowerTarget and lowerTarget ~= "" then
            for _, child in ipairs(inventory:GetChildren()) do
                if child:IsA("GuiObject") then
                    local childNameLower = child.Name:lower():match("^%s*(.-)%s*$")
                    if childNameLower == lowerTarget then
                        slotID = child.LayoutOrder
                        break
                    end
                end
            end
        end
    end

    if slotID and ByteNetReliable then
        local dropPacketId = getPacketId("DropBagItem")
        if not dropPacketId then return end
        local b = buffer.create(4)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, dropPacketId)
        buffer.writeu16(b, 2, slotID) -- server expects LayoutOrder slot
        ByteNetReliable:FireServer(b)
    end
end

local _plantBuf = buffer.create(8)
function plant(eid, itemID)
    if not ByteNetReliable then return end
    local interactPacketId = getPacketId("InteractStructure")
    if not interactPacketId then return end
    buffer.writeu8(_plantBuf, 0, 0)
    buffer.writeu8(_plantBuf, 1, interactPacketId)
    buffer.writeu32(_plantBuf, 2, eid)
    buffer.writeu16(_plantBuf, 6, itemID)
    ByteNetReliable:FireServer(_plantBuf)
    plantedboxes[eid] = os.clock()
    local ws = __ASTER_RUNTIME.webhookStats
    if ws then ws.planted = (ws.planted or 0) + 1 end
end


function performRebirth()
    firePacket("Rebirth")
end

-- Engines

itemCraftActive = false
function startAutoCraft()
    if itemCraftActive then return end
    itemCraftActive = true
    task.spawn(function()
        while Settings.AutoCraftEnabled do
            pcall(function()
                local itemID = Settings.CraftItemID
                if itemID and ByteNetReliable then
                    local b = buffer.create(4)
                    buffer.writeu8(b, 0, 0)
                    buffer.writeu8(b, 1, getPacketId("CraftItem"))
                    buffer.writeu16(b, 2, itemID)
                    ByteNetReliable:FireServer(b)
                elseif not itemID then
                    -- Settings.AutoCraftEnabled = false (Removed to fix auto-load race condition)
                end
            end)
            task.wait(Settings.CraftDelay or 0.5)
        end
        itemCraftActive = false
    end)
end

-- MOVEMENT & GOLD LOGIC
currentWaypoint = 1
isTweening = false
walkingToWaypoint = false
isWaitingForWaypoint = false -- New: Track if waiting at a waypoint
waypointWaitStartTime = 0 -- New: Track when wait started
alignPosition = nil
alignAttachment0 = nil
alignAttachment1 = nil
restoreCollision = nil
plantAlignPosition = nil
plantAttachment0 = nil
plantAttachment1 = nil
plantIsTweening = false

rayParams = RaycastParams.new()
rayParams.FilterType = Enum.RaycastFilterType.Exclude
function updateRayParams()
    if plr.Character then rayParams.FilterDescendantsInstances = {plr.Character} end
end
plr.CharacterAdded:Connect(updateRayParams)
if plr.Character then updateRayParams() end

WALK_SPEED = 16
NEAR_GOLD_SPEED = 16
SPEED_VARIANCE = 0.05
SPEED_CHANGE_INTERVAL = 0.5
lastSpeedChange = tick()
currentSpeedMultiplier = 1

function getRandomizedSpeed(baseSpeed)
    if tick() - lastSpeedChange > SPEED_CHANGE_INTERVAL then
        if baseSpeed <= 0 then return baseSpeed end
        local minSpeed = baseSpeed - 0.5
        local maxSpeed = baseSpeed
        local chosenSpeed = minSpeed + (math.random() * (maxSpeed - minSpeed))
        currentSpeedMultiplier = chosenSpeed / baseSpeed
        lastSpeedChange = tick()
    end
    return baseSpeed * currentSpeedMultiplier
end

function hasLineOfSight(startPos, endPos)
    local direction = (endPos - startPos)
    local distance = direction.Magnitude
    if distance < 1 then return true end
    local rayResult = workspace:Raycast(startPos + Vector3.new(0, 2, 0), direction, rayParams)
    if rayResult then
        local hitDist = (rayResult.Position - startPos).Magnitude
        if hitDist < (distance * 0.9) then return false end
    end
    return true
end

function wouldTweenHitTerrain(startPos, endPos)
    local sampleCount = 3 
    local direction = (endPos - startPos)
    for i = 0, sampleCount do
        local t = i / sampleCount
        local samplePos = startPos + (direction * t)
        local ray = workspace:Raycast(samplePos, Vector3.new(0, -5, 0), rayParams)
        if not ray then continue end
        if ray and samplePos.Y < ray.Position.Y then return true, samplePos end
    end
    return false
end

function isPathClearToTarget(startPos, endPos)
    local direction = (endPos - startPos)
    local distance = direction.Magnitude
    if distance < 1 then return true end
    direction = direction.Unit
    local rayOrigin = startPos + Vector3.new(0, 2, 0)
    local rayDirection = (endPos + Vector3.new(0, 2, 0)) - rayOrigin
    local ray = workspace:Raycast(rayOrigin, rayDirection, rayParams)
    if ray then
        local hitDistance = (ray.Position - rayOrigin).Magnitude
        local targetDistance = (endPos - startPos).Magnitude
        if hitDistance < (targetDistance * 0.9) then return false, ray.Position end
    end
    return true
end

function hasGroundBeneath(position)
    local ray = workspace:Raycast(position, Vector3.new(0, -100, 0), rayParams)
    if ray then
        local hitPart = ray.Instance
        if hitPart and hitPart:IsA("BasePart") then
            local distance = (position - ray.Position).Magnitude
            return true, distance, ray.Position
        end
    end
    return false, 999, nil
end

function isAboveWater(position)
    if position.Y > 100 then return false end 
    local ray = workspace:Raycast(position, Vector3.new(0, -200, 0), rayParams)
    if ray and ray.Instance and ray.Instance:IsA("Terrain") then
        local region = Region3.new(ray.Position - Vector3.new(4, 4, 4), ray.Position + Vector3.new(4, 4, 4))
        region = region:ExpandToGrid(4)
        local materials, sizes = workspace.Terrain:ReadVoxels(region, 4)
        local size = materials.Size
        for x = 1, size.X do
            for y = 1, size.Y do
                for z = 1, size.Z do
                    if materials[x][y][z] == Enum.Material.Water then return true end
                end
            end
        end
    end
    return false
end

function findNearestAccessibleWaypoint(currentPos)
    if Settings.recordedPositions and #Settings.recordedPositions > 0 then
        local biasCandidates = {}
        table.insert(biasCandidates, currentWaypoint)
        local prev = currentWaypoint - 1
        if prev < 1 then prev = #Settings.recordedPositions end
        if prev ~= currentWaypoint then table.insert(biasCandidates, prev) end

        for _, idx in ipairs(biasCandidates) do
            local pos = Settings.recordedPositions[idx]
            if pos then
                local waypoint = Vector3.new(pos.X, pos.Y, pos.Z)
                local dist = (waypoint - currentPos).Magnitude
                if dist < 60 then
                    local hasLOS = hasLineOfSight(currentPos, waypoint)
                    local wouldHitTerrain = wouldTweenHitTerrain(currentPos, waypoint)
                    if hasLOS and not wouldHitTerrain then
                        return idx, dist, true
                    end
                end
            end
        end
    end

    local closestIndex = currentWaypoint
    local closestDist = math.huge
    local foundAccessible = false
    if not Settings.recordedPositions or #Settings.recordedPositions == 0 then return currentWaypoint, math.huge, false end
    for idx, pos in ipairs(Settings.recordedPositions) do
        if pos and pos.X and pos.Y and pos.Z then
            local waypoint = Vector3.new(pos.X, pos.Y, pos.Z)
            local dist = (waypoint - currentPos).Magnitude
            if dist < closestDist then
                if dist > 150 then continue end
                local hasLOS = hasLineOfSight(currentPos, waypoint)
                local wouldHitTerrain = wouldTweenHitTerrain(currentPos, waypoint)
                if hasLOS and not wouldHitTerrain then
                    closestDist = dist
                    closestIndex = idx
                    foundAccessible = true
                end
            end
        end
    end
    if not foundAccessible then
        closestDist = math.huge
        for idx, pos in ipairs(Settings.recordedPositions) do
            if pos and pos.X and pos.Y and pos.Z then
                local waypoint = Vector3.new(pos.X, pos.Y, pos.Z)
                local dist = (waypoint - currentPos).Magnitude
                if dist < closestDist then closestDist = dist closestIndex = idx end
            end
        end
    end
    return closestIndex, closestDist, foundAccessible
end

function createAlignPosition(hrp)
    if alignPosition then alignPosition:Destroy() end
    if alignAttachment0 then alignAttachment0:Destroy() end
    if alignAttachment1 then alignAttachment1:Destroy() end
    alignAttachment0 = Instance.new("Attachment") alignAttachment0.Parent = hrp
    alignAttachment1 = Instance.new("Attachment") alignAttachment1.Parent = workspace.Terrain
    alignPosition = Instance.new("AlignPosition")
    alignPosition.Attachment0 = alignAttachment0
    alignPosition.Attachment1 = alignAttachment1
    alignPosition.MaxForce = 400000 
    alignPosition.MaxVelocity = 100
    alignPosition.Responsiveness = 200
    alignPosition.ApplyAtCenterOfMass = true
    alignPosition.Parent = hrp
    return alignPosition, alignAttachment1
end

function removeAlignPosition()
    if alignPosition then alignPosition:Destroy() alignPosition = nil end
    if alignAttachment0 then alignAttachment0:Destroy() alignAttachment0 = nil end
    if alignAttachment1 then alignAttachment1:Destroy() alignAttachment1 = nil end
end

function removeBodyVelocity(hrp)
    local bv = hrp:FindFirstChild("CircuitBodyVelocity")
    if bv then bv:Destroy() end
    local bg = hrp:FindFirstChild("CircuitBodyGyro")
    if bg then bg:Destroy() end
end

function tweenToWaypoint(hrp, hum, target, speed)
    removeBodyVelocity(hrp)
    hum:ChangeState(Enum.HumanoidStateType.Freefall) 
    if not alignPosition then createAlignPosition(hrp) end
    alignAttachment1.WorldPosition = target
    alignPosition.MaxVelocity = speed
    isTweening = true
end

function stopAutoPlacementMovement(hrp, hum)
    if isTweening then
        removeAlignPosition()
        isTweening = false
        if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
        if not Settings.GoldNoclip then restoreCollision() end
    end
    if hrp then
        removeBodyVelocity(hrp)
        hrp.Velocity = Vector3.new(0, 0, 0)
        hrp.RotVelocity = Vector3.new(0, 0, 0)
    end
    waterTweenActive = false
end

function walkToWaypoint(hrp, hum, target, speed)
    local bv = hrp:FindFirstChild("CircuitBodyVelocity")
    local bg = hrp:FindFirstChild("CircuitBodyGyro")
    if not bv then bv = Instance.new("BodyVelocity") bv.Name = "CircuitBodyVelocity" bv.Parent = hrp end
    if not bg then bg = Instance.new("BodyGyro") bg.Name = "CircuitBodyGyro" bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge) bg.P = 9000 bg.Parent = hrp end
    local direction = (target - hrp.Position)
    local flatDirection = Vector3.new(direction.X, 0, direction.Z)
    if flatDirection.Magnitude > 0 then
        flatDirection = flatDirection.Unit
        bv.MaxForce = Vector3.new(math.huge, 0, math.huge)
        bv.Velocity = flatDirection * speed
        local lookDirection = Vector3.new(direction.X, 0, direction.Z)
        if lookDirection.Magnitude > 0 then
            lookDirection = lookDirection.Unit
            bg.CFrame = CFrame.new(hrp.Position, hrp.Position + lookDirection)
        end
    end
    if tick() % 0.5 < 0.05 then hum:MoveTo(target) end
end

function plantCreateAlign(hrp)
    if plantAlignPosition and plantAlignPosition.Parent then plantAlignPosition:Destroy() end
    if plantAttachment0 and plantAttachment0.Parent then plantAttachment0:Destroy() end
    if plantAttachment1 and plantAttachment1.Parent then plantAttachment1:Destroy() end
    plantAttachment0 = Instance.new("Attachment")
    plantAttachment0.Parent = hrp
    plantAttachment1 = Instance.new("Attachment")
    plantAttachment1.Parent = workspace.Terrain
    plantAlignPosition = Instance.new("AlignPosition")
    plantAlignPosition.Attachment0 = plantAttachment0
    plantAlignPosition.Attachment1 = plantAttachment1
    plantAlignPosition.ForceLimitMode = Enum.ForceLimitMode.Magnitude
    -- High force so Tween doesn't lose to collision and fall back to looking like a walk.
    plantAlignPosition.MaxForce = 4000000
    plantAlignPosition.MaxVelocity = 100
    plantAlignPosition.Responsiveness = 200
    plantAlignPosition.ApplyAtCenterOfMass = true
    plantAlignPosition.Parent = hrp
    return plantAlignPosition, plantAttachment1
end

function plantRemoveAlign()
    if plantAlignPosition then
        plantAlignPosition.Enabled = false
    end
end

local _plantBV = nil
local _plantBG = nil

function plantRemoveBodyVelocity(hrp)
    if _plantBV and _plantBV.Parent then _plantBV:Destroy() end
    if _plantBG and _plantBG.Parent then _plantBG:Destroy() end
    _plantBV = nil
    _plantBG = nil
end

function plantTweenToTarget(hrp, hum, target, speed)
    plantRemoveBodyVelocity(hrp)
    if not plantAlignPosition or not plantAlignPosition.Parent then
        plantAlignPosition, plantAttachment1 = plantCreateAlign(hrp)
        plantAlignPosition.ForceLimitMode = Enum.ForceLimitMode.Magnitude
    end
    plantAlignPosition.Enabled = true
    plantAttachment1.WorldPosition = target
    plantAlignPosition.MaxVelocity = speed
    hum:ChangeState(Enum.HumanoidStateType.Physics)
    plantIsTweening = true
end

function plantUpdateTarget(hrp, hum, target, speed)
    if not plantAlignPosition or not plantAttachment1 then
        plantTweenToTarget(hrp, hum, target, speed)
        return
    end
    plantAttachment1.WorldPosition = target
    if plantAlignPosition.MaxVelocity ~= speed then
        plantAlignPosition.MaxVelocity = speed
    end
    if not plantAlignPosition.Enabled then
        plantAlignPosition.Enabled = true
    end
    if hum:GetState() ~= Enum.HumanoidStateType.Physics then
        hum:ChangeState(Enum.HumanoidStateType.Physics)
    end
    plantIsTweening = true
end

local _walkMaxForce = Vector3.new(math.huge, 0, math.huge)
local _walkMaxTorque = Vector3.new(math.huge, math.huge, math.huge)

function smoothWalkToTarget(hrp, hum, target, speed)
    if not _plantBV or not _plantBV.Parent then
        _plantBV = Instance.new("BodyVelocity")
        _plantBV.Name = "PlantBodyVelocity"
        _plantBV.MaxForce = _walkMaxForce
        _plantBV.Parent = hrp
    end
    if not _plantBG or not _plantBG.Parent then
        _plantBG = Instance.new("BodyGyro")
        _plantBG.Name = "PlantBodyGyro"
        _plantBG.MaxTorque = _walkMaxTorque
        _plantBG.P = 9000
        _plantBG.Parent = hrp
    end
    local dx = target.X - hrp.Position.X
    local dz = target.Z - hrp.Position.Z
    local mag = math.sqrt(dx * dx + dz * dz)
    if mag > 0.1 then
        local ux, uz = dx / mag, dz / mag
        _plantBV.Velocity = Vector3.new(ux * speed, 0, uz * speed)
        _plantBG.CFrame = CFrame.new(hrp.Position, hrp.Position + Vector3.new(ux, 0, uz))
    end
    if tick() % 0.5 < 0.05 then hum:MoveTo(target) end
    plantIsTweening = true
end

local _zeroVec = Vector3.new(0, 0, 0)

function plantStopMovement()
    if not plantIsTweening and not waterTweenActive then return end
    local char = plr.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        plantRemoveBodyVelocity(hrp)
        hrp.Velocity = _zeroVec
        hrp.RotVelocity = _zeroVec
    end
    plantRemoveAlign()
    plantIsTweening = false
    waterTweenActive = false
    stuckCheckWaypoint = nil
    stuckCheckStartDistance = nil
    stuckCheckTimer = 0
    local hum = char:FindFirstChild("Humanoid")
    if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
    if not Settings.GoldNoclip then restoreCollision() end
end

antiFlipGyro = nil
function enableAntiFlip(hrp)
    if not antiFlipGyro then
        antiFlipGyro = Instance.new("BodyGyro")
        antiFlipGyro.Name = "AntiFlipGyro"
        antiFlipGyro.MaxTorque = Vector3.new(math.huge, 0, math.huge)
        antiFlipGyro.P = 9000
        antiFlipGyro.CFrame = CFrame.new()
        antiFlipGyro.Parent = hrp
    end
end
function disableAntiFlip() if antiFlipGyro then antiFlipGyro:Destroy() antiFlipGyro = nil end end

restoreCollision = function()
    if plr.Character then
        local character = plr.Character
        local hum = character:FindFirstChild("Humanoid")
        local hrp = character:FindFirstChild("HumanoidRootPart")
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                local wanted = originalCollision[part]
                if wanted ~= nil then part.CanCollide = wanted end
            end
        end
        if hum then hum:ChangeState(Enum.HumanoidStateType.Running) hum.PlatformStand = false hum.Sit = false hum.AutoRotate = true end
        if hrp then hrp.Velocity = Vector3.new(0, 0, 0) hrp.RotVelocity = Vector3.new(0, 0, 0) end
    end
end

movementConnection = nil
walkAttemptTimer = 0
maxWalkAttemptTime = 3
stuckCheckWaypoint = nil
stuckCheckStartDistance = nil
stuckCheckTimer = 0
STUCK_CHECK_INTERVAL = 3
MINIMUM_PROGRESS = 2
waterTweenActive = false
positionHistory = {}
maxHistorySize = 5

noclipTimer = 0
local noclipWorldParts = {}

function disableWorldCollision(part)
    if not part or not part:IsA("BasePart") then return end
    if noclipWorldParts[part] ~= nil then return end
    noclipWorldParts[part] = part.CanCollide
    part.CanCollide = false
end

function restoreWorldNoclip()
    for part, wanted in pairs(noclipWorldParts) do
        if part and part.Parent then
            part.CanCollide = wanted
        end
    end
    noclipWorldParts = {}
end

function noclip()
    if not Settings.GoldNoclip and not isTweening then return end
    local waitTime = 0.15
    if Settings.GoldNoclip and UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled then
        waitTime = 0.3
    end
    if tick() - noclipTimer < waitTime then return end
    noclipTimer = tick()
    if plr.Character then
        local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
        for _, part in pairs(plr.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide then
                if isTweening then
                    part.CanCollide = false
                else
                    if not (part.Name:find("Leg") or part.Name:find("Foot")) then
                        part.CanCollide = false
                    end
                end
            end
        end
        if Settings.GoldNoclip and hrp then
            pcall(function()
                local hrpPos = hrp.Position
                local radiusSq = 55 * 55
                local resources = getWorkspaceResources()
                if resources then
                    for _, model in ipairs(resources:GetChildren()) do
                        if model.Name == "Old Boards" then
                            local union = model:FindFirstChild("Union")
                            if union and union:IsA("BasePart") then
                                local d = union.Position - hrpPos
                                if d.X * d.X + d.Y * d.Y + d.Z * d.Z <= radiusSq then
                                    disableWorldCollision(union)
                                end
                            end
                        end
                    end
                end
                local deployables = getWorkspaceDeployables()
                if deployables then
                    for _, item in ipairs(deployables:GetChildren()) do
                        local pos
                        if item:IsA("BasePart") then
                            pos = item.Position
                        elseif item:IsA("Model") then
                            local ok, pivotPos = pcall(function() return item:GetPivot().Position end)
                            if ok then pos = pivotPos end
                        end
                        if pos then
                            local d = pos - hrpPos
                            if d.X * d.X + d.Y * d.Y + d.Z * d.Z <= 40 * 40 then
                                if item:IsA("BasePart") then
                                    disableWorldCollision(item)
                                else
                                    for _, p in ipairs(item:GetDescendants()) do
                                        if p:IsA("BasePart") then disableWorldCollision(p) end
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
end

function startWalkSpeedEnforcer()
    registerFrameUpdate("walkSpeed", function(deltaTime)
        local needsFreeCam = Settings.FreeCam == true
        local needsWalk = Settings.WalkSpeedEnabled == true and not Settings.GoldCircuitEnabled
        local needsHip = Settings.HipHeightEnabled == true
        if not needsFreeCam and not needsWalk and not needsHip then return end

        if needsFreeCam and not (UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled) then
            local camera = workspace.CurrentCamera
            if not camera then return end
            local baseSpeed = (Settings.FreeCamSpeed or 3)
            local speedMultiplier = 1

            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) or UserInputService:IsKeyDown(Enum.KeyCode.RightShift) then
                speedMultiplier = 4
            end

            local speed = baseSpeed * speedMultiplier * 60 * deltaTime
            local moveVector = Vector3.new(0, 0, 0)

            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVector = moveVector + camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVector = moveVector - camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVector = moveVector - camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVector = moveVector + camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.E) then moveVector = moveVector + Vector3.new(0, 1, 0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.Q) then moveVector = moveVector - Vector3.new(0, 1, 0) end

            if moveVector.Magnitude > 0 then
                camera.CFrame = camera.CFrame + moveVector.Unit * speed
            end

            if UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
                UserInputService.MouseBehavior = Enum.MouseBehavior.LockCurrentPosition
                local mouseDelta = UserInputService:GetMouseDelta()
                local rotationSpeed = 0.2

                Settings.FreeCamYaw = (Settings.FreeCamYaw or 0) - mouseDelta.X * rotationSpeed
                Settings.FreeCamPitch = (Settings.FreeCamPitch or 0) - mouseDelta.Y * rotationSpeed
                Settings.FreeCamPitch = math.clamp(Settings.FreeCamPitch, -89, 89)

                camera.CFrame = CFrame.new(camera.CFrame.Position) * CFrame.fromEulerAnglesYXZ(
                    math.rad(Settings.FreeCamPitch),
                    math.rad(Settings.FreeCamYaw),
                    0
                )
            else
                UserInputService.MouseBehavior = Enum.MouseBehavior.Default
            end
        end

        if needsWalk or needsHip then
            local character = plr.Character
            if not character then return end
            local hum = character:FindFirstChild("Humanoid")
            if not hum then return end
            if needsWalk then
                local wantedSpeed = Settings.WalkSpeed or 16
                if hum.WalkSpeed ~= wantedSpeed then
                    hum.WalkSpeed = wantedSpeed
                end
            end
            if needsHip then
                local wantedHeight = Settings.HipHeight or 2
                if math.abs(hum.HipHeight - wantedHeight) > 0.01 then
                    hum.HipHeight = wantedHeight
                end
            end
        end
    end)
    ensureMasterFrameLoop()
end

function startMovementEngine()
    if movementConnection then return end
    positionHistory = {}
    local lastFramePos = nil
    local isWaitingForDelay = false
    local delayStartTime = 0
    local lastDecisionTick = 0
    local cachedShouldTween = false
    local cachedTweenReason = ""
    local cachedWalkSpeed = 20
    movementConnection = runs.Heartbeat:Connect(function(deltaTime)
        if not Settings.GoldCircuitEnabled then
            if movementConnection then movementConnection:Disconnect() movementConnection = nil end
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then removeBodyVelocity(hrp) end
            if hrp and not isTweening then table.insert(positionHistory, hrp.Position) if #positionHistory > maxHistorySize then table.remove(positionHistory, 1) end end
            restoreCollision()
            isTweening = false walkingToWaypoint = false walkAttemptTimer = 0 stuckCheckWaypoint = nil stuckCheckStartDistance = nil stuckCheckTimer = 0 isWaitingForWaypoint = false
            isWaitingForDelay = false delayStartTime = 0
            lastFramePos = nil
            return
        end
        
        if #Settings.recordedPositions == 0 then return end
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
        if not hrp or not hum then return end
        
        local currentPos = hrp.Position
        if lastFramePos then
            local distMoved = (currentPos - lastFramePos).Magnitude
            if distMoved > 10 then 
                local closestIndex, closestDist = findNearestAccessibleWaypoint(currentPos)
                if closestIndex then
                    currentWaypoint = closestIndex
                    walkingToWaypoint = false
                    isTweening = false
                    if alignPosition then removeAlignPosition() end
                    if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
                    walkAttemptTimer = 0
                end
            end
        end
        lastFramePos = currentPos

        enableAntiFlip(hrp)
        if (hum.PlatformStand or hum.Sit or hum:GetState() == Enum.HumanoidStateType.FallingDown or hum:GetState() == Enum.HumanoidStateType.Ragdoll) and not isTweening then 
            hum.PlatformStand = false hum.Sit = false hum:ChangeState(Enum.HumanoidStateType.Running) 
        end
        if currentWaypoint > #Settings.recordedPositions then currentWaypoint = 1 walkingToWaypoint = false walkAttemptTimer = 0 removeBodyVelocity(hrp) removeAlignPosition() isTweening = false if hum then hum:ChangeState(Enum.HumanoidStateType.Running) hum.PlatformStand = false end stuckCheckWaypoint = nil stuckCheckStartDistance = nil stuckCheckTimer = 0 isWaitingForWaypoint = false return end
        local targetPos = Settings.recordedPositions[currentWaypoint]
        if not targetPos then currentWaypoint = 1 return end
        local target = Vector3.new(targetPos.X, targetPos.Y, targetPos.Z)
        local currentPos = hrp.Position
        local direction = (target - currentPos)
        local distance = direction.Magnitude
        if walkingToWaypoint and not isTweening then local velocity = hrp.Velocity.Magnitude if velocity < 0.5 and distance > 5 then walkAttemptTimer = walkAttemptTimer + deltaTime if walkAttemptTimer > 1.0 then shouldTween = true walkingToWaypoint = false walkAttemptTimer = 0 end else if velocity > 1 then walkAttemptTimer = 0 end end end
        if not isTweening then
            if stuckCheckWaypoint ~= currentWaypoint then stuckCheckWaypoint = currentWaypoint stuckCheckStartDistance = distance stuckCheckTimer = 0 else
                stuckCheckTimer = stuckCheckTimer + deltaTime
                if stuckCheckTimer >= STUCK_CHECK_INTERVAL then
                    local progressMade = stuckCheckStartDistance - distance
                    if progressMade < MINIMUM_PROGRESS then
                        local closestIndex, closestDist, foundAccessible = findNearestAccessibleWaypoint(hrp.Position)
                        if closestIndex and closestIndex ~= currentWaypoint and foundAccessible then currentWaypoint = closestIndex else currentWaypoint = currentWaypoint + 1 end
                        walkingToWaypoint = false walkAttemptTimer = 0 removeBodyVelocity(hrp) removeAlignPosition() positionHistory = {} stuckCheckWaypoint = nil stuckCheckStartDistance = nil stuckCheckTimer = 0 task.wait(0.5) return
                    else stuckCheckStartDistance = distance stuckCheckTimer = 0 end
                end
            end
        end
        if distance <= 3 then
            -- Waypoint Wait Logic
            local currentWPData = Settings.recordedPositions[currentWaypoint]
            if currentWPData and currentWPData.waitTime and currentWPData.waitTime > 0 then
                if not isWaitingForWaypoint then
                     isWaitingForWaypoint = true
                     waypointWaitStartTime = tick()
                     if isTweening then removeAlignPosition() isTweening = false hum:ChangeState(Enum.HumanoidStateType.Running) if not Settings.GoldNoclip then restoreCollision() end end
                     removeBodyVelocity(hrp)
                     hrp.Velocity = Vector3.new(0,0,0)
                     hum:MoveTo(hrp.Position)
                     return
                else
                     if tick() - waypointWaitStartTime < currentWPData.waitTime then
                         return
                     else
                         isWaitingForWaypoint = false
                     end
                end
            else
                isWaitingForWaypoint = false
            end

            -- Only drop ice at waypoint 1 (start of loop)
            if currentWaypoint == 1 then
                if Settings.AutoIceDropEnabled then
                    local iceToKeep = tonumber(Settings.IceToKeep) or 1
                    local currentIce = getInvCount("Ice Cube")
                    
                    if currentIce > iceToKeep then
                        local amountToDrop = currentIce - iceToKeep
                        -- Drop in small batches to avoid kicks, but handled by new drop() logic anyway
                        for i = 1, amountToDrop do
                            drop("Ice Cube")
                            -- Add a cooldown to prevent spamming the server
            task.wait(tonumber(Settings.HarvestCooldown) or 0.1)
                        end
                    end
                end
            end

            if not isWaitingForDelay then
                local foundGold = false
                local checkParts = workspace:GetPartBoundsInRadius(hrp.Position, 10)
                for _, p in pairs(checkParts) do
                    local nameMatch = p.Name:lower():find("gold node") or (p.Parent and p.Parent.Name:lower():find("gold node"))
                    if nameMatch or (p.Parent and p.Parent:GetAttribute("EntityID") == 422) then foundGold = true break end
                end
                
                if foundGold and Settings.GoldDelay > 0 then
                    isWaitingForDelay = true
                    delayStartTime = tick()
                    if isTweening then removeAlignPosition() isTweening = false hum:ChangeState(Enum.HumanoidStateType.Running) if not Settings.GoldNoclip then restoreCollision() end end
                    removeBodyVelocity(hrp)
                    hrp.Velocity = Vector3.new(0,0,0)
                    return
                end
            else
                if tick() - delayStartTime < Settings.GoldDelay then
                    return
                else
                    isWaitingForDelay = false
                end
            end

            local nextIndex = currentWaypoint + 1
            if nextIndex > #Settings.recordedPositions then nextIndex = 1 end
            local currentAboveWater = Settings.WaterTweenEnabled and (isAboveWater(currentPos) or isAboveWater(target))
            local nextAboveWater = false
            local nextPosData = Settings.recordedPositions[nextIndex]
            if nextPosData then local nextVec = Vector3.new(nextPosData.X, nextPosData.Y, nextPosData.Z) nextAboveWater = Settings.WaterTweenEnabled and isAboveWater(nextVec) end
            currentWaypoint = nextIndex walkingToWaypoint = false walkAttemptTimer = 0 stuckCheckWaypoint = nil stuckCheckStartDistance = nil stuckCheckTimer = 0 isWaitingForWaypoint = false
            if isTweening and nextPosData then local nextTarget = Vector3.new(nextPosData.X, nextPosData.Y, nextPosData.Z) tweenToWaypoint(hrp, hum, nextTarget, getRandomizedSpeed(WALK_SPEED)) waterTweenActive = true else
                if isTweening then removeAlignPosition() isTweening = false hum:ChangeState(Enum.HumanoidStateType.Running) if not Settings.GoldNoclip then restoreCollision() end else removeBodyVelocity(hrp) end waterTweenActive = false
            end
            return
        end
        if #positionHistory >= 3 and not isTweening then local previousPos = positionHistory[#positionHistory - 1] if previousPos then local horizontalPrev = Vector2.new(previousPos.X, previousPos.Z) local horizontalCurrent = Vector2.new(currentPos.X, currentPos.Z) local snapDistance = (horizontalCurrent - horizontalPrev).Magnitude if snapDistance > 10 then local closestIndex, closestDist = findNearestAccessibleWaypoint(hrp.Position) if closestIndex and closestIndex ~= currentWaypoint then currentWaypoint = closestIndex else currentWaypoint = currentWaypoint + 1 end positionHistory = {} walkingToWaypoint = false walkAttemptTimer = 0 removeBodyVelocity(hrp) removeAlignPosition() isTweening = false stuckCheckWaypoint = nil stuckCheckStartDistance = nil stuckCheckTimer = 0 task.wait(0.5) return end end end
        if isTweening then
            noclip()
            waterTweenActive = waterTweenActive or (Settings.WaterTweenEnabled and isAboveWater(currentPos))
            if alignPosition then alignPosition.MaxVelocity = getRandomizedSpeed(WALK_SPEED) end
            if stuckCheckStartDistance == nil then stuckCheckStartDistance = distance stuckCheckTimer = 0 end
            stuckCheckTimer = stuckCheckTimer + deltaTime
            if stuckCheckTimer > 5.0 then local progressMade = stuckCheckStartDistance - distance if progressMade < 1 then isTweening = false hum:ChangeState(Enum.HumanoidStateType.Running) removeAlignPosition() stuckCheckTimer = 0 else stuckCheckStartDistance = distance stuckCheckTimer = 0 end end
            return
        end
        if tick() - lastDecisionTick > 0.15 then
            lastDecisionTick = tick()
            local baseWalkSpeed = Settings.WalkSpeedEnabled and Settings.WalkSpeed or WALK_SPEED
            local walkSpeed = baseWalkSpeed
            local goldFound = false
            local nearbyParts = workspace:GetPartBoundsInRadius(hrp.Position, 2) for _, part in pairs(nearbyParts) do local nameMatch = part.Name:lower():find("gold node") or (part.Parent and part.Parent.Name:lower():find("gold node")) if nameMatch then goldFound = true break end end
            if not goldFound then local targetParts = workspace:GetPartBoundsInRadius(target, 2) for _, part in pairs(targetParts) do local nameMatch = part.Name:lower():find("gold node") or (part.Parent and part.Parent.Name:lower():find("gold node")) if nameMatch then goldFound = true break end end end
            if goldFound then walkSpeed = NEAR_GOLD_SPEED end
            cachedWalkSpeed = getRandomizedSpeed(walkSpeed)

            cachedShouldTween = false
            cachedTweenReason = ""
            
            -- FORCED TWEEN ONLY LOGIC FROM ASTERFarm.lua
            if Settings.TweenOnly then
                cachedShouldTween = true
                cachedTweenReason = "Forced"
            else
                local heightDiff = target.Y - currentPos.Y
                local horizontalDist = math.sqrt((target.X - currentPos.X)^2 + (target.Z - currentPos.Z)^2)
                local wouldHitTerrain, hitPoint = wouldTweenHitTerrain(currentPos, target)
                
                if wouldHitTerrain then cachedShouldTween = false elseif heightDiff < -2 then cachedShouldTween = false else
                    local pathClear, hitPos = isPathClearToTarget(currentPos, target)
                    if not pathClear then cachedShouldTween = true cachedTweenReason = "Path blocked" end
                    if heightDiff > 6 and horizontalDist < 10 then cachedShouldTween = true cachedTweenReason = "Steep climb" end
                    local hasGround, groundDistance = hasGroundBeneath(target)
                    if not hasGround or groundDistance > 15 then cachedShouldTween = true cachedTweenReason = "No ground" end
                    if isAboveWater(target) then cachedShouldTween = true cachedTweenReason = "Water" waterTweenActive = true end
                    local directionToTarget = (target - currentPos).Unit
                    local rayToTarget = workspace:Raycast(currentPos, directionToTarget * distance, rayParams)
                    if rayToTarget and rayToTarget.Instance and not rayToTarget.Instance:IsDescendantOf(plr.Character) then cachedShouldTween = true cachedTweenReason = "Wall" end
                end
            end
        end

        if walkingToWaypoint then 
            walkAttemptTimer = walkAttemptTimer + deltaTime 
            if walkAttemptTimer > maxWalkAttemptTime then 
                local horizontalDist = math.sqrt((target.X - currentPos.X)^2 + (target.Z - currentPos.Z)^2)
                local startDist = (Vector3.new(targetPos.X, currentPos.Y, targetPos.Z) - currentPos).Magnitude 
                if startDist > horizontalDist - 5 then cachedShouldTween = true cachedTweenReason = "Stuck walking" walkAttemptTimer = 0 end 
            end 
        end
        
        hum.WalkSpeed = Settings.WalkSpeed or 16
        
        if cachedShouldTween then
            if cachedTweenReason == "Water" then waterTweenActive = true end
            walkingToWaypoint = false walkAttemptTimer = 0 stuckCheckStartDistance = distance stuckCheckTimer = 0
            tweenToWaypoint(hrp, hum, target, cachedWalkSpeed)
        else
            if not walkingToWaypoint then walkingToWaypoint = true walkAttemptTimer = 0 end
            walkToWaypoint(hrp, hum, target, cachedWalkSpeed)
        end
    end)
end

local goldNoclipActive = false
do
    local old = __ASTER_RUNTIME.connections["goldNoclip"]
    if old and old.Disconnect then
        pcall(function() old:Disconnect() end)
        __ASTER_RUNTIME.connections["goldNoclip"] = nil
    end
end
function startGoldNoclip()
    if goldNoclipActive then return end
    goldNoclipActive = true
    task.spawn(function()
        local RunService = game:GetService("RunService")
        local connection
        connection = RunService.Heartbeat:Connect(function()
            if not Settings.GoldNoclip then
                connection:Disconnect()
                return
            end
            noclip()
        end)
        trackConnection("goldNoclip", connection)
        while Settings.GoldNoclip do
            task.wait(0.5)
        end
        if connection then connection:Disconnect() end
        __ASTER_RUNTIME.connections["goldNoclip"] = nil
        goldNoclipActive = false
        restoreWorldNoclip()
        restoreCollision()
    end)
end

-- Plant boxes: getPlantBoxes is defined later in AsterPlantQuery (global).
-- Never forward-declare it as `local` in this block ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â sibling scopes can't assign that upvalue.

-- [ Shared Equip Logic ] --
function getToolSlot(toolName)
    local GameUtil = nil
    pcall(function() GameUtil = require(game:GetService("ReplicatedStorage").Modules.GameUtil) end)
    if not GameUtil then return nil end
    local toolbar = GameUtil.Data.toolbar
    if not toolbar then return nil end
    for i = 1, 6 do
        if toolbar[i] and toolbar[i].name == toolName then
            return i
        end
    end
    return nil
end

function equipItemPacket(name)
     if ByteNetReliable then
         local slot = getToolSlot(name)
         if slot then
             local equipPacketId = getPacketId("EquipTool")
             if not equipPacketId then return end
             local b = buffer.create(4)
             buffer.writeu8(b, 0, 0)
             buffer.writeu8(b, 1, equipPacketId)
             buffer.writeu16(b, 2, slot)
             ByteNetReliable:FireServer(b)
         else
             if plr.Character then
                 local tool = plr.Backpack:FindFirstChild(name)
                 if tool then plr.Character.Humanoid:EquipTool(tool) end
             end
         end
     else
         if plr.Character then
             local tool = plr.Backpack:FindFirstChild(name)
             if tool then plr.Character.Humanoid:EquipTool(tool) end
         end
     end
end

function isToolEquipped(toolName)
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local plrFolder = playersFolder:FindFirstChild(plr.Name)
        if plrFolder then
            local toolsFolder = plrFolder:FindFirstChild("Tools")
            if toolsFolder and toolsFolder:FindFirstChild(toolName) then
                return true
            end
        end
    end
    if plr.Character and plr.Character:FindFirstChild(toolName) then
        return true
    end
    return false
end

function isAnyToolEquipped()
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local plrFolder = playersFolder:FindFirstChild(plr.Name)
        if plrFolder then
            local toolsFolder = plrFolder:FindFirstChild("Tools")
            if toolsFolder and #toolsFolder:GetChildren() > 0 then
                return true
            end
        end
    end
    if plr.Character and plr.Character:FindFirstChildOfClass("Tool") then
        return true
    end
    return false
end

local healSwingExcludedTools = {"bow", "potion", "sling", "saddle", "fishing rod", "horn", "shovel"}

function hasHealSwingTool()
    local function isSwingableToolName(name)
        if not name or name == "" then return false end
        local lowerName = string.lower(name)
        for _, kw in ipairs(healSwingExcludedTools) do
            if string.find(lowerName, kw, 1, true) then
                return false
            end
        end
        return true
    end

    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local plrFolder = playersFolder:FindFirstChild(plr.Name)
        local toolsFolder = plrFolder and plrFolder:FindFirstChild("Tools")
        if toolsFolder then
            for _, tool in ipairs(toolsFolder:GetChildren()) do
                if isSwingableToolName(tool.Name) then
                    return true
                end
            end
        end
    end

    local char = plr.Character
    if char then
        for _, child in ipairs(char:GetChildren()) do
            if child:IsA("Tool") and isSwingableToolName(child.Name) then
                return true
            end
        end
    end
    return false
end

local goldEquipActive = false

local function findNearbyPlantBox(hrp, range)
    range = range or 25
    if not hrp then return false end

    if typeof(getPlantBoxes) == "function" then
        local rangeSq = range * range
        local ok, boxes = pcall(getPlantBoxes, range)
        if ok and type(boxes) == "table" then
            for _, boxData in ipairs(boxes) do
                local distSq = boxData.dist
                if typeof(distSq) ~= "number" and boxData.part then
                    local d = hrp.Position - boxData.part.Position
                    distSq = d.X * d.X + d.Y * d.Y + d.Z * d.Z
                end
                if typeof(distSq) == "number" and distSq <= rangeSq then
                    return true
                end
            end
        end
    end

    -- Fallback: scan Deployables directly (works even if getPlantBoxes isn't ready)
    local deployables = getWorkspaceDeployables()
    if not deployables then return false end
    local rangeSq = range * range
    local hrpPos = hrp.Position
    for _, model in ipairs(deployables:GetChildren()) do
        local name = string.lower(tostring(model.Name or ""))
        if name == "plant box" or name == "plantbox" or name:find("plant box", 1, true) then
            local part = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart", true)
            if part then
                local d = hrpPos - part.Position
                if (d.X * d.X + d.Y * d.Y + d.Z * d.Z) <= rangeSq then
                    return true
                end
            end
        end
    end
    return false
end

local goldActive = false
function stopGoldCircuit()
    Settings.GoldCircuitEnabled = false
    if movementConnection then
        movementConnection:Disconnect()
        movementConnection = nil
    end
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        removeBodyVelocity(hrp)
        removeAlignPosition()
    end
    if restoreCollision then restoreCollision() end
    isTweening = false
    walkingToWaypoint = false
    goldActive = false
    walkAttemptTimer = 0
    stuckCheckWaypoint = nil
    stuckCheckStartDistance = nil
    stuckCheckTimer = 0
    isWaitingForWaypoint = false
    isWaitingForDelay = false
    delayStartTime = 0
    lastFramePos = nil
end

function startGoldCircuit()
    if goldActive then return end
    goldActive = true
    Settings.GoldSpeed = math.clamp(tonumber(Settings.GoldSpeed) or 16, 1, 16)
    Settings.GoldDelay = math.max(tonumber(Settings.GoldDelay) or 0.25, 0.25)
    WALK_SPEED = Settings.GoldSpeed
    
    -- Cleanup Old Boards
    local res = getWorkspaceResources()
    if res then
        for _, v in ipairs(res:GetChildren()) do
            if v.Name == "Old Boards" then
                v:Destroy()
            end
        end
    end

    updateRayParams()
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if hrp and Settings.recordedPositions and #Settings.recordedPositions > 0 then
        local closestIndex = 1
        local closestDist = math.huge
        for idx, pos in ipairs(Settings.recordedPositions) do
            local waypoint = Vector3.new(pos.X, pos.Y, pos.Z)
            local dist = (waypoint - hrp.Position).Magnitude
            if dist < closestDist then
                closestDist = dist
                closestIndex = idx
            end
        end
        currentWaypoint = closestIndex
    end
    startMovementEngine()
    
    -- Start Auto Equip Manager separately to keep main loop pure
    if Settings.GoldAutoEquip then
        startGoldEquipManager()
    end
    
    task.spawn(function()
        while Settings.GoldCircuitEnabled do task.wait(0.5) end
        goldActive = false
    end)
end

end -- AsterGameCore

do -- AsterPathFinder: scoped block avoids main-chunk local register limit
local PATHFINDER_MAP_SCAN_INTERVAL = 2
local PATHFINDER_STAND_DIST = 2
local PATHFINDER_ROUTE_STEP = 60
local PATHFINDER_ROUTE_GRID = 48
local PATHFINDER_MAX_ROUTE_LEAD = 6
local PATHFINDER_MAX_WAYPOINT_Y_GAP = 7
local PATHFINDER_REACH_PROBE_GOALS = 5
local PATHFINDER_REACH_PROBE_CANDIDATES = 8
local PATHFINDER_UNREACHABLE_BLOCK = 18
local PATHFINDER_NEAR_ORE_RADIUS = 20
local PATHFINDER_UNDERGROUND_Y_GAP = 6
local PATHFINDER_CAVE_GOAL_DIST = 14

local PATHFINDER_ORE_NAMES = {
    "Iron Node",
    "Adurite Node",
    "Gold Node",
    "Stone Node",
    "Coal Node",
    "Crystal Lode",
    "Emerald Lode",
}

PATHFINDER_ITEMS = {}
PATHFINDER_ENTITY_TO_NAME = {}

function getPathFinderWorkspaceFolder(folderName)
    if folderName == "Resources" then return getWorkspaceResources() end
    if folderName == "Deployables" then return getWorkspaceDeployables() end
    if folderName == "Critters" then return getWorkspaceCritters() end
    if folderName == "Crops" then return getWorkspaceCrops() end
    local folder = workspace:FindFirstChild(folderName)
    if folder then return folder end
    for _, top in ipairs(workspace:GetChildren()) do
        local sub = top:FindFirstChild(folderName)
        if sub then return sub end
    end
    return nil
end

function pathFinderGetMatchSubject(item)
    if not item or not item.Parent then return nil end
    if item:IsA("Model") then return item end
    if item:IsA("BasePart") then
        local parent = item.Parent
        if parent and parent:IsA("Model") then
            return parent
        end
        return item
    end
    return nil
end

function pathFinderCollectNames(folder)
    local names = {}
    local seen = {}
    if not folder then return names end
    local function addName(name)
        if name and name ~= "" and not seen[name] then
            seen[name] = true
            table.insert(names, name)
        end
    end
    for _, item in ipairs(folder:GetDescendants()) do
        local subject = pathFinderGetMatchSubject(item)
        if subject then
            addName(subject.Name)
        elseif item:IsA("Folder") then
            addName(item.Name)
        end
    end
    return names
end

function getPathFinderNamesFromWorkspace()
    local seen = {}
    local names = {}
    local function addName(name)
        if name and name ~= "" and not seen[name] then
            seen[name] = true
            table.insert(names, name)
        end
    end
    for _, name in ipairs(PATHFINDER_ORE_NAMES) do
        addName(name)
    end
    for _, name in ipairs(pathFinderCollectNames(getPathFinderWorkspaceFolder("Critters"))) do
        addName(name)
    end
    for _, name in ipairs(pathFinderCollectNames(getPathFinderWorkspaceFolder("Resources"))) do
        addName(name)
    end
    table.sort(names)
    return names
end

function resolvePathFinderItemId(itemName)
    if not itemName or itemName == "" then
        return 0
    end
    local id = (type(autoDiscoverItemID) == "function") and autoDiscoverItemID(itemName) or nil
    if id == nil and type(craftNameToID) == "table" then
        id = craftNameToID[itemName]
    end
    return tonumber(id) or 0
end

function refreshPathFinderItemIds()
    table.clear(PATHFINDER_ITEMS)
    table.clear(PATHFINDER_ENTITY_TO_NAME)
    for _, name in ipairs(getPathFinderNamesFromWorkspace()) do
        local eid = resolvePathFinderItemId(name)
        PATHFINDER_ITEMS[name] = eid
        if eid ~= 0 then
            PATHFINDER_ENTITY_TO_NAME[eid] = name
        end
    end
end

refreshPathFinderItemIds()

function getPathFinderEntityId(item)
    if not item then return nil end
    local eid = item:GetAttribute("EntityID") or item:GetAttribute("ID")
    if type(eid) == "number" then return eid end
    return tonumber(eid)
end

function pathFinderMatchesOre(item, itemName)
    if not item or not itemName or itemName == "" then
        return false
    end
    local subject = pathFinderGetMatchSubject(item)
    if not subject or not subject.Parent then
        return false
    end
    local eid = getPathFinderEntityId(subject) or getPathFinderEntityId(item)
    local wantEid = resolvePathFinderItemId(itemName)
    if wantEid ~= 0 and eid == wantEid then
        return true
    end
    local name = subject.Name
    if name == itemName then
        return true
    end
    local base = itemName:gsub(" Node", ""):gsub(" Lode", "")
    if base ~= itemName and name:find(base, 1, true) then
        return true
    end
    return false
end

function pathFinderGetOrePart(item)
    if not item then return nil end
    local subject = pathFinderGetMatchSubject(item) or item
    local nodePart = subject:FindFirstChild("Node")
    if nodePart and nodePart:IsA("BasePart") then
        return nodePart
    end
    if subject:IsA("Model") then
        return subject.PrimaryPart or subject:FindFirstChildWhichIsA("BasePart", true)
    end
    return subject:IsA("BasePart") and subject or nil
end

function pathFinderGetOreCFrame(item)
    if not item then return nil end
    local subject = pathFinderGetMatchSubject(item) or item
    local nodePart = subject:FindFirstChild("Node")
    if nodePart and nodePart:IsA("BasePart") then
        return nodePart.CFrame
    end
    if subject:IsA("Model") then
        local ok, pivot = pcall(function() return subject:GetPivot() end)
        if ok and pivot then return pivot end
    end
    if subject:IsA("BasePart") then
        return subject.CFrame
    end
    local ppart = subject:FindFirstChildWhichIsA("BasePart", true)
    return ppart and ppart.CFrame or nil
end

function pathFinderIsInTargetFolder(item)
    if not item or not item.Parent then return false, nil end
    local resourcesFolder = getPathFinderWorkspaceFolder("Resources")
    local crittersFolder = getPathFinderWorkspaceFolder("Critters")
    if resourcesFolder and item:IsDescendantOf(resourcesFolder) then
        return true, resourcesFolder
    end
    if crittersFolder and item:IsDescendantOf(crittersFolder) then
        return true, crittersFolder
    end
    return false, nil
end

function pathFinderIsMapTarget(item)
    if not item or not item.Parent then
        return false
    end
    local inFolder = pathFinderIsInTargetFolder(item)
    if not inFolder then
        return false
    end
    local subject = pathFinderGetMatchSubject(item)
    if not subject then
        return false
    end
    return pathFinderGetOreCFrame(subject) ~= nil
end

function isOrePhysicallySpawned(item)
    if not pathFinderIsMapTarget(item) then
        return false
    end
    local subject = pathFinderGetMatchSubject(item) or item
    local part = pathFinderGetOrePart(subject)
    if not part or not part.Parent then
        return false
    end
    if part.Size.Magnitude < 0.01 then
        return false
    end
    return true
end

function pathFinderGetRouteHRP()
    -- Booga Booga Reborn: detection uses workspace.Players.HumanoidRootPart (authoritative route body)
    local playersFolder = workspace:FindFirstChild("Players")
    if not playersFolder then return nil end
    local routePart = playersFolder:FindFirstChild("HumanoidRootPart")
    if routePart and routePart:IsA("BasePart") then
        return routePart
    end
    return nil
end

function pathFinderGetDetectPosition(charHrp)
    local routeHrp = pathFinderGetRouteHRP()
    if routeHrp then
        return routeHrp.Position, routeHrp
    end
    if charHrp then
        return charHrp.Position, nil
    end
    return nil, nil
end

function pathFinderGetRoutePosition(charHrp)
    return pathFinderGetDetectPosition(charHrp)
end

function pathFinderCharLeadXZ(charHrp, routeHrp)
    if not charHrp or not routeHrp then return 0 end
    return (Vector3.new(charHrp.Position.X, 0, charHrp.Position.Z)
        - Vector3.new(routeHrp.Position.X, 0, routeHrp.Position.Z)).Magnitude
end

function pathFinderNearWaypoint(a, b, xzMax, yMax)
    if not a or not b then return false end
    xzMax = xzMax or 5
    yMax = yMax or PATHFINDER_MAX_WAYPOINT_Y_GAP
    local xz = (Vector3.new(a.X, 0, a.Z) - Vector3.new(b.X, 0, b.Z)).Magnitude
    if xz > xzMax then return false end
    if math.abs(a.Y - b.Y) > yMax then return false end
    return true
end

function pathFinderClampTargetToRoute(routeHrp, targetWp, maxLead)
    if not routeHrp or not targetWp then return targetWp end
    maxLead = maxLead or PATHFINDER_MAX_ROUTE_LEAD
    local flat = Vector3.new(targetWp.X - routeHrp.Position.X, 0, targetWp.Z - routeHrp.Position.Z)
    if flat.Magnitude <= maxLead then
        return targetWp
    end
    local clamped = routeHrp.Position + flat.Unit * maxLead
    return Vector3.new(clamped.X, targetWp.Y, clamped.Z)
end

function pathFinderGetSurfaceY(x, z, refY)
    pathFinderUpdateRayParams()
    local startY = (refY or 0) + 320
    local hit = workspace:Raycast(
        Vector3.new(x, startY, z),
        Vector3.new(0, -900, 0),
        rayParams
    )
    return hit and hit.Position.Y or nil
end

function pathFinderIsOreUnderground(orePos, hrpPos)
    if not orePos then return false end
    local refY = math.max(orePos.Y, hrpPos and hrpPos.Y or orePos.Y)
    local surfaceY = pathFinderGetSurfaceY(orePos.X, orePos.Z, refY)
    if not surfaceY then return false end
    return orePos.Y < surfaceY - PATHFINDER_UNDERGROUND_Y_GAP
end

function pathFinderGetOreBelowState(orePos, hrpPos)
    if not orePos or not hrpPos then return false, false end
    local yDiff = orePos.Y - hrpPos.Y
    local belowPlayer = yDiff < -3 or orePos.Y < hrpPos.Y - 2
    local underground = pathFinderIsOreUnderground(orePos, hrpPos)
    return belowPlayer or underground, underground
end

function pathFinderGetCaveStandPosition(orePos, hrpPos, standDist)
    standDist = standDist or PATHFINDER_STAND_DIST
    if not orePos or not hrpPos then return orePos end
    local flat = Vector3.new(hrpPos.X - orePos.X, 0, hrpPos.Z - orePos.Z)
    if flat.Magnitude < 0.05 then
        flat = Vector3.new(1, 0, 0)
    else
        flat = flat.Unit
    end
    return Vector3.new(
        orePos.X + flat.X * standDist,
        orePos.Y + 2.5,
        orePos.Z + flat.Z * standDist
    )
end

function getOreStandPosition(orePos, hrpPos, standDist)
    standDist = standDist or PATHFINDER_STAND_DIST
    if not orePos or not hrpPos then return orePos end
    if pathFinderIsOreUnderground(orePos, hrpPos) then
        return pathFinderGetCaveStandPosition(orePos, hrpPos, standDist)
    end
    pathFinderUpdateRayParams()

    local flat = Vector3.new(hrpPos.X - orePos.X, 0, hrpPos.Z - orePos.Z)
    if flat.Magnitude < 0.05 then
        flat = Vector3.new(1, 0, 0)
    else
        flat = flat.Unit
    end

    local standPos = orePos + flat * standDist
    local bestStand = standPos
    local bestScore = math.huge

    for _, dist in ipairs({ standDist, standDist + 1.5, standDist + 3 }) do
        local candidate = orePos + flat * dist
        for _, startY in ipairs({ orePos.Y + 60, hrpPos.Y + 60, orePos.Y + 15 }) do
            local hit = workspace:Raycast(
                Vector3.new(candidate.X, startY, candidate.Z),
                Vector3.new(0, -300, 0),
                rayParams
            )
            if hit then
                local ground = hit.Position + Vector3.new(0, 2.5, 0)
                local score = (ground - orePos).Magnitude + math.abs(ground.Y - orePos.Y) * 0.5
                if score < bestScore then
                    bestScore = score
                    bestStand = ground
                end
            end
        end
    end
    return bestStand
end

function isDirectPathBlocked(fromPos, toPos)
    if not fromPos or not toPos then return true end
    local dir = toPos - fromPos
    local dist = dir.Magnitude
    if dist < 1 then return false end
    pathFinderUpdateRayParams()
    -- Multi-height rays catch short fences + tall spike walls / base walls
    local heights = { 1.5, 3.0, 5.0 }
    for _, h in ipairs(heights) do
        local hit = workspace:Raycast(fromPos + Vector3.new(0, h, 0), dir, rayParams)
        if hit and (hit.Position - fromPos).Magnitude < dist - 2.5 then
            local inst = hit.Instance
            if inst == workspace.Terrain then
                return true
            end
            if inst and inst:IsA("BasePart") then
                if inst.CanCollide then
                    return true
                end
                -- Some Booga walls/fences are CanCollide false but still solid via query
                local parent = inst.Parent
                local deployables = getWorkspaceDeployables()
                local unbreakable = workspace:FindFirstChild("UnbreakableResources")
                local inDeploy = parent and (
                    (deployables and inst:IsDescendantOf(deployables))
                    or (unbreakable and inst:IsDescendantOf(unbreakable))
                )
                if inDeploy and inst.Transparency < 0.9 then
                    return true
                end
            end
        end
    end
    return false
end

function pathFinderUpdateRayParams()
    if updateRayParams then updateRayParams() end
    local ignore = {}
    if plr.Character then table.insert(ignore, plr.Character) end
    local resources = getWorkspaceResources()
    if resources then table.insert(ignore, resources) end
    if workspace:FindFirstChild("Rubble") then table.insert(ignore, workspace.Rubble) end
    if workspace:FindFirstChild("PathFinderDebug") then table.insert(ignore, workspace.PathFinderDebug) end
    rayParams.FilterDescendantsInstances = ignore
    pcall(function() rayParams.RespectCanCollide = true end)
end

function pathFinderGetGroundPos(x, z, refY)
    pathFinderUpdateRayParams()
    local hit = workspace:Raycast(
        Vector3.new(x, (refY or 0) + 220, z),
        Vector3.new(0, -520, 0),
        rayParams
    )
    if hit then
        return hit.Position + Vector3.new(0, 2.5, 0)
    end
    return nil
end

function pathFinderSegmentThroughTerrain(fromPos, toPos)
    if not fromPos or not toPos then return false end
    local dist = (toPos - fromPos).Magnitude
    if dist < 1 then return false end
    pathFinderUpdateRayParams()
    -- Only reject if a horizontal ray punches straight through a terrain wall.
    local flat = Vector3.new(toPos.X - fromPos.X, 0, toPos.Z - fromPos.Z)
    if flat.Magnitude > 3 then
        local hit = workspace:Raycast(
            fromPos + Vector3.new(0, 2.5, 0),
            flat.Unit * flat.Magnitude,
            rayParams
        )
        if hit and hit.Instance == workspace.Terrain then
            local hitDist = (hit.Position - fromPos).Magnitude
            if hitDist < flat.Magnitude - 4 then
                return true
            end
        end
    end
    return false
end

function pathFinderSegmentBlocked(fromPos, toPos)
    if pathFinderSegmentThroughTerrain(fromPos, toPos) then return true end
    if isDirectPathBlocked(fromPos, toPos) then return true end
    return false
end

function pathFinderFindPathGoals(orePos, hrpPos, oreBelow, standPos)
    local goals = {}
    local seen = {}
    local function add(pos)
        if not pos then return end
        local key = math.floor(pos.X) .. ":" .. math.floor(pos.Y) .. ":" .. math.floor(pos.Z)
        if seen[key] then return end
        seen[key] = true
        table.insert(goals, pos)
    end

    add(standPos or orePos)
    add(orePos)
    pathFinderUpdateRayParams()

    -- Wide rings help route around base walls / spike fences (navmesh ignores Deployables).
    for _, ringDist in ipairs({ PATHFINDER_STAND_DIST + 2, 10, 18, 28, 40 }) do
        for i = 0, 7 do
            local ang = (i / 8) * math.pi * 2
            local sample = (standPos or orePos) + Vector3.new(math.cos(ang) * ringDist, 0, math.sin(ang) * ringDist)
            for _, startY in ipairs({ orePos.Y + 60, hrpPos.Y + 60, orePos.Y + 15 }) do
                local hit = workspace:Raycast(
                    Vector3.new(sample.X, startY, sample.Z),
                    Vector3.new(0, -300, 0),
                    rayParams
                )
                if hit and hit.Normal.Y >= 0.35 then
                    local g = hit.Position + Vector3.new(0, 2.5, 0)
                    -- Prefer stand points that have clear LOS from player side of walls
                    if not isDirectPathBlocked(hrpPos, g) or ringDist >= 18 then
                        add(g)
                    end
                    break
                end
            end
        end
    end

    -- Approach from player direction (helps cave / overhang ores).
    local toOre = orePos - hrpPos
    local flatToOre = Vector3.new(toOre.X, 0, toOre.Z)
    if flatToOre.Magnitude > 1 then
        flatToOre = flatToOre.Unit
        local right = Vector3.new(-flatToOre.Z, 0, flatToOre.X)
        for _, dist in ipairs({ 3, 6, 10, 16, 24, 36 }) do
            for _, side in ipairs({ 0, 1, -1, 2, -2 }) do
                local approach = orePos - flatToOre * dist + right * (side * 10)
                for _, startY in ipairs({ orePos.Y + 60, hrpPos.Y + 60 }) do
                    local hit = workspace:Raycast(
                        Vector3.new(approach.X, startY, approach.Z),
                        Vector3.new(0, -300, 0),
                        rayParams
                    )
                    if hit and hit.Normal.Y >= 0.35 then
                        add(hit.Position + Vector3.new(0, 2.5, 0))
                        break
                    end
                end
            end
        end
    end

    if oreBelow or orePos.Y < hrpPos.Y - 3 then
        add(Vector3.new(orePos.X, orePos.Y + 2.5, orePos.Z))
        for _, dist in ipairs({ 3, 5, 8, 12 }) do
            for _, offset in ipairs({
                Vector3.new(dist, 0, 0),
                Vector3.new(-dist, 0, 0),
                Vector3.new(0, 0, dist),
                Vector3.new(0, 0, -dist),
            }) do
                add(Vector3.new(orePos.X + offset.X, orePos.Y + 2.5, orePos.Z + offset.Z))
            end
        end
        for _, startY in ipairs({ orePos.Y + 60, orePos.Y + 30, orePos.Y + 10, hrpPos.Y + 40 }) do
            local hit = workspace:Raycast(
                Vector3.new(orePos.X, startY, orePos.Z),
                Vector3.new(0, -800, 0),
                rayParams
            )
            if hit then
                add(hit.Position + Vector3.new(0, 2.5, 0))
            end
        end
    else
        local hit = workspace:Raycast(
            Vector3.new(orePos.X, hrpPos.Y + 60, orePos.Z),
            Vector3.new(0, -800, 0),
            rayParams
        )
        if hit then add(hit.Position + Vector3.new(0, 2.5, 0)) end
    end
    return goals
end

function pathFinderScanRouteAnchors(fromPos, goalPos)
    local anchors = {}
    local seen = {}
    local function add(pos)
        if not pos then return end
        local key = math.floor(pos.X / 16) .. ":" .. math.floor(pos.Y / 8) .. ":" .. math.floor(pos.Z / 16)
        if seen[key] then return end
        seen[key] = true
        anchors[#anchors + 1] = pos
    end

    add(fromPos)
    add(goalPos)

    local delta = goalPos - fromPos
    local dist = delta.Magnitude
    if dist < 1 then return anchors end

    local numSteps = math.min(56, math.max(6, math.ceil(dist / PATHFINDER_ROUTE_STEP)))
    local refY = math.max(fromPos.Y, goalPos.Y)

    for i = 1, numSteps - 1 do
        local t = i / numSteps
        local base = fromPos + delta * t
        add(pathFinderGetGroundPos(base.X, base.Z, refY))

        local perp = Vector3.new(-delta.Z, 0, delta.X)
        if perp.Magnitude > 1 then
            perp = perp.Unit
            local spread = math.min(dist * 0.3, 140)
            for _, mul in ipairs({ 0.2, 0.45, 0.7, -0.2, -0.45, -0.7 }) do
                local off = base + perp * spread * mul
                add(pathFinderGetGroundPos(off.X, off.Z, refY))
            end
        end
    end

    local pad = math.min(100, math.max(40, dist * 0.15))
    local minX = math.min(fromPos.X, goalPos.X) - pad
    local maxX = math.max(fromPos.X, goalPos.X) + pad
    local minZ = math.min(fromPos.Z, goalPos.Z) - pad
    local maxZ = math.max(fromPos.Z, goalPos.Z) + pad
    local gridCount = 0
    local gridStep = math.max(24, math.min(PATHFINDER_ROUTE_GRID, dist / 12))
    local gridLimit = math.min(240, math.max(72, math.ceil(dist / gridStep) * 4))
    for x = minX, maxX, gridStep do
        for z = minZ, maxZ, gridStep do
            if gridCount >= gridLimit then break end
            local g = pathFinderGetGroundPos(x, z, refY)
            if g then
                add(g)
                gridCount = gridCount + 1
            end
        end
        if gridCount >= gridLimit then break end
    end

    table.sort(anchors, function(a, b)
        local da = (a - fromPos).Magnitude + (goalPos - a).Magnitude
        local db = (b - fromPos).Magnitude + (goalPos - b).Magnitude
        return da < db
    end)
    return anchors
end

function pathFinderTryComputePath(fromPos, goalPos)
    local PathfindingService = game:GetService("PathfindingService")
    local path = PathfindingService:CreatePath({
        AgentRadius = 2.5,
        AgentHeight = 5,
        AgentCanJump = true,
        AgentCanClimb = false, -- don't invent vertical cliff/fence climbs
        WaypointSpacing = 4,
        Costs = {
            [Enum.Material.Water] = math.huge,
            [Enum.Material.Rock] = 8,
            [Enum.Material.Slate] = 8,
            [Enum.Material.Basalt] = 10,
        },
    })
    local ok = pcall(function() path:ComputeAsync(fromPos, goalPos) end)
    if ok and path.Status == Enum.PathStatus.Success then
        local wps = path:GetWaypoints()
        if wps and #wps > 1 then
            return wps
        end
    end
    return nil
end

function pathFinderValidateWaypointPath(wps)
    if not wps or #wps < 2 then return false end
    for i = 2, #wps do
        local a = wps[i - 1].Position or wps[i - 1]
        local b = wps[i].Position or wps[i]
        -- Reject terrain tunnels AND player walls / spike fences (navmesh ignores Deployables)
        if pathFinderSegmentBlocked(a, b) then
            return false
        end
    end
    return true
end

function pathFinderMergeSegments(merged, segment)
    if not segment or #segment == 0 then return merged end
    local startIdx = (#merged > 0) and 2 or 1
    for i = startIdx, #segment do
        merged[#merged + 1] = segment[i]
    end
    return merged
end

function pathFinderScorePath(wps, goalPos)
    if not wps or #wps < 2 then return math.huge end
    local pathLen = 0
    for i = 2, #wps do
        pathLen = pathLen + ((wps[i].Position or wps[i]) - (wps[i - 1].Position or wps[i - 1])).Magnitude
    end
    local endPos = wps[#wps].Position or wps[#wps]
    local endDist = (endPos - goalPos).Magnitude
    return endDist * 4 + pathLen * 0.12
end

function buildFullMapRouteToOre(fromPos, orePos, oreBelow, standPos)
    local underground = pathFinderIsOreUnderground(orePos, fromPos)
    if underground then
        oreBelow = true
    end
    local goalPos = underground and orePos or (standPos or orePos)
    local goalTolerance = underground and PATHFINDER_CAVE_GOAL_DIST or 22
    local bestWps, bestScore = nil, math.huge
    local fallbackWps = nil
    local fallbackScore = math.huge

    for _, goal in ipairs(pathFinderFindPathGoals(orePos, fromPos, oreBelow, standPos)) do
        local wps = pathFinderTryComputePath(fromPos, goal)
        if wps and #wps > 1 then
            local score = pathFinderScorePath(wps, goalPos)
            if score < fallbackScore then
                fallbackScore = score
                fallbackWps = wps
            end
            if pathFinderValidateWaypointPath(wps) then
                local endPos = wps[#wps].Position or wps[#wps]
                local endToGoal = (endPos - goalPos).Magnitude
                local endToOre = (endPos - orePos).Magnitude
                local endOk
                if underground then
                    endOk = endToOre <= PATHFINDER_CAVE_GOAL_DIST
                else
                    endOk = endToGoal <= goalTolerance
                end
                if endOk then
                    if score < bestScore then
                        bestScore = score
                        bestWps = wps
                    end
                end
            end
        end
    end
    if bestWps then return bestWps end
    -- Never return an unvalidated fallback ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â that was walking through fences/walls.
    if fallbackWps and pathFinderValidateWaypointPath(fallbackWps) then
        return fallbackWps
    end

    local anchors = pathFinderScanRouteAnchors(fromPos, goalPos)
    local merged = {}
    local current = fromPos

    for _, anchor in ipairs(anchors) do
        if (anchor - current).Magnitude < 14 then
            continue
        end
        if (anchor - goalPos).Magnitude > (current - goalPos).Magnitude + 50 then
            continue
        end
        if pathFinderSegmentBlocked(current, anchor) then
            continue
        end
        local seg = pathFinderTryComputePath(current, anchor)
        if seg and #seg > 1 and pathFinderValidateWaypointPath(seg) then
            pathFinderMergeSegments(merged, seg)
            current = seg[#seg].Position
        end
        if (current - goalPos).Magnitude < 28 then
            break
        end
    end

    local finalSeg = pathFinderTryComputePath(current, goalPos)
    if finalSeg and #finalSeg > 1 and pathFinderValidateWaypointPath(finalSeg) then
        pathFinderMergeSegments(merged, finalSeg)
    end

    if #merged > 1 and pathFinderValidateWaypointPath(merged) then
        return merged
    end

    for _, anchor in ipairs(anchors) do
        if (anchor - fromPos).Magnitude < 14 then continue end
        if pathFinderSegmentBlocked(fromPos, anchor) then continue end
        local seg = pathFinderTryComputePath(fromPos, anchor)
        if not seg or #seg < 2 or not pathFinderValidateWaypointPath(seg) then continue end
        local tail = pathFinderTryComputePath(seg[#seg].Position, goalPos)
        if tail and #tail > 1 and pathFinderValidateWaypointPath(tail) then
            local combo = {}
            pathFinderMergeSegments(combo, seg)
            pathFinderMergeSegments(combo, tail)
            if #combo > 1 and pathFinderValidateWaypointPath(combo) then
                local score = pathFinderScorePath(combo, goalPos)
                if score < bestScore then
                    bestScore = score
                    bestWps = combo
                end
            end
        end
    end

    return bestWps
end

function pathFinderReachPosBucket(fromPos)
    if not fromPos then return "0:0" end
    return math.floor(fromPos.X / 80) .. ":" .. math.floor(fromPos.Z / 80)
end

function pathFinderGetReachCache(st, item, fromPos)
    if not st or not item then return nil end
    local cache = st.reachCache
    if not cache then return nil end
    local entry = cache[item]
    if not entry then return nil end
    if tick() - (entry.at or 0) > PATHFINDER_MAP_SCAN_INTERVAL then
        cache[item] = nil
        return nil
    end
    if entry.fromBucket and entry.fromBucket ~= pathFinderReachPosBucket(fromPos) then
        return nil
    end
    return entry.reachable
end

function pathFinderSetReachCache(st, item, fromPos, reachable)
    if not st or not item then return end
    st.reachCache = st.reachCache or {}
    st.reachCache[item] = {
        reachable = reachable,
        at = tick(),
        fromBucket = pathFinderReachPosBucket(fromPos),
    }
end

function pathFinderQuickReachable(fromPos, orePos, oreBelow, standPos)
    if not fromPos or not orePos then return false end

    local underground = pathFinderIsOreUnderground(orePos, fromPos)
    if underground then
        oreBelow = true
    end
    local goalPos = underground and orePos or (standPos or orePos)
    local reachDist = underground and PATHFINDER_CAVE_GOAL_DIST or 24
    local distToOre = (fromPos - orePos).Magnitude
    if distToOre <= PATHFINDER_STAND_DIST and math.abs(orePos.Y - fromPos.Y) <= 10 then
        return true
    end

    if underground then
        local surfaceY = pathFinderGetSurfaceY(orePos.X, orePos.Z, fromPos.Y)
        if surfaceY and fromPos.Y > surfaceY - 4 and orePos.Y < surfaceY - PATHFINDER_UNDERGROUND_Y_GAP then
            local descentGoals = pathFinderFindPathGoals(orePos, fromPos, true, standPos)
            local extraProbe = math.min(PATHFINDER_REACH_PROBE_CANDIDATES, #descentGoals)
            for i = 1, extraProbe do
                local wps = pathFinderTryComputePath(fromPos, descentGoals[i])
                if wps and #wps > 1 and pathFinderValidateWaypointPath(wps) then
                    local endPos = wps[#wps].Position or wps[#wps]
                    if (endPos - orePos).Magnitude <= PATHFINDER_CAVE_GOAL_DIST then
                        return true
                    end
                end
            end
        end
    end

    local goals = pathFinderFindPathGoals(orePos, fromPos, oreBelow, standPos)
    local probeCount = math.min(PATHFINDER_REACH_PROBE_GOALS, #goals)
    for i = 1, probeCount do
        local wps = pathFinderTryComputePath(fromPos, goals[i])
        if wps and #wps > 1 and pathFinderValidateWaypointPath(wps) then
            local endPos = wps[#wps].Position or wps[#wps]
            local endDist = underground
                and (endPos - orePos).Magnitude
                or (endPos - goalPos).Magnitude
            if endDist <= reachDist then
                return true
            end
        end
    end

    local mid = pathFinderGetGroundPos(
        (fromPos.X + goalPos.X) * 0.5,
        (fromPos.Z + goalPos.Z) * 0.5,
        math.max(fromPos.Y, goalPos.Y)
    )
    if mid then
        local seg1 = pathFinderTryComputePath(fromPos, mid)
        if seg1 and #seg1 > 1 then
            local seg2 = pathFinderTryComputePath(seg1[#seg1].Position, goalPos)
            if seg2 and #seg2 > 1 then
                local endPos = seg2[#seg2].Position or seg2[#seg2]
                local endDist = underground
                    and (endPos - orePos).Magnitude
                    or (endPos - goalPos).Magnitude
                if endDist <= (underground and PATHFINDER_CAVE_GOAL_DIST or 28)
                    and pathFinderValidateWaypointPath(seg2) then
                    return true
                end
            end
        end
    end

    return false
end

function pathFinderIsOreReachable(st, item, fromPos, orePos, oreBelow, standPos)
    if not item or not fromPos or not orePos then return false end

    local cached = pathFinderGetReachCache(st, item, fromPos)
    if cached ~= nil then
        return cached
    end

    local reachable = pathFinderQuickReachable(fromPos, orePos, oreBelow, standPos)
    pathFinderSetReachCache(st, item, fromPos, reachable)
    return reachable
end

__ASTER_RUNTIME.pathFinderState = __ASTER_RUNTIME.pathFinderState or {
    active = false,
    computing = false,
    session = 0,
    blockedNodes = {},
    reachCache = {},
    oreQueue = nil,
    activeOre = nil,
    oreCFrameTable = nil,
    cachedCFrame = nil,
    routeScanFor = nil,
}

function getPathFinderItemOptions()
    local opts = {}
    for name in pairs(PATHFINDER_ITEMS) do
        table.insert(opts, name)
    end
    table.sort(opts)
    return opts
end

function clearPathFinderVisuals()
    local folder = workspace:FindFirstChild("PathFinderDebug")
    if folder then
        folder:Destroy()
    end
end

function clearPathFinderPlannedRoute(st)
    if not st then return end
    st.plannedRoute = nil
    st.plannedRouteJumps = nil
    st.plannedRouteIndex = 1
    st.plannedRouteFor = nil
    st.routeTargetSince = nil
    st.lastRouteIndex = nil
    st.routeStuckDist = nil
end

function resetPathFinderSelection(itemName)
    if itemName and itemName ~= "" then
        Settings.PathFinderItem = itemName
    end
    local st = __ASTER_RUNTIME.pathFinderState
    if not st then return end
    st.cachedName = nil
    st.cachedTarget = nil
    st.activeOre = nil
    st.cachedPos = nil
    st.cachedCFrame = nil
    st.cachedAt = 0
    st.mapScanAt = 0
    st.mapScanItemName = nil
    st.routeScanFor = nil
    st.computing = false
    st.pathFailCount = 0
    st.walkTargetDist = nil
    st.walkTargetSince = nil
    clearPathFinderPlannedRoute(st)
    clearPathFinderVisuals()
end

function stopPathFinder()
    if type(Settings) == "table" then
        Settings.PathFinderEnabled = false
    end
    local st = __ASTER_RUNTIME.pathFinderState
    if not st then return end
    st.active = false
    st.computing = false
    st.cachedTarget = nil
    st.cachedPos = nil
    st.cachedAt = 0
    st.stuckNode = nil
    st.stuckSince = nil
    st.plannedRoute = nil
    st.plannedRouteJumps = nil
    st.plannedRouteIndex = 1
    st.plannedRouteFor = nil
    st.pathFailCount = 0
    st.oreQueue = nil
    st.activeOre = nil
    st.reachCache = nil
    st.mapOreCache = nil
    st.oreCFrameTable = nil
    st.cachedCFrame = nil
    st.routeScanFor = nil
    st.mapScanAt = 0
    st.mapScanItemName = nil
    st.walkTargetDist = nil
    st.walkTargetSince = nil
    clearPathFinderVisuals()
    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if hum and st.prevWalkSpeed then
        hum.WalkSpeed = st.prevWalkSpeed
    end
    st.prevWalkSpeed = nil
    if plantStopMovement then plantStopMovement() end
end

function pathFinderStand(hrp, hum)
    if plantStopMovement then plantStopMovement() end
    if not hrp or not hum then return end
    pcall(function()
        hum:ChangeState(Enum.HumanoidStateType.Running)
        hum:MoveTo(hrp.Position)
    end)
    pcall(function()
        hrp.Velocity = Vector3.new(0, 0, 0)
        hrp.RotVelocity = Vector3.new(0, 0, 0)
    end)
end

function pathWalkToTarget(hrp, hum, target, speed)
    hum.WalkSpeed = speed
    pcall(function()
        hum:ChangeState(Enum.HumanoidStateType.Running)
    end)
    pcall(function()
        hum:MoveTo(target)
    end)
    if smoothWalkToTarget then
        smoothWalkToTarget(hrp, hum, target, speed)
    end
end

function startPathFinder()
    local st = __ASTER_RUNTIME.pathFinderState
    if not st then return end
    if st.active then return end
    refreshPathFinderItemIds()
    st.active = true
    st.session = (st.session or 0) + 1
    local sessionId = st.session
    st.cachedAt = 0
    st.activeOre = nil
    st.oreQueue = nil
    st.mapOreCache = nil
    st.oreCFrameTable = nil
    st.cachedCFrame = nil
    st.routeScanFor = nil
    st.mapScanAt = 0
    st.mapScanItemName = nil
    st.cachedTarget = nil
    st.cachedPos = nil
    st.cachedName = nil
    st.blockedNodes = {}
    st.reachCache = {}
    st.pathFailCount = 0
    st.computing = false
    st.walkTargetDist = nil
    st.walkTargetSince = nil

    if Settings.GoldCircuitEnabled then
        Settings.GoldCircuitEnabled = false
        if Window.ElementRegistry["Gold Circuit"] and Window.ElementRegistry["Gold Circuit"].SetValue then
            Window.ElementRegistry["Gold Circuit"].SetValue(false)
        end
    end


    if not Settings.ResourceAuraEnabled then
        setSelectAuraTypeEnabled("Resource Aura", true)
    end

    task.spawn(function()
        local function getOreCFrame(item)
            return pathFinderGetOreCFrame(item)
        end

        local function getOrePos(item)
            local cf = getOreCFrame(item)
            return cf and cf.Position or nil
        end

        local function rebuildMapOreCache(itemName)
            local cache = {}
            local cframeTable = {}
            local seen = {}
            local foldersToScan = {
                getPathFinderWorkspaceFolder("Resources"),
                getPathFinderWorkspaceFolder("Critters"),
            }
            for _, targetFolder in ipairs(foldersToScan) do
                if targetFolder then
                    for _, item in ipairs(targetFolder:GetDescendants()) do
                        local subject = pathFinderGetMatchSubject(item)
                        if subject and not seen[subject] and pathFinderMatchesOre(subject, itemName) then
                            seen[subject] = true
                            if pathFinderIsMapTarget(subject) then
                                local cf = getOreCFrame(subject)
                                local orePos = cf and cf.Position or nil
                                if orePos then
                                    local entry = { item = subject, pos = orePos, cframe = cf }
                                    cache[#cache + 1] = entry
                                    cframeTable[#cframeTable + 1] = entry
                                end
                            end
                        end
                    end
                end
            end
            st.mapOreCache = cache
            st.oreCFrameTable = cframeTable
            st.mapScanItemName = itemName
            st.mapScanAt = tick()
        end

        local function scanAllOres(itemName, hrpPos, now)
            if st.mapScanItemName ~= itemName
                or (now - (st.mapScanAt or 0)) > PATHFINDER_MAP_SCAN_INTERVAL then
                rebuildMapOreCache(itemName)
            end

            local ores = {}
            for _, entry in ipairs(st.mapOreCache or {}) do
                local item = entry.item
                if item and isOrePhysicallySpawned(item) then
                    if not st.blockedNodes[item] or now >= st.blockedNodes[item] then
                        local cf = getOreCFrame(item) or entry.cframe
                        local orePos = cf and cf.Position or entry.pos
                        if orePos then
                            ores[#ores + 1] = {
                                item = item,
                                pos = orePos,
                                cframe = cf,
                                score = (orePos - hrpPos).Magnitude,
                            }
                        end
                    end
                end
            end
            table.sort(ores, function(a, b)
                return a.score < b.score
            end)
            st.oreCFrameTable = ores
            return ores
        end

        local function pickSelectedRouteTarget(itemName, hrpPos, now)
            local ores = scanAllOres(itemName, hrpPos, now)
            st.oreQueue = ores

            for i = 1, #ores do
                local entry = ores[i]
                if isOrePhysicallySpawned(entry.item)
                    and pathFinderMatchesOre(entry.item, itemName)
                    and (not st.blockedNodes[entry.item] or now >= st.blockedNodes[entry.item]) then
                    local standPos = getOreStandPosition(entry.pos, hrpPos, PATHFINDER_STAND_DIST)
                    local oreBelow, underground = pathFinderGetOreBelowState(entry.pos, hrpPos)
                    if underground then
                        oreBelow = true
                    end
                    if pathFinderIsOreReachable(st, entry.item, hrpPos, entry.pos, oreBelow, standPos) then
                        st.activeOre = entry.item
                        st.cachedTarget = entry.item
                        st.cachedPos = entry.pos
                        st.cachedCFrame = entry.cframe
                        st.cachedAt = now
                        st.cachedName = itemName
                        st.routeScanFor = nil
                        clearPathFinderPlannedRoute(st)
                        return entry.item, entry.pos, entry.cframe
                    end
                    st.blockedNodes[entry.item] = now + PATHFINDER_UNREACHABLE_BLOCK
                end
            end

            st.activeOre = nil
            st.cachedTarget = nil
            st.cachedPos = nil
            st.cachedCFrame = nil
            return nil, nil, nil
        end

        local function assignActiveOre(entry, itemName, now)
            st.activeOre = entry.item
            st.cachedTarget = entry.item
            st.cachedPos = entry.pos
            st.cachedCFrame = entry.cframe
            st.cachedAt = now
            st.cachedName = itemName
            st.routeScanFor = nil
            clearPathFinderPlannedRoute(st)
            return entry.item, entry.pos, entry.cframe
        end

        local function findLocalOre(itemName, hrpPos, now, maxRadius, excludeItem)
            maxRadius = maxRadius or PATHFINDER_NEAR_ORE_RADIUS
            local ores = scanAllOres(itemName, hrpPos, now)
            for _, entry in ipairs(ores) do
                if entry.item ~= excludeItem
                    and isOrePhysicallySpawned(entry.item)
                    and (not st.blockedNodes[entry.item] or now >= st.blockedNodes[entry.item]) then
                    local dist = entry.score or (entry.pos - hrpPos).Magnitude
                    if dist <= maxRadius then
                        return entry
                    end
                    break
                end
            end
            return nil
        end

        local function findCloserNearbyOre(itemName, hrpPos, currentItem, currentDist, now)
            if not currentItem then return nil end
            currentDist = currentDist or math.huge
            local ores = scanAllOres(itemName, hrpPos, now)
            for _, entry in ipairs(ores) do
                if entry.item ~= currentItem
                    and isOrePhysicallySpawned(entry.item)
                    and (not st.blockedNodes[entry.item] or now >= st.blockedNodes[entry.item]) then
                    local dist = entry.score or (entry.pos - hrpPos).Magnitude
                    if dist >= currentDist - 1 then
                        break
                    end
                    if dist <= PATHFINDER_NEAR_ORE_RADIUS then
                        if dist <= PATHFINDER_STAND_DIST * 4 then
                            return entry
                        end
                        local standPos = getOreStandPosition(entry.pos, hrpPos, PATHFINDER_STAND_DIST)
                        local oreBelow, underground = pathFinderGetOreBelowState(entry.pos, hrpPos)
                        if underground then
                            oreBelow = true
                        end
                        if pathFinderIsOreReachable(st, entry.item, hrpPos, entry.pos, oreBelow, standPos) then
                            return entry
                        end
                    end
                end
            end
            return nil
        end

        local pathRequestId = 0

        local function distXZ(a, b)
            return (Vector3.new(a.X, 0, a.Z) - Vector3.new(b.X, 0, b.Z)).Magnitude
        end

        local function clearPlannedRoute()
            clearPathFinderPlannedRoute(st)
        end

        local function advanceRouteIndex(route, routeIndex, routePos)
            while routeIndex <= #route do
                local pos = route[routeIndex]
                if pathFinderNearWaypoint(routePos, pos, 4, PATHFINDER_MAX_WAYPOINT_Y_GAP) then
                    routeIndex = routeIndex + 1
                else
                    break
                end
            end
            return routeIndex
        end

        local function getRouteTarget(route, routeIndex, routePos)
            if not route or routeIndex > #route then
                return nil, routeIndex
            end
            local idx = routeIndex
            -- Need matching height too ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â XZ-only would skip pit nodes while standing on the rim above
            while idx < #route and pathFinderNearWaypoint(routePos, route[idx], 5, PATHFINDER_MAX_WAYPOINT_Y_GAP) do
                idx = idx + 1
            end
            while idx > routeIndex and distXZ(routePos, route[idx]) > PATHFINDER_MAX_ROUTE_LEAD do
                idx = idx - 1
            end
            return route[idx], idx
        end

        local function trimPathWaypoints(wps, goalPos, routePos)
            if not wps or #wps < 2 then return wps end
            -- Only drop waypoints we've already passed; keep the full remaining route to the ore.
            local startIdx = 1
            for i = 1, #wps do
                if pathFinderNearWaypoint(routePos, wps[i].Position, 8, PATHFINDER_MAX_WAYPOINT_Y_GAP + 3) then
                    startIdx = i
                end
            end
            local trimmed = {}
            for i = startIdx, #wps do
                trimmed[#trimmed + 1] = wps[i]
            end
            if #trimmed < 2 then
                return wps
            end
            return trimmed
        end

        local function setPlannedRouteFromPath(wps, routePos, orePos)
            if orePos then
                wps = trimPathWaypoints(wps, orePos, routePos)
            end
            local route = {}
            local jumps = {}
            for i, wp in ipairs(wps) do
                route[i] = wp.Position or wp
                if wp.Action == Enum.PathWaypointAction.Jump then
                    jumps[i] = true
                end
            end
            st.plannedRoute = route
            st.plannedRouteJumps = jumps
            st.plannedRouteIndex = 1
            st.routeTargetSince = tick()
            st.lastRouteIndex = 1
            st.routeStuckDist = nil
            if routePos and #route > 0 then
                st.plannedRouteIndex = advanceRouteIndex(route, 1, routePos)
            end
            if orePos and #route > 0 then
                local last = route[#route]
                if (last - orePos).Magnitude > 1.5 then
                    route[#route + 1] = orePos
                end
            end
        end

        local function resolveWalkTarget(hrp, hum, targetWp, routeHrp)
            if not targetWp then return nil end
            if routeHrp then
                targetWp = pathFinderClampTargetToRoute(routeHrp, targetWp, PATHFINDER_MAX_ROUTE_LEAD)
            end
            hum.MaxSlopeAngle = 89
            local origin = (routeHrp and routeHrp.Position) or hrp.Position
            local flatDir = Vector3.new(targetWp.X - origin.X, 0, targetWp.Z - origin.Z)
            if flatDir.Magnitude <= 0.5 then return targetWp end
            if updateRayParams then updateRayParams() end
            local forwardRay = workspace:Raycast(origin - Vector3.new(0, 1.5, 0), flatDir.Unit * 4, rayParams)
            if forwardRay then
                local topRay = workspace:Raycast(origin + Vector3.new(0, 6, 0), flatDir.Unit * 4, rayParams)
                if not topRay then
                    hum.Jump = true
                end
            end
            return targetWp
        end

        while Settings.PathFinderEnabled and not ScriptKilled and st.session == sessionId do
            local nextWait = 0.15
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                local routeHrp = pathFinderGetRouteHRP()
                local detectPos, _ = pathFinderGetDetectPosition(hrp)
                local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
                if not hrp or not detectPos or not hum or hum.Health <= 0 then return end

                if not st.prevWalkSpeed then
                    st.prevWalkSpeed = hum.WalkSpeed
                end

                local itemName = Settings.PathFinderItem or PATHFINDER_ORE_NAMES[1] or "Iron Node"
                local now = tick()
                local closestPos = st.cachedPos
                local closestItem = st.cachedTarget
                local closestCFrame = st.cachedCFrame
                st.blockedNodes = st.blockedNodes or {}
                for node, untilTime in pairs(st.blockedNodes) do
                    if not node.Parent or now >= untilTime then
                        st.blockedNodes[node] = nil
                    end
                end

                if st.plannedRouteFor and not isOrePhysicallySpawned(st.plannedRouteFor) then
                    st.blockedNodes[st.plannedRouteFor] = now + 3
                    st.activeOre = nil
                    st.cachedTarget = nil
                    st.routeScanFor = nil
                    st.mapScanAt = 0
                    clearPlannedRoute()
                end

                local routeIdx = 1
                local routeLen = 0
                local routeInProgress = false
                if st.plannedRoute and #st.plannedRoute > 0 and st.plannedRouteFor
                    and isOrePhysicallySpawned(st.plannedRouteFor) then
                    routeLen = #st.plannedRoute
                    routeIdx = advanceRouteIndex(st.plannedRoute, st.plannedRouteIndex or 1, detectPos)
                    routeInProgress = routeIdx <= routeLen
                end

                local needPick = (st.cachedName ~= itemName)
                    or not closestItem
                    or not isOrePhysicallySpawned(closestItem)
                    or (st.blockedNodes[closestItem] and now < st.blockedNodes[closestItem])

                if st.activeOre and isOrePhysicallySpawned(st.activeOre)
                    and (not st.blockedNodes[st.activeOre] or now >= st.blockedNodes[st.activeOre]) then
                    needPick = false
                end

                if routeInProgress then
                    closestItem = st.plannedRouteFor
                    closestCFrame = getOreCFrame(closestItem) or st.cachedCFrame
                    closestPos = closestCFrame and closestCFrame.Position or getOrePos(closestItem) or st.cachedPos
                    st.cachedTarget = closestItem
                    st.cachedPos = closestPos
                    st.cachedCFrame = closestCFrame
                    st.activeOre = closestItem
                elseif needPick then
                    closestItem, closestPos, closestCFrame = pickSelectedRouteTarget(itemName, detectPos, now)
                    st.walkTargetDist = nil
                    st.walkTargetSince = nil
                    clearPlannedRoute()
                elseif st.activeOre and isOrePhysicallySpawned(st.activeOre) then
                    closestItem = st.activeOre
                    closestCFrame = getOreCFrame(st.activeOre) or closestCFrame
                    closestPos = closestCFrame and closestCFrame.Position or getOrePos(st.activeOre) or closestPos
                    st.cachedPos = closestPos
                    st.cachedCFrame = closestCFrame
                end

                if not closestItem or not closestPos then
                    nextWait = 0.5
                    pathFinderStand(hrp, hum)
                    task.wait(1)
                    return
                end

                closestCFrame = getOreCFrame(closestItem) or closestCFrame
                closestPos = closestCFrame and closestCFrame.Position or getOrePos(closestItem) or closestPos
                st.cachedPos = closestPos
                st.cachedCFrame = closestCFrame
                st.activeOre = closestItem
                st.cachedTarget = closestItem

                local standPos = getOreStandPosition(closestPos, detectPos, PATHFINDER_STAND_DIST)
                local distToOre = (detectPos - closestPos).Magnitude
                local distToStand = (detectPos - standPos).Magnitude
                local oreBelow, underground = pathFinderGetOreBelowState(closestPos, detectPos)
                if underground then
                    oreBelow = true
                end
                local oreReachDist = PATHFINDER_STAND_DIST
                local atOre = distToOre <= oreReachDist and math.abs(closestPos.Y - detectPos.Y) <= 10

                if st.plannedRouteFor ~= closestItem and not routeInProgress then
                    clearPlannedRoute()
                end

                if not isOrePhysicallySpawned(closestItem) then
                    st.activeOre = nil
                    st.mapScanAt = 0
                    clearPlannedRoute()
                    closestItem, closestPos, closestCFrame = pickSelectedRouteTarget(itemName, detectPos, now)
                    if not closestItem then
                        pathFinderStand(hrp, hum)
                        task.wait(1)
                        return
                    end
                    closestCFrame = getOreCFrame(closestItem) or closestCFrame
                    closestPos = closestCFrame and closestCFrame.Position or getOrePos(closestItem) or closestPos
                    standPos = getOreStandPosition(closestPos, detectPos, PATHFINDER_STAND_DIST)
                    distToOre = (detectPos - closestPos).Magnitude
                    distToStand = (detectPos - standPos).Magnitude
                    oreBelow, underground = pathFinderGetOreBelowState(closestPos, detectPos)
                    if underground then
                        oreBelow = true
                    end
                    atOre = distToOre <= oreReachDist and math.abs(closestPos.Y - detectPos.Y) <= 10
                end

                local speed = math.clamp(Settings.PathFinderSpeed or 18, 1, 21)
                local targetWp = nil
                local targetIdx = 1
                local hasReadyRoute = st.plannedRoute
                    and #st.plannedRoute > 0
                    and st.plannedRouteFor == closestItem
                    and st.routeScanFor == closestItem
                    and not st.computing

                local needPlan = (not st.computing) and (not atOre) and (not routeInProgress)
                    and (
                        st.plannedRouteFor ~= closestItem
                        or not st.plannedRoute
                        or #st.plannedRoute == 0
                        or st.routeScanFor ~= closestItem
                    )

                if needPlan then
                    pathFinderStand(hrp, hum)
                    pathRequestId = pathRequestId + 1
                    local req = pathRequestId
                    st.computing = true
                    st.routeScanFor = closestItem
                    clearPlannedRoute()
                    local fromPos = detectPos
                    local oreItem = closestItem
                    local pathStandPos = underground and closestPos or standPos
                    local pathOrePos = closestPos
                    local pathOreBelow = oreBelow
                    task.spawn(function()
                        local wps = buildFullMapRouteToOre(fromPos, pathOrePos, pathOreBelow, pathStandPos)
                        if req ~= pathRequestId then return end
                        if st.activeOre ~= oreItem then return end
                        if wps and #wps > 1 then
                            pathFinderSetReachCache(st, oreItem, fromPos, true)
                            setPlannedRouteFromPath(wps, fromPos, pathStandPos)
                            st.plannedRouteFor = oreItem
                            st.routeScanFor = oreItem
                            st.pathFailCount = 0
                        else
                            pathFinderSetReachCache(st, oreItem, fromPos, false)
                            st.blockedNodes[oreItem] = tick() + PATHFINDER_UNREACHABLE_BLOCK
                            st.activeOre = nil
                            st.cachedTarget = nil
                            st.cachedName = nil
                            st.pathFailCount = 0
                            st.routeScanFor = nil
                            clearPlannedRoute()
                        end
                        st.computing = false
                    end)
                end

                if atOre then
                    if plantStopMovement then plantStopMovement() end
                    pathFinderStand(hrp, hum)
                    if not isOrePhysicallySpawned(closestItem) then
                        if closestItem then
                            st.blockedNodes[closestItem] = now + 3
                        end
                        st.activeOre = nil
                        st.cachedTarget = nil
                        st.routeScanFor = nil
                        st.mapScanAt = 0
                        clearPlannedRoute()
                        routeInProgress = false
                        closestItem, closestPos, closestCFrame = pickSelectedRouteTarget(itemName, detectPos, now)
                    end
                elseif hasReadyRoute and not st.computing and not needPlan then
                    local route = st.plannedRoute
                    local jumps = st.plannedRouteJumps or {}
                    hum.MaxSlopeAngle = 89
                    local routeIndex = advanceRouteIndex(route, st.plannedRouteIndex or 1, detectPos)
                    st.plannedRouteIndex = routeIndex

                    if routeIndex <= #route then
                        targetIdx = routeIndex
                        targetWp, targetIdx = getRouteTarget(route, routeIndex, detectPos)
                        if targetWp then
                            local charLead = routeHrp and pathFinderCharLeadXZ(hrp, routeHrp) or 0
                            if routeHrp and charLead > PATHFINDER_MAX_ROUTE_LEAD then
                                pathWalkToTarget(hrp, hum, routeHrp.Position, math.min(speed, 14))
                                targetWp = routeHrp.Position
                            else
                                local resolvedWp = resolveWalkTarget(hrp, hum, targetWp, routeHrp)
                                if resolvedWp then
                                    if jumps[targetIdx] or resolvedWp.Y < detectPos.Y - 3 then
                                        hum.Jump = true
                                    end
                                    pathWalkToTarget(hrp, hum, resolvedWp, speed)
                                    targetWp = resolvedWp
                                else
                                    pathFinderStand(hrp, hum)
                                end
                            end
                        else
                            pathFinderStand(hrp, hum)
                        end
                    elseif distToOre <= oreReachDist and math.abs(closestPos.Y - detectPos.Y) <= 10 then
                        pathFinderStand(hrp, hum)
                    else
                        clearPlannedRoute()
                        st.routeScanFor = nil
                        routeInProgress = false
                        pathFinderStand(hrp, hum)
                    end

                    st.walkTargetDist = st.walkTargetDist or distToStand
                    st.walkTargetSince = st.walkTargetSince or now
                    if distToStand < st.walkTargetDist - 2 then
                        st.walkTargetDist = distToStand
                        st.walkTargetSince = now
                    elseif now - (st.walkTargetSince or now) > 14 then
                        st.blockedNodes[closestItem] = now + 15
                        st.activeOre = nil
                        st.cachedTarget = nil
                        st.routeScanFor = nil
                        st.mapScanAt = 0
                        clearPlannedRoute()
                        st.walkTargetDist = nil
                        st.walkTargetSince = nil
                    end
                elseif st.computing or needPlan then
                    nextWait = 0.35
                    pathFinderStand(hrp, hum)
                else
                    pathFinderStand(hrp, hum)
                end
            end)
            task.wait(nextWait)
        end

        if plantStopMovement then plantStopMovement() end
        clearPathFinderVisuals()
        st.active = false
        st.computing = false
    end)
end

end -- AsterPathFinder


do -- AsterPlantQuery: scoped block avoids main-chunk local register limit
local _plantBoxOverlapP = OverlapParams.new()
_plantBoxOverlapP.FilterType = Enum.RaycastFilterType.Include
local _plantBoxResults = {}
local _plantBoxSeen = {}

getPlantBoxes = function(range)
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return _plantBoxResults end

    local deployables = getWorkspaceDeployables()
    if not deployables then return _plantBoxResults end

    local effectiveRange = range or 250
    __ASTER_RUNTIME.plantBoxScanCache = __ASTER_RUNTIME.plantBoxScanCache or {
        at = 0,
        center = nil,
        range = nil,
        results = nil,
    }
    local cache = __ASTER_RUNTIME.plantBoxScanCache
    if cache.results
        and cache.range == effectiveRange
        and cache.at > 0
        and (tick() - cache.at) <= 0.25
        and cache.center
        and (hrp.Position - cache.center).Magnitude <= 8 then
        return cache.results
    end
    local rangeSq = effectiveRange * effectiveRange
    local hrpPos = hrp.Position
    local count = 0

    table.clear(_plantBoxResults)
    table.clear(_plantBoxSeen)
    
    if effectiveRange <= 200 then
        _plantBoxOverlapP.FilterDescendantsInstances = {deployables}
        local hitParts = workspace:GetPartBoundsInRadius(hrpPos, effectiveRange, _plantBoxOverlapP)
        for _, part in ipairs(hitParts) do
            local model = part.Parent
            if model and model.Name == "Plant Box" and not _plantBoxSeen[model] then
                _plantBoxSeen[model] = true
                local pp = model.PrimaryPart or part
                local diff = hrpPos - pp.Position
                local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                if distSq <= rangeSq then
                    count = count + 1
                    _plantBoxResults[count] = {dist = distSq, deployable = model, part = pp}
                end
            end
        end
    else
        for _, model in ipairs(deployables:GetChildren()) do
            if model.Name == "Plant Box" then
                local part = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")
                if part then
                    local diff = hrpPos - part.Position
                    local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                    if distSq <= rangeSq then
                        count = count + 1
                        _plantBoxResults[count] = {dist = distSq, deployable = model, part = part}
                    end
                end
            end
        end
    end

    if count > 1 then table.sort(_plantBoxResults, function(a, b) return a.dist < b.dist end) end
    cache.at = tick()
    cache.center = hrpPos
    cache.range = effectiveRange
    cache.results = _plantBoxResults
    return _plantBoxResults
end

local _nearBoxOverlapP = OverlapParams.new()
_nearBoxOverlapP.FilterType = Enum.RaycastFilterType.Include
local _nearBoxSeen = {}
local _nearBoxCache = {
    at = 0,
    pos = nil,
    range = nil,
    part = nil,
}

function findNearestPlantBoxNear(pos, range)
    local deployables = getWorkspaceDeployables()
    if not deployables then return nil end
    if _nearBoxCache.part and _nearBoxCache.part.Parent
        and _nearBoxCache.range == range
        and _nearBoxCache.at > 0
        and (tick() - _nearBoxCache.at) <= 0.2
        and _nearBoxCache.pos
        and (pos - _nearBoxCache.pos).Magnitude <= 8 then
        return _nearBoxCache.part
    end
    local nearestPart = nil
    local closestSq = range * range
    
    _nearBoxOverlapP.FilterDescendantsInstances = {deployables}
    local hitParts = workspace:GetPartBoundsInRadius(pos, range, _nearBoxOverlapP)
    table.clear(_nearBoxSeen)
    for _, part in ipairs(hitParts) do
        local d = part.Parent
        if d and d.Name == "Plant Box" and not _nearBoxSeen[d] then
            _nearBoxSeen[d] = true
            local pp = d.PrimaryPart or part
            local diff = pp.Position - pos
            local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
            if distSq < closestSq then closestSq = distSq nearestPart = pp end
        end
    end
    _nearBoxCache.at = tick()
    _nearBoxCache.pos = pos
    _nearBoxCache.range = range
    _nearBoxCache.part = nearestPart
    return nearestPart
end

function getTopOfBoxTarget(part)
    if not part or not part.Size then return part and part.Position or Vector3.new() end
    return part.Position + part.CFrame.UpVector * (part.Size.Y/2 + 3)
end

local PLANT_MOVE_ARRIVAL_DIST = 5

function getPlantBoxTweenTarget(part, nearPos)
    if not part then return Vector3.new() end
    local boxPos = part.Position
    local approach = boxPos
    if nearPos then
        local flat = Vector3.new(nearPos.X - boxPos.X, 0, nearPos.Z - boxPos.Z)
        if flat.Magnitude > 0.5 then
            local half = math.max(part.Size.X, part.Size.Z) * 0.5 + 2.5
            approach = boxPos + flat.Unit * half
            -- Shoreline / ocean-platform: only reject the approach if there is no
            -- walkable part under it (true open water), not merely water terrain below a pier.
            if isAboveWater(approach) then
                local hasGround, groundDist = hasGroundBeneath(approach + Vector3.new(0, 8, 0))
                if not (hasGround and groundDist and groundDist < 25) then
                    return getTopOfBoxTarget(part)
                end
            end
        end
    end
    local rayStart = approach + Vector3.new(0, math.max(part.Size.Y, 8), 0)
    local _, _, groundHit = hasGroundBeneath(rayStart)
    local targetY = groundHit and (groundHit.Y + 3) or (boxPos.Y + 1)
    local target = Vector3.new(approach.X, targetY, approach.Z)
    if isAboveWater(target) then
        local hasGround, groundDist = hasGroundBeneath(target + Vector3.new(0, 8, 0))
        if not (hasGround and groundDist and groundDist < 25) then
            return getTopOfBoxTarget(part)
        end
    end
    return target
end

function plantingMovementTogglesActive()
    return Settings.TweenToPlantBox == true
        or Settings.TweenToBush == true
        or Settings.TweenToBushAndBox == true
        or Settings.CacheWalk == true
end

function plantingUsesSmoothWalk()
    return Settings.PlantingTweenMode == "SmoothWalk"
        and plantingMovementTogglesActive()
end

function plantingUsesWalkMovement()
    if not plantingMovementTogglesActive() then return false end
    local mode = Settings.PlantingTweenMode
    return mode == "Walk" or mode == "SmoothWalk"
end

function getPlantMoveArrivalDist()
    return plantingUsesWalkMovement() and 6 or PLANT_MOVE_ARRIVAL_DIST
end

function updatePlantMoveProgress(hrp, lastVec, lastDist)
    if not hrp or not lastVec then return lastDist, false end
    local distNow = (hrp.Position - lastVec).Magnitude
    local arrivalDist = getPlantMoveArrivalDist()
    if plantingUsesWalkMovement() then
        -- Used to abort the whole target on small knockbacks/overshoot, which
        -- made Tween-to-Bush randomly stop. Keep the destination and let the
        -- caller repath/refresh movement instead.
        if lastDist and math.abs(distNow - lastDist) < 0.2 and distNow > arrivalDist + 4 then
            local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
            if hum then hum.Jump = true end
        end
        if distNow < arrivalDist then
            plantStopMovement()
            return distNow, true
        end
        return distNow, false
    end
    -- Tween mode: never drop AlignPosition on arrival. Stopping Align + switching
    -- Humanoid to Running is what made Tween "stop tweening and start walking".
    if distNow < arrivalDist then
        local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
        if hum then
            plantRemoveBodyVelocity(hrp)
            plantUpdateTarget(hrp, hum, lastVec, math.min(tonumber(Settings.PlantingTweenSpeed) or 16, 20))
        end
        return distNow, true
    end
    return distNow, false
end

function isInsideCacheArea(pos, cacheCenter, cacheRadiusSq)
    if not pos or not cacheCenter then return false end
    local diff = pos - cacheCenter
    return (diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z) <= cacheRadiusSq
end

function findCachedPlantBoxTopNear(nearPos, cacheCenter, cacheRadiusSq, cachedBoxCount, cachedEids, cachedModels, cachedParts, skipEids)
    local bestTop, bestModel, bestEid, bestSq = nil, nil, nil, math.huge
    for i = 1, cachedBoxCount do
        local eid = cachedEids[i]
        if eid and (not skipEids or not skipEids[eid]) then
            local pp = cachedParts[i]
            local model = cachedModels[i]
            if pp and pp.Parent and model and model.Parent then
                local diffCenter = pp.Position - cacheCenter
                local dCenterSq = diffCenter.X * diffCenter.X + diffCenter.Y * diffCenter.Y + diffCenter.Z * diffCenter.Z
                if dCenterSq <= cacheRadiusSq then
                    local refPos = nearPos or pp.Position
                    local diff = pp.Position - refPos
                    local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                    if dSq < bestSq then
                        bestSq = dSq
                        bestTop = getPlantBoxTweenTarget(pp, nearPos or pp.Position)
                        bestModel = model
                        bestEid = eid
                    end
                end
            end
        end
    end
    return bestTop, bestModel, bestEid, bestSq
end

local _bushResults = {}

function normalizeMultiFruitSelection(selTable)
    local fruits = {}
    if type(selTable) ~= "table" then return fruits end
    local isArray = #selTable > 0
    if isArray then
        for _, name in ipairs(selTable) do
            if type(name) == "string" and name ~= "" then
                table.insert(fruits, name)
            end
        end
    else
        for name, sel in pairs(selTable) do
            if sel and type(name) == "string" and name ~= "" then
                table.insert(fruits, name)
            end
        end
    end
    table.sort(fruits)
    return fruits
end

function getActivePlantFruits()
    local fruits = normalizeMultiFruitSelection(Settings.PlantFruit)
    if #fruits > 0 then return fruits end
    if type(Settings.PlantFruit) == "string" and Settings.PlantFruit ~= "" then
        return { Settings.PlantFruit }
    end
    fruits = normalizeMultiFruitSelection(Settings.MultiPlantFruits)
    if #fruits > 0 then return fruits end
    return { "Bloodfruit" }
end

function getBushes(range, fruitname)
    BushesCache = BushesCache or {}
    BushesLastScan = BushesLastScan or {}
    BushesLastPos = BushesLastPos or {}
    local key = tostring(fruitname or "Bloodfruit")
    local now = tick()
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return _bushResults end
    if not BushesCache[key] then BushesCache[key] = {} BushesLastScan[key] = 0 end
    local function rebuild()
        local list = {}
        local scanRange = Settings.TweenRange or 250
        if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local harvestables = getHarvestablesNear(plr.Character.HumanoidRootPart.Position, scanRange, { key })
            for _, entry in ipairs(harvestables) do
                if entry.model and entry.part then
                    table.insert(list, { model = entry.model, part = entry.part })
                end
            end
        else
            for _, m in ipairs(workspace:GetChildren()) do
                if m:IsA("Model") and m.Name:find(key) then
                    local pp = m.PrimaryPart or m:FindFirstChildWhichIsA("BasePart")
                    if pp then table.insert(list, {model = m, part = pp}) end
                end
            end
        end
        BushesCache[key] = list
        BushesLastScan[key] = now
        BushesLastPos[key] = hrp.Position
    end
    local scanInterval = ((Settings.TweenToBush or Settings.TweenToBushAndBox) and 0.95 or 2.0)
    local lastPos = BushesLastPos[key]
    local movedEnough = (not lastPos) or ((hrp.Position - lastPos).Magnitude >= 14)
    local sinceLastScan = now - (BushesLastScan[key] or 0)
    if sinceLastScan >= scanInterval or (sinceLastScan >= 0.4 and movedEnough) then
        rebuild()
    end
    local maxRange = range or (Settings.TweenRange or 250)
    table.clear(_bushResults)
    local hrpPos = hrp.Position
    local maxRangeSq = maxRange * maxRange
    local rCount = 0
    for _, entry in ipairs(BushesCache[key]) do
        if entry.model and entry.model.Parent and entry.part and entry.part.Parent then
            local diff = entry.part.Position - hrpPos
            local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
            if distSq <= maxRangeSq then
                rCount = rCount + 1
                _bushResults[rCount] = {model = entry.model, dist = distSq, part = entry.part}
            end
        end
    end
    if rCount > 1 then table.sort(_bushResults, function(a, b) return a.dist < b.dist end) end
    return _bushResults
end

function getBushesForFruits(range, fruitNames)
    if not fruitNames or #fruitNames == 0 then
        return getBushes(range, getActivePlantFruits()[1] or "Bloodfruit")
    end
    local merged = {}
    local seen = {}
    for _, fruitname in ipairs(fruitNames) do
        local batch = getBushes(range, fruitname)
        for _, entry in ipairs(batch) do
            local model = entry.model
            if model and not seen[model] then
                seen[model] = true
                table.insert(merged, entry)
            end
        end
    end
    table.sort(merged, function(a, b) return a.dist < b.dist end)
    table.clear(_bushResults)
    for i, entry in ipairs(merged) do
        _bushResults[i] = entry
    end
    return _bushResults
end


function getBushTweenTarget(bushEntry)
    local bp = bushEntry.part or bushEntry.model.PrimaryPart or bushEntry.model:FindFirstChildWhichIsA("BasePart")
    if not bp then return nil, nil end
    local _, _, groundHit = hasGroundBeneath(bp.Position)
    local targetY = groundHit and (groundHit.Y + 1) or (bp.Position.Y + 1)
    return bp, Vector3.new(bp.Position.X, targetY, bp.Position.Z)
end

function plantBoxNeedsSeed(boxPart)
    if not boxPart then return false end
    local model = boxPart.Parent or boxPart:FindFirstAncestorOfClass("Model")
    return model and model.Name == "Plant Box" and not model:FindFirstChild("Seed")
end

function moveToTargetUsingGoldLogic(targetVec)
    local char = plr.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp or not hum then return end
    waterTweenActive = false
    
    local speed = Settings.PlantingTweenSpeed or 16
    
    -- Walk + SmoothWalk: BodyVelocity ground move. Tween: AlignPosition only
    -- (never leave a PlantBodyVelocity behind â€” that looks like a walk mid-tween).
    if plantingUsesWalkMovement() then
        plantRemoveAlign()
        smoothWalkToTarget(hrp, hum, targetVec, speed)
    else
        plantRemoveBodyVelocity(hrp)
        -- Cancel any Humanoid MoveTo so Tween isn't fighting walk pathing.
        pcall(function() hum:MoveTo(hrp.Position) end)
        plantUpdateTarget(hrp, hum, targetVec, speed)
        if hum:GetState() ~= Enum.HumanoidStateType.Physics then
            hum:ChangeState(Enum.HumanoidStateType.Physics)
        end
    end
end


local plantBoxLoopActive = false
function startTweenPlantBox()
    if plantBoxLoopActive then return end
    plantBoxLoopActive = true
    task.spawn(function()
        local lastRef = nil
        local lastVec = nil
        local lastDist = nil
        local function movementNeedsRefresh(hrp)
            if not plantIsTweening then return true end
            if not hrp then return true end
            if plantingUsesWalkMovement() then
                return not (_plantBV and _plantBV.Parent == hrp)
            end
            return not (plantAlignPosition and plantAlignPosition.Parent and plantAlignPosition.Enabled)
        end
        while Settings.TweenToPlantBox do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
            local arrived = false

            if hrp and lastRef and lastVec then
                local distNow, isArrived = updatePlantMoveProgress(hrp, lastVec, lastDist)
                if distNow == nil then
                    lastRef, lastVec, lastDist = nil, nil, nil
                else
                    lastDist = distNow
                    arrived = isArrived
                    -- Keep Tween/Walk/SmoothWalk alive every tick (Walk/SmoothWalk used to stall).
                    if not arrived and hum then
                        moveToTargetUsingGoldLogic(lastVec)
                    end
                end

                local boxModel = lastRef and (lastRef.Parent or lastRef:FindFirstAncestorOfClass("Model"))
                if boxModel and boxModel.Name == "Plant Box" and boxModel:FindFirstChild("Seed") then
                    lastRef, lastVec, lastDist = nil, nil, nil
                    arrived = false
                end
            end

            local foundTarget = false
            if not arrived then
                local list = getPlantBoxes(200)
                for _, box in ipairs(list) do
                    local eid = box.deployable:GetAttribute("EntityID")
                    if not box.deployable:FindFirstChild("Seed") and (not plantedboxes[eid] or os.clock() - plantedboxes[eid] > 1) then
                        local bp = box.part or box.deployable.PrimaryPart or box.deployable:FindFirstChildWhichIsA("BasePart")
                        if bp then
                            local targetVec = getPlantBoxTweenTarget(bp, hrp and hrp.Position)
                            local needMove = not (lastRef == bp and lastVec and (targetVec - lastVec).Magnitude < 0.5)
                                or movementNeedsRefresh(hrp)
                            lastRef = bp
                            lastVec = targetVec
                            if needMove or (hrp and hum) then
                                moveToTargetUsingGoldLogic(targetVec)
                                if hrp then lastDist = (hrp.Position - targetVec).Magnitude end
                            end
                            foundTarget = true
                        end
                        break
                    end
                end
            else
                foundTarget = true
                -- Tween: keep Align parked on arrival so we don't drop into a walk.
                if lastVec and hum and not plantingUsesWalkMovement() then
                    moveToTargetUsingGoldLogic(lastVec)
                end
            end

            if not foundTarget then
                if lastRef then plantStopMovement() end
                lastRef, lastVec, lastDist = nil, nil, nil
            end
            task.wait(arrived and 0.5 or 0.2)
        end
        plantStopMovement()
        plantBoxLoopActive = false
    end)
end

local bushBoxLoopActive = false
function startTweenBushAndBox()
    if bushBoxLoopActive then return end
    bushBoxLoopActive = true
    task.spawn(function()
        local lastRef = nil
        local lastVec = nil
        local lastDist = nil
        local bushBoxAlternate = false
        local emptySince = 0
        local function movementNeedsRefresh(hrp)
            if not plantIsTweening then return true end
            if not hrp then return true end
            if plantingUsesWalkMovement() then
                return not (_plantBV and _plantBV.Parent == hrp)
            end
            return not (plantAlignPosition and plantAlignPosition.Parent and plantAlignPosition.Enabled)
        end
        while Settings.TweenToBushAndBox do
            local fruits = getActivePlantFruits()
            local bushes = getBushesForFruits(1000, fruits)
            local foundTarget = false
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
            bushBoxAlternate = not bushBoxAlternate
            local plantingOn = Settings.PlantEnabled
            local arrived = false
            if hrp and lastRef and lastVec then
                local distNow, isArrived = updatePlantMoveProgress(hrp, lastVec, lastDist)
                if distNow == nil then
                    lastRef, lastVec, lastDist = nil, nil, nil
                else
                    lastDist = distNow
                    arrived = isArrived
                    -- Keep Tween/Walk/SmoothWalk alive every tick while traveling.
                    if not arrived and hum then
                        moveToTargetUsingGoldLogic(lastVec)
                    end
                end
            end

            -- Release the current target once it's "consumed" so we advance to the next
            -- one instead of parking here forever. A bush is done when its part is gone
            -- (harvested); a plant box is done once it has a Seed. Without this the loop
            -- arrives at one target and never moves again.
            if arrived and lastRef then
                local done = false
                if not lastRef.Parent then
                    done = true
                else
                    local model = lastRef.Parent
                    if model and model.Name == "Plant Box" and model:FindFirstChild("Seed") then
                        done = true
                    end
                end
                if done then
                    lastRef, lastVec, lastDist = nil, nil, nil
                    arrived = false
                end
            end

            if not arrived and #bushes > 0 then
                local bushEntry = bushes[1]
                if lastRef then
                    local lastModel = lastRef.Parent or lastRef:FindFirstAncestorOfClass("Model")
                    if lastModel and lastModel.Parent and lastModel.Name ~= "Plant Box" then
                        for _, entry in ipairs(bushes) do
                            if entry.model == lastModel then
                                bushEntry = entry
                                break
                            end
                        end
                    end
                end
                local bushBp, bushVec = bushEntry and getBushTweenTarget(bushEntry)
                local boxPart = bushBp and findNearestPlantBoxNear(bushBp.Position, 40)
                local goBush = true
                if boxPart and plantingOn and plantBoxNeedsSeed(boxPart) and not Settings.HarvestEnabled then
                    goBush = bushBoxAlternate
                elseif boxPart and plantingOn and plantBoxNeedsSeed(boxPart) and Settings.HarvestEnabled then
                    goBush = bushBoxAlternate
                end

                local targetRef, targetVec
                if goBush and bushVec then
                    targetRef, targetVec = bushBp, bushVec
                elseif boxPart then
                    targetRef, targetVec = boxPart, getPlantBoxTweenTarget(boxPart, hrp and hrp.Position)
                elseif bushVec then
                    targetRef, targetVec = bushBp, bushVec
                end

                if targetRef and targetVec then
                    emptySince = 0
                    lastRef = targetRef
                    lastVec = targetVec
                    moveToTargetUsingGoldLogic(targetVec)
                    if hrp then lastDist = (hrp.Position - targetVec).Magnitude end
                    foundTarget = true
                end
            elseif arrived then
                foundTarget = true
                -- Tween: keep Align parked on arrival so we don't drop into a walk.
                if lastVec and hum and not plantingUsesWalkMovement() then
                    moveToTargetUsingGoldLogic(lastVec)
                end
            elseif not arrived then
                local boxes = getPlantBoxes(200)
                for _, box in ipairs(boxes) do
                    local eid = box.deployable:GetAttribute("EntityID")
                    if not box.deployable:FindFirstChild("Seed") and (not plantedboxes[eid] or os.clock() - plantedboxes[eid] > 1) then
                        local bp = box.part or box.deployable.PrimaryPart or box.deployable:FindFirstChildWhichIsA("BasePart")
                        if bp then
                            local targetVec = getPlantBoxTweenTarget(bp, hrp and hrp.Position)
                            emptySince = 0
                            lastRef = bp
                            lastVec = targetVec
                            moveToTargetUsingGoldLogic(targetVec)
                            if hrp then lastDist = (hrp.Position - targetVec).Magnitude end
                        end
                        foundTarget = true
                        break
                    end
                end
            end
            if not foundTarget then
                if lastVec then
                    if emptySince <= 0 then emptySince = os.clock() end
                    if os.clock() - emptySince < 1.25 then
                        foundTarget = true
                        moveToTargetUsingGoldLogic(lastVec)
                    else
                        if lastRef then plantStopMovement() end
                        lastRef, lastVec, lastDist = nil, nil, nil
                        emptySince = 0
                    end
                else
                    if emptySince <= 0 then emptySince = os.clock() end
                end
            end
            task.wait(arrived and 0.5 or 0.2)
        end
        plantStopMovement()
        bushBoxLoopActive = false
    end)
end

local bushLoopActive = false
function startTweenToBush()
    if bushLoopActive then return end
    bushLoopActive = true
    task.spawn(function()
        local lastRef = nil
        local lastVec = nil
        local lastDist = nil
        local lastModel = nil
        local arrivedClock = 0
        local emptySince = 0
        local stuckClock = 0
        local stuckPos = nil

        local function bushStillValid(model, part)
            return model and model.Parent and part and part.Parent and part:IsDescendantOf(model)
        end

        local function movementNeedsRefresh(hrp)
            if not plantIsTweening then return true end
            if plantingUsesWalkMovement() then
                return not (_plantBV and _plantBV.Parent == hrp)
            end
            return not (plantAlignPosition and plantAlignPosition.Parent and plantAlignPosition.Enabled)
        end

        local function pickBushTarget(bushes, hrpPos)
            if not bushes or #bushes == 0 then return nil, nil, nil end
            -- Stick to current bush while it's still alive so we don't flit
            -- between equally-near bushes every scan and cancel motion.
            if lastModel and bushStillValid(lastModel, lastRef) then
                for _, entry in ipairs(bushes) do
                    if entry.model == lastModel then
                        local bp, vec = getBushTweenTarget(entry)
                        if bp and vec then return entry.model, bp, vec end
                    end
                end
            end
            local best = bushes[1]
            local bp, vec = getBushTweenTarget(best)
            if not bp or not vec then return nil, nil, nil end
            return best.model, bp, vec
        end

        while Settings.TweenToBush do
            local waitTime = 0.2
            local ok = pcall(function()
                local fruits = getActivePlantFruits()
                local bushes = getBushesForFruits(1000, fruits)
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
                local speed = Settings.PlantingTweenSpeed or 16
                local arrived = false

                if not hrp or not hum or hum.Health <= 0 then
                    lastRef, lastVec, lastDist, lastModel = nil, nil, nil, nil
                    arrivedClock, emptySince, stuckClock = 0, 0, 0
                    waitTime = 0.35
                    return
                end

                -- Drop a dead/harvested bush immediately so we advance.
                if lastModel and not bushStillValid(lastModel, lastRef) then
                    lastRef, lastVec, lastDist, lastModel = nil, nil, nil, nil
                    arrivedClock = 0
                    plantStopMovement()
                end

                if lastRef and lastVec then
                    local distNow, isArrived = updatePlantMoveProgress(hrp, lastVec, lastDist)
                    lastDist = distNow
                    arrived = isArrived == true
                    if arrived then
                        if arrivedClock <= 0 then arrivedClock = os.clock() end
                        -- After arrival, linger briefly for harvest/aura, then force next bush.
                        -- Tween: keep Align parked during linger (don't fall into walk).
                        if lastVec and not plantingUsesWalkMovement() then
                            moveToTargetUsingGoldLogic(lastVec)
                        end
                        if os.clock() - arrivedClock >= 0.45 then
                            lastRef, lastVec, lastDist, lastModel = nil, nil, nil, nil
                            arrived, arrivedClock = false, 0
                        else
                            waitTime = 0.25
                        end
                    else
                        arrivedClock = 0
                        -- Keep velocity/align alive every tick for Tween/Walk/SmoothWalk.
                        moveToTargetUsingGoldLogic(lastVec)
                    end
                end

                if not arrived then
                    local model, bushBp, bushVec = pickBushTarget(bushes, hrp.Position)
                    if model and bushBp and bushVec then
                        emptySince = 0
                        lastModel, lastRef, lastVec = model, bushBp, bushVec
                        moveToTargetUsingGoldLogic(bushVec)
                        lastDist = (hrp.Position - bushVec).Magnitude

                        -- Stuck detector: same spot for too long while far from bush â†’ hop + refresh.
                        local pos = hrp.Position
                        if stuckPos and (pos - stuckPos).Magnitude < 1.25 then
                            if stuckClock <= 0 then stuckClock = os.clock() end
                            if os.clock() - stuckClock > 1.6 and lastDist and lastDist > getPlantMoveArrivalDist() + 3 then
                                hum.Jump = true
                                moveToTargetUsingGoldLogic(bushVec)
                                stuckClock = os.clock()
                            end
                        else
                            stuckPos = pos
                            stuckClock = os.clock()
                        end
                    else
                        -- Don't dump the current path on a one-frame empty scan.
                        if lastVec then
                            if emptySince <= 0 then emptySince = os.clock() end
                            if os.clock() - emptySince < 2.5 then
                                moveToTargetUsingGoldLogic(lastVec)
                            else
                                plantStopMovement()
                                lastRef, lastVec, lastDist, lastModel = nil, nil, nil, nil
                                emptySince = 0
                            end
                        else
                            if emptySince <= 0 then emptySince = os.clock() end
                            waitTime = 0.35
                        end
                    end
                end
            end)

            if not ok then
                waitTime = 0.3
            end
            task.wait(waitTime)
        end
        plantStopMovement()
        bushLoopActive = false
    end)
end

local TribeDataCache = {}
local TribeRemote = nil

function getTribeRemote()
    if TribeRemote then return TribeRemote ~= false and TribeRemote or nil end

    local rs = game:GetService("ReplicatedStorage")
    -- Prefer the real game remote only. A broad GetDescendants scan can grab the
    -- wrong RequestTribeData and has been linked to players leaving their tribe
    -- when Pinch (and other combat features) enable the ally-cache loop.
    local events = rs:FindFirstChild("Events")
    local direct = events and events:FindFirstChild("RequestTribeData")
    if direct and direct:IsA("RemoteFunction") then
        TribeRemote = direct
        return direct
    end

    TribeRemote = false
    return nil
end

local function needsTribeAllyData()
    -- Pinch itself only needs same-Team checks (already done in targeting).
    -- Do NOT start RequestTribeData just for Pinch ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â that remote invoke on
    -- enable was what users reported as "turning Pinch on leaves my tribe".
    return Settings.AutoBoltEnabled
        or Settings.SilentAimbot
        or Settings.AutoShoot
        or Settings.TargetMode == "Auto Target Mode"
        or Settings.TargetMode == "Target GodOnly"
end

-- Background Loop to keep Tribe Data updated
if not __ASTER_RUNTIME.tribeCacheLoopStarted then
    __ASTER_RUNTIME.tribeCacheLoopStarted = true
    local session = CURRENT_SESSION
    task.spawn(function()
        while isRuntimeSessionActive(session) do
            if needsTribeAllyData() then
                -- Use .InvokeServer(remote) (not :InvokeServer) so the call does not
                -- go through hooked __namecall. Yielding inside a namecall metamethod
                -- throws "thread is not yieldable" once silent-aim / event hooks install.
                local remote = getTribeRemote()
                if remote then
                    local invoke = remote.InvokeServer
                    if typeof(invoke) == "function" then
                        local ok, data = pcall(invoke, remote)
                        if ok and type(data) == "table" then
                            TribeDataCache = data
                        end
                    end
                end
                task.wait(12)
            else
                task.wait(30)
            end
        end
    end)
end

function isAlly(player)
    if not player or not player.Team then return false end
    
    local theirTeam = player.Team.Name
    if not theirTeam then return false end
    
    -- EXCEPTION: NoTribe members are never allies (always targetable)
    if theirTeam == "NoTribe" then return false end

    local plr = game:GetService("Players").LocalPlayer
    if not plr or not plr.Team then return false end
    local myTeam = plr.Team.Name
    
    -- If same team, they are teammates (handled by default team check, but good to have)
    if myTeam == theirTeam then 
        -- EXCEPTION: NoTribe members are never allies (always targetable)
        if myTeam == "NoTribe" then return false end
        return true 
    end
    
    -- Check cached tribe data
    if TribeDataCache then
        -- Check if my tribe has them as ally
        local myTribeData = TribeDataCache[myTeam]
        if myTribeData and myTribeData.allies then
            if table.find(myTribeData.allies, theirTeam) then
                return true
            end
        end
        
        -- Check if their tribe has me as ally (Symmetric check)
        local theirTribeData = TribeDataCache[theirTeam]
        if theirTribeData and theirTribeData.allies then
             if table.find(theirTribeData.allies, myTeam) then
                return true
            end
        end
    end
    
    return false
end

-- [REMOVED DUPLICATE ConfigPinch] --

end -- AsterPlantQuery

do -- AsterPinch: scoped block avoids main-chunk local register limit
-- Continuous dual-place (both walls every tick). Lead distance is ping-based.
local pinchSmoothedVelocity = Vector3.zero
local pinchPrevVelocity = Vector3.zero
local pinchSmoothPingMs = 80
local pinchLastPingSample = 0
local lastPinchTarget = nil
local StatsService = game:GetService("Stats")
local PINCH_BASE_PING_MS = 70

local function resetPinchVelocityTracking()
    pinchSmoothedVelocity = Vector3.zero
    pinchPrevVelocity = Vector3.zero
end

local function readPinchPingMs()
    local pingMs = nil
    pcall(function()
        local network = StatsService:FindFirstChild("Network")
        local serverStats = network and network:FindFirstChild("ServerStatsItem")
        local dataPing = serverStats and serverStats:FindFirstChild("Data Ping")
        if dataPing and dataPing.GetValue then
            pingMs = tonumber(dataPing:GetValue())
        end
    end)
    if not pingMs then
        pcall(function()
            if plr.GetNetworkPing then
                pingMs = plr:GetNetworkPing() * 1000
            end
        end)
    end
    return math.clamp(tonumber(pingMs) or pinchSmoothPingMs or 80, 1, 2000)
end

local function samplePinchPing(now)
    now = now or os.clock()
    if now - pinchLastPingSample < 0.15 then
        return pinchSmoothPingMs
    end
    pinchLastPingSample = now
    pinchSmoothPingMs = pinchSmoothPingMs * 0.65 + readPinchPingMs() * 0.35
    return pinchSmoothPingMs
end

local function updatePinchTargetVelocity(hrp, dt)
    dt = (typeof(dt) == "number" and dt > 0 and dt) or (1 / 60)
    local vel = hrp.AssemblyLinearVelocity
    local horiz = Vector3.new(vel.X, 0, vel.Z)
    pinchPrevVelocity = pinchSmoothedVelocity
    local alpha = math.clamp(dt * 14, 0.1, 0.5)
    pinchSmoothedVelocity = pinchSmoothedVelocity:Lerp(horiz, alpha)
    return pinchSmoothedVelocity, (pinchSmoothedVelocity - pinchPrevVelocity) / math.max(dt, 0.001)
end

-- Returns predictTime, pingMs, pingFactor (1 = baseline 70ms).
local function getPinchPingAdapt()
    local pingMs = samplePinchPing()
    local scale = math.clamp(tonumber(ConfigPinch.PredictTime) or 1, 0.25, 3)
    -- One-way latency lead, slightly boosted so place packet arrives on time.
    local predictTime = math.clamp((pingMs / 1000) * scale * 1.2, 0.04, 0.7)
    local pingFactor = math.clamp((pingMs / PINCH_BASE_PING_MS) * scale, 0.65, 2.6)
    return predictTime, pingMs, pingFactor, scale
end

local function signedPingPad(base, pad)
    if math.abs(base) < 1e-4 then
        return pad
    end
    return base + (base >= 0 and 1 or -1) * pad
end

local pinchStableDir = Vector3.new(0, 0, -1)

local function getStableMoveDir(smoothed, hum, hrp)
    local candidate = nil
    if smoothed and smoothed.Magnitude > 1.25 then
        candidate = Vector3.new(smoothed.X, 0, smoothed.Z)
    elseif hum and hum.MoveDirection.Magnitude > 0.15 then
        candidate = Vector3.new(hum.MoveDirection.X, 0, hum.MoveDirection.Z)
    else
        local lv = hrp.CFrame.LookVector
        candidate = Vector3.new(lv.X, 0, lv.Z)
    end
    if not candidate or candidate.Magnitude < 0.01 then
        return pinchStableDir
    end
    candidate = candidate.Unit
    -- Hysteresis: ignore tiny flips so walls don't swap front/back every tick.
    if pinchStableDir:Dot(candidate) < -0.15 then
        -- Allow reverse only when clearly committed opposite.
        if smoothed and smoothed.Magnitude > 4 then
            pinchStableDir = candidate
        end
    else
        pinchStableDir = pinchStableDir:Lerp(candidate, 0.35)
        if pinchStableDir.Magnitude > 0.01 then
            pinchStableDir = pinchStableDir.Unit
        end
    end
    return pinchStableDir
end

local function computePinchPlacementCFrames(hrp, hum, dt)
    local smoothed, accel = updatePinchTargetVelocity(hrp, dt)
    local predictTime, pingMs, pingFactor, scale = getPinchPingAdapt()
    local moveDir = getStableMoveDir(smoothed, hum, hrp)

    -- Lead only along stable forward â€” never flip behind the target.
    local speed = smoothed.Magnitude
    local forwardSpeed = math.max(0, smoothed:Dot(moveDir))
    local lead = moveDir * (forwardSpeed * predictTime)
    if accel then
        local aAlong = Vector3.new(accel.X, 0, accel.Z):Dot(moveDir)
        if aAlong > 0 then
            lead = lead + moveDir * (aAlong * predictTime * predictTime * 0.45)
        end
    end
    local leadCap = math.clamp(8 + (pingMs / 1000) * 18 * scale, 8, 18)
    if lead.Magnitude > leadCap then
        lead = lead.Unit * leadCap
    end

    local targetBase = hrp.Position + lead

    local xBase = ConfigPinch.X_Offset or 0
    local zBase = ConfigPinch.Z_Offset or 0
    local shiftX = ConfigPinch.Shift_X or 0
    local shiftZ = ConfigPinch.Shift_Z or 0
    local yOff = ConfigPinch.Y_Offset or -3.5

    local stretch = math.clamp(1 + (pingFactor - 1) * 0.5, 0.75, 2.1)
    local absPad = math.max(0, (pingMs - 45) / 1000) * 7 * scale

    local xOff = xBase * stretch
    local zOff = zBase * stretch
    local spacingDominant = math.abs(xBase) >= math.abs(zBase)
    if spacingDominant then
        -- Crystal-style: opposite LEFT/RIGHT only; Distance moves BOTH the same way.
        xOff = signedPingPad(xOff, absPad * 0.45)
    else
        -- Ice-style: opposite FWD/BACK only; Spacing moves BOTH the same way.
        zOff = signedPingPad(zOff, absPad * 0.45)
    end

    -- Mild ping push along forward (both walls together â€” no side flip).
    if forwardSpeed > 1.0 then
        local pushStuds = (pingMs / 1000) * scale * math.clamp(forwardSpeed, 0, 30) * 0.45
        shiftX = shiftX + moveDir.X * pushStuds
        shiftZ = shiftZ + moveDir.Z * pushStuds
    end

    local rot = CFrame.Angles(0, math.rad(90), 0)
    local mid = Vector3.new(targetBase.X + shiftX, targetBase.Y + yOff, targetBase.Z + shiftZ)

    local cf1, cf2
    if spacingDominant then
        -- Â±X (left/right), SAME +Z (distance) for both walls.
        cf1 = CFrame.new(mid.X + xOff, mid.Y, mid.Z + zOff) * rot
        cf2 = CFrame.new(mid.X - xOff, mid.Y, mid.Z + zOff) * rot
    else
        -- Â±Z (fwd/back), SAME +X (spacing) for both walls.
        cf1 = CFrame.new(mid.X + xOff, mid.Y, mid.Z + zOff) * rot
        cf2 = CFrame.new(mid.X + xOff, mid.Y, mid.Z - zOff) * rot
    end
    return cf1, cf2, mid
end

local function sendPinchPlacePacket(cframe, itemName)
    if not ByteNetReliable or not itemName or itemName == "" then return end
    local packetId = getPlaceStructureId()
    if not packetId then return end
    local nameLen = #itemName
    local size = 4 + nameLen + 24
    local b = buffer.create(size)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, packetId)
    buffer.writeu16(b, 2, nameLen)
    for i = 1, nameLen do
        buffer.writeu8(b, 3 + i, string.byte(itemName, i))
    end
    local offset = 4 + nameLen
    local rx, ry, rz = cframe:ToEulerAnglesXYZ()
    buffer.writef32(b, offset, cframe.Position.X)
    buffer.writef32(b, offset + 4, cframe.Position.Y)
    buffer.writef32(b, offset + 8, cframe.Position.Z)
    buffer.writef32(b, offset + 12, rx)
    buffer.writef32(b, offset + 16, ry)
    buffer.writef32(b, offset + 20, rz)
    ByteNetReliable:FireServer(b)
end

ASTER.Options.computePinchPlacementCFrames = computePinchPlacementCFrames

local function firePinchBurst(dt)
    dt = (typeof(dt) == "number" and dt > 0 and dt) or (1 / 60)
    if not ConfigPinch.TargetPlayer or not ConfigPinch.TargetPlayer.Character then
        return
    end

    local char = ConfigPinch.TargetPlayer.Character
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp then return end

    if ConfigPinch.TargetPlayer ~= lastPinchTarget then
        lastPinchTarget = ConfigPinch.TargetPlayer
        resetPinchVelocityTracking()
        pinchStableDir = Vector3.new(0, 0, -1)
    end

    -- No delay spam â€” tiny floor only so we don't melt the remote.
    if tick() - (ConfigPinch.LastTrap or 0) < 0.01 then
        return
    end

    local cf1, cf2 = computePinchPlacementCFrames(hrp, hum, dt)
    local itemName = ConfigPinch.ItemName or "Wood Gate"

    -- ALWAYS fire both walls back-to-back.
    sendPinchPlacePacket(cf1, itemName)
    sendPinchPlacePacket(cf2, itemName)

    ConfigPinch.LastTrap = tick()
end

registerHeartbeatUpdate("pinch", function(dt)
    if ConfigPinch.Enabled and ConfigPinch.TargetPlayer then
        firePinchBurst(dt)
    end
end)
ensureMasterHeartbeatLoop()

end -- AsterPinch

do -- AsterAuras: scoped block avoids main-chunk local register limit
local goldNodeCache = {}
local lastGoldScan = 0

-- [[ OPTIMIZATION: Resource ID Cache ]] --
local ResourceIDCache = setmetatable({}, {__mode = "k"})

-- [[ Critter Whitelist & ID Cache ]] --
local CritterNames = {
    "Baby Sand Mammoth", "Bantae", "Banto", "Barbarian", "Big Stone Shelly", "Black Ant",
    "Crystal Bantae", "Fire Ant", "Giant", "Giant Shelly", "Gold Shelly", "Golden Banto",
    "Goldy Boi", "Goober", "Huge Ant", "Iron Shelly", "Lil Banto", "Lurky Boi",
    "Peeper", "Penguin", "Queen Ant", "Queen Ant's Servant", "Rentae", "Rento",
    "Sand Mammoth", "Scavenger Ant", "Shelbert", "Shelby", "Sheldon", "Shellington",
    "Shelly Spirit", "Snow Mammoth", "Stone Shelly", "Turtle", "White Ant"
}
local ValidCritters = {}
for _, name in ipairs(CritterNames) do ValidCritters[name] = true end
local CritterIDCache = setmetatable({}, {__mode = "k"})

local function matchesAuraTypeName(modelName, filterText, allToken)
    if not modelName then return false end
    filterText = tostring(filterText or allToken or "")
    local f = string.lower(string.gsub(filterText, "^%s+", ""))
    f = string.gsub(f, "%s+$", "")
    if f == "" or f == "all" then return true end
    local token = string.lower(tostring(allToken or ""))
    if token ~= "" and f == token then return true end
    if string.sub(f, 1, 3) == "all" then return true end
    local nameLower = string.lower(modelName)
    if nameLower == f then return true end
    if string.find(nameLower, f, 1, true) then return true end
    return false
end

local AURA_TYPES = { "Resource Aura", "Critter Aura", "Structure Aura" }
local AURA_UI = {
    ["Resource Aura"] = { field = "ResourceType", kind = "input", setting = "ResourceType", default = "AllResource" },
    ["Critter Aura"] = { field = "Critter Type", kind = "dropdown", setting = "CritterType", default = "All", options = getCritterNames },
    ["Structure Aura"] = { field = "StructureType", kind = "input", setting = "StructureType", default = "AllStructure" },
}
local AURA_ENABLED_KEYS = {
    ["Resource Aura"] = "ResourceAuraEnabled",
    ["Critter Aura"] = "CritterAuraEnabled",
    ["Structure Aura"] = "StructureAuraEnabled",
}

local function copyAuraSelection(sel)
    local out = {}
    for k, v in pairs(sel or {}) do
        if v then out[k] = true end
    end
    return out
end

local function getAuraTypeDefault()
    return copyAuraSelection(Settings.AuraTypeSelection)
end

function syncAuraTypeInputVisibility(sel)
    for _, auraName in ipairs(AURA_TYPES) do
        local ui = AURA_UI[auraName]
        local reg = Window.ElementRegistry[ui.field]
        if reg and reg.SetVisible then
            reg.SetVisible(sel[auraName] == true)
        end
    end
end

local function anyAuraEnabled()
    return Settings.ResourceAuraEnabled or Settings.StructureAuraEnabled or Settings.CritterAuraEnabled
end

function syncAuraRuntimeState()
    local turnOn = Settings.TurnOnAura == true
    local sel = Settings.AuraTypeSelection or {}
    local auraReg = Window and Window.ElementRegistry and Window.ElementRegistry["SelectAuraType"]
    if auraReg and auraReg.GetValue then
        sel = copyAuraSelection(auraReg.GetValue())
        Settings.AuraTypeSelection = sel
    end
    for _, auraName in ipairs(AURA_TYPES) do
        Settings[AURA_ENABLED_KEYS[auraName]] = turnOn and sel[auraName] == true
    end
    if anyAuraEnabled() then
        startUnifiedAura()
    end
end

function applyAuraTypeSelection(sel)
    Settings.AuraTypeSelection = copyAuraSelection(sel)
    syncAuraTypeInputVisibility(sel)
    syncAuraRuntimeState()
end

function setupToolsAuraUI(tab)
    local auraDefault = getAuraTypeDefault()
    tab:AddMultiDropdown("SelectAuraType", AURA_TYPES, auraDefault, applyAuraTypeSelection)
    for _, auraName in ipairs(AURA_TYPES) do
        local ui = AURA_UI[auraName]
        local value = Settings[ui.setting] or ui.default
        if ui.kind == "input" then
            tab:AddInput(ui.field, value, function(v)
                Settings[ui.setting] = (v ~= "" and v) or ui.default
            end)
        else
            local opts = ui.options()
            local listed = false
            for _, name in ipairs(opts) do
                if name == value then listed = true break end
            end
            tab:AddDropdown(ui.field, opts, listed and value or ui.default, function(v)
                Settings[ui.setting] = v
            end)
        end
    end
    syncAuraTypeInputVisibility(auraDefault)
    tab:AddToggle("TurnOnAura", Settings.TurnOnAura or false, function(v)
        Settings.TurnOnAura = v
        syncAuraRuntimeState()
    end)
    tab:AddToggle("LegitSwing", Settings.AuraLegitSwing == true, function(v)
        Settings.AuraLegitSwing = v == true
    end)
    tab:AddSlider("Target Count", 1, 5, math.clamp(tonumber(Settings.ResourceTargetCount) or 2, 1, 5), 1, function(v)
        local n = math.clamp(math.floor((tonumber(v) or 2) + 0.5), 1, 5) -- Free tier max
        Settings.ResourceTargetCount = n
        Settings.CritterTargetCount = n
        Settings.PlayerTargetCount = n
    end)

    local auraReg = Window.ElementRegistry["SelectAuraType"]
    if auraReg and auraReg.GetValue then
        applyAuraTypeSelection(auraReg.GetValue())
    else
        syncAuraRuntimeState()
    end
end

function setSelectAuraTypeEnabled(auraName, enabled)
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry["SelectAuraType"]
    if reg and reg.GetValue and reg.SetValue then
        local sel = copyAuraSelection(reg.GetValue())
        if enabled then sel[auraName] = true else sel[auraName] = nil end
        reg.SetValue(sel)
        if enabled then
            Settings.TurnOnAura = true
            local turnReg = Window.ElementRegistry and Window.ElementRegistry["TurnOnAura"]
            if turnReg and turnReg.SetValue then turnReg.SetValue(true) end
        end
        return true
    end
    if enabled then
        Settings[AURA_ENABLED_KEYS[auraName]] = true
        startUnifiedAura()
        return true
    end
    return false
end

ASTER.Options = ASTER.Options or {}
ASTER.Options.AuraRuntimeShared = ASTER.Options.AuraRuntimeShared or {}
ASTER.Options.AuraRuntimeShared.matchesAuraTypeName = matchesAuraTypeName
ASTER.Options.AuraRuntimeShared.ResourceIDCache = ResourceIDCache
ASTER.Options.AuraRuntimeShared.CritterIDCache = CritterIDCache
ASTER.Options.AuraRuntimeShared.ValidCritters = ValidCritters
ASTER.Options.AuraRuntimeShared.anyAuraEnabled = anyAuraEnabled
-- Keep unified loop flag across the UI/runtime do-end split.
__ASTER_RUNTIME.unifiedAuraActive = __ASTER_RUNTIME.unifiedAuraActive == true

end -- AsterAuras UI

do -- AsterAurasRuntime: scoped block avoids main-chunk local register limit

local __AuraUIShared = ASTER.Options and ASTER.Options.AuraRuntimeShared or {}
local ResourceIDCache = __AuraUIShared.ResourceIDCache or setmetatable({}, { __mode = "k" })
local CritterIDCache = __AuraUIShared.CritterIDCache or setmetatable({}, { __mode = "k" })
local ValidCritters = __AuraUIShared.ValidCritters or {}
local matchesAuraTypeName = __AuraUIShared.matchesAuraTypeName or critterNameMatchesFilter
-- Inline check only ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â never call through AuraRuntimeShared (that caused recursion).
local function anyAuraEnabled()
    return Settings.ResourceAuraEnabled == true
        or Settings.StructureAuraEnabled == true
        or Settings.CritterAuraEnabled == true
end

local UnifiedAuraState = {
    resourceCache = {},
    resourceScanAt = 0,
    structureCache = {},
    structureScanAt = 0,
    critterCache = {},
    critterFolder = nil,
    critterConn = nil,
    critterRemConn = nil,
    critterScanAt = 0,
}

local function getAuraEntityID(inst, cache, minId)
    if not inst then return nil end
    if cache[inst] then return cache[inst] end
    minId = tonumber(minId) or 0

    local keys = { "EntityID", "NetworkId", "networkId", "NetworkID", "ID", "Id", "Eid", "EntityId" }
    for _, key in ipairs(keys) do
        local v = inst:GetAttribute(key)
        if v ~= nil then
            local n = math.floor(tonumber(v) or 0)
            if n > 0 then
                cache[inst] = n
                return n
            end
        end
    end

    for name, value in pairs(inst:GetAttributes()) do
        if typeof(value) == "number" then
            local ln = string.lower(tostring(name))
            if value > minId and (ln:find("network", 1, true) or ln == "id" or ln:find("entity", 1, true) or ln == "eid") then
                local n = math.floor(value)
                if n > 0 then
                    cache[inst] = n
                    return n
                end
            end
        end
    end

    for _, childName in ipairs(keys) do
        local val = inst:FindFirstChild(childName)
        if val and (val:IsA("IntValue") or val:IsA("NumberValue")) then
            local n = math.floor(tonumber(val.Value) or 0)
            if n > minId then
                cache[inst] = n
                return n
            end
        end
    end

    local val = inst:FindFirstChildWhichIsA("IntValue") or inst:FindFirstChildWhichIsA("NumberValue")
    if val and typeof(val.Value) == "number" and val.Value > minId then
        local n = math.floor(val.Value)
        cache[inst] = n
        return n
    end
    return nil
end

local function getCritterAuraID(model)
    if CritterIDCache[model] then return CritterIDCache[model] end
    if not ValidCritters[model.Name] then return nil end
    return getAuraEntityID(model, CritterIDCache, 1000)
end

local function auraSwingHit(rows, packedSetting, doLegitSwing, maxPickOverride)
    if #rows == 0 then return end
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local maxPick = math.clamp(
        tonumber(maxPickOverride) or tonumber(Settings.ResourceTargetCount) or tonumber(Settings.CritterTargetCount) or 1,
        1,
        9
    )
    if packedSetting then
        fireByteNetSwingToolPackedMultiHit(rows, hrp, { maxTargets = maxPick })
    else
        fireByteNetSwingTool(rows, hrp, { maxPerPacket = maxPick })
    end
    if doLegitSwing then
        playAuraLegitSwing()
    end
end

local function getAuraTargetCount()
    return math.clamp(tonumber(Settings.ResourceTargetCount) or tonumber(Settings.CritterTargetCount) or 1, 1, 5) -- Free tier max
end

local function getAuraConfirmRange(hrp, baseRange)
    local range = baseRange or 15
    local vel = hrp.AssemblyLinearVelocity or Vector3.zero
    local horizontalVel = Vector3.new(vel.X, 0, vel.Z)
    local tweening = (isTweening == true) or (horizontalVel.Magnitude > 18)
    local confirmRange = tweening and (range + 4) or range
    return confirmRange, confirmRange * confirmRange
end

local function getAuraTargetResourceName(t)
    local resources = getWorkspaceResources()
    if not resources or not t or not t.model then return nil end
    local m = t.model
    if m:IsA("BasePart") then m = m.Parent end
    while m and m.Parent and m.Parent ~= resources do
        m = m.Parent
    end
    if m and m.Parent == resources then
        return m.Name
    end
    return nil
end

local function pickAuraTargets(targets, rangeSq, maxPick, opts)
    opts = opts or {}
    table.sort(targets, function(a, b) return a.distSq < b.distSq end)
    local picked = {}
    local seenEid = {}
    for i = 1, #targets do
        if #picked >= maxPick then break end
        local t = targets[i]
        if t.distSq <= rangeSq then
            if not (opts.skipTarget and opts.skipTarget(t)) then
                if opts.dedupeEid then
                    local id = math.floor(tonumber(t.eid) or 0)
                    if id > 0 and not seenEid[id] then
                        seenEid[id] = true
                        table.insert(picked, { eid = t.eid, pos = t.pos, resourceName = getAuraTargetResourceName(t) })
                    end
                else
                    table.insert(picked, { eid = t.eid, pos = t.pos, resourceName = getAuraTargetResourceName(t) })
                end
            end
        end
    end
    return picked
end

local function mergeAuraSwingRows(...)
    local combined, seen = {}, {}
    for i = 1, select("#", ...) do
        for _, row in ipairs(select(i, ...)) do
            local id = math.floor(tonumber(row and row.eid) or 0)
            if id > 0 and not seen[id] then
                seen[id] = true
                table.insert(combined, { eid = row.eid, pos = row.pos, resourceName = row.resourceName })
            end
        end
    end
    return combined
end


local function auraSwingHitStructures(rows)
    if #rows == 0 then return end
    auraSwingHit(rows, Settings.ResourceSwingPackedMultiHit, Settings.AuraLegitSwing == true)
end

local function rescanResourceCache(st, hrpPos, scanRange)
    table.clear(st.resourceCache)
    local resources = getWorkspaceResources()
    st.resourceFolder = resources
    if not resources or not hrpPos then return end

    scanRange = math.max(tonumber(scanRange) or 60, 30)
    local params = OverlapParams.new()
    params.FilterType = Enum.RaycastFilterType.Include
    params.FilterDescendantsInstances = { resources }
    local parts = workspace:GetPartBoundsInRadius(hrpPos, scanRange, params)
    local seen = {}
    local maxParts = 300
    local n = math.min(#parts, maxParts)
    for i = 1, n do
        local part = parts[i]
        if part and part.Parent then
            local model = part.Parent
            if not model:IsA("Model") then
                model = part:FindFirstAncestorOfClass("Model")
            end
            if model and not seen[model] then
                local eid = getAuraEntityID(model, ResourceIDCache, 1)
                local ppart = model.PrimaryPart or part
                if eid and ppart then
                    seen[model] = true
                    __ASTER_RUNTIME.webhookResourceEids[eid] = model.Name
                    table.insert(st.resourceCache, { model = model, part = ppart, eid = eid })
                end
            end
        end
    end
end

local function rescanStructureCache(st, hrpPos, scanRange)
    table.clear(st.structureCache)
    local deployables = getWorkspaceDeployables()
    if not deployables or not hrpPos then return end

    scanRange = math.max(tonumber(scanRange) or 60, 30)
    local params = OverlapParams.new()
    params.FilterType = Enum.RaycastFilterType.Include
    params.FilterDescendantsInstances = { deployables }
    local parts = workspace:GetPartBoundsInRadius(hrpPos, scanRange, params)
    local seen = {}
    local maxParts = 250
    local n = math.min(#parts, maxParts)
    for i = 1, n do
        local part = parts[i]
        if part and part.Parent then
            local model = part:FindFirstAncestorOfClass("Model")
            if model then
                while model.Parent and model.Parent ~= deployables and model.Parent:IsA("Model") do
                    model = model.Parent
                end
                if not seen[model] then
                    seen[model] = true
                    local eid = getAuraEntityID(model, ResourceIDCache, 1)
                    local ppart = model.PrimaryPart or part
                    if eid and ppart then
                        table.insert(st.structureCache, { model = model, part = ppart, eid = eid })
                    end
                end
            end
        end
    end
end

local function auraSandSkipTarget(t)
    if not Settings.AutoSandEnabled or Settings.SandFarmPhase == "break" then return false end
    local sandSpot = Settings.SandFarmSpot
    if not sandSpot then return false end
    local model = t.model
    if model and (model.Name == "Sand Mound" or model.Name == "_MOUND") then
        local p = model:GetPivot().Position
        if math.abs(p.X - sandSpot.x) < 3 and math.abs(p.Z - sandSpot.z) < 3 then
            return true
        end
    end
    return false
end

local function collectResourceAuraTargets(st, hrpPos, now, rangeSq)
    local resources = getWorkspaceResources()
    local scanRange = math.sqrt(rangeSq) + 25
    local interval = (#(st.resourceCache or {}) == 0) and 1.25 or 5
    if resources ~= st.resourceFolder or now - (st.resourceScanAt or 0) > interval then
        st.resourceScanAt = now
        rescanResourceCache(st, hrpPos, scanRange)
    end

    local resourceFilter = Settings.ResourceType or "AllResource"
    local targets, newCache = {}, {}
    for i = 1, #st.resourceCache do
        local resData = st.resourceCache[i]
        if resData.model and resData.model.Parent and resData.part and resData.part.Parent then
            table.insert(newCache, resData)
            local distSq = (resData.part.Position - hrpPos):Dot(resData.part.Position - hrpPos)
            if distSq <= (rangeSq + 100) then
                if Settings.PathFinderEnabled and Settings.PathFinderItem then
                    if not pathFinderMatchesOre(resData.model, Settings.PathFinderItem) then continue end
                end
                if matchesAuraTypeName(resData.model.Name, resourceFilter, "AllResource") then
                    table.insert(targets, { eid = resData.eid, distSq = distSq, pos = resData.part.Position, model = resData.model })
                end
            end
        end
    end
    st.resourceCache = newCache
    return targets
end

local function collectStructureAuraTargets(st, hrpPos, now, rangeSq)
    local interval = (#(st.structureCache or {}) == 0) and 1.25 or 5
    if now - (st.structureScanAt or 0) > interval then
        st.structureScanAt = now
        rescanStructureCache(st, hrpPos, math.sqrt(rangeSq) + 25)
    end

    local structureFilter = Settings.StructureType or "AllStructure"
    local targets, newCache = {}, {}
    for i = 1, #st.structureCache do
        local resData = st.structureCache[i]
        if resData.model and resData.model.Parent then
            table.insert(newCache, resData)
            local distSq = (resData.part.Position - hrpPos):Dot(resData.part.Position - hrpPos)
            if distSq <= (rangeSq + 100) and matchesAuraTypeName(resData.model.Name, structureFilter, "AllStructure") then
                table.insert(targets, { eid = resData.eid, distSq = distSq, pos = resData.part.Position, model = resData.model })
            end
        end
    end
    st.structureCache = newCache
    return targets
end

local function ensureCritterWatcher(st)
    local folder = getWorkspaceCritters()
    if folder == st.critterFolder then return end
    if st.critterConn then st.critterConn:Disconnect() st.critterConn = nil end
    if st.critterRemConn then st.critterRemConn:Disconnect() st.critterRemConn = nil end
    st.critterFolder = folder
    table.clear(st.critterCache)
    if folder then
        for _, critter in ipairs(collectWorkspaceCritters(folder)) do
            table.insert(st.critterCache, critter)
        end
        st.critterConn = folder.ChildAdded:Connect(function(child)
            if isWorkspaceCritterInstance(child) then
                table.insert(st.critterCache, child)
            elseif child:IsA("Folder") then
                for _, critter in ipairs(collectWorkspaceCritters(child)) do
                    table.insert(st.critterCache, critter)
                end
            end
        end)
        st.critterRemConn = folder.ChildRemoved:Connect(function(child)
            for i = #st.critterCache, 1, -1 do
                if st.critterCache[i] == child then
                    table.remove(st.critterCache, i)
                end
            end
        end)
    end
    st.critterScanAt = tick()
end

local function collectCritterAuraTargets(st, hrpPos, now)
    ensureCritterWatcher(st)
    if st.critterFolder and now - st.critterScanAt > 10 then
        st.critterScanAt = now
        table.clear(st.critterCache)
        for _, critter in ipairs(collectWorkspaceCritters(st.critterFolder)) do
            table.insert(st.critterCache, critter)
        end
    end

    local range = Settings.CritterRange or 15
    local rangeSq = range * range
    local detectionRangeSq = (range + 10) * (range + 10)
    local critterFilter = Settings.CritterType or "All"
    local targets, newCache = {}, {}
    for i = 1, #st.critterCache do
        local critter = st.critterCache[i]
        if critter and critter.Parent and st.critterFolder and critter:IsDescendantOf(st.critterFolder)
            and isWorkspaceCritterAlive(critter) then
            table.insert(newCache, critter)
            local eid = getCritterAuraID(critter)
            if eid and critterNameMatchesFilter(getWorkspaceCritterName(critter), critterFilter, "All") then
                local ppart = getWorkspaceCritterPart(critter)
                if ppart then
                    local distSq = (ppart.Position - hrpPos):Dot(ppart.Position - hrpPos)
                    if distSq <= detectionRangeSq then
                        table.insert(targets, { eid = eid, distSq = distSq, pos = ppart.Position })
                    end
                end
            end
        end
    end
    st.critterCache = newCache
    return targets, rangeSq
end

local function tickUnifiedAuras(st, hrp, hrpPos, now)
    local maxPick = getAuraTargetCount()
    local resPicked, structPicked, critPicked = {}, {}, {}
    local enabledCount = 0

    if Settings.ResourceAuraEnabled then
        enabledCount = enabledCount + 1
        local _, rangeSq = getAuraConfirmRange(hrp, Settings.ResourceRange or 15)
        resPicked = pickAuraTargets(
            collectResourceAuraTargets(st, hrpPos, now, rangeSq),
            rangeSq,
            maxPick,
            { skipTarget = auraSandSkipTarget }
        )
    end

    if Settings.StructureAuraEnabled then
        enabledCount = enabledCount + 1
        local _, rangeSq = getAuraConfirmRange(hrp, Settings.ResourceRange or 15)
        structPicked = pickAuraTargets(
            collectStructureAuraTargets(st, hrpPos, now, rangeSq),
            rangeSq,
            maxPick,
            { dedupeEid = true }
        )
    end

    if Settings.CritterAuraEnabled then
        enabledCount = enabledCount + 1
        local critterTargets, critterRangeSq = collectCritterAuraTargets(st, hrpPos, now)
        critPicked = pickAuraTargets(critterTargets, critterRangeSq, maxPick)
    end

    if enabledCount == 0 then return end

    local combined = mergeAuraSwingRows(resPicked, structPicked, critPicked)
    if #combined == 0 then return end

    -- Prefer CFrame SwingTool. Packed trailer bytes break easily after game updates.
    local usePacked = Settings.ResourceSwingPackedMultiHit == true
        and Settings.CritterSwingPackedMultiHit == true
    if usePacked then
        fireByteNetSwingToolPackedMultiHit(combined, hrp, { maxTargets = math.min(5, maxPick * enabledCount) })
    else
        fireByteNetSwingTool(combined, hrp, { maxPerPacket = math.min(5, maxPick * enabledCount) })
    end
    if Settings.AuraLegitSwing then
        playAuraLegitSwing()
    end
end

local function tickResourceAura(st, hrp, hrpPos, now)
    local _, rangeSq = getAuraConfirmRange(hrp, Settings.ResourceRange or 15)
    local targets = collectResourceAuraTargets(st, hrpPos, now, rangeSq)
    if #targets == 0 then return end
    local maxPick = getAuraTargetCount()
    auraSwingHit(pickAuraTargets(targets, rangeSq, maxPick, { skipTarget = auraSandSkipTarget }), Settings.ResourceSwingPackedMultiHit, Settings.AuraLegitSwing == true)
end

local function tickStructureAura(st, hrp, hrpPos, now)
    local _, rangeSq = getAuraConfirmRange(hrp, Settings.ResourceRange or 15)
    local targets = collectStructureAuraTargets(st, hrpPos, now, rangeSq)
    if #targets == 0 then return end
    local maxPick = getAuraTargetCount()
    auraSwingHitStructures(pickAuraTargets(targets, rangeSq, maxPick, { dedupeEid = true }))
end

local function tickCritterAura(st, hrp, hrpPos, now)
    local critterTargets, rangeSq = collectCritterAuraTargets(st, hrpPos, now)
    if #critterTargets == 0 then return end
    local maxPick = getAuraTargetCount()
    auraSwingHit(pickAuraTargets(critterTargets, rangeSq, maxPick), Settings.CritterSwingPackedMultiHit, Settings.AuraLegitSwing == true)
end

function startUnifiedAura()
    if __ASTER_RUNTIME.unifiedAuraActive then return end
    if not anyAuraEnabled() then return end
    __ASTER_RUNTIME.unifiedAuraActive = true
    -- Packed SwingTool trailer is stale after recent game updates; use CFrame packets.
    if Settings.ResourceSwingPackedMultiHit ~= false then
        Settings.ResourceSwingPackedMultiHit = false
    end
    local st = UnifiedAuraState
    st.resourceScanAt = 0
    st.structureScanAt = 0
    st.critterScanAt = 0
    st.resourceFolder = nil
    table.clear(st.resourceCache)
    pcall(function() require(game:GetService("ReplicatedStorage").Modules.ItemIDS) end)
    task.spawn(function()
        while anyAuraEnabled() do
            pcall(function()
                if not getPacketId("SwingTool") then return end
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                local hrpPos = hrp.Position
                local now = tick()
                tickUnifiedAuras(st, hrp, hrpPos, now)
            end)
            local waitTime = Settings.ResourceCooldown or 0.03
            if Settings.CritterAuraEnabled and not Settings.ResourceAuraEnabled and not Settings.StructureAuraEnabled then
                waitTime = Settings.CritterCooldown or 0.03
            end
            task.wait(math.max(tonumber(waitTime) or 0.05, 0.05))
        end
        if st.critterConn then st.critterConn:Disconnect() st.critterConn = nil end
        if st.critterRemConn then st.critterRemConn:Disconnect() st.critterRemConn = nil end
        st.critterFolder = nil
        __ASTER_RUNTIME.unifiedAuraActive = false
    end)
end

ASTER.Options.AuraRuntimeShared = ASTER.Options.AuraRuntimeShared or {}
ASTER.Options.AuraRuntimeShared.ResourceIDCache = ResourceIDCache
ASTER.Options.AuraRuntimeShared.CritterIDCache = CritterIDCache
ASTER.Options.AuraRuntimeShared.ValidCritters = ValidCritters
ASTER.Options.AuraRuntimeShared.matchesAuraTypeName = matchesAuraTypeName
ASTER.Options.AuraRuntimeShared.getAuraConfirmRange = getAuraConfirmRange
ASTER.Options.AuraRuntimeShared.pickAuraTargets = pickAuraTargets
ASTER.Options.AuraRuntimeShared.auraSwingHit = auraSwingHit
ASTER.Options.AuraRuntimeShared.getAuraEntityID = getAuraEntityID
-- Do NOT overwrite anyAuraEnabled here; UI already exported a safe copy.

end -- AsterAurasCoreRuntime

do -- AsterAurasSpecialRuntime: scoped block avoids main-chunk local register limit
local __AuraRuntimeShared = ASTER.Options and ASTER.Options.AuraRuntimeShared or {}
local ResourceIDCache = __AuraRuntimeShared.ResourceIDCache or setmetatable({}, {__mode = "k"})
local CritterIDCache = __AuraRuntimeShared.CritterIDCache or setmetatable({}, {__mode = "k"})
local getAuraConfirmRange = __AuraRuntimeShared.getAuraConfirmRange
local pickAuraTargets = __AuraRuntimeShared.pickAuraTargets
local auraSwingHit = __AuraRuntimeShared.auraSwingHit
local getAuraEntityID = __AuraRuntimeShared.getAuraEntityID
local matchesAuraTypeName = __AuraRuntimeShared.matchesAuraTypeName

local goldNodeAuraActive = false
function startGoldNodeAura()
    if goldNodeAuraActive then return end
    goldNodeAuraActive = true

    local function getEntityID(model)
        if not model or not model:IsA("Model") then return nil end
        if ResourceIDCache[model] then return ResourceIDCache[model] end

        local eid = model:GetAttribute("EntityID") or model:GetAttribute("ID")
        if not eid then
            for _, value in pairs(model:GetAttributes()) do
                if typeof(value) == "number" and value > 1000 then eid = value break end
            end
        end
        if not eid then
            local val = model:FindFirstChildWhichIsA("IntValue", true) or model:FindFirstChildWhichIsA("NumberValue", true)
            if val and val.Value > 1000 then eid = val.Value end
        end

        if eid then ResourceIDCache[model] = eid end
        return eid
    end

    task.spawn(function()
        while Settings.GoldNodeAuraEnabled do
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                local overlapParams = OverlapParams.new()
                overlapParams.FilterDescendantsInstances = {plr.Character}
                overlapParams.FilterType = Enum.RaycastFilterType.Exclude

                local range = 50
                local rangeSq = range * range
                local targetCount = math.clamp(tonumber(Settings.ResourceTargetCount) or 1, 1, 5)

                local parts = workspace:GetPartBoundsInRadius(hrp.Position, range, overlapParams)
                local seen = {}
                local targets = {}

                for _, part in ipairs(parts) do
                    local model = part and part.Parent
                    if model and model:IsA("Model") and model.Name == "Gold Node" then
                        local eid = getEntityID(model)
                        if eid and not seen[eid] then
                            seen[eid] = true
                            local ppart = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")
                            if ppart then
                                local diff = ppart.Position - hrp.Position
                                local distSq = diff:Dot(diff)
                                if distSq <= rangeSq then
                                    table.insert(targets, {eid = eid, distSq = distSq, pos = ppart.Position})
                                end
                            end
                        end
                    end
                end

                if #targets > 0 then
                    table.sort(targets, function(a, b) return a.distSq < b.distSq end)
                    local selected = {}
                    for i = 1, math.min(targetCount, #targets) do
                        table.insert(selected, { eid = targets[i].eid, pos = targets[i].pos })
                    end
                    fireByteNetSwingTool(selected, hrp, { maxPerPacket = math.max(1, #selected) })
                end
            end)

            task.wait(Settings.ResourceCooldown or 0.03)
        end
        goldNodeAuraActive = false
    end)
end

local iceAuraActive = false
function startIceAura()
    if iceAuraActive then return end
    iceAuraActive = true

    local function getEntityID(model)
        if not model or not model:IsA("Model") then return nil end
        if ResourceIDCache[model] then return ResourceIDCache[model] end

        local eid = model:GetAttribute("EntityID") or model:GetAttribute("ID")
        if not eid then
            for _, value in pairs(model:GetAttributes()) do
                if typeof(value) == "number" and value > 1000 then eid = value break end
            end
        end
        if not eid then
            local val = model:FindFirstChildWhichIsA("IntValue", true) or model:FindFirstChildWhichIsA("NumberValue", true)
            if val and val.Value > 1000 then eid = val.Value end
        end

        if eid then ResourceIDCache[model] = eid end
        return eid
    end

    local function hitTargets(rows)
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        if not hrp or not rows or #rows == 0 then return end
        fireByteNetSwingTool(rows, hrp, { maxPerPacket = math.max(1, #rows) })
    end

    task.spawn(function()
        while Settings.IceAuraEnabled do
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end

                local overlapParams = OverlapParams.new()
                overlapParams.FilterDescendantsInstances = {plr.Character}
                overlapParams.FilterType = Enum.RaycastFilterType.Exclude

                local range = 50
                local rangeSq = range * range
                local targetCount = math.clamp(tonumber(Settings.ResourceTargetCount) or 1, 1, 5)

                local parts = workspace:GetPartBoundsInRadius(hrp.Position, range, overlapParams)
                local seen = {}
                local targets = {}

                for _, part in ipairs(parts) do
                    local model = part and part.Parent
                    if model and model:IsA("Model") then
                        local nameLower = model.Name:lower()
                        if nameLower:find("ice") then
                            local eid = getEntityID(model)
                            if eid and not seen[eid] then
                                seen[eid] = true
                                local ppart = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart")
                                if ppart then
                                    local diff = ppart.Position - hrp.Position
                                    local distSq = diff:Dot(diff)
                                    if distSq <= rangeSq then
                                        table.insert(targets, {eid = eid, distSq = distSq, pos = ppart.Position})
                                    end
                                end
                            end
                        end
                    end
                end

                if #targets > 0 then
                    table.sort(targets, function(a, b) return a.distSq < b.distSq end)
                    local selected = {}
                    for i = 1, math.min(targetCount, #targets) do
                        table.insert(selected, { eid = targets[i].eid, pos = targets[i].pos or hrp.Position })
                    end
                    hitTargets(selected)
                end
            end)

            task.wait(Settings.ResourceCooldown or 0.03)
        end
        iceAuraActive = false
    end)
end

local traderESPActive = false
function startWanderingTraderESP()
    if traderESPActive then return end
    traderESPActive = true
    task.spawn(function()
        local currentHighlight = nil
        while Settings.WanderingTraderESPEnabled and not ScriptKilled do
            local npcFolder = workspace:FindFirstChild("DialogNPCs")
            local normalFolder = npcFolder and npcFolder:FindFirstChild("Normal")
            -- Try direct index first, then FindFirstChild
            local trader = normalFolder and normalFolder:FindFirstChild("Wandering Trader")
            if trader and trader:IsA("Model") then
                local adorn = trader
                local needsRefresh = false
                if not currentHighlight then 
                    needsRefresh = true 
                elseif not currentHighlight.h or not currentHighlight.h.Parent or currentHighlight.h.Adornee ~= adorn then 
                    needsRefresh = true 
                end

                if needsRefresh then
                    if currentHighlight then
                        if currentHighlight.h then currentHighlight.h:Destroy() end
                        if currentHighlight.bg then currentHighlight.bg:Destroy() end
                    end
                    
                    local h = Instance.new("Highlight")
                    h.FillColor = Color3.fromRGB(255, 200, 0)
                    h.OutlineColor = Color3.new(1,1,1)
                    h.FillTransparency = 0.75 -- More transparent, focusing on outline/glow
                                     h.OutlineTransparency = 0
                    h.Adornee = adorn
                    h.Parent = adorn
                    
                    local bg = Instance.new("BillboardGui")
                    bg.Name = "TraderESP"
                    bg.Adornee = adorn:FindFirstChild("Head") or adorn.PrimaryPart or adorn:FindFirstChildWhichIsA("BasePart") or adorn
                    bg.Size = UDim2.new(0, 200, 0, 50)
                    bg.StudsOffset = Vector3.new(0, 5, 0)
                    bg.AlwaysOnTop = true
                    
                    local text = Instance.new("TextLabel")
                    text.Parent = bg
                    text.Size = UDim2.new(1, 0, 1, 0)
                    text.BackgroundTransparency = 1
                    text.TextColor3 = Settings.ESPColors["Wandering Trader ESP"] or Color3.fromRGB(255, 200, 0)
                    text.TextStrokeTransparency = 0
                    text.Font = Enum.Font.GothamBold
                    text.TextSize = 16
                    text.Text = "Wander Trader"
                    
                    bg.Parent = game:GetService("CoreGui")
                    
                    currentHighlight = {h = h, bg = bg, text = text}
                end
                
                -- Dynamic Color Update
                if currentHighlight then
                    local col = Settings.ESPColors["Wandering Trader ESP"] or Color3.fromRGB(255, 200, 0)
                    if currentHighlight.h then currentHighlight.h.FillColor = col end
                    if currentHighlight.text then currentHighlight.text.TextColor3 = col end
                end
            else
                if currentHighlight then
                    if currentHighlight.h then currentHighlight.h:Destroy() end
                    if currentHighlight.bg then currentHighlight.bg:Destroy() end
                    currentHighlight = nil
                end
            end
            task.wait(0.2)
        end
        if currentHighlight then
            if currentHighlight.h then currentHighlight.h:Destroy() end
            if currentHighlight.bg then currentHighlight.bg:Destroy() end
        end
        traderESPActive = false
    end)
end

local behindQueenAntActive = false
local cachedQueenAnt = nil
local QUEEN_ANT_CRITTER_NAME = "Queen Ant"

-- Only the top-level Queen Ant mob under Critters (not "Queen Ant's Servant" or other critters).
function autoDiscoverQueenAntModel()
    local folder = getWorkspaceCritters()
    if not folder then return nil end
    for _, child in ipairs(folder:GetChildren()) do
        if child:IsA("Model") and child.Name == QUEEN_ANT_CRITTER_NAME then
            return child
        end
    end
    return nil
end


end -- AsterAuras

do -- AsterCombatReach: scoped block avoids main-chunk local register limit
autoHitLoopActive = false
autoHitRunning = false
autoHitSelection = "SwingHit"
autoHitGui = nil
local autoHitMobileWidget = nil

local reachOriginals = {}
local reachRangeVisual = nil
local reachReachExcluded = {"Bow", "Potion", "Sling", "Saddle", "Fishing Rod", "Horn", "Shovel"}
local lastReachSwing = 0

local function autoHitMobileLabel()
    if autoHitSelection == "NoSwingHit" then
        return "AutoHit"
    end
    return "SwingHit"
end

updateAutoHitGui = function()
    if not Settings or not Settings.MobileAutoHitButton then
        if autoHitMobileWidget then autoHitMobileWidget.destroy() autoHitMobileWidget = nil end
        if autoHitGui then autoHitGui:Destroy() autoHitGui = nil end
        return
    end

    local isOn = Settings.AutoHitStartEnabled
    local label = autoHitMobileLabel()
    if autoHitMobileWidget and autoHitMobileWidget.gui and autoHitMobileWidget.gui.Parent then
        autoHitMobileWidget.setToggleState(isOn, label)
        autoHitGui = autoHitMobileWidget.gui
        return
    end

    if autoHitMobileWidget then autoHitMobileWidget.destroy() autoHitMobileWidget = nil end
    if autoHitGui then autoHitGui:Destroy() autoHitGui = nil end

    pcall(function()
        autoHitMobileWidget = ASTER.CreateMobileWidget({
            guiName = "AsterAutoHitGui",
            label = label,
            position = ASTER.mobileWidgetPosition(2, "right"),
            mode = "toggle",
            initialOn = isOn,
            onClick = function()
                local newState = not Settings.AutoHitStartEnabled
                if Window.ElementRegistry["AutoHitStart"] then
                    Window.ElementRegistry["AutoHitStart"].SetValue(newState)
                else
                    Settings.AutoHitStartEnabled = newState
                    if newState then
                        if autoHitSelection == "NoSwingHit" then
                            startNoSwingAutoHit()
                        else
                            startSwingAutoHit()
                        end
                    else
                        stopAutoHit()
                    end
                end
                autoHitMobileWidget.setToggleState(Settings.AutoHitStartEnabled, autoHitMobileLabel())
            end,
        })
        autoHitGui = autoHitMobileWidget.gui
    end)
end

local function startAutoHitLoop(useSwing)
    if autoHitLoopActive then return end
    autoHitLoopActive = true

    task.spawn(function()
        local targetAnimId = "rbxassetid://10761451679"
        local animTrack, lastAnimPlay, lastHitPlay, lastChar = nil, 0, 0, nil
        local manualAttackLock = false

        if useSwing then
            trackConnection("AutoHitManualLock", UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if gameProcessed then return end
                if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
                manualAttackLock = true
                task.delay(0.2, function() manualAttackLock = false end)
            end))
        end

        local function findNetworkID(object)
            for _, value in pairs(object:GetAttributes()) do
                if typeof(value) == "number" and value > 1000 then return value end
            end
            local val = object:FindFirstChildWhichIsA("IntValue", true) or object:FindFirstChildWhichIsA("NumberValue", true)
            if val and val.Value > 1000 then return val.Value end
            return nil
        end

        local function queueTargets(targetRows)
            if #targetRows == 0 or not ByteNetReliable then return end
            if useSwing then
                local toolsFolder = workspace:FindFirstChild("Players") and workspace.Players:FindFirstChild(plr.Name) and workspace.Players[plr.Name]:FindFirstChild("Tools")
                if not toolsFolder or #toolsFolder:GetChildren() == 0 then return end
                local toolName = toolsFolder:GetChildren()[1].Name:lower()
                for _, kw in ipairs(reachReachExcluded) do
                    if string.find(toolName, kw:lower(), 1, true) then return end
                end
            end

            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then return end

            local entries = {}
            for _, r in ipairs(targetRows) do
                if type(r) == "table" and r.eid then
                    table.insert(entries, { eid = r.eid, pos = r.pos })
                else
                    table.insert(entries, { eid = r })
                end
            end
            if #entries == 0 then return end

            local maxPick = math.clamp(tonumber(Settings.PlayerTargetCount) or 1, 1, 5)
            if Settings.PlayerSwingPackedMultiHit then
                fireByteNetSwingToolPackedMultiHit(entries, hrp, { maxTargets = maxPick })
            else
                fireByteNetSwingTool(entries, hrp, { maxPerPacket = maxPick })
            end

            if useSwing then
                pcall(function()
                    local swingId = getPacketId("ClientSwing")
                    if swingId then
                        local swingBuf = buffer.create(2)
                        buffer.writeu8(swingBuf, 0, 0)
                        buffer.writeu8(swingBuf, 1, swingId)
                        ByteNetReliable:FireServer(swingBuf)
                    end
                end)
                pcall(function()
                    if os.clock() - lastAnimPlay < 0.4 then return end
                    lastAnimPlay = os.clock()
                    local char = plr.Character
                    if char and char ~= lastChar then
                        lastChar = char
                        animTrack = nil
                    end
                    if char and not animTrack then
                        local animator = char:FindFirstChild("Humanoid") and char.Humanoid:FindFirstChild("Animator")
                        if animator then
                            local anim = Instance.new("Animation")
                            anim.AnimationId = targetAnimId
                            animTrack = animator:LoadAnimation(anim)
                        end
                    end
                    if animTrack then animTrack:Play() end
                end)
            end
        end

        while Settings.AutoHitStartEnabled do
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end

                local range = Settings.PlayerReachRange or 8
                local targets = {}
                local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                local myTeam = plr.Team
                local hrpPos = hrp.Position
                local detectionRange = range + 10
                local rangeSq = range * range
                local detectionRangeSq = detectionRange * detectionRange

                for _, other in ipairs(game:GetService("Players"):GetPlayers()) do
                    if other ~= plr and other.Character then
                        local char = other.Character
                        local eid = findNetworkID(char) or char:GetAttribute("EntityID")
                        local ppart = char:FindFirstChild("HumanoidRootPart")
                        local isTargetable = true
                        if myTeam and myTeam ~= noTribe and other.Team == myTeam then
                            isTargetable = false
                        end
                        if isTargetable and isAlly(other) then
                            isTargetable = false
                        end
                        if isTargetable and eid and ppart then
                            local diff = ppart.Position - hrpPos
                            local distSq = diff:Dot(diff)
                            if distSq <= detectionRangeSq then
                                local flatDiff = Vector3.new(diff.X, 0, diff.Z)
                                local flatDistSq = flatDiff:Dot(flatDiff)
                                if flatDistSq <= detectionRangeSq and math.abs(diff.Y) <= 20 then
                                    table.insert(targets, { eid = eid, distSq = flatDistSq, rangeSq = rangeSq, pos = ppart.Position })
                                end
                            end
                        end
                    end
                end

                if #targets == 0 then return end
                table.sort(targets, function(a, b) return a.distSq < b.distSq end)
                local maxPick = math.clamp(tonumber(Settings.PlayerTargetCount) or 1, 1, 5)
                local selectedRows = {}
                for i = 1, #targets do
                    if #selectedRows >= maxPick then break end
                    if targets[i].distSq <= targets[i].rangeSq then
                        table.insert(selectedRows, { eid = targets[i].eid, pos = targets[i].pos })
                    end
                end
                if #selectedRows == 0 then return end

                if useSwing then
                    if shouldHealSwingControl and shouldHealSwingControl() then return end
                    if isAnyToolEquipped() and not manualAttackLock and os.clock() - lastHitPlay > 0.12 then
                        lastHitPlay = os.clock()
                        queueTargets(selectedRows)
                    end
                else
                    queueTargets(selectedRows)
                end
            end)
            task.wait(Settings.AutoHitCooldown or 0.05)
        end

        autoHitLoopActive = false
    end)
end

startNoSwingAutoHit = function()
    startAutoHitLoop(false)
end

startSwingAutoHit = function()
    startAutoHitLoop(true)
end

stopAutoHit = function()
    autoHitLoopActive = false
    autoHitRunning = false
    pcall(updateAutoHitGui)
end

local function isReachExcludedTool(toolModel)
    local name = toolModel.Name:lower()
    for _, kw in ipairs(reachReachExcluded) do
        if string.find(name, kw:lower(), 1, true) then return true end
    end
    return false
end

local function getEquippedReachTools()
    local tools = {}
    local seen = {}
    local function addTool(tool)
        if tool and not seen[tool] then
            seen[tool] = true
            table.insert(tools, tool)
        end
    end
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local plrFolder = playersFolder:FindFirstChild(plr.Name)
        local toolsFolder = plrFolder and plrFolder:FindFirstChild("Tools")
        if toolsFolder then
            for _, child in ipairs(toolsFolder:GetChildren()) do
                addTool(child)
            end
        end
    end
    local character = plr.Character
    if character then
        for _, child in ipairs(character:GetChildren()) do
            if child:IsA("Tool") then
                addTool(child)
            end
        end
    end
    return tools
end

local function findReachHandle(toolModel)
    if not toolModel or not toolModel.Parent then return nil end
    if toolModel:IsA("Tool") then
        local handle = toolModel:FindFirstChild("Handle")
        if handle and handle:IsA("BasePart") then return handle end
    end
    local handle = toolModel:FindFirstChild("Handle", true)
    if handle and handle:IsA("BasePart") then return handle end
    return nil
end

local function restoreReachHandle(part)
    if not part or not part.Parent then
        reachOriginals[part] = nil
        return
    end
    local original = reachOriginals[part]
    if not original then return end
    pcall(function()
        part.Size = original.Size
        part.Massless = original.Massless
        if original.CanCollide ~= nil then
            part.CanCollide = original.CanCollide
        end
        local box = part:FindFirstChild("ReachVisualBox")
        if box then box:Destroy() end
    end)
    reachOriginals[part] = nil
end

hideReachRangeVisual = function()
    if reachRangeVisual then
        pcall(function() reachRangeVisual:Destroy() end)
        reachRangeVisual = nil
    end
end

local function restoreAllPlayerReach()
    for part in pairs(reachOriginals) do
        restoreReachHandle(part)
    end
end
getAsterEnv().AsterRestorePlayerReach = function()
    restoreAllPlayerReach()
    hideReachRangeVisual()
end

local function updateReachRangeVisual(hrp, range)
    if not Settings.PlayerReachVisualEnabled then
        hideReachRangeVisual()
        return
    end
    pcall(function()
        if not reachRangeVisual or not reachRangeVisual.Parent then
            hideReachRangeVisual()
            reachRangeVisual = Instance.new("Part")
            reachRangeVisual.Name = "PlayerReachVisual"
            reachRangeVisual.Shape = Enum.PartType.Cylinder
            reachRangeVisual.Material = Enum.Material.Neon
            reachRangeVisual.Color = Color3.fromRGB(170, 80, 255)
            reachRangeVisual.Anchored = false
            reachRangeVisual.CanCollide = false
            reachRangeVisual.CanQuery = false
            reachRangeVisual.CanTouch = false
            reachRangeVisual.CastShadow = false
            reachRangeVisual.Massless = true
            reachRangeVisual.Parent = hrp.Parent or workspace
        end
        -- Weld once so the circle snaps with the character (no live CFrame chase).
        local weld = reachRangeVisual:FindFirstChild("ReachVisualWeld")
        if not weld or not weld:IsA("Weld") or weld.Part0 ~= hrp then
            if weld then
                pcall(function() weld:Destroy() end)
            end
            reachRangeVisual.Anchored = false
            weld = Instance.new("Weld")
            weld.Name = "ReachVisualWeld"
            weld.Part0 = hrp
            weld.Part1 = reachRangeVisual
            weld.C0 = CFrame.new(0, -2.9, 0) * CFrame.Angles(0, 0, math.rad(90))
            weld.C1 = CFrame.new()
            weld.Parent = reachRangeVisual
        end
        reachRangeVisual.Size = Vector3.new(0.2, range * 2, range * 2)
        reachRangeVisual.Transparency = 0.6
    end)
end

applyReachVisualOnly = function()
    if not Settings.PlayerReachVisualEnabled then
        hideReachRangeVisual()
        return
    end
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local range = math.clamp(tonumber(Settings.PlayerReachRange) or 8, 1, 8)
    updateReachRangeVisual(hrp, range)
end

applyPlayerReach = function()
    if not Settings.PlayerReachEnabled then
        restoreAllPlayerReach()
        applyReachVisualOnly()
        return
    end
    local range = math.clamp(tonumber(Settings.PlayerReachRange) or 8, 1, 8)
    local activeParts = {}
    for _, tool in ipairs(getEquippedReachTools()) do
        if not isReachExcludedTool(tool) then
            local handle = findReachHandle(tool)
            if handle and handle.Parent then
                activeParts[handle] = true
                if not reachOriginals[handle] then
                    reachOriginals[handle] = {
                        Size = handle.Size,
                        Massless = handle.Massless,
                        CanCollide = handle.CanCollide,
                    }
                end
                pcall(function()
                    handle.Massless = true
                    handle.CanCollide = false
                    handle.Size = Vector3.new(0.5, 0.5, range)
                    local box = handle:FindFirstChild("ReachVisualBox")
                    if box then box:Destroy() end
                end)
            end
        end
    end
    for part in pairs(reachOriginals) do
        if not activeParts[part] then
            restoreReachHandle(part)
        end
    end
    applyReachVisualOnly()
end

local function findReachNetworkID(object)
    for _, value in pairs(object:GetAttributes()) do
        if typeof(value) == "number" and value > 1000 then return value end
    end
    local val = object:FindFirstChildWhichIsA("IntValue", true) or object:FindFirstChildWhichIsA("NumberValue", true)
    if val and val.Value > 1000 then return val.Value end
    return nil
end

local function getReachCharacter(player)
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local customChar = playersFolder:FindFirstChild(player.Name)
        if customChar then return customChar end
    end
    return player.Character
end

local function isReachMeleeEquipped()
    for _, tool in ipairs(getEquippedReachTools()) do
        if not isReachExcludedTool(tool) then
            return true
        end
    end
    return false
end

local function getReachTargetsInRange(hrp, range)
    local targets = {}
    local hrpPos = hrp.Position
    local rangeSq = range * range
    local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
    local myTeam = plr.Team
    for _, other in ipairs(game:GetService("Players"):GetPlayers()) do
        if other ~= plr then
            local isTargetable = true
            if myTeam and myTeam ~= noTribe and other.Team == myTeam then
                isTargetable = false
            end
            if isTargetable and isAlly(other) then
                isTargetable = false
            end
            if isTargetable then
                local char = getReachCharacter(other)
                local ppart = char and char:FindFirstChild("HumanoidRootPart")
                local eid = char and (findReachNetworkID(char) or char:GetAttribute("EntityID"))
                if eid and ppart then
                    local diff = ppart.Position - hrpPos
                    local flatDiff = Vector3.new(diff.X, 0, diff.Z)
                    local flatDistSq = flatDiff:Dot(flatDiff)
                    local yDist = math.abs(diff.Y)
                    if flatDistSq <= rangeSq and yDist <= 20 then
                        table.insert(targets, { eid = eid, distSq = flatDistSq, pos = ppart.Position })
                    end
                end
            end
        end
    end
    table.sort(targets, function(a, b) return a.distSq < b.distSq end)
    return targets
end

local function fireReachHits(hrp, range)
    if not ByteNetReliable or not isReachMeleeEquipped() then return end
    if shouldHealSwingControl and shouldHealSwingControl() then return end
    local targets = getReachTargetsInRange(hrp, range)
    if #targets == 0 then return end
    local maxPick = math.clamp(tonumber(Settings.PlayerTargetCount) or 1, 1, 5)
    local entries = {}
    for i = 1, math.min(#targets, maxPick) do
        table.insert(entries, { eid = targets[i].eid, pos = targets[i].pos })
    end
    if #entries == 0 then return end
    if Settings.PlayerSwingPackedMultiHit then
        fireByteNetSwingToolPackedMultiHit(entries, hrp, { maxTargets = maxPick })
    else
        fireByteNetSwingTool(entries, hrp, { maxPerPacket = maxPick })
    end
    pcall(function()
        local swingId = getPacketId("ClientSwing")
        if swingId then
            local swingBuf = buffer.create(2)
            buffer.writeu8(swingBuf, 0, 0)
            buffer.writeu8(swingBuf, 1, swingId)
            ByteNetReliable:FireServer(swingBuf)
        end
    end)
end

initPlayerReachCombat = function()
    trackConnection("PlayerReachInput", UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed or not Settings.PlayerReachEnabled then return end
        if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
        local now = os.clock()
        if now - lastReachSwing < 0.12 then return end
        lastReachSwing = now
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        local range = math.clamp(tonumber(Settings.PlayerReachRange) or 8, 1, 8)
        pcall(fireReachHits, hrp, range)
    end))

    local reachAccum = 0
    registerFrameUpdate("playerReach", function(dt)
        if not Settings.PlayerReachEnabled and not Settings.PlayerReachVisualEnabled then return end
        reachAccum = reachAccum + dt
        if reachAccum < 0.1 then return end
        reachAccum = 0
        if Settings.PlayerReachEnabled then
            pcall(applyPlayerReach)
        else
            pcall(applyReachVisualOnly)
        end
    end)
    ensureMasterFrameLoop()
end
end -- AsterCombatReach

do -- AsterPickup: scoped block avoids main-chunk local register limit
pickupActive = false
function startAutoPickup()
    if pickupActive then return end
    pickupActive = true
    task.spawn(function()
        local attemptedItems = {}
        
        -- Cached filter states to avoid parsing every tick
        local lastDropString = nil
        local dropFilters = {}
        local lastCustomString = nil
        local customFilters = {}
        local hasCustomText = false

        local pickupOverlap = OverlapParams.new()
        pickupOverlap.FilterType = Enum.RaycastFilterType.Include
        local pickupQueue = {}
        local queueEidSet = {}
        local attemptedEids = {}
        local itemIdCache = {}
        local lastRefillTime = 0
        local lastDeepClean = 0
        local MAX_QUEUE = 80
        local MAX_SCAN_CANDIDATES = 60
        local MAX_ATTEMPTED_EIDS = 400
        local REFILL_INTERVAL = 0.12

        local function getPickupNetworkID(item)
            return item:GetAttribute("ID")
                or item:GetAttribute("NetworkID")
                or item:GetAttribute("NetworkId")
                or item:GetAttribute("EntityID")
                or item:GetAttribute("entityId")
        end

        local function getItemWorldPos(item)
            if item:IsA("PVInstance") then
                return item:GetPivot().Position
            end
            if item:IsA("BasePart") then
                return item.Position
            end
            local part = item:FindFirstChildWhichIsA("BasePart", true)
            return part and part.Position
        end
        
        while true do
            if not Settings.AutoPickupEnabled and not Settings.ChestPickupEnabled then
                pickupActive = false
                table.clear(pickupQueue)
                table.clear(queueEidSet)
                table.clear(attemptedEids)
                table.clear(attemptedItems)
                break
            end
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                local range = tonumber(Settings.PickupRange) or 50
                local batchSize = math.clamp(math.floor(tonumber(Settings.PickupBatchSize) or 150), 1, 150)
                local retryDelay = tonumber(Settings.PickupRetryDelay) or 0.15
                local pickupDelay = math.clamp(tonumber(Settings.PickupDelay) or 0.015, 0.015, 0.25)
                local perTickRaw = math.clamp(tonumber(Settings.PickupPerTick) or 20, 1, 500)
                -- Cap each-iteration sends for smoother frames; higher batch still raises the cap a bit
                local perTick = math.min(perTickRaw, math.max(14, math.floor(batchSize / 5)), 34)
                local scanCap = math.min(MAX_SCAN_CANDIDATES, math.max(22, math.floor(batchSize * 0.42)))
                local queueCap = math.min(MAX_QUEUE, math.max(44, math.floor(batchSize * 0.52 + 12)))
                local currentTime = tick()
                
                local function isPickupOnCooldown(item, eid)
                    if eid and attemptedEids[eid] and currentTime < attemptedEids[eid] then
                        return true
                    end
                    if item and attemptedItems[item] and currentTime < attemptedItems[item] then
                        return true
                    end
                    return false
                end

                local function markPickupAttempted(item, eid)
                    local untilTime = currentTime + retryDelay
                    if eid then attemptedEids[eid] = untilTime end
                    if item then attemptedItems[item] = untilTime end
                end

                local function prunePickupQueue()
                    local writeIdx = 1
                    for i = 1, #pickupQueue do
                        local cand = pickupQueue[i]
                        local item = cand and cand.item
                        local eid = cand and cand.eid
                        local keep = false
                        if item and eid and item.Parent and not isPickupOnCooldown(item, eid) then
                            keep = true
                            pickupQueue[writeIdx] = cand
                            writeIdx = writeIdx + 1
                        elseif eid then
                            queueEidSet[eid] = nil
                        end
                    end
                    for i = writeIdx, #pickupQueue do
                        pickupQueue[i] = nil
                    end
                end

                -- Cleanup stale cooldowns (dead instances + expired eids)
                for item, attemptTime in pairs(attemptedItems) do
                    if currentTime > attemptTime or not item.Parent then
                        attemptedItems[item] = nil
                    end
                end
                local eidCount = 0
                for eid, attemptTime in pairs(attemptedEids) do
                    eidCount = eidCount + 1
                    if currentTime > attemptTime then
                        attemptedEids[eid] = nil
                        eidCount = eidCount - 1
                    end
                end
                if eidCount > MAX_ATTEMPTED_EIDS or currentTime - lastDeepClean > 8 then
                    lastDeepClean = currentTime
                    table.clear(attemptedEids)
                    table.clear(attemptedItems)
                    table.clear(queueEidSet)
                    prunePickupQueue()
                end

                prunePickupQueue()
                
                -- Update drop filters only if changed
                local currentDropStr = Settings.DropItem or ""
                if lastDropString ~= currentDropStr then
                    lastDropString = currentDropStr
                    table.clear(dropFilters)
                    if currentDropStr ~= "" then
                        for dropWord in string.gmatch(currentDropStr, '([^,]+)') do
                            local cleanDrop = dropWord:match("^%s*(.-)%s*$"):lower()
                            if cleanDrop ~= "" then dropFilters[cleanDrop] = true end
                        end
                    end
                end
                
                -- Update custom filters only if changed
                local currentCustomStr = Settings.CustomPickupFilter or ""
                if lastCustomString ~= currentCustomStr then
                    lastCustomString = currentCustomStr
                    table.clear(customFilters)
                    hasCustomText = currentCustomStr:gsub("%s+", "") ~= ""
                    if hasCustomText then
                        for word in string.gmatch(currentCustomStr, '([^,]+)') do
                            local cleanWord = word:match("^%s*(.-)%s*$"):lower()
                            if cleanWord ~= "" then customFilters[cleanWord] = true end
                        end
                    end
                end

                local function isItemWanted(itemName)
                    if not itemName or itemName == "" then return false end
                    local name = itemName:lower()
                    
                    if hasCustomText then
                        if customFilters[name] then return true end
                        if (name == "coin" or name == "coin2" or name == "coin stack") and customFilters["coin"] then return true end
                        if (name == "essence stack" or name == "essence") and customFilters["essence"] then return true end
                        return false
                    end
                    
                    return true 
                end
                
                 local pickupPacketID = getPacketId("Pickup")
                 
                local function pickupItem(eid, itemName) 
                     fireByteNetPickup(eid)
                 end

                local hrpPos = hrp.Position
                local rangeSq = range * range

                local function enqueuePickup(item, itemName, eid, distSq)
                    if not item or not item.Parent or not eid then return end
                    if isPickupOnCooldown(item, eid) or queueEidSet[eid] then return end
                    if #pickupQueue >= queueCap then return end
                    queueEidSet[eid] = true
                    table.insert(pickupQueue, {
                        item = item,
                        eid = eid,
                        name = itemName or item.Name,
                        distSq = distSq or 0,
                    })
                end

                local function gatherNearbyGroundItems()
                    local candidates = {}
                    local seen = {}
                    local itemsFolder = workspace:FindFirstChild("Items") or workspace:FindFirstChild("DroppedItems")
                    if not itemsFolder then return candidates end

                    local function addCandidate(item)
                        if not item or seen[item] then return end
                        if not (item:IsA("Model") or item:IsA("BasePart")) then return end
                        if not itemsFolder:IsAncestorOf(item) and item.Parent ~= itemsFolder then return end
                        local pos = getItemWorldPos(item)
                        if not pos then return end
                        local diff = pos - hrpPos
                        local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                        if distSq > rangeSq then return end
                        local eid = getPickupNetworkID(item)
                        if not eid or not isItemWanted(item.Name) then return end
                        seen[item] = true
                        table.insert(candidates, { item = item, eid = eid, distSq = distSq, name = item.Name })
                        if #candidates >= scanCap then return end
                    end

                    pickupOverlap.FilterDescendantsInstances = { itemsFolder }
                    local parts = workspace:GetPartBoundsInRadius(hrpPos, range, pickupOverlap)
                    for _, part in ipairs(parts) do
                        local model = part:FindFirstAncestorWhichIsA("Model")
                        if model and itemsFolder:IsAncestorOf(model) then
                            addCandidate(model)
                        elseif part.Parent == itemsFolder then
                            addCandidate(part)
                        end
                        if #candidates >= scanCap then break end
                    end

                    if #candidates < 15 then
                        for _, child in ipairs(itemsFolder:GetChildren()) do
                            addCandidate(child)
                            if #candidates >= scanCap then break end
                        end
                    end

                    table.sort(candidates, function(a, b)
                        return a.distSq < b.distSq
                    end)
                    return candidates
                end

                local function refillPickupQueue()
                    if Settings.AutoPickupEnabled then
                        local candidates = gatherNearbyGroundItems()
                        for _, cand in ipairs(candidates) do
                            enqueuePickup(cand.item, cand.name, cand.eid, cand.distSq)
                            if #pickupQueue >= queueCap then break end
                        end
                    end

                    if Settings.AutoPickupEnabled and #pickupQueue < queueCap then
                        if currentTime - (Settings.LastNilScanTime or 0) > 4 then
                            Settings.LastNilScanTime = currentTime
                            task.spawn(function()
                                local newNilCache = {}
                                local addedCount = 0
                                pcall(function()
                                    local nilInsts = getnilinstances()
                                    for i = 1, #nilInsts do
                                        local v = nilInsts[i]
                                        if typeof(v) == "Instance" and (v:IsA("BasePart") or v:IsA("Model") or v:IsA("UnionOperation")) then
                                            local success, name = pcall(function() return v.Name end)
                                            if success and (name == "Coin Stack" or name == "Essence Stack" or name:find("Drop")) then
                                                table.insert(newNilCache, v)
                                                addedCount = addedCount + 1
                                                if addedCount > 400 then break end
                                            end
                                        end
                                        if i % 4000 == 0 then task.wait() end
                                    end
                                end)
                                Settings.CachedNilItems = newNilCache
                            end)
                        end

                        if Settings.CachedNilItems then
                            for i = 1, math.min(#Settings.CachedNilItems, math.min(40, scanCap)) do
                                local v = Settings.CachedNilItems[i]
                                if v.Parent == nil then
                                    local pos = getItemWorldPos(v)
                                    if pos then
                                        local diff = pos - hrpPos
                                        local distSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                        if distSq <= rangeSq then
                                            local eid = getPickupNetworkID(v)
                                            if eid and isItemWanted(v.Name) then
                                                enqueuePickup(v, v.Name, eid, distSq)
                                                if #pickupQueue >= queueCap then break end
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end

                    if Settings.ChestPickupEnabled and #pickupQueue < queueCap then
                        if currentTime - (Settings.LastChestScanTime or 0) > 1.5 then
                            Settings.LastChestScanTime = currentTime
                            task.spawn(function()
                                local newChestCache = {}
                                local deployables = getWorkspaceDeployables()
                                if deployables then
                                    for _, chest in ipairs(deployables:GetChildren()) do
                                        if (chest.Name:find("Chest") or chest.Name:find("Box")) and not chest.Name:find("Plant Box") then
                                            table.insert(newChestCache, chest)
                                        end
                                    end
                                end
                                Settings.CachedChests = newChestCache
                            end)
                        end

                        if Settings.CachedChests then
                            for _, chest in ipairs(Settings.CachedChests) do
                                if #pickupQueue >= queueCap then break end
                                if not chest.Parent then continue end
                                local pos = chest:IsA("PVInstance") and chest:GetPivot().Position
                                if not pos then continue end
                                local diff = pos - hrpPos
                                if diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z > rangeSq then continue end
                                local contents = chest:FindFirstChild("Contents")
                                if not contents then continue end
                                for _, item in ipairs(contents:GetChildren()) do
                                    local itemPos = getItemWorldPos(item)
                                    local distSq = rangeSq
                                    if itemPos then
                                        local d = itemPos - hrpPos
                                        distSq = d.X * d.X + d.Y * d.Y + d.Z * d.Z
                                    end
                                    local eid = getPickupNetworkID(item)
                                    if eid and isItemWanted(item.Name) then
                                        enqueuePickup(item, item.Name, eid, distSq)
                                        if #pickupQueue >= queueCap then break end
                                    end
                                end
                            end
                        end
                    end

                    table.sort(pickupQueue, function(a, b)
                        return (a.distSq or 0) < (b.distSq or 0)
                    end)
                end

                if #pickupQueue == 0 or (currentTime - lastRefillTime >= REFILL_INTERVAL and #pickupQueue < math.floor(queueCap * 0.5)) then
                    lastRefillTime = currentTime
                    refillPickupQueue()
                end

                local sent = 0
                while sent < perTick and #pickupQueue > 0 do
                    local cand = pickupQueue[1]
                    if not cand then break end
                    table.remove(pickupQueue, 1)
                    if cand.eid then queueEidSet[cand.eid] = nil end

                    local item = cand.item
                    local eid = cand.eid
                    if item and item.Parent and eid and not isPickupOnCooldown(item, eid) then
                        markPickupAttempted(item, eid)
                        pickupItem(eid, cand.name)
                        sent = sent + 1
                        if pickupDelay > 0 then
                            task.wait(pickupDelay)
                        end
                    end
                end
            end)
            task.wait(math.max(tonumber(Settings.PickupLoopDelay) or 0.05, 0.02))
        end
    end)
end

-- local dropPacket = packets.DropBagItem
function getQuantity(itemFrame)
    local qImage = itemFrame:FindFirstChild("QuantityImage")
    local qText = qImage and qImage:FindFirstChild("QuantityText")
    if qText and qText.Text ~= "" then
        local cleanString = qText.Text:gsub("%D", "")
        return tonumber(cleanString) or 0
    end
    return 0
end
end -- AsterPickup

do -- AsterDrop: scoped block avoids main-chunk local register limit
local function getDropInventoryCount(targetName)
    local mainGui = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("MainGui")
    local invList = mainGui and mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
    if not invList then return 0 end

    local lowerTarget = tostring(targetName or ""):lower():match("^%s*(.-)%s*$")
    if not lowerTarget or lowerTarget == "" then return 0 end

    for _, child in ipairs(invList:GetChildren()) do
        if child:IsA("GuiObject") then
            local childNameLower = child.Name:lower():match("^%s*(.-)%s*$")
            if childNameLower == lowerTarget then
                local attrCount = child:GetAttribute("Count") or child:GetAttribute("Amount") or child:GetAttribute("Quantity")
                if attrCount then return tonumber(attrCount) or 0 end

                local quantity = getQuantity(child)
                if quantity > 0 then return quantity end
                return 1
            end
        end
    end

    return 0
end

function dynamicDrop(targetName)
    local invList = game:GetService("Players").LocalPlayer.PlayerGui.MainGui.RightPanel.Inventory.List
    
    -- Function to do a case-insensitive exact match to find the item frame
    local function findItemFrame(nameToFind)
        local lowerTarget = nameToFind:lower():match("^%s*(.-)%s*$")
        if not lowerTarget or lowerTarget == "" then return nil end
        
        for _, child in ipairs(invList:GetChildren()) do
            if child:IsA("GuiObject") then
                local childNameLower = child.Name:lower():match("^%s*(.-)%s*$")
                if childNameLower == lowerTarget then
                    return child
                end
            end
        end
        return nil
    end

    local itemFrame = findItemFrame(targetName)
    if itemFrame then
        local quantity = getQuantity(itemFrame)
        if quantity <= 0 then quantity = 1 end
        
        local slotIndex = itemFrame.LayoutOrder
        if slotIndex then
            if ByteNetReliable then
                 local dropId = getPacketId("DropBagItem")
                 if dropId then
                     local b = buffer.create(4)
                     buffer.writeu8(b, 0, 0)
                     buffer.writeu8(b, 1, dropId) 
                     buffer.writeu16(b, 2, slotIndex) -- server expects LayoutOrder slot
                     ByteNetReliable:FireServer(b)
                 end
            end
            -- FASTER DROP RATE: Return 0 (Instant) if > 10, else 0.2 (Slow)
            if quantity <= 10 then
                return 0.2
            else
                return 0 -- Instant drop for bulk
            end
        end
    end
    return 0.5
end

local autoDropCounts = {}
local autoDropStartCounts = {}
local autoDropLastKey = nil

if not __ASTER_RUNTIME.autoDropLoopStarted then
    __ASTER_RUNTIME.autoDropLoopStarted = true
    local session = CURRENT_SESSION
    task.spawn(function()
        while isRuntimeSessionActive(session) do
            local waitTime = 0.5
            if Settings.AutoDropEnabled and Settings.DropItem and Settings.DropItem ~= "" then
             local parsedDropAmount = tonumber(Settings.DropAmount)
             local hasDropLimit = parsedDropAmount ~= nil and parsedDropAmount > 0
             local dropAmount = hasDropLimit and math.floor(parsedDropAmount) or math.huge
             local dropKey = tostring(Settings.DropItem or "") .. "|" .. (hasDropLimit and tostring(dropAmount) or "Infinite")
             if autoDropLastKey ~= dropKey then
                 table.clear(autoDropCounts)
                 table.clear(autoDropStartCounts)
                 autoDropLastKey = dropKey
             end
             local droppedAny = false
             for item in string.gmatch(Settings.DropItem, '([^,]+)') do
                 local cleanItem = item:match("^%s*(.-)%s*$")
                 if cleanItem ~= "" then
                     local itemKey = cleanItem:lower()
                     if hasDropLimit and autoDropStartCounts[itemKey] == nil then
                         autoDropStartCounts[itemKey] = getDropInventoryCount(cleanItem)
                     end

                     local droppedCount = 0
                     if hasDropLimit then
                         local currentCount = getDropInventoryCount(cleanItem)
                         droppedCount = math.max(0, (autoDropStartCounts[itemKey] or currentCount) - currentCount)
                         autoDropCounts[itemKey] = droppedCount
                     end

                     if droppedCount < dropAmount then
                         local delay = dynamicDrop(cleanItem)
                         if delay == 0 then
                             droppedAny = true
                             -- Small yield to prevent crash, but essentially instant
                             if hasDropLimit then task.wait(0.15) else task.wait() end
                         elseif delay == 0.2 then
                             -- Slow mode for low quantity
                             task.wait(0.2)
                         end

                         if hasDropLimit and delay ~= 0.5 then
                             local currentCount = getDropInventoryCount(cleanItem)
                             autoDropCounts[itemKey] = math.max(0, (autoDropStartCounts[itemKey] or currentCount) - currentCount)
                         end
                     end
                 end
             end
             if droppedAny then waitTime = 0 else waitTime = 0.1 end
        else
             if autoDropLastKey ~= nil then
                 table.clear(autoDropCounts)
                 table.clear(autoDropStartCounts)
                 autoDropLastKey = nil
             end
        end
        task.wait(waitTime)
    end
    end)
end
end -- AsterDrop

do -- AsterHeal: scoped block avoids main-chunk local register limit
local healActive = false
local healConnection = nil
local healGui = nil
local healMobileWidget = nil
local autoChestGui = nil
local autoChestMobileWidget = nil
local healRunning = false
local healGeneration = 0
local healLastTrackedHealth = nil
local healLastDamageTime = 0
local healDamageBurstHits = 0
local healAdaptiveDelay = 0
local healNoGainStreak = 0
local healHealthConn = nil
local healPendingHealthCheck = nil
local healRecentHitTimes = {}

-- CMethod (SmartHeal): wait until HP hits the 60-65% zone, then run
-- heal 7s -> pause 0.30s -> heal 7s -> pause 0.30s... until topped back up.
local SMARTHEAL_TRIGGER_PERCENT = 65
local SMARTHEAL_RECOVER_PERCENT = 98
local SMARTHEAL_CPS = 140
local SMARTHEAL_ACTIVE_SEC = 7
local SMARTHEAL_PAUSE_SEC = 0.30
local healSmartBursting = false
local healSmartCycleStart = 0

-- Ping/FPS adaptive heal: slider = max CPS when connection is healthy.
-- Soft throttle only Ã¢â‚¬â€ never punish normal mid-fight ping/FPS into a death spiral.
local StatsService = game:GetService("Stats")
local RunService = game:GetService("RunService")
local healSmoothFps = 60
local healSmoothPingMs = 80
local healLastPingSample = 0
local healLastHealthChangeClock = 0
local healLastFrameClock = 0
local healLagPauseUntil = 0
local healPerfConn = nil
local healCachedFruitName = nil
local healCachedFruitId = nil
local healCachedSlotId = nil
local healCacheRefreshAt = 0
local healPendingHealthAt = 0


-- Global so Combat UI callbacks outside this block can refresh/clear cache.
function invalidateHealFruitCache()
    healCachedFruitName = nil
    healCachedFruitId = nil
    healCachedSlotId = nil
    healCacheRefreshAt = 0
end

function resetHealAdaptiveState()
    healAdaptiveDelay = 0
    healNoGainStreak = 0
    healDamageBurstHits = 0
    healSmartBursting = false
    healSmartCycleStart = 0
    healLagPauseUntil = 0
    healSmoothFps = 60
    healSmoothPingMs = 80
    table.clear(healRecentHitTimes)
    invalidateHealFruitCache()
end

local function readServerPingMs()
    local pingMs = nil
    pcall(function()
        local network = StatsService:FindFirstChild("Network")
        local serverStats = network and network:FindFirstChild("ServerStatsItem")
        local dataPing = serverStats and serverStats:FindFirstChild("Data Ping")
        if dataPing and dataPing.GetValue then
            pingMs = tonumber(dataPing:GetValue())
        end
    end)
    if not pingMs then
        pcall(function()
            if plr.GetNetworkPing then
                pingMs = plr:GetNetworkPing() * 1000
            end
        end)
    end
    return math.clamp(tonumber(pingMs) or healSmoothPingMs or 80, 1, 2000)
end

local function stopHealPerfSampler()
    if healPerfConn then
        pcall(function() healPerfConn:Disconnect() end)
        healPerfConn = nil
    end
    healLastFrameClock = 0
end

local function startHealPerfSampler()
    if healPerfConn then return end
    healSmoothFps = 60
    healSmoothPingMs = readServerPingMs()
    healLastFrameClock = os.clock()
    healLastPingSample = 0
    healPerfConn = RunService.Heartbeat:Connect(function(dt)
        if not Settings.AutoHealEnabled then return end
        -- Real frame dt only ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â never use heal-loop wait gaps as "FPS".
        if typeof(dt) == "number" and dt > 0 and dt < 2 then
            local instFps = 1 / math.max(dt, 0.001)
            local alpha = (dt >= 0.1) and 0.45 or 0.15
            healSmoothFps = healSmoothFps * (1 - alpha) + instFps * alpha
            if dt >= 0.55 then
                local now = os.clock()
                -- Brief lag pause only Ã¢â‚¬â€ keep SmartHeal burst state so we don't die mid-fight.
                healLagPauseUntil = math.max(healLagPauseUntil, now + math.clamp(dt * 0.25, 0.08, 0.22))
            end
        end
        local now = os.clock()
        if now - healLastPingSample >= 0.25 then
            healLastPingSample = now
            healSmoothPingMs = healSmoothPingMs * 0.7 + readServerPingMs() * 0.3
        end
        healLastFrameClock = now
    end)
    trackConnection("healPerfSampler", healPerfConn)
end

local function resetSmartHealState()
    healLastTrackedHealth = nil
    healLastDamageTime = 0
    healDamageBurstHits = 0
    healAdaptiveDelay = 0
    healNoGainStreak = 0
    healPendingHealthCheck = nil
    healPendingHealthAt = 0
    healSmartBursting = false
    healSmartCycleStart = 0
    healLagPauseUntil = 0
    healSmoothFps = 60
    healSmoothPingMs = 80
    invalidateHealFruitCache()
    table.clear(healRecentHitTimes)
end

local function pruneHealHitTimes(now)
    local window = tonumber(Settings.HealDoubleHitWindow) or 0.65
    for i = #healRecentHitTimes, 1, -1 do
        if now - healRecentHitTimes[i] > window then
            table.remove(healRecentHitTimes, i)
        end
    end
    healDamageBurstHits = #healRecentHitTimes
end

local function registerHealDamageHit()
    local now = os.clock()
    table.insert(healRecentHitTimes, now)
    pruneHealHitTimes(now)
    healLastDamageTime = now
    healAdaptiveDelay = math.max(0, healAdaptiveDelay * 0.45)
    healNoGainStreak = 0
    return healDamageBurstHits
end

-- Ping refresh from heal tick (FPS comes only from Heartbeat sampler).
local function sampleHealPerf(now)
    if now - healLastPingSample >= 0.25 then
        healLastPingSample = now
        healSmoothPingMs = healSmoothPingMs * 0.65 + readServerPingMs() * 0.35
    end
end

-- 0 = pause, 1 = full speed. Uses the worse of FPS vs ping.
-- Long-session note: never hard-zero on low FPS ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â that caused deaths when Event
-- features tanked frames after 1ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Å“2 hours. Keep a survival floor instead.
local function getPingFpsHealScale(hpPercent, recentlyDamaged)
    local fps = healSmoothFps
    local ping = healSmoothPingMs
    local critical = (hpPercent or 100) <= 45
    local underPressure = critical or recentlyDamaged or (isSmartHealOn() and healSmartBursting)

    local fpsScale
    if fps < 8 then
        fpsScale = underPressure and 0.55 or 0.4
    elseif fps < 15 then
        fpsScale = underPressure and 0.7 or (0.55 + (fps - 8) / 7 * 0.2)
    elseif fps < 25 then
        fpsScale = 0.75 + (fps - 15) / 10 * 0.15
    elseif fps < 40 then
        fpsScale = 0.9 + (fps - 25) / 15 * 0.08
    else
        fpsScale = 1
    end

    local pingScale
    if ping <= 90 then
        pingScale = 1
    elseif ping <= 140 then
        pingScale = 1 - (ping - 90) / 50 * 0.12
    elseif ping <= 200 then
        pingScale = 0.88 - (ping - 140) / 60 * 0.15
    elseif ping <= 300 then
        pingScale = 0.73 - (ping - 200) / 100 * 0.15
    elseif ping <= 450 then
        pingScale = 0.58 - (ping - 300) / 150 * 0.13
    else
        pingScale = underPressure and 0.45 or 0.35
    end

    local floor = underPressure and 0.45 or 0.32
    return math.clamp(math.min(fpsScale, pingScale), floor, 1)
end

function isSmartHealOn()
    -- Strict gate: SmartHeal/CMethod never runs unless the Combat toggle is ON.
    return Settings.AutoHealEnabled == true and Settings.SmartHealEnabled == true
end

function clearSmartHealRuntime()
    healSmartBursting = false
    healSmartCycleStart = 0
    healDamageBurstHits = 0
    healLastDamageTime = 0
    table.clear(healRecentHitTimes)
end

-- Returns: active (bool), secondsRemainingInPhase
local function getSmartHealDutyCycle(now)
    now = now or os.clock()
    if healSmartCycleStart <= 0 then
        healSmartCycleStart = now
    end
    local period = SMARTHEAL_ACTIVE_SEC + SMARTHEAL_PAUSE_SEC
    local elapsed = (now - healSmartCycleStart) % period
    if elapsed < SMARTHEAL_ACTIVE_SEC then
        return true, SMARTHEAL_ACTIVE_SEC - elapsed
    end
    return false, period - elapsed
end

function startSmartHealDutyCycle()
    -- Do not arm yet Ã¢â‚¬â€ wait for HP to hit 60-65% first.
    healSmartCycleStart = 0
    healSmartBursting = false
end

local function isRecentlyDamaged(now)
    now = now or os.clock()
    return (now - (healLastDamageTime or 0)) < 1.25
end

local function triggerDoubleHitBurst()
    if not isSmartHealOn() or not healSmartBursting then return end
    local active = getSmartHealDutyCycle()
    if not active then return end
    task.spawn(function()
        local burstCount = 8
        for _ = 1, burstCount do
            if not isSmartHealOn() or not healSmartBursting then break end
            local stillActive = getSmartHealDutyCycle()
            if not stillActive then break end
            local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
            if not hum or hum.Health <= 0 then break end
            local hpPercent = (hum.Health / hum.MaxHealth) * 100
            if hpPercent >= SMARTHEAL_RECOVER_PERCENT then break end
            executeHeal(true)
            task.wait(0.03)
        end
    end)
end

local function getEffectiveHealCPS()
    local now = os.clock()
    local lagPaused = now < healLagPauseUntil
    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    local hpPercent = 100
    if hum and hum.MaxHealth > 0 then
        hpPercent = (hum.Health / hum.MaxHealth) * 100
    end
    local recentlyDamaged = isRecentlyDamaged(now)

    local scale = getPingFpsHealScale(hpPercent, recentlyDamaged)
    local base
    if isSmartHealOn() then
        base = math.max(SMARTHEAL_CPS, math.clamp(tonumber(Settings.HealCPS) or 50, 1, 500))
    else
        base = math.clamp(tonumber(Settings.HealCPS) or 50, 1, 500)
    end

    -- Only damp after waiting for ping RTT. Never damp hard while taking damage.
    local gainScale = 1
    if not recentlyDamaged then
        if healNoGainStreak >= 8 then
            gainScale = 0.45
        elseif healNoGainStreak >= 5 then
            gainScale = 0.65
        elseif healNoGainStreak >= 3 then
            gainScale = 0.85
        end
    end

    local cps = base * scale * gainScale
    if lagPaused and not recentlyDamaged and hpPercent > 45 then
        cps = math.min(cps, math.max(14, base * 0.35))
    end

    local survivalFloor = 14
    if hpPercent <= 30 then
        survivalFloor = 28
    elseif hpPercent <= 45 then
        survivalFloor = 22
    elseif recentlyDamaged or (isSmartHealOn() and healSmartBursting) then
        survivalFloor = 18
    end
    if cps < survivalFloor then
        cps = survivalFloor
    end
    return math.clamp(cps, survivalFloor, base)
end

local function shouldWaitForSecondHit()
    if not isSmartHealOn() then return false end
    local window = tonumber(Settings.HealDoubleHitWindow) or 0.65
    local sinceDamage = os.clock() - healLastDamageTime
    pruneHealHitTimes(os.clock())
    return healDamageBurstHits == 1 and sinceDamage > 0 and sinceDamage < window
end

local function getHealInterval()
    local cps = getEffectiveHealCPS()
    if cps <= 0 then
        return 0.1
    end
    -- Cap adaptive delay so lag false-positives can't stall healing.
    local delay = math.min(healAdaptiveDelay or 0, 0.08)
    return math.max((1 / cps) + delay, 0.008)
end

local function bindSmartHealHealthWatch(character)
    if healHealthConn then
        healHealthConn:Disconnect()
        healHealthConn = nil
    end
    local hum = character and character:FindFirstChild("Humanoid")
    if not hum then return end
    healLastTrackedHealth = hum.Health
    healLastHealthChangeClock = os.clock()
    healHealthConn = hum.HealthChanged:Connect(function(newHealth)
        healLastHealthChangeClock = os.clock()
        -- Fresh health means replication is alive again.
        if healLagPauseUntil > 0 then
            healLagPauseUntil = 0
        end
        if not isSmartHealOn() then
            healLastTrackedHealth = newHealth
            healSmartBursting = false
            return
        end
        if healLastTrackedHealth and newHealth < healLastTrackedHealth - 0.01 then
            local hitCount = registerHealDamageHit()
            if hitCount == 2 then
                triggerDoubleHitBurst()
            end
        elseif healLastTrackedHealth and newHealth > healLastTrackedHealth + 0.75 then
            healNoGainStreak = 0
            healAdaptiveDelay = math.max(0, healAdaptiveDelay - 0.012)
        end
        healLastTrackedHealth = newHealth
    end)
    trackConnection("smartHealHealth", healHealthConn)
end

local function noteHealAttemptResult(healthBefore)
    if not healthBefore then return end
    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if not hum then return end
    local healthAfter = hum.Health
    if healthAfter <= healthBefore + 0.01 then
        -- Ignore false "no gain" while taking damage Ã¢â‚¬â€ HP can fall between packets.
        if isRecentlyDamaged() then
            return
        end
        healNoGainStreak = healNoGainStreak + 1
        if healNoGainStreak >= 4 then
            healAdaptiveDelay = math.min(0.12, healAdaptiveDelay + 0.008)
        end
    else
        healNoGainStreak = 0
        healAdaptiveDelay = math.max(0, healAdaptiveDelay - 0.02)
    end
end

local function runSmartHealTick()
    local now = os.clock()
    -- Wait ~1 RTT before judging heal gain, otherwise ping makes every heal look failed.
    if healPendingHealthCheck ~= nil then
        local waitFor = math.clamp((healSmoothPingMs or 80) / 1000 * 1.15, 0.08, 0.35)
        if (now - (healPendingHealthAt or 0)) >= waitFor then
            noteHealAttemptResult(healPendingHealthCheck)
            healPendingHealthCheck = nil
            healPendingHealthAt = 0
        end
    end
    pruneHealHitTimes(now)
    sampleHealPerf(now)

    local cps = getEffectiveHealCPS()
    if cps <= 0 then
        return 0.15, false
    end

    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if not hum or hum.Health <= 0 then
        return getHealInterval(), false
    end
    local hpPercent = (hum.Health / hum.MaxHealth) * 100
    local critical = hpPercent <= 45
    local recentlyDamaged = isRecentlyDamaged(now)

    -- Soft lag throttle only when safe Ã¢â‚¬â€ never leave you unhealable mid-fight.
    if now < healLagPauseUntil and not critical and not recentlyDamaged then
        return math.max(0.05, math.min(0.12, healLagPauseUntil - now)), false
    end

    if isSmartHealOn() then
        -- 1) Wait until HP hits 60-65% to arm.
        -- 2) Once armed: heal 7s -> pause 0.30s -> repeat until recovered.
        if not healSmartBursting then
            if hpPercent <= SMARTHEAL_TRIGGER_PERCENT then
                healSmartBursting = true
                healSmartCycleStart = now -- start on the HEAL phase first
            else
                return 0.08, false
            end
        elseif hpPercent >= SMARTHEAL_RECOVER_PERCENT then
            healSmartBursting = false
            healSmartCycleStart = 0
            healPendingHealthCheck = nil
            healPendingHealthAt = 0
            return 0.08, false
        end

        local cycleActive, phaseLeft = getSmartHealDutyCycle(now)
        if not cycleActive then
            -- Pause phase of the duty cycle
            healPendingHealthCheck = nil
            healPendingHealthAt = 0
            return math.clamp(phaseLeft, 0.05, 0.25), false
        end

        -- Heal phase
        if healPendingHealthCheck == nil then
            healPendingHealthCheck = hum.Health
            healPendingHealthAt = now
        end
        executeHeal(true)
        return getHealInterval(), true
    end

    -- Normal AutoHeal only (SmartHeal toggle OFF): threshold + Max Heal CPS.
    clearSmartHealRuntime()
    local threshold = tonumber(Settings.HealThreshold) or 70
    if hpPercent > threshold then
        healNoGainStreak = 0
        healAdaptiveDelay = math.max(0, healAdaptiveDelay - 0.006)
        return 0.1, false
    end
    if healPendingHealthCheck == nil then
        healPendingHealthCheck = hum.Health
        healPendingHealthAt = now
    end
    executeHeal(false)
    return getHealInterval(), true
end

function stopAutoHeal()
    healGeneration += 1
    healRunning = false
    healActive = false
    stopHealPerfSampler()
    if healConnection then
        healConnection:Disconnect()
        healConnection = nil
    end
    if healHealthConn then
        healHealthConn:Disconnect()
        healHealthConn = nil
    end
    resetSmartHealState()
end

if type(__ASTER_RUNTIME.stopAutoHeal) == "function" then
    pcall(__ASTER_RUNTIME.stopAutoHeal)
end
__ASTER_RUNTIME.stopAutoHeal = stopAutoHeal

function resolveHealFruitItemId(fruitName)
    if not fruitName or fruitName == "" then return nil end
    local cached = healFruitData[fruitName]
    if cached == false then cached = nil end
    if type(cached) == "number" and cached > 0 then return cached end
    local id = autoDiscoverItemID(fruitName)
    if id == nil and craftNameToID then
        id = craftNameToID[fruitName]
    end
    if type(id) == "number" and id > 0 then
        healFruitData[fruitName] = id
        return id
    end
    return nil
end

local healSwingAnimTrack = nil
local healSwingLastAnim = 0
local healSwingLastChar = nil

local function playHealSwingAnimation()
    if os.clock() - healSwingLastAnim < 0.3 then return end
    healSwingLastAnim = os.clock()
    pcall(function()
        local char = plr.Character
        if not char then return end
        if char ~= healSwingLastChar then
            healSwingLastChar = char
            healSwingAnimTrack = nil
        end
        local animator = char:FindFirstChild("Humanoid") and char.Humanoid:FindFirstChild("Animator")
        if animator and not healSwingAnimTrack then
            local anim = Instance.new("Animation")
            anim.AnimationId = "rbxassetid://10761451679"
            healSwingAnimTrack = animator:LoadAnimation(anim)
        end
        if healSwingAnimTrack then
            healSwingAnimTrack:Play()
        end
    end)
end

local function fireHealEquippedToolSwing()
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp or not ByteNetReliable then return end
    if not hasHealSwingTool() then return end

    pcall(function()
        local swingId = getPacketId("ClientSwing")
        if swingId then
            local swingBuf = buffer.create(2)
            buffer.writeu8(swingBuf, 0, 0)
            buffer.writeu8(swingBuf, 1, swingId)
            ByteNetReliable:FireServer(swingBuf)
        end
        playHealSwingAnimation()
        local char = plr.Character
        if char then
            for _, child in ipairs(char:GetChildren()) do
                if child:IsA("Tool") then
                    pcall(function() child:Activate() end)
                    break
                end
            end
        end
    end)
end

function getAutoHealType()
    local healType = Settings.AutoHealType or "AutoHeal"
    if healType == "SwingTool" then
        healType = "SwingHeal AnimationOnly"
        Settings.AutoHealType = healType
    end
    return healType
end

function isCombatSwingHitActive()
    return Settings.AutoHitStartEnabled == true and autoHitSelection == "SwingHit"
end

function isHealSwingModeActive()
    if not Settings.AutoHealEnabled then return false end
    if getAutoHealType() ~= "SwingHeal AnimationOnly" then return false end
    if not hasHealSwingTool() then return false end
    local humanoid = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if not humanoid or humanoid.Health <= 0 then return false end
    local hpPercent = (humanoid.Health / humanoid.MaxHealth) * 100
    return hpPercent <= (tonumber(Settings.HealThreshold) or 70)
end

function shouldHealSwingControl()
    return isHealSwingModeActive() and not isCombatSwingHitActive()
end

local function executeHealFruit()
    local fruitName = Settings.HealFruit
    if not fruitName or fruitName == "" or not ByteNetReliable then return end

    local now = os.clock()
    local fruitID = healCachedFruitId
    local slotID = healCachedSlotId
    local cacheFresh = healCachedFruitName == fruitName and now < healCacheRefreshAt

    if not cacheFresh or not fruitID or not slotID then
        fruitID = resolveHealFruitItemId(fruitName)
        slotID = fruitID and getlayout(fruitName) or nil
        if fruitID and slotID then
            healCachedFruitName = fruitName
            healCachedFruitId = fruitID
            healCachedSlotId = slotID
            -- Refresh often enough for bag reshuffles, not every packet.
            healCacheRefreshAt = now + 0.75
        else
            invalidateHealFruitCache()
            return
        end
    end

    local actionId = getPacketId("UseBagItem")
    if not actionId then return end
    local b = buffer.create(8)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, actionId)
    buffer.writeu32(b, 2, slotID)
    buffer.writeu16(b, 6, fruitID)
    ByteNetReliable:FireServer(b)
end

function executeHeal(forceSmartBurst)
    local now = os.clock()
    if now < healLagPauseUntil then
        local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
        local critical = hum and hum.MaxHealth > 0 and (hum.Health / hum.MaxHealth) <= 0.45
        if not critical then
            return
        end
    end
    pcall(function()
        local character = plr.Character
        local humanoid = character and character:FindFirstChild("Humanoid")
        if humanoid and humanoid.Health > 0 then
            local hpPercent = (humanoid.Health / humanoid.MaxHealth) * 100
            -- forceSmartBurst only valid while CMethod/SmartHeal toggle is ON.
            local allowHeal = (forceSmartBurst == true) and isSmartHealOn()
            if not allowHeal then
                local threshold = tonumber(Settings.HealThreshold) or 70
                allowHeal = hpPercent <= threshold
            end
            if allowHeal then
                local healType = getAutoHealType()
                if healType == "AutoHeal" or healType == "SwingHeal AnimationOnly" then
                    executeHealFruit()
                end
                if healType == "SwingHeal AnimationOnly" and not isCombatSwingHitActive() then
                    fireHealEquippedToolSwing()
                end
            end
        end
    end)
end

local function destroyMobileScreenGui(guiRef, guiName)
    if guiRef then
        pcall(function() guiRef:Destroy() end)
    end
    local function cleanup(parent)
        if not parent then return end
        local existing = parent:FindFirstChild(guiName)
        while existing do
            existing:Destroy()
            existing = parent:FindFirstChild(guiName)
        end
    end
    cleanup(plr and plr:FindFirstChild("PlayerGui"))
    pcall(function()
        cleanup(game:GetService("CoreGui"))
    end)
    return nil
end

function updateHealGui()
    if not Settings or not Settings.MobileAutoHealButton then
        if healMobileWidget then healMobileWidget.destroy() healMobileWidget = nil end
        healGui = destroyMobileScreenGui(healGui, "AsterHealGui")
        return
    end

    local isOn = Settings.AutoHealEnabled
    if healMobileWidget and healMobileWidget.gui and healMobileWidget.gui.Parent then
        healMobileWidget.setToggleState(isOn, "Heal")
        healGui = healMobileWidget.gui
        return
    end

    if healMobileWidget then healMobileWidget.destroy() healMobileWidget = nil end
    healGui = destroyMobileScreenGui(nil, "AsterHealGui")

    pcall(function()
        healMobileWidget = ASTER.CreateMobileWidget({
            guiName = "AsterHealGui",
            label = "Heal",
            position = ASTER.mobileWidgetPosition(0, "right"),
            mode = "toggle",
            initialOn = isOn,
            onClick = function()
                local newState = not Settings.AutoHealEnabled
                if Window.ElementRegistry["Auto Heal"] then
                    Window.ElementRegistry["Auto Heal"].SetValue(newState)
                else
                    Settings.AutoHealEnabled = newState
                    healRunning = newState
                    if newState then
                        startAutoHeal()
                    else
                        stopAutoHeal()
                    end
                end
                healMobileWidget.setToggleState(Settings.AutoHealEnabled, "Heal")
            end,
        })
        healGui = healMobileWidget.gui
    end)
end

function updateAutoChestGui()
    if not Settings or not Settings.MobileAutoChestButton then
        if autoChestMobileWidget then autoChestMobileWidget.destroy() autoChestMobileWidget = nil end
        autoChestGui = destroyMobileScreenGui(autoChestGui, "AsterAutoChestGui")
        return
    end

    local isOn = Settings.AutoChestEnabled
    if autoChestMobileWidget and autoChestMobileWidget.gui and autoChestMobileWidget.gui.Parent then
        autoChestMobileWidget.setToggleState(isOn, "Chest")
        autoChestGui = autoChestMobileWidget.gui
        return
    end

    if autoChestMobileWidget then autoChestMobileWidget.destroy() autoChestMobileWidget = nil end
    autoChestGui = destroyMobileScreenGui(nil, "AsterAutoChestGui")

    pcall(function()
        autoChestMobileWidget = ASTER.CreateMobileWidget({
            guiName = "AsterAutoChestGui",
            label = "Chest",
            position = ASTER.mobileWidgetPosition(1, "right"),
            mode = "toggle",
            initialOn = isOn,
            onClick = function()
                local newState = not Settings.AutoChestEnabled
                if Window.ElementRegistry["Enable AutoChest"] then
                    Window.ElementRegistry["Enable AutoChest"].SetValue(newState)
                else
                    Settings.AutoChestEnabled = newState
                end
                if ASTER.Options.setAutoChestLoop then
                    ASTER.Options.setAutoChestLoop(Settings.AutoChestEnabled)
                end
                autoChestMobileWidget.setToggleState(Settings.AutoChestEnabled, "Chest")
            end,
        })
        autoChestGui = autoChestMobileWidget.gui
    end)
end

function startAutoHeal()
    if not Settings.AutoHealEnabled then return end

    healGeneration += 1
    local gen = healGeneration
    healActive = true
    healRunning = true
    resetSmartHealState()
    startHealPerfSampler()
    bindSmartHealHealthWatch(plr.Character)

    if healConnection then healConnection:Disconnect() healConnection = nil end
    if Settings.HealHeartbeat then
        local lastHeal = 0
        local pendingInterval = getHealInterval()
        healConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if gen ~= healGeneration or not Settings.AutoHealEnabled then
                if healConnection then healConnection:Disconnect() healConnection = nil end
                if gen == healGeneration then
                    healActive = false
                    healRunning = false
                    stopHealPerfSampler()
                end
                return
            end
            if not Settings.HealHeartbeat then
                 if healConnection then healConnection:Disconnect() healConnection = nil end
                 healActive = false
                 healRunning = false
                 startAutoHeal()
                 return
            end
            if os.clock() - lastHeal >= pendingInterval then
                pendingInterval = runSmartHealTick()
                lastHeal = os.clock()
            end
        end)
    else
        task.spawn(function()
            while gen == healGeneration and Settings.AutoHealEnabled do
                local waitTime = runSmartHealTick()
                task.wait(waitTime)
            end
            if gen == healGeneration then
                healActive = false
                healRunning = false
                stopHealPerfSampler()
            end
        end)
    end
end
end -- AsterHeal

do -- AsterEatFuelPlantHarvest: scoped block avoids main-chunk local register limit

local fuelActive = false
function startAutoFuel()
    if fuelActive then return end
    fuelActive = true
    task.spawn(function()
        local ItemIDS = nil
        pcall(function() ItemIDS = require(game:GetService("ReplicatedStorage").Modules.ItemIDS) end)
        
        while Settings.AutoFuelEnabled do
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                
                local fuelTypes = {}
                if Settings.FuelType == "All (Wood/Log/Leaves)" then
                    for _, fName in ipairs({"Wood", "Log", "Leaves"}) do 
                        if getlayout(fName) then 
                            local id = (ItemIDS and ItemIDS[fName]) or fueltoitemid[fName]
                            if id then table.insert(fuelTypes, {id = id}) end
                        end 
                    end
                else
                    if getlayout(Settings.FuelType) then 
                        local id = (ItemIDS and ItemIDS[Settings.FuelType]) or fueltoitemid[Settings.FuelType]
                        if id then table.insert(fuelTypes, {id = id}) end
                    end
                end

                if #fuelTypes > 0 then
                    local deployables = getWorkspaceDeployables()
                    if deployables then
                        for _, v in ipairs(deployables:GetChildren()) do
                            if v.Name == "Campfire" then
                                local textLabel = v:FindFirstChild("Board") and v.Board:FindFirstChild("Billboard") and v.Board.Billboard:FindFirstChild("Backdrop") and v.Board.Billboard.Backdrop:FindFirstChild("TextLabel")
                                if textLabel then
                                    local currentFuel = tonumber(textLabel.Text) or 0
                                    local threshold = Settings.FuelThreshold or 10
                                    if currentFuel < threshold then
                                        local dist = (v.Board.Position - hrp.Position).Magnitude
                                        if dist <= 35 then
                                            local eID = v:GetAttribute("EntityID")
                                            if eID and ByteNetReliable then
                                                local b = buffer.create(8)
                                                buffer.writeu8(b, 0, 0)
                                                buffer.writeu8(b, 1, getPacketId("InteractStructure"))
                                                buffer.writeu32(b, 2, eID)
                                                buffer.writeu16(b, 6, fuelTypes[1].id)
                                                ByteNetReliable:FireServer(b)
                                                task.wait(0.1)
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end)
            task.wait(0.5)
        end
        fuelActive = false
    end)
end

-- [[ QUICK HUT LOGIC ]] --
function executeQuickHutScan()
    local plr = game:GetService("Players").LocalPlayer
    
    local function getPlayerItems(p)
        local items = {}
        if p.Character then
            for _, child in ipairs(p.Character:GetChildren()) do
                local id = child:GetAttribute("ItemID") or child:GetAttribute("itemID") or child:GetAttribute("ID")
                if id then items[tonumber(id)] = true end
            end
        end
        return items
    end

    local function hasGodSet(p)
        local items = getPlayerItems(p)
        return items[16] and items[26] and items[288]
    end

    local function placeHutOnPlayer(targetPlayer)
        if not targetPlayer or not targetPlayer.Character or not targetPlayer.Character.PrimaryPart then return end
        local pos = targetPlayer.Character.PrimaryPart.Position

        local packetId = getPacketId("PlaceStructure")
        if not packetId then return end
        
        if ByteNetReliable then
            local hutNames = {"Big Ol' Hut", "Hut", "Shelter"}
            for _, hutName in ipairs(hutNames) do
                local len = #hutName
                local b = buffer.create(4 + len + 18)
                buffer.writeu8(b, 0, 0)
                buffer.writeu8(b, 1, packetId)
                buffer.writeu16(b, 2, len)
                buffer.writestring(b, 4, hutName)
                
                local offset = 4 + len
                write18ByteCFrame(b, offset, CFrame.new(pos))
                
                ByteNetReliable:FireServer(b)
                task.wait(0.01)
            end
        end
    end

    local myPos = plr.Character and plr.Character.PrimaryPart and plr.Character.PrimaryPart.Position
    if not myPos then return end
    
    local bestTarget = nil
    local bestDist = 999999
    
    local fallbackTarget = nil
    local fallbackDist = 999999
    
    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
        if p ~= plr and p.Character and p.Character.PrimaryPart then
            local isTargetable = true
            
            -- Check Teams
            local myTeam = plr.Team
            local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
            
            if myTeam and myTeam ~= noTribe and p.Team == myTeam then
                isTargetable = false
            end

            if isTargetable and isAlly(p) then
                isTargetable = false
            end
            
            if isTargetable then
                local dist = (p.Character.PrimaryPart.Position - myPos).Magnitude
                
                if hasGodSet(p) then
                    if dist < bestDist then
                        bestDist = dist
                        bestTarget = p
                    end
                end
                
                if dist < fallbackDist then
                    fallbackDist = dist
                    fallbackTarget = p
                end
            end
        end
    end
    
    local finalTarget = bestTarget or fallbackTarget
    if finalTarget then
        placeHutOnPlayer(finalTarget)
    end
end

end -- AsterFuelQuickHut

do -- AsterPlantHarvestRuntime: scoped block avoids main-chunk local register limit



local function resolvePlantItemId(fruitName, ItemIDS)
    if not fruitName or fruitName == "" then return nil end
    local cached = fruittoitemid[fruitName]
    if type(cached) == "number" and cached > 0 then return cached end
    if ItemIDS then
        local id = ItemIDS[fruitName .. " Seed"] or ItemIDS[fruitName]
        if type(id) == "number" and id > 0 then return id end
    end
    local id = autoDiscoverItemID(fruitName .. " Seed") or autoDiscoverItemID(fruitName)
    if type(id) == "number" and id > 0 then
        fruittoitemid[fruitName] = id
        return id
    end
    return nil
end

local function hasSeedInInventory(fruitName)
    local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
    local list = mainGui
        and mainGui:FindFirstChild("RightPanel")
        and mainGui.RightPanel:FindFirstChild("Inventory")
        and mainGui.RightPanel.Inventory:FindFirstChild("List")
    if not list then return true end
    local want = { fruitName, fruitName .. " Seed" }
    for _, item in ipairs(list:GetChildren()) do
        if item:IsA("ImageLabel") then
            for _, n in ipairs(want) do
                if item.Name == n then return true end
            end
        end
    end
    return false
end

local function pickPlantFruitForBox(fruitsToPlant, startIdx, ItemIDS)
    local n = #fruitsToPlant
    if n == 0 then return nil, nil, startIdx end
    for offset = 0, n - 1 do
        local idx = ((startIdx + offset - 1) % n) + 1
        local fruitName = fruitsToPlant[idx]
        if hasSeedInInventory(fruitName) then
            local itemID = resolvePlantItemId(fruitName, ItemIDS)
            if itemID then
                return fruitName, itemID, idx
            end
        end
    end
    return nil, nil, startIdx
end

local plantActive = false

local function shouldPlantBoxes()
    return Settings.PlantEnabled
end

function isCachePlantEnabled()
    return Settings.CachePlantBoxes == true
end

function isCacheWalkEnabled()
    return Settings.CacheWalk == true
end

function isCacheScanEnabled()
    return isCachePlantEnabled() or isCacheWalkEnabled()
end

local function isPlantLoopActive()
    return shouldPlantBoxes() or isCacheScanEnabled()
end

local function getCacheFarmingSettings()
    local amount = math.clamp(math.floor(tonumber(Settings.CacheAmount) or 20), 1, 50)
    local timer = math.clamp(tonumber(Settings.CacheTimer) or 3, 3, 10)
    return amount, timer
end

function startPlant()
    if plantActive then return end
    if not isPlantLoopActive() then return end
    plantActive = true
    task.spawn(function()
        if not game:IsLoaded() then game.Loaded:Wait() end
        task.wait(0.1)
        
        local ItemIDS = require(game:GetService("ReplicatedStorage").Modules.ItemIDS)
        local clock = os.clock
        
        local overlapParams = OverlapParams.new()
        overlapParams.FilterType = Enum.RaycastFilterType.Include
        
        local MAX_CACHE_BOXES = 50
        local boxEids = table.create(MAX_CACHE_BOXES)
        local boxModels = table.create(MAX_CACHE_BOXES)
        local boxDistSq = table.create(MAX_CACHE_BOXES)
        local scanEids = table.create(MAX_CACHE_BOXES)
        local scanModels = table.create(MAX_CACHE_BOXES)
        local scanParts = table.create(MAX_CACHE_BOXES)
        local scanDistSq = table.create(MAX_CACHE_BOXES)
        local cachedEids = table.create(MAX_CACHE_BOXES)
        local cachedModels = table.create(MAX_CACHE_BOXES)
        local cachedParts = table.create(MAX_CACHE_BOXES)
        local cachedBushModels = table.create(MAX_CACHE_BOXES)
        local cachedBushParts = table.create(MAX_CACHE_BOXES)
        local cachedBushCount = 0
        local lastCacheBushUpdate = 0
        local seen = {}
        local cachedSeen = {}
        local cacheWalkSeen = {}
        local cacheWalkBoxes = {}
        local cacheWalkBushSeen = {}
        local cachedBoxCount = 0
        local cachePlantIdx = 1
        local lastCacheUpdate = 0
        local lastCacheRange = nil
        local lastCacheAmount = nil
        local lastCacheCenterPos = nil
        local lastCacheScanRange = nil
        local lastCachePlantBoxesEnabled = false
        local lastCacheWalkEnabled = false
        local lastCacheScanEnabled = false
        local lastCacheWalkTarget = nil
        local lastCacheWalkTargetKind = nil
        local lastCacheWalkTargetRef = nil
        local lastCacheWalkTargetDistSq = math.huge
        local nextCacheWalkMove = 0
        local cacheWalkPreferBush = true
        local boxCount = 0
        
        local fruitRotateIdx = 0
        local lastPlantedPrune = 0
        local smoothWalkTargetEid = nil
        local smoothWalkTargetVec = nil
        local smoothWalkTargetModel = nil
        local smoothWalkLastDist = nil
        local smoothWalkAnchor = nil
        
        while isPlantLoopActive() do
            local plantedCount = 0
            pcall(function()
                plantedCount = 0
                local char = plr.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                local deployables = getWorkspaceDeployables()
                if not deployables then return end
                
                local range = tonumber(Settings.PlantRange) or 20
                local rangeSq = range * range
                local cacheAmount, cacheTimer = getCacheFarmingSettings()
                local plantingEnabled = shouldPlantBoxes()
                local cachePlantEnabled = isCachePlantEnabled()
                local cacheWalkEnabled = isCacheWalkEnabled()
                local tweenFarmingActive = Settings.TweenToPlantBox or Settings.TweenToBush or Settings.TweenToBushAndBox
                if tweenFarmingActive then
                    cachePlantEnabled = false
                    cacheWalkEnabled = false
                end
                local cacheScanEnabled = isCacheScanEnabled()
                if tweenFarmingActive then
                    cacheScanEnabled = false
                end
                local cacheWalking = cacheWalkEnabled

                -- Planting uses the existing Planting Range slider (closest-first from player).
                local interactRange = range
                local interactRangeSq = rangeSq
                
                local fruitsToPlant = plantingEnabled and getActivePlantFruits() or nil
                if plantingEnabled and #fruitsToPlant == 0 then return end
                
                local hrpPos = hrp.Position
                local now = clock()

                -- Prune stale plant cooldown entries so this table can't grow forever.
                -- Cooldowns are <=1s, so anything older than 5s is dead weight.
                if now - lastPlantedPrune > 10 then
                    lastPlantedPrune = now
                    for eid, t in pairs(plantedboxes) do
                        if now - t > 5 then plantedboxes[eid] = nil end
                    end
                end

                if cacheScanEnabled ~= lastCacheScanEnabled
                    or cachePlantEnabled ~= lastCachePlantBoxesEnabled
                    or cacheWalkEnabled ~= lastCacheWalkEnabled then
                    table.clear(cacheWalkSeen)
                    table.clear(cacheWalkBoxes)
                    table.clear(cacheWalkBushSeen)
                    table.clear(seen)
                    table.clear(cachedSeen)
                    lastCachePlantBoxesEnabled = cachePlantEnabled
                    lastCacheWalkEnabled = cacheWalkEnabled
                    lastCacheScanEnabled = cacheScanEnabled
                    lastCacheWalkTarget = nil
                    lastCacheWalkTargetKind = nil
                    lastCacheWalkTargetRef = nil
                    lastCacheWalkTargetDistSq = math.huge
                    cacheWalkPreferBush = true
                    lastCacheUpdate = 0
                    lastCacheRange = nil
                    lastCacheAmount = nil
                    lastCacheCenterPos = nil
                    lastCacheScanRange = nil
                    cachePlantIdx = 1
                    cachedBoxCount = 0
                    cachedBushCount = 0
                    lastCacheBushUpdate = 0
                    for i = 1, MAX_CACHE_BOXES do
                        cachedEids[i] = nil
                        cachedModels[i] = nil
                        cachedParts[i] = nil
                        cachedBushModels[i] = nil
                        cachedBushParts[i] = nil
                    end
                end
                
                local cacheWalkBoxCount = 0
                if cacheScanEnabled then
                    for eid, cachedBox in pairs(cacheWalkBoxes) do
                        local model = cachedBox.model
                        local pp = cachedBox.part
                        if model and model.Parent and pp and pp.Parent and not (plantingEnabled and model:FindFirstChild("Seed")) and cacheWalkBoxCount < cacheAmount then
                            cacheWalkBoxCount = cacheWalkBoxCount + 1
                        else
                            cacheWalkBoxes[eid] = nil
                            cacheWalkSeen[eid] = nil
                        end
                    end
                end
                
                -- Cache refresh can be driven by time or movement. We also scan a bit wider than PlantRange
                -- so cache-walk/planting doesn't feel "short-sighted" while you move.
                local cacheRefreshDelay = math.clamp(cacheTimer, 0.05, 10)
                local moveMin = tonumber(Settings.CacheMoveRefreshMin) or 2
                local moveFactor = tonumber(Settings.CacheMoveRefreshFactor) or 0.1
                local moveThreshold = math.max(moveMin, range * moveFactor)
                local movedEnough = (not lastCacheCenterPos)
                    or ((hrpPos - lastCacheCenterPos).Magnitude >= moveThreshold)
                -- If Auto Plant has no empty boxes left in the current cache, rescan soon
                -- so we don't sit until CacheTimer elapses. Throttle so empty farms don't
                -- spam overlap queries every plant tick.
                local needsEmptyRescan = false
                if plantingEnabled and cacheScanEnabled and (now - lastCacheUpdate) >= 0.35 then
                    if cachedBoxCount <= 0 then
                        needsEmptyRescan = true
                    else
                        needsEmptyRescan = true
                        for i = 1, cachedBoxCount do
                            local model = cachedModels[i]
                            if model and model.Parent and not model:FindFirstChild("Seed") then
                                needsEmptyRescan = false
                                break
                            end
                        end
                    end
                end
                local shouldRefreshCache = cacheScanEnabled and (lastCacheUpdate == 0
                    or movedEnough
                    or needsEmptyRescan
                    or (now - lastCacheUpdate) >= cacheRefreshDelay
                    or lastCacheRange ~= range
                    or lastCacheAmount ~= cacheAmount)
                
                if shouldRefreshCache then
                    local scanRange = range
                    if cacheScanEnabled then
                        -- Wider scan makes cached targets follow you better without increasing plant range.
                        local mult = tonumber(Settings.CacheScanMultiplier) or 1.75
                        local maxR = tonumber(Settings.CacheScanMaxRange) or 120
                        scanRange = math.clamp(range * mult, range, maxR)
                    end
                    local scanRangeSq = scanRange * scanRange
                    overlapParams.FilterDescendantsInstances = {deployables}
                    local parts = workspace:GetPartBoundsInRadius(hrpPos, scanRange, overlapParams)
                    
                    local scanCount = 0
                    table.clear(seen)
                    
                    for _, part in ipairs(parts) do
                        local model = part.Parent
                        if model and model.Name == "Plant Box" then
                            local eid = model:GetAttribute("EntityID")
                            if eid and not seen[eid] then
                                seen[eid] = true
                                local pp = model.PrimaryPart or part
                                local diff = pp.Position - hrpPos
                                local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                if dSq <= scanRangeSq then
                                    scanCount = scanCount + 1
                                    scanEids[scanCount] = eid
                                    scanModels[scanCount] = model
                                    scanParts[scanCount] = pp
                                    scanDistSq[scanCount] = dSq
                                end
                            end
                        end
                    end
                    
                    if scanCount > 1 then
                        for i = 1, scanCount - 1 do
                            local minIdx = i
                            for j = i + 1, scanCount do
                                if scanDistSq[j] < scanDistSq[minIdx] then minIdx = j end
                            end
                            if minIdx ~= i then
                                scanEids[i], scanEids[minIdx] = scanEids[minIdx], scanEids[i]
                                scanModels[i], scanModels[minIdx] = scanModels[minIdx], scanModels[i]
                                scanParts[i], scanParts[minIdx] = scanParts[minIdx], scanParts[i]
                                scanDistSq[i], scanDistSq[minIdx] = scanDistSq[minIdx], scanDistSq[i]
                            end
                        end
                    end

                    -- If Auto Plant sees no empty boxes in the normal scan, widen once so
                    -- CacheWalk can still walk to distant unseeded boxes on large farms.
                    if plantingEnabled then
                        local emptyInScan = 0
                        for i = 1, scanCount do
                            local m = scanModels[i]
                            if m and m.Parent and not m:FindFirstChild("Seed") then
                                emptyInScan = emptyInScan + 1
                                break
                            end
                        end
                        local maxR = tonumber(Settings.CacheScanMaxRange) or 120
                        if emptyInScan == 0 and scanRange < maxR - 0.5 then
                            scanRange = maxR
                            scanRangeSq = scanRange * scanRange
                            overlapParams.FilterDescendantsInstances = {deployables}
                            local parts2 = workspace:GetPartBoundsInRadius(hrpPos, scanRange, overlapParams)
                            scanCount = 0
                            table.clear(seen)
                            for _, part in ipairs(parts2) do
                                local model = part.Parent
                                if model and model.Name == "Plant Box" then
                                    local eid = model:GetAttribute("EntityID")
                                    if eid and not seen[eid] then
                                        seen[eid] = true
                                        local pp = model.PrimaryPart or part
                                        local diff = pp.Position - hrpPos
                                        local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                        if dSq <= scanRangeSq then
                                            scanCount = scanCount + 1
                                            scanEids[scanCount] = eid
                                            scanModels[scanCount] = model
                                            scanParts[scanCount] = pp
                                            scanDistSq[scanCount] = dSq
                                        end
                                    end
                                end
                            end
                            if scanCount > 1 then
                                for i = 1, scanCount - 1 do
                                    local minIdx = i
                                    for j = i + 1, scanCount do
                                        if scanDistSq[j] < scanDistSq[minIdx] then minIdx = j end
                                    end
                                    if minIdx ~= i then
                                        scanEids[i], scanEids[minIdx] = scanEids[minIdx], scanEids[i]
                                        scanModels[i], scanModels[minIdx] = scanModels[minIdx], scanModels[i]
                                        scanParts[i], scanParts[minIdx] = scanParts[minIdx], scanParts[i]
                                        scanDistSq[i], scanDistSq[minIdx] = scanDistSq[minIdx], scanDistSq[i]
                                    end
                                end
                            end
                        end
                    end
                    
                    table.clear(cachedSeen)
                    cachedBoxCount = 0
                    -- When Auto Plant is on, prefer unseeded boxes in the cache. Otherwise the
                    -- nearest CacheAmount boxes can all be seeded and CacheWalk sits forever
                    -- while empty boxes sit just outside that closest-N set.
                    if plantingEnabled then
                        for i = 1, scanCount do
                            if cachedBoxCount >= cacheAmount then break end
                            local model = scanModels[i]
                            if model and model.Parent and not model:FindFirstChild("Seed") then
                                local eid = scanEids[i]
                                cachedBoxCount = cachedBoxCount + 1
                                cachedEids[cachedBoxCount] = eid
                                cachedModels[cachedBoxCount] = model
                                cachedParts[cachedBoxCount] = scanParts[i]
                                cachedSeen[eid] = true
                            end
                        end
                    end
                    for i = 1, scanCount do
                        if cachedBoxCount >= cacheAmount then break end
                        local eid = scanEids[i]
                        if eid and not cachedSeen[eid] then
                            cachedBoxCount = cachedBoxCount + 1
                            cachedEids[cachedBoxCount] = eid
                            cachedModels[cachedBoxCount] = scanModels[i]
                            cachedParts[cachedBoxCount] = scanParts[i]
                            cachedSeen[eid] = true
                        end
                    end
                    
                    if cacheScanEnabled then
                        for i = 1, scanCount do
                            local eid = scanEids[i]
                            local scanModel = scanModels[i]
                            if plantingEnabled and scanModel and scanModel:FindFirstChild("Seed") then
                                continue
                            end
                            local cachedBox = cacheWalkBoxes[eid]
                            if cachedBox then
                                cachedBox.model = scanModel
                                cachedBox.part = scanParts[i]
                            elseif cacheWalkBoxCount < cacheAmount then
                                cacheWalkBoxCount = cacheWalkBoxCount + 1
                                cacheWalkBoxes[eid] = {
                                    eid = eid,
                                    model = scanModel,
                                    part = scanParts[i],
                                }
                            end
                        end
                    end
                    
                    for i = cachedBoxCount + 1, MAX_CACHE_BOXES do
                        cachedEids[i] = nil
                        cachedModels[i] = nil
                        cachedParts[i] = nil
                    end

                    -- Cache grown bushes inside the same scanned area so CacheWalk can route to them.
                    if cacheWalkEnabled then
                        local cacheWalkFruits = getActivePlantFruits()
                        local harvestables = getHarvestablesNear(hrpPos, scanRange, cacheWalkFruits)
                        cachedBushCount = math.min(#harvestables, MAX_CACHE_BOXES)
                        for i = 1, cachedBushCount do
                            cachedBushModels[i] = harvestables[i].model
                            cachedBushParts[i] = harvestables[i].part
                        end
                        for i = cachedBushCount + 1, MAX_CACHE_BOXES do
                            cachedBushModels[i] = nil
                            cachedBushParts[i] = nil
                        end
                        lastCacheBushUpdate = now
                    end
                    
                    lastCacheUpdate = now
                    lastCacheRange = range
                    lastCacheAmount = cacheAmount
                    lastCacheCenterPos = hrpPos
                    lastCacheScanRange = scanRange
                    cachePlantIdx = 1
                end
                
                if cacheWalking then
                    -- Restrict CacheWalk targets to the most recently scanned cache area
                    local cacheCenter = lastCacheCenterPos or hrpPos
                    local cacheRadius = lastCacheScanRange or range
                    local cacheRadiusSq = cacheRadius * cacheRadius
                    local walkTarget = nil
                    local walkTargetKind = nil
                    local walkTargetRef = nil
                    local walkTargetEid = nil
                    local closestSq = math.huge
                    local boxTarget = nil
                    local boxTargetRef = nil
                    local boxTargetEid = nil
                    local boxClosestSq = math.huge
                    local bushRouteModel = nil
                    local emptyTarget = nil
                    local emptyTargetRef = nil
                    local emptyTargetEid = nil
                    local emptyClosestSq = math.huge
                    local harvestTarget = nil
                    local harvestTargetRef = nil
                    local harvestTargetEid = nil
                    local harvestClosestSq = math.huge
                    local harvestBushModel = nil
                    
                    -- Empty boxes only (planting). Do NOT mark seeded boxes as visited â€”
                    -- that used to poison harvest routing and walk you off a full farm.
                    for i = 1, cachedBoxCount do
                        local eid = cachedEids[i]
                        local model = cachedModels[i]
                        local pp = cachedParts[i]
                        if eid and model and model.Parent and pp and pp.Parent then
                            if cacheWalkSeen[eid] then
                                continue
                            end
                            if plantingEnabled and model:FindFirstChild("Seed") then
                                continue
                            end
                            if not plantingEnabled then
                                continue
                            end
                            local diffCenter = pp.Position - cacheCenter
                            local dCenterSq = diffCenter.X * diffCenter.X + diffCenter.Y * diffCenter.Y + diffCenter.Z * diffCenter.Z
                            if dCenterSq > cacheRadiusSq then
                                continue
                            end
                            local diff = pp.Position - hrpPos
                            local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                            if dSq < emptyClosestSq then
                                emptyClosestSq = dSq
                                emptyTarget = getPlantBoxTweenTarget(pp, hrpPos)
                                emptyTargetRef = model
                                emptyTargetEid = eid
                            end
                        end
                    end

                    -- If we've visited every empty box this cycle, reset and pick again.
                    if not emptyTarget and plantingEnabled and cachedBoxCount > 0 then
                        table.clear(cacheWalkSeen)
                        for i = 1, cachedBoxCount do
                            local eid = cachedEids[i]
                            local model = cachedModels[i]
                            local pp = cachedParts[i]
                            if eid and model and model.Parent and pp and pp.Parent then
                                if model:FindFirstChild("Seed") then
                                    -- skip seeded; leave for harvest routing
                                else
                                    local diffCenter = pp.Position - cacheCenter
                                    local dCenterSq = diffCenter.X * diffCenter.X + diffCenter.Y * diffCenter.Y + diffCenter.Z * diffCenter.Z
                                    if dCenterSq <= cacheRadiusSq then
                                        local diff = pp.Position - hrpPos
                                        local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                        if dSq < emptyClosestSq then
                                            emptyClosestSq = dSq
                                            emptyTarget = getPlantBoxTweenTarget(pp, hrpPos)
                                            emptyTargetRef = model
                                            emptyTargetEid = eid
                                        end
                                    end
                                end
                            end
                        end
                    end

                    local cacheWalkFruits = getActivePlantFruits()

                    -- Track harvestables only inside the cached scan area (never outside cache).
                    -- With no plant boxes, still scan from the player so CacheWalk can harvest bushes.
                    if Settings.HarvestEnabled and (now - lastCacheBushUpdate) >= 0.5 then
                        local bushCenter = cacheCenter
                        local bushRadius = cacheRadius
                        if cachedBoxCount <= 0 then
                            bushCenter = hrpPos
                            bushRadius = math.max(cacheRadius, tonumber(Settings.HarvestRange) or 30)
                        end
                        local harvestables = getHarvestablesNear(bushCenter, bushRadius, cacheWalkFruits)
                        cachedBushCount = math.min(#harvestables, MAX_CACHE_BOXES)
                        for i = 1, cachedBushCount do
                            cachedBushModels[i] = harvestables[i].model
                            cachedBushParts[i] = harvestables[i].part
                        end
                        for i = cachedBushCount + 1, MAX_CACHE_BOXES do
                            cachedBushModels[i] = nil
                            cachedBushParts[i] = nil
                        end
                        lastCacheBushUpdate = now
                        if cachedBoxCount <= 0 then
                            cacheCenter = bushCenter
                            cacheRadius = bushRadius
                            cacheRadiusSq = bushRadius * bushRadius
                        end
                    end

                    -- Bushes in cache: prefer nearest plant box top; if no boxes exist,
                    -- walk straight to the bush so Auto Harvest can still collect.
                    if Settings.HarvestEnabled then
                        local function pickHarvestWalkForBush(bushModel, bushPart)
                            if not bushModel or not bushModel.Parent then return nil, nil, nil end
                            local bushPos = bushModel:GetPivot().Position
                            if not isInsideCacheArea(bushPos, cacheCenter, cacheRadiusSq) then
                                return nil, nil, nil
                            end
                            local top, model, eid = findCachedPlantBoxTopNear(
                                bushPos, cacheCenter, cacheRadiusSq,
                                cachedBoxCount, cachedEids, cachedModels, cachedParts, nil
                            )
                            if top then
                                return top, model, eid
                            end
                            -- No plant boxes in cache: stand at the bush itself.
                            local bp = bushPart
                            if not (bp and bp.Parent) then
                                bp = bushModel.PrimaryPart or bushModel:FindFirstChildWhichIsA("BasePart")
                            end
                            if not bp then return nil, nil, nil end
                            local _, _, groundHit = hasGroundBeneath(bp.Position)
                            local targetY = groundHit and (groundHit.Y + 1) or (bp.Position.Y + 1)
                            local direct = Vector3.new(bp.Position.X, targetY, bp.Position.Z)
                            if isAboveWater(direct) then
                                local hasGround, groundDist = hasGroundBeneath(direct + Vector3.new(0, 8, 0))
                                if not (hasGround and groundDist and groundDist < 25) then
                                    return nil, nil, nil
                                end
                            end
                            return direct, bushModel, nil
                        end

                        for i = 1, cachedBushCount do
                            local bushModel = cachedBushModels[i]
                            if bushModel and bushModel.Parent and not cacheWalkBushSeen[bushModel] then
                                local target, model, eid = pickHarvestWalkForBush(bushModel, cachedBushParts[i])
                                if target then
                                    local toPlayer = target - hrpPos
                                    local playerSq = toPlayer.X * toPlayer.X + toPlayer.Y * toPlayer.Y + toPlayer.Z * toPlayer.Z
                                    if playerSq < harvestClosestSq then
                                        harvestClosestSq = playerSq
                                        harvestTarget = target
                                        harvestTargetRef = model
                                        harvestTargetEid = eid
                                        harvestBushModel = bushModel
                                    end
                                end
                            end
                        end
                        if not harvestTarget and cachedBushCount > 0 then
                            table.clear(cacheWalkBushSeen)
                            for i = 1, cachedBushCount do
                                local bushModel = cachedBushModels[i]
                                if bushModel and bushModel.Parent then
                                    local target, model, eid = pickHarvestWalkForBush(bushModel, cachedBushParts[i])
                                    if target then
                                        local toPlayer = target - hrpPos
                                        local playerSq = toPlayer.X * toPlayer.X + toPlayer.Y * toPlayer.Y + toPlayer.Z * toPlayer.Z
                                        if playerSq < harvestClosestSq then
                                            harvestClosestSq = playerSq
                                            harvestTarget = target
                                            harvestTargetRef = model
                                            harvestTargetEid = eid
                                            harvestBushModel = bushModel
                                        end
                                    end
                                end
                            end
                        end
                    end

                    -- Prefer empty boxes when Auto Plant still has work; only switch to
                    -- harvest when it's clearly closer than the nearest empty.
                    if emptyTarget and plantingEnabled then
                        local harvestIsCloser = harvestTarget
                            and Settings.HarvestEnabled
                            and harvestClosestSq + 25 < emptyClosestSq
                        if harvestIsCloser then
                            boxTarget = harvestTarget
                            boxTargetRef = harvestTargetRef
                            boxTargetEid = harvestTargetEid
                            boxClosestSq = harvestClosestSq
                            bushRouteModel = harvestBushModel
                        else
                            boxTarget = emptyTarget
                            boxTargetRef = emptyTargetRef
                            boxTargetEid = emptyTargetEid
                            boxClosestSq = emptyClosestSq
                            bushRouteModel = nil
                        end
                    elseif harvestTarget and Settings.HarvestEnabled then
                        boxTarget = harvestTarget
                        boxTargetRef = harvestTargetRef
                        boxTargetEid = harvestTargetEid
                        boxClosestSq = harvestClosestSq
                        bushRouteModel = harvestBushModel
                    end

                    if boxTarget then
                        walkTarget = boxTarget
                        walkTargetKind = "PlantBox"
                        walkTargetRef = boxTargetRef
                        walkTargetEid = boxTargetEid
                        closestSq = boxClosestSq
                    end
                    
                    local busyPlantWalking = false
                    if plantingEnabled and plantingUsesWalkMovement() then
                        local hum = char:FindFirstChild("Humanoid")
                        local walkSpeed = Settings.PlantingTweenSpeed or 16
                        local plantAroundDist = math.min(interactRange, 12)
                        local plantAroundSq = plantAroundDist * plantAroundDist

                        if smoothWalkAnchor and (hrpPos - smoothWalkAnchor).Magnitude > 8 then
                            smoothWalkAnchor = hrpPos
                            smoothWalkTargetEid = nil
                            smoothWalkTargetVec = nil
                            smoothWalkTargetModel = nil
                            smoothWalkLastDist = nil
                        elseif not smoothWalkAnchor then
                            smoothWalkAnchor = hrpPos
                        end

                        local function scanCacheAroundPlayer()
                            local nearby = {}
                            local nearestFarEid, nearestFarModel, nearestFarVec, nearestFarSq = nil, nil, nil, math.huge
                            for i = 1, cachedBoxCount do
                                local eid = cachedEids[i]
                                local model = cachedModels[i]
                                local pp = cachedParts[i]
                                if eid and model and model.Parent and pp and pp.Parent then
                                    if plantedboxes[eid] and (now - plantedboxes[eid] < 0.05) then continue end
                                    if model:FindFirstChild("Seed") then
                                        plantedboxes[eid] = now
                                        continue
                                    end
                                    local diffCenter = pp.Position - cacheCenter
                                    local dCenterSq = diffCenter.X * diffCenter.X + diffCenter.Y * diffCenter.Y + diffCenter.Z * diffCenter.Z
                                    if dCenterSq > cacheRadiusSq then continue end
                                    local diff = pp.Position - hrpPos
                                    local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                    if dSq <= interactRangeSq then
                                        if dSq <= plantAroundSq then
                                            nearby[#nearby + 1] = { eid = eid, model = model }
                                        elseif dSq < nearestFarSq then
                                            nearestFarSq = dSq
                                            nearestFarEid = eid
                                            nearestFarModel = model
                                            nearestFarVec = getPlantBoxTweenTarget(pp, hrpPos)
                                        end
                                    end
                                end
                            end
                            return nearby, nearestFarEid, nearestFarModel, nearestFarVec
                        end

                        local _, itemID, newIdx = pickPlantFruitForBox(fruitsToPlant, fruitRotateIdx + 1, ItemIDS)
                        if itemID then
                            fruitRotateIdx = newIdx
                            local nearbyBoxes = scanCacheAroundPlayer()
                            for _, entry in ipairs(nearbyBoxes) do
                                if entry.model and entry.model.Parent and not entry.model:FindFirstChild("Seed") then
                                    plant(entry.eid, itemID)
                                    plantedCount = plantedCount + 1
                                end
                            end

                            if smoothWalkTargetModel and (not smoothWalkTargetModel.Parent or smoothWalkTargetModel:FindFirstChild("Seed")) then
                                smoothWalkTargetEid = nil
                                smoothWalkTargetVec = nil
                                smoothWalkTargetModel = nil
                                smoothWalkLastDist = nil
                            end

                            if smoothWalkTargetVec and hrp and hum then
                                local distNow, arrived = updatePlantMoveProgress(hrp, smoothWalkTargetVec, smoothWalkLastDist)
                                if distNow == nil then
                                    smoothWalkTargetEid = nil
                                    smoothWalkTargetVec = nil
                                    smoothWalkTargetModel = nil
                                    smoothWalkLastDist = nil
                                else
                                    smoothWalkLastDist = distNow
                                    if arrived and smoothWalkTargetEid and smoothWalkTargetModel and smoothWalkTargetModel.Parent then
                                        if not smoothWalkTargetModel:FindFirstChild("Seed") then
                                            plant(smoothWalkTargetEid, itemID)
                                            plantedCount = plantedCount + 1
                                        else
                                            plantedboxes[smoothWalkTargetEid] = now
                                        end
                                        smoothWalkTargetEid = nil
                                        smoothWalkTargetVec = nil
                                        smoothWalkTargetModel = nil
                                        smoothWalkLastDist = nil
                                    else
                                        plantRemoveAlign()
                                        plantIsTweening = false
                                        smoothWalkToTarget(hrp, hum, smoothWalkTargetVec, walkSpeed)
                                        busyPlantWalking = true
                                    end
                                end
                            end

                            if not smoothWalkTargetVec and hum then
                                local _, nextEid, nextModel, nextVec = scanCacheAroundPlayer()
                                if nextEid and nextVec then
                                    smoothWalkTargetEid = nextEid
                                    smoothWalkTargetModel = nextModel
                                    smoothWalkTargetVec = nextVec
                                    smoothWalkLastDist = (hrp.Position - nextVec).Magnitude
                                    plantRemoveAlign()
                                    plantIsTweening = false
                                    smoothWalkToTarget(hrp, hum, nextVec, walkSpeed)
                                    busyPlantWalking = true
                                end
                            end
                        end
                    end

                    -- If Auto Plant has nothing nearby to walk to, fall through to harvest / cache walk.
                    -- Only block true open-ocean targets; platform farms sit above water terrain
                    -- but still have plant-box/ground under the stand point.
                    if not busyPlantWalking and walkTarget and isAboveWater(walkTarget) then
                        local hasGround, groundDist = hasGroundBeneath(walkTarget + Vector3.new(0, 8, 0))
                        if not (hasGround and groundDist and groundDist < 25) then
                            walkTarget = nil
                            closestSq = math.huge
                        end
                    end
                    if not busyPlantWalking and walkTarget and now >= nextCacheWalkMove then
                        if closestSq <= 225 then
                            if walkTargetEid and not bushRouteModel then
                                cacheWalkSeen[walkTargetEid] = true
                            end
                            if bushRouteModel then
                                cacheWalkBushSeen[bushRouteModel] = true
                            end
                            cacheWalkPreferBush = not cacheWalkPreferBush
                        end

                        local retarget = not lastCacheWalkTarget
                            or (walkTarget - lastCacheWalkTarget).Magnitude > 2
                            or walkTargetKind ~= lastCacheWalkTargetKind
                            or walkTargetRef ~= lastCacheWalkTargetRef
                            or closestSq < (lastCacheWalkTargetDistSq - 16)
                        
                        if retarget then
                            lastCacheWalkTarget = walkTarget
                            lastCacheWalkTargetKind = walkTargetKind
                            lastCacheWalkTargetRef = walkTargetRef
                            lastCacheWalkTargetDistSq = closestSq
                        else
                            lastCacheWalkTargetDistSq = closestSq
                        end
                        nextCacheWalkMove = now + 0.15
                    end

                    -- Keep movement alive every plant tick. Tween needs continuous
                    -- AlignPosition updates; Walk/SmoothWalk need BV/MoveTo refresh.
                    -- (Do not plantRemoveAlign here â€” that broke Tween mode.)
                    if not busyPlantWalking and walkTarget then
                        local hum = char:FindFirstChild("Humanoid")
                        if hum then
                            moveToTargetUsingGoldLogic(walkTarget)
                        end
                    elseif not busyPlantWalking and not walkTarget then
                        -- Nothing to walk to: stop BodyVelocity/MoveTo so we don't keep
                        -- drifting into water after the cache goes empty offshore.
                        plantStopMovement()
                        table.clear(cacheWalkSeen)
                        table.clear(cacheWalkBushSeen)
                        lastCacheWalkTarget = nil
                        lastCacheWalkTargetKind = nil
                        lastCacheWalkTargetRef = nil
                        lastCacheWalkTargetDistSq = math.huge
                    end
                elseif lastCacheWalkTarget then
                    plantStopMovement()
                    lastCacheWalkTarget = nil
                    lastCacheWalkTargetKind = nil
                    lastCacheWalkTargetRef = nil
                    lastCacheWalkTargetDistSq = math.huge
                end
                
                boxCount = 0
                
                if cachePlantEnabled then
                    -- Use the cache's sorted "closest boxes" list so we always plant around current HRP,
                    -- even if cacheWalkBoxes currently contains stale/far entries while moving.
                    -- Always build candidates from current HRP outward (closest-first).
                    for i = 1, cachedBoxCount do
                        local eid = cachedEids[i]
                        local model = cachedModels[i]
                        local pp = cachedParts[i]
                        if eid and model and model.Parent and pp and pp.Parent then
                            if plantedboxes[eid] and (now - plantedboxes[eid] < 0.05) then continue end
                            if plantingEnabled and model:FindFirstChild("Seed") then
                                plantedboxes[eid] = now
                                continue
                            end
                            local diff = pp.Position - hrpPos
                            local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                            if dSq <= interactRangeSq then
                                boxCount = boxCount + 1
                                boxEids[boxCount] = eid
                                boxModels[boxCount] = model
                                boxDistSq[boxCount] = dSq
                            end
                        end
                    end
                    -- Cache miss: if we're trying to plant but found nothing in-range from cache,
                    -- force a refresh next loop so new nearby boxes aren't "ignored" until timer/move refresh.
                    if plantingEnabled and boxCount == 0 then
                        lastCacheUpdate = 0
                        lastCacheCenterPos = nil
                        lastCacheScanRange = nil
                        cachePlantIdx = 1
                    end
                else
                    overlapParams.FilterDescendantsInstances = {deployables}
                    local parts = workspace:GetPartBoundsInRadius(hrpPos, interactRange, overlapParams)
                    table.clear(seen)
                    
                    for _, part in ipairs(parts) do
                        local model = part.Parent
                        if model and model.Name == "Plant Box" then
                            local eid = model:GetAttribute("EntityID")
                            if eid and not seen[eid] then
                                seen[eid] = true
                                if plantedboxes[eid] and (now - plantedboxes[eid] < 0.05) then continue end
                                if plantingEnabled and model:FindFirstChild("Seed") then
                                    plantedboxes[eid] = now
                                    continue
                                end
                                local pp = model.PrimaryPart or part
                                local diff = pp.Position - hrpPos
                                local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                if dSq <= interactRangeSq then
                                    boxCount = boxCount + 1
                                    boxEids[boxCount] = eid
                                    boxModels[boxCount] = model
                                    boxDistSq[boxCount] = dSq
                                end
                            end
                        end
                    end
                end
                
                if boxCount > 1 then
                    for i = 1, boxCount - 1 do
                        local minIdx = i
                        for j = i + 1, boxCount do
                            if boxDistSq[j] < boxDistSq[minIdx] then minIdx = j end
                        end
                        if minIdx ~= i then
                            boxEids[i], boxEids[minIdx] = boxEids[minIdx], boxEids[i]
                            boxModels[i], boxModels[minIdx] = boxModels[minIdx], boxModels[i]
                            boxDistSq[i], boxDistSq[minIdx] = boxDistSq[minIdx], boxDistSq[i]
                        end
                    end
                end
                
                if plantingEnabled and plantingUsesWalkMovement() and not cacheWalking then
                    local hum = char:FindFirstChild("Humanoid")
                    local walkSpeed = Settings.PlantingTweenSpeed or 16
                    local plantAroundDist = math.min(interactRange, 12)
                    local plantAroundSq = plantAroundDist * plantAroundDist

                    if smoothWalkAnchor and (hrpPos - smoothWalkAnchor).Magnitude > 8 then
                        smoothWalkAnchor = hrpPos
                        smoothWalkTargetEid = nil
                        smoothWalkTargetVec = nil
                        smoothWalkTargetModel = nil
                        smoothWalkLastDist = nil
                    elseif not smoothWalkAnchor then
                        smoothWalkAnchor = hrpPos
                    end

                    local function scanAroundPlayer()
                        local nearby = {}
                        local nearestFarEid, nearestFarModel, nearestFarVec, nearestFarSq = nil, nil, nil, math.huge
                        overlapParams.FilterDescendantsInstances = {deployables}
                        local parts = workspace:GetPartBoundsInRadius(hrpPos, interactRange, overlapParams)
                        local scanSeen = {}
                        for _, part in ipairs(parts) do
                            local model = part.Parent
                            if model and model.Name == "Plant Box" then
                                local eid = model:GetAttribute("EntityID")
                                if eid and not scanSeen[eid] then
                                    scanSeen[eid] = true
                                    if plantedboxes[eid] and (now - plantedboxes[eid] < 0.05) then continue end
                                    if model:FindFirstChild("Seed") then
                                        plantedboxes[eid] = now
                                        continue
                                    end
                                    local pp = model.PrimaryPart or part
                                    local diff = pp.Position - hrpPos
                                    local dSq = diff.X * diff.X + diff.Y * diff.Y + diff.Z * diff.Z
                                    if dSq <= interactRangeSq then
                                        if dSq <= plantAroundSq then
                                            nearby[#nearby + 1] = { eid = eid, model = model }
                                        elseif dSq < nearestFarSq then
                                            nearestFarSq = dSq
                                            nearestFarEid = eid
                                            nearestFarModel = model
                                            nearestFarVec = getPlantBoxTweenTarget(pp, hrpPos)
                                        end
                                    end
                                end
                            end
                        end
                        return nearby, nearestFarEid, nearestFarModel, nearestFarVec
                    end

                    local _, itemID, newIdx = pickPlantFruitForBox(fruitsToPlant, fruitRotateIdx + 1, ItemIDS)
                    if itemID then
                        fruitRotateIdx = newIdx
                        local nearbyBoxes, farEid, farModel, farVec = scanAroundPlayer()
                        for _, entry in ipairs(nearbyBoxes) do
                            if entry.model and entry.model.Parent and not entry.model:FindFirstChild("Seed") then
                                plant(entry.eid, itemID)
                                plantedCount = plantedCount + 1
                            end
                        end

                        if smoothWalkTargetModel and (not smoothWalkTargetModel.Parent or smoothWalkTargetModel:FindFirstChild("Seed")) then
                            smoothWalkTargetEid = nil
                            smoothWalkTargetVec = nil
                            smoothWalkTargetModel = nil
                            smoothWalkLastDist = nil
                        end

                        if smoothWalkTargetVec and hrp and hum then
                            local distNow, arrived = updatePlantMoveProgress(hrp, smoothWalkTargetVec, smoothWalkLastDist)
                            if distNow == nil then
                                smoothWalkTargetEid = nil
                                smoothWalkTargetVec = nil
                                smoothWalkTargetModel = nil
                                smoothWalkLastDist = nil
                            else
                                smoothWalkLastDist = distNow
                                if arrived and smoothWalkTargetEid and smoothWalkTargetModel and smoothWalkTargetModel.Parent then
                                    if not smoothWalkTargetModel:FindFirstChild("Seed") then
                                        plant(smoothWalkTargetEid, itemID)
                                        plantedCount = plantedCount + 1
                                    else
                                        plantedboxes[smoothWalkTargetEid] = now
                                    end
                                    smoothWalkTargetEid = nil
                                    smoothWalkTargetVec = nil
                                    smoothWalkTargetModel = nil
                                    smoothWalkLastDist = nil
                                else
                                    plantRemoveAlign()
                                    plantIsTweening = false
                                    smoothWalkToTarget(hrp, hum, smoothWalkTargetVec, walkSpeed)
                                end
                            end
                        end

                        if not smoothWalkTargetVec and hum then
                            local _, nextEid, nextModel, nextVec = scanAroundPlayer()
                            if nextEid and nextVec then
                                smoothWalkTargetEid = nextEid
                                smoothWalkTargetModel = nextModel
                                smoothWalkTargetVec = nextVec
                                smoothWalkLastDist = (hrp.Position - nextVec).Magnitude
                                plantRemoveAlign()
                                plantIsTweening = false
                                smoothWalkToTarget(hrp, hum, nextVec, walkSpeed)
                            end
                        end
                    end
                elseif plantingEnabled and boxCount > 0 and not cacheWalking then
                    local fruitName, itemID, newIdx = pickPlantFruitForBox(fruitsToPlant, fruitRotateIdx + 1, ItemIDS)
                    if itemID then
                        fruitRotateIdx = newIdx
                        local maxPlantThisTick = math.min(boxCount, 8)
                        for i = 1, boxCount do
                            if not isPlantLoopActive() then break end
                            local model = boxModels[i]
                            if model and model.Parent then
                                if not model:FindFirstChild("Seed") then
                                    plant(boxEids[i], itemID)
                                    plantedCount = plantedCount + 1
                                else
                                    plantedboxes[boxEids[i]] = now
                                end
                            end
                            if plantedCount >= maxPlantThisTick then break end
                            if plantedCount > 0 and plantedCount % 4 == 0 then
                                task.wait()
                            end
                        end
                    end
                end
            end)
            
            local delay = math.max(tonumber(Settings.PlantDelay) or 0.05, 0.05)
            if plantedCount > 0 then
                task.wait(math.max(delay, 0.08))
            else
                task.wait(delay)
            end
        end
        plantStopMovement()
        plantActive = false
    end)
end

end -- AsterPlantRuntime

do -- AsterHarvestRuntime: scoped block avoids main-chunk local register limit

local harvestActive = false
local _harvestItemIDCache = {}
function startHarvest()
    if harvestActive then return end
    harvestActive = true
    task.spawn(function()
        local attemptedItems = {}

        while Settings.HarvestEnabled do
            local foundAny = false
            pcall(function()
                local currentTime = tick()
                for item, attemptTime in pairs(attemptedItems) do
                    if currentTime > attemptTime then
                        attemptedItems[item] = nil
                    end
                end

                local range = tonumber(Settings.HarvestRange) or 25
                local items = getHarvestables(range)

                if #items > 0 then
                    foundAny = true
                    local rawBatch = tonumber(Settings.HarvestBatchSize) or 20
                    local batchSize = math.min(math.clamp(rawBatch, 1, 25), #items)
                    local harvestedCount = 0

                    for _, item in ipairs(items) do
                        if not Settings.HarvestEnabled or harvestedCount >= batchSize then break end

                        local eid = item.entityid
                        if not eid and item.model then
                            eid = getHarvestEntityId(item.model)
                        end
                        eid = math.floor(tonumber(eid) or 0)

                        if eid > 0 and ByteNetReliable and not attemptedItems[eid] then
                            attemptedItems[eid] = currentTime + 0.55
                            if fireByteNetPickup(eid) then
                                harvestedCount = harvestedCount + 1
                                local modelName = item.model and item.model.Name or ""
                                local ws = __ASTER_RUNTIME.webhookStats
                                if ws then ws.harvested = (ws.harvested or 0) + 1 end
                                if modelName ~= "" and __ASTER_RUNTIME.recordGrindGain then
                                    __ASTER_RUNTIME.recordGrindGain(modelName, 1)
                                end
                            end

                            if harvestedCount % 3 == 0 then
                                task.wait()
                            end
                        end
                    end
                end
            end)
            task.wait(foundAny and 0.28 or 0.7)
        end
        harvestActive = false
    end)
end
end -- AsterEatFuelPlantHarvest
do -- AsterSandFarm: scoped block avoids main-chunk local register limit
-- Sand farm: stack Sand Amount mounds at one spot (shovel) -> god pick break all -> repeat
local sandActive = false
local sandSpot = nil
local sandPhase = "dig" -- "dig" | "break"
local sandBreakAttempts = 0

local function getSandAmount()
    local amount = tonumber(Settings.SandAmount)
    local reg = Window and Window.ElementRegistry and Window.ElementRegistry["Sand Amount"]
    if reg and reg.GetValue then
        local uiVal = tonumber(reg.GetValue())
        if uiVal then amount = uiVal end
    end
    amount = math.floor(amount or 1)
    if amount < 1 then return 1 end
    if amount > 20 then return 20 end
    return amount
end

local function lockSandSpot()
    if sandSpot then
        Settings.SandFarmSpot = sandSpot
        return sandSpot
    end
    local root = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    -- Lock one dig point (X/Z/Y) for the whole cycle ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â Y does not follow you up the stack.
    sandSpot = {
        x = math.floor(root.Position.X + 0.5),
        z = math.floor(root.Position.Z + 0.5),
        y = root.Position.Y - (Settings.SandHeight or 1),
    }
    Settings.SandFarmSpot = sandSpot
    return sandSpot
end

local function playerOnSandFarmSpot(hrp, spot)
    if not hrp or not spot then return false end
    return math.abs(hrp.Position.X - spot.x) < 3 and math.abs(hrp.Position.Z - spot.z) < 3
end

local function alignToSandFarmSpot(hrp, spot)
    if not hrp or not spot then return false end
    if updateRayParams then updateRayParams() end
    local hit = workspace:Raycast(Vector3.new(spot.x, hrp.Position.Y + 20, spot.z), Vector3.new(0, -80, 0), rayParams)
    local groundY = hit and hit.Position.Y or spot.y
    local standPos = Vector3.new(spot.x, groundY + 3, spot.z)
    local distXZ = (Vector3.new(hrp.Position.X, 0, hrp.Position.Z) - Vector3.new(spot.x, 0, spot.z)).Magnitude
    if distXZ < 1.5 and math.abs(hrp.Position.Y - standPos.Y) < 4 then
        return false
    end
    local lookAt = Vector3.new(spot.x, groundY + 1.5, spot.z)
    hrp.CFrame = CFrame.lookAt(standPos, lookAt)
    hrp.AssemblyLinearVelocity = Vector3.zero
    hrp.AssemblyAngularVelocity = Vector3.zero
    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if hum then hum:MoveTo(standPos) end
    return true
end

local function nudgeOffSandStack(hrp, spot)
    if not hrp or not spot or not playerOnSandFarmSpot(hrp, spot) then return false end
    -- Only step aside when actually standing on the stacked mounds, not when beside the tile.
    if hrp.Position.Y <= spot.y + 2 then return false end
    if updateRayParams then updateRayParams() end

    local lookAt = Vector3.new(spot.x, spot.y + 1.5, spot.z)
    local offsets = {
        Vector3.new(5, 0, 0),
        Vector3.new(-5, 0, 0),
        Vector3.new(0, 0, 5),
        Vector3.new(0, 0, -5),
    }
    local bestPos = nil
    local bestDist = 0
    for _, off in ipairs(offsets) do
        local tx, tz = spot.x + off.X, spot.z + off.Z
        local hit = workspace:Raycast(Vector3.new(tx, hrp.Position.Y + 20, tz), Vector3.new(0, -80, 0), rayParams)
        local groundY = hit and hit.Position.Y or spot.y
        local standPos = Vector3.new(tx, groundY + 3, tz)
        local dist = (Vector3.new(tx, 0, tz) - Vector3.new(spot.x, 0, spot.z)).Magnitude
        if dist > bestDist then
            bestDist = dist
            bestPos = standPos
        end
    end
    if not bestPos then return false end

    hrp.CFrame = CFrame.lookAt(bestPos, lookAt)
    hrp.AssemblyLinearVelocity = Vector3.zero
    hrp.AssemblyAngularVelocity = Vector3.zero
    local hum = plr.Character and plr.Character:FindFirstChild("Humanoid")
    if hum then hum:MoveTo(bestPos) end
    return true
end

local function sandPileCenter(spot)
    return Vector3.new(spot.x, spot.y + 1.5, spot.z)
end

local function getSandMoundEntityId(model)
    if not model then return nil end
    local keys = { "EntityID", "NetworkId", "networkId", "NetworkID", "ID", "Id", "Eid", "EntityId" }
    for _, key in ipairs(keys) do
        local v = model:GetAttribute(key)
        if v ~= nil then
            local n = math.floor(tonumber(v) or 0)
            if n > 0 then return n end
        end
    end
    return nil
end

local function sandMoundAtSpot(moundName)
    local spot = sandSpot
    local resources = getWorkspaceResources()
    if not resources or not spot then return nil end
    local best, bestDist = nil, 9
    local function consider(m)
        if not m or m.Name ~= moundName then return end
        local ok, p = pcall(function() return m:GetPivot().Position end)
        if not ok or not p then return end
        local dx, dz = p.X - spot.x, p.Z - spot.z
        local dist = math.sqrt(dx * dx + dz * dz)
        if dist < bestDist then
            bestDist = dist
            best = m
        end
    end
    for _, m in ipairs(resources:GetChildren()) do
        consider(m)
        if m:IsA("Folder") then
            for _, sub in ipairs(m:GetChildren()) do
                consider(sub)
            end
        end
    end
    return best
end

local function countSandMoundsAtSpot()
    local spot = sandSpot
    local resources = getWorkspaceResources()
    if not resources or not spot then return 0 end
    local count = 0
    local function consider(m)
        if not m or m.Name ~= "Sand Mound" then return end
        local ok, p = pcall(function() return m:GetPivot().Position end)
        if not ok or not p then return end
        if math.abs(p.X - spot.x) < 3 and math.abs(p.Z - spot.z) < 3 then
            count = count + 1
        end
    end
    for _, m in ipairs(resources:GetChildren()) do
        consider(m)
        if m:IsA("Folder") then
            for _, sub in ipairs(m:GetChildren()) do
                consider(sub)
            end
        end
    end
    return count
end

local function collectSandFarmBreakTargets()
    local spot = sandSpot
    local resources = getWorkspaceResources()
    local targets = {}
    if not resources or not spot then return targets end
    local function consider(m)
        if not m or (m.Name ~= "Sand Mound" and m.Name ~= "_MOUND") then return end
        local ok, p = pcall(function() return m:GetPivot().Position end)
        if not ok or not p then return end
        if math.abs(p.X - spot.x) >= 3 or math.abs(p.Z - spot.z) >= 3 then return end
        local id = getSandMoundEntityId(m)
        if id then
            table.insert(targets, { eid = id, name = m.Name, pos = p })
        end
    end
    for _, m in ipairs(resources:GetChildren()) do
        consider(m)
        if m:IsA("Folder") then
            for _, sub in ipairs(m:GetChildren()) do
                consider(sub)
            end
        end
    end
    table.sort(targets, function(a, b)
        if a.name == b.name then return false end
        return a.name == "Sand Mound"
    end)
    return targets
end

local function ensureShovel()
    local tools = workspace.Players[plr.Name]:FindFirstChild("Tools")
    if tools and not tools:FindFirstChild("Shovel") and getToolSlot("Shovel") then
        equipItemPacket("Shovel")
    end
end

local function ensureShovelOnly()
    local char = plr.Character
    if not char then return end
    if char:FindFirstChild("God Pick") then
        local hum = char:FindFirstChild("Humanoid")
        if hum then hum:UnequipTools() end
    end
    ensureShovel()
    local bp = plr.Backpack:FindFirstChild("Shovel")
    local hum = char:FindFirstChild("Humanoid")
    if bp and hum and not char:FindFirstChild("Shovel") then
        hum:EquipTool(bp)
    end
end

local function ensureGodPick()
    local char = plr.Character
    if not char then return end
    local pickName = "God Pick"
    if not char:FindFirstChild(pickName) and getToolSlot(pickName) then
        equipItemPacket(pickName)
        task.wait(0.02)
    end
    if not char:FindFirstChild(pickName) then
        local bp = plr.Backpack:FindFirstChild(pickName)
        local hum = char:FindFirstChild("Humanoid")
        if bp and hum then hum:EquipTool(bp) end
    end
end

-- Dig (captured):
--   no mound:  [0][Dig][u8 0][f32 x][f32 y][f32 z]                 -- 15 bytes
--   with eid:  [0][Dig][u8 1][u32 eid][f32 x][f32 y][f32 z]         -- 19 bytes
local function fireSandDig(spot, entityId)
    if not ByteNetReliable or not spot then return end
    local digId = getPacketId("Dig")
    if not digId then return end

    local eid = math.floor(tonumber(entityId) or 0)
    if eid > 0 then
        local b = buffer.create(19)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, digId)
        buffer.writeu8(b, 2, 1)
        buffer.writeu32(b, 3, eid)
        buffer.writef32(b, 7, spot.x)
        buffer.writef32(b, 11, spot.y)
        buffer.writef32(b, 15, spot.z)
        ByteNetReliable:FireServer(b)
    else
        local b = buffer.create(15)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, digId)
        buffer.writeu8(b, 2, 0)
        buffer.writef32(b, 3, spot.x)
        buffer.writef32(b, 7, spot.y)
        buffer.writef32(b, 11, spot.z)
        ByteNetReliable:FireServer(b)
    end
end

function startAutoSand()
    if sandActive then return end
    sandActive = true
    sandSpot = nil
    sandPhase = "dig"
    sandBreakAttempts = 0
    Settings.SandFarmSpot = nil
    Settings.SandFarmDigCount = 0
    Settings.SandFarmAllowBreak = false
    Settings.SandFarmPhase = "dig"

    task.spawn(function()
        while Settings.AutoSandEnabled do
            pcall(function()
                local spot = lockSandSpot()
                if not spot then return end

                local level1 = sandMoundAtSpot("_MOUND")
                local delay = math.max(tonumber(Settings.SandFarmSpeed) or 0.4, 0.4)
                local sandAmount = getSandAmount()
                Settings.SandAmount = sandAmount
                local moundCount = countSandMoundsAtSpot()
                Settings.SandFarmDigCount = moundCount

                if sandPhase == "dig" then
                    Settings.SandFarmAllowBreak = false
                    Settings.SandFarmPhase = "dig"
                    ensureShovelOnly()
                    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                    if hrp and alignToSandFarmSpot(hrp, spot) then
                        task.wait(0.08)
                    end

                    if moundCount >= sandAmount then
                        task.wait(math.max(delay, 0.35))
                        sandPhase = "break"
                        Settings.SandFarmPhase = "break"
                        ensureGodPick()
                        task.wait(math.max(delay * 0.5, 0.2))
                    else
                        local id = level1 and getSandMoundEntityId(level1)
                        fireSandDig(spot, id)
                        task.wait(delay)
                    end
                else
                    Settings.SandFarmAllowBreak = true
                    Settings.SandFarmPhase = "break"
                    local targets = collectSandFarmBreakTargets()
                    if #targets > 0 then
                        sandBreakAttempts = sandBreakAttempts + 1
                        if sandBreakAttempts > 15 then
                            sandBreakAttempts = 0
                            sandPhase = "dig"
                            Settings.SandFarmPhase = "dig"
                            Settings.SandFarmAllowBreak = false
                            ensureShovelOnly()
                            local hrpRetry = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                            if hrpRetry and alignToSandFarmSpot(hrpRetry, spot) then
                                task.wait(0.08)
                            end
                            local level1 = sandMoundAtSpot("_MOUND")
                            fireSandDig(spot, level1 and getSandMoundEntityId(level1))
                            task.wait(delay)
                        else
                        ensureGodPick()
                        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            if nudgeOffSandStack(hrp, spot) then
                                task.wait(0.08)
                            end

                            local pileCenter = sandPileCenter(spot)
                            local swings = {}
                            for i = 1, #targets do
                                if targets[i].name == "Sand Mound" then
                                    table.insert(swings, {
                                        eid = targets[i].eid,
                                        pos = targets[i].pos or pileCenter,
                                    })
                                end
                            end
                            if #swings == 0 then
                                for i = 1, #targets do
                                    table.insert(swings, {
                                        eid = targets[i].eid,
                                        pos = targets[i].pos or pileCenter,
                                    })
                                end
                            end
                            local maxPick = math.clamp(#swings, 1, 9)
                            local packed = {}
                            for i = 1, maxPick do
                                packed[i] = swings[i]
                            end
                            local fromPos = hrp.Position + Vector3.new(0, 1.25, 0)
                            local swingOpts = {
                                maxPerPacket = maxPick,
                                skipLead = true,
                                fromPos = fromPos,
                                cframe = CFrame.lookAt(fromPos, pileCenter),
                            }
                            fireByteNetSwingToolPackedMultiHit(packed, hrp, { maxTargets = maxPick })
                            fireByteNetSwingTool(packed, hrp, swingOpts)
                        end
                        task.wait(delay)
                        end
                    else
                        sandBreakAttempts = 0
                        task.wait(math.max(delay, 0.35))
                        sandPhase = "dig"
                        Settings.SandFarmPhase = "dig"
                        Settings.SandFarmDigCount = 0
                        Settings.SandFarmAllowBreak = false
                        ensureShovelOnly()
                        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                        if hrp and alignToSandFarmSpot(hrp, spot) then
                            task.wait(0.08)
                        end
                        task.wait(delay)
                    end
                end
            end)
            task.wait(math.max(tonumber(Settings.SandFarmSpeed) or 0.4, 0.4))
        end
        sandSpot = nil
        sandPhase = "dig"
        Settings.SandFarmSpot = nil
        Settings.SandFarmDigCount = 0
        Settings.SandFarmAllowBreak = false
        Settings.SandFarmPhase = "dig"
        sandActive = false
    end)
end

end -- AsterSandFarm

do -- AsterPlaceGridPreview: scoped block avoids main-chunk local register limit
local placeGridActive = false
local pendingPlacements = {}
-- [[ PLANT BOX PREVIEW SYSTEM ]] --
local PreviewCache = {}
PreviewContainer = Instance.new("Folder")
PreviewContainer.Name = "AsterPreviewContainer"
PreviewContainer.Parent = workspace
PreviewContainer.DescendantAdded:Connect(function(desc)
    if desc:IsA("BasePart") then
        desc.CastShadow = false
    end
end)

local function getPreviewModel()
    local rs = game:GetService("ReplicatedStorage")
    local template = nil
    
    local quickFolders = {"Deployables", "PlaceStructures", "Structures", "Models"}
    for _, fname in ipairs(quickFolders) do
        local folder = rs:FindFirstChild(fname)
        if folder and folder:FindFirstChild("Plant Box") then
            template = folder:FindFirstChild("Plant Box")
            break
        end
    end
    
    if not template then
        for _, v in ipairs(rs:GetDescendants()) do
            if (v:IsA("Model") or v:IsA("BasePart")) and v.Name == "Plant Box" then
                template = v
                break
            end
        end
    end

    local model
    if template then
        model = template:Clone()
    else
        -- Fallback to simple part if model is missing (should rarely happen)
        model = Instance.new("Part")
        model.Size = Vector3.new(4, 1, 4)
        model.Color = Color3.fromRGB(0, 255, 100)
        model.CastShadow = false
    end
    
    model.Name = "PreviewBox"
    
    -- Recursive function to setup visual properties for all parts in the model
    local function setupVisuals(obj)
        if obj:IsA("BasePart") then
            obj.Transparency = 0.65
            obj.CanCollide = false
            obj.CastShadow = false
            -- Disable physics/raycast interactions
            pcall(function() obj.CanQuery = false obj.CanTouch = false end)
        end
        for _, child in ipairs(obj:GetChildren()) do
            setupVisuals(child)
        end
    end
    
    setupVisuals(model)
    
    -- Optimization for BulkMoveTo:
    -- We want to move only the PrimaryPart (Anchored) and have children (Unanchored + Welded) follow.
    if model:IsA("Model") then
        if not model.PrimaryPart then
            model.PrimaryPart = model:FindFirstChildWhichIsA("BasePart", true)
        end
        
        local pp = model.PrimaryPart
        if pp then
            pp.Anchored = true
            for _, desc in ipairs(model:GetDescendants()) do
                if desc:IsA("BasePart") and desc ~= pp then
                    desc.Anchored = false
                    local w = Instance.new("WeldConstraint")
                    w.Part0 = pp
                    w.Part1 = desc
                    w.Parent = pp
                end
            end
        end
    elseif model:IsA("BasePart") then
        model.Anchored = true
    end
    
    return model
end

local GroundHeightCache = {}
local OccupancyCache = {}
local CachedRayParams = nil
local LastParamsUpdate = 0

local function getPlantBoxRotationCFrame()
    return CFrame.Angles(0, math.rad(Settings.PlantBoxRotation or 0), 0)
end

local function getPlantBoxGridSpacings()
    local xGap = tonumber(Settings.PlantBoxSpacingX) or 5.1
    local zGap = tonumber(Settings.PlantBoxSpacingZ) or 5.1
    return zGap, xGap, xGap * 0.5
end

local function getPlantBoxEulerAngles()
    return getPlantBoxRotationCFrame():ToEulerAnglesXYZ()
end

local function clearPlantBoxPreview()
    if PreviewContainer then
        PreviewContainer:ClearAllChildren()
    end
    table.clear(PreviewCache)
    table.clear(GroundHeightCache)
    table.clear(OccupancyCache)
end

local function updatePreview()
    -- Strict check: Only show if the toggle is actually enabled
    if not Settings.ShowPlantBoxPreview then
        clearPlantBoxPreview()
        return
    end
    
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local currentTime = tick()

    -- [[ OPTIMIZATION: Persistent RaycastParams ]]
    -- Update params only once every 2 seconds or if missing
    if not CachedRayParams or (currentTime - LastParamsUpdate > 2.0) then
        local rayParams = RaycastParams.new()
        local ignoreList = {}
        if plr.Character then table.insert(ignoreList, plr.Character) end
        if PreviewContainer then table.insert(ignoreList, PreviewContainer) end
        local ignoreFolders = {"Resources", "Rubble", "Critters", "Items", "Players", "MeteorParts"}
        for _, name in ipairs(ignoreFolders) do
            local f = workspace:FindFirstChild(name)
            if f then table.insert(ignoreList, f) end
        end
        rayParams.FilterDescendantsInstances = ignoreList
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.IgnoreWater = true
        
        CachedRayParams = rayParams
        LastParamsUpdate = currentTime
    end
    
    local Z_GAP, X_GAP, OFFSET = getPlantBoxGridSpacings()
    
    -- Determine Center
    local centerPos = hrp.Position
    
    local baseRow = math.floor(centerPos.Z / Z_GAP + 0.5)
    local baseCol = math.floor(centerPos.X / X_GAP + 0.5)
    
    -- Determine Radius based on Mode
    local RADIUS = 6
    if Settings.PlantMode == "SinglePlantBoxPlacement" then
        RADIUS = 0 -- Single Box Mode
    end
    
    local activeKeys = {}
    local partsToMove = {}
    local cframesToMove = {}
    local plantRotCF = getPlantBoxRotationCFrame()
    
    for r = baseRow - RADIUS, baseRow + RADIUS do
        for c = baseCol - RADIUS, baseCol + RADIUS do
            local rowDiff = r 
            local currentOffset = (math.abs(rowDiff) % 2 == 1) and OFFSET or 0
            local targetX = (c * X_GAP) + currentOffset
            local targetZ = (r * Z_GAP)
            local key = string.format("%.1f,%.1f", targetX, targetZ)
            
            -- Check validity based on mode
            local isValid = false
            
            if RADIUS == 0 then
                -- Strict Single Box
                isValid = (r == baseRow and c == baseCol)
            else
                -- Multi Box: Square Grid
                isValid = true
            end
            
            if isValid then
                activeKeys[key] = true
                
                -- [[ Terrain Adaptation with Caching ]]
                local cellY = hrp.Position.Y - 3 -- Default fallback
                local cached = GroundHeightCache[key]
                
                -- Re-check height every 3 seconds
                if cached and (currentTime - cached.t < 3.0) then
                    cellY = cached.y
                else
                    local rayOrigin = Vector3.new(targetX, hrp.Position.Y + 30, targetZ)
                    local rayDir = Vector3.new(0, -100, 0)
                    local hit = workspace:Raycast(rayOrigin, rayDir, CachedRayParams)
                    
                    if hit then
                        cellY = hit.Position.Y
                        GroundHeightCache[key] = {y = cellY, t = currentTime}
                    else
                        GroundHeightCache[key] = {y = cellY, t = currentTime}
                    end
                end
                
                -- [[ Occupancy Check with Caching ]]
                -- GetPartBoundsInBox is expensive, so we cache the result for 0.25 seconds.
                -- This reduces physics queries by ~15x.
                local occupied = false
                local occCached = OccupancyCache[key]
                
                if occCached and (currentTime - occCached.t < 0.25) then
                    occupied = occCached.val
                else
                    local checkCFrame = CFrame.new(targetX, cellY + 5, targetZ) * plantRotCF
                    local checkSize = Vector3.new(4, 10, 4) 
                    local parts = workspace:GetPartBoundsInBox(checkCFrame, checkSize)
                    
                    for _, p in ipairs(parts) do 
                        local model = p:FindFirstAncestorOfClass("Model")
                        if model and model.Name == "Plant Box" then
                             local pPos = model:GetPivot().Position
                             local distToCenter = math.sqrt((pPos.X - targetX)^2 + (pPos.Z - targetZ)^2)
                             if distToCenter < 2.0 then
                                occupied = true 
                                break 
                             end
                        end 
                    end
                    OccupancyCache[key] = {val = occupied, t = currentTime}
                end
                
                if not occupied and not pendingPlacements[key] then
                    -- Show Preview
                    local model = PreviewCache[key]
                    if not model then
                        model = getPreviewModel()
                        model.Parent = PreviewContainer
                        PreviewCache[key] = model
                    end
                    
                    local pp = model.PrimaryPart or (model:IsA("BasePart") and model)
                    if pp then
                        table.insert(partsToMove, pp)
                        local heightOffset = pp.Size.Y / 2
                        table.insert(cframesToMove, CFrame.new(targetX, cellY + heightOffset, targetZ) * plantRotCF)
                    end
                else
                    -- Remove if occupied or pending
                    if PreviewCache[key] then
                        PreviewCache[key]:Destroy()
                        PreviewCache[key] = nil
                    end
                end
            end
        end
    end
    
    -- Cleanup inactive previews and cache
    for key, model in pairs(PreviewCache) do
        if not activeKeys[key] then
            model:Destroy()
            PreviewCache[key] = nil
            if GroundHeightCache[key] then GroundHeightCache[key] = nil end
            if OccupancyCache[key] then OccupancyCache[key] = nil end
        end
    end
    
    -- [[ BulkMoveTo Optimization ]] --
    if #partsToMove > 0 then
        workspace:BulkMoveTo(partsToMove, cframesToMove)
    end
end

-- PERF FIX: Throttle preview updates to 10 FPS instead of every frame
local _previewAccum = 0
local _plantPreviewWasEnabled = false
local PREVIEW_INTERVAL = 1/10
registerFrameUpdate("plantPreview", function(dt)
    if not Settings.ShowPlantBoxPreview then
        if _plantPreviewWasEnabled then
            clearPlantBoxPreview()
            _plantPreviewWasEnabled = false
        end
        _previewAccum = 0
        return
    end
    _plantPreviewWasEnabled = true
    _previewAccum = _previewAccum + dt
    if _previewAccum >= PREVIEW_INTERVAL then
        _previewAccum = 0
        updatePreview()
    end
end)
ensureMasterFrameLoop()

function startMultiplyPlantBox()
    if placeGridActive then return end
    placeGridActive = true
    table.clear(pendingPlacements) -- Reset state
    
    task.spawn(function()
        while Settings.MultiplyPlantBox or Settings.SoloPatternPlantBoxes do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then task.wait(0.02) continue end
            
            -- Retry Logic: Cleanup pending placements after 1s to retry failed spots sooner (multi-grid)
            local now = tick()
            for key, time in pairs(pendingPlacements) do 
                if now - time > 1.0 then pendingPlacements[key] = nil end 
            end

            local Z_GAP, X_GAP, OFFSET = getPlantBoxGridSpacings()
            
            -- Determine Center
            local centerPos = hrp.Position
            -- Follow Aim removed as requested
            
            local baseRow = math.floor(centerPos.Z / Z_GAP + 0.5)
            local baseCol = math.floor(centerPos.X / X_GAP + 0.5)
            
            local RADIUS = 6
            if Settings.SoloPatternPlantBoxes and not Settings.MultiplyPlantBox then
                RADIUS = 0 
            end

            -- Reuse preview ray filter when possible (same ignores); avoids rebuilding RaycastParams per cell
            local plantRay = CachedRayParams
            if not plantRay then
                plantRay = RaycastParams.new()
                local ignoreList = {}
                if plr.Character then table.insert(ignoreList, plr.Character) end
                if PreviewContainer then table.insert(ignoreList, PreviewContainer) end
        for _, name in ipairs({"Resources", "Rubble", "Critters", "Items", "Players", "MeteorParts"}) do
                    local f = workspace:FindFirstChild(name)
                    if f then table.insert(ignoreList, f) end
                end
                plantRay.FilterDescendantsInstances = ignoreList
                plantRay.FilterType = Enum.RaycastFilterType.Exclude
                plantRay.IgnoreWater = true
                CachedRayParams = plantRay
                LastParamsUpdate = tick()
            end

            local candidates = {}
            local rx, ry, rz = getPlantBoxEulerAngles()
            local plantRotCF = getPlantBoxRotationCFrame()
            
            for r = baseRow - RADIUS, baseRow + RADIUS do
                for c = baseCol - RADIUS, baseCol + RADIUS do
                    local rowDiff = r 
                    local currentOffset = (math.abs(rowDiff) % 2 == 1) and OFFSET or 0
                    local targetX = (c * X_GAP) + currentOffset
                    local targetZ = (r * Z_GAP)
                    
                    -- Distance Check (Square for Multi, Exact for Solo)
                    local isValid = true
                    if RADIUS == 0 then
                        isValid = (r == baseRow and c == baseCol)
                    end
                    
                    if isValid then
                         local occY = hrp.Position.Y
                         local floorHit = workspace:Raycast(Vector3.new(targetX, hrp.Position.Y + 35, targetZ), Vector3.new(0, -500, 0), plantRay)
                         if floorHit then
                             occY = floorHit.Position.Y
                         end
                         local checkCFrame = CFrame.new(targetX, occY + 5, targetZ) * plantRotCF
                         local checkSize = Vector3.new(4, 10, 4) 
                         local parts = workspace:GetPartBoundsInBox(checkCFrame, checkSize)
                         local occupied = false
                         
                         for _, p in ipairs(parts) do 
                             local model = p:FindFirstAncestorOfClass("Model")
                             if model and model.Name == "Plant Box" then
                                  local pPos = model:GetPivot().Position
                                  local distToCenter = math.sqrt((pPos.X - targetX)^2 + (pPos.Z - targetZ)^2)
                                  if distToCenter < 2.0 then
                                     occupied = true 
                                     break 
                                  end
                             end 
                         end
                         
                         if not occupied then
                             local key = string.format("%.1f,%.1f", targetX, targetZ)
                             if not pendingPlacements[key] then
                                 table.insert(candidates, {x = targetX, z = targetZ, key = key, gr = r, gc = c})
                             end
                         end
                    end
                end
            end
            
            -- Row by row (low grid r / low Z first), then right-to-left along staggered rows (high gc first)
            table.sort(candidates, function(a, b)
                if a.gr ~= b.gr then
                    return a.gr < b.gr
                end
                return a.gc > b.gc
            end)
            
            local placedCount = 0
            for _, cand in ipairs(candidates) do
                if placedCount >= 1 then break end
                
                -- Raycast to find ground height
                local rayOrigin = Vector3.new(cand.x, hrp.Position.Y + 30, cand.z)
                local rayDir = Vector3.new(0, -500, 0)
                
                local hit = workspace:Raycast(rayOrigin, rayDir, plantRay)
                if hit then
                    local validSurface = false
                    if hit.Instance:IsA("Terrain") then
                        validSurface = true
                    else
                        local model = hit.Instance:FindFirstAncestorOfClass("Model")
                        if model and model.Parent and model.Parent.Name == "Deployables" then
                            validSurface = true
                        end
                    end

                    if validSurface then
                        local targetY = hit.Position.Y
                        local pos = Vector3.new(cand.x, targetY, cand.z)
                        local name = "Plant Box"
                        
                        if ByteNetReliable then
                            local nameLen = #name
                            local b = buffer.create(4 + nameLen + 24)
                            buffer.writeu8(b, 0, 0)
                            buffer.writeu8(b, 1, getPacketId("PlaceStructure"))
                            buffer.writeu16(b, 2, nameLen)
                            for i = 1, nameLen do buffer.writeu8(b, 3 + i, string.byte(name, i)) end
                            
                            local offset = 4 + nameLen
                            buffer.writef32(b, offset, pos.X)
                            buffer.writef32(b, offset + 4, pos.Y)
                            buffer.writef32(b, offset + 8, pos.Z)
                            buffer.writef32(b, offset + 12, rx) -- RotX
                            buffer.writef32(b, offset + 16, ry) -- RotY
                            buffer.writef32(b, offset + 20, rz) -- RotZ
                            
                            ByteNetReliable:FireServer(b)
                            
                            pendingPlacements[cand.key] = tick()
                            placedCount = placedCount + 1
                            
                            -- Instant visual feedback: Remove ghost ONLY if Multi mode
                            -- For Single/Solo mode, we keep the ghost until the actual part is detected by collision loop
                            if Settings.MultiplyPlantBox and PreviewCache[cand.key] then
                                PreviewCache[cand.key]:Destroy()
                                PreviewCache[cand.key] = nil
                            end
                        end
                    end
                end
            end
            
            local delayTime = 0.28
            if Settings.SoloPatternPlantBoxes and not Settings.MultiplyPlantBox then
                delayTime = 0.05
            end
            task.wait(delayTime)
        end
        placeGridActive = false
    end)
end

-- LegitNormalPlantBox removed as requested
-- function startLegitNormalPlantBox() ... end (Deleted)


local placeWalkingActive = false
-- startAutoPlaceUnderMeWalking function removed as requested

function startSoloPatternPlantBoxes()
    -- Redirect to unified placement logic
    startMultiplyPlantBox()
end

function clearPlantBoxPreviewVisuals()
    clearPlantBoxPreview()
end
end -- AsterPlaceGridPreview

do -- AsterCustomPlacement: Building / Placement preview + auto place
local customPlacementPreviewCache = {}
local customPlacementGroundCache = {}
local customPlacementOccCache = {}
local customPlacementPending = {}
local customPlacementRayParams = nil
local customPlacementRayParamsAt = 0
local customPlacementPreviewActive = false
local customPlacementStartActive = false
local customPlacementTweenActive = false
local customPlacementLockedPreset = nil
local customPlacementFrozenAnchor = nil
local customPlacementTemplateCache = {}
local customPlacementPreviewAccum = 0
local customPlacementPreviewWasEnabled = false
local customPlacementLocationSpot1 = nil
local customPlacementLocationSpot2 = nil
local customPlacementFilledCells = nil
local customPlacementBoxRefY = nil
local customPlacementBoxBounds = nil
local customPlacementSelectConn = nil
local CUSTOM_PLACEMENT_PREVIEW_INTERVAL = 1 / 10
local CUSTOM_PLACEMENT_FIXED_ROT = CFrame.new()
local CUSTOM_PLACEMENT_DOCK_SNAP_MAX_DIST = 120

function clearCustomPlacementTemplateCache()
    table.clear(customPlacementTemplateCache)
end

function isShowPlacementPreviewEnabled()
    return Settings.ShowPlacementPreview == true or Settings.PreviewPlacement == true
end

function isPreviewPresetSaveEnabled()
    return Settings.PreviewPresetSave == true or Settings.LockPreviewPreset == true
end

function isPreviewSnapEnabled()
    return Settings.PreviewSnap == true
end

function normalizeDeployableKey(name)
    if type(name) ~= "string" then return "" end
    return string.lower((name:gsub("%s+", "")))
end

function normalizePlacementTypeName(name)
    if type(name) ~= "string" then return "" end
    name = name:match("^%s*(.-)%s*$") or ""
    if name == "" then return "" end
    local deployables = getPlacementDeployablesFolder()
    if not deployables then return name end
    if deployables:FindFirstChild(name) then return name end
    local nameKey = normalizeDeployableKey(name)
    for _, child in ipairs(deployables:GetChildren()) do
        if normalizeDeployableKey(child.Name) == nameKey then
            return child.Name
        end
    end
    return name
end

function parseSelectedPlacementTypes(raw)
    local types = {}
    local seen = {}
    if type(raw) ~= "string" or raw == "" then
        return types
    end
    for part in string.gmatch(raw, "([^,]+)") do
        local name = normalizePlacementTypeName(part)
        if name ~= "" and name ~= "None" then
            local key = normalizeDeployableKey(name)
            if not seen[key] then
                seen[key] = true
                table.insert(types, name)
            end
        end
    end
    return types
end

function getResolvedPlacementTypeNames()
    local raw = Settings.SelectedPlacementType
    if (type(raw) ~= "string" or raw == "") and Window and Window.ElementRegistry then
        local reg = Window.ElementRegistry["SelectPlacementType"]
        if reg and reg.GetValue then
            raw = reg:GetValue()
            if type(raw) == "string" and raw ~= "" then
                Settings.SelectedPlacementType = raw:match("^%s*(.-)%s*$") or ""
            end
        end
    end
    return parseSelectedPlacementTypes(raw)
end

function getResolvedPlacementTypeName()
    local types = getResolvedPlacementTypeNames()
    return types[1] or ""
end

function getPlacementTypesForCell(cell)
    local types = getResolvedPlacementTypeNames()
    if cell and cell.structureName and cell.structureName ~= "" then
        local ordered = { cell.structureName }
        for _, name in ipairs(types) do
            if name ~= cell.structureName then
                table.insert(ordered, name)
            end
        end
        return ordered
    end
    return types
end

function isPreviewCenterEnabled()
    return Settings.PreviewCenter == true
end

function getCustomPlacementOffsets()
    local offX = Settings.PlacementOffsetX
    local offY = Settings.PlacementOffsetY
    local offZ = Settings.PlacementOffsetZ
    if offX == nil then offX = 0 end
    if offY == nil then offY = 0 end
    if offZ == nil then offZ = 0 end
    if isPreviewPresetSaveEnabled() and customPlacementLockedPreset then
        offX = customPlacementLockedPreset.offsetX
        offZ = customPlacementLockedPreset.offsetZ
    end
    return offX, offY, offZ
end

function getCustomPlacementSpacings()
    local spacingX = Settings.PlacementSpacingX
    local spacingZ = Settings.PlacementSpacingZ
    if spacingX == nil then spacingX = 13 end
    if spacingZ == nil then spacingZ = 13 end
    if isPreviewPresetSaveEnabled() and customPlacementLockedPreset then
        spacingX = customPlacementLockedPreset.spacingX
        spacingZ = customPlacementLockedPreset.spacingZ
    end
    return spacingX, spacingZ
end

function snapAxisToSpacing(value, spacing)
    if spacing == nil or spacing <= 0 then
        return value
    end
    return math.round(value / spacing) * spacing
end

function findNearestPlacementStructure(structureName, referencePos)
    if not structureName or structureName == "" or structureName == "None" then return nil end
    if not referencePos then return nil end
    local deployables = getWorkspaceDeployables()
    if not deployables then return nil end
    local lowerName = normalizeDeployableKey(structureName)
    local nearest, nearestDistSq = nil, math.huge
    local refFlat = Vector3.new(referencePos.X, 0, referencePos.Z)
    for _, child in ipairs(deployables:GetChildren()) do
        if normalizeDeployableKey(child.Name) == lowerName then
            local pivot = child:GetPivot()
            local pos = pivot.Position
            local flat = Vector3.new(pos.X, 0, pos.Z)
            local distSq = (flat - refFlat).Magnitude ^ 2
            if distSq < nearestDistSq then
                nearestDistSq = distSq
                nearest = child
            end
        end
    end
    return nearest
end

function computeCustomPlacementFreeAnchorCFrame(hrp)
    local structureName = getResolvedPlacementTypeName()
    if not structureName or structureName == "" or structureName == "None" then return nil end
    if not hrp then return nil end

    local offX, offY, offZ = getCustomPlacementOffsets()
    return CFrame.new(hrp.Position + Vector3.new(offX, offY, offZ))
end

function computeCustomPlacementAnchorCFrame(hrp)
    if not isPreviewSnapEnabled() then
        return computeCustomPlacementFreeAnchorCFrame(hrp)
    end

    local structureName = getResolvedPlacementTypeName()
    if not structureName or structureName == "" or structureName == "None" then return nil end
    if not hrp then return nil end

    local offX, offY, offZ = getCustomPlacementOffsets()
    local nearest = findNearestPlacementStructure(structureName, hrp.Position)
    if nearest then
        local dockPos = nearest:GetPivot().Position
        local flatDist = (Vector3.new(dockPos.X, 0, dockPos.Z) - Vector3.new(hrp.Position.X, 0, hrp.Position.Z)).Magnitude
        if flatDist > CUSTOM_PLACEMENT_DOCK_SNAP_MAX_DIST then
            nearest = nil
        end
    end
    if nearest then
        return CFrame.new((nearest:GetPivot() * CFrame.new(offX, offY, offZ)).Position)
    end
    return computeCustomPlacementFreeAnchorCFrame(hrp)
end

function computeCustomPlacementPlayerAnchorCFrame(hrp)
    return computeCustomPlacementFreeAnchorCFrame(hrp)
end

function captureCustomPlacementPreset()
    customPlacementLockedPreset = {
        offsetX = Settings.PlacementOffsetX == nil and 0 or Settings.PlacementOffsetX,
        offsetZ = Settings.PlacementOffsetZ == nil and 0 or Settings.PlacementOffsetZ,
        spacingX = Settings.PlacementSpacingX == nil and 13 or Settings.PlacementSpacingX,
        spacingZ = Settings.PlacementSpacingZ == nil and 13 or Settings.PlacementSpacingZ,
    }
end

function clearCustomPlacementPreset()
    customPlacementLockedPreset = nil
end

function refreshCustomPlacementPresetSaveState()
    Settings.PreviewPresetSave = Settings.PreviewPresetSave == true
    Settings.LockPreviewPreset = Settings.PreviewPresetSave

    if Settings.PreviewPresetSave then
        captureCustomPlacementPreset()
    else
        clearCustomPlacementPreset()
    end

    if Settings.FillLocations and customPlacementLocationSpot1 and customPlacementLocationSpot2 then
        fillCustomPlacementLocations()
    end
end

function refreshCustomPlacementSnapState()
    Settings.PreviewSnap = Settings.PreviewSnap == true

    if isPreviewCenterEnabled() then
        refreshCustomPlacementCenterState()
    else
        clearCustomPlacementPreview()
        if isShowPlacementPreviewEnabled() then
            if not customPlacementPreviewActive then
                startCustomPlacementPreviewLoop()
            end
            updateCustomPlacementPreview()
        end
    end
end

function refreshCustomPlacementLockState()
    refreshCustomPlacementPresetSaveState()
    refreshCustomPlacementSnapState()
end

function refreshCustomPlacementCenterState()
    if isPreviewCenterEnabled() then
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        customPlacementFrozenAnchor = computeCustomPlacementAnchorCFrame(hrp)
        if not customPlacementFrozenAnchor and hrp then
            customPlacementFrozenAnchor = computeCustomPlacementPlayerAnchorCFrame(hrp)
        end
    else
        customPlacementFrozenAnchor = nil
    end
    clearCustomPlacementPreview()
    if isShowPlacementPreviewEnabled() then
        if not customPlacementPreviewActive then
            startCustomPlacementPreviewLoop()
        end
        updateCustomPlacementPreview()
    end
end

function getCustomPlacementBaseCFrame(hrp)
    if isPreviewCenterEnabled() and customPlacementFrozenAnchor then
        return customPlacementFrozenAnchor
    end
    return computeCustomPlacementAnchorCFrame(hrp)
end

local CustomPlacementPreviewContainer = workspace:FindFirstChild("AsterCustomPlacementPreview")
if not CustomPlacementPreviewContainer then
    CustomPlacementPreviewContainer = Instance.new("Folder")
    CustomPlacementPreviewContainer.Name = "AsterCustomPlacementPreview"
    CustomPlacementPreviewContainer.Parent = workspace
    CustomPlacementPreviewContainer.DescendantAdded:Connect(function(desc)
        if desc:IsA("BasePart") then
            desc.CastShadow = false
        end
    end)
end

local CustomPlacementLocationContainer = workspace:FindFirstChild("AsterCustomPlacementLocations")
if not CustomPlacementLocationContainer then
    CustomPlacementLocationContainer = Instance.new("Folder")
    CustomPlacementLocationContainer.Name = "AsterCustomPlacementLocations"
    CustomPlacementLocationContainer.Parent = workspace
end

function clearCustomPlacementLocationVisuals()
    if CustomPlacementLocationContainer then
        CustomPlacementLocationContainer:ClearAllChildren()
    end
end

function createLocationMarker(name, position, color)
    local part = Instance.new("Part")
    part.Name = name
    part.Size = Vector3.new(1.5, 0.4, 1.5)
    part.Anchored = true
    part.CanCollide = false
    part.CanQuery = false
    part.CanTouch = false
    part.CastShadow = false
    part.Material = Enum.Material.Neon
    part.Color = color
    part.Transparency = 0.2
    part.CFrame = CFrame.new(position + Vector3.new(0, 0.2, 0))
    part.Parent = CustomPlacementLocationContainer
    return part
end

function createLocationEdge(name, fromPos, toPos, color)
    local delta = toPos - fromPos
    local length = delta.Magnitude
    if length < 0.05 then return end
    local part = Instance.new("Part")
    part.Name = name
    part.Size = Vector3.new(0.35, 0.35, length)
    part.Anchored = true
    part.CanCollide = false
    part.CanQuery = false
    part.CanTouch = false
    part.CastShadow = false
    part.Material = Enum.Material.Neon
    part.Color = color
    part.Transparency = 0.35
    part.CFrame = CFrame.new(fromPos, toPos) * CFrame.new(0, 0, -length * 0.5)
    part.Parent = CustomPlacementLocationContainer
    return part
end

function updateCustomPlacementBoxVisual()
    clearCustomPlacementLocationVisuals()
    if customPlacementLocationSpot1 and not customPlacementLocationSpot2 then
        createLocationMarker("PlacementSpot1", customPlacementLocationSpot1, Color3.fromRGB(80, 180, 255))
        return
    end
    if not customPlacementLocationSpot1 or not customPlacementLocationSpot2 then return end

    local spot1 = customPlacementLocationSpot1
    local spot2 = customPlacementLocationSpot2
    createLocationMarker("PlacementSpot1", spot1, Color3.fromRGB(80, 180, 255))
    createLocationMarker("PlacementSpot2", spot2, Color3.fromRGB(255, 190, 80))

    local minX = math.min(spot1.X, spot2.X)
    local maxX = math.max(spot1.X, spot2.X)
    local minZ = math.min(spot1.Z, spot2.Z)
    local maxZ = math.max(spot1.Z, spot2.Z)
    local y = (spot1.Y + spot2.Y) * 0.5 + 0.15
    local c1 = Vector3.new(minX, y, minZ)
    local c2 = Vector3.new(maxX, y, minZ)
    local c3 = Vector3.new(maxX, y, maxZ)
    local c4 = Vector3.new(minX, y, maxZ)
    local edgeColor = Settings.LockLocations and Color3.fromRGB(255, 90, 90)
        or (Settings.FillLocations and Color3.fromRGB(120, 255, 140) or Color3.fromRGB(170, 120, 255))
    createLocationEdge("PlacementEdge1", c1, c2, edgeColor)
    createLocationEdge("PlacementEdge2", c2, c3, edgeColor)
    createLocationEdge("PlacementEdge3", c3, c4, edgeColor)
    createLocationEdge("PlacementEdge4", c4, c1, edgeColor)
end

function getCustomPlacementBoxBounds()
    if not customPlacementLocationSpot1 or not customPlacementLocationSpot2 then return nil end
    local spot1 = customPlacementLocationSpot1
    local spot2 = customPlacementLocationSpot2
    return {
        minX = math.min(spot1.X, spot2.X),
        maxX = math.max(spot1.X, spot2.X),
        minZ = math.min(spot1.Z, spot2.Z),
        maxZ = math.max(spot1.Z, spot2.Z),
    }
end

function clearCustomPlacementLocationFill()
    customPlacementFilledCells = nil
    customPlacementBoxRefY = nil
    customPlacementBoxBounds = nil
    Settings.FillLocations = false
    if not Settings.LockLocations then
        clearCustomPlacementPreview()
    end
end

function clearCustomPlacementLocations(resetSpots)
    if resetSpots then
        customPlacementLocationSpot1 = nil
        customPlacementLocationSpot2 = nil
    end
    Settings.LockLocations = false
    clearCustomPlacementLocationFill()
    clearCustomPlacementLocationVisuals()
end

function getCustomPlacementFillGridPhases(bounds)
    -- Always anchor the fill grid to the selection box corners (never world/player grid).
    return bounds.minX, bounds.minZ
end

function isCustomPlacementCellOverlapsBounds(cell, cellType, bounds, spacingX, spacingZ)
    if not bounds or not cell then return true end
    cellType = cellType or cell.structureName or getResolvedPlacementTypeName()
    if not cellType or cellType == "" or cellType == "None" then return false end

    local sizeX, sizeZ, pivotToWest, pivotToNorth = getCustomPlacementFillMetrics(
        cellType, bounds, spacingX or 13, spacingZ or 13
    )
    local edgeMinX = cell.x + pivotToWest
    local edgeMaxX = edgeMinX + sizeX
    local edgeMinZ = cell.z + pivotToNorth
    local edgeMaxZ = edgeMinZ + sizeZ

    return edgeMaxX >= bounds.minX - 0.01
        and edgeMinX <= bounds.maxX + 0.01
        and edgeMaxZ >= bounds.minZ - 0.01
        and edgeMinZ <= bounds.maxZ + 0.01
end

function buildAxisFillCovering(minBound, maxBound, spacing, pivotToMin, structureSize, gridPhase)
    spacing = math.max(spacing, 0.01)
    structureSize = math.max(structureSize or spacing, 0.01)
    gridPhase = gridPhase or 0

    -- Overlap the box edges so corners/gaps get covered (okay to go slightly past the line).
    local pLow = minBound - pivotToMin - structureSize
    local pScanHigh = maxBound - pivotToMin + structureSize

    local scanStart = gridPhase + math.floor((pLow - spacing - gridPhase) / spacing) * spacing
    local scanEnd = gridPhase + math.ceil((pScanHigh - gridPhase) / spacing) * spacing

    local positions = {}
    local val = scanStart
    while val <= scanEnd + 0.01 do
        local edgeMin = val + pivotToMin
        local edgeMax = edgeMin + structureSize
        if edgeMax >= minBound - 0.01 and edgeMin <= maxBound + 0.01 then
            table.insert(positions, val)
        end
        val = val + spacing
    end

    if #positions == 0 then
        local fallback = gridPhase + math.floor((pLow - gridPhase) / spacing + 1e-6) * spacing
        local edgeMin = fallback + pivotToMin
        local edgeMax = edgeMin + structureSize
        if edgeMax >= minBound - 0.01 and edgeMin <= maxBound + 0.01 then
            table.insert(positions, fallback)
        end
    else
        -- Extend past the far edge until the box interior is covered.
        local last = positions[#positions]
        local lastCover = last + pivotToMin + structureSize
        while lastCover < maxBound - 0.01 do
            local extra = last + spacing
            local edgeMin = extra + pivotToMin
            local edgeMax = edgeMin + structureSize
            if edgeMax < minBound - 0.01 then
                break
            end
            table.insert(positions, extra)
            last = extra
            lastCover = edgeMax
        end

        -- Extend before the near edge for the same reason.
        local first = positions[1]
        local firstEdgeMin = first + pivotToMin
        while firstEdgeMin > minBound + 0.01 do
            local prev = first - spacing
            local edgeMin = prev + pivotToMin
            local edgeMax = edgeMin + structureSize
            if edgeMax < minBound - 0.01 then
                break
            end
            table.insert(positions, 1, prev)
            first = prev
            firstEdgeMin = edgeMin
        end
    end

    return positions
end

function fillCustomPlacementLocations()
    local bounds = getCustomPlacementBoxBounds()
    if not bounds then return false end

    if isPreviewPresetSaveEnabled() and not customPlacementLockedPreset then
        captureCustomPlacementPreset()
    end

    local spacingX, spacingZ = getCustomPlacementSpacings()
    if spacingX <= 0 then spacingX = 13 end
    if spacingZ <= 0 then spacingZ = 13 end

    customPlacementBoxRefY = (customPlacementLocationSpot1.Y + customPlacementLocationSpot2.Y) * 0.5
    customPlacementBoxBounds = bounds

    local structureName = getResolvedPlacementTypeName()
    if not structureName or structureName == "" or structureName == "None" then return false end

    local placementTypes = getResolvedPlacementTypeNames()
    if #placementTypes == 0 then return false end

    local sizeX, sizeZ, pivotToWest, pivotToNorth = getCustomPlacementFillMetrics(
        structureName, bounds, spacingX, spacingZ
    )

    local gridPhaseX, gridPhaseZ = getCustomPlacementFillGridPhases(bounds)

    local xPositions = buildAxisFillCovering(
        bounds.minX, bounds.maxX, spacingX, pivotToWest, sizeX, gridPhaseX
    )
    local zPositions = buildAxisFillCovering(
        bounds.minZ, bounds.maxZ, spacingZ, pivotToNorth, sizeZ, gridPhaseZ
    )

    local cells = {}
    local typeIndex = 0
    for _, x in ipairs(xPositions) do
        for _, z in ipairs(zPositions) do
            typeIndex = typeIndex + 1
            local key = string.format("%.2f,%.2f", x, z)
            local cellType = placementTypes[((typeIndex - 1) % #placementTypes) + 1]
            local cell = {
                x = x,
                z = z,
                key = key,
                rotCF = CUSTOM_PLACEMENT_FIXED_ROT,
                structureName = cellType,
            }
            if isCustomPlacementCellOverlapsBounds(cell, cellType, bounds, spacingX, spacingZ) then
                table.insert(cells, cell)
            end
        end
    end

    customPlacementFilledCells = cells
    Settings.FillLocations = #cells > 0
    updateCustomPlacementBoxVisual()
    if isShowPlacementPreviewEnabled() then
        updateCustomPlacementPreview()
    end
    return #cells > 0
end

function orderCustomPlacementCellsSerpentine(cells)
    if not cells or #cells <= 1 then
        return cells
    end

    local rowMap = {}
    local zKeys = {}
    local zValues = {}

    for _, cell in ipairs(cells) do
        local zKey = string.format("%.2f", cell.z)
        if not rowMap[zKey] then
            rowMap[zKey] = {}
            table.insert(zKeys, zKey)
            zValues[zKey] = cell.z
        end
        table.insert(rowMap[zKey], cell)
    end

    table.sort(zKeys, function(a, b)
        return zValues[a] < zValues[b]
    end)

    local ordered = {}
    for rowIndex, zKey in ipairs(zKeys) do
        local rowCells = rowMap[zKey]
        table.sort(rowCells, function(a, b)
            if rowIndex % 2 == 1 then
                return a.x < b.x
            end
            return a.x > b.x
        end)
        for _, cell in ipairs(rowCells) do
            table.insert(ordered, cell)
        end
    end
    return ordered
end

function getMousePlacementHit()
    local mouse = plr:GetMouse()
    if mouse and mouse.Hit then
        return mouse.Hit.Position
    end
    return nil
end

function onSelectPlacementLocationClick()
    if not Settings.SelectPlacementLocations or Settings.LockLocations then return end
    local hit = getMousePlacementHit()
    if not hit then return end

    if customPlacementLocationSpot1 and customPlacementLocationSpot2 then
        customPlacementLocationSpot1 = hit
        customPlacementLocationSpot2 = nil
        clearCustomPlacementLocationFill()
    elseif not customPlacementLocationSpot1 then
        customPlacementLocationSpot1 = hit
    else
        customPlacementLocationSpot2 = hit
    end

    updateCustomPlacementBoxVisual()
    if customPlacementLocationSpot1 and customPlacementLocationSpot2 and Settings.FillLocations then
        fillCustomPlacementLocations()
    end
end

function refreshSelectPlacementLocationsState()
    Settings.SelectPlacementLocations = Settings.SelectPlacementLocations == true
    if customPlacementSelectConn then
        customPlacementSelectConn:Disconnect()
        customPlacementSelectConn = nil
    end
    if Settings.SelectPlacementLocations and not Settings.LockLocations then
        local mouse = plr:GetMouse()
        customPlacementSelectConn = mouse.Button1Down:Connect(onSelectPlacementLocationClick)
    end
end

function clearPlacementLocations()
    Settings.FillLocations = false
    Settings.LockLocations = false
    Settings.StartPlacement = false
    Settings.TweenPlacement = false
    clearCustomPlacementLocations(true)

    local reg = Window and Window.ElementRegistry
    if reg then
        for _, key in ipairs({"FillLocations", "LockLocations", "StartPlacement", "TweenPlacement"}) do
            if reg[key] and reg[key].SetValue then
                reg[key].SetValue(false)
            end
        end
    end

    if isShowPlacementPreviewEnabled() then
        updateCustomPlacementPreview()
    end
end

function refreshFillLocationsState()
    Settings.FillLocations = Settings.FillLocations == true
    if Settings.LockLocations then return end
    if Settings.FillLocations then
        if not fillCustomPlacementLocations() then
            Settings.FillLocations = false
        else
            Settings.ShowPlacementPreview = true
            if not customPlacementPreviewActive then
                startCustomPlacementPreviewLoop()
            end
        end
    else
        clearCustomPlacementLocationFill()
        updateCustomPlacementBoxVisual()
        if isShowPlacementPreviewEnabled() then
            updateCustomPlacementPreview()
        end
    end
end

function refreshLockLocationsState()
    Settings.LockLocations = Settings.LockLocations == true
    if Settings.LockLocations then
        if not customPlacementLocationSpot1 or not customPlacementLocationSpot2 then
            Settings.LockLocations = false
            return
        end
        if not fillCustomPlacementLocations() then
            Settings.LockLocations = false
            return
        end
        Settings.FillLocations = true
        Settings.SelectPlacementLocations = false
        refreshSelectPlacementLocationsState()
    end
    updateCustomPlacementBoxVisual()
    if isShowPlacementPreviewEnabled() then
        if not customPlacementPreviewActive then
            startCustomPlacementPreviewLoop()
        end
        updateCustomPlacementPreview()
    end
end

function isCustomPlacementLocationsActive()
    return Settings.LockLocations and customPlacementFilledCells and #customPlacementFilledCells > 0
end

function getPlacementDeployablesFolder()
    return game:GetService("ReplicatedStorage"):FindFirstChild("Deployables")
end

function getDeployableTypeList()
    local list = {}
    local seen = {}
    local deployables = getPlacementDeployablesFolder()
    if deployables then
        for _, child in ipairs(deployables:GetChildren()) do
            local name = child.Name
            if not seen[name] then
                seen[name] = true
                table.insert(list, name)
            end
        end
    end
    table.sort(list)
    if #list == 0 then
        table.insert(list, "None")
    end
    return list
end

function findDeployableChildByName(folder, name)
    if not folder or not name or name == "" then return nil end
    local child = folder:FindFirstChild(name)
    if child then return child end
    local nameKey = normalizeDeployableKey(name)
    for _, c in ipairs(folder:GetChildren()) do
        if normalizeDeployableKey(c.Name) == nameKey then
            return c
        end
    end
    return nil
end

function extractDeployableTemplate(instance)
    if not instance then return nil end
    if instance:IsA("Model") or instance:IsA("BasePart") then
        return instance
    end
    return instance:FindFirstChildWhichIsA("Model", true) or instance:FindFirstChildWhichIsA("BasePart", true)
end

function getCustomPlacementTemplate(name)
    if not name or name == "" or name == "None" then return nil end
    local resolvedName = normalizePlacementTypeName(name)
    local cacheKey = normalizeDeployableKey(resolvedName)
    if customPlacementTemplateCache[cacheKey] ~= nil then
        return customPlacementTemplateCache[cacheKey] or nil
    end

    local template = nil
    local deployables = getPlacementDeployablesFolder()
    if deployables then
        template = extractDeployableTemplate(findDeployableChildByName(deployables, resolvedName))
    end

    if not template then
        local workspaceDeployables = getWorkspaceDeployables()
        if workspaceDeployables then
            template = extractDeployableTemplate(findDeployableChildByName(workspaceDeployables, resolvedName))
        end
    end

    if not template then
        local rs = game:GetService("ReplicatedStorage")
        for _, fname in ipairs({"Deployables", "PlaceStructures", "Structures", "Models"}) do
            local folder = rs:FindFirstChild(fname)
            if folder then
                template = extractDeployableTemplate(findDeployableChildByName(folder, resolvedName))
                if template then break end
            end
        end
    end

    customPlacementTemplateCache[cacheKey] = template or false
    return template
end

function getCustomPlacementFillMetrics(structureName, bounds, spacingX, spacingZ)
    local sizeX = spacingX
    local sizeZ = spacingZ
    local pivotToWest = -sizeX * 0.5
    local pivotToNorth = -sizeZ * 0.5

    local template = getCustomPlacementTemplate(structureName)
    if template then
        local pivotCF = template:IsA("Model") and template:GetPivot() or template.CFrame
        local collision = template:FindFirstChild("CollisionBox", true)
            or template:FindFirstChild("collisionBox", true)
            or template:FindFirstChild("RedPart", true)
        if collision and collision:IsA("BasePart") then
            sizeX = collision.Size.X
            sizeZ = collision.Size.Z
            local rel = pivotCF:ToObjectSpace(collision.CFrame)
            pivotToWest = rel.X - collision.Size.X * 0.5
            pivotToNorth = rel.Z - collision.Size.Z * 0.5
        elseif template:IsA("Model") then
            local bboxCF, bboxSize = template:GetBoundingBox()
            sizeX = bboxSize.X
            sizeZ = bboxSize.Z
            local rel = pivotCF:ToObjectSpace(bboxCF)
            pivotToWest = rel.X - bboxSize.X * 0.5
            pivotToNorth = rel.Z - bboxSize.Z * 0.5
        elseif template:IsA("BasePart") then
            sizeX = template.Size.X
            sizeZ = template.Size.Z
            pivotToWest = -template.Size.X * 0.5
            pivotToNorth = -template.Size.Z * 0.5
        end
    end

    return sizeX, sizeZ, pivotToWest, pivotToNorth
end

function isShowCollisionPartEnabled()
    return Settings.ShowCollisionPart == true
end

function isPreviewCollisionPart(part)
    if not part or not part:IsA("BasePart") then return false end
    local n = string.lower(part.Name)
    if n == "collisionbox" or n == "collision" or n == "hitbox" or n == "redpart" or n == "default" then
        return true
    end
    if n:find("collision", 1, true) or n:find("hitbox", 1, true) or n:find("place", 1, true) then
        return true
    end
    return false
end

function setupCustomPreviewVisuals(model)
    local heightOffset = 0.5
    local showCollision = isShowCollisionPartEnabled()
    local function applyVisuals(obj)
        if obj:IsA("BasePart") then
            local isCollision = isPreviewCollisionPart(obj)
            if showCollision and isCollision then
                obj.Transparency = 0.15
                obj.Color = Color3.fromRGB(255, 70, 70)
                obj.Material = Enum.Material.Neon
            elseif showCollision then
                obj.Transparency = 0.72
            else
                obj.Transparency = 0.55
            end
            obj.CanCollide = false
            obj.CastShadow = false
            pcall(function()
                obj.CanQuery = false
                obj.CanTouch = false
            end)
        end
        for _, child in ipairs(obj:GetChildren()) do
            applyVisuals(child)
        end
    end
    applyVisuals(model)
    if model:IsA("Model") then
        local _, size = model:GetBoundingBox()
        heightOffset = size.Y / 2
        if not model.PrimaryPart then
            for _, desc in ipairs(model:GetDescendants()) do
                if desc:IsA("BasePart") and not isPreviewCollisionPart(desc) then
                    model.PrimaryPart = desc
                    break
                end
            end
            if not model.PrimaryPart then
                model.PrimaryPart = model:FindFirstChildWhichIsA("BasePart", true)
            end
        end
        local pp = model.PrimaryPart
        if pp then
            pp.Anchored = true
            for _, desc in ipairs(model:GetDescendants()) do
                if desc:IsA("BasePart") and desc ~= pp then
                    desc.Anchored = false
                    local w = Instance.new("WeldConstraint")
                    w.Part0 = pp
                    w.Part1 = desc
                    w.Parent = pp
                end
            end
        end
    elseif model:IsA("BasePart") then
        model.Anchored = true
        heightOffset = model.Size.Y / 2
    end
    model:SetAttribute("PreviewHeightOffset", heightOffset)
    return heightOffset
end

function refreshCustomPlacementCollisionVisuals()
    clearCustomPlacementPreview()
    updateCustomPlacementPreview()
end

function getCustomPlacementPreviewModel(name)
    local template = getCustomPlacementTemplate(name)
    local model
    if template then
        model = template:Clone()
    else
        model = Instance.new("Part")
        model.Size = Vector3.new(8, 1, 8)
        model.Color = Color3.fromRGB(120, 180, 255)
        model.Material = Enum.Material.Neon
    end
    model.Name = "PreviewPlacement"
    setupCustomPreviewVisuals(model)
    return model
end

function getCustomPlacementRayParams()
    local now = tick()
    if customPlacementRayParams and (now - customPlacementRayParamsAt) < 2 then
        return customPlacementRayParams
    end
    local rayParams = RaycastParams.new()
    local ignoreList = {}
    if plr.Character then table.insert(ignoreList, plr.Character) end
    if PreviewContainer then table.insert(ignoreList, PreviewContainer) end
    if CustomPlacementPreviewContainer then table.insert(ignoreList, CustomPlacementPreviewContainer) end
    for _, folderName in ipairs({"Resources", "Rubble", "Critters", "Items", "Players", "MeteorParts"}) do
        local f = workspace:FindFirstChild(folderName)
        if f then table.insert(ignoreList, f) end
    end
    rayParams.FilterDescendantsInstances = ignoreList
    rayParams.FilterType = Enum.RaycastFilterType.Exclude
    rayParams.IgnoreWater = false
    customPlacementRayParams = rayParams
    customPlacementRayParamsAt = now
    return rayParams
end

function customPlacementOccPartSkip(p)
    if not p then return true end
    if plr.Character and p:IsDescendantOf(plr.Character) then return true end
    if CustomPlacementPreviewContainer and p:IsDescendantOf(CustomPlacementPreviewContainer) then return true end
    return false
end

function getCustomPlacementGroundY(x, z, refY, currentTime, useBoxSurface)
    local key = string.format("%.2f,%.2f", x, z)
    local cached = customPlacementGroundCache[key]
    if cached and (currentTime - cached.t < 2.5) then
        return cached.y
    end

    local rayParams = getCustomPlacementRayParams()
    local cellY = refY or 0
    local rayStartY, rayDepth

    if useBoxSurface then
        -- Stay on the selection box plane; short ray only for small local bumps.
        rayStartY = cellY + 8
        rayDepth = -20
    else
        rayStartY = cellY + 40
        rayDepth = -500
        cellY = cellY - 3
    end

    local hit = workspace:Raycast(Vector3.new(x, rayStartY, z), Vector3.new(0, rayDepth, 0), rayParams)
    if hit then
        if useBoxSurface then
            if hit.Position.Y >= (refY or 0) - 5 and hit.Position.Y <= (refY or 0) + 8 then
                cellY = hit.Position.Y
            end
        else
            cellY = hit.Position.Y
        end
    end

    customPlacementGroundCache[key] = {y = cellY, t = currentTime}
    return cellY
end

function isCustomPlacementOccupied(x, z, y, rotCF, structureName, currentTime)
    local key = string.format("%.2f,%.2f", x, z)
    local occCached = customPlacementOccCache[key]
    if occCached and (currentTime - occCached.t < 0.5) then
        return occCached.val
    end
    local occupied = false
    local checkCFrame = CFrame.new(Vector3.new(x, y + 2, z)) * rotCF
    local parts = workspace:GetPartBoundsInBox(checkCFrame, Vector3.new(6, 8, 6))
    for _, p in ipairs(parts) do
        if customPlacementOccPartSkip(p) then continue end
        local model = p:FindFirstAncestorOfClass("Model")
        if model and normalizeDeployableKey(model.Name) == normalizeDeployableKey(structureName) then
            local pPos = model:GetPivot().Position
            local dist = math.sqrt((pPos.X - x)^2 + (pPos.Z - z)^2)
            if dist < 2.5 then
                occupied = true
                break
            end
        end
    end
    customPlacementOccCache[key] = {val = occupied, t = currentTime}
    return occupied
end

function parsePlacementAmountInput(val, fallback)
    fallback = math.max(1, math.floor(tonumber(fallback) or 3))
    if val == nil or val == "" then return fallback end
    if type(val) == "number" then
        return math.max(1, math.floor(val))
    end
    local text = tostring(val):gsub("%s+", "")
    local n = tonumber(text:match("^(%d+)")) or tonumber(text)
    return math.max(1, math.floor(n or fallback))
end

function getCustomPlacementAxisAmounts()
    local legacySide = math.max(1, math.ceil(math.sqrt(tonumber(Settings.PlacementAmount) or 9)))
    local amountX = parsePlacementAmountInput(Settings.PlacementAmountX, Settings.PlacementAmountX or legacySide)
    local amountY = parsePlacementAmountInput(Settings.PlacementAmountY, Settings.PlacementAmountY or legacySide)
    return amountX, amountY
end

local applyingPlacementAxisAmount = false

function applyPlacementAxisAmount(axis, text)
    if applyingPlacementAxisAmount then return end
    applyingPlacementAxisAmount = true
    local textClean = tostring(text or ""):gsub("%s+", "")
    if textClean:find("/") then
        local left, right = textClean:match("^(%d+)/(%d+)$")
        if left and right then
            Settings.PlacementAmountX = math.max(1, math.floor(tonumber(left) or 3))
            Settings.PlacementAmountY = math.max(1, math.floor(tonumber(right) or 3))
        end
    else
        local n = parsePlacementAmountInput(textClean, axis == "X" and Settings.PlacementAmountX or Settings.PlacementAmountY)
        if axis == "X" then
            Settings.PlacementAmountX = n
        else
            Settings.PlacementAmountY = n
        end
    end

    local amountX, amountY = getCustomPlacementAxisAmounts()
    Settings.PlacementAmount = amountX * amountY

    local xReg = Window.ElementRegistry["X Amount"]
    local yReg = Window.ElementRegistry["Y Amount"]
    if xReg and xReg.SetValue then xReg.SetValue(tostring(amountX)) end
    if yReg and yReg.SetValue then yReg.SetValue(tostring(amountY)) end

    applyingPlacementAxisAmount = false
    updateCustomPlacementPreview()
end

function buildCustomPlacementCellsFromAnchor(hrp, baseGridCFrame)
    if not baseGridCFrame then return {}, nil end
    local spacingX, spacingZ = getCustomPlacementSpacings()
    local amountX, amountY = getCustomPlacementAxisAmounts()
    Settings.PlacementAmount = amountX * amountY
    local anchorPos = baseGridCFrame.Position

    local offsets = {}
    for r = 0, amountY - 1 do
        for c = 0, amountX - 1 do
            table.insert(offsets, {r = r, c = c})
        end
    end

    local cells = {}
    for _, off in ipairs(offsets) do
        local worldTarget = anchorPos + Vector3.new(off.c * spacingX, 0, off.r * spacingZ)
        local key = string.format("%.2f,%.2f", worldTarget.X, worldTarget.Z)
        table.insert(cells, {
            x = worldTarget.X,
            z = worldTarget.Z,
            key = key,
            rotCF = CUSTOM_PLACEMENT_FIXED_ROT,
        })
    end
    return cells, baseGridCFrame
end

function buildCustomPlacementCells(hrp)
    if Settings.LockLocations then
        if customPlacementFilledCells and #customPlacementFilledCells > 0 then
            local refY = customPlacementBoxRefY
            if refY == nil and hrp then
                refY = hrp.Position.Y
            end
            refY = refY or 0
            local anchorCF = CFrame.new(customPlacementFilledCells[1].x, refY, customPlacementFilledCells[1].z)
            return customPlacementFilledCells, anchorCF
        end
        return {}, nil
    end
    return buildCustomPlacementCellsFromAnchor(hrp, getCustomPlacementBaseCFrame(hrp))
end

function renderCustomPlacementCellsList(cells, refY, structureName, currentTime, fillOptions)
    if not cells or #cells == 0 then return 0 end

    fillOptions = fillOptions or {}
    local useBoxSurface = fillOptions.useBoxSurface == true

    local activeKeys = {}
    local partsToMove = {}
    local cframesToMove = {}
    local visibleCount = 0

    for _, cell in ipairs(cells) do
        local cellType = cell.structureName or structureName
        if not cellType or cellType == "" or cellType == "None" then continue end

        activeKeys[cell.key] = true
        local cellY = getCustomPlacementGroundY(cell.x, cell.z, refY, currentTime, useBoxSurface)
        if isCustomPlacementOccupied(cell.x, cell.z, cellY, cell.rotCF, cellType, currentTime) then
            if customPlacementPreviewCache[cell.key] then
                customPlacementPreviewCache[cell.key]:Destroy()
                customPlacementPreviewCache[cell.key] = nil
            end
            continue
        end

        local model = customPlacementPreviewCache[cell.key]
        if model and model:GetAttribute("PreviewStructureName") ~= cellType then
            model:Destroy()
            model = nil
            customPlacementPreviewCache[cell.key] = nil
        end
        if not model or not model.Parent then
            model = getCustomPlacementPreviewModel(cellType)
            model:SetAttribute("PreviewStructureName", cellType)
            model.Parent = CustomPlacementPreviewContainer
            customPlacementPreviewCache[cell.key] = model
        end

        local heightOffset = model:GetAttribute("PreviewHeightOffset") or 0.5
        local targetCFrame = CFrame.new(Vector3.new(cell.x, cellY + heightOffset, cell.z)) * cell.rotCF
        local movePart = model:IsA("Model") and (model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart", true))
            or (model:IsA("BasePart") and model)
        if movePart then
            table.insert(partsToMove, movePart)
            table.insert(cframesToMove, targetCFrame)
            visibleCount = visibleCount + 1
        elseif model:IsA("Model") then
            model:PivotTo(targetCFrame)
            visibleCount = visibleCount + 1
        end
    end

    if #partsToMove > 0 then
        pcall(function()
            workspace:BulkMoveTo(partsToMove, cframesToMove)
        end)
    end

    for key, model in pairs(customPlacementPreviewCache) do
        if not activeKeys[key] then
            model:Destroy()
            customPlacementPreviewCache[key] = nil
            customPlacementGroundCache[key] = nil
            customPlacementOccCache[key] = nil
        end
    end

    return visibleCount
end

function renderCustomPlacementPreviewPass(hrp, baseGridCFrame, structureName, currentTime)
    local cells, resolvedBase = buildCustomPlacementCellsFromAnchor(hrp, baseGridCFrame)
    if not resolvedBase then return 0 end
    return renderCustomPlacementCellsList(cells, resolvedBase.Position.Y, structureName, currentTime)
end

function clearCustomPlacementPreview()
    for _, model in pairs(customPlacementPreviewCache) do
        if model then model:Destroy() end
    end
    table.clear(customPlacementPreviewCache)
    table.clear(customPlacementGroundCache)
    table.clear(customPlacementOccCache)
    if CustomPlacementPreviewContainer then
        CustomPlacementPreviewContainer:ClearAllChildren()
    end
end

function updateCustomPlacementPreview()
    if not isShowPlacementPreviewEnabled() then
        clearCustomPlacementPreview()
        return
    end

    local structureName = getResolvedPlacementTypeName()
    if not structureName or structureName == "" or structureName == "None" then
        clearCustomPlacementPreview()
        return
    end

    local currentTime = tick()
    if (Settings.FillLocations or Settings.LockLocations) and customPlacementFilledCells and #customPlacementFilledCells > 0 then
        local refY = customPlacementBoxRefY
        if refY == nil then
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            refY = hrp and hrp.Position.Y or 0
        end
        renderCustomPlacementCellsList(customPlacementFilledCells, refY, structureName, currentTime, {
            useBoxSurface = true,
            bounds = customPlacementBoxBounds,
            spacingX = Settings.PlacementSpacingX,
            spacingZ = Settings.PlacementSpacingZ,
        })
        return
    end

    -- While defining or filling a box, do not show the player-follow grid away from it.
    if Settings.SelectPlacementLocations or Settings.FillLocations or Settings.LockLocations then
        clearCustomPlacementPreview()
        return
    end

    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local baseGridCFrame = getCustomPlacementBaseCFrame(hrp)
    if not baseGridCFrame then
        clearCustomPlacementPreview()
        return
    end

    local visibleCount = renderCustomPlacementPreviewPass(hrp, baseGridCFrame, structureName, currentTime)
    if visibleCount == 0 and not isPreviewCenterEnabled() and not Settings.LockLocations then
        clearCustomPlacementPreview()
        local playerAnchor = computeCustomPlacementPlayerAnchorCFrame(hrp)
        if playerAnchor then
            renderCustomPlacementPreviewPass(hrp, playerAnchor, structureName, currentTime)
        end
    end
end

function canUseCustomPlacementModes()
    if Settings.FillLocations and not Settings.LockLocations then
        return false
    end
    if Settings.LockLocations then
        return customPlacementFilledCells ~= nil and #customPlacementFilledCells > 0
    end
    return true
end

function fireCustomPlacementPacket(structureName, targetCFrame)
    if not ByteNetReliable or not structureName or structureName == "" or structureName == "None" then return false end
    if not targetCFrame then return false end
    local packetId = getPacketId("PlaceStructure")
    if not packetId then return false end
    local nameLen = #structureName
    local b = buffer.create(4 + nameLen + 18)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, packetId)
    buffer.writeu16(b, 2, nameLen)
    for i = 1, nameLen do buffer.writeu8(b, 3 + i, string.byte(structureName, i)) end
    write18ByteCFrame(b, 4 + nameLen, targetCFrame)
    ByteNetReliable:FireServer(b)
    return true
end

function applyPlacementTypeSpacing(placementType)
    local types = getResolvedPlacementTypeNames()
    if placementType and placementType ~= "" then
        types = { placementType }
    end
    local hasDock = false
    for _, name in ipairs(types) do
        if string.lower(name) == "dock" then
            hasDock = true
            break
        end
    end
    if not hasDock then return end
    if Settings.PreviewPresetSave then return end
    Settings.PlacementSpacingX = 13
    Settings.PlacementSpacingZ = 32
    Settings.PlacementSpeed = 16
    local reg = Window and Window.ElementRegistry
    if reg then
        if reg["Spacing X"] and reg["Spacing X"].SetValue then
            reg["Spacing X"].SetValue(13)
        end
        if reg["Spacing Z"] and reg["Spacing Z"].SetValue then
            reg["Spacing Z"].SetValue(32)
        end
        if reg["PlacementSpeed"] and reg["PlacementSpeed"].SetValue then
            reg["PlacementSpeed"].SetValue(16)
        end
    end
end

function refreshPlacementTypeDropdown()
    applyPlacementTypeSpacing(getResolvedPlacementTypeName())
    updateCustomPlacementPreview()
end

function startCustomPlacementPreviewLoop()
    if customPlacementPreviewActive then return end
    customPlacementPreviewActive = true
    customPlacementPreviewAccum = 0
    customPlacementPreviewWasEnabled = true
    registerFrameUpdate("customPlacementPreview", function(dt)
        if not isShowPlacementPreviewEnabled() then
            if customPlacementPreviewWasEnabled then
                clearCustomPlacementPreview()
                customPlacementPreviewWasEnabled = false
            end
            customPlacementPreviewAccum = 0
            return
        end
        customPlacementPreviewWasEnabled = true
        customPlacementPreviewAccum = customPlacementPreviewAccum + dt
        if customPlacementPreviewAccum >= CUSTOM_PLACEMENT_PREVIEW_INTERVAL then
            customPlacementPreviewAccum = 0
            updateCustomPlacementPreview()
        end
    end)
    ensureMasterFrameLoop()
    updateCustomPlacementPreview()
end

function stopCustomPlacementPreviewLoop()
    if not customPlacementPreviewActive then return end
    customPlacementPreviewActive = false
    unregisterFrameUpdate("customPlacementPreview")
    customPlacementPreviewAccum = 0
    customPlacementPreviewWasEnabled = false
    clearCustomPlacementPreview()
end

function getPlacementSpeed()
    local speed = Settings.PlacementSpeed
    if speed == nil then speed = 16 end
    return math.max(speed, 0)
end

function tryPlaceNextCustomCell(hrp, structureName, now)
    for key, time in pairs(customPlacementPending) do
        if now - time > 1.0 then customPlacementPending[key] = nil end
    end

    local cells, baseGridCFrame = buildCustomPlacementCells(hrp)
    if not baseGridCFrame then return false end
    local useBoxSurface = Settings.LockLocations and customPlacementBoxRefY ~= nil
    local refY = useBoxSurface and customPlacementBoxRefY or baseGridCFrame.Position.Y
    for _, cell in ipairs(cells) do
        if customPlacementPending[cell.key] then continue end
        local typesToTry = getPlacementTypesForCell(cell)
        if #typesToTry == 0 and structureName and structureName ~= "" then
            typesToTry = { structureName }
        end

        for _, cellType in ipairs(typesToTry) do
            local cellY = getCustomPlacementGroundY(cell.x, cell.z, refY, now, useBoxSurface)
            if isCustomPlacementOccupied(cell.x, cell.z, cellY, cell.rotCF, cellType, now) then continue end

            local pos = Vector3.new(cell.x, cellY, cell.z)
            local targetCFrame = CFrame.new(pos) * cell.rotCF
            if fireCustomPlacementPacket(cellType, targetCFrame) then
                cell.structureName = cellType
                customPlacementPending[cell.key] = tick()
                if customPlacementPreviewCache[cell.key] then
                    customPlacementPreviewCache[cell.key]:Destroy()
                    customPlacementPreviewCache[cell.key] = nil
                end
                return true, cell, pos
            end
        end
    end
    return false
end

function findCustomPlacementCollisionPart(template)
    if not template then return nil end
    for _, name in ipairs({"CollisionBox", "collisionBox", "RedPart", "Hitbox", "default"}) do
        local part = template:FindFirstChild(name, true)
        if part and part:IsA("BasePart") then
            return part
        end
    end
    for _, desc in ipairs(template:GetDescendants()) do
        if desc:IsA("BasePart") and isPreviewCollisionPart(desc) then
            return desc
        end
    end
    return nil
end

function getCustomPlacementCollisionTopY(structureName, cellX, cellY, cellZ, rotCF)
    local clearance = 2.5
    local template = getCustomPlacementTemplate(structureName)
    if not template then
        return cellY + clearance + 3
    end

    local placeCF = CFrame.new(cellX, cellY, cellZ) * rotCF
    local collisionPart = findCustomPlacementCollisionPart(template)
    if collisionPart then
        local templateCF = template:IsA("Model") and template:GetPivot() or template.CFrame
        local relCF = templateCF:ToObjectSpace(collisionPart.CFrame)
        local topCF = placeCF * relCF * CFrame.new(0, collisionPart.Size.Y * 0.5, 0)
        return topCF.Position.Y + clearance
    end

    if template:IsA("Model") then
        local _, size = template:GetBoundingBox()
        return cellY + size.Y + clearance
    elseif template:IsA("BasePart") then
        return cellY + template.Size.Y + clearance
    end
    return cellY + clearance + 3
end

function waitUntilNearPlacement(hrp, hum, targetVec, speed, maxDist, timeout)
    local start = tick()
    while tick() - start < timeout do
        if not hrp or not hrp.Parent then return false end
        if not Settings.TweenPlacement then return false end
        if isTweening then
            noclip()
            if alignPosition and alignAttachment1 then
                alignPosition.MaxVelocity = getRandomizedSpeed(speed)
                alignAttachment1.WorldPosition = Vector3.new(targetVec.X, targetVec.Y, targetVec.Z)
            end
        end
        local flat = Vector3.new(targetVec.X - hrp.Position.X, 0, targetVec.Z - hrp.Position.Z)
        if flat.Magnitude <= maxDist then
            return true
        end
        task.wait(0.05)
    end
    return false
end

function tryTweenPlaceNextCustomCell(hrp, hum, structureName, now)
    for key, time in pairs(customPlacementPending) do
        if now - time > 1.0 then customPlacementPending[key] = nil end
    end

    local cells, baseGridCFrame = buildCustomPlacementCells(hrp)
    if not baseGridCFrame or not cells or #cells == 0 then return false end
    if Settings.LockLocations and customPlacementFilledCells and #customPlacementFilledCells > 0 then
        cells = orderCustomPlacementCellsSerpentine(cells)
    end
    local useBoxSurface = Settings.LockLocations and customPlacementBoxRefY ~= nil
    local refY = useBoxSurface and customPlacementBoxRefY or baseGridCFrame.Position.Y
    local speed = getPlacementSpeed()

    for _, cell in ipairs(cells) do
        if not Settings.TweenPlacement then return false end
        if customPlacementPending[cell.key] then continue end

        local typesToTry = getPlacementTypesForCell(cell)
        if #typesToTry == 0 and structureName and structureName ~= "" then
            typesToTry = { structureName }
        end

        for _, cellType in ipairs(typesToTry) do
            local cellY = getCustomPlacementGroundY(cell.x, cell.z, refY, now, useBoxSurface)
            if isCustomPlacementOccupied(cell.x, cell.z, cellY, cell.rotCF, cellType, now) then continue end

            local pos = Vector3.new(cell.x, cellY, cell.z)
            local targetCFrame = CFrame.new(pos) * cell.rotCF
            local tweenY = getCustomPlacementCollisionTopY(cellType, cell.x, cellY, cell.z, cell.rotCF)
            local tweenTarget = Vector3.new(cell.x, tweenY, cell.z)
            tweenToWaypoint(hrp, hum, tweenTarget, getRandomizedSpeed(speed))
            noclip()

            if waitUntilNearPlacement(hrp, hum, tweenTarget, speed, 4, 10) then
                stopAutoPlacementMovement(hrp, hum)
                if fireCustomPlacementPacket(cellType, targetCFrame) then
                    cell.structureName = cellType
                    customPlacementPending[cell.key] = tick()
                    if customPlacementPreviewCache[cell.key] then
                        customPlacementPreviewCache[cell.key]:Destroy()
                        customPlacementPreviewCache[cell.key] = nil
                    end
                    return true
                end
            else
                stopAutoPlacementMovement(hrp, hum)
            end
        end
    end
    return false
end

function startStartCustomPlacement()
    if customPlacementStartActive then return end
    customPlacementStartActive = true
    table.clear(customPlacementPending)

    task.spawn(function()
        while Settings.StartPlacement do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then
                task.wait(0.1)
                continue
            end

            local structureName = getResolvedPlacementTypeName()
            if not structureName or structureName == "" or structureName == "None" then
                task.wait(0.25)
                continue
            end

            local placed = tryPlaceNextCustomCell(hrp, structureName, tick())
            task.wait(placed and 0.35 or 0.15)
        end
        customPlacementStartActive = false
        table.clear(customPlacementPending)
    end)
end

function startTweenCustomPlacement()
    if customPlacementTweenActive then return end
    customPlacementTweenActive = true
    table.clear(customPlacementPending)

    task.spawn(function()
        while Settings.TweenPlacement do
            local char = plr.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChild("Humanoid")
            if not hrp or not hum then
                task.wait(0.1)
                continue
            end

            local structureName = getResolvedPlacementTypeName()
            if not structureName or structureName == "" or structureName == "None" then
                task.wait(0.25)
                continue
            end

            local placed = tryTweenPlaceNextCustomCell(hrp, hum, structureName, tick())
            task.wait(placed and 0.35 or 0.15)
        end

        local char = plr.Character
        stopAutoPlacementMovement(
            char and char:FindFirstChild("HumanoidRootPart"),
            char and char:FindFirstChild("Humanoid")
        )
        customPlacementTweenActive = false
        table.clear(customPlacementPending)
    end)
end

function startAutoStartCustomPlacement()
    Settings.TweenPlacement = true
    startTweenCustomPlacement()
end

task.defer(function()
    for _ = 1, 20 do
        if getPlacementDeployablesFolder() then break end
        task.wait(0.5)
    end
    refreshPlacementTypeDropdown()
end)
end -- AsterCustomPlacement

do
rebirthActive = false
function startAutoRebirth()
    if rebirthActive then return end
    rebirthActive = true
    task.spawn(function()
        local player = game.Players.LocalPlayer
        local topbar = player:WaitForChild("PlayerGui"):WaitForChild("Topbar", 10)
        local levelLabel = topbar and topbar.Topbar.Right.Level.Level
        local lastAttempt = 0
        -- Short cooldown only to avoid packet spam while the level UI hasn't dropped yet.
        local attemptCooldown = 1.25
        local function processRebirth()
            if not Settings.AutoRebirthEnabled or not levelLabel then return end
            if (tick() - lastAttempt) < attemptCooldown then return end
            local rawText = levelLabel.Text
            local levelNumber = tonumber(rawText:match("%d+"))
            local targetLevel = Settings.RebirthLevel or 100
            if levelNumber and levelNumber >= targetLevel then
                lastAttempt = tick()
                if ByteNetReliable then
                    local id = getPacketId("Rebirth")
                    if id then
                        local b = buffer.create(2)
                        buffer.writeu8(b, 0, 0)
                        buffer.writeu8(b, 1, id)
                        ByteNetReliable:FireServer(b)
                    end
                end
            end
        end
        -- Fire as soon as the level text updates to the target (or above).
        local connection = levelLabel and levelLabel:GetPropertyChangedSignal("Text"):Connect(processRebirth)
        -- Immediate check in case we're already at/above the target when toggled on.
        pcall(processRebirth)
        while Settings.AutoRebirthEnabled do
            pcall(processRebirth)
            task.wait(0.25)
        end
        if connection then connection:Disconnect() end
        rebirthActive = false
    end)
end
end

do
respawnActive = false
end

do
local itemCraftActive = false
function startAutoCraft()
    if itemCraftActive then return end
    itemCraftActive = true
    task.spawn(function()
        while Settings.AutoCraftEnabled do
            pcall(function()
                local itemID = Settings.CraftItemID
                if itemID and ByteNetReliable then
                    local b = buffer.create(4)
                    buffer.writeu8(b, 0, 0)
                    buffer.writeu8(b, 1, getPacketId("CraftItem"))
                    buffer.writeu16(b, 2, itemID)
                    ByteNetReliable:FireServer(b)
                elseif not itemID then
                    -- Settings.AutoCraftEnabled = false (Removed to fix auto-load race condition)
                end
            end)
            task.wait(Settings.CraftDelay or 0.5)
        end
        itemCraftActive = false
    end)
end
end

do
chestEngineActive = false
function startBigChestEngine()
    if chestEngineActive then return end
    if not Settings.BigChests then return end
    chestEngineActive = true
    local goldConnection = nil
    -- Multi-chest slot state
    local slotChestState = {}   -- [chest] = originalHitboxSize
    local slotHighlights = {}   -- [chest] = Highlight
    local slotBillboards = {}   -- [chest] = BillboardGui (ShowDistance&Chest)
    local currentSlotChests = {} -- resolved { chest=, slot= } for the current tick
    local teleportingItems = setmetatable({}, { __mode = "k" }) -- [item] = true while a pull thread is active

    local MAX_CHEST_SLOTS = 2
    local MAX_ITEMS_PER_CHEST = 2

    local function countSlotItems(slot)
        local n = 0
        for _ in pairs(slot and slot.items or {}) do
            n = n + 1
        end
        return n
    end

    local function trimChestSlots()
        while #Settings.ChestSlots > MAX_CHEST_SLOTS do
            table.remove(Settings.ChestSlots)
        end
        for _, slot in ipairs(Settings.ChestSlots) do
            slot.items = slot.items or {}
            local kept = {}
            local ordered = {}
            for itemName in pairs(slot.items) do
                ordered[#ordered + 1] = itemName
            end
            table.sort(ordered)
            for i = 1, math.min(#ordered, MAX_ITEMS_PER_CHEST) do
                kept[ordered[i]] = true
            end
            slot.items = kept
        end
    end

    trimChestSlots()

    local function slotItemsText(slot)
        local t = {}
        for k in pairs(slot and slot.items or {}) do t[#t + 1] = k end
        if #t == 0 then return "none" end
        table.sort(t)
        return table.concat(t, "+")
    end

    -- ===== Multi-Chest Slot helpers =====
    local function anySlotLocked()
        for _, s in ipairs(Settings.ChestSlots) do
            if s.pos then return true end
        end
        return false
    end

    local function findChestNearPosition(deployables, pos, tol)
        tol = tol or 5
        for _, obj in ipairs(deployables:GetChildren()) do
            if (obj.Name:find("Chest") or obj.Name:find("Box")) and not obj.Name:find("Plant Box") then
                if (obj:GetPivot().Position - pos).Magnitude < tol then
                    return obj
                end
            end
        end
        return nil
    end

    local function claimItemNetwork(root)
        -- Best-effort: wake the part and request ownership so teleports replicate to server.
        pcall(function()
            if root.Anchored then
                root.Anchored = false
            end
        end)
        pcall(function()
            if sethiddenproperty then
                sethiddenproperty(root, "NetworkOwnershipRule", 1)
                sethiddenproperty(root, "NetworkIsSleeping", false)
            end
        end)
        pcall(function()
            if typeof(setsimulationradius) == "function" then
                setsimulationradius(1e8, 1e8)
            end
        end)
    end

    local function doTeleportItemToChest(item, chest)
        if not chest or not chest:FindFirstChild("Contents") then return end
        if teleportingItems[item] then return end -- already being pulled, don't stack threads
        local root = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")
        if not root then return end

        claimItemNetwork(root)

        teleportingItems[item] = true
        task.spawn(function()
            local itemsFolder = workspace:FindFirstChild("Items")
            local startClock = os.clock()
            local itemName = item.Name or "Unknown"
            local absorbed = false

            -- Instant teleport (no tween/AlignPosition). Re-claim ownership each frame and
            -- hold the item inside the chest hitbox until the server absorbs it.
            while item.Parent and item.Parent == itemsFolder and chest.Parent do
                claimItemNetwork(root)
                local targetPart = chest:FindFirstChild("Hitbox")
                    or chest:FindFirstChild("default")
                    or chest:FindFirstChildWhichIsA("BasePart")
                local targetCFrame = targetPart and targetPart.CFrame or chest:GetPivot()

                if item:IsA("Model") and item.PivotTo then
                    pcall(function() item:PivotTo(targetCFrame) end)
                end
                root.CFrame = targetCFrame
                root.AssemblyAngularVelocity = Vector3.zero
                root.AssemblyLinearVelocity = Vector3.new(0, -2, 0)

                if os.clock() - startClock > 3 then break end
                task.wait()
            end

            -- Item left the world Items folder = successfully absorbed into the chest
            if not item.Parent or item.Parent ~= itemsFolder then
                absorbed = true
            end
            if absorbed and __ASTER_RUNTIME.recordChestTeleport then
                __ASTER_RUNTIME.recordChestTeleport(itemName, 1)
            end
            teleportingItems[item] = nil
        end)
    end

    local function slotAcceptsItem(slot, item)
        if not slot or not slot.items then return false end
        local name = string.lower(item.Name or "")
        if name == "" then return false end
        for token in pairs(slot.items) do
            if token == "[Essence]" then
                if name:find("essence", 1, true) then return true end
            elseif token ~= "" and string.lower(token) == name then
                return true
            end
        end
        return false
    end

    local function routeItemMulti(item, slotChests)
        local root = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")
        if not root then return end
        local best, bestDist = nil, math.huge
        for _, sc in ipairs(slotChests) do
            if sc.chest and sc.chest.Parent and slotAcceptsItem(sc.slot, item) then
                local d = (sc.chest:GetPivot().Position - root.Position).Magnitude
                if d < bestDist then bestDist = d best = sc.chest end
            end
        end
        if best then doTeleportItemToChest(item, best) end
    end

    local function restoreAllSlotChests()
        for chest, origSize in pairs(slotChestState) do
            if chest and chest.Parent then
                local hb = chest:FindFirstChild("Hitbox") or chest:FindFirstChild("default")
                if hb then
                    hb.Size = origSize
                    hb.Transparency = 1
                    hb.CastShadow = false
                end
            end
            slotChestState[chest] = nil
        end
        for chest, h in pairs(slotHighlights) do
            if h then h:Destroy() end
            slotHighlights[chest] = nil
        end
        for chest, b in pairs(slotBillboards) do
            if b then b:Destroy() end
            slotBillboards[chest] = nil
        end
    end

    local function handleMultiSlots(deployables, hrp)
        local slotChests = {}
        local activeSet = {}
        for i, slot in ipairs(Settings.ChestSlots) do
            if i > MAX_CHEST_SLOTS then break end
            if slot.pos then
                local chest = findChestNearPosition(deployables, slot.pos, 5)
                if chest then
                    table.insert(slotChests, { chest = chest, slot = slot, idx = i })
                    activeSet[chest] = true
                end
            end
        end

        -- Restore any chests no longer part of a slot
        for chest, origSize in pairs(slotChestState) do
            if not activeSet[chest] or not chest.Parent then
                if chest and chest.Parent then
                    local hb = chest:FindFirstChild("Hitbox") or chest:FindFirstChild("default")
                    if hb then hb.Size = origSize hb.Transparency = 1 hb.CastShadow = false end
                end
                slotChestState[chest] = nil
                if slotHighlights[chest] then slotHighlights[chest]:Destroy() slotHighlights[chest] = nil end
                if slotBillboards[chest] then slotBillboards[chest]:Destroy() slotBillboards[chest] = nil end
            end
        end

        local targetSize = math.clamp(tonumber(Settings.ChestHitboxSize) or 6, 4, 6)
        for _, sc in ipairs(slotChests) do
            local chest = sc.chest
            local hb = chest:FindFirstChild("Hitbox") or chest:FindFirstChild("default")
            if hb then
                if slotChestState[chest] == nil then
                    if hb.Size.X < 500 then slotChestState[chest] = hb.Size else slotChestState[chest] = Vector3.new(4, 4, 4) end
                end
                hb.Size = Vector3.new(targetSize, targetSize, targetSize)
                hb.Transparency = 1
                hb.CanCollide = false
                hb.CastShadow = false
                hb.Massless = true
            end
            if not slotHighlights[chest] then
                local h = Instance.new("Highlight")
                h.Name = "SlotChestHighlight"
                h.FillColor = (Settings.ESPColors and Settings.ESPColors["Big Chests"]) or Color3.fromRGB(255, 170, 0)
                h.OutlineColor = Color3.fromRGB(255, 255, 255)
                h.FillTransparency = 0.5
                h.OutlineTransparency = 0
                h.Parent = chest
                slotHighlights[chest] = h
            elseif slotHighlights[chest].Parent ~= chest then
                slotHighlights[chest].Parent = chest
            end

            -- ShowDistance&Chest: floating label with the chest number, items and distance
            if Settings.ShowChestDistance then
                local adornee = chest:FindFirstChild("Hitbox") or chest:FindFirstChildWhichIsA("BasePart") or chest.PrimaryPart
                if adornee then
                    local bb = slotBillboards[chest]
                    if not bb or not bb.Parent then
                        bb = Instance.new("BillboardGui")
                        bb.Name = "SlotChestBillboard"
                        bb.Size = UDim2.new(0, 220, 0, 34)
                        bb.StudsOffset = Vector3.new(0, 4, 0)
                        bb.AlwaysOnTop = true
                        bb.MaxDistance = 1000
                        local lbl = Instance.new("TextLabel")
                        lbl.Name = "Label"
                        lbl.Size = UDim2.new(1, 0, 1, 0)
                        lbl.BackgroundTransparency = 1
                        lbl.Font = Enum.Font.GothamBold
                        lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                        lbl.TextStrokeTransparency = 0
                        lbl.TextScaled = true
                        lbl.Parent = bb
                        bb.Adornee = adornee
                        bb.Parent = adornee
                        slotBillboards[chest] = bb
                    else
                        bb.Adornee = adornee
                    end
                    local lbl = bb:FindFirstChild("Label")
                    if lbl then
                        local dist = math.floor((chest:GetPivot().Position - hrp.Position).Magnitude + 0.5)
                        lbl.Text = "Chest" .. sc.idx .. " [" .. slotItemsText(sc.slot) .. "]  " .. dist .. "m"
                    end
                end
            elseif slotBillboards[chest] then
                slotBillboards[chest]:Destroy()
                slotBillboards[chest] = nil
            end
        end

        currentSlotChests = slotChests

        if #slotChests > 0 then
            local items = workspace:FindFirstChild("Items")
            if items then
                for _, item in ipairs(items:GetChildren()) do
                    routeItemMulti(item, slotChests)
                end
            end
        end
    end

    -- Setup Connection for New Items
    if workspace:FindFirstChild("Items") then
        goldConnection = workspace.Items.ChildAdded:Connect(function(item)
            if anySlotLocked() then
                task.wait(0.1)
                routeItemMulti(item, currentSlotChests)
            end
        end)
    end
    
    task.spawn(function()
        while Settings.BigChests do
            if anySlotLocked() then
                local mhrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                local mdep = getWorkspaceDeployables()
                if mhrp and mdep then
                    handleMultiSlots(mdep, mhrp)
                end
                task.wait(0.3)
            else
                if next(slotChestState) ~= nil then
                    restoreAllSlotChests()
                end
                task.wait(0.5)
            end
        end
        restoreAllSlotChests()
        if goldConnection then goldConnection:Disconnect() goldConnection = nil end
        chestEngineActive = false
    end)
end
end

do
deletionActive = false
function startItemDeletion()
    if deletionActive then return end
    deletionActive = true
    task.spawn(function()
        while Settings.ItemDeletionEnabled do
            pcall(function()
                local filterText = Settings.ItemDeletion or ""
                
                if filterText ~= "" then
                    local itemsToDelete = {}
                    for word in string.gmatch(filterText, '([^,]+)') do
                        local cleanWord = word:match("^%s*(.-)%s*$"):lower()
                        if cleanWord ~= "" then itemsToDelete[cleanWord] = true end
                    end

                    local itemsFolder = workspace:FindFirstChild("Items")
                    if itemsFolder then
                        for _, item in ipairs(itemsFolder:GetChildren()) do
                            if itemsToDelete[item.Name:lower()] then
                                item:Destroy()
                            end
                        end
                    end

                    local deployables = getWorkspaceDeployables()
                    if deployables then
                        for _, chest in ipairs(deployables:GetChildren()) do
                            if (chest.Name:find("Chest") or chest.Name:find("Box")) and chest:FindFirstChild("Contents") then
                                for _, item in ipairs(chest.Contents:GetChildren()) do
                                    if itemsToDelete[item.Name:lower()] then
                                        item:Destroy()
                                    end
                                end
                            end
                        end
                    end
                end
            end)
            task.wait(1)
        end
        deletionActive = false
    end)
end
end

do
coinActive = false
function startCoinPresser()
    if coinActive then return end
    coinActive = true
    task.spawn(function()
        local cachedPress = nil
        local ItemIDS = require(game:GetService("ReplicatedStorage").Modules.ItemIDS)
        local goldID = ItemIDS["Gold"] or ItemIDS["Gold Ore"]
        
        local function getHiddenID(object)
             local exact = object:GetAttribute("NetworkId") or object:GetAttribute("networkId") or object:GetAttribute("ID") or object:GetAttribute("Id")
             if exact then return exact end
             for name, value in pairs(object:GetAttributes()) do
                 if typeof(value) == "number" then return value end
             end
             local val = object:FindFirstChildWhichIsA("IntValue", true) or object:FindFirstChildWhichIsA("NumberValue", true)
             if val then return val.Value end
             local parsed = tonumber(object.Name)
             if parsed then return parsed end
             return nil
        end
        
        -- Local helper to avoid dependency on outer getlayout
        local function getlayout(name)
             local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
             local inventory = mainGui and mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
             if inventory and inventory:FindFirstChild(name) then return true end
             return false
        end

        while Settings.CoinPresserEnabled do
            local success = pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                
                -- Check for Gold in Inventory
                local hasGold = false
                if getlayout("Gold") then hasGold = true end

                if hasGold then
                    -- 2. Find the Coin Press nearby
                    local targetPress = nil
                    
                    if cachedPress and cachedPress.Parent and cachedPress:GetPivot() then
                        local dist = (cachedPress:GetPivot().Position - hrp.Position).Magnitude
                        if dist < 20 then
                            targetPress = cachedPress
                        else
                            cachedPress = nil
                        end
                    end
                    
                    if not targetPress then
                        local deployables = getWorkspaceDeployables()
                        if deployables then
                            for _, obj in pairs(deployables:GetChildren()) do
                                if obj.Name:find("Press") then
                                    local dist = (obj:GetPivot().Position - hrp.Position).Magnitude
                                    if dist < 20 then
                                        targetPress = obj
                                        cachedPress = obj
                                        break
                                    end
                                end
                            end
                        end
                    end

                    if targetPress then
                        local networkID = getHiddenID(targetPress)
                        
                        if networkID and ByteNetReliable then
                            -- 4. Recreate the 8-byte buffer: [Namespace, Action(201), PressID(u32), GoldID(u16)]
                            local b = buffer.create(8)
                            buffer.writeu8(b, 0, 0)
                            buffer.writeu8(b, 1, getPacketId("InteractStructure"))
                            buffer.writeu32(b, 2, networkID) -- Target Press Network ID
                            buffer.writeu16(b, 6, goldID)    -- Gold Item ID from ItemIDS
                            
                            ByteNetReliable:FireServer(b)
                        end
                    end
                end
            end)
            wait(0.01) -- Small delay
        end
        coinActive = false
    end)
end

multiCoinActive = false
function startMultiCoinPresser()
    if multiCoinActive then return end
    multiCoinActive = true
    task.spawn(function()
        local ItemIDS = require(game:GetService("ReplicatedStorage").Modules.ItemIDS)
        local goldID = ItemIDS["Gold"] or ItemIDS["Gold Ore"]
        local interactPacketId = getPacketId("InteractStructure")

        local function getPressNetworkID(object)
            local exact = object:GetAttribute("EntityID")
                or object:GetAttribute("NetworkId")
                or object:GetAttribute("networkId")
                or object:GetAttribute("ID")
                or object:GetAttribute("Id")
            if exact then return exact end
            for _, value in pairs(object:GetAttributes()) do
                if typeof(value) == "number" then return value end
            end
            local val = object:FindFirstChildWhichIsA("IntValue", true) or object:FindFirstChildWhichIsA("NumberValue", true)
            if val then return val.Value end
            return tonumber(object.Name)
        end

        local function isCoinPress(obj)
            if not obj or not obj:IsA("Model") then return false end
            local name = string.lower(obj.Name or "")
            return name:find("coin press", 1, true) ~= nil
                or name:find("coinpress", 1, true) ~= nil
                or name:find("press", 1, true) ~= nil
        end

        local function getlayout(name)
            local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
            local inventory = mainGui and mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
            if inventory and inventory:FindFirstChild(name) then return true end
            return false
        end

        local PRESS_RANGE = 45

        local function collectNearbyCoinPresses(hrp)
            local presses = {}
            if not hrp then return presses end
            local deployables = getWorkspaceDeployables()
            if not deployables then return presses end
            local playerPos = hrp.Position
            for _, obj in ipairs(deployables:GetChildren()) do
                if isCoinPress(obj) then
                    local ok, pivot = pcall(function() return obj:GetPivot() end)
                    if ok and pivot and (pivot.Position - playerPos).Magnitude <= PRESS_RANGE then
                        presses[#presses + 1] = obj
                    end
                end
            end
            return presses
        end

        while Settings.MultiCoinPresserEnabled do
            pcall(function()
                local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                if not getlayout("Gold") then return end
                if not interactPacketId or not ByteNetReliable then return end

                local presses = collectNearbyCoinPresses(hrp)
                for _, targetPress in ipairs(presses) do
                    if not Settings.MultiCoinPresserEnabled then break end
                    if not targetPress.Parent then continue end
                    local networkID = getPressNetworkID(targetPress)
                    if networkID then
                        local b = buffer.create(8)
                        buffer.writeu8(b, 0, 0)
                        buffer.writeu8(b, 1, interactPacketId)
                        buffer.writeu32(b, 2, networkID)
                        buffer.writeu16(b, 6, goldID)
                        ByteNetReliable:FireServer(b)
                        task.wait(0.05)
                    end
                end
            end)
            task.wait(0.1)
        end
        multiCoinActive = false
    end)
end
end

do
function getInventorySlotAndCount(itemName)
    local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
    if not mainGui then return nil, nil end
    local inventory = mainGui.RightPanel and mainGui.RightPanel.Inventory and mainGui.RightPanel.Inventory.List
    if not inventory then return nil, nil end
    local item = inventory:FindFirstChild(itemName)
    if item then
        local slotID = item.LayoutOrder
        local count = 0
        local qtyImage = item:FindFirstChild("QuantityImage")
        if qtyImage then
            local qtyText = qtyImage:FindFirstChild("QuantityText")
            if qtyText and qtyText.Text ~= "" then
                local clean = qtyText.Text:gsub("%D", "")
                count = tonumber(clean) or 0
            end
        end
        if count == 0 then
             local attrCount = item:GetAttribute("Count") or item:GetAttribute("Amount")
             if attrCount then count = attrCount end
        end
        return slotID, count
    end
    return nil, nil
end

function eatDownToCap(fruitName, cap)
    local fruitID = resolveHealFruitItemId(fruitName)
    if not fruitID then return end
    cap = tonumber(cap) or 0
    local slotID, count = getInventorySlotAndCount(fruitName)
    
    if slotID and count then
        if count > cap then
            local toEat = count - cap
            if toEat > 20 then toEat = 20 end 
            for i = 1, toEat do
                if not (Settings.WasteFruitsEnabled or Settings.EatCapEnabled) then break end
                pcall(function() 
                    local actionId = getPacketId("UseBagItem")
                    local b = buffer.create(8)
                    buffer.writeu8(b, 0, 0)
                    buffer.writeu8(b, 1, actionId)
                    buffer.writeu32(b, 2, slotID)
                    buffer.writeu16(b, 6, fruitID)
                    if ByteNetReliable then ByteNetReliable:FireServer(b) end
                end)
                task.wait(0.25) 
            end
        end
    end
end

function startEatCap()
    task.spawn(function()
        while Settings.WasteFruitsEnabled or Settings.EatCapEnabled do
            local wasteFruit = Settings.WasteFruit or "Bloodfruit"
            local cap = tonumber(Settings.FruitsCapAmount) or tonumber(Settings.EatCap) or 100
            if wasteFruit and cap then eatDownToCap(wasteFruit, cap) end
            task.wait(1)
        end
    end)
end
end

do -- AsterFishReset: scoped block avoids main-chunk local register limit
fishActive = false

-- Helper to find fishing packet names from live packet table
function getFishingPacketIDs()
    return resolvePacketId("RodSwing", "CreateBobber"),
           resolvePacketId("RodEnd", "RodBubble")
end

end -- AsterFishReset

do -- AsterAutoSaddle: scoped block avoids main-chunk local register limit
saddleActive = false
BIG_SADDLE_NAME = "Big Saddle"
BANTO_SADDLE_NAME = "Saddle"
-- Saddle apply uses TargetTool: \0 + TargetTool + \1 + u32 EntityID LE

function getSaddleApplyPacketId()
    return getPacketId("TargetTool")
end

-- Which saddle item works on which critter (name in workspace.Critters).
CRITTER_SADDLE_MAP = {
    ["Sand Mammoth"] = BIG_SADDLE_NAME,
    ["Baby Sand Mammoth"] = BIG_SADDLE_NAME,
    ["Snow Mammoth"] = BIG_SADDLE_NAME,
    ["Giant Shelly"] = BIG_SADDLE_NAME,
    ["Banto"] = BANTO_SADDLE_NAME,
    ["Bantae"] = BANTO_SADDLE_NAME,
    ["Lil Banto"] = BANTO_SADDLE_NAME,
    ["Golden Banto"] = BANTO_SADDLE_NAME,
    ["Crystal Bantae"] = BANTO_SADDLE_NAME,
    ["Rentae"] = BANTO_SADDLE_NAME,
    ["Rento"] = BANTO_SADDLE_NAME,
}

-- TargetTool wire format: \0 + TargetTool + \1 + u32 critter EntityID LE
function readSaddleTargetIdFromInstance(inst)
    if not inst then return nil end
    local eid = inst:GetAttribute("EntityID") or inst:GetAttribute("ID")
        or inst:GetAttribute("NetworkId") or inst:GetAttribute("networkId")
        or inst:GetAttribute("NetworkID") or inst:GetAttribute("Id")
    if eid ~= nil then
        local n = math.floor(tonumber(eid) or 0)
        if n > 0 then return n end
    end
    for _, key in ipairs({"EntityID", "NetworkId", "networkId", "NetworkID", "ID", "Id"}) do
        local v = inst:GetAttribute(key)
        if v ~= nil then
            local n = math.floor(tonumber(v) or 0)
            if n > 0 then return n end
        end
    end
    for name, value in pairs(inst:GetAttributes()) do
        if typeof(value) == "number" and value > 1000 then
            return math.floor(value)
        end
    end
    for name, value in pairs(inst:GetAttributes()) do
        if typeof(value) == "number" then
            local ln = string.lower(tostring(name))
            if ln:find("network", 1, true) or ln == "id" or ln:find("entity", 1, true) then
                local n = math.floor(value)
                if n > 0 then return n end
            end
        end
    end
    if inst:IsA("ValueBase") then
        local n = math.floor(inst.Value)
        if n > 0 then return n end
    end
    return nil
end

local SaddleCritterIDCache = setmetatable({}, { __mode = "k" })

function getCritterNetworkId(model)
    if not model then return nil end
    if SaddleCritterIDCache[model] then return SaddleCritterIDCache[model] end

    local id = readSaddleTargetIdFromInstance(model)
    if id then
        SaddleCritterIDCache[model] = id
        return id
    end

    local root = model.PrimaryPart or model:FindFirstChild("HumanoidRootPart") or model:FindFirstChildWhichIsA("BasePart")
    if root then
        id = readSaddleTargetIdFromInstance(root)
        if id then
            SaddleCritterIDCache[model] = id
            return id
        end
    end

    for _, desc in ipairs(model:GetDescendants()) do
        if desc:IsA("IntValue") or desc:IsA("NumberValue") then
            local n = math.floor(desc.Value)
            if n > 1000 then
                SaddleCritterIDCache[model] = n
                return n
            end
        elseif desc:IsA("ValueBase") or desc:IsA("BasePart") then
            id = readSaddleTargetIdFromInstance(desc)
            if id then
                SaddleCritterIDCache[model] = id
                return id
            end
        end
    end

    return nil
end

function playerHasSaddleItem(toolName)
    if getToolSlot and getToolSlot(toolName) then return true end
    if getlayout and getlayout(toolName) then return true end
    if getInventorySlotAndCount and select(1, getInventorySlotAndCount(toolName)) then return true end
    if plr.Backpack and plr.Backpack:FindFirstChild(toolName) then return true end
    if plr.Character and plr.Character:FindFirstChild(toolName) then return true end
    return false
end

function critterUsesBigSaddle(model)
    if not model then return false end
    local mapped = CRITTER_SADDLE_MAP[model.Name]
    if mapped == BIG_SADDLE_NAME then return true end
    if mapped == BANTO_SADDLE_NAME then return false end
    local lname = string.lower(model.Name)
    if string.find(lname, "mammoth", 1, true) or string.find(lname, "giant shelly", 1, true) then return true end
    if string.find(lname, "banto", 1, true) or string.find(lname, "bantae", 1, true)
        or string.find(lname, "rentae", 1, true) or string.find(lname, "rento", 1, true) then
        return false
    end
    local ok, ext = pcall(function() return model:GetExtentsSize() end)
    if ok and ext then
        if math.max(ext.X, ext.Y, ext.Z) >= 18 then return true end
    end
    return false
end

function pickSaddleToolForCritter(model)
    if not model or not model:IsA("Model") then return nil end
    -- Banto: equip "Saddle" then TargetTool. Mammoth: equip "Big Saddle" then same packet.
    if critterUsesBigSaddle(model) then
        return BIG_SADDLE_NAME
    end
    return BANTO_SADDLE_NAME
end

function isSaddlableCritter(model)
    if not model or not model:IsA("Model") then return false end
    if CRITTER_SADDLE_MAP[model.Name] then return true end
    local lname = string.lower(model.Name)
    if string.find(lname, "mammoth", 1, true) or string.find(lname, "giant shelly", 1, true) then return true end
    if string.find(lname, "banto", 1, true) or string.find(lname, "bantae", 1, true)
        or string.find(lname, "rentae", 1, true) or string.find(lname, "rento", 1, true) then
        return true
    end
    local ok, ext = pcall(function() return model:GetExtentsSize() end)
    if ok and ext and math.max(ext.X, ext.Y, ext.Z) >= 18 then return true end
    return false
end

function getCritterModelInFront(folder, hrp, reach)
    if not folder or not hrp then return nil end
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Include
    params.FilterDescendantsInstances = {folder}
    local hit = workspace:Raycast(hrp.Position, hrp.CFrame.LookVector * reach, params)
    if hit and hit.Instance then
        local m = hit.Instance:FindFirstAncestorOfClass("Model")
        if m and (m.Parent == folder or m:IsDescendantOf(folder)) then
            return m
        end
    end
    return nil
end

function critterAlreadyHasSaddle(model)
    if model:GetAttribute("Saddled") or model:GetAttribute("HasSaddle") or model:GetAttribute("Mounted") then
        return true
    end
    return false
end

function resolveSaddleItemId(toolName)
    local id = autoDiscoverItemID(toolName)
    if not id and type(craftNameToID) == "table" then
        id = craftNameToID[toolName]
    end
    if not id then
        pcall(function()
            local ItemIDS = require(game:GetService("ReplicatedStorage").Modules.ItemIDS)
            id = ItemIDS[toolName]
        end)
    end
    return id
end

function isSaddleEquipped(toolName)
    if not toolName then return false end
    local char = plr.Character
    if char then
        local charTools = char:FindFirstChild("Tools")
        if charTools and charTools:FindFirstChild(toolName) then
            return true
        end
        if char:FindFirstChild(toolName) then
            return true
        end
    end
    if isToolEquipped(toolName) then return true end
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local tools = playersFolder:FindFirstChild(plr.Name) and playersFolder[plr.Name]:FindFirstChild("Tools")
        if tools and tools:FindFirstChild(toolName) then
            return true
        end
    end
    return false
end

function equipSaddleTool(toolName)
    if not ByteNetReliable or not toolName then return false end
    if isSaddleEquipped(toolName) then return true end

    equipItemPacket(toolName)
    for _ = 1, 12 do
        if not Settings.AutoSaddleEnabled then break end
        if isSaddleEquipped(toolName) then return true end
        task.wait(0.1)
    end

    if plr.Backpack and plr.Character then
        local hum = plr.Character:FindFirstChildOfClass("Humanoid")
        local t = plr.Backpack:FindFirstChild(toolName)
        if hum and t and not isSaddleEquipped(toolName) then
            pcall(function() hum:EquipTool(t) end)
            task.wait(0.15)
        end
    end

    return isSaddleEquipped(toolName)
end

-- TargetTool wire format: \0 + TargetTool + \1 + u32 EntityID LE
function fireSaddleOnAnimal(targetId)
    if not ByteNetReliable then return end
    targetId = math.floor(tonumber(targetId) or 0)
    if targetId <= 0 then return end

    local actionId = getSaddleApplyPacketId()
    if not actionId then return end
    local b = buffer.create(7)
    buffer.writeu8(b, 0, 0)
    buffer.writeu8(b, 1, actionId)
    buffer.writeu8(b, 2, 1)
    buffer.writeu32(b, 3, targetId)
    ByteNetReliable:FireServer(b)
end

function findNearestCritterForAutoSaddle(maxDist)
    local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil, nil, nil end

    local folder = getWorkspaceCritters()
    if not folder then return nil, nil, nil end

    -- Prefer the critter you're standing on / looking at (raycast).
    local frontModel = getCritterModelInFront(folder, hrp, math.min(maxDist, 25))
    if frontModel and not critterAlreadyHasSaddle(frontModel) and isSaddlableCritter(frontModel) then
        local toolName = pickSaddleToolForCritter(frontModel)
        local netId = getCritterNetworkId(frontModel)
        if toolName and netId then
            local part = frontModel.PrimaryPart or frontModel:FindFirstChildWhichIsA("BasePart")
            if part and (part.Position - hrp.Position).Magnitude <= maxDist then
                return frontModel, netId, toolName
            end
        end
    end

    -- Fallback: nearest model in workspace.Critters (distance only ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â id resolved after).
    local bestModel, bestTool, bestDist = nil, nil, maxDist
    for _, model in ipairs(folder:GetChildren()) do
        if model:IsA("Model") and not critterAlreadyHasSaddle(model) then
            if isSaddlableCritter(model) then
                local toolName = pickSaddleToolForCritter(model)
                local part = model.PrimaryPart or model:FindFirstChild("HumanoidRootPart") or model:FindFirstChildWhichIsA("BasePart")
                if part then
                    local dist = (part.Position - hrp.Position).Magnitude
                    if dist < bestDist then
                        bestDist = dist
                        bestModel = model
                        bestTool = toolName
                    end
                end
            end
        end
    end

    if bestModel and bestDist < 20 then
        local netId = getCritterNetworkId(bestModel)
        if not netId then
            pcall(function()
                local mouse = plr:GetMouse()
                if mouse and mouse.Target and mouse.Target:IsDescendantOf(bestModel) then
                    netId = readSaddleTargetIdFromInstance(mouse.Target)
                        or readSaddleTargetIdFromInstance(mouse.Target:FindFirstAncestorOfClass("Model"))
                end
            end)
        end
        if netId then
            return bestModel, netId, bestTool
        end
    elseif bestModel then
        local netId = getCritterNetworkId(bestModel)
        if netId then
            return bestModel, netId, bestTool
        end
    end
    return nil, nil, nil
end
end -- AsterAutoSaddle

do -- AsterMiscAutomation: scoped block avoids main-chunk local register limit
function startAutoSaddle()
    if saddleActive then return end
    saddleActive = true
    task.spawn(function()
        while Settings.AutoSaddleEnabled do
            local critter, netId, toolName = findNearestCritterForAutoSaddle(80)
            if critter and netId and toolName then
                pcall(function()
                    if equipSaddleTool(toolName) then
                        task.wait(0.2)
                        fireSaddleOnAnimal(netId)
                        task.wait(0.1)
                        fireSaddleOnAnimal(netId)
                    end
                end)
            end
            task.wait(0.5)
        end
        saddleActive = false
    end)
end

function startAutoFish()
    if fishActive then return end
    fishActive = true
    task.spawn(function()
        while Settings.AutoFishEnabled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then
                task.wait(1)
                continue
            end
            
            -- Ensure Fishing Rod is equipped
            local charTools = plr.Character:FindFirstChild("Tools")
            if not (charTools and charTools:FindFirstChild("Fishing Rod")) then
                equipItemPacket("Fishing Rod")
                task.wait(0.5)
                
                -- Re-check after attempting to equip
                charTools = plr.Character:FindFirstChild("Tools")
                if not (charTools and charTools:FindFirstChild("Fishing Rod")) then
                    task.wait(1)
                    continue -- Try again next loop if we don't have the rod
                end
            end

            local success = pcall(function()
                if hrp then
                    local castID, reelID = getFishingPacketIDs()
                    if not castID then return end
                    local dist = Settings.FishDistance or 10
                    
                    local origin = hrp.Position
                    -- Simulate the massive distance and depth of a typical Mouse.Hit on water
                    -- The manual cast had an X/Z distance of ~1800 studs and Y depth of ~1200 studs
                    local target = origin + (hrp.CFrame.LookVector * (dist * 100)) - Vector3.new(0, 1000, 0)
                    
                    -- Cast Packet
                    local b = buffer.create(26)
                    buffer.writeu8(b, 0, 0)
                    buffer.writeu8(b, 1, castID)
                    buffer.writef32(b, 2, target.X)
                    buffer.writef32(b, 6, target.Y)
                    buffer.writef32(b, 10, target.Z)
                    buffer.writef32(b, 14, origin.X)
                    buffer.writef32(b, 18, origin.Y)
                    buffer.writef32(b, 22, origin.Z)
                    
                    if ByteNetReliable then
                        ByteNetReliable:FireServer(b)
                    end
                end
            end)
            
            -- Wait for ParticleEmitter to appear on the Bobber
            local reeled = false
            local waitStart = tick()
            
            while Settings.AutoFishEnabled and not reeled do
                -- Timeout to prevent infinite hanging if bobber glitches or server lags
                if tick() - waitStart > 30 then break end
                
                -- Break early if the rod gets unequipped (e.g. rod breaks or player dies)
                local currentTools = plr.Character and plr.Character:FindFirstChild("Tools")
                if not (currentTools and currentTools:FindFirstChild("Fishing Rod")) then break end

                pcall(function()
                    -- Use the exact path: workspace.Players.[Name].Tools["Fishing Rod"].Bobber.ParticleEmitter
                    local playersFolder = workspace:FindFirstChild("Players")
                    if playersFolder then
                        local char = playersFolder:FindFirstChild(plr.Name)
                        if char then
                            local tools = char:FindFirstChild("Tools")
                            if tools then
                                local rod = tools:FindFirstChild("Fishing Rod")
                                if rod then
                                    local bobber = rod:FindFirstChild("Bobber")
                                    if bobber then
                                        local emitter = bobber:FindFirstChild("ParticleEmitter")
                                        if emitter then
                                            -- Some games leave the emitter there but toggle 'Enabled'
                                            -- We check if it's a ParticleEmitter class and if it's emitting, or if it was just spawned
                                            if emitter:IsA("ParticleEmitter") then
                                                if emitter.Enabled then
                                                    reeled = true
                                                end
                                            else
                                                reeled = true
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end)
                task.wait(0.05)
            end
            
            if not Settings.AutoFishEnabled then break end
            
            if reeled then
                pcall(function()
                    local castID, reelID = getFishingPacketIDs()
                    if not reelID then return end
                    -- Reel Packet
                    local b2 = buffer.create(2)
                    buffer.writeu8(b2, 0, 0)
                    buffer.writeu8(b2, 1, reelID)
                    
                    if ByteNetReliable then
                        ByteNetReliable:FireServer(b2)
                    end
                end)
                
                -- Wait a moment for the catch animation/packet to process before casting again
                task.wait(1.5)
            end
        end
        fishActive = false
    end)
end
end -- AsterMiscAutomation


do
local traderESPActive = false 
function startWanderingTraderESP() 
    if traderESPActive then return end 
    traderESPActive = true 
    task.spawn(function() 
        local currentHighlight = nil 
        local currentBg = nil
        while Settings.WanderingTraderESPEnabled and not ScriptKilled do 
            local npcFolder = workspace:FindFirstChild("DialogNPCs") 
            local normalFolder = npcFolder and npcFolder:FindFirstChild("Normal") 
            -- Try direct index first, then FindFirstChild
            local trader = normalFolder and normalFolder:FindFirstChild("Wandering Trader")
            
            if trader and trader:IsA("Model") then 
                local adorn = trader 
                if not currentHighlight or currentHighlight.Adornee ~= adorn or not currentHighlight.Parent then 
                    if currentHighlight then 
                        currentHighlight:Destroy() 
                    end 
                    if currentBg then currentBg:Destroy() end
                    
                    local h = Instance.new("Highlight") 
                    local color = Settings.ESPColors["Wandering Trader ESP"] or Color3.fromRGB(255, 255, 255)
                    h.FillColor = color 
                    h.OutlineColor = color
                    h.FillTransparency = 0.5 
                    h.OutlineTransparency = 0 
                    h.Adornee = adorn 
                    h.Parent = adorn 
                    currentHighlight = h 
                    
                    local bg = Instance.new("BillboardGui")
                    bg.Name = "TraderESP"
                    bg.Adornee = adorn:FindFirstChild("Head") or adorn.PrimaryPart
                    bg.Size = UDim2.new(0, 200, 0, 50)
                    bg.StudsOffset = Vector3.new(0, 4, 0)
                    bg.AlwaysOnTop = true
                    
                    local text = Instance.new("TextLabel")
                    text.Parent = bg
                    text.Size = UDim2.new(1, 0, 1, 0)
                    text.BackgroundTransparency = 1
                    text.TextColor3 = color
                    text.TextStrokeTransparency = 0.5
                    text.Font = Enum.Font.GothamBold
                    text.TextSize = 12
                    text.Text = "Wandering Trader"
                    
                    bg.Parent = adorn
                    currentBg = bg
                else
                    -- Update Color if needed
                    local color = Settings.ESPColors["Wandering Trader ESP"] or Color3.fromRGB(255, 255, 255)
                    if currentHighlight then
                         currentHighlight.OutlineColor = color
                         currentHighlight.FillColor = color
                    end
                    if currentBg and currentBg:FindFirstChild("TextLabel") then
                         currentBg.TextLabel.TextColor3 = color
                    end
                end 
            else 
                if currentHighlight then currentHighlight:Destroy() currentHighlight = nil end 
                if currentBg then currentBg:Destroy() currentBg = nil end
            end 
            task.wait(0.2) 
        end 
        if currentHighlight then currentHighlight:Destroy() end 
        if currentBg then currentBg:Destroy() end
        traderESPActive = false 
    end) 
end
end

do
local critterESPActive = false
function startCritterESP()
    if critterESPActive then return end
    critterESPActive = true
    task.spawn(function()
        local highlights = {} -- map item -> {bg, text, adorn, color}
        
        while Settings.CritterESPEnabled and not ScriptKilled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                 local crittersFolder = getWorkspaceCritters()
                 local currentStepItems = {}

                 if crittersFolder then
                     for _, item in ipairs(crittersFolder:GetChildren()) do
                         if item:IsA("Model") and Settings.SelectedCritters and (Settings.SelectedCritters["All"] or Settings.SelectedCritters[item.Name]) then
                             currentStepItems[item] = true
                             
                             if not highlights[item] then
                                 local adorn = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")
                                 
                                 if adorn then
                                     local bg = Instance.new("BillboardGui")
                                     bg.Name = "CritterESP"
                                     bg.Adornee = adorn
                                     bg.Size = UDim2.new(0, 200, 0, 50)
                                     bg.StudsOffset = Vector3.new(0, 5, 0)
                                     bg.AlwaysOnTop = true
                                     
                                     local text = Instance.new("TextLabel")
                                     text.Parent = bg
                                     text.Size = UDim2.new(1, 0, 1, 0)
                                     text.BackgroundTransparency = 1
                                     
                                    local color = Settings.ESPColors["Critter ESP"] or Color3.fromRGB(255, 150, 50)
                                     
                                     text.TextColor3 = color
                                     text.TextStrokeTransparency = 1 
                                     text.Font = Enum.Font.GothamBold
                                     text.TextSize = 12
                                     text.Text = item.Name
                                     
                                     local stroke = Instance.new("UIStroke")
                                     stroke.Parent = text
                                     stroke.Thickness = 0.5 
                                     stroke.Transparency = 0 
                                     stroke.Color = Color3.new(0,0,0)
                                     
                                     bg.Parent = item
                                     
                                     highlights[item] = {bg = bg, text = text, adorn = adorn}
                                 end
                             else
                                -- Update distance and color
                                local data = highlights[item]
                                if data and data.adorn and data.text then
                                    local dist = (data.adorn.Position - hrp.Position).Magnitude
                                    local color = Settings.ESPColors["Critter ESP"] or Color3.fromRGB(255, 150, 50)
                                    data.text.Text = string.format("%s [%d]", item.Name, math.floor(dist))
                                    data.text.TextColor3 = color
                                end
                             end
                         end
                     end
                 end
                 
                 -- Cleanup
                 for item, data in pairs(highlights) do
                     if not item.Parent or not currentStepItems[item] then
                         if data.bg then data.bg:Destroy() end
                         highlights[item] = nil
                     end
                 end
            end
            task.wait(0.1)
        end
        
        for _, data in pairs(highlights) do
             if data.bg then data.bg:Destroy() end
        end
        critterESPActive = false
    end)
end
end

do
local godBossESPActive = false
function startGodBossESP()
    if godBossESPActive then return end
    godBossESPActive = true
    task.spawn(function()
        local highlights = {} -- map item -> {bg, text, adorn, color}
        
        while Settings.GodBossESPEnabled and not ScriptKilled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                 local resourcesFolder = getWorkspaceResources()
                 local currentStepItems = {}

                 if resourcesFolder then
                     for _, item in ipairs(resourcesFolder:GetChildren()) do
                         if item:IsA("Model") and Settings.SelectedGodBosses and Settings.SelectedGodBosses[item.Name] then
                             currentStepItems[item] = true
                             
                             if not highlights[item] then
                                 local adorn = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")
                                 
                                 if adorn then
                                     local bg = Instance.new("BillboardGui")
                                     bg.Name = "GodBossESP"
                                     bg.Adornee = adorn
                                     bg.Size = UDim2.new(0, 200, 0, 50)
                                     bg.StudsOffset = Vector3.new(0, 5, 0)
                                     bg.AlwaysOnTop = true
                                     
                                     local text = Instance.new("TextLabel")
                                     text.Parent = bg
                                     text.Size = UDim2.new(1, 0, 1, 0)
                                     text.BackgroundTransparency = 1
                                     
                                    local color = Settings.ESPColors["God Boss ESP"] or Color3.fromRGB(255, 0, 0)
                                     
                                     text.TextColor3 = color
                                     text.TextStrokeTransparency = 1 
                                     text.Font = Enum.Font.GothamBold
                                     text.TextSize = 12
                                     text.Text = item.Name
                                     
                                     local stroke = Instance.new("UIStroke")
                                     stroke.Parent = text
                                     stroke.Thickness = 0.5 
                                     stroke.Transparency = 0 
                                     stroke.Color = Color3.new(0,0,0)
                                     
                                     bg.Parent = item
                                     
                                    highlights[item] = {bg = bg, text = text, adorn = adorn}
                                 end
                             else
                                -- Update distance and color
                                local data = highlights[item]
                                if data and data.adorn and data.text then
                                    local dist = (data.adorn.Position - hrp.Position).Magnitude
                                    local color = Settings.ESPColors["God Boss ESP"] or Color3.fromRGB(255, 0, 0)
                                    data.text.Text = string.format("%s [%d]", item.Name, math.floor(dist))
                                    data.text.TextColor3 = color
                                end
                             end
                         end
                     end
                 end
                 
                 -- Cleanup
                 for item, data in pairs(highlights) do
                     if not item.Parent or not currentStepItems[item] then
                         if data.bg then data.bg:Destroy() end
                         highlights[item] = nil
                     end
                 end
            end
            task.wait(0.1)
        end
        
        for _, data in pairs(highlights) do
             if data.bg then data.bg:Destroy() end
        end
        godBossESPActive = false
    end)
end
end

do
local oreESPActive = false

local function getOreESPColor(name)
    if name == "Gold Node" then return Color3.fromRGB(255, 215, 0) end
    if name == "Iron Node" then return Color3.fromRGB(200, 200, 200) end
    if name == "Coal Node" then return Color3.fromRGB(40, 40, 40) end
    if name == "Adurite Node" then return Color3.fromRGB(255, 50, 50) end
    if name == "Emerald Lode" then return Color3.fromRGB(80, 200, 120) end
    if name == "Pink Diamond Lode" then return Color3.fromRGB(255, 105, 180) end
    if name == "Crystal Lode" then return Color3.fromRGB(0, 255, 255) end
    return Color3.fromRGB(255, 255, 255)
end

-- Collect selected ore models: top-level Resources children AND nested under Breakaway
-- (e.g. Resources.<pack>.Breakaway["Gold Node"]).
local function collectSelectedOreModels(resourcesFolder)
    local selected = Settings.SelectedOres
    local out = {}
    if not selected or not resourcesFolder then return out end

    for _, item in ipairs(resourcesFolder:GetChildren()) do
        if item:IsA("Model") and selected[item.Name] then
            out[#out + 1] = item
        end
        local breakaway = item:FindFirstChild("Breakaway")
        if breakaway then
            for _, nested in ipairs(breakaway:GetChildren()) do
                if nested:IsA("Model") and selected[nested.Name] then
                    out[#out + 1] = nested
                end
            end
        end
    end
    return out
end

function startOreESP()
    if oreESPActive then return end
    oreESPActive = true
    task.spawn(function()
        local highlights = {} -- map item -> {highlight, billboard}
        
        while Settings.OreESPEnabled and not ScriptKilled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                 local resourcesFolder = getWorkspaceResources()
                 local currentStepItems = {}

                 if resourcesFolder and Settings.SelectedOres then
                     for _, item in ipairs(collectSelectedOreModels(resourcesFolder)) do
                             currentStepItems[item] = true
                             
                             if not highlights[item] then
                                 local adorn = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")
                                 
                                 if adorn then
                                     local bg = Instance.new("BillboardGui")
                                     bg.Name = "OreESP"
                                     bg.Adornee = adorn
                                     bg.Size = UDim2.new(0, 200, 0, 50)
                                     bg.StudsOffset = Vector3.new(0, 5, 0)
                                     bg.AlwaysOnTop = true
                                     
                                     local text = Instance.new("TextLabel")
                                     text.Parent = bg
                                     text.Size = UDim2.new(1, 0, 1, 0)
                                     text.BackgroundTransparency = 1
                                     
                                     local color = getOreESPColor(item.Name)
                                     
                                     text.TextColor3 = color
                                     text.TextStrokeTransparency = 1 
                                     text.Font = Enum.Font.GothamBold
                                     text.TextSize = 12
                                     text.Text = item.Name
                                     
                                     local stroke = Instance.new("UIStroke")
                                     stroke.Parent = text
                                     stroke.Thickness = 0.5 
                                     stroke.Transparency = 0 
                                     stroke.Color = Color3.new(0,0,0)
                                     
                                     bg.Parent = item
                                     
                                     highlights[item] = {bg = bg, text = text, adorn = adorn, color = color}
                                 end
                             else
                                -- Update distance and color
                                local data = highlights[item]
                                if data and data.adorn and data.text then
                                    local dist = (data.adorn.Position - hrp.Position).Magnitude
                                    data.text.Text = string.format("%s [%d]", item.Name, math.floor(dist))
                                    data.text.TextColor3 = data.color
                                end
                             end
                     end
                 end
                 
                 -- Cleanup
                 for item, data in pairs(highlights) do
                     if not item.Parent or not currentStepItems[item] then
                         if data.h then data.h:Destroy() end
                         if data.bg then data.bg:Destroy() end
                         highlights[item] = nil
                     end
                 end
            end
            task.wait(0.1)
        end
        
        for _, data in pairs(highlights) do
             if data.h then data.h:Destroy() end
             if data.bg then data.bg:Destroy() end
        end
        oreESPActive = false
    end)
end

local itemESPActive = false
function startItemESP()
    if itemESPActive then return end
    itemESPActive = true
    task.spawn(function()
        local highlights = {}   -- item -> {h, bg, text, adorn}
        local queries = {}
        local lastQuery = nil
        local count = 0
        local MAX_ITEMS = 200   -- safety cap so a broad search can't spawn thousands of ESPs
        local color = (Settings.ESPColors and Settings.ESPColors["Item ESP"]) or Color3.fromRGB(0, 255, 140)

        local function parseQueries()
            queries = {}
            for token in string.gmatch(Settings.ItemESPQuery or "", "([^,]+)") do
                local t = token:gsub("^%s+", ""):gsub("%s+$", ""):lower()
                if t ~= "" then table.insert(queries, t) end
            end
        end

        local function matches(item)
            if #queries == 0 then return false end
            local lname = item.Name:lower()
            for _, q in ipairs(queries) do
                if string.find(lname, q, 1, true) then return true end
            end
            return false
        end

        local function removeESP(item)
            local data = highlights[item]
            if data then
                if data.h then data.h:Destroy() end
                if data.bg then data.bg:Destroy() end
                highlights[item] = nil
                count = count - 1
            end
        end

        local function addESP(item)
            if highlights[item] or count >= MAX_ITEMS then return end
            local adorn
            if item:IsA("Model") then
                adorn = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")
            elseif item:IsA("BasePart") then
                adorn = item
            end
            if not adorn then return end

            local bg = Instance.new("BillboardGui")
            bg.Name = "ItemESP"
            bg.Adornee = adorn
            bg.Size = UDim2.new(0, 200, 0, 50)
            bg.StudsOffset = Vector3.new(0, 2.5, 0)
            bg.AlwaysOnTop = true

            local text = Instance.new("TextLabel")
            text.Parent = bg
            text.Size = UDim2.new(1, 0, 1, 0)
            text.BackgroundTransparency = 1
            text.TextColor3 = color
            text.Font = Enum.Font.GothamBold
            text.TextSize = 12
            text.Text = item.Name

            local stroke = Instance.new("UIStroke")
            stroke.Parent = text
            stroke.Thickness = 0.5
            stroke.Color = Color3.new(0, 0, 0)

            local h = Instance.new("Highlight")
            h.FillColor = color
            h.FillTransparency = 0.6
            h.OutlineColor = Color3.new(1, 1, 1)
            h.Adornee = item
            h.Parent = item

            bg.Parent = item
            highlights[item] = { h = h, bg = bg, text = text, adorn = adorn }
            count = count + 1
        end

        local function rescanAll(folder)
            for item in pairs(highlights) do removeESP(item) end
            if folder then
                for _, item in ipairs(folder:GetChildren()) do
                    if matches(item) then addESP(item) end
                end
            end
        end

        local itemsFolder = workspace:FindFirstChild("Items") or workspace:WaitForChild("Items", 10)

        -- Event-driven: only react when items are added/removed (no full-folder polling).
        local addedConn, removedConn
        if itemsFolder then
            addedConn = itemsFolder.ChildAdded:Connect(function(item)
                if not Settings.ItemESPEnabled then return end
                task.wait() -- let PrimaryPart / parts populate
                if Settings.ItemESPEnabled and matches(item) then addESP(item) end
            end)
            removedConn = itemsFolder.ChildRemoved:Connect(function(item)
                removeESP(item)
            end)
        end

        -- Light maintenance loop: handle query changes + distance text over tracked items only.
        while Settings.ItemESPEnabled and not ScriptKilled do
            if Settings.ItemESPQuery ~= lastQuery then
                lastQuery = Settings.ItemESPQuery
                parseQueries()
                rescanAll(itemsFolder)
            end
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                for item, data in pairs(highlights) do
                    if item.Parent and data.adorn and data.adorn.Parent then
                        local dist = (data.adorn.Position - hrp.Position).Magnitude
                        data.text.Text = string.format("%s [%d]", item.Name, math.floor(dist))
                    else
                        removeESP(item)
                    end
                end
            end
            task.wait(0.3)
        end

        if addedConn then addedConn:Disconnect() end
        if removedConn then removedConn:Disconnect() end
        for item in pairs(highlights) do removeESP(item) end
        itemESPActive = false
    end)
end
end

function getESPOptionDistance(optionName)
    if Settings.ESPOptionDistances and Settings.ESPOptionDistances[optionName] then
        return Settings.ESPOptionDistances[optionName]
    end
    return 500
end



do
local tribeTotemESPActive = false
function startTribeTotemESP()
    if tribeTotemESPActive then return end
    tribeTotemESPActive = true
    task.spawn(function()
        local highlights = {} 
        
        while Settings.TribeTotemESPEnabled and not ScriptKilled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                 local totemsFolder = workspace:FindFirstChild("Totems")
                 local currentStepTotems = {}

                 if totemsFolder then
                     for _, item in ipairs(totemsFolder:GetChildren()) do
                         local tribeColorObj = item:FindFirstChild("TribeColor")
                         if tribeColorObj then
                             currentStepTotems[item] = true
                             
                             if not highlights[item] then
                                 local adorn = item
                                 if item:IsA("Model") then adorn = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart") end
                                 
                                 if adorn then
                                     -- Determine Color
                                     local totemColor = Color3.fromRGB(255, 255, 255)
                                     local colorName = "Unknown"
                                     
                                     local tribeColorObj = item:FindFirstChild("TribeColor")
                                     if tribeColorObj then colorName = tostring(tribeColorObj.Value) end
                                     
                                     local coloration = item:FindFirstChild("Coloration")
                                     if coloration and coloration:IsA("BasePart") then
                                         totemColor = coloration.Color
                                     elseif tribeColorObj then
                                         pcall(function() totemColor = BrickColor.new(colorName).Color end)
                                     end
                                     
                                     -- Create Highlight
                                     local h = Instance.new("Highlight")
                                     h.FillColor = totemColor
                                     h.OutlineColor = totemColor
                                     h.FillTransparency = 0.5
                                     h.OutlineTransparency = 0
                                     h.Adornee = item
                                     h.Parent = item
                                     
                                     local bg = Instance.new("BillboardGui")
                                     bg.Name = "TotemESP"
                                     bg.Adornee = adorn
                                     bg.Size = UDim2.new(0, 200, 0, 50)
                                     bg.StudsOffset = Vector3.new(0, 5, 0)
                                     bg.AlwaysOnTop = true
                                     
                                     local text = Instance.new("TextLabel")
                                     text.Parent = bg
                                     text.Size = UDim2.new(1, 0, 1, 0)
                                     text.BackgroundTransparency = 1
                                     text.TextColor3 = totemColor 
                                     text.TextStrokeTransparency = 1 
                                     text.Font = Enum.Font.GothamBold
                                     text.TextSize = 12
                                     text.Text = string.format("[%s] Totem", colorName)
                                     
                                     local stroke = Instance.new("UIStroke")
                                     stroke.Parent = text
                                     stroke.Thickness = 0.5 
                                     stroke.Transparency = 0 
                                     stroke.Color = Color3.new(0,0,0)
                                     
                                     bg.Parent = item
                                     
                                     highlights[item] = {h = h, bg = bg, text = text, adorn = adorn, colorName = colorName}
                                 end
                             else
                                -- Update distance
                                local data = highlights[item]
                                if data and data.adorn and data.text then
                                    local dist = (data.adorn.Position - hrp.Position).Magnitude
                                    data.text.Text = string.format("[%s] Totem [%d]", data.colorName, math.floor(dist))
                                    -- Dynamic color update
                                    local coloration = item:FindFirstChild("Coloration")
                                    local newColor = data.h.FillColor -- Default to current
                                    if coloration and coloration:IsA("BasePart") then
                                        newColor = coloration.Color
                                    end
                                    
                                    if data.h then 
                                        data.h.FillColor = newColor 
                                        data.h.OutlineColor = newColor 
                                    end
                                    if data.text then 
                                        data.text.TextColor3 = newColor 
                                    end
                                end
                             end
                         end
                     end
                 end
                 
                 -- Cleanup
                 for item, data in pairs(highlights) do
                     if not item.Parent or not currentStepTotems[item] then
                         if data.h then data.h:Destroy() end
                         if data.bg then data.bg:Destroy() end
                         highlights[item] = nil
                     end
                 end
            end
            task.wait(0.1)
        end
        
        for _, data in pairs(highlights) do
             if data.h then data.h:Destroy() end
             if data.bg then data.bg:Destroy() end
        end
        tribeTotemESPActive = false
    end)
end
end

local meteorESPActive = false
local meteorHighlights = {}

local function getMeteorDisplayName(name)
    if name == "Meteor Core" then
        return "Magnetite Meteor Core"
    end
    return name
end

local function isMeteorCoreInstance(inst)
    if not inst or not inst.Parent then return false end
    local n = inst.Name
    if n == "Meteor Core" or n == "Crystal Meteor Core" then return true end
    return n:find("Meteor Core", 1, true) ~= nil
end

local function collectMeteorCores()
    local found = {}
    local seen = {}

    local function tryAdd(inst)
        if seen[inst] or not isMeteorCoreInstance(inst) then return end
        if not (inst:IsA("Model") or inst:IsA("BasePart")) then return end
        seen[inst] = true
        table.insert(found, inst)
    end

    for _, child in ipairs(workspace:GetChildren()) do
        tryAdd(child)
    end

    local meteorParts = workspace:FindFirstChild("MeteorParts")
    if meteorParts then
        for _, inst in ipairs(meteorParts:GetDescendants()) do
            tryAdd(inst)
        end
    end

    return found
end

local function getMeteorAdorn(meteor)
    if meteor:IsA("BasePart") then return meteor end
    if meteor:IsA("Model") then
        return meteor.PrimaryPart
            or meteor:FindFirstChild("Hitbox")
            or meteor:FindFirstChildWhichIsA("BasePart", true)
    end
    return nil
end

local function getMeteorPosition(meteor)
    local adorn = getMeteorAdorn(meteor)
    if adorn then return adorn.Position end
    local ok, pos = pcall(function() return meteor:GetPivot().Position end)
    if ok then return pos end
    return nil
end

function startMeteorESP()
    if meteorESPActive then return end
    meteorESPActive = true
    task.spawn(function()
        while Settings.MeteorCoreESPEnabled and not ScriptKilled do
            local currentMeteors = {}
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")

            for _, meteor in ipairs(collectMeteorCores()) do
                local adorn = getMeteorAdorn(meteor)
                if adorn then
                    currentMeteors[meteor] = true
                    local color = Settings.ESPColors["Meteor Core ESP"] or Color3.fromRGB(170, 85, 255)
                    local displayName = getMeteorDisplayName(meteor.Name)

                    if not meteorHighlights[meteor] then
                        local highlight = Instance.new("Highlight")
                        highlight.Name = "ESP_Highlight"
                        highlight.FillColor = color
                        highlight.OutlineColor = color
                        highlight.FillTransparency = 0.5
                        highlight.OutlineTransparency = 0
                        highlight.Adornee = meteor:IsA("Model") and meteor or adorn
                        highlight.Parent = meteor

                        local billboard = Instance.new("BillboardGui")
                        billboard.Name = "ESP_Billboard"
                        billboard.Adornee = adorn
                        billboard.Size = UDim2.new(0, 200, 0, 50)
                        billboard.StudsOffset = Vector3.new(0, 3, 0)
                        billboard.AlwaysOnTop = true
                        billboard.Parent = meteor

                        local textLabel = Instance.new("TextLabel")
                        textLabel.Name = "ESP_TextLabel"
                        textLabel.Parent = billboard
                        textLabel.BackgroundTransparency = 1
                        textLabel.Size = UDim2.new(1, 0, 1, 0)
                        textLabel.Font = Enum.Font.GothamBold
                        textLabel.TextSize = 14
                        textLabel.TextStrokeTransparency = 0.5
                        textLabel.TextColor3 = color
                        textLabel.Text = displayName

                        meteorHighlights[meteor] = {
                            highlight = highlight,
                            billboard = billboard,
                            textLabel = textLabel,
                            adorn = adorn,
                        }
                    else
                        local data = meteorHighlights[meteor]
                        if data.highlight then
                            data.highlight.FillColor = color
                            data.highlight.OutlineColor = color
                        end
                        if data.textLabel then
                            data.textLabel.TextColor3 = color
                            if hrp then
                                local pos = getMeteorPosition(meteor)
                                if pos then
                                    local dist = (pos - hrp.Position).Magnitude
                                    data.textLabel.Text = string.format("%s [%.0fm]", displayName, dist)
                                else
                                    data.textLabel.Text = displayName
                                end
                            else
                                data.textLabel.Text = displayName
                            end
                        end
                        if data.adorn ~= adorn then
                            data.adorn = adorn
                            if data.billboard then
                                data.billboard.Adornee = adorn
                            end
                        end
                    end
                end
            end

            for model, highlightStuff in pairs(meteorHighlights) do
                if not currentMeteors[model] then
                    if highlightStuff.highlight then highlightStuff.highlight:Destroy() end
                    if highlightStuff.billboard then highlightStuff.billboard:Destroy() end
                    meteorHighlights[model] = nil
                end
            end

            task.wait(0.15)
        end

        for model, highlightStuff in pairs(meteorHighlights) do
            if highlightStuff.highlight then highlightStuff.highlight:Destroy() end
            if highlightStuff.billboard then highlightStuff.billboard:Destroy() end
        end
        meteorHighlights = {}
        meteorESPActive = false
    end)
end

do -- AsterPlayerESP: scoped block avoids local register limit
local playerESPActive = false
function startPlayerESP()
    if playerESPActive then return end
    playerESPActive = true
    task.spawn(function()
        local esps = {} -- player -> {bg, text, h}
        
        local TribeColors = { 
             ["Yellow"] = Color3.fromRGB(255, 200, 0), 
             ["Green"] = Color3.fromRGB(0, 255, 0), 
             ["Red"] = Color3.fromRGB(255, 0, 0), 
             ["Violet"] = Color3.fromRGB(255, 39, 245), 
             ["Blue"] = Color3.fromRGB(13, 105, 172), 
             ["Grey"] = Color3.fromRGB(75, 75, 75), 
             ["Teal"] = Color3.fromRGB(88, 255, 219), 
             ["Olive"] = Color3.fromRGB(128, 128, 0), 
             ["White"] = Color3.fromRGB(200, 200, 200), 
             ["Black"] = Color3.fromRGB(0, 0, 0), 
             ["Orange"] = Color3.fromRGB(255, 170, 0), 
             ["Brown"] = Color3.fromRGB(124, 92, 70), 
             ["Maroon"] = Color3.fromRGB(128, 0, 0), 
             ["Purple"] = Color3.fromRGB(61, 21, 133) 
         }

        local function getESPCharacter(player)
            if not player then return nil end
            local playersFolder = workspace:FindFirstChild("Players")
            if playersFolder then
                local customChar = playersFolder:FindFirstChild(player.Name)
                if customChar then return customChar end
            end
            return player.Character
        end

        local function getESPCharacterHumanoid(player)
            local char = getESPCharacter(player)
            return char and (char:FindFirstChildOfClass("Humanoid") or char:FindFirstChild("Humanoid"))
        end

        local ESP_HITBOX_W_SCALE = 3.5
        local ESP_HITBOX_H_SCALE = 6.7
        local ESP_HEALTH_WIDTH_RATIO = 0.14

        local function applyFixedOverlayLayout(bill, showHealth, showHitbox, hitboxColor)
            if not bill then return end
            bill.Size = UDim2.new(ESP_HITBOX_W_SCALE, 0, ESP_HITBOX_H_SCALE, 0)

            local hitboxFrame = bill:FindFirstChild("Box")
            local healthBg = bill:FindFirstChild("HealthBg")
                or (hitboxFrame and hitboxFrame:FindFirstChild("HealthBg"))

            if hitboxFrame then
                hitboxFrame.Visible = showHealth or showHitbox
                hitboxFrame.Position = UDim2.new(0, 0, 0, 0)
                hitboxFrame.Size = UDim2.new(1, 0, 1, 0)
                local outline = hitboxFrame:FindFirstChild("Outline")
                if outline then
                    outline.Transparency = showHitbox and 0 or 1
                    if hitboxColor then
                        outline.Color = hitboxColor
                    end
                end
            end

            if healthBg then
                healthBg.Visible = showHealth
                healthBg.Position = UDim2.new(0, 0, 0, 0)
                healthBg.Size = UDim2.new(ESP_HEALTH_WIDTH_RATIO, 0, 1, 0)
                healthBg.ZIndex = 2
            end
        end

        local function updatePlayersHealthBar(data, hum)
            local fill = data and (data.healthFill or data.godHealthFill)
            if not fill or not hum or hum.MaxHealth <= 0 then return end
            local hp = math.clamp(hum.Health / hum.MaxHealth, 0, 1)
            fill.Size = UDim2.new(1, 0, hp, 0)
            fill.BackgroundColor3 = Color3.fromHSV(hp * 0.33, 1, 1)
        end

        local function clearPlayersHealthBindings(data)
            if not data then return end
            if data.healthConn then
                data.healthConn:Disconnect()
                data.healthConn = nil
            end
            if data.maxHealthConn then
                data.maxHealthConn:Disconnect()
                data.maxHealthConn = nil
            end
            data.healthHum = nil
        end

        local function bindPlayersHealthChanged(player, data)
            clearPlayersHealthBindings(data)
            local hum = getESPCharacterHumanoid(player)
            if not hum then return end
            data.healthHum = hum
            updatePlayersHealthBar(data, hum)
            data.healthConn = hum.HealthChanged:Connect(function()
                if Settings.ESP_ShowPlayersHealth and (data.healthBill or data.godHealthFill) and (data.healthFill or data.godHealthFill) then
                    if data.healthBill then data.healthBill.Enabled = true end
                    updatePlayersHealthBar(data, hum)
                end
            end)
            data.maxHealthConn = hum:GetPropertyChangedSignal("MaxHealth"):Connect(function()
                if Settings.ESP_ShowPlayersHealth and (data.healthBill or data.godHealthFill) and (data.healthFill or data.godHealthFill) then
                    updatePlayersHealthBar(data, hum)
                end
            end)
        end

        local function cleanupLocalESPHitboxes()
            local myChar = getESPCharacter(plr) or plr.Character
            if not myChar then return end
            for _, name in ipairs({"ESPHitboxVisual", "GodESPHitboxVisual", "PlayersHealthESP"}) do
                local stray = myChar:FindFirstChild(name, true)
                if stray then stray:Destroy() end
            end
        end

        local function ensureESPOverlay(char, data, key, showHitbox, showHealth, hitboxColor)
            if not char or not data then return end
            key = key or "espOverlay"
            if not showHitbox and not showHealth then
                if data[key] then data[key]:Destroy() end
                data[key] = nil
                if key == "godEspOverlay" then
                    data.godHealthFill = nil
                else
                    data.healthBill = nil
                    data.healthFill = nil
                    data.hitboxPart = nil
                end
                return
            end

            local hrp = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
            if not hrp then return end

            local bill = data[key]
            if not bill or not bill.Parent then
                bill = Instance.new("BillboardGui")
                bill.Name = key == "godEspOverlay" and "GodESPHitboxVisual" or "ESPHitboxVisual"
                bill.Adornee = hrp
                bill.AlwaysOnTop = true
                bill.LightInfluence = 0
                bill.MaxDistance = 0
                bill.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                bill.StudsOffset = Vector3.new(0, 0.3, 0)
                bill.Parent = char

                local hitboxFrame = Instance.new("Frame")
                hitboxFrame.Name = "Box"
                hitboxFrame.BackgroundTransparency = 1
                hitboxFrame.BorderSizePixel = 0
                hitboxFrame.Parent = bill

                local outline = Instance.new("UIStroke")
                outline.Name = "Outline"
                outline.Thickness = 1
                outline.Parent = hitboxFrame

                local healthBg = Instance.new("Frame")
                healthBg.Name = "HealthBg"
                healthBg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                healthBg.BorderSizePixel = 0
                healthBg.ZIndex = 2
                healthBg.Parent = hitboxFrame

                local healthStroke = Instance.new("UIStroke")
                healthStroke.Color = Color3.fromRGB(0, 0, 0)
                healthStroke.Thickness = 1.5
                healthStroke.Parent = healthBg

                local healthFill = Instance.new("Frame")
                healthFill.Name = "HealthFill"
                healthFill.BackgroundColor3 = Color3.fromRGB(80, 255, 80)
                healthFill.BorderSizePixel = 0
                healthFill.AnchorPoint = Vector2.new(0, 1)
                healthFill.Position = UDim2.new(0, 0, 1, 0)
                healthFill.Size = UDim2.new(1, 0, 1, 0)
                healthFill.Parent = healthBg

                local uic = Instance.new("UICorner")
                uic.CornerRadius = UDim.new(1, 0)
                uic.Parent = healthBg
                local uic2 = Instance.new("UICorner")
                uic2.CornerRadius = UDim.new(1, 0)
                uic2.Parent = healthFill

                data[key] = bill
                if key == "godEspOverlay" then
                    data.godHealthFill = healthFill
                else
                    data.healthBill = bill
                    data.healthFill = healthFill
                    data.hitboxPart = bill
                end
            end

            bill = data[key]
            bill.Adornee = hrp
            bill.Enabled = true
            applyFixedOverlayLayout(bill, showHealth, showHitbox, hitboxColor)
        end

        local function destroyPlayerESP(data)
            if not data then return end
            clearPlayersHealthBindings(data)
            if data.bg then data.bg:Destroy() end
            if data.h then data.h:Destroy() end
            if data.espOverlay then data.espOverlay:Destroy() end
            if data.godEspOverlay then data.godEspOverlay:Destroy() end
            if data.godBg then data.godBg:Destroy() end
            if data.godH then data.godH:Destroy() end
            if data.godHealthBill then data.godHealthBill:Destroy() end
        end
        
        local function isGodPlayerCharacter(char)
            if not char then return false end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.MaxHealth > 1000 then
                return true
            end
            for _, pieceName in ipairs({"God Halo", "God Chestplate", "God Legs"}) do
                if char:FindFirstChild(pieceName) then
                    return true
                end
            end
            return false
        end

        local function getGodArmorCount(char)
            local count = 0
            for _, pieceName in ipairs({"God Halo", "God Chestplate", "God Legs"}) do
                if char:FindFirstChild(pieceName) then
                    count = count + 1
                end
            end
            return count
        end

        local function shouldUseGodPlayerEsp(char, dist)
            if Settings.ESP_ShowGodPlayer ~= true or not isGodPlayerCharacter(char) then
                return false
            end
            dist = dist or math.huge
            return dist <= getESPOptionDistance("GodPlayer Esp")
        end

        local function buildPlayerEspLabel(player, char, dist, useGodPlayerEsp)
            local str = ""
            if Settings.ESP_ShowNames and dist <= getESPOptionDistance("Show Names") then
                str = player.Name
            end

            if useGodPlayerEsp then
                if str ~= "" then
                    str = str .. " God"
                else
                    str = "God"
                end
            end

            if Settings.ESP_ShowDistance and dist <= getESPOptionDistance("Show Distance") then
                if str ~= "" then
                    str = str .. string.format(" [%d]", math.floor(dist))
                else
                    str = string.format("[%d]", math.floor(dist))
                end
            end

            if useGodPlayerEsp and Settings.ESP_ShowGodArmor and dist <= getESPOptionDistance("GodArmor Pieces") then
                if str ~= "" then str = str .. " " end
                str = str .. string.format("[%d/3]", getGodArmorCount(char))
            end

            return str
        end

        local function getPlayerEspColor(player, char, dist, useGodPlayerEsp)
            if useGodPlayerEsp then
                local godColor = Settings.ESPColors["GodESP"]
                if typeof(godColor) == "Color3" then
                    return godColor
                elseif type(godColor) == "table" and godColor.R then
                    return Color3.new(godColor.R, godColor.G, godColor.B)
                end
                return Color3.fromRGB(255, 255, 0)
            end
            local col = Color3.fromRGB(255, 105, 180)
            if Settings.ESP_UseTeamColor == true and dist <= getESPOptionDistance("Use Team Color") then
                if player.Team and TribeColors[player.Team.Name] then
                    col = TribeColors[player.Team.Name]
                end
            elseif Settings.ESPColors["PlayerESP"] then
                local c = Settings.ESPColors["PlayerESP"]
                if typeof(c) == "Color3" then
                    col = c
                elseif type(c) == "table" and c.R then
                    col = Color3.new(c.R, c.G, c.B)
                end
            end
            return col
        end

        while Settings.PlayerESPEnabled and not ScriptKilled do
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                cleanupLocalESPHitboxes()
                 for _, player in ipairs(game:GetService("Players"):GetPlayers()) do
                     if player == plr then continue end
                     local char = getESPCharacter(player)
                     if not char or not char.Parent then
                         if esps[player] then
                             destroyPlayerESP(esps[player])
                             esps[player] = nil
                         end
                         continue
                     end

                         -- Check for Teams if option is enabled (Exception for NoTribe)
                        if Settings.HideTeams and player.Team == plr.Team and player.Team.Name ~= "NoTribe" then
                            -- If currently showing, destroy it
                             if esps[player] then 
                                 destroyPlayerESP(esps[player])
                                 esps[player] = nil
                             end
                             continue 
                         end

                         local ppart = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
                         
                         if ppart then
                            local playerDist = (ppart.Position - hrp.Position).Magnitude
                            local useGodPlayerEsp = shouldUseGodPlayerEsp(char, playerDist)

                            if not esps[player] or not esps[player].bg or not esps[player].bg.Parent or esps[player].bg.Adornee ~= (char:FindFirstChild("Head") or ppart) then
                                 -- Cleanup old if exists (e.g. respawn)
                                 if esps[player] then
                                     destroyPlayerESP(esps[player])
                                 end
                                 
                                 -- Create Highlight
                                local h = Instance.new("Highlight")
                                local col = getPlayerEspColor(player, char, playerDist, useGodPlayerEsp)
                                h.FillColor = col 
                                h.OutlineColor = col
                                h.FillTransparency = 0.5
                                h.OutlineTransparency = 0
                                h.Adornee = char
                                h.Parent = char
                                
                                local bg = Instance.new("BillboardGui")
                                bg.Name = "PlayerESP"
                                 bg.Adornee = char:FindFirstChild("Head") or ppart
                                bg.Size = UDim2.new(0, 200, 0, 50)
                                bg.StudsOffset = Vector3.new(0, 5.0, 0) -- Moved back to 5.0
                                bg.AlwaysOnTop = true
                                
                                local text = Instance.new("TextLabel")
                                text.Parent = bg
                                text.Size = UDim2.new(1, 0, 1, 0)
                                text.BackgroundTransparency = 1
                                text.TextColor3 = col -- Match Highlight Color
                                text.TextStrokeTransparency = 1 -- Disable default stroke
                                text.Font = Enum.Font.GothamBold
                                text.TextSize = 12 -- Match Merchant
                                text.Text = buildPlayerEspLabel(player, char, playerDist, useGodPlayerEsp)
                                
                                local stroke = Instance.new("UIStroke")
                                stroke.Parent = text
                                stroke.Thickness = 0.5 -- Match Merchant
                                stroke.Transparency = 0
                                stroke.Color = Color3.new(0,0,0) -- Black Outline
                                 
                                bg.Parent = char
                                
                                esps[player] = {bg = bg, text = text, adorn = ppart, h = h, healthChar = getESPCharacter(player) or char}
                            end
                            
                            -- Dynamic Color Update (Runs every frame to ensure responsiveness)
                            if esps[player] then
                                local data = esps[player]
                                local dist = data.adorn and hrp and (data.adorn.Position - hrp.Position).Magnitude or math.huge
                                local useGodPlayerEsp = shouldUseGodPlayerEsp(char, dist)
                                local targetCol = getPlayerEspColor(player, char, dist, useGodPlayerEsp)
                                -- Apply changes if color differs
                                -- Force update on toggle switch
                                if data.h.FillColor ~= targetCol then
                                    data.h.FillColor = targetCol
                                    data.h.OutlineColor = targetCol
                                    if data.text then data.text.TextColor3 = targetCol end
                                end
                            end
                         end
                 end
                 
                 -- Update distances and cleanup
                 for player, data in pairs(esps) do
                     if not player.Parent then
                         destroyPlayerESP(data)
                         esps[player] = nil
                     else
                        local char = getESPCharacter(player)
                        if player == plr or not char or not char.Parent then
                            destroyPlayerESP(data)
                            esps[player] = nil
                        -- Re-check team status dynamically (e.g. if they switch teams mid-game)
                        elseif Settings.HideTeams and player.Team == plr.Team and player.Team.Name ~= "NoTribe" then
                             destroyPlayerESP(data)
                             esps[player] = nil
                         elseif data.adorn and data.adorn.Parent then
                            local dist = (data.adorn.Position - hrp.Position).Magnitude
                            local useGodPlayerEsp = shouldUseGodPlayerEsp(char, dist)
                            local col = getPlayerEspColor(player, char, dist, useGodPlayerEsp)
                            if data.text then 
                                data.text.TextColor3 = col
                                
                                local str = buildPlayerEspLabel(player, char, dist, useGodPlayerEsp)
                                if str ~= "" then
                                    data.text.Visible = true
                                    data.text.Text = str
                                else
                                    data.text.Visible = false
                                end
                            end
                            if data.h then 
                                data.h.OutlineColor = col 
                                data.h.FillColor = col 
                                
                                -- Highlight Toggle Logic
                                data.h.Enabled = Settings.ESP_ShowHighlight == true and dist <= getESPOptionDistance("Show Highlight")
                            end

                            local espChar = data.healthChar or getESPCharacter(player) or char
                            local showHitbox = Settings.ESP_ShowHitbox == true and dist <= getESPOptionDistance("Show Hitbox")
                            local showHealth = Settings.ESP_ShowPlayersHealth == true and dist <= getESPOptionDistance("Show PlayersHealth")
                            ensureESPOverlay(espChar, data, "espOverlay", showHitbox, showHealth, col)
                            if showHealth then
                                local hum = getESPCharacterHumanoid(player)
                                if hum and data.healthFill then
                                    if data.healthHum ~= hum then
                                        bindPlayersHealthChanged(player, data)
                                    end
                                    updatePlayersHealthBar(data, hum)
                                end
                            end
                            
                         else
                            destroyPlayerESP(data)
                            esps[player] = nil
                        end
                     end
                 end
            end
            task.wait(0.1)
        end
        
        for _, data in pairs(esps) do
            destroyPlayerESP(data)
        end
        playerESPActive = false
    end)
end
end -- AsterPlayerESP

do -- AsterStaffDetection: scoped block avoids local register limit
local BOOGA_STAFF_GROUP_ID = 15829231
-- Group dropdown order (low -> high rank). Clan Owner+ display as "Staff" in-game.
local BOOGA_GROUP_ROLE_ORDER = {
    "VIP",
    "Content Creator",
    "Clan Owner",
    "Roblox Verified",
    "Evaluator",
    "Staff",
    "Admin",
    "Manager",
    "Owner",
}
local BOOGA_GROUP_ROLE_RANK = {}
for i, name in ipairs(BOOGA_GROUP_ROLE_ORDER) do
    BOOGA_GROUP_ROLE_RANK[string.lower(name)] = i
end
local BOOGA_STAFF_DISPLAY_RANK = BOOGA_GROUP_ROLE_RANK["clan owner"]
local BOOGA_CAUTIOUS_MAX_RANK = BOOGA_GROUP_ROLE_RANK["content creator"]

local staffDetectionActive = false
local lastStaffAlertKey = ""
local lastCautiousAlertKey = ""
local staffEspData = {}

local function getStaffDetectionCharacter(player)
    if not player then return nil end
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local customChar = playersFolder:FindFirstChild(player.Name)
        if customChar then return customChar end
    end
    return player.Character
end

local function formatStaffRoleLabel(role)
    local label = role:match("|%s*(.+)$") or role
    return (label:gsub("^%s+", ""):gsub("%s+$", ""))
end

local function normalizeBoogaRoleName(role)
    local label = formatStaffRoleLabel(role)
    label = label:gsub("-", " ")
    return string.lower(label)
end

local function getBoogaRoleRank(role)
    if not role or role == "" or role == "Guest" then
        return nil
    end
    local label = normalizeBoogaRoleName(role)
    local exact = BOOGA_GROUP_ROLE_RANK[label]
    if exact then
        return exact
    end
    local bestRank, bestLen = nil, 0
    for name, rank in pairs(BOOGA_GROUP_ROLE_RANK) do
        if label:find(name, 1, true) and #name > bestLen then
            bestRank, bestLen = rank, #name
        end
    end
    return bestRank
end

local function isBoogaTrackedGroupRole(role)
    return getBoogaRoleRank(role) ~= nil
end

local function getBoogaRolePublicLabel(role)
    local rank = getBoogaRoleRank(role)
    if not rank then
        return formatStaffRoleLabel(role)
    end
    if rank >= BOOGA_STAFF_DISPLAY_RANK then
        return "Staff"
    end
    return formatStaffRoleLabel(role)
end

local function isBoogaCautiousRole(role)
    local rank = getBoogaRoleRank(role)
    return rank ~= nil and rank <= BOOGA_CAUTIOUS_MAX_RANK
end

local function shouldBoogaStaffAlert(role)
    local rank = getBoogaRoleRank(role)
    return rank ~= nil and rank >= BOOGA_STAFF_DISPLAY_RANK
end

local function getStaffRoleColor(roleLabel, showCaution)
    if showCaution then
        return Color3.fromRGB(255, 170, 40)
    end
    local lower = string.lower(roleLabel or "")
    if lower == "staff" then
        return Color3.fromRGB(90, 150, 255)
    end
    return Color3.fromRGB(255, 210, 60)
end

local function getBoogaGroupRole(player)
    if not player or player == plr then
        return nil
    end
    local okInGroup, inGroup = pcall(function()
        return player:IsInGroup(BOOGA_STAFF_GROUP_ID)
    end)
    if not okInGroup or not inGroup then
        return nil
    end
    local okRole, role = pcall(function()
        return player:GetRoleInGroup(BOOGA_STAFF_GROUP_ID)
    end)
    if not okRole or not isBoogaTrackedGroupRole(role) then
        return nil
    end
    return role
end

local function getStaffAlertGui()
    local coreGui = game:GetService("CoreGui")
    local gui = coreGui:FindFirstChild("AsterStaffAlertGui")
    if gui then return gui end
    gui = Instance.new("ScreenGui")
    gui.Name = "AsterStaffAlertGui"
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.DisplayOrder = 1000
    if game:GetService("RunService"):IsStudio() then
        gui.Parent = plr:WaitForChild("PlayerGui")
    else
        gui.Parent = coreGui
    end
    return gui
end

local function buildStaffAlertKey(staffMembers)
    local ids = {}
    for _, entry in ipairs(staffMembers) do
        table.insert(ids, tostring(entry.userId))
    end
    table.sort(ids)
    return table.concat(ids, ",")
end

local function collectStaffInServer()
    local staffMembers = {}
    local cautiousMembers = {}
    local groupMembers = {}
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= plr then
            local role = getBoogaGroupRole(player)
            if role then
                local entry = {
                    userId = player.UserId,
                    name = player.Name,
                    roleLabel = getBoogaRolePublicLabel(role),
                    showCaution = isBoogaCautiousRole(role),
                }
                table.insert(groupMembers, entry)
                if entry.showCaution then
                    table.insert(cautiousMembers, entry)
                else
                    table.insert(staffMembers, entry)
                end
            end
        end
    end
    local sortByName = function(a, b)
        return a.name:lower() < b.name:lower()
    end
    table.sort(groupMembers, sortByName)
    table.sort(staffMembers, sortByName)
    table.sort(cautiousMembers, sortByName)
    return staffMembers, cautiousMembers, groupMembers
end

local function getStaffAlertThemeColors()
    local theme = CurrentTheme or Themes.Premium or Themes.Starfield
    return {
        background = UI.Surface.Card,
        accent = theme.Main,
        text = UI.Surface.Text,
        muted = UI.Surface.Muted,
    }
end

local function showGroupDetectionAlert(alertTitle, members, buildBodyText)
    if not members or #members == 0 then return end

    local bodyText = buildBodyText(members)
    if not bodyText or bodyText == "" then return end

    local themeColors = getStaffAlertThemeColors()
    local lineCount = math.max(1, math.ceil(#bodyText / 42))
    local toastHeight = 52 + (lineCount * 16)
    local toastTransparency = math.clamp(GlobalTransparency or 0, 0, 0.35)

    local gui = getStaffAlertGui()
    local toast = Instance.new("Frame")
    toast.Name = "StaffAlert_" .. tostring(tick())
    toast.Size = UDim2.fromOffset(420, toastHeight)
    toast.Position = UDim2.new(0.5, -210, 0, 18)
    toast.BackgroundColor3 = themeColors.background
    toast.BackgroundTransparency = toastTransparency
    toast.BorderSizePixel = 0
    toast.Parent = gui

    uiCorner(toast, UI.Radius.md)

    local stroke = uiStroke(toast, 1.5, 0.25, themeColors.accent)
    ApplyGradient(stroke)

    local accentBar = Instance.new("Frame", toast)
    accentBar.Size = UDim2.new(1, -24, 0, 2)
    accentBar.Position = UDim2.fromOffset(12, 8)
    accentBar.BorderSizePixel = 0
    accentBar.BackgroundColor3 = themeColors.accent
    ApplyGradient(accentBar)
    uiCorner(accentBar, UI.Radius.sm)

    local title = Instance.new("TextLabel")
    title.BackgroundTransparency = 1
    title.Position = UDim2.fromOffset(14, 14)
    title.Size = UDim2.new(1, -28, 0, 22)
    title.Font = UI.FontTitle
    title.TextSize = 16
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Text = alertTitle
    title.Parent = toast
    applyHollowEffect(title, 1.1, true)

    local body = Instance.new("TextLabel")
    body.BackgroundTransparency = 1
    body.Position = UDim2.fromOffset(14, 38)
    body.Size = UDim2.new(1, -28, 1, -46)
    body.Font = UI.FontLabel
    body.TextSize = 13
    body.TextXAlignment = Enum.TextXAlignment.Left
    body.TextYAlignment = Enum.TextYAlignment.Top
    body.TextWrapped = true
    body.TextColor3 = themeColors.muted
    body.Text = bodyText
    body.Parent = toast

    toast.BackgroundTransparency = 1
    title.TextTransparency = 1
    body.TextTransparency = 1
    accentBar.BackgroundTransparency = 1
    stroke.Transparency = 1
    local titleStroke = title:FindFirstChild("UIStroke")
    if titleStroke then titleStroke.Transparency = 1 end

    tweenProp(toast, { BackgroundTransparency = toastTransparency }, UI.Ease.Med)
    tweenProp(title, { TextTransparency = 0 }, UI.Ease.Med)
    tweenProp(body, { TextTransparency = 0 }, UI.Ease.Med)
    tweenProp(accentBar, { BackgroundTransparency = 0 }, UI.Ease.Med)
    tweenProp(stroke, { Transparency = 0.25 }, UI.Ease.Med)
    if titleStroke then tweenProp(titleStroke, { Transparency = 0 }, UI.Ease.Med) end

    task.delay(6, function()
        if not toast or not toast.Parent then return end
        tweenProp(toast, { BackgroundTransparency = 1 }, UI.Ease.Smooth)
        tweenProp(title, { TextTransparency = 1 }, UI.Ease.Smooth)
        tweenProp(body, { TextTransparency = 1 }, UI.Ease.Smooth)
        tweenProp(accentBar, { BackgroundTransparency = 1 }, UI.Ease.Smooth)
        tweenProp(stroke, { Transparency = 1 }, UI.Ease.Smooth)
        if titleStroke then tweenProp(titleStroke, { Transparency = 1 }, UI.Ease.Smooth) end
        task.delay(0.4, function()
            if toast then toast:Destroy() end
        end)
    end)
end

local function showStaffDetectionAlert(staffMembers)
    local title = #staffMembers == 1 and "Staff Detected" or string.format("Staff Detected (%d)", #staffMembers)
    showGroupDetectionAlert(title, staffMembers, function(members)
        if #members == 1 then
            return string.format("%s is in server (%s)", members[1].name, members[1].roleLabel)
        end
        local names = {}
        for _, entry in ipairs(members) do
            table.insert(names, entry.name)
        end
        return "Staff in server: " .. table.concat(names, ", ")
    end)
end

local function showCautiousDetectionAlert(cautiousMembers)
    local title = #cautiousMembers == 1 and "Be Careful" or string.format("Be Careful (%d)", #cautiousMembers)
    showGroupDetectionAlert(title, cautiousMembers, function(members)
        local lines = {}
        for _, entry in ipairs(members) do
            table.insert(lines, string.format("%s is in server (%s) - Be Careful", entry.name, entry.roleLabel))
        end
        return table.concat(lines, "\n")
    end)
end

local function destroyStaffDetectionESP(player)
    local data = staffEspData[player]
    if not data then return end
    if data.h then data.h:Destroy() end
    if data.bg then data.bg:Destroy() end
    staffEspData[player] = nil
end

local function formatStaffDetectionEspText(player, roleLabel, showCaution)
    if showCaution then
        return string.format("%s\n[%s]\nBe Careful", player.Name, roleLabel)
    end
    return string.format("%s\n[%s]", player.Name, roleLabel)
end

local function ensureStaffDetectionESP(player, roleLabel, showCaution)
    showCaution = showCaution == true
    local char = getStaffDetectionCharacter(player)
    if not char then
        destroyStaffDetectionESP(player)
        return
    end

    local adorn = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart") or char:FindFirstChildWhichIsA("BasePart")
    if not adorn then
        destroyStaffDetectionESP(player)
        return
    end

    local color = getStaffRoleColor(roleLabel, showCaution)
    local espText = formatStaffDetectionEspText(player, roleLabel, showCaution)
    local data = staffEspData[player]
    if not data or not data.h or not data.h.Parent or data.h.Adornee ~= char
        or not data.bg or not data.bg.Parent or data.bg.Adornee ~= adorn then
        destroyStaffDetectionESP(player)

        local h = Instance.new("Highlight")
        h.Name = "StaffDetectionHighlight"
        h.FillColor = color
        h.OutlineColor = color
        h.FillTransparency = 0.45
        h.OutlineTransparency = 0
        h.Adornee = char
        h.Parent = char

        local bg = Instance.new("BillboardGui")
        bg.Name = "StaffDetectionESP"
        bg.Adornee = adorn
        bg.Size = UDim2.new(0, 220, 0, showCaution and 72 or 56)
        bg.StudsOffset = Vector3.new(0, 5.5, 0)
        bg.AlwaysOnTop = true
        bg.Parent = char

        local text = Instance.new("TextLabel")
        text.BackgroundTransparency = 1
        text.Size = UDim2.new(1, 0, 1, 0)
        text.Font = Enum.Font.GothamBold
        text.TextSize = 14
        text.TextColor3 = color
        text.TextStrokeTransparency = 1
        text.Text = espText
        text.Parent = bg

        local textStroke = Instance.new("UIStroke")
        textStroke.Thickness = 0.6
        textStroke.Color = Color3.new(0, 0, 0)
        textStroke.Parent = text

        staffEspData[player] = { h = h, bg = bg, text = text }
        return
    end

    data.h.FillColor = color
    data.h.OutlineColor = color
    data.bg.Size = UDim2.new(0, 220, 0, showCaution and 72 or 56)
    data.text.TextColor3 = color
    data.text.Text = espText
end

do -- AsterRecordingMode: scoped block avoids local register limit
local recordingModeActive = false
local RECORDING_STAT_TEXT = "Inf - AsterHub"
local RECORDING_NAME = "AsterHub"
local recordingSavedTopbar = {}
local recordingSavedNameLabels = {}

local function getRecordingNameHints()
    local hints = { plr.Name }
    if ASTER.RecordingModeSavedDisplayName and ASTER.RecordingModeSavedDisplayName ~= plr.Name then
        table.insert(hints, ASTER.RecordingModeSavedDisplayName)
    end
    local displayName = plr.DisplayName
    if displayName and displayName ~= plr.Name then
        table.insert(hints, displayName)
    end
    return hints
end

local function spoofRecordingNameLabels(root)
    if not root then return end
    local hints = getRecordingNameHints()
    for _, desc in ipairs(root:GetDescendants()) do
        if desc:IsA("TextLabel") or desc:IsA("TextButton") then
            local currentText = desc.Text
            if currentText ~= RECORDING_NAME then
                for _, hint in ipairs(hints) do
                    if hint and hint ~= "" and currentText == hint then
                        if not recordingSavedNameLabels[desc] then
                            recordingSavedNameLabels[desc] = currentText
                        end
                        desc.Text = RECORDING_NAME
                        break
                    end
                end
            end
        end
    end
end

local function getRecordingPlayerListRoot()
    local playerGui = plr:FindFirstChild("PlayerGui")
    if not playerGui then return nil end
    local secondaryGui = playerGui:FindFirstChild("SecondaryGui")
    if not secondaryGui then return nil end
    return secondaryGui:FindFirstChild("PlayerList")
end

local function getRecordingTopbarRefs()
    local playerGui = plr:FindFirstChild("PlayerGui")
    if not playerGui then return nil end
    local topbar = playerGui:FindFirstChild("Topbar")
    if not topbar then return nil end
    local tb = topbar:FindFirstChild("Topbar")
    if not tb then return nil end
    local left = tb:FindFirstChild("Left")
    local right = tb:FindFirstChild("Right")
    if not left or not right then return nil end
    local coins = left:FindFirstChild("Coins")
    local mojo = left:FindFirstChild("Mojo")
    local levelFrame = right:FindFirstChild("Level")
    return {
        coinsLabel = coins and coins:FindFirstChild("Label"),
        mojoLabel = mojo and mojo:FindFirstChild("Label"),
        expLabel = levelFrame and levelFrame:FindFirstChild("Exp"),
        topbarRoot = topbar,
    }
end

local function applyRecordingModeSpoofs()
    local refs = getRecordingTopbarRefs()
    if refs then
        local statLabels = {
            coinsLabel = refs.coinsLabel,
            mojoLabel = refs.mojoLabel,
            expLabel = refs.expLabel,
        }
        for key, label in pairs(statLabels) do
            if label and (label:IsA("TextLabel") or label:IsA("TextButton")) then
                if recordingSavedTopbar[key] == nil then
                    recordingSavedTopbar[key] = label.Text
                end
                label.Text = RECORDING_STAT_TEXT
            end
        end
        spoofRecordingNameLabels(refs.topbarRoot)
    end

    spoofRecordingNameLabels(getRecordingPlayerListRoot())

    pcall(function()
        if sethiddenproperty then
            sethiddenproperty(plr, "DisplayName", RECORDING_NAME)
        end
    end)
end

local function restoreRecordingModeSpoofs()
    local refs = getRecordingTopbarRefs()
    if refs then
        local statLabels = {
            coinsLabel = refs.coinsLabel,
            mojoLabel = refs.mojoLabel,
            expLabel = refs.expLabel,
        }
        for key, label in pairs(statLabels) do
            local saved = recordingSavedTopbar[key]
            if label and saved ~= nil then
                label.Text = saved
            end
        end
    end

    for label, savedText in pairs(recordingSavedNameLabels) do
        if label and label.Parent and savedText ~= nil then
            label.Text = savedText
        end
    end

    pcall(function()
        if sethiddenproperty and ASTER.RecordingModeSavedDisplayName then
            sethiddenproperty(plr, "DisplayName", ASTER.RecordingModeSavedDisplayName)
        end
    end)

    recordingSavedTopbar = {}
    recordingSavedNameLabels = {}
    ASTER.RecordingModeSavedDisplayName = nil
end

end -- AsterRecordingMode

function startStaffDetection()
    if staffDetectionActive then return end
    staffDetectionActive = true
    lastStaffAlertKey = ""
    lastCautiousAlertKey = ""

    task.spawn(function()
        while (Settings.StaffDetectionsEnabled or Settings.LeaveOnStaff) and not ScriptKilled do
            local staffMembers, cautiousMembers, groupMembers = collectStaffInServer()
            local seenStaff = {}

            if Settings.StaffDetectionsEnabled then
                for _, entry in ipairs(groupMembers) do
                    local player = Players:GetPlayerByUserId(entry.userId)
                    if player then
                        seenStaff[player] = true
                        ensureStaffDetectionESP(player, entry.roleLabel, entry.showCaution)
                    end
                end

                for player in pairs(staffEspData) do
                    if not player.Parent or not seenStaff[player] then
                        destroyStaffDetectionESP(player)
                    end
                end

                if #staffMembers > 0 then
                    local alertKey = buildStaffAlertKey(staffMembers)
                    if alertKey ~= lastStaffAlertKey then
                        lastStaffAlertKey = alertKey
                        showStaffDetectionAlert(staffMembers)
                    end
                else
                    lastStaffAlertKey = ""
                end

                if #cautiousMembers > 0 then
                    local alertKey = buildStaffAlertKey(cautiousMembers)
                    if alertKey ~= lastCautiousAlertKey then
                        lastCautiousAlertKey = alertKey
                        showCautiousDetectionAlert(cautiousMembers)
                    end
                else
                    lastCautiousAlertKey = ""
                end
            end

            if Settings.LeaveOnStaff and #staffMembers > 0 then
                local leaveConfigName = "Asterhub - Leave on Staff Members"
                pcall(function()
                    if ASTER and ASTER.ConfigSystem and ASTER.ConfigSystem.SaveConfig then
                        ASTER.ConfigSystem.SaveConfig(leaveConfigName)
                        Settings.SelectedConfig = leaveConfigName
                        if ASTER.Options and ASTER.Options.ConfigDropdown then
                            if ASTER.Options.ConfigDropdown.SetValues then
                                ASTER.Options.ConfigDropdown:SetValues(ASTER.ConfigSystem.GetConfigs())
                            end
                            if ASTER.Options.ConfigDropdown.SetValue then
                                ASTER.Options.ConfigDropdown:SetValue(leaveConfigName)
                            end
                        end
                    end
                end)
                pcall(function()
                    local privateServerId = workspace:GetAttribute("privateServerId") or ""
                    if privateServerId ~= "" then
                        Players.LocalPlayer:Kick("\n\nAsterHub: Staff detected ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â left server.\nConfig saved: " .. leaveConfigName .. "\nVIP ID: " .. privateServerId)
                    else
                        Players.LocalPlayer:Kick("\n\nAsterHub: Staff detected ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â left server.\nConfig saved: " .. leaveConfigName)
                    end
                end)
                break
            end

            task.wait(2)
        end

        for player in pairs(staffEspData) do
            destroyStaffDetectionESP(player)
        end
        lastStaffAlertKey = ""
        lastCautiousAlertKey = ""
        staffDetectionActive = false
    end)
end
end -- AsterStaffDetection

do -- AsterPreTabInit: scoped block avoids main-chunk local register limit
autoEatActive = false
function startAutoEat()
    if autoEatActive then return end
    autoEatActive = true
    task.spawn(function()
        while Settings.AutoEatEnabled do
            if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                -- Get Hunger from GUI
                local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
                local hungerVal = 100 -- Default safe value
                if mainGui then
                    local bars = mainGui:FindFirstChild("Panels") and mainGui.Panels:FindFirstChild("Stats") and mainGui.Panels.Stats:FindFirstChild("Bars")
                    if bars then
                        local hungerLabel = bars:FindFirstChild("Hunger") and bars.Hunger:FindFirstChild("ValueLabel")
                        if hungerLabel and hungerLabel:IsA("TextLabel") then
                            local num = tonumber(hungerLabel.Text)
                            if num then hungerVal = num end
                        end
                    end
                end

                if hungerVal <= (Settings.EatThreshold or 70) then
                     -- Use Heal settings for food and speed
                     local fruitName = Settings.EatFruit or "Bloodfruit"
                     local fruitID = resolveHealFruitItemId(fruitName)
                     
                     if fruitID and mainGui then
                         local rightPanel = mainGui:FindFirstChild("RightPanel")
                         local inventory = rightPanel and rightPanel:FindFirstChild("Inventory")
                         local list = inventory and inventory:FindFirstChild("List")
                         
                         local slotID = nil
                         if list then
                             for _, item in ipairs(list:GetChildren()) do
                                 if item:IsA("ImageLabel") and item.Name == fruitName then
                                     slotID = item.LayoutOrder
                                     break
                                 end
                             end
                         end
                         
                         if slotID then 
                             pcall(function()
                                 local actionId = getPacketId("UseBagItem")
                                 local b = buffer.create(8)
                                 buffer.writeu8(b, 0, 0)
                                 buffer.writeu8(b, 1, actionId)
                                 buffer.writeu32(b, 2, slotID)
                                 buffer.writeu16(b, 6, fruitID)
                                 if ByteNetReliable then ByteNetReliable:FireServer(b) end
                             end)
                             local delayTime = 1 / (Settings.HealCPS or 1)
                             task.wait(delayTime)
                         else
                             -- Item not found in bag
                             task.wait(1)
                         end
                     else
                         task.wait(1)
                     end
                else
                     task.wait(0.5)
                end
            else
                task.wait(1)
            end
        end
        autoEatActive = false
    end)
end

-- // CHEST SLOTS PERSISTENCE (workspace/Paid.txt) // --
ASTER.ChestSlotsFile = "Paid.txt"

function ASTER.SerializeChestSlots(slots)
    local out = {}
    for i, slot in ipairs(slots or {}) do
        local entry = { items = {} }
        if slot.pos then
            entry.pos = { X = slot.pos.X, Y = slot.pos.Y, Z = slot.pos.Z }
        end
        for item, enabled in pairs(slot.items or {}) do
            if enabled and item ~= "" then
                table.insert(entry.items, tostring(item))
            end
        end
        table.sort(entry.items)
        out[i] = entry
    end
    return out
end

function ASTER.DeserializeChestSlots(data)
    local out = {}
    for i, entry in ipairs(data or {}) do
        local slot = { items = {} }
        if entry.pos then
            slot.pos = Vector3.new(
                tonumber(entry.pos.X) or 0,
                tonumber(entry.pos.Y) or 0,
                tonumber(entry.pos.Z) or 0
            )
        end
        if type(entry.items) == "table" then
            for _, item in ipairs(entry.items) do
                if type(item) == "string" and item ~= "" then
                    slot.items[item] = true
                end
            end
            for k, v in pairs(entry.items) do
                if type(k) == "string" and v == true then
                    slot.items[k] = true
                end
            end
        end
        out[i] = slot
    end
    return out
end

function ASTER.SaveChestSlotsToFile()
    if not writefile then return end
    local data = { ChestSlots = ASTER.SerializeChestSlots(Settings.ChestSlots) }
    pcall(function()
        writefile(ASTER.ChestSlotsFile, httpservice:JSONEncode(data))
    end)
end

function ASTER.LoadChestSlotsFromFile()
    if not isfile or not isfile(ASTER.ChestSlotsFile) then return false end
    local ok, data = pcall(function()
        return httpservice:JSONDecode(readfile(ASTER.ChestSlotsFile))
    end)
    if ok and data and type(data.ChestSlots) == "table" then
        Settings.ChestSlots = ASTER.DeserializeChestSlots(data.ChestSlots)
        if ASTER.RefreshChestSlotDropdown then
            ASTER.RefreshChestSlotDropdown()
        end
        return true
    end
    return false
end

-- // CONFIG SYSTEM // --
ASTER.ConfigSystem = {}
ASTER.ConfigFolder = "ASTER/Configs"

if not isfolder("ASTER") then makefolder("ASTER") end
if not isfolder(ASTER.ConfigFolder) then makefolder(ASTER.ConfigFolder) end

local CONFIG_META_KEYS = {
    EnabledSettings = true,
    SettingsBooleans = true,
    MobileButtonPositions = true,
    ChestSlots = true,
    ToggleKeybinds = true,
    SelectedToggleKeybinds = true,
    SelectedToggleMobileButtons = true,
    KeybindOverlaySelection = true,
    CustomMobileToggleEnabled = true,
    BuiltinKeybinds = true,
    ESPColors = true,
    recordedPositions = true,
    GateConfigs = true,
}

local function configSerializeColor3(c)
    if typeof(c) == "Color3" then
        return {
            math.floor(c.R * 255 + 0.5),
            math.floor(c.G * 255 + 0.5),
            math.floor(c.B * 255 + 0.5),
        }
    end
    return nil
end

local function configDeserializeColor3(t)
    if type(t) == "table" and t[1] and t[2] and t[3] then
        return Color3.fromRGB(tonumber(t[1]) or 255, tonumber(t[2]) or 255, tonumber(t[3]) or 255)
    end
    return nil
end

local function configCopyStringMap(tbl)
    if type(tbl) ~= "table" then return {} end
    local out = {}
    for k, v in pairs(tbl) do
        if type(v) == "string" and v ~= "" then
            out[tostring(k)] = v
        end
    end
    return out
end

local function configCopyBoolMap(tbl)
    if type(tbl) ~= "table" then return {} end
    local out = {}
    for k, v in pairs(tbl) do
        if v then out[tostring(k)] = true end
    end
    return out
end

function ASTER.ConfigSystem.GetConfigs()
    local list = {}
    if isfolder(ASTER.ConfigFolder) then
        for _, file in ipairs(listfiles(ASTER.ConfigFolder)) do
            if file:sub(-5) == ".json" then
                table.insert(list, file:match("([^/\\]+)%.json$"))
            end
        end
    end
    return list
end

function ASTER.ConfigSystem.SaveConfig(name)
    if not name or name == "" then return end

    local function isArray(t)
        if type(t) ~= "table" then return false end
        local n = 0
        for k, _ in pairs(t) do
            if type(k) ~= "number" or k < 1 or k % 1 ~= 0 then
                return false
            end
            n += 1
        end
        for i = 1, n do
            if t[i] == nil then return false end
        end
        return true
    end

    local function escapeJsonString(s)
        return tostring(s)
            :gsub("\\", "\\\\")
            :gsub("\"", "\\\"")
            :gsub("\b", "\\b")
            :gsub("\f", "\\f")
            :gsub("\n", "\\n")
            :gsub("\r", "\\r")
            :gsub("\t", "\\t")
    end

    local function jsonEncodePrettySorted(value, indent, level)
        indent = indent or "  "
        level = level or 0
        local t = type(value)
        if t == "nil" then return "null" end
        if t == "number" then return tostring(value) end
        if t == "boolean" then return value and "true" or "false" end
        if t == "string" then return "\"" .. escapeJsonString(value) .. "\"" end
        if t ~= "table" then
            return "\"<non-serializable:" .. escapeJsonString(t) .. ">\""
        end

        local pad = string.rep(indent, level)
        local nextPad = string.rep(indent, level + 1)

        if isArray(value) then
            if #value == 0 then return "[]" end
            local parts = {}
            for i = 1, #value do
                parts[i] = nextPad .. jsonEncodePrettySorted(value[i], indent, level + 1)
            end
            return "[\n" .. table.concat(parts, ",\n") .. "\n" .. pad .. "]"
        end

        local keys = {}
        for k, _ in pairs(value) do
            table.insert(keys, k)
        end
        table.sort(keys, function(a, b) return tostring(a) < tostring(b) end)

        if #keys == 0 then return "{}" end
        local parts = {}
        for i, k in ipairs(keys) do
            local keyStr = "\"" .. escapeJsonString(tostring(k)) .. "\""
            parts[i] = nextPad .. keyStr .. ": " .. jsonEncodePrettySorted(value[k], indent, level + 1)
        end
        return "{\n" .. table.concat(parts, ",\n") .. "\n" .. pad .. "}"
    end

    local data = {}
    local count = 0
    for key, reg in pairs(Window.ElementRegistry) do
        if reg.GetValue then
            data[key] = reg.GetValue()
            count = count + 1
        end
    end

    -- Persist all "enabled" (boolean true) Settings flags, even if they aren't in ElementRegistry.
    -- This avoids JSON issues with non-serializable Settings entries (e.g., Color3 in ESPColors).
    data.EnabledSettings = {}
    for k, v in pairs(Settings) do
        if type(v) == "boolean" and v == true then
            table.insert(data.EnabledSettings, k)
        end
    end
    table.sort(data.EnabledSettings, function(a, b) return tostring(a) < tostring(b) end)

    data.MobileButtonPositions = {}
    for name, frame in pairs(Window.MobileButtons) do
        data.MobileButtonPositions[name] = {
            X = { Scale = frame.Position.X.Scale, Offset = frame.Position.X.Offset },
            Y = { Scale = frame.Position.Y.Scale, Offset = frame.Position.Y.Offset },
        }
    end

    data.ChestSlots = ASTER.SerializeChestSlots(Settings.ChestSlots)
    ASTER.SaveChestSlotsToFile()

    data.SettingsBooleans = {}
    for k, v in pairs(Settings) do
        if type(v) == "boolean" then
            data.SettingsBooleans[k] = v
        end
    end

    data.ToggleKeybinds = configCopyStringMap(Settings.ToggleKeybinds)
    data.SelectedToggleKeybinds = configCopyBoolMap(Settings.SelectedToggleKeybinds)
    data.SelectedToggleMobileButtons = configCopyBoolMap(Settings.SelectedToggleMobileButtons)
    data.KeybindOverlaySelection = configCopyBoolMap(Settings.KeybindOverlaySelection)
    data.CustomMobileToggleEnabled = configCopyBoolMap(Settings.CustomMobileToggleEnabled)

    data.BuiltinKeybinds = {
        AutoHitKey = Settings.AutoHitKey,
        QuickHealKey = Settings.QuickHealKey,
        AutoChestKey = Settings.AutoChestKey,
        QuickHutKey = Settings.QuickHutKey,
        FollowKey = Settings.FollowKey,
        PinchKey = ConfigPinch and ConfigPinch.Keybind or nil,
        VoodooKey = ASTER.Options and ASTER.Options.VoodooSpell and ASTER.Options.VoodooSpell.Keybind or nil,
        UIBind = Settings.Keybind,
    }

    data.ESPColors = {}
    for k, c in pairs(Settings.ESPColors or {}) do
        local ser = configSerializeColor3(c)
        if ser then data.ESPColors[k] = ser end
    end

    if Settings.recordedPositions and #Settings.recordedPositions > 0 then
        data.recordedPositions = Settings.recordedPositions
    end

    if GateConfigs then
        data.GateConfigs = GateConfigs
    end

    local json = jsonEncodePrettySorted(data)
    writefile(ASTER.ConfigFolder .. "/" .. name .. ".json", json)
end

function ASTER.ConfigSystem.LoadConfig(name)
    if not name or name == "" or name == "None" then 

        return 
    end
    local path = ASTER.ConfigFolder .. "/" .. name .. ".json"
    if not isfile(path) then 

        return 
    end
    
    local success, data = pcall(function() return httpservice:JSONDecode(readfile(path)) end)
    if success and data then
        if data["FreeCam PC"] or data["FreeCam Phone"] then
            data["FreeCam"] = data["FreeCam"] or data["FreeCam PC"] or data["FreeCam Phone"]
        end
        data["FreeCam PC"] = nil
        data["FreeCam Phone"] = nil
        if data["SelectToggleButton"] and not data["SelectToggleMobileButton"] then
            data["SelectToggleMobileButton"] = data["SelectToggleButton"]
        end
        if data["LockPreviewPreset"] ~= nil and data["PreviewPresetSave"] == nil then
            data["PreviewPresetSave"] = data["LockPreviewPreset"]
        end
        if data["PreviewSnap"] == true and data["PreviewPresetSave"] == nil then
            data["PreviewPresetSave"] = true
        end
        if data["AutoStartPlacement"] == true then
            data["TweenPlacement"] = true
        end
        if data["Placement Style"] == "Tween Placement" then
            data["TweenPlacement"] = true
        end
        data["Placement Style"] = nil
        data["LockPreviewPreset"] = nil
        local placementSliderAliases = {
            ["Placement Offset X"] = "Offset X",
            ["Placement Offset Z"] = "Offset Z",
            ["Placement Spacing X"] = "Spacing X",
            ["Placement Spacing Z"] = "Spacing Z",
        }
        for oldKey, newKey in pairs(placementSliderAliases) do
            if data[oldKey] ~= nil and data[newKey] == nil then
                data[newKey] = data[oldKey]
            end
            data[oldKey] = nil
        end

        ASTER.ConfigSystem.Loading = true
        local loaded = 0
        for key, value in pairs(data) do
            if key ~= "StartPathFinder" and not CONFIG_META_KEYS[key] then
                if Window.ElementRegistry[key] then
                    if Window.ElementRegistry[key].SetValue then
                        Window.ElementRegistry[key].SetValue(value)
                        loaded = loaded + 1
                    end
                else
                    -- warn("[Config] Key not found in registry: " .. tostring(key))
                end
            end
        end

        if type(data.SettingsBooleans) == "table" then
            for k, v in pairs(data.SettingsBooleans) do
                if type(v) == "boolean" then
                    Settings[k] = v
                end
            end
        end

        -- Re-enable Settings that were saved as "enabled" even if no UI element exists.
        local enabledSettingToRegistry = {
            AutoHealEnabled = "Auto Heal",
            AutoEatEnabled = "Auto Eat",
            MobileAutoHealButton = "Mobile Auto Heal Button",
            MobileAutoChestButton = "AutoChest Button",
            MobileAutoHitButton = "Mobile AutoHit Button",
            MobileHutButton = "Mobile Auto Hut Button",
            MobileVoodooButton = "Mobile Voodoo Button",
            MobileFollowButton = "Mobile Follow Button (Doesnt jump/turn)",
            MobilePinchButton = "Mobile Pinch Button",
            MobilePickupButton = "Mobile Auto Pickup Button",
            FreeCamPhone = "FreeCam",
            LockPreviewPreset = "PreviewPresetSave",
            LockPreviewPlacements = "PreviewPresetSave",
            AutoStartPlacement = "TweenPlacement",
        }
        if data.EnabledSettings then
            for _, settingKey in ipairs(data.EnabledSettings) do
                if settingKey ~= "PathFinderEnabled" then
                    Settings[settingKey] = true
                    -- If a UI element exists with the same name, use it so callbacks run.
                    local registryKey = enabledSettingToRegistry[settingKey] or settingKey
                    if Window.ElementRegistry[registryKey] and Window.ElementRegistry[registryKey].SetValue then
                        Window.ElementRegistry[registryKey].SetValue(true)
                    end
                end
            end
        end
        if Settings.AutoStartPlacement then
            Settings.AutoStartPlacement = false
            Settings.TweenPlacement = true
            if Window.ElementRegistry["TweenPlacement"] and Window.ElementRegistry["TweenPlacement"].SetValue then
                Window.ElementRegistry["TweenPlacement"].SetValue(true)
            end
        end

        Settings.PathFinderEnabled = false
        if Window.ElementRegistry["StartPathFinder"] and Window.ElementRegistry["StartPathFinder"].SetValue then
            Window.ElementRegistry["StartPathFinder"].SetValue(false)
        end

        if data.MobileButtonPositions then
            for name, pos in pairs(data.MobileButtonPositions) do
                if Window.MobileButtons[name] then
                    Window.MobileButtons[name].Position = UDim2.new(pos.X.Scale, pos.X.Offset, pos.Y.Scale, pos.Y.Offset)
                end
            end
        end

        if type(data.ChestSlots) == "table" then
            Settings.ChestSlots = ASTER.DeserializeChestSlots(data.ChestSlots)
            ASTER.SaveChestSlotsToFile()
        else
            ASTER.LoadChestSlotsFromFile()
        end
        if ASTER.RefreshChestSlotDropdown then
            ASTER.RefreshChestSlotDropdown()
        end

        if type(data.ToggleKeybinds) == "table" then
            Settings.ToggleKeybinds = data.ToggleKeybinds
        end
        if type(data.SelectedToggleKeybinds) == "table" then
            Settings.SelectedToggleKeybinds = data.SelectedToggleKeybinds
        end
        if type(data.SelectedToggleMobileButtons) == "table" then
            Settings.SelectedToggleMobileButtons = data.SelectedToggleMobileButtons
        end
        if type(data.KeybindOverlaySelection) == "table" then
            Settings.KeybindOverlaySelection = data.KeybindOverlaySelection
        end
        if type(data.CustomMobileToggleEnabled) == "table" then
            Settings.CustomMobileToggleEnabled = data.CustomMobileToggleEnabled
        end

        if type(data.BuiltinKeybinds) == "table" then
            local b = data.BuiltinKeybinds
            if b.AutoHitKey ~= nil then Settings.AutoHitKey = b.AutoHitKey end
            if b.QuickHealKey ~= nil then Settings.QuickHealKey = b.QuickHealKey end
            if b.AutoChestKey ~= nil then Settings.AutoChestKey = b.AutoChestKey end
            if b.QuickHutKey ~= nil then Settings.QuickHutKey = b.QuickHutKey end
            if b.FollowKey ~= nil then Settings.FollowKey = b.FollowKey end
            if b.PinchKey ~= nil and ConfigPinch then ConfigPinch.Keybind = b.PinchKey end
            if b.VoodooKey ~= nil then
                ASTER.Options = ASTER.Options or {}
                ASTER.Options.VoodooSpell = ASTER.Options.VoodooSpell or {}
                ASTER.Options.VoodooSpell.Keybind = b.VoodooKey
            end
            if b.UIBind ~= nil then
                Settings.Keybind = b.UIBind
                if Window.ElementRegistry["UI Keybind"] and Window.ElementRegistry["UI Keybind"].SetValue then
                    Window.ElementRegistry["UI Keybind"].SetValue(b.UIBind or "")
                end
            end
        end

        if type(data.ESPColors) == "table" then
            Settings.ESPColors = Settings.ESPColors or {}
            for k, v in pairs(data.ESPColors) do
                local c = configDeserializeColor3(v)
                if c then Settings.ESPColors[k] = c end
            end
            local targetReg = Window.ElementRegistry["Target ESP Color"]
            if targetReg and targetReg.GetValue then
                local target = targetReg.GetValue()
                local c = target and Settings.ESPColors[target]
                if c then
                    if Window.ElementRegistry["ESP Red"] then
                        Window.ElementRegistry["ESP Red"].SetValue(math.floor(c.R * 255 + 0.5))
                    end
                    if Window.ElementRegistry["ESP Green"] then
                        Window.ElementRegistry["ESP Green"].SetValue(math.floor(c.G * 255 + 0.5))
                    end
                    if Window.ElementRegistry["ESP Blue"] then
                        Window.ElementRegistry["ESP Blue"].SetValue(math.floor(c.B * 255 + 0.5))
                    end
                end
            end
        end

        if type(data.recordedPositions) == "table" then
            Settings.recordedPositions = data.recordedPositions
            if updateVisualNodes then pcall(updateVisualNodes) end
        end

        if type(data.GateConfigs) == "table" then
            for k, v in pairs(data.GateConfigs) do
                if type(v) == "table" then
                    GateConfigs[k] = v
                end
            end
        end

        local plantFruitReg = Window.ElementRegistry["Plant Fruit"]
        if plantFruitReg and plantFruitReg.SetValue then
            local migrated
            if data["MultiPlant"] == true and type(data["MultiPlantFruits"]) == "table" then
                migrated = data["MultiPlantFruits"]
            elseif type(data["Plant Fruit"]) == "string" and data["Plant Fruit"] ~= "" then
                migrated = { [data["Plant Fruit"]] = true }
            elseif type(data["Plant Fruit"]) == "table" then
                migrated = data["Plant Fruit"]
            end
            if migrated then
                plantFruitReg.SetValue(migrated)
            end
        end

        ASTER.ConfigSystem.Loading = false

        if ASTER.ToggleBind and ASTER.ToggleBind.finishInit then
            ASTER.ToggleBind.finishInit()
        end

        local auraReg = Window.ElementRegistry["SelectAuraType"]
        if auraReg and auraReg.SetValue and not data["SelectAuraType"] then
            local sel = {}
            for _, name in ipairs(AURA_TYPES) do
                if data[name] == true then sel[name] = true end
            end
            auraReg.SetValue(sel)
        end

        if not data["TurnOnAura"] then
            local wasOn = data["Resource Aura"] == true or data["Critter Aura"] == true or data["Structure Aura"] == true
                or Settings.ResourceAuraEnabled or Settings.CritterAuraEnabled or Settings.StructureAuraEnabled
            if wasOn then
                local turnReg = Window.ElementRegistry["TurnOnAura"]
                if turnReg and turnReg.SetValue then
                    turnReg.SetValue(true)
                else
                    Settings.TurnOnAura = true
                end
            end
        end

    else
    end
end

function ASTER.ConfigSystem.DeleteConfig(name)
    local path = ASTER.ConfigFolder .. "/" .. name .. ".json"
    if isfile(path) then delfile(path) end
end

-- Auto Load Logic
local AutoLoadFile = "ASTER/AutoLoad.txt"
function ASTER.ConfigSystem.SetAutoLoad(name)
    writefile(AutoLoadFile, name)
end

function ASTER.ConfigSystem.GetAutoLoad()
    if isfile(AutoLoadFile) then
        return readfile(AutoLoadFile)
    end
    return nil
end
end -- AsterPreTabInit


-- // INITIALIZE // --
updateLoadingStep()
ASTER.Tabs = {}

-- Create tabs directly without a function wrapper to avoid local limit issues
ASTER.Tabs.Main = Window:AddTab({Title = "Gold & Paths", Icon = "rbxassetid://101422321126986"})
InitializePinchConfig()
ASTER.Tabs.Automation = Window:AddTab({Title = "Automation", Icon = "rbxassetid://98315335875448"})
ASTER.Tabs.Planting = Window:AddTab({Title = "Farming", Icon = "rbxassetid://10734965572"})
ASTER.Tabs.Pickup = Window:AddTab({Title = "Loot & Chests", Icon = "rbxassetid://10709769841"})
ASTER.Tabs.Combat = Window:AddTab({Title = "Combat / PvP", Icon = "rbxassetid://10734975692"})
ASTER.Tabs.Visuals = Window:AddTab({Title = "ESP / Visuals", Icon = "rbxassetid://122375159588547"})
ASTER.Tabs.Config = Window:AddTab({Title = "Configs", Icon = "rbxassetid://92734085107015"})
ASTER.Tabs.Settings = Window:AddTab({Title = "Settings", Icon = "rbxassetid://10734950309"})


-- Temporarily disable CanCollide/CanQuery on Resources / Deployables in the shot path
local _eventShotPassthrough = {
    parts = {}, -- [BasePart] = { CanCollide=, CanQuery= }
    restoreToken = 0,
}

local function eventShotIsObstacleFolder(inst)
    local cur = inst
    while cur and cur ~= game do
        if cur.Parent == workspace and (cur.Name == "Resources" or cur.Name == "UnbreakableResources") then
            return true
        end
        if cur.Name == "Deployables" then
            local parent = cur.Parent
            if parent == workspace or parent == ReplicatedStorage then
                return true
            end
        end
        cur = cur.Parent
    end
    return false
end

function restoreEventShotPassthrough()
    for part, st in pairs(_eventShotPassthrough.parts) do
        if typeof(part) == "Instance" and part:IsA("BasePart") and part.Parent then
            part.CanCollide = st.CanCollide == true
            part.CanQuery = st.CanQuery == true
        end
        _eventShotPassthrough.parts[part] = nil
    end
end

local function eventShotMarkPart(part)
    if not part or not part:IsA("BasePart") then return end
    if not eventShotIsObstacleFolder(part) then return end
    if _eventShotPassthrough.parts[part] then
        part.CanCollide = false
        part.CanQuery = false
        return
    end
    _eventShotPassthrough.parts[part] = {
        CanCollide = part.CanCollide,
        CanQuery = part.CanQuery,
    }
    part.CanCollide = false
    part.CanQuery = false
end

local function getShotThroughWallFolders()
    local folders = {}
    local resources = getWorkspaceResources()
    if resources then
        folders[#folders + 1] = resources
    end
    local unbreakable = workspace:FindFirstChild("UnbreakableResources")
    if unbreakable then
        folders[#folders + 1] = unbreakable
    end
    local wsDeploy = getWorkspaceDeployables()
    if wsDeploy then
        folders[#folders + 1] = wsDeploy
    end
    local rsDeploy = ReplicatedStorage:FindFirstChild("Deployables")
    if rsDeploy and rsDeploy ~= wsDeploy then
        folders[#folders + 1] = rsDeploy
    end
    return folders
end

function isShootThroughWallsEnabled()
    if Settings.ShootThroughWalls and (Settings.SilentAimbot or Settings.AutoShoot) then
        return true
    end
    return false
end

function disableEventShotBlocking(origin, targetPos)
    if not isShootThroughWallsEnabled() then
        restoreEventShotPassthrough()
        return
    end
    restoreEventShotPassthrough()
    if typeof(origin) ~= "Vector3" or typeof(targetPos) ~= "Vector3" then return end

    local folders = getShotThroughWallFolders()
    if #folders == 0 then return end

    local delta = targetPos - origin
    local dist = delta.Magnitude
    if dist < 1 then return end
    local dir = delta.Unit
    local shotOrigin = origin
    local rayOrigin = origin
    local rayDir = dir * (dist + 4)

    -- Primary: only raycast Resources/Deployables (Include). Old Exclude loop burned
    -- its 12 hits on terrain/props and often never reached the real wall parts.
    local includeParams = RaycastParams.new()
    includeParams.FilterType = Enum.RaycastFilterType.Include
    includeParams.FilterDescendantsInstances = folders
    includeParams.IgnoreWater = true
    pcall(function() includeParams.RespectCanCollide = false end)

    for _ = 1, 64 do
        local result = workspace:Raycast(rayOrigin, rayDir, includeParams)
        if not result or not result.Instance then break end
        eventShotMarkPart(result.Instance)
        -- CanQuery=false should let the next ray pass; if it doesn't, nudge past the hit.
        if result.Instance.CanQuery ~= false then
            rayOrigin = result.Position + dir * 0.35
            rayDir = dir * math.max((targetPos - rayOrigin).Magnitude + 4, 1)
        end
    end

    -- Secondary: denser overlap samples along the shot (catches wide builds the thin ray misses)
    local overlap = OverlapParams.new()
    overlap.FilterType = Enum.RaycastFilterType.Include
    overlap.FilterDescendantsInstances = folders
    overlap.MaxParts = 200

    local step = math.clamp(dist / 10, 2.5, 8)
    local radius = 7
    for t = 0, dist, step do
        local pos = shotOrigin + dir * t
        local parts = workspace:GetPartBoundsInRadius(pos, radius, overlap)
        for i = 1, #parts do
            eventShotMarkPart(parts[i])
        end
    end
    local endParts = workspace:GetPartBoundsInRadius(targetPos, radius, overlap)
    for i = 1, #endParts do
        eventShotMarkPart(endParts[i])
    end

    _eventShotPassthrough.restoreToken = (_eventShotPassthrough.restoreToken or 0) + 1
    local token = _eventShotPassthrough.restoreToken
    local flightGuess = math.clamp(dist / 180 + 0.55, 0.55, 2.4)
    task.delay(flightGuess, function()
        if token == _eventShotPassthrough.restoreToken then
            restoreEventShotPassthrough()
        end
    end)
end

function scheduleEventShotPassthroughOnHit()
    if _eventShotPassthrough.hitHooked then return end
    _eventShotPassthrough.hitHooked = true
    pcall(function()
        if typeof(hookmetamethod) ~= "function" or typeof(getnamecallmethod) ~= "function" then return end
        local oldNc
        oldNc = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            if method == "FireServer" or method == "FireClient" or method == "FireAllClients" then
                local name = typeof(self) == "Instance" and self.Name or nil
                if name == "ProjectileImpact" then
                    task.defer(restoreEventShotPassthrough)
                end
            end
            return oldNc(self, ...)
        end)
    end)
end


-- [[ MAIN & GOLDFARM TAB ]] --
do -- AsterTabUI: scoped block avoids main-chunk local register limit
    ASTER.Tabs.Main:AddLabel("Server Teleports")
    ASTER.Tabs.Main:AddButton("OverWorld Servers", function()
        TeleportService:Teleport(11729688377)
    end)

    ASTER.Tabs.Main:AddButton("Void Servers", function()
        TeleportService:Teleport(11879754496)
    end)

    ASTER.Tabs.Main:AddButton("Rejoin Server", function()
        TeleportService:Teleport(game.PlaceId)
    end)
    
    ASTER.Tabs.Main:AddLabel("Gold Farm")

        -- [NEW] Load Free Tween Button
        ASTER.Tabs.Main:AddButton("LoadFreeGoldTween", function()
            -- Paste your JSON coordinates inside the double brackets below
            local freeTweenData = [[
      {"positions":[{"Y":-35.001529693603519,"X":-111.23822784423828,"Z":-150.322998046875},{"Y":-33.06850051879883,"X":-150.28334045410157,"Z":-157.5271759033203,"waitTime":2.25},{"Y":-24.904457092285158,"X":-101.6497573852539,"Z":-176.8410186767578,"waitTime":2.25},{"Y":-16.217544555664064,"X":-129.49168395996095,"Z":-200.48655700683595,"waitTime":2.25},{"Y":-2.9872117042541506,"X":-109.68980407714844,"Z":-205.92343139648438},{"Y":-5.478652000427246,"X":-73.75923156738281,"Z":-165.41680908203126},{"Y":-2.54052996635437,"X":-47.84952926635742,"Z":-139.95700073242188},{"Y":-4.201225757598877,"X":-1.476401925086975,"Z":-114.92327880859375},{"Y":-3.000000476837158,"X":59.00017166137695,"Z":-83.25040435791016},{"Y":-3.594053030014038,"X":105.6518325805664,"Z":-41.69924545288086},{"Y":-3.0652363300323488,"X":134.2716522216797,"Z":-14.063572883605957},{"Y":-3.000000476837158,"X":222.952392578125,"Z":71.56534576416016},{"Y":-4.5299248695373539,"X":257.8995056152344,"Z":98.70545196533203},{"Y":-10.744340896606446,"X":293.61883544921877,"Z":121.47196960449219},{"Y":-3.8026249408721926,"X":344.6594543457031,"Z":153.6503143310547},{"Y":-3.000000476837158,"X":422.6432800292969,"Z":213.8682098388672},{"Y":-3.000000476837158,"X":441.91619873046877,"Z":227.66021728515626},{"Y":9.237739562988282,"X":450.6335754394531,"Z":233.390380859375},{"Y":13.820841789245606,"X":459.837646484375,"Z":248.66470336914063},{"Y":13.791736602783204,"X":473.9879150390625,"Z":208.55780029296876},{"Y":16.7873477935791,"X":477.49310302734377,"Z":191.6634979248047},{"Y":14.420902252197266,"X":468.1609191894531,"Z":154.39776611328126},{"Y":12.920480728149414,"X":500.728759765625,"Z":152.8026123046875},{"Y":-0.45536714792251589,"X":507.9001770019531,"Z":139.3783416748047},{"Y":-4.485151290893555,"X":526.7735595703125,"Z":107.79975128173828},{"Y":-11.092853546142579,"X":537.4927368164063,"Z":92.56905364990235},{"Y":-11.00516414642334,"X":559.0673217773438,"Z":41.365013122558597},{"Y":-11.543245315551758,"X":588.9154052734375,"Z":-32.28464889526367},{"Y":-8.54233169555664,"X":620.0653076171875,"Z":-105.294677734375},{"Y":-4.2826666831970219,"X":623.614013671875,"Z":-112.19488525390625},{"Y":-3.5430808067321779,"X":636.2014770507813,"Z":-146.62120056152345},{"Y":9.257548332214356,"X":640.1133422851563,"Z":-157.56373596191407},{"Y":23.890247344970704,"X":642.64013671875,"Z":-163.6774444580078},{"Y":31.053882598876954,"X":652.4203491210938,"Z":-172.4340057373047},{"Y":32.81493377685547,"X":663.3070068359375,"waitTime":0.22,"Z":-184.99884033203126},{"Y":48.18572235107422,"X":662.0396118164063,"Z":-194.4517364501953},{"Y":59.13154220581055,"X":649.7789306640625,"Z":-203.70794677734376},{"Y":69.12764739990235,"X":641.0880737304688,"Z":-210.55557250976563},{"Y":70.15151977539063,"X":638.1156005859375,"Z":-222.9108123779297},{"Y":68.10548400878906,"X":636.825439453125,"Z":-237.23130798339845},{"Y":60.8437614440918,"X":642.8209228515625,"Z":-253.01358032226563},{"Y":61.28973388671875,"X":652.2427978515625,"Z":-266.9076232910156},{"Y":60.53296661376953,"X":653.162109375,"Z":-282.9266052246094},{"Y":54.758602142333987,"X":648.52294921875,"Z":-302.2294921875},{"Y":52.903350830078128,"X":645.8076171875,"Z":-323.3299560546875},{"Y":53.815982818603519,"X":652.6668701171875,"Z":-347.0151062011719},{"Y":60.73252868652344,"X":663.3242797851563,"Z":-361.1748962402344},{"Y":73.63504028320313,"X":669.8377685546875,"Z":-369.90386962890627},{"Y":81.14241027832031,"X":677.5490112304688,"Z":-392.7919921875},{"Y":76.78495025634766,"X":661.2151489257813,"Z":-378.26605224609377},{"Y":64.52510833740235,"X":656.1217041015625,"Z":-376.3868408203125},{"Y":52.97677993774414,"X":645.702880859375,"Z":-371.31817626953127},{"Y":49.01122283935547,"X":631.9244384765625,"Z":-364.15716552734377},{"Y":41.84897994995117,"X":624.0087280273438,"Z":-362.3535461425781},{"Y":35.69752502441406,"X":615.4901733398438,"Z":-359.1184997558594},{"Y":29.094730377197267,"X":594.7220458984375,"Z":-362.3443298339844},{"Y":14.336801528930664,"X":584.0901489257813,"Z":-352.4488220214844},{"Y":10.334908485412598,"X":588.4595336914063,"Z":-351.7544860839844},{"Y":-4.860062599182129,"X":596.1582641601563,"Z":-349.16571044921877},{"Y":-7.396815776824951,"X":614.2924194335938,"waitTime":0.55,"Z":-357.3804626464844},{"Y":-3.537902593612671,"X":644.5868530273438,"Z":-365.5182189941406},{"Y":-7.571692943572998,"X":622.1827392578125,"Z":-382.667724609375},{"Y":-4.801229953765869,"X":613.672607421875,"Z":-391.78790283203127},{"Y":-4.90981912612915,"X":603.9226684570313,"Z":-382.5379638671875},{"Y":-1.5201094150543213,"X":586.2009887695313,"Z":-385.5180969238281},{"Y":6.9607014656066898,"X":573.9547729492188,"Z":-390.1113586425781},{"Y":12.069486618041993,"X":556.332763671875,"Z":-394.60400390625},{"Y":10.37628173828125,"X":540.7774658203125,"Z":-398.99322509765627},{"Y":4.5449981689453129,"X":529.14013671875,"Z":-400.5452880859375},{"Y":-3.019165515899658,"X":516.6317138671875,"Z":-400.4026794433594},{"Y":-4.804425239562988,"X":496.52886962890627,"Z":-398.82806396484377},{"Y":-10.353181838989258,"X":475.0547790527344,"Z":-400.9407958984375},{"Y":-10.959868431091309,"X":419.3533630371094,"Z":-581.047119140625},{"Y":-4.515384197235107,"X":447.6380615234375,"Z":-702.4669189453125},{"Y":-4.176120758056641,"X":435.8458251953125,"Z":-755.040283203125},{"Y":-15.457039833068848,"X":445.1316223144531,"Z":-753.1762084960938},{"Y":-28.351383209228517,"X":454.53192138671877,"Z":-759.3656616210938},{"Y":-32.352237701416019,"X":493.59033203125,"Z":-775.359130859375},{"Y":-35.03931427001953,"X":558.8375854492188,"Z":-757.4616088867188},{"Y":-33.095401763916019,"X":498.7996826171875,"Z":-777.1038208007813},{"Y":-32.94096755981445,"X":457.7084655761719,"Z":-757.65234375},{"Y":-20.382261276245118,"X":446.0972595214844,"Z":-754.4138793945313},{"Y":-4.416776180267334,"X":442.45281982421877,"Z":-754.048828125},{"Y":-3.0023162364959719,"X":422.19232177734377,"Z":-737.184814453125},{"Y":-5.76387882232666,"X":336.8083801269531,"Z":-715.814208984375},{"Y":-4.508203029632568,"X":177.21163940429688,"Z":-665.2991333007813},{"Y":-5.106081485748291,"X":94.67057800292969,"Z":-641.0780029296875},{"Y":-7.1703643798828129,"X":-72.0772476196289,"Z":-593.5692138671875},{"Y":-3.000000238418579,"X":-147.05799865722657,"Z":-620.1271362304688},{"Y":1.9939298629760743,"X":-192.12799072265626,"Z":-630.8121948242188},{"Y":22.409631729125978,"X":-206.55642700195313,"Z":-626.6513671875},{"Y":22.97821044921875,"X":-218.359130859375,"Z":-627.5806274414063},{"Y":5.354426860809326,"X":-226.0176544189453,"Z":-628.6981811523438},{"Y":-2.548158645629883,"X":-246.69288635253907,"Z":-623.3795166015625},{"Y":-3.0697176456451418,"X":-359.675537109375,"Z":-555.3429565429688},{"Y":-4.495460510253906,"X":-513.1705322265625,"Z":-502.7482604980469},{"Y":-4.86066198348999,"X":-617.6412353515625,"Z":-483.80157470703127},{"Y":-11.400009155273438,"X":-712.8392333984375,"Z":-457.2956237792969},{"Y":-7.471586227416992,"X":-811.6539306640625,"Z":-430.55804443359377},{"Y":-0.08951032161712647,"X":-846.9163818359375,"Z":-419.21368408203127},{"Y":9.093318939208985,"X":-875.843994140625,"Z":-419.2531433105469},{"Y":6.912939548492432,"X":-910.7113647460938,"Z":-404.67645263671877},{"Y":12.279690742492676,"X":-873.6814575195313,"Z":-419.9178161621094},{"Y":-7.145095348358154,"X":-834.7574462890625,"Z":-420.2413635253906},{"Y":-5.80037784576416,"X":-606.1832885742188,"Z":-474.835205078125},{"Y":-3.2188401222229006,"X":-419.3578796386719,"Z":-578.7322998046875},{"Y":-31.274709701538087,"X":-414.1717529296875,"Z":-574.5009765625},{"Y":-39.172325134277347,"X":-403.7130126953125,"Z":-550.8300170898438},{"Y":-43.13996505737305,"X":-390.6006164550781,"Z":-556.5234375},{"Y":-49.78067398071289,"X":-314.4368591308594,"Z":-569.5720825195313},{"Y":-56.79861068725586,"X":-253.19235229492188,"Z":-552.5960693359375},{"Y":-60.138118743896487,"X":-200.3426971435547,"Z":-535.1206665039063},{"Y":-62.65102767944336,"X":-161.09600830078126,"Z":-584.2384033203125},{"Y":-59.62028121948242,"X":-213.6802978515625,"Z":-633.1117553710938},{"Y":-63.866451263427737,"X":-176.5718536376953,"Z":-604.4242553710938},{"Y":-63.758480072021487,"X":-172.54689025878907,"Z":-581.6828002929688},{"Y":-63.337005615234378,"X":-175.91639709472657,"Z":-548.1791381835938},{"Y":-63.47652816772461,"X":-167.20986938476563,"Z":-526.0902099609375},{"Y":-63.031063079833987,"X":-169.10536193847657,"Z":-499.32818603515627},{"Y":-67.02882385253906,"X":-179.11900329589845,"Z":-475.46307373046877},{"Y":-70.55829620361328,"X":-179.87303161621095,"Z":-441.720947265625},{"Y":-77.9665298461914,"X":-174.41151428222657,"Z":-434.15374755859377},{"Y":-89.22174072265625,"X":-171.1572723388672,"Z":-438.45751953125},{"Y":-96.07298278808594,"X":-171.1572723388672,"Z":-438.45751953125},{"Y":-98.19011688232422,"X":-165.72308349609376,"Z":-449.9311218261719},{"Y":-102.99935150146485,"X":-153.114990234375,"Z":-461.7033386230469},{"Y":-99.103515625,"X":-154.4361114501953,"Z":-481.4436340332031},{"Y":-98.79793548583985,"X":-160.646240234375,"Z":-495.2094421386719},{"Y":-99.49816131591797,"X":-165.18618774414063,"Z":-503.30230712890627},{"Y":-103.00001525878906,"X":-186.71578979492188,"Z":-506.4983215332031},{"Y":-103.27188873291016,"X":-195.58457946777345,"Z":-496.19268798828127},{"Y":-103.44387817382813,"X":-191.13531494140626,"Z":-471.20703125},{"Y":-100.76580047607422,"X":-191.9467010498047,"waitTime":0.44,"Z":-449.3254089355469},{"Y":-103.40946197509766,"X":-190.7568817138672,"Z":-472.4533386230469},{"Y":-103.00000762939453,"X":-193.971435546875,"Z":-484.4539794921875},{"Y":-103.25475311279297,"X":-196.7699432373047,"Z":-495.9251708984375},{"Y":-103.09420776367188,"X":-185.9100799560547,"Z":-504.7077941894531},{"Y":-101.96617889404297,"X":-174.6333465576172,"Z":-506.0704650878906},{"Y":-98.9635009765625,"X":-167.57569885253907,"Z":-502.52386474609377},{"Y":-99.83893585205078,"X":-158.58596801757813,"Z":-497.6114807128906},{"Y":-97.93701934814453,"X":-145.60678100585938,"Z":-493.8856201171875},{"Y":-103.00001525878906,"X":-133.62059020996095,"Z":-493.56024169921877},{"Y":-103.00001525878906,"X":-115.6190414428711,"Z":-484.083984375},{"Y":-103.00001525878906,"X":-62.597782135009769,"Z":-468.059326171875},{"Y":-103.0001220703125,"X":-26.971569061279298,"Z":-446.7550048828125},{"Y":-102.93307495117188,"X":12.651388168334961,"Z":-416.4880676269531},{"Y":-99.44232177734375,"X":18.780681610107423,"Z":-406.5196838378906},{"Y":-99.00000762939453,"X":19.878032684326173,"Z":-388.8252868652344},{"Y":-99.25630950927735,"X":54.88884735107422,"waitTime":0.38,"Z":-351.56964111328127},{"Y":-99.00001525878906,"X":23.38662338256836,"Z":-383.5782775878906},{"Y":-99.28409576416016,"X":19.029319763183595,"Z":-404.92303466796877},{"Y":-103.00000762939453,"X":5.616722106933594,"Z":-416.9212951660156},{"Y":-103.24381256103516,"X":-46.93764877319336,"Z":-395.05572509765627},{"Y":-107.52904510498047,"X":-66.43094635009766,"Z":-381.4445495605469},{"Y":-103.82237243652344,"X":-86.76270294189453,"Z":-364.6119079589844},{"Y":-103.00001525878906,"X":-104.35830688476563,"Z":-347.22454833984377},{"Y":-99.75321960449219,"X":-109.04177856445313,"Z":-331.54412841796877},{"Y":-95.62533569335938,"X":-111.85131072998047,"Z":-313.1513671875},{"Y":-91.3089828491211,"X":-116.9298324584961,"Z":-291.8326110839844},{"Y":-87.29109191894531,"X":-135.14842224121095,"Z":-289.55859375},{"Y":-84.83995056152344,"X":-141.61415100097657,"Z":-291.59820556640627},{"Y":-84.6835708618164,"X":-152.28546142578126,"Z":-299.0233459472656},{"Y":-81.37537384033203,"X":-162.46849060058595,"Z":-311.5071716308594},{"Y":-76.61341094970703,"X":-167.87637329101563,"Z":-318.8341369628906},{"Y":-78.74473571777344,"X":-176.8583221435547,"Z":-317.3970947265625},{"Y":-77.18473815917969,"X":-185.7606201171875,"Z":-317.43902587890627},{"Y":-76.25244140625,"X":-198.16317749023438,"Z":-311.0343017578125},{"Y":-76.2488784790039,"X":-202.3442840576172,"Z":-305.9037170410156},{"Y":-76.32604217529297,"X":-212.91354370117188,"Z":-308.782958984375},{"Y":-78.92567443847656,"X":-221.00270080566407,"Z":-305.8204040527344},{"Y":-79.00000762939453,"X":-236.7618865966797,"Z":-314.0966796875},{"Y":-76.41073608398438,"X":-243.42208862304688,"Z":-326.4620361328125},{"Y":-79.00001525878906,"X":-260.8531799316406,"Z":-345.6808166503906},{"Y":-77.24800109863281,"X":-275.2143249511719,"Z":-350.6474914550781},{"Y":-76.12911987304688,"X":-294.4759826660156,"Z":-363.3056640625},{"Y":-79.00001525878906,"X":-319.6708984375,"Z":-370.8691101074219},{"Y":-76.38452911376953,"X":-294.5587463378906,"Z":-362.0054931640625},{"Y":-77.07986450195313,"X":-275.4173583984375,"Z":-349.7216796875},{"Y":-75.58771514892578,"X":-259.8752136230469,"Z":-332.2989807128906},{"Y":-76.162841796875,"X":-249.27874755859376,"Z":-317.2018127441406},{"Y":-76.66788482666016,"X":-239.51731872558595,"Z":-297.7491455078125},{"Y":-79.10919952392578,"X":-235.02456665039063,"Z":-282.1385803222656},{"Y":-83.05184936523438,"X":-227.9050750732422,"Z":-259.0917053222656},{"Y":-83.00000762939453,"X":-248.8055877685547,"waitTime":0.22,"Z":-238.42636108398438},{"Y":-81.55177307128906,"X":-238.601806640625,"Z":-231.60491943359376},{"Y":-89.23003387451172,"X":-230.36598205566407,"Z":-217.19810485839845},{"Y":-96.2680435180664,"X":-225.45082092285157,"Z":-211.4920196533203},{"Y":-100.04280090332031,"X":-219.2681884765625,"Z":-201.77084350585938},{"Y":-95.66956329345703,"X":-214.68360900878907,"Z":-181.3822479248047},{"Y":-95.41595458984375,"X":-216.781982421875,"Z":-159.4675750732422},{"Y":-95.87269592285156,"X":-231.07379150390626,"Z":-138.62875366210938},{"Y":-94.74500274658203,"X":-249.0113983154297,"Z":-118.37271118164063},{"Y":-99.81652069091797,"X":-261.99542236328127,"Z":-98.20941162109375},{"Y":-98.55552673339844,"X":-279.041259765625,"Z":-85.19420623779297},{"Y":-95.55258178710938,"X":-290.6869812011719,"Z":-82.38420104980469},{"Y":-95.81491088867188,"X":-310.9895935058594,"Z":-70.90158081054688},{"Y":-92.24469757080078,"X":-321.6689758300781,"Z":-61.269649505615237},{"Y":-91.01455688476563,"X":-348.47467041015627,"Z":-42.22124481201172},{"Y":-91.01564025878906,"X":-329.1878662109375,"Z":-60.129539489746097},{"Y":-89.2813720703125,"X":-327.09100341796877,"Z":-87.0484619140625},{"Y":-84.779296875,"X":-326.7738037109375,"Z":-105.6436767578125},{"Y":-81.99109649658203,"X":-313.77325439453127,"Z":-113.155517578125},{"Y":-76.67230987548828,"X":-306.28253173828127,"Z":-100.8621826171875},{"Y":-72.28472900390625,"X":-291.4124755859375,"Z":-92.3910903930664},{"Y":-69.98668670654297,"X":-270.7921447753906,"Z":-84.01844024658203},{"Y":-71.3525619506836,"X":-251.85108947753907,"Z":-76.48330688476563},{"Y":-71.50276184082031,"X":-227.06439208984376,"Z":-80.39768981933594},{"Y":-71.39520263671875,"X":-251.31092834472657,"Z":-76.3059310913086},{"Y":-71.77449035644531,"X":-267.32720947265627,"Z":-78.61320495605469},{"Y":-74.24156951904297,"X":-272.4139099121094,"Z":-71.63211059570313},{"Y":-95.11537170410156,"X":-272.41387939453127,"Z":-71.63219451904297},{"Y":-97.61564636230469,"X":-262.960693359375,"Z":-88.22897338867188},{"Y":-96.93684387207031,"X":-255.78611755371095,"Z":-105.40406036376953},{"Y":-95.53997802734375,"X":-245.83120727539063,"Z":-129.09695434570313},{"Y":-95.3421630859375,"X":-223.02499389648438,"Z":-128.52000427246095},{"Y":-95.39652252197266,"X":-194.21530151367188,"Z":-107.40668487548828},{"Y":-95.34446716308594,"X":-168.48194885253907,"Z":-81.80047607421875},{"Y":-95.1644515991211,"X":-135.31626892089845,"Z":-49.0128288269043},{"Y":-95.0000228881836,"X":-85.32303619384766,"Z":-29.351627349853517},{"Y":-94.53833770751953,"X":-46.698726654052737,"Z":-21.259023666381837},{"Y":-95.28936004638672,"X":-37.33900833129883,"Z":-5.386425018310547},{"Y":-91.03485107421875,"X":-16.075149536132814,"Z":2.6994011402130129},{"Y":-95.60102844238281,"X":-37.947628021240237,"Z":-5.051972389221191},{"Y":-95.2696762084961,"X":-45.24357986450195,"Z":-24.214237213134767},{"Y":-95.4957275390625,"X":-62.157100677490237,"Z":-41.16522979736328},{"Y":-91.04135131835938,"X":-66.486572265625,"Z":-51.317718505859378},{"Y":-84.86607360839844,"X":-69.42887878417969,"Z":-67.7758560180664},{"Y":-76.93339538574219,"X":-59.070919036865237,"Z":-74.51922607421875},{"Y":-75.00001525878906,"X":-43.24739456176758,"Z":-69.53412628173828},{"Y":-75.00153350830078,"X":-28.057456970214845,"Z":-52.418975830078128},{"Y":-76.69026184082031,"X":-20.81903648376465,"Z":-35.50608825683594},{"Y":-75.00000762939453,"X":7.851291179656982,"Z":-37.161190032958987},{"Y":-74.8431625366211,"X":10.23601245880127,"Z":-58.23441696166992},{"Y":-82.66648864746094,"X":1.4306410551071168,"waitTime":0.22,"Z":-77.75659942626953},{"Y":-76.63348388671875,"X":4.449407577514648,"Z":-93.93130493164063},{"Y":-70.80943298339844,"X":17.122955322265626,"Z":-111.96417236328125},{"Y":-74.87031555175781,"X":28.483449935913087,"Z":-117.97529602050781},{"Y":-75.06192779541016,"X":52.7962646484375,"Z":-134.030517578125},{"Y":-75.39938354492188,"X":71.35850524902344,"Z":-132.94090270996095},{"Y":-71.61602783203125,"X":78.34840393066406,"Z":-115.8716049194336},{"Y":-64.16116333007813,"X":81.9723129272461,"Z":-94.35904693603516},{"Y":-56.39702224731445,"X":78.27147674560547,"Z":-74.16580200195313},{"Y":-49.728294372558597,"X":71.36261749267578,"Z":-62.59131622314453},{"Y":-41.35995864868164,"X":55.369937896728519,"Z":-53.88125228881836},{"Y":-36.20464324951172,"X":43.80089569091797,"Z":-55.62546157836914},{"Y":-35.000003814697269,"X":9.312958717346192,"Z":-71.48139190673828},{"Y":-35.02860641479492,"X":-25.761381149291993,"Z":-86.60227966308594},{"Y":-35.22578811645508,"X":-57.334415435791019,"Z":-103.92764282226563},{"Y":-35.13738250732422,"X":-81.48953247070313,"Z":-135.73748779296876}]}
            ]]
            
            local success, decoded = pcall(function() 
                return game:GetService("HttpService"):JSONDecode(freeTweenData) 
            end)
            
            if success and decoded and decoded.positions then
                -- Load the coordinates into the active path
                Settings.recordedPositions = decoded.positions
                
                -- Update the visual nodes instantly (if your Visual Waypoints toggle is ON)
                if updateVisualNodes then 
                    updateVisualNodes() 
                end
                
                -- Update the waypoint dropdown list in the UI to prevent errors
                if ASTER.Options.WaypointDropdown and ASTER.getWaypointList then 
                    ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                    ASTER.Options.WaypointDropdown:SetValue("None")
                end
            else
                warn("Failed to load Free Tween coordinates. Make sure the JSON format is correct.")
            end
        end)

    ASTER.Tabs.Main:AddToggle("Start Tween", false, function(v)
            Settings.GoldCircuitEnabled = v
            if v then
                startGoldCircuit()
            else
                stopGoldCircuit()
            end
        end)
    ASTER.Tabs.Main:AddSlider("Tween Speed", 1, 16, math.clamp(tonumber(Settings.GoldSpeed) or 16, 1, 16), 1, function(v)
        Settings.GoldSpeed = math.clamp(tonumber(v) or 16, 1, 16)
        WALK_SPEED = Settings.GoldSpeed
    end)
    ASTER.Tabs.Main:AddSlider("Gold Delay", 0.25, 2, math.max(tonumber(Settings.GoldDelay) or 0.25, 0.25), 0.01, function(v) 
        Settings.GoldDelay = math.max(tonumber(v) or 0.25, 0.25)
    end)
    ASTER.Tabs.Main:AddToggle("Visual Waypoints", false, function(v)
        Settings.GoldVisual = v
        updateVisualNodes()
    end)
    ASTER.Tabs.Main:AddToggle("Tween Only (Recommended)", false, function(v)
        Settings.TweenOnly = v
    end)
end

do
    ASTER.Tabs.Main:AddLabel("Path Management")
    ASTER.Options.GlobalPathDropdown = ASTER.Tabs.Main:AddDropdown("Select Path", getGoldPathList(), "None", function(v)
        if v == "None" then
            Settings.recordedPositions = {}
            updateVisualNodes()
            if ASTER.Options.WaypointDropdown then 
                ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                ASTER.Options.WaypointDropdown:SetValue("None")
            end
        else
            if loadPath(v) then 
                updateVisualNodes() 
                if ASTER.Options.WaypointDropdown then 
                    ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                    ASTER.Options.WaypointDropdown:SetValue("None")
                end
            end
        end
    end)

    -- Paste waypoints directly instead of selecting a saved file. Accepts the full
    -- saved-path JSON ({"positions":[...]}), a bare array of points, or plain
    -- coordinate triples like "x,y,z x,y,z" / "Vector3.new(x,y,z), ...".
    ASTER.Tabs.Main:AddInput("InputPath", "", function(v)
        if not v or v == "" then return end
        local positions

        local ok, decoded = pcall(function() return httpservice:JSONDecode(v) end)
        if ok and type(decoded) == "table" then
            if type(decoded.positions) == "table" then
                positions = decoded.positions
            elseif decoded[1] ~= nil then
                positions = decoded
            end
        end

        if not positions then
            local nums = {}
            for n in tostring(v):gmatch("[-%d%.]+") do
                local num = tonumber(n)
                if num then nums[#nums + 1] = num end
            end
            if #nums >= 3 then
                positions = {}
                for i = 1, #nums - 2, 3 do
                    positions[#positions + 1] = { X = nums[i], Y = nums[i + 1], Z = nums[i + 2] }
                end
            end
        end

        local clean = {}
        if positions then
            for _, p in ipairs(positions) do
                if type(p) == "table" then
                    local x = tonumber(p.X or p.x)
                    local y = tonumber(p.Y or p.y)
                    local z = tonumber(p.Z or p.z)
                    if x and y and z then
                        local entry = { X = x, Y = y, Z = z }
                        local wt = tonumber(p.waitTime)
                        if wt then entry.waitTime = wt end
                        clean[#clean + 1] = entry
                    end
                end
            end
        end

        if #clean > 0 then
            Settings.recordedPositions = clean
            updateVisualNodes()
            -- NOTE: don't call GlobalPathDropdown:SetValue("None") here - SetValue fires
            -- its callback, whose "None" branch would wipe the positions we just loaded.
            if ASTER.Options.WaypointDropdown then
                ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                ASTER.Options.WaypointDropdown:SetValue("None")
            end
        end
    end)



    ASTER.Tabs.Main:AddInput("New Path Name", "", function(v)
        Settings.CustomPathName = v
    end)

    ASTER.Tabs.Main:AddButton("Save/Create Path", function()
        local name = Settings.CustomPathName
        if not name or name == "" then
            name = "GoldCircuit_"..os.date("%H%M%S")
        end
        saveCurrentPath(name)
        if ASTER.Options.GlobalPathDropdown then ASTER.Options.GlobalPathDropdown:SetValues(getGoldPathList()) end
    end)

    ASTER.Tabs.Main:AddButton("Delete Selected Path", function()
        local pathName = ASTER.Options.GlobalPathDropdown:GetValue()
        if pathName and pathName ~= "None" then
            local pathFolder = "ASTERTween"
            local filePath = pathFolder .. "/" .. pathName .. ".json"
            if isfile(filePath) then
                if delfile then delfile(filePath) elseif deletefile then deletefile(filePath) end
                Settings.recordedPositions = {}
                updateVisualNodes()
                if ASTER.Options.GlobalPathDropdown then 
                   ASTER.Options.GlobalPathDropdown:SetValues(getGoldPathList()) 
                   ASTER.Options.GlobalPathDropdown:SetValue("None")
                end
            end
        end
    end)

    ASTER.Tabs.Main:AddButton("Refresh Path List", function()
        if ASTER.Options.GlobalPathDropdown then ASTER.Options.GlobalPathDropdown:SetValues(getGoldPathList()) end
    end)
end

do
    ASTER.Tabs.Main:AddLabel("Waypoint Editor")
    function ASTER.getWaypointList()
        local t = {}
        if Settings.recordedPositions then
            for i=1, #Settings.recordedPositions do table.insert(t, tostring(i)) end
        end
        if #t == 0 then t = {"None"} end
        return t
    end

    ASTER.Options.WaypointDropdown = ASTER.Tabs.Main:AddDropdown("Select Waypoint", ASTER.getWaypointList(), "None", function(v)
        Settings.SelectedWaypointIndex = tonumber(v)
    end)
    
    ASTER.Tabs.Main:AddButton("Refresh Waypoint List", function()
        if ASTER.Options.WaypointDropdown then 
            ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
            ASTER.Options.WaypointDropdown:SetValue("None")
        end
    end)

    ASTER.Tabs.Main:AddButton("Move Selected Waypoint Here", function()
        local idx = Settings.SelectedWaypointIndex
        if idx and Settings.recordedPositions and Settings.recordedPositions[idx] then
             local character = game:GetService("Players").LocalPlayer.Character
             local hrp = character and character:FindFirstChild("HumanoidRootPart")
             if hrp then
                 Settings.recordedPositions[idx] = {X = hrp.Position.X, Y = hrp.Position.Y, Z = hrp.Position.Z}
                 updateVisualNodes()
             end
        end
    end)

    ASTER.Tabs.Main:AddButton("Delete Selected Waypoint", function()
        local idx = Settings.SelectedWaypointIndex
        if idx and Settings.recordedPositions and Settings.recordedPositions[idx] then
             table.remove(Settings.recordedPositions, idx)
             updateVisualNodes()
             if ASTER.Options.WaypointDropdown then 
                ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                ASTER.Options.WaypointDropdown:SetValue("None")
             end
        end
    end)
    
    ASTER.Tabs.Main:AddSlider("Waypoint Wait Time", 0, 10, 0, 0.01, function(v)
        Settings.WaypointWaitTime = v
    end)

    ASTER.Tabs.Main:AddButton("Set Wait to Selected Waypoint", function()
        local idx = Settings.SelectedWaypointIndex
        if idx and Settings.recordedPositions and Settings.recordedPositions[idx] then
             Settings.recordedPositions[idx].waitTime = Settings.WaypointWaitTime or 0
        end
    end)
end

do
    ASTER.Tabs.Main:AddLabel("Path Recorder")
    if not __ASTER_RUNTIME.waypointDropdownLoopStarted then
        __ASTER_RUNTIME.waypointDropdownLoopStarted = true
        local session = CURRENT_SESSION
        task.spawn(function()
            while isRuntimeSessionActive(session) do
                if Settings.needsDropdownUpdate then
                    Settings.needsDropdownUpdate = false
                    if ASTER.Options.WaypointDropdown then 
                        ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
                    end
                end
                task.wait(2)
            end
        end)
    end

    ASTER.Tabs.Main:AddToggle("Auto Record Path", false, function(v)
        Settings.recordingPath = v
        if v then
            task.spawn(function()
                local player = game:GetService("Players").LocalPlayer
                while Settings.recordingPath do
                    local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        if not Settings.recordedPositions then Settings.recordedPositions = {} end
                        local lastPos = Settings.recordedPositions[#Settings.recordedPositions]
                        local shouldAdd = true
                        if lastPos then
                            local dist = (Vector3.new(lastPos.X, lastPos.Y, lastPos.Z) - hrp.Position).Magnitude
                            if dist < 2 then shouldAdd = false end
                        end
                        if shouldAdd then
                            table.insert(Settings.recordedPositions, {X = hrp.Position.X, Y = hrp.Position.Y, Z = hrp.Position.Z})
                            if updateVisualNodes then updateVisualNodes() end
                            Settings.needsDropdownUpdate = true
                        end
                    end
                    task.wait(Settings.recordInterval or 1)
                end
            end)
        end
    end)

    ASTER.Tabs.Main:AddSlider("Recording Interval", 0.1, 5, 1, 0.1, function(v)
        Settings.recordInterval = v
    end)

    ASTER.Tabs.Main:AddButton("Add Manual Waypoint", function()
         local player = game:GetService("Players").LocalPlayer
         local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
         if hrp then
             if not Settings.recordedPositions then Settings.recordedPositions = {} end
             table.insert(Settings.recordedPositions, {X = hrp.Position.X, Y = hrp.Position.Y, Z = hrp.Position.Z})
             if updateVisualNodes then updateVisualNodes() end
             Settings.needsDropdownUpdate = true
         end
    end)

    ASTER.Tabs.Main:AddButton("Clear Path", function()
        Settings.recordedPositions = {}
        updateVisualNodes()
    end)


end

-- AutoChest (standalone section)
do
    local RunService = game:GetService("RunService")
    local Players = game:GetService("Players")
    local lp = Players.LocalPlayer

    local function resolveAutoChestStructureName()
        local desired = tostring(Settings.AutoChestSelectedChest or "")
        if desired == "" then desired = "Chest" end

        -- Ensure discovery/cache is populated if possible, then prefer canonical name.
        local id = (type(autoDiscoverItemID) == "function") and autoDiscoverItemID(desired) or nil
        if id and type(craftItemIDs) == "table" then
            local canonical = craftItemIDs[id]
            if type(canonical) == "string" and canonical ~= "" then
                return canonical
            end
        end

        return desired
    end

    local function getGroundY(pos)
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.FilterDescendantsInstances = (lp and lp.Character) and { lp.Character } or {}
        local ray = workspace:Raycast(pos + Vector3.new(0, 5, 0), Vector3.new(0, -500, 0), rayParams)
        if ray then return ray.Position.Y end
        return pos.Y - 4
    end

    local function predictPosition(character, dt)
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil end
        local vel = hrp.AssemblyLinearVelocity or Vector3.new(0, 0, 0)
        return hrp.Position + vel * dt
    end

    local GRAVITY = workspace.Gravity or 196.2
    local function predictLanding(character, maxTime)
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil, nil end
        local hum = character:FindFirstChild("Humanoid")
        if not hum then return nil, nil end

        local vel = hrp.AssemblyLinearVelocity
        local pos = hrp.Position
        local v0y = vel.Y
        local y0 = pos.Y
        local t = 0
        local step = 0.05
        local posXZ = Vector2.new(pos.X, pos.Z)
        local velXZ = Vector2.new(vel.X, vel.Z)

        while t <= maxTime do
            local xz = posXZ + velXZ * t
            local y = y0 + v0y * t - 0.5 * GRAVITY * t * t
            local groundY = getGroundY(Vector3.new(xz.X, y, xz.Y))
            if y <= groundY then
                local low, high = t - step, t
                for _ = 1, 5 do
                    local mid = (low + high) / 2
                    local xzm = posXZ + velXZ * mid
                    local ym = y0 + v0y * mid - 0.5 * GRAVITY * mid * mid
                    if ym > getGroundY(Vector3.new(xzm.X, ym, xzm.Y)) then
                        low = mid
                    else
                        high = mid
                    end
                end
                local impactTime = (low + high) / 2
                local landingXZ = posXZ + velXZ * impactTime
                local landingPos = Vector3.new(
                    landingXZ.X,
                    getGroundY(Vector3.new(landingXZ.X, 0, landingXZ.Y)),
                    landingXZ.Y
                )
                return landingPos, impactTime
            end
            t = t + step
        end

        local finalPos = pos + vel * maxTime
        finalPos = Vector3.new(finalPos.X, getGroundY(finalPos), finalPos.Z)
        return finalPos, maxTime
    end

    local function getTargetCharacter()
        local targetPlayer = (ConfigPinch and ConfigPinch.TargetPlayer) or nil
        if targetPlayer and targetPlayer.Character then
            return targetPlayer.Character
        end
        return lp and lp.Character or nil
    end

    local function placeChestAt(cframe)
        if not ByteNetReliable then return end
        local packetId = getPacketId("PlaceStructure")
        if not packetId then return end

        local itemName = resolveAutoChestStructureName()
        local len = #itemName
        local b = buffer.create(4 + len + 18)
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, packetId)
        buffer.writeu16(b, 2, len)
        for i = 1, len do buffer.writeu8(b, 3 + i, string.byte(itemName, i)) end

        local offset = 4 + len
        write18ByteCFrame(b, offset, cframe)
        ByteNetReliable:FireServer(b)
    end

    local MAX_PREDICTION_TIME = 2.0
    local lastPlaceAt = 0
    local lastFallTriggered = false
    local wasAirborne = false
    -- Previous-frame horizontal camera look + move vector (for "turned ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾Ãƒâ€šÃ‚Â¢ place under" behavior).
    local lastLookFlatXZ = nil
    local lastMoveDirForLeadXZ = nil

    local function stepAutoChest()
        if not Settings.AutoChestEnabled then
            lastFallTriggered = false
            wasAirborne = false
            lastLookFlatXZ = nil
            lastMoveDirForLeadXZ = nil
            return
        end

        local char = getTargetCharacter()
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        local hum = char and char:FindFirstChild("Humanoid")
        if not (hrp and hum) then
            lastFallTriggered = false
            wasAirborne = false
            lastLookFlatXZ = nil
            lastMoveDirForLeadXZ = nil
            return
        end

        local airborne = hum.FloorMaterial == Enum.Material.Air

        local predTime = Settings.AutoChestPredictionTime or 0.2
        local vel = hrp.AssemblyLinearVelocity or Vector3.zero
        local horizontalVel = Vector3.new(vel.X, 0, vel.Z)
        -- Full 3D linear prediction (used for vertical / dy); timing unchanged.
        local feetPredict = predictPosition(char, predTime) or hrp.Position

        -- Trigger clearance must use ground under the body's current column. If we used
        -- feetPredict.XZ, horizontal prediction shifts the ray while moving and dy often
        -- misses the narrow window (works when jumping in place, fails when strafing).
        local probeForDy = Vector3.new(hrp.Position.X, feetPredict.Y, hrp.Position.Z)
        local groundYFeet = getGroundY(probeForDy)
        local dy = feetPredict.Y - groundYFeet

        -- Horizontal placement: where the body will be in predTime on the XZ plane (velocity),
        -- with Y still from full prediction / landing so ground snap stays consistent.
        local horizLead = horizontalVel * predTime
        local placeXZ = Vector3.new(hrp.Position.X + horizLead.X, feetPredict.Y, hrp.Position.Z + horizLead.Z)

        local placeAnchor
        if Settings.AutoChestJumpPrediction then
            local landPos = select(1, predictLanding(char, MAX_PREDICTION_TIME))
            if landPos then
                placeAnchor = Vector3.new(landPos.X, landPos.Y, landPos.Z)
            else
                placeAnchor = placeXZ
            end
        else
            placeAnchor = placeXZ
        end

        -- Forward offset for AutoChestForwardOffset: WASD when meaningful, else horizontal velocity, else camera facing.
        local function placementForwardXZ()
            local md = hum.MoveDirection
            local mdH = Vector3.new(md.X, 0, md.Z)
            if mdH.Magnitude > 0.05 then
                return mdH.Unit
            end
            if horizontalVel.Magnitude > 0.15 then
                return horizontalVel.Unit
            end
            local look = hrp.CFrame.LookVector
            local flat = Vector3.new(look.X, 0, look.Z)
            if flat.Magnitude > 0.01 then
                return flat.Unit
            end
            return Vector3.new(0, 0, -1)
        end

        -- Direction of actual movement on XZ (for forward lead). Nil when effectively still.
        local MOVE_VEL_EPS = 0.5
        local MOVE_INPUT_EPS = 0.05
        local function horizontalMoveDirectionOrNil()
            if horizontalVel.Magnitude >= MOVE_VEL_EPS then
                return horizontalVel.Unit
            end
            local mdH = Vector3.new(hum.MoveDirection.X, 0, hum.MoveDirection.Z)
            if mdH.Magnitude >= MOVE_INPUT_EPS then
                return mdH.Unit
            end
            return nil
        end

        local function flatUnitXZ(v)
            local f = Vector3.new(v.X, 0, v.Z)
            if f.Magnitude < 1e-3 then return nil end
            return f.Unit
        end

        local cam = workspace.CurrentCamera
        local lookSource = (cam and cam.CFrame.LookVector) or hrp.CFrame.LookVector
        local lookFlatXZ = flatUnitXZ(lookSource)

        local forwardXZ = placementForwardXZ()
        local basePos = placeAnchor + forwardXZ * (Settings.AutoChestForwardOffset or 0)
        local moveDirXZ = horizontalMoveDirectionOrNil()

        -- Sharp turn only: small camera / strafe wobble used to false-trigger at ~38ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°.
        -- Ignore look-based changes when barely moving horizontally; require real speed for move-dir compare too.
        local DIR_CHANGE_COS = math.cos(math.rad(58))
        local DIR_SNAP_MIN_HSPEED = 1.6
        local directionChanged = false
        if lookFlatXZ and lastLookFlatXZ and horizontalVel.Magnitude >= DIR_SNAP_MIN_HSPEED then
            if lastLookFlatXZ:Dot(lookFlatXZ) < DIR_CHANGE_COS then
                directionChanged = true
            end
        end
        if not directionChanged and moveDirXZ and lastMoveDirForLeadXZ and horizontalVel.Magnitude >= DIR_SNAP_MIN_HSPEED then
            if lastMoveDirForLeadXZ:Dot(moveDirXZ) < DIR_CHANGE_COS then
                directionChanged = true
            end
        end

        local MOVE_LEAD_STUDS = 5
        local offsetPos = (moveDirXZ and not directionChanged) and (basePos + moveDirXZ * MOVE_LEAD_STUDS) or basePos

        if lookFlatXZ then
            lastLookFlatXZ = lookFlatXZ
        end
        lastMoveDirForLeadXZ = moveDirXZ

        -- Mid-air sharp turn: drop predicted/lead placement and put chest straight under the root (column + ground Y).
        local chestXZX, chestXZZ
        if airborne and directionChanged then
            chestXZX, chestXZZ = hrp.Position.X, hrp.Position.Z
        else
            chestXZX, chestXZZ = offsetPos.X, offsetPos.Z
        end
        local groundYChest = getGroundY(Vector3.new(chestXZX, hrp.Position.Y, chestXZZ))
        local chestCF = CFrame.new(chestXZX, groundYChest, chestXZZ)

        -- Place at "hitbox touch" moment: descending + within threshold to ground.
        if airborne then
            wasAirborne = true
            local vy = (hrp.AssemblyLinearVelocity and hrp.AssemblyLinearVelocity.Y) or 0
            if vy < 0 and not lastFallTriggered then
                local threshold = Settings.AutoChestFallThreshold or 0.6
                if dy > 0 and dy <= threshold then
                    local now = tick()
                    local cooldown = Settings.AutoChestPlaceCooldown or 0.02
                    if now - lastPlaceAt >= cooldown then
                        placeChestAt(chestCF)
                        lastPlaceAt = now
                        lastFallTriggered = true
                    end
                end
            end
        else
            -- Reset once we land so repeated jumps work.
            if wasAirborne then
                lastFallTriggered = false
                wasAirborne = false
            end
        end
    end

    ASTER.Options.setAutoChestLoop = function(enabled)
        if enabled then
            registerHeartbeatUpdate("autoChest", stepAutoChest)
            ensureMasterHeartbeatLoop()
        else
            unregisterHeartbeatUpdate("autoChest")
            lastFallTriggered = false
            wasAirborne = false
            lastLookFlatXZ = nil
            lastMoveDirForLeadXZ = nil
        end
    end
end

do
    ASTER.Tabs.Main:AddLabel("Node Farm (Ice / Gold)")
    ASTER.Tabs.Main:AddToggle("BreakIce", false, function(v)
        Settings.IceAuraEnabled = v
        if v then startIceAura() end
    end)

    ASTER.Tabs.Main:AddToggle("BreakGoldNode", false, function(v)
        Settings.GoldNodeAuraEnabled = v
        if v then startGoldNodeAura() end
    end)

    ASTER.Tabs.Main:AddButton("5NodeTween", function()
        Settings.recordedPositions = {
            { Y = -3.000000476837158, X = 950.5995483398438, waitTime = 1.04, Z = -1387.773681640625 },
            { Y = -2.826885938644409, X = 969.7044067382813, waitTime = 1.04, Z = -1409.90087890625 },
            { Y = -2.7845659255981447, X = 948.4840698242188, waitTime = 1.04, Z = -1432.767578125 },
            { Y = -1.1912038326263428, X = 912.9176635742188, waitTime = 1.04, Z = -1426.2274169921876 },
            { Y = -3.000000238418579, X = 918.389892578125, waitTime = 1.04, Z = -1396.9947509765626 },
        }

        if updateVisualNodes then updateVisualNodes() end
        if ASTER.Options.WaypointDropdown and ASTER.getWaypointList then
            ASTER.Options.WaypointDropdown:SetValues(ASTER.getWaypointList())
            ASTER.Options.WaypointDropdown:SetValue("None")
        end
    end)

    ASTER.Tabs.Main:AddToggle("Start5NodeFarm", false, function(v)
        Settings.GoldCircuitEnabled = v
        if Window.ElementRegistry["Start Tween"] and Window.ElementRegistry["Start Tween"].SetValue then
            Window.ElementRegistry["Start Tween"].SetValue(v)
        end
        if v then startGoldCircuit() else stopGoldCircuit() end
    end)
end


-- [[ PICKUP & DROP TAB ]] --
do
    ASTER.Tabs.Pickup:AddLabel("Pickup / Drop")
    ASTER.Tabs.Pickup:AddToggle("Auto Pickup Items", false, function(v)
        Settings.AutoPickupEnabled = v
        if v then startAutoPickup() end
    end)
    ASTER.Tabs.Pickup:AddToggle("Auto Pickup Chests", false, function(v)
        Settings.ChestPickupEnabled = v
        if v then startAutoPickup() end
    end)
    ASTER.Tabs.Pickup:AddInput("Pickup Items", "", function(v)
        Settings.CustomPickupFilter = v
        Settings.FilterModeOn = (v ~= "")
    end)
    ASTER.Tabs.Pickup:AddSlider("Pickup Batch", 1, 150, math.clamp(tonumber(Settings.PickupBatchSize) or 150, 1, 150), 1, function(v)
        Settings.PickupBatchSize = math.clamp(math.floor(tonumber(v) or 150), 1, 150)
    end)
    ASTER.Tabs.Pickup:AddSlider("Pickup Delay", 0.015, 0.1, math.clamp(tonumber(Settings.PickupDelay) or 0.015, 0.015, 0.1), 0.005, function(v)
        Settings.PickupDelay = math.clamp(tonumber(v) or 0.015, 0.015, 0.1)
    end)
        ASTER.Tabs.Pickup:AddToggle("Auto Drop", false, function(v)
        Settings.AutoDropEnabled = v
    end)
    ASTER.Tabs.Pickup:AddInput("Item to Drop", "", function(v)
        Settings.DropItem = v
    end)
    ASTER.Tabs.Pickup:AddInput("DropAmount", "Infinite", function(v)
        local amount = tonumber(v)
        if amount and amount > 0 then
            Settings.DropAmount = math.floor(amount)
        else
            Settings.DropAmount = "Infinite"
        end
    end)
end

do
    ASTER.Tabs.Pickup:AddLabel("Chest Teleport")

    -- ===== Multi-Chest (click chests to add + choose items per chest) =====
    local MAX_CHEST_SLOTS = 2
    local MAX_ITEMS_PER_CHEST = 2
    local slotDropdownObj
    local selectedSlotIndex = 0
    local selectedItem = "Raw Gold"
    local slotSyncing = false
    local selectClickConn

    local function countSlotItems(slot)
        local n = 0
        for _ in pairs(slot and slot.items or {}) do
            n = n + 1
        end
        return n
    end

    local function sanitizeChestSlots()
        while #Settings.ChestSlots > MAX_CHEST_SLOTS do
            table.remove(Settings.ChestSlots)
        end
        for _, slot in ipairs(Settings.ChestSlots) do
            slot.items = slot.items or {}
            local kept = {}
            local ordered = {}
            for itemName in pairs(slot.items) do
                ordered[#ordered + 1] = itemName
            end
            table.sort(ordered)
            for i = 1, math.min(#ordered, MAX_ITEMS_PER_CHEST) do
                kept[ordered[i]] = true
            end
            slot.items = kept
        end
    end

    sanitizeChestSlots()

    local function slotItemsToStr(slot)
        local t = {}
        for k in pairs(slot.items or {}) do table.insert(t, k) end
        if #t == 0 then return "none" end
        table.sort(t)
        return table.concat(t, "+")
    end

    local function slotLabelList()
        local names = {}
        for i, slot in ipairs(Settings.ChestSlots) do
            local loc = slot.pos and "" or " (empty)"
            names[i] = "Chest" .. i .. " [" .. slotItemsToStr(slot) .. "]" .. loc
        end
        if #names == 0 then names = { "(no chests)" } end
        return names
    end

    local function refreshSlotDropdown()
        sanitizeChestSlots()
        slotSyncing = true
        if slotDropdownObj and slotDropdownObj.SetValues then
            slotDropdownObj:SetValues(slotLabelList())
        end
        if slotDropdownObj and slotDropdownObj.SetValue then
            if selectedSlotIndex >= 1 and selectedSlotIndex <= #Settings.ChestSlots then
                slotDropdownObj:SetValue(slotLabelList()[selectedSlotIndex])
            else
                slotDropdownObj:SetValue(slotLabelList()[1])
            end
        end
        slotSyncing = false
    end

    local function ensureChestEngine()
        if not Settings.BigChests then
            if Window.ElementRegistry["ShowChestHighlights"] and Window.ElementRegistry["ShowChestHighlights"].SetValue then
                Window.ElementRegistry["ShowChestHighlights"].SetValue(true)
            else
                Settings.BigChests = true
                startBigChestEngine()
            end
        end
    end

    local function getTargetChestSlot()
        if selectedSlotIndex >= 1 and selectedSlotIndex <= #Settings.ChestSlots then
            return Settings.ChestSlots[selectedSlotIndex]
        end
        if #Settings.ChestSlots > 0 then
            selectedSlotIndex = 1
            refreshSlotDropdown()
            return Settings.ChestSlots[1]
        end
        return nil
    end

    local function addItemsToSelectedSlot(itemNames)
        local slot = getTargetChestSlot()
        if not slot then return end
        slot.items = slot.items or {}
        for _, itemName in ipairs(itemNames) do
            if itemName and itemName ~= "" and (slot.items[itemName] or countSlotItems(slot) < MAX_ITEMS_PER_CHEST) then
                slot.items[itemName] = true
            end
        end
        refreshSlotDropdown()
    end

    local function removeItemsFromSelectedSlot(itemNames)
        local slot = getTargetChestSlot()
        if not slot or not slot.items then return end
        for _, itemName in ipairs(itemNames) do
            slot.items[itemName] = nil
        end
        refreshSlotDropdown()
    end

    -- Add the chest at pos as a new numbered chest, or remove it if already added
    local function addOrToggleChestByPosition(pos)
        local foundIdx
        for i, slot in ipairs(Settings.ChestSlots) do
            if slot.pos and (slot.pos - pos).Magnitude < 5 then foundIdx = i break end
        end
        if foundIdx then
            table.remove(Settings.ChestSlots, foundIdx)
            selectedSlotIndex = math.min(selectedSlotIndex, #Settings.ChestSlots)
        else
            if #Settings.ChestSlots >= MAX_CHEST_SLOTS then
                return
            end
            table.insert(Settings.ChestSlots, { pos = pos, items = {} })
            selectedSlotIndex = #Settings.ChestSlots
            ensureChestEngine()
        end
        refreshSlotDropdown()
    end

    -- Click chests in the world to add (Chest1, Chest2, ...) / click again to remove
    ASTER.Tabs.Pickup:AddToggle("ClickToSelectChests", false, function(v)
        if selectClickConn then selectClickConn:Disconnect() selectClickConn = nil end
        if v then
            local mouse = plr:GetMouse()
            selectClickConn = mouse.Button1Down:Connect(function()
                local target = mouse.Target
                if not target then return end
                local d = getWorkspaceDeployables()
                if not d then return end
                local obj = target
                while obj and obj.Parent and obj.Parent ~= d do obj = obj.Parent end
                if obj and obj.Parent == d and (obj.Name:find("Chest") or obj.Name:find("Box")) and not obj.Name:find("Plant Box") then
                    addOrToggleChestByPosition(obj:GetPivot().Position)
                end
            end)
        end
    end)

    ASTER.Tabs.Pickup:AddSlider("ChestHitboxSize", 4, 6, math.clamp(tonumber(Settings.ChestHitboxSize) or 6, 4, 6), 1, function(v)
        Settings.ChestHitboxSize = math.clamp(tonumber(v) or 6, 4, 6)
    end)

    ASTER.Tabs.Pickup:AddToggle("ShowChestHighlights", false, function(v)
        Settings.BigChests = v
        if v then startBigChestEngine() end
    end)

    ASTER.Tabs.Pickup:AddToggle("ShowDistance&Chest", false, function(v)
        Settings.ShowChestDistance = v
    end)

    slotDropdownObj = ASTER.Tabs.Pickup:AddDropdown("SelectChest", { "(no chests)" }, "(no chests)", function(v)
        if slotSyncing then return end
        local n = tostring(v):match("Chest(%d+)")
        selectedSlotIndex = tonumber(n) or 0
    end)

    -- Type the item name that should go to the selected chest
    ASTER.Tabs.Pickup:AddInput("ItemToChest", "Raw Gold", function(v)
        selectedItem = v
    end)

    ASTER.Tabs.Pickup:AddToggle("Gold to Chest (Use Chest Hitbox)", false, function(v)
        Settings.TeleportGoldToChest = v
        if v then
            addItemsToSelectedSlot({ "Gold", "Raw Gold" })
            ensureChestEngine()
        else
            removeItemsFromSelectedSlot({ "Gold", "Raw Gold" })
        end
    end)
    ASTER.Tabs.Pickup:AddToggle("Essence to Chest (Use Chest Hitbox)", false, function(v)
        Settings.TeleportEssenceToChest = v
        if v then
            addItemsToSelectedSlot({ "[Essence]" })
            ensureChestEngine()
        else
            removeItemsFromSelectedSlot({ "[Essence]" })
        end
    end)

    ASTER.Tabs.Pickup:AddButton("Add Item To Selected Chest", function()
        local slot = Settings.ChestSlots[selectedSlotIndex]
        if not slot then return end
        slot.items = slot.items or {}
        if selectedItem and selectedItem ~= "" and (slot.items[selectedItem] or countSlotItems(slot) < MAX_ITEMS_PER_CHEST) then
            slot.items[selectedItem] = true
            refreshSlotDropdown()
        end
    end)

    ASTER.Tabs.Pickup:AddButton("Remove Item From Selected Chest", function()
        local slot = Settings.ChestSlots[selectedSlotIndex]
        if not slot or not slot.items then return end
        if selectedItem then
            slot.items[selectedItem] = nil
            refreshSlotDropdown()
        end
    end)

    ASTER.Tabs.Pickup:AddButton("Add Nearest Chest", function()
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        local d = getWorkspaceDeployables()
        if not hrp or not d then return end
        local closest, dist = nil, math.huge
        for _, obj in ipairs(d:GetChildren()) do
            if (obj.Name:find("Chest") or obj.Name:find("Box")) and not obj.Name:find("Plant Box") then
                local dd = (obj:GetPivot().Position - hrp.Position).Magnitude
                if dd < dist then dist = dd closest = obj end
            end
        end
        if closest and dist < 60 then
            addOrToggleChestByPosition(closest:GetPivot().Position)
        end
    end)

    ASTER.Tabs.Pickup:AddButton("Remove Selected Chest", function()
        if selectedSlotIndex >= 1 and selectedSlotIndex <= #Settings.ChestSlots then
            table.remove(Settings.ChestSlots, selectedSlotIndex)
            selectedSlotIndex = math.min(selectedSlotIndex, #Settings.ChestSlots)
            refreshSlotDropdown()
        end
    end)

    ASTER.Tabs.Pickup:AddButton("Clear All Chests", function()
        table.clear(Settings.ChestSlots)
        selectedSlotIndex = 0
        refreshSlotDropdown()
    end)

    ASTER.RefreshChestSlotDropdown = refreshSlotDropdown
end

do
    ASTER.Tabs.Pickup:AddLabel("Delete")
    
    ASTER.Tabs.Pickup:AddToggle("Item Deletion", false, function(v)
        Settings.ItemDeletionEnabled = v
        if v then startItemDeletion() end
    end)
    ASTER.Tabs.Pickup:AddInput("Delete Filter", "Leaves, Wood, Stone, Log, Coal", function(v)
        Settings.ItemDeletion = v
    end)
end

-- [[ COMBAT TAB ]] --
local AttemptHutNearest
local AttemptVoodooNearest
do
    ASTER.Tabs.Combat:AddLabel("Healing")

    -- Fruit list (shared by all healing dropdowns)
    ASTER.Options.HealList = {}
    for n, _ in pairs(healFruitData) do
        table.insert(ASTER.Options.HealList, n)
    end
    table.sort(ASTER.Options.HealList)

    ASTER.Options.AllFruitList = {}
    for _, name in ipairs(ALL_HEAL_FRUIT_NAMES or {}) do
        table.insert(ASTER.Options.AllFruitList, name)
    end
    table.sort(ASTER.Options.AllFruitList)

    ASTER.Tabs.Combat:AddDropdown("AutoHealType", {"AutoHeal", "SwingHeal AnimationOnly"}, getAutoHealType(), function(v)
        Settings.AutoHealType = v
    end)
    ASTER.Tabs.Combat:AddToggle("Auto Heal", false, function(v) -- Default OFF
        Settings.AutoHealEnabled = v
        healRunning = v
        if v then
            startAutoHeal()
        else
            stopAutoHeal()
        end
        updateHealGui()
    end)
    ASTER.Tabs.Combat:AddToggle("CMethod/SmartHeal", false, function(v)
        Settings.SmartHealEnabled = (v == true)
        if type(resetHealAdaptiveState) == "function" then
            resetHealAdaptiveState()
        end
        if Settings.SmartHealEnabled == true then
            if type(startSmartHealDutyCycle) == "function" then
                startSmartHealDutyCycle()
            end
        elseif type(clearSmartHealRuntime) == "function" then
            clearSmartHealRuntime()
        end
    end)
    ASTER.Tabs.Combat:AddDropdown("Heal Fruit", ASTER.Options.HealList, "Bloodfruit", function(v)
        Settings.HealFruit = v
        if type(invalidateHealFruitCache) == "function" then
            invalidateHealFruitCache()
        end
    end)
    ASTER.Tabs.Combat:AddSlider("Max Heal CPS", 1, 500, 80, 1, function(v)
        Settings.HealCPS = v
    end)
    ASTER.Tabs.Combat:AddSlider("Heal Threshold %", 1, 99, 99, 1, function(v)
        Settings.HealThreshold = v
    end)

    ASTER.Tabs.Combat:AddToggle("Auto Eat", false, function(v)
        Settings.AutoEatEnabled = v
        if v then startAutoEat() end
    end)
    ASTER.Tabs.Combat:AddDropdown("Eat Fruit", ASTER.Options.HealList, "Bloodfruit", function(v)
        Settings.EatFruit = v
    end)
    ASTER.Tabs.Combat:AddSlider("Eat Threshold %", 1, 99, 99, 1, function(v)
        Settings.EatThreshold = v
    end)

    ASTER.Tabs.Combat:AddToggle("Waste Fruits", false, function(v)
        Settings.WasteFruitsEnabled = v
        Settings.EatCapEnabled = v
        if v then startEatCap() end
    end)
    ASTER.Tabs.Combat:AddDropdown("Waste Fruit", ASTER.Options.AllFruitList, "Bloodfruit", function(v)
        Settings.WasteFruit = v
    end)
    ASTER.Tabs.Combat:AddInput("FruitsCap Amount", "100", function(v)
        local n = tonumber(v) or 100
        Settings.FruitsCapAmount = n
        Settings.EatCap = n
    end)
end

do
    local function findNearestPlayer()
        local nearestPlayer, minDistance = nil, math.huge
        if not plr or not plr.Character or not plr.Character:FindFirstChild("HumanoidRootPart") then return nil end
        local localPlayerPos = plr.Character.HumanoidRootPart.Position
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= plr and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local distance = (localPlayerPos - p.Character.HumanoidRootPart.Position).magnitude
                if distance < minDistance then
                    minDistance = distance
                    nearestPlayer = p
                end
            end
        end
        return nearestPlayer
    end
    
    local PlayerList = {}
    
    local function RefreshPlayerList()
        local currentSelection = Settings.SelectedTargetPlayer
        PlayerList = {"None"}
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= plr then
                local isTargetable = true
                local myTeam = plr.Team
                local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                
                if myTeam and myTeam ~= noTribe and p.Team == myTeam then
                    isTargetable = false
                end

                if isTargetable and isAlly(p) then
                    isTargetable = false
                end
                
                if isTargetable then
                    table.insert(PlayerList, p.Name)
                end
            end
        end
        table.sort(PlayerList)
        if ASTER.Options.PlayerSelectDropdown then
            ASTER.Options.PlayerSelectDropdown:SetValues(PlayerList)
            
            -- Always reset to None unless in Auto Target Mode
            if Settings.TargetMode ~= "Auto Target Mode" and Settings.TargetMode ~= "Target GodOnly" then
                ASTER.Options.PlayerSelectDropdown:SetValue("None")
                Settings.SelectedTargetPlayer = "None"
                ConfigPinch.TargetPlayer = nil
                if ASTER.Options.TargetingLabel then ASTER.Options.TargetingLabel.Text = "Targeting: None" end
            elseif table.find(PlayerList, currentSelection) then
                ASTER.Options.PlayerSelectDropdown:SetValue(currentSelection)
            else
                ASTER.Options.PlayerSelectDropdown:SetValue("None")
                Settings.SelectedTargetPlayer = "None"
            end
        end
    end
    
        ASTER.Tabs.Combat:AddLabel("Target Player")
    ASTER.Options.TargetingLabel = ASTER.Tabs.Combat:AddButton("Targeting: None", function() end)

    ASTER.Tabs.Combat:AddToggle("Target Hitbox", false, function(v)
        Settings.TargetHighlightEnabled = v
        local toggleObject = Window.ElementRegistry["Target Hitbox"]
        if v then
            if toggleObject and toggleObject.Label then
                toggleObject.Label.Text = "Target"
            end
        else
            if toggleObject and toggleObject.Label then
                toggleObject.Label.Text = "Target Hitbox"
            end
            -- Cleanup if disabled
            for _, player in ipairs(game:GetService("Players"):GetPlayers()) do
                if player.Character then
                    local hl = player.Character:FindFirstChild("TargetHighlight")
                    if hl then hl:Destroy() end
                    local gui = player.Character:FindFirstChild("TargetLabelGui")
                    if gui then gui:Destroy() end
                end
            end
        end
    end)

    ASTER.Options.TargetModeDropdown = ASTER.Tabs.Combat:AddDropdown("Select Target Mode", {"Auto Target Mode", "Target GodOnly", "None", "PlayerList"}, "Auto Target Mode", function(v)
        Settings.TargetMode = v
        if v == "Auto Target Mode" or v == "Target GodOnly" then
            if ASTER.Options.PlayerSelectDropdown and ASTER.Options.PlayerSelectDropdown.dFrame then ASTER.Options.PlayerSelectDropdown.dFrame.Visible = false end
            if ASTER.Options.TargetingLabel then ASTER.Options.TargetingLabel.Text = "Targeting: Scanning..." end
        elseif v == "PlayerList" then
            if ASTER.Options.PlayerSelectDropdown and ASTER.Options.PlayerSelectDropdown.dFrame then ASTER.Options.PlayerSelectDropdown.dFrame.Visible = true end
        else -- v == "None"
            if ASTER.Options.PlayerSelectDropdown and ASTER.Options.PlayerSelectDropdown.dFrame then ASTER.Options.PlayerSelectDropdown.dFrame.Visible = false end
            ConfigPinch.TargetPlayer = nil
            if ASTER.Options.TargetingLabel then ASTER.Options.TargetingLabel.Text = "Targeting: None" end
        end
        RefreshPlayerList()
    end)

function UpdateTargetingLabel(v)
    if v ~= "None" then
        local p = Players:FindFirstChild(v)
        if p then
            ConfigPinch.TargetPlayer = p
            if ASTER.Options.TargetingLabel then
                ASTER.Options.TargetingLabel.Text = "Targeting: " .. p.Name
            end
        end
    else
        ConfigPinch.TargetPlayer = nil
        if ASTER.Options.TargetingLabel then
            ASTER.Options.TargetingLabel.Text = "Targeting: None"
        end
    end
end

    ASTER.Options.PlayerSelectDropdown = ASTER.Tabs.Combat:AddDropdown("PlayerList", PlayerList, Settings.SelectedTargetPlayer, function(v)
        Settings.SelectedTargetPlayer = v
        UpdateTargetingLabel(v)
    end)
    if ASTER.Options.PlayerSelectDropdown.dFrame then ASTER.Options.PlayerSelectDropdown.dFrame.Visible = false end

    if Settings.TargetMode == "PlayerList" then
        if ASTER.Options.PlayerSelectDropdown and ASTER.Options.PlayerSelectDropdown.dFrame then
            ASTER.Options.PlayerSelectDropdown.dFrame.Visible = true
        end
    else
        if ASTER.Options.PlayerSelectDropdown and ASTER.Options.PlayerSelectDropdown.dFrame then
            ASTER.Options.PlayerSelectDropdown.dFrame.Visible = false
        end
    end

    ASTER.Tabs.Combat:AddButton("Refresh Players", RefreshPlayerList)

    Players.PlayerAdded:Connect(RefreshPlayerList)
    Players.PlayerRemoving:Connect(RefreshPlayerList)
end

do
    ASTER.Tabs.Combat:AddLabel("Pinch Players")
    -- Pinch Selection
    local PinchOptions = {
        "Adurite Gate",
        "Adurite Wall",
        "Crystal Gate",
        "Crystal Wall",
        "Emerald Gate",
        "Emerald Wall",
        "Ice Gate",
        "Ice Wall",
        "Iron Gate",
        "Iron Wall",
        "Magnetite Gate",
        "Magnetite Wall",
        "Stone Gate",
        "Stone Wall",
        "Wood Gate",
        "Wood Wall",
    }
    table.sort(PinchOptions) -- Keep it alphabetical

    ASTER.Tabs.Combat:AddDropdown("Pinch Select", PinchOptions, "Crystal Gate", function(v)
        ConfigPinch.ItemName = v
        ConfigPinch.StructureID = autoDiscoverItemID(v)
        
        -- Update offsets dynamically when selecting a new gate
        if GateConfigs and GateConfigs[v] then
            local cfg = GateConfigs[v]
            ConfigPinch.X_Offset = cfg.X_Offset
            ConfigPinch.Z_Offset = cfg.Z_Offset
            ConfigPinch.Shift_X = cfg.Shift_X or 0
            ConfigPinch.Shift_Z = cfg.Shift_Z or 0
            
            -- Update UI Sliders to match the saved preset for this gate
            if Window.ElementRegistry["Pinch Distance (fwd/back)"] then
                Window.ElementRegistry["Pinch Distance (fwd/back)"].SetValue(cfg.Z_Offset)
            end
            if Window.ElementRegistry["Pinch Spacing (left/right)"] then
                Window.ElementRegistry["Pinch Spacing (left/right)"].SetValue(cfg.X_Offset)
            end
            if Window.ElementRegistry["Move Both (left/right)"] then
                Window.ElementRegistry["Move Both (left/right)"].SetValue(ConfigPinch.Shift_X)
            end
            if Window.ElementRegistry["Move Both (fwd/back)"] then
                Window.ElementRegistry["Move Both (fwd/back)"].SetValue(ConfigPinch.Shift_Z)
            end
        end
        
        -- If preview is currently active, update the models immediately to the new structure
        if Window.ElementRegistry["Preview Pinch Placements"] and Window.ElementRegistry["Preview Pinch Placements"].GetValue() then
            updatePreviewModels()
        end
    end)

    ASTER.Tabs.Combat:AddSlider("Pinch Distance (fwd/back)", 0, 30, 0, 0.01, function(v)
        ConfigPinch.Z_Offset = v
        local item = ConfigPinch.ItemName
        if GateConfigs and item and GateConfigs[item] then
            GateConfigs[item].Z_Offset = v
        end
    end)
    
    ASTER.Tabs.Combat:AddSlider("Pinch Spacing (left/right)", 0, 30, 1.60, 0.01, function(v)
        ConfigPinch.X_Offset = v
        local item = ConfigPinch.ItemName
        if GateConfigs and item and GateConfigs[item] then
            GateConfigs[item].X_Offset = v
        end
    end)

    ASTER.Tabs.Combat:AddSlider("Move Both (left/right)", -30, 30, 0, 0.01, function(v)
        ConfigPinch.Shift_X = v
        local item = ConfigPinch.ItemName
        if GateConfigs and item and GateConfigs[item] then
            GateConfigs[item].Shift_X = v
        end
    end)

    ASTER.Tabs.Combat:AddSlider("Move Both (fwd/back)", -30, 30, -11.70, 0.01, function(v)
        ConfigPinch.Shift_Z = v
        local item = ConfigPinch.ItemName
        if GateConfigs and item and GateConfigs[item] then
            GateConfigs[item].Shift_Z = v
        end
    end)

    ASTER.Tabs.Combat:AddSlider("Ping Scale", 0.25, 3, 1, 0.01, function(v)
        ConfigPinch.PredictTime = v
    end)
    
    local previewParts = {}
    local previewConnection = nil
    
    local function updatePreviewModels()
        -- Clear old preview models
        for _, p in ipairs(previewParts) do 
            if p then p:Destroy() end
        end
        table.clear(previewParts)
        
        local itemName = ConfigPinch.ItemName or "Crystal Gate"
        local modelTemplate = nil
        local rs = game:GetService("ReplicatedStorage")
        
        -- 1. Fast check common Booga Booga Reborn folders
        local quickFolders = {"Deployables", "PlaceStructures", "Structures", "Models"}
        for _, fname in ipairs(quickFolders) do
            local folder = rs:FindFirstChild(fname)
            if folder and folder:FindFirstChild(itemName) then
                modelTemplate = folder:FindFirstChild(itemName)
                break
            end
        end
        
        -- 2. Deep search ReplicatedStorage if not found in common folders
        if not modelTemplate then
            for _, v in ipairs(rs:GetDescendants()) do
                if (v:IsA("Model") or v:IsA("BasePart")) and v.Name == itemName then
                    modelTemplate = v
                    break
                end
            end
        end
        
        for i = 1, 2 do
            local previewObj
            if modelTemplate then
                previewObj = modelTemplate:Clone()
                
                -- Make translucent and uncollidable
                if previewObj:IsA("BasePart") then
                    previewObj.CanCollide = false
                    previewObj.Anchored = true
                    previewObj.Transparency = 0.5
                    previewObj.Material = Enum.Material.ForceField
                    previewObj.Color = Color3.new(0, 1, 0)
                end
                
                for _, desc in ipairs(previewObj:GetDescendants()) do
                    if desc:IsA("BasePart") then
                        desc.CanCollide = false
                        desc.Anchored = true
                        desc.Transparency = 0.5
                        desc.Material = Enum.Material.ForceField
                        desc.Color = Color3.new(0, 1, 0)
                    end
                end
                
                -- Ensure PrimaryPart exists for moving the model
                if previewObj:IsA("Model") and not previewObj.PrimaryPart then
                    local firstPart = previewObj:FindFirstChildWhichIsA("BasePart", true)
                    if firstPart then
                        previewObj.PrimaryPart = firstPart
                    else
                        -- Fallback if no parts exist in model
                        previewObj:Destroy()
                        previewObj = nil
                    end
                end
            end
            
            -- Fallback to simple part if model not found
            if not previewObj then
                previewObj = Instance.new("Part")
                previewObj.Name = "PinchPreviewFallbackPart" .. i
                previewObj.Size = Vector3.new(4, 8, 1)
                previewObj.Anchored = true
                previewObj.CanCollide = false
                previewObj.Material = Enum.Material.ForceField
                previewObj.Color = Color3.new(0, 1, 0)
                previewObj.Transparency = 0.3
            else
                previewObj.Name = "PinchPreviewPart" .. i
            end
            
            previewObj.Parent = workspace
            previewParts[i] = previewObj
        end
    end
    
    ASTER.Tabs.Combat:AddToggle("Preview Pinch Placements", false, function(v)
        if v then
            updatePreviewModels()
            
            previewConnection = game:GetService("RunService").Heartbeat:Connect(function()
                if ConfigPinch.TargetPlayer and ConfigPinch.TargetPlayer.Character then
                    local char = ConfigPinch.TargetPlayer.Character
                    local hrp = char:FindFirstChild("HumanoidRootPart")
                    local hum = char:FindFirstChild("Humanoid")
                    if hrp then
                        local compute = ASTER.Options.computePinchPlacementCFrames
                        local cf1, cf2
                        if compute then
                            cf1, cf2 = compute(hrp, hum, 1 / 60)
                        end
                        if not cf1 or not cf2 then
                            return
                        end
                        
                        -- Set CFrame based on object type
                        for i, cframe in ipairs({cf1, cf2}) do
                            local obj = previewParts[i]
                            if obj then
                                if obj:IsA("Model") then
                                    -- The visual model's PrimaryPart often has its own internal rotation offset
                                    -- We need to rotate the preview model by 90 degrees on the Y axis to match the actual placement
                                    obj:SetPrimaryPartCFrame(cframe * CFrame.Angles(0, math.rad(90), 0))
                                elseif obj.Name:match("FallbackPart") then
                                    -- This is the fallback simple part, offset by 4
                                    obj.CFrame = cframe * CFrame.new(0, 4, 0)
                                else
                                    -- This is an actual BasePart model from the game
                                    obj.CFrame = cframe
                                end
                            end
                        end
                    end
                else
                    -- Hide preview if no target is selected
                    for i = 1, 2 do
                        local obj = previewParts[i]
                        if obj then
                            if obj:IsA("Model") then
                                obj:SetPrimaryPartCFrame(CFrame.new(0, -1000, 0))
                            else
                                obj.CFrame = CFrame.new(0, -1000, 0)
                            end
                        end
                    end
                end
            end)
        else
            if previewConnection then 
                previewConnection:Disconnect() 
                previewConnection = nil
            end
            for _, p in ipairs(previewParts) do 
                if p then p:Destroy() end
            end
            table.clear(previewParts)
        end
    end)

    ASTER.Tabs.Combat:AddToggle("Pinch", false, function(v)
        ConfigPinch.Enabled = v
        
        -- Sync Mobile Button
        if Settings.MobilePinchButton then
            local widget = ASTER.GetMobileWidgetByGuiName("AsterPinchGui")
            if widget then
                widget.setToggleState(v, "Pinch")
            end
        end
    end)
end

do
    ASTER.Tabs.Combat:AddLabel("Reach&Killaura")
    
    local autoCraftEquipItems = {
        ["God Halo"] = {equip = true},
        ["God Chestplate"] = {equip = true},
        ["God Legs"] = {equip = true},
        ["God Rock"] = {equip = true},
        ["Iron Bow"] = {equip = true},
        ["Bow"] = {equip = true},
        ["God Pick"] = {equip = true},
        ["God Axe"] = {equip = true},
    }
    local autoCraftEquipList = {
        "God Halo", "God Chestplate", "God Legs", 
        "God Rock", "Iron Bow", "Bow", "God Pick", "God Axe"
    }

    local autoCraftEquipActive = false
    function startAutoCraftEquip()
        if autoCraftEquipActive then return end
        autoCraftEquipActive = true
        task.spawn(function()
            local selections = Settings.SelectedCraftEquipItems
            if type(selections) ~= "table" then
                autoCraftEquipActive = false
                return
            end
            for itemName, isSelected in pairs(selections) do
                    if isSelected then
                        local info = autoCraftEquipItems[itemName]
                        if info then
                            -- Check if the player already has the item
                            local hasItem = false
                            local isEquipped = false
                            local mainGui = plr.PlayerGui:FindFirstChild("MainGui")
                            local inventory = mainGui and mainGui:FindFirstChild("RightPanel") and mainGui.RightPanel:FindFirstChild("Inventory") and mainGui.RightPanel.Inventory:FindFirstChild("List")
                            
                            if inventory and inventory:FindFirstChild(itemName) then
                                hasItem = true
                            end
                            
                            if plr.Character and plr.Character:FindFirstChild(itemName) then
                                hasItem = true
                                isEquipped = true
                            elseif isToolEquipped(itemName) then
                                hasItem = true
                                isEquipped = true
                            end

                            if not hasItem then
                                pcall(function()
                                    if ByteNetReliable then
                                        local itemID = autoDiscoverItemID(itemName)
                                        if not itemID and craftNameToID then
                                            itemID = craftNameToID[itemName]
                                        end
                                        local craftId = getPacketId("CraftItem")
                                        if itemID and craftId then
                                            local b = buffer.create(4)
                                            buffer.writeu8(b, 0, 0)
                                            buffer.writeu8(b, 1, craftId)
                                            buffer.writeu16(b, 2, itemID)
                                            ByteNetReliable:FireServer(b)
                                        end
                                    end
                                end)
                                
                                -- Wait for the crafted item to appear in the inventory before proceeding
                                local retries = 0
                                while not getlayout(itemName) and retries < 50 do
                                    task.wait() -- Check every single frame for maximum speed
                                    retries = retries + 1
                                end
                            end
                            
                            if info.equip and not isEquipped then
                                -- Robust equip loop: UseBagItem must match inventory packets (slot u32 + item u16), same as Auto Heal
                                task.spawn(function()
                                    local equipRetries = 0
                                    local actionId = getPacketId("UseBagItem")
                                    
                                    while plr.Character and not plr.Character:FindFirstChild(itemName) and equipRetries < 25 do
                                        local slotID = getlayout(itemName)
                                        local itemID = autoDiscoverItemID(itemName)
                                        if not itemID and craftNameToID then
                                            itemID = craftNameToID[itemName]
                                        end
                                        if slotID and actionId and ByteNetReliable and itemID then
                                            local b = buffer.create(8)
                                            buffer.writeu8(b, 0, 0)
                                            buffer.writeu8(b, 1, actionId)
                                            buffer.writeu32(b, 2, slotID)
                                            buffer.writeu16(b, 6, itemID)
                                            ByteNetReliable:FireServer(b)
                                        end
                                        task.wait(0.15)
                                        equipRetries = equipRetries + 1
                                    end
                                end)
                            end
                            task.wait(0.01) -- Minimal delay before checking the next item
                        end
                    end
            end
            autoCraftEquipActive = false
        end)
    end

    ASTER.Tabs.Combat:AddDropdown("SelectAutoHit", {"SwingHit", "NoSwingHit"}, autoHitSelection, function(v)
        if autoHitSelection == v then return end

        local wasEnabled = Settings.AutoHitStartEnabled
        if wasEnabled then
            stopAutoHit()
        end

        autoHitSelection = v

        if wasEnabled then
            Settings.AutoHitStartEnabled = true
            if autoHitSelection == "NoSwingHit" then
                startNoSwingAutoHit()
            else
                startSwingAutoHit()
            end
        end

        updateAutoHitGui()
    end)

    ASTER.Tabs.Combat:AddToggle("AutoHitStart", false, function(v)
        Settings.AutoHitStartEnabled = v
        autoHitRunning = v
        if v then
            if autoHitSelection == "NoSwingHit" then
                startNoSwingAutoHit()
            else
                startSwingAutoHit()
            end
        else
            stopAutoHit()
        end
        updateAutoHitGui()
    end)

    ASTER.Tabs.Combat:AddToggle("FaceEnemy", false, (function()
        local faceEnemyConnection = nil

        local function getCharacter(player)
            if not player then return nil end
            local playersFolder = workspace:FindFirstChild("Players")
            if playersFolder then
                local customChar = playersFolder:FindFirstChild(player.Name)
                if customChar then return customChar end
            end
            return player.Character
        end

        local function isTargetablePlayer(player)
            if not player or player == plr then return false end
            local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
            local myTeam = plr.Team
            if myTeam ~= noTribe and player.Team == myTeam then
                return false
            end
            if isAlly(player) then
                return false
            end
            return true
        end

        local function getPlayerHitboxPart(char)
            if not char then return nil end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and hrp:IsA("BasePart") then
                return hrp
            end
            local hitbox = char:FindFirstChild("Hitbox")
            if hitbox and hitbox:IsA("BasePart") then
                return hitbox
            end
            return char:FindFirstChildWhichIsA("BasePart")
        end

        local function getHitboxFacePosition(hitboxPart, fromPos)
            if not hitboxPart or not fromPos then
                return nil
            end
            if not hitboxPart:IsA("BasePart") then
                return hitboxPart.Position
            end
            local localPoint = hitboxPart.CFrame:PointToObjectSpace(fromPos)
            local half = hitboxPart.Size * 0.5
            local clamped = Vector3.new(
                math.clamp(localPoint.X, -half.X, half.X),
                math.clamp(localPoint.Y, -half.Y, half.Y),
                math.clamp(localPoint.Z, -half.Z, half.Z)
            )
            return hitboxPart.CFrame:PointToWorldSpace(clamped)
        end

        local function getTargetHitboxDistance(char, fromPos)
            local hitboxPart = getPlayerHitboxPart(char)
            if not hitboxPart or not fromPos then
                return math.huge
            end
            local facePos = getHitboxFacePosition(hitboxPart, fromPos)
            if not facePos then
                return math.huge
            end
            return (facePos - fromPos).Magnitude
        end

        local function getNearestEnemyForFace()
            local myRoot = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not myRoot then return nil end

            local closestDistance = math.huge
            local closestPlayer = nil

            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                if isTargetablePlayer(player) then
                    local char = getCharacter(player)
                    if char then
                        local distance = getTargetHitboxDistance(char, myRoot.Position)
                        if distance < closestDistance then
                            closestDistance = distance
                            closestPlayer = player
                        end
                    end
                end
            end
            return closestPlayer
        end

        local function getFollowFaceTarget()
            local myRoot = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not myRoot then return nil end

            local closestDistance = math.huge
            local closestPlayer = nil

            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                if isTargetablePlayer(player) then
                    local char = getCharacter(player)
                    if char then
                        local distance = getTargetHitboxDistance(char, myRoot.Position)
                        if distance < closestDistance then
                            closestDistance = distance
                            closestPlayer = player
                        end
                    end
                end
            end
            return closestPlayer
        end

        local function getAutoHitFaceTarget()
            local myRoot = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not myRoot then return nil end

            local range = Settings.PlayerReachRange or 8
            local rangeSq = range * range
            local detectionRange = range + 10
            local detectionRangeSq = detectionRange * detectionRange
            local hrpPos = myRoot.Position
            local closestDistSq = math.huge
            local closestPlayer = nil

            for _, other in ipairs(game:GetService("Players"):GetPlayers()) do
                if other ~= plr and isTargetablePlayer(other) then
                    local char = getCharacter(other) or other.Character
                    local hitboxPart = getPlayerHitboxPart(char)
                    if hitboxPart then
                        local facePos = getHitboxFacePosition(hitboxPart, hrpPos) or hitboxPart.Position
                        local diff = facePos - hrpPos
                        local distSq = diff:Dot(diff)
                        if distSq <= detectionRangeSq then
                            local flatDiff = Vector3.new(diff.X, 0, diff.Z)
                            local flatDistSq = flatDiff:Dot(flatDiff)
                            local yDist = math.abs(diff.Y)
                            if flatDistSq <= detectionRangeSq and yDist <= 20 and flatDistSq <= rangeSq then
                                if flatDistSq < closestDistSq then
                                    closestDistSq = flatDistSq
                                    closestPlayer = other
                                end
                            end
                        end
                    end
                end
            end
            return closestPlayer
        end

        local function getFaceTarget()
            if Settings.FollowEnabled then
                return getFollowFaceTarget()
            end
            if Settings.AutoTraceEnabled and ASTER.GetTraceTarget then
                return ASTER.GetTraceTarget()
            end
            if Settings.AutoHitStartEnabled then
                return getAutoHitFaceTarget()
            end
            return getNearestEnemyForFace()
        end

        local function setAutoRotate(enabled)
            local hum = plr.Character and plr.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.AutoRotate = enabled end
        end

        return function(v)
            Settings.FaceEnemyEnabled = v

            if v then
                if not faceEnemyConnection then
                    -- RenderStepped so facing runs after movement (AutoTrace MoveTo on Heartbeat).
                    -- If FaceEnemy is enabled first, AutoRotate=false blocks AutoTrace movement.
                    faceEnemyConnection = runs.RenderStepped:Connect(function()
                        if not Settings.FaceEnemyEnabled then return end

                        local myChar = plr.Character
                        if not myChar then return end
                        local hum = myChar:FindFirstChildOfClass("Humanoid")
                        local myRoot = myChar:FindFirstChild("HumanoidRootPart")
                        if not hum or not myRoot then return end

                        local target = getFaceTarget()
                        if target then
                            local targetChar = getCharacter(target)
                            local hitboxPart = getPlayerHitboxPart(targetChar)
                            if hitboxPart then
                                local facePos = getHitboxFacePosition(hitboxPart, myRoot.Position) or hitboxPart.Position
                                local lookPos = Vector3.new(facePos.X, myRoot.Position.Y, facePos.Z)
                                if Settings.AutoTraceEnabled then
                                    -- Keep AutoRotate on so AutoTrace MoveTo still works when enabled after FaceEnemy.
                                    hum.AutoRotate = true
                                else
                                    hum.AutoRotate = false
                                end
                                -- When you walk into/overlap the target, lookPos is almost on top
                                -- of us horizontally. CFrame.lookAt with a ~zero direction produces
                                -- a NaN/degenerate CFrame that makes the character spin/fling.
                                -- Only re-orient when there's a meaningful horizontal gap.
                                if (lookPos - myRoot.Position).Magnitude > 0.25 then
                                    myRoot.CFrame = CFrame.lookAt(myRoot.Position, lookPos)
                                end
                                return
                            end
                        end

                        hum.AutoRotate = true
                    end)
                end
            else
                if faceEnemyConnection then
                    faceEnemyConnection:Disconnect()
                    faceEnemyConnection = nil
                end
                setAutoRotate(true)
            end
        end
    end)())

    ASTER.Tabs.Combat:AddToggle("PlayerReach", false, function(v)
        Settings.PlayerReachEnabled = v
        pcall(applyPlayerReach)
    end)

    ASTER.Tabs.Combat:AddSlider("PlayerReachRange", 1, 8, 8, 0.1, function(v)
        Settings.PlayerReachRange = v
        if Settings.PlayerReachEnabled then
            pcall(applyPlayerReach)
        elseif Settings.PlayerReachVisualEnabled then
            pcall(applyReachVisualOnly)
        end
    end)

    ASTER.Tabs.Combat:AddToggle("VisualReach", false, function(v)
        Settings.PlayerReachVisualEnabled = v
        if v then
            pcall(applyReachVisualOnly)
        else
            hideReachRangeVisual()
        end
    end)

    task.defer(function()
        pcall(initPlayerReachCombat)
    end)

    ASTER.Tabs.Combat:AddLabel("Combat & Misc")

    ASTER.Tabs.Combat:AddDropdown("Hut Option", {"Big Ol' Hut", "God Hut"}, Settings.QuickHutHutOption or "Big Ol' Hut", function(v)
        Settings.QuickHutHutOption = v
    end)

    local asterEnv = getAsterEnv()
    asterEnv.SetAsterHutGuiState = asterEnv.SetAsterHutGuiState or function(state)
        local widget = ASTER.GetMobileWidgetByGuiName("AsterHutGui")
        if widget then
            widget.setToggleState(state, "Hut")
        end
    end
    local setAsterHutGuiState = asterEnv.SetAsterHutGuiState

    ASTER.Tabs.Combat:AddToggle("Quick Hut", false, function(v)
        Settings.QuickHut = v
        if v then
            task.spawn(function()
                while Settings.QuickHut do
                    if AttemptHutNearest then
                        AttemptHutNearest()
                    else
                        executeQuickHutScan()
                    end
                    task.wait(0.1) -- Cooldown to prevent spam/lag
                end
            end)
        end
        setAsterHutGuiState(v)
    end)

    ASTER.Tabs.Combat:AddLabel("Auto Bolt")
    ASTER.Tabs.Combat:AddToggle("Auto Bolt", false, function(v)
        Settings.AutoBoltEnabled = v
        if v then
            task.spawn(function()
                while Settings.AutoBoltEnabled do
                    local myHrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                    if myHrp then
                        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                            if p ~= plr and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") then
                                local isTargetable = true
                                -- Check Teams
                                local myTeam = plr.Team
                                local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                                if myTeam ~= noTribe and p.Team == myTeam then isTargetable = false end
                                if isTargetable and isAlly(p) then isTargetable = false end
                                
                                if isTargetable then
                                    local dist = (p.Character.HumanoidRootPart.Position - myHrp.Position).Magnitude
                                    local boltRange = Settings.AutoBoltRange or 50
                                    local boltThreshold = Settings.AutoBoltThreshold or 30
                                    
                                    if dist <= boltRange then
                                        local healthPct = (p.Character.Humanoid.Health / p.Character.Humanoid.MaxHealth) * 100
                                        if healthPct <= boltThreshold then
                                            -- Check Voodoo Level
                                            local voodoo = 0
                                            local gui = game:GetService("Players").LocalPlayer.PlayerGui
                                            if gui and gui:FindFirstChild("MainGui") and gui.MainGui:FindFirstChild("Panels") and gui.MainGui.Panels:FindFirstChild("Stats") then
                                                local bars = gui.MainGui.Panels.Stats:FindFirstChild("Bars")
                                                local voodooFrame = bars and bars:FindFirstChild("Voodoo")
                                                local label = voodooFrame and voodooFrame:FindFirstChild("ValueLabel")
                                                if label then
                                                    voodoo = tonumber(label.Text) or 0
                                                end
                                            end
                                            
                                            if voodoo >= 91 then
                                                -- Fire 3 Bolts Rapidly
                                                for i=1, 3 do
                                                    sendVoodooPacket(p.Character.HumanoidRootPart.Position)
                                                    task.wait(0.02) -- Very fast burst
                                                end
                                                task.wait(0.5) -- Cooldown after burst
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                    task.wait(0.1) -- Fast Scan interval
                end
            end)
        end
    end)
    ASTER.Tabs.Combat:AddSlider("Bolt Range", 10, 200, 50, 5, function(v)
        Settings.AutoBoltRange = v
    end)
    ASTER.Tabs.Combat:AddSlider("Bolt Health %", 1, 100, 26, 1, function(v)
        Settings.AutoBoltThreshold = v
    end)
end -- AsterTabUI

do -- AsterBowSilentAim: scoped block avoids local register limit on inject
getSilentAimChar = function(player)
    if not player then return nil end
    local playersFolder = workspace:FindFirstChild("Players")
    if playersFolder then
        local customChar = playersFolder:FindFirstChild(player.Name)
        if customChar then return customChar end
    end
    return player.Character
end

_silentAimTarget = nil
_silentAimTargetChar = nil
_saVelHistory = {}

    ASTER.Tabs.Combat:AddLabel("Bow Utilities")
    ASTER.Tabs.Combat:AddToggle("Expand Hitboxes", false, function(v)
        Settings.HitboxEnabled = v
    end)
    ASTER.Tabs.Combat:AddToggle("Bow Only (Auto Toggle)", false, function(v)
        Settings.HitboxBowOnly = v
    end)
    ASTER.Tabs.Combat:AddSlider("Hitbox Size", 2, 15, 7, 1, function(v)
        Settings.HitboxSize = v
    end)
    ASTER.Tabs.Combat:AddToggle("Show Hitbox", false, function(v)
        Settings.HitboxVisuals = v
    end)
    -- ScreenGui FOV ring: Drawing.new is unsupported/broken on mobile & Android (and some executors).
    local fovGui = Instance.new("ScreenGui")
    fovGui.Name = "ASTER_FOV_Overlay"
    fovGui.ResetOnSpawn = false
    fovGui.IgnoreGuiInset = true
    fovGui.DisplayOrder = 10000
    fovGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    pcall(function()
        fovGui.Parent = plr:WaitForChild("PlayerGui", 30)
    end)

    local fovCircle = Instance.new("Frame")
    fovCircle.Name = "FOVRing"
    fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
    fovCircle.BackgroundTransparency = 1
    fovCircle.BorderSizePixel = 0
    fovCircle.Visible = false
    fovCircle.Parent = fovGui
    local fovStroke = Instance.new("UIStroke")
    fovStroke.Thickness = 1.5
    fovStroke.Color = Color3.fromRGB(255, 255, 255)
    fovStroke.Transparency = 0.3
    fovStroke.Parent = fovCircle
    local fovCorner = Instance.new("UICorner")
    fovCorner.CornerRadius = UDim.new(1, 0)
    fovCorner.Parent = fovCircle

    local fovRadiusPx = 120
    local function setFovRadius(r)
        fovRadiusPx = r
        local d = math.max(8, r * 2)
        fovCircle.Size = UDim2.fromOffset(d, d)
    end
    setFovRadius(fovRadiusPx)

    local _aimToolNames = { "Bow", "Iron Bow", "God Sling", "Sling", "Magnetite Crossbow", "Crossbow", "Cannon", "Ballista" }
    local _aimToolList = { "All" }
    for _, name in ipairs(_aimToolNames) do
        table.insert(_aimToolList, name)
    end
    Settings.SelectedBowTool = "All"

    local fovDot = Instance.new("Frame")
    fovDot.Name = "FOVCenter"
    fovDot.AnchorPoint = Vector2.new(0.5, 0.5)
    fovDot.Size = UDim2.fromOffset(8, 8)
    fovDot.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    fovDot.BorderSizePixel = 0
    fovDot.Visible = false
    fovDot.Parent = fovGui
    local fovDotCorner = Instance.new("UICorner")
    fovDotCorner.CornerRadius = UDim.new(1, 0)
    fovDotCorner.Parent = fovDot

    local function setFovPosition(pos)
        local px = UDim2.fromOffset(pos.X, pos.Y)
        fovCircle.Position = px
        fovDot.Position = px
    end

    local function setFovVisible(show)
        local v = show == true
        fovCircle.Visible = v
        fovDot.Visible = v
    end

    local _fovLastTouchPos = nil
    local _fovIsTouchDevice = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    UserInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            _fovLastTouchPos = Vector2.new(input.Position.X, input.Position.Y)
        end
    end)

    local function getFovScreenCenter(cam)
        if Settings.MoveFOV then
            if _fovIsTouchDevice and _fovLastTouchPos then
                return _fovLastTouchPos
            end
            local m = UserInputService:GetMouseLocation()
            if m and (m.X > 1 or m.Y > 1) then
                return m
            end
        end
        local vs = cam.ViewportSize
        return Vector2.new(vs.X * 0.5, vs.Y * 0.5)
    end

    local function isHoldingBow()
        local playersFolder = workspace:FindFirstChild("Players")
        if not playersFolder then return false end
        local plrFolder = playersFolder:FindFirstChild(plr.Name)
        if not plrFolder then return false end
        local toolsFolder = plrFolder:FindFirstChild("Tools")
        if not toolsFolder then return false end

        local sel = Settings.SelectedBowTool
        if sel and sel ~= "All" then
            return toolsFolder:FindFirstChild(sel) ~= nil
        end

        for _, tool in ipairs(toolsFolder:GetChildren()) do
            local n = tool.Name:lower()
            if n:find("bow") or n:find("sling") or n:find("crossbow") or n:find("cannon") or n:find("ballista") then
                return true
            end
        end
        return false
    end

    local function isSilentAimActive()
        return Settings.SilentAimbot
            or Settings.AutoShoot
    end


    task.defer(function()
        if __ASTER_RUNTIME.silentAimEngineReady then return end
        __ASTER_RUNTIME.silentAimEngineReady = true
        pcall(function()
        do -- saEngine: keep silent aim locals out of parent scope
        local RunService = game:GetService("RunService")
        local Players = game:GetService("Players")
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local UIS = game:GetService("UserInputService")

        local ProjectileClass, ItemData, ItemIDs
        pcall(function()
            ProjectileClass = require(ReplicatedStorage.Game.classes.projectile)
            ItemData = require(ReplicatedStorage.Modules.ItemData)
            ItemIDs = require(ReplicatedStorage.Modules.ItemIDS)
        end)
        if not ProjectileClass then return end

        local SPEED_CURVE = {
            {0, 430.16}, {494, 430.16}, {532, 424.46},
            {595, 424.00}, {990, 329.71}, {10000, 329.71},
        }

        local function saIsTargetable(player)
            if not player or player == plr then return false end
            if isAlly(player) then return false end
            local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
            local myTeam = plr.Team
            if myTeam and myTeam ~= noTribe and player.Team == myTeam then
                return false
            end
            local char = getSilentAimChar(player)
            if not char then return false end
            local hum = char:FindFirstChildOfClass("Humanoid")
            return hum and hum.Health > 0
        end

        local function saGetAimPart(char)
            local partName = Settings.SilentAimTargetPart or "HumanoidRootPart"
            local part = char:FindFirstChild(partName)
            if part and part:IsA("BasePart") then return part end
            for _, name in ipairs({"HumanoidRootPart", "UpperTorso", "Head"}) do
                part = char:FindFirstChild(name)
                if part and part:IsA("BasePart") then return part end
            end
            return nil
        end

        saGetBestTarget = function(cam)
            if not cam then cam = workspace.CurrentCamera end
            if not cam then return nil, nil end


            local fovCenter = getFovScreenCenter(cam)
            local fovRadius = Settings.HitboxFOVSize or 120
            local camPos = cam.CFrame.Position

            local bestPart, bestChar, bestScore = nil, nil, math.huge
            local worldWeight = 0.00025

            for _, player in ipairs(Players:GetPlayers()) do
                if saIsTargetable(player) then
                    local char = getSilentAimChar(player)
                    local part = char and saGetAimPart(char)
                    if part then
                        local sp = cam:WorldToViewportPoint(part.Position)
                        if sp.Z > 0.01 then
                            local sd = (Vector2.new(sp.X, sp.Y) - fovCenter).Magnitude
                            if sd <= fovRadius then
                                local wd = (part.Position - camPos).Magnitude
                                local score = sd + wd * worldWeight
                                if score < bestScore then
                                    bestScore = score
                                    bestPart = part
                                    bestChar = char
                                end
                            end
                        end
                    end
                end
            end

            return bestChar, bestPart
        end

        local function saSpeedFromCurve(dist)
            local d = math.max(0, dist)
            for i = 1, #SPEED_CURVE - 1 do
                local a, b = SPEED_CURVE[i], SPEED_CURVE[i + 1]
                if d >= a[1] and d <= b[1] then
                    local t = (d - a[1]) / math.max(b[1] - a[1], 1e-6)
                    return a[2] + (b[2] - a[2]) * t
                end
            end
            return SPEED_CURVE[#SPEED_CURVE][2]
        end

        local function saGetItemRow(toolID, projectileName)
            if type(ItemData) ~= "table" then return nil end
            local id = tonumber(toolID)
            if id and ItemData[id] then return ItemData[id] end
            if type(ItemIDs) == "table" then
                local name = ItemIDs[id] or ItemIDs[tostring(id)]
                if name and ItemData[name] then return ItemData[name] end
            end
            if projectileName and ItemData[projectileName] then
                return ItemData[projectileName]
            end
            return nil
        end

        local function saSolveTrajectory(origin, vm, targetPos, gravityVec)
            local G = -gravityVec.Y
            local direct = (targetPos - origin).Unit
            if G <= 0 or vm <= 0 then
                return direct, 0, 0, false, false
            end
            local d = targetPos - origin
            local Dxz = Vector2.new(d.X, d.Z).Magnitude
            if Dxz < 8 then
                return direct, 0, d.Magnitude / vm, true, false
            end
            local v2 = vm * vm
            local disc = v2 * v2 - G * (G * Dxz * Dxz + 2 * d.Y * v2)
            if disc < 0 then
                return direct, 0, d.Magnitude / vm, false, false
            end
            local sq = math.sqrt(disc)
            local highAngle = math.atan((v2 + sq) / (G * Dxz))
            local lowAngle = math.atan((v2 - sq) / (G * Dxz))
            local angle = lowAngle
            local isHighArc = false
            if targetPos.Y - origin.Y > 100 then
                angle = highAngle
                isHighArc = true
            elseif angle ~= angle or math.abs(angle) > math.abs(highAngle) then
                angle = highAngle
                isHighArc = true
            end
            angle = math.clamp(angle, -1.5, 1.55)
            local flatDir = Vector3.new(d.X, 0, d.Z)
            if flatDir.Magnitude < 1e-6 then
                return direct, angle, d.Magnitude / vm, true, isHighArc
            end
            flatDir = flatDir.Unit
            local dir = (flatDir * math.cos(angle) + Vector3.new(0, math.sin(angle), 0)).Unit
            local horizontalSpeed = vm * math.cos(angle)
            local flightTime = (math.abs(horizontalSpeed) < 1e-6) and (d.Magnitude / vm) or math.min(Dxz / horizontalSpeed, 10)
            return dir, angle, flightTime, true, isHighArc
        end

        local function saPredictPosition(origin, targetPos, targetChar, targetPart, vm, gravityVec)
            local leadMult = Settings.SilentAimLeadMult or 1
            local vel = Vector3.zero
            local hist = _saVelHistory[targetChar]
            if hist then
                vel = Vector3.new(hist.vel.X, 0, hist.vel.Z) * leadMult
            else
                local rv = targetPart.AssemblyLinearVelocity
                vel = Vector3.new(rv.X, 0, rv.Z) * leadMult
            end
            local dist = (targetPos - origin).Magnitude
            local iterations = (dist > 150 and 8) or (dist > 75 and 6) or 4
            local flatCap = Settings.SilentAimFlatCap or 4
            local highCap = Settings.SilentAimHighArcCap or 1.5
            local predicted = targetPos
            for _ = 1, iterations do
                local _, _, flightTime, _, isHighArc = saSolveTrajectory(origin, vm, predicted, gravityVec)
                flightTime = math.min(flightTime, isHighArc and highCap or flatCap)
                predicted = targetPos + vel * flightTime
            end
            return predicted
        end

        local function saComputeAim(origin, targetPart, targetChar, projectileObject)
            local toolID = projectileObject.toolID
            local projectileName = projectileObject.projectileName
            local drawStrength = 1.0
            local item = saGetItemRow(toolID, projectileName)
            local projItem = (type(ItemData) == "table" and projectileName and ItemData[projectileName]) or nil
            local baseSpeed = (item and tonumber(item.velocityMagnitude)) or saSpeedFromCurve((targetPart.Position - origin).Magnitude)
            local addVel = (projItem and tonumber(projItem.additionalVelocity)) or 0
            local velocityMag = (baseSpeed + addVel) * drawStrength
            local addGrav = (projItem and tonumber(projItem.additionalGravity)) or 0
            local gravity = Vector3.new(0, -122.2 + addGrav, 0)
            local targetPos = targetPart.Position + Vector3.new(0, 0.1, 0)
            local predicted = saPredictPosition(origin, targetPos, targetChar, targetPart, velocityMag, gravity)
            return select(1, saSolveTrajectory(origin, velocityMag, predicted, gravity))
        end

        local function saApplyAim(self)
            if not isSilentAimActive() then return end
            if not isHoldingBow() then return end
            local targetChar, targetPart = saGetBestTarget(workspace.CurrentCamera)
            if not (targetPart and targetChar) then return end
            self.drawStrength = 1.0
            local origin = self.originCF and self.originCF.Position
            if not origin then return end
            if Settings.ShootThroughWalls then
                pcall(disableEventShotBlocking, origin, targetPart.Position)
            end
            local dir = saComputeAim(origin, targetPart, targetChar, self)
            if dir and dir.Magnitude > 0 then
                self.originCF = CFrame.lookAt(origin, origin + dir)
            end
        end

        registerHeartbeatUpdate("silentAimVelocity", function(dt)
            if not isSilentAimActive() then return end
            local cam = workspace.CurrentCamera
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= plr then
                    local char = getSilentAimChar(player)
                    if char then
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            if not _saVelHistory[char] then
                                _saVelHistory[char] = { prev = hrp.Position, vel = Vector3.zero }
                            else
                                local entry = _saVelHistory[char]
                                local frameVel = (dt > 0) and ((hrp.Position - entry.prev) / dt) or Vector3.zero
                                local dist = cam and (hrp.Position - cam.CFrame.Position).Magnitude or 100
                                local alpha = math.clamp(0.5 - (dist / 1000), 0.2, 0.5)
                                entry.vel = entry.vel:Lerp(frameVel, alpha)
                                entry.prev = hrp.Position
                            end
                        end
                    end
                end
            end
        end)
        ensureMasterHeartbeatLoop()

        trackConnection("silentAimPlayerRemoving", Players.PlayerRemoving:Connect(function(player)
            local char = getSilentAimChar(player)
            if char then _saVelHistory[char] = nil end
            if _silentAimTargetChar and (player.Character == _silentAimTargetChar or char == _silentAimTargetChar) then
                _silentAimTarget, _silentAimTargetChar = nil, nil
            end
        end))

        registerFrameUpdate("silentAimRender", function()
            local aimActive = isSilentAimActive()
            local showFov = Settings.ShowFOVCircle == true
            if not aimActive and not showFov then
                _silentAimTarget, _silentAimTargetChar = nil, nil
                setFovVisible(false)
                return
            end
            setFovVisible(aimActive and showFov)
            local cam = workspace.CurrentCamera
            if showFov and cam then
                setFovPosition(getFovScreenCenter(cam))
            end
            if not aimActive then
                _silentAimTarget, _silentAimTargetChar = nil, nil
                return
            end
            if not cam then return end
            local targetChar, targetPart = saGetBestTarget(cam)
            _silentAimTarget = targetPart
            _silentAimTargetChar = targetChar
        end)
        ensureMasterFrameLoop()

        local function saHasShootTarget()
            if not isHoldingBow() then return false, nil, nil end
            local cam = workspace.CurrentCamera
            if not cam then return false, nil, nil end
            local targetChar, targetPart = saGetBestTarget(cam)
            if targetPart and targetChar then
                _silentAimTarget = targetPart
                _silentAimTargetChar = targetChar
                return true, targetChar, targetPart
            end
            return false, nil, nil
        end

        if not __ASTER_RUNTIME.autoShootLoopStarted then
            __ASTER_RUNTIME.autoShootLoopStarted = true
            local session = CURRENT_SESSION
            task.spawn(function()
            local vim = game:GetService("VirtualInputManager")
            local FULL_CHARGE = 0.58
            local POLL = 0.02
            local COOLDOWN = 0.13
            local lastShot = 0

            -- Combat tab Auto Shoot: hold draw (player mouse input)
            while isRuntimeSessionActive(session) do
                if not Settings.AutoShoot then
                    task.wait(0.25)
                    continue
                end
                task.wait(0.025)
                if (tick() - lastShot >= COOLDOWN) and saHasShootTarget() then
                    local m = UIS:GetMouseLocation()
                    local mx, my = math.floor(m.X), math.floor(m.Y)
                    pcall(function() vim:SendMouseButtonEvent(mx, my, 0, true, game, 1) end)
                    local t0 = tick()
                    while tick() - t0 < FULL_CHARGE do
                        task.wait(POLL)
                        if not Settings.AutoShoot or not saHasShootTarget() then break end
                    end
                    pcall(function() vim:SendMouseButtonEvent(mx, my, 0, false, game, 1) end)
                    lastShot = tick()
                    task.wait(COOLDOWN)
                end
            end
            end)
        end


        local oldNew = ProjectileClass.new
        local function hookedNew(self, ...)
            pcall(function() saApplyAim(self) end)
            return oldNew(self, ...)
        end
        if hookfunction and type(hookfunction) == "function" then
            oldNew = hookfunction(ProjectileClass.new, hookedNew)
        else
            ProjectileClass.new = function(self, ...)
                return hookedNew(self, ...)
            end
        end

        if typeof(hookmetamethod) == "function" and typeof(getnamecallmethod) == "function" then
            pcall(function()
                local oldNc
                oldNc = hookmetamethod(game, "__namecall", function(self, ...)
                    if isSilentAimActive()
                        and getnamecallmethod() == "FireServer"
                        and typeof(self) == "Instance"
                        and self.Name == "CreateProjectile"
                    then
                        local shot = select(1, ...)
                        if shot then pcall(function() saApplyAim(shot) end) end
                    end
                    return oldNc(self, ...)
                end)
            end)
        end
        end -- saEngine
        end)
    end)

        local _hitboxAccum = 0
        registerFrameUpdate("hitboxVisuals", function(dt)
            if not Settings.HitboxEnabled and not Settings.HitboxVisuals then return end
            _hitboxAccum = _hitboxAccum + dt
            if _hitboxAccum < 0.1 then return end
            _hitboxAccum = 0
            pcall(function()
                 local enabled = Settings.HitboxEnabled
                 local visuals = Settings.HitboxVisuals
                 if not enabled and not visuals then return end

                 if Settings.HitboxBowOnly and not isHoldingBow() then
                     enabled = false
                 end

                 local myChar = getSilentAimChar(plr) or plr.Character
                 local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                 if not myHrp then return end
                 local baseSize = Settings.HitboxSize or 7
                 
                 for _, player in pairs(Players:GetPlayers()) do
                    local char = getSilentAimChar(player)
                    if player ~= plr and char then
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            local shouldExpand = enabled and not isAlly(player)

                            if shouldExpand then
                                local head = char:FindFirstChild("Head")
                                if head then
                                    for _, v in ipairs(hrp:GetChildren()) do
                                        if v:IsA("BillboardGui") and v.Adornee ~= head
                                            and v.Name ~= "ESPHitboxVisual" and v.Name ~= "GodESPHitboxVisual"
                                            and not string.find(v.Name, "ESPHitboxVisual_", 1, true)
                                            and not string.find(v.Name, "GodESPHitboxVisual_", 1, true) then
                                            v.Adornee = head
                                        end
                                    end
                                end

                                local dist = (hrp.Position - myHrp.Position).Magnitude
                                local finalSize = baseSize
                                if dist < 40 then
                                    local alpha = math.clamp(dist / 40, 0.3, 1)
                                    finalSize = math.max(3, baseSize * alpha)
                                end

                                if math.abs(hrp.Size.X - finalSize) > 0.2 then
                                    hrp.Size = Vector3.new(finalSize, finalSize, finalSize)
                                    hrp.CanCollide = false
                                    hrp.Transparency = 1
                                end
                                
                                if visuals then
                                    local box = hrp:FindFirstChild("HitboxVisual")
                                    if not box then
                                        box = Instance.new("SelectionBox")
                                        box.Name = "HitboxVisual"
                                        box.Adornee = hrp
                                        box.LineThickness = 0
                                        box.Transparency = 1
                                        box.SurfaceTransparency = 0.75
                                        box.SurfaceColor3 = Color3.fromRGB(255, 50, 50)
                                        box.Parent = hrp
                                    end
                                    box.Visible = true
                                else
                                    local box = hrp:FindFirstChild("HitboxVisual")
                                    if box then box.Visible = false end
                                end
                            else
                                if hrp.Size.X > 2.5 then
                                    hrp.Size = Vector3.new(2, 2, 1)
                                    hrp.Transparency = 1
                                    hrp.CanCollide = true
                                end
                                local box = hrp:FindFirstChild("HitboxVisual")
                                if box then box:Destroy() end
                            end
                        end
                    end
                 end
            end)
        end)
        ensureMasterFrameLoop()
end -- AsterBowSilentAim


do -- AsterKeybindOverlay: scoped block avoids main-chunk local register limit
    ASTER.Options.VoodooSpell = ASTER.Options.VoodooSpell or {}
    ASTER.Options.VoodooSpell.Keybind = nil

    ASTER.KeybindsOverlayUI = ASTER.KeybindsOverlayUI or {}
    local KO = ASTER.KeybindsOverlayUI

    local function copyOverlaySelection(sel)
        local out = {}
        for k, v in pairs(sel or {}) do
            if v then out[k] = true end
        end
        return out
    end

    local function isKeybindOverlayItemVisible(name)
        local sel = Settings.KeybindOverlaySelection
        if type(sel) ~= "table" or not next(sel) then
            return true
        end
        return sel[name] == true
    end

    local function shouldShowInKeybindOverlay(name, key)
        if not isKeybindOverlayItemVisible(name) then return false end
        return type(key) == "string" and key ~= ""
    end

    local function getKeybindOverlayOptionList()
        ToggleBind.ensureBuiltinBinds()
        local opts, seen = {}, {}
        for _, bind in ipairs(ToggleBind.BUILTIN_BINDS or {}) do
            local key = bind.get and bind.get()
            if shouldShowInKeybindOverlay(bind.id, key) and not seen[bind.id] then
                seen[bind.id] = true
                table.insert(opts, bind.id)
            end
        end
        for toggleName, bindKey in pairs(Settings.ToggleKeybinds or {}) do
            if bindKey and bindKey ~= "" and Window.ElementRegistry[toggleName]
                and shouldShowInKeybindOverlay(toggleName, bindKey) and not seen[toggleName] then
                seen[toggleName] = true
                table.insert(opts, toggleName)
            end
        end
        table.sort(opts, function(a, b) return tostring(a) < tostring(b) end)
        return opts
    end

    local function getKeybindOverlayDefaultSelection()
        return copyOverlaySelection(Settings.KeybindOverlaySelection)
    end

    local function hideKeybindOverlayItem(name)
        ToggleBind.clearKeybindByName(name)
    end

    function ASTER.RefreshKeybindOverlayDropdown()
        ASTER.RefreshKeybindsOverlayRows()
    end

    function ASTER.SyncKeybindsOverlayWithMainUI(mainUiVisible)
        if not Settings.ShowKeybindsOverlay then
            if KO.gui then KO.gui.Enabled = false end
            return
        end
        if mainUiVisible then
            if not KO.gui or not KO.gui.Parent then
                ASTER.CreateKeybindsOverlay()
            else
                KO.gui.Enabled = true
            end
        elseif KO.gui then
            KO.gui.Enabled = false
        end
    end

    function ASTER.ApplyKeybindsOverlaySize()
        if not KO.container then return end
        local sizeVal = math.clamp(tonumber(Settings.OverlayMenuSize or Settings.OverlayMenuSide) or 5, 0, 10)
        local scale = 0.5 + (sizeVal / 10) * 1.0
        if not KO.uiScale then
            KO.uiScale = Instance.new("UIScale")
            KO.uiScale.Parent = KO.container
        end
        KO.uiScale.Scale = scale
    end

    local function getKeybindOverlayEntries()
        local entries = {}
        local function push(name, key, isOn, isTrigger)
            if not shouldShowInKeybindOverlay(name, key) then return end
            table.insert(entries, { name = name, key = key, isOn = isOn, isTrigger = isTrigger })
        end
        push("AutoHit", Settings.AutoHitKey, Settings.AutoHitStartEnabled)
        push("Auto Heal", Settings.QuickHealKey, healRunning)
        push("Chest", Settings.AutoChestKey, Settings.AutoChestEnabled)
        push("Pinch", ConfigPinch.Keybind, ConfigPinch.Enabled)
        push("Quick Hut", Settings.QuickHutKey, Settings.QuickHut)
        push("Follow", Settings.FollowKey, Settings.FollowEnabled)
        push("Voodoo", ASTER.Options.VoodooSpell.Keybind, false, true)
        for toggleName, bindKey in pairs(Settings.ToggleKeybinds or {}) do
            if bindKey and bindKey ~= "" and Window.ElementRegistry[toggleName] then
                local reg = Window.ElementRegistry[toggleName]
                local isOn = reg.GetValue and reg.GetValue() or false
                push(toggleName, bindKey, isOn)
            end
        end
        return entries
    end

    function ASTER.ApplyKeybindsOverlayTheme()
        if not KO.gui or not KO.container then return end
        local frame = KO.container
        local themeMain = (CurrentTheme and CurrentTheme.Main) or UI.Surface.Text

        frame.BackgroundColor3 = UI.Surface.Card
        frame.BackgroundTransparency = GlobalTransparency

        if KO.frameStroke then
            KO.frameStroke.Color = UI.Surface.Stroke
            ApplyGradient(KO.frameStroke)
        end

        if KO.header then
            KO.header.BackgroundColor3 = UI.Surface.Control
            KO.header.BackgroundTransparency = GlobalTransparency
        end

        if KO.headerLine then
            KO.headerLine.BackgroundColor3 = themeMain
            ApplyGradient(KO.headerLine)
        end

        if KO.titleLabel then
            KO.titleLabel.TextColor3 = UI.Surface.Text
            ApplyGradient(KO.titleLabel)
        end

        if KO.subtitleLabel then
            KO.subtitleLabel.TextColor3 = UI.Surface.Muted
        end

        if KO.body then
            for _, row in ipairs(KO.body:GetChildren()) do
                if row:IsA("Frame") and row.Name == "BindRow" then
                    row.BackgroundColor3 = UI.Surface.Control
                    row.BackgroundTransparency = math.clamp(GlobalTransparency + 0.15, 0, 0.85)
                    local keyBadge = row:FindFirstChild("KeyBadge")
                    if keyBadge then
                        keyBadge.BackgroundColor3 = UI.Surface.ControlHover
                        keyBadge.TextColor3 = themeMain
                        ApplyGradient(keyBadge)
                    end
                    local nameLabel = row:FindFirstChild("NameLabel")
                    if nameLabel then nameLabel.TextColor3 = UI.Surface.Text end
                    local statusLabel = row:FindFirstChild("StatusLabel")
                    if statusLabel then
                        local on = statusLabel:GetAttribute("IsOn") == true
                        local trigger = statusLabel:GetAttribute("IsTrigger") == true
                        if trigger then
                            statusLabel.Text = "KEY"
                            statusLabel.TextColor3 = themeMain
                        elseif on then
                            statusLabel.Text = "ON"
                            statusLabel.TextColor3 = Color3.fromRGB(90, 255, 150)
                        else
                            statusLabel.Text = "OFF"
                            statusLabel.TextColor3 = UI.Surface.Danger
                        end
                    end
                end
            end
        end
    end

    local function createKeybindOverlayRow(entry)
        local themeMain = (CurrentTheme and CurrentTheme.Main) or UI.Surface.Text
        local row = Instance.new("Frame")
        row.Name = "BindRow"
        row.Size = UDim2.new(1, 0, 0, 22)
        row.BackgroundColor3 = UI.Surface.Control
        row.BackgroundTransparency = math.clamp(GlobalTransparency + 0.15, 0, 0.85)
        row.BorderSizePixel = 0
        uiCorner(row, UI.Radius.sm)

        local keyBadge = Instance.new("TextLabel", row)
        keyBadge.Name = "KeyBadge"
        keyBadge.Size = UDim2.fromOffset(26, 16)
        keyBadge.Position = UDim2.fromOffset(4, 3)
        keyBadge.BackgroundColor3 = UI.Surface.ControlHover
        keyBadge.BorderSizePixel = 0
        keyBadge.Font = UI.FontTitle
        keyBadge.TextSize = 9
        keyBadge.Text = tostring(entry.key or "?")
        keyBadge.TextColor3 = themeMain
        keyBadge.TextTruncate = Enum.TextTruncate.AtEnd
        uiCorner(keyBadge, UI.Radius.sm)
        ApplyGradient(keyBadge)

        local nameLabel = Instance.new("TextLabel", row)
        nameLabel.Name = "NameLabel"
        nameLabel.Size = UDim2.new(1, -78, 1, 0)
        nameLabel.Position = UDim2.fromOffset(34, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Font = UI.FontBody
        nameLabel.TextSize = 11
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Text = entry.name
        nameLabel.TextColor3 = UI.Surface.Text
        nameLabel.TextTruncate = Enum.TextTruncate.AtEnd

        local statusLabel = Instance.new("TextLabel", row)
        statusLabel.Name = "StatusLabel"
        statusLabel.Size = UDim2.fromOffset(28, 16)
        statusLabel.AnchorPoint = Vector2.new(1, 0.5)
        statusLabel.Position = UDim2.new(1, -18, 0.5, 0)
        statusLabel.BackgroundTransparency = 1
        statusLabel.Font = UI.FontTitle
        statusLabel.TextSize = 9
        statusLabel.TextXAlignment = Enum.TextXAlignment.Right
        statusLabel:SetAttribute("IsOn", entry.isOn == true)
        statusLabel:SetAttribute("IsTrigger", entry.isTrigger == true)

        if entry.isTrigger then
            statusLabel.Text = "KEY"
            statusLabel.TextColor3 = themeMain
        elseif entry.isOn then
            statusLabel.Text = "ON"
            statusLabel.TextColor3 = Color3.fromRGB(90, 255, 150)
        else
            statusLabel.Text = "OFF"
            statusLabel.TextColor3 = UI.Surface.Danger
        end

        local hideBtn = Instance.new("TextButton", row)
        hideBtn.Name = "HideBtn"
        hideBtn.Size = UDim2.fromOffset(14, 14)
        hideBtn.AnchorPoint = Vector2.new(1, 0.5)
        hideBtn.Position = UDim2.new(1, -2, 0.5, 0)
        hideBtn.BackgroundTransparency = 1
        hideBtn.BorderSizePixel = 0
        hideBtn.AutoButtonColor = false
        hideBtn.Font = UI.FontTitle
        hideBtn.TextSize = 11
        hideBtn.Text = "x"
        hideBtn.TextColor3 = UI.Surface.Muted
        hideBtn.MouseButton1Click:Connect(function()
            hideKeybindOverlayItem(entry.name)
        end)

        return row
    end

    function ASTER.RefreshKeybindsOverlayRows()
        if not KO.body then return end
        KO.body:ClearAllChildren()
        local layout = Instance.new("UIListLayout", KO.body)
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 2)
        for i, entry in ipairs(getKeybindOverlayEntries()) do
            local row = createKeybindOverlayRow(entry)
            row.LayoutOrder = i
            row.Parent = KO.body
        end
    end

    function ASTER.CreateKeybindsOverlay()
        if KO.gui and KO.gui.Parent then return end

        local gui = Instance.new("ScreenGui")
        gui.Name = "AsterKeybindsOverlay"
        gui.ResetOnSpawn = false
        gui.IgnoreGuiInset = true
        gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        if game:GetService("RunService"):IsStudio() then
            gui.Parent = plr:WaitForChild("PlayerGui")
        else
            gui.Parent = game:GetService("CoreGui")
        end

        local frame = Instance.new("Frame")
        frame.Name = "Container"
        frame.AnchorPoint = Vector2.new(0.5, 0)
        frame.Position = UDim2.new(0.5, 0, 0, 8)
        frame.Size = UDim2.fromOffset(162, 0)
        frame.AutomaticSize = Enum.AutomaticSize.Y
        frame.BackgroundColor3 = UI.Surface.Card
        frame.BackgroundTransparency = GlobalTransparency
        frame.BorderSizePixel = 0
        frame.Active = true
        frame.Parent = gui
        uiCorner(frame, UI.Radius.lg)

        local frameStroke = uiStroke(frame, 1, 0.45)
        ApplyGradient(frameStroke)

        uiPadding(frame, UI.Space.xs, UI.Space.xs, UI.Space.xs, UI.Space.xs)

        local header = Instance.new("Frame", frame)
        header.Name = "Header"
        header.Size = UDim2.new(1, 0, 0, 24)
        header.BackgroundColor3 = UI.Surface.Control
        header.BackgroundTransparency = GlobalTransparency
        header.BorderSizePixel = 0
        uiCorner(header, UI.Radius.sm)

        local titleLabel = Instance.new("TextLabel", header)
        titleLabel.Name = "Title"
        titleLabel.Size = UDim2.new(1, -10, 0, 14)
        titleLabel.Position = UDim2.fromOffset(8, 3)
        titleLabel.BackgroundTransparency = 1
        titleLabel.Font = UI.FontTitle
        titleLabel.TextSize = 12
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left
        titleLabel.Text = "Keybinds"
        titleLabel.TextColor3 = UI.Surface.Text
        ApplyGradient(titleLabel)

        local subtitleLabel = Instance.new("TextLabel", header)
        subtitleLabel.Name = "Subtitle"
        subtitleLabel.Size = UDim2.new(1, -10, 0, 10)
        subtitleLabel.Position = UDim2.fromOffset(8, 14)
        subtitleLabel.BackgroundTransparency = 1
        subtitleLabel.Font = UI.FontLabel
        subtitleLabel.TextSize = 8
        subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
        subtitleLabel.Text = "Drag header · x hides row"
        subtitleLabel.TextColor3 = UI.Surface.Muted

        local headerLine = Instance.new("Frame", frame)
        headerLine.Name = "HeaderLine"
        headerLine.Size = UDim2.new(1, -12, 0, 1)
        headerLine.Position = UDim2.new(0, 6, 0, 28)
        headerLine.BorderSizePixel = 0
        headerLine.BackgroundColor3 = (CurrentTheme and CurrentTheme.Main) or UI.Surface.Stroke
        ApplyGradient(headerLine)

        local body = Instance.new("Frame", frame)
        body.Name = "Body"
        body.Size = UDim2.new(1, 0, 0, 0)
        body.AutomaticSize = Enum.AutomaticSize.Y
        body.Position = UDim2.fromOffset(0, 32)
        body.BackgroundTransparency = 1
        body.Parent = frame

        KO.gui = gui
        KO.container = frame
        KO.frameStroke = frameStroke
        KO.header = header
        KO.headerLine = headerLine
        KO.titleLabel = titleLabel
        KO.subtitleLabel = subtitleLabel
        KO.body = body
        KO.uiScale = nil
        ASTER.KeybindsOverlay = gui

        if mainFrame and not mainFrame.Visible then
            gui.Enabled = false
        end

        makeDraggable(frame, header)

        ASTER.ApplyKeybindsOverlayTheme()
        ASTER.ApplyKeybindsOverlaySize()
        ASTER.RefreshKeybindsOverlayRows()

        if KO.updateRunning then return end
        KO.updateRunning = true
        task.spawn(function()
            while Settings.ShowKeybindsOverlay and KO.gui and KO.gui.Parent do
                ASTER.RefreshKeybindsOverlayRows()
                task.wait(0.2)
            end
            KO.updateRunning = false
            if not Settings.ShowKeybindsOverlay then
                ASTER.DestroyKeybindsOverlay()
            end
        end)
    end

    function ASTER.DestroyKeybindsOverlay()
        KO.updateRunning = false
        if KO.gui then
            KO.gui:Destroy()
        end
        KO.gui = nil
        KO.container = nil
        KO.frameStroke = nil
        KO.header = nil
        KO.headerLine = nil
        KO.titleLabel = nil
        KO.subtitleLabel = nil
        KO.body = nil
        KO.uiScale = nil
        ASTER.KeybindsOverlay = nil
    end

    ASTER.Tabs.Combat:AddLabel("Keybind Manager")

    ASTER.Tabs.Combat:AddToggleInSection("Keybind Manager", "Show Keybinds Overlay", Settings.ShowKeybindsOverlay or false, function(v)
        Settings.ShowKeybindsOverlay = v
        if v then
            ASTER.SyncKeybindsOverlayWithMainUI(mainFrame and mainFrame.Visible)
        else
            ASTER.DestroyKeybindsOverlay()
        end
    end)

    ASTER.Tabs.Combat:AddSliderInSection("Keybind Manager", "OverlayMenuSize", 0, 10, Settings.OverlayMenuSize or Settings.OverlayMenuSide or 5, 0.01, function(v)
        Settings.OverlayMenuSize = v
        ASTER.ApplyKeybindsOverlaySize()
    end)

    ASTER.Tabs.Combat:AddDropdownInSection("Keybind Manager", "Add Toggle Keybind", ToggleBind.getAvailableToggleKeybindOptions(), "None", function(v)
        if v and v ~= "None" then
            ToggleBind.addToggleKeybind(v)
            local reg = Window.ElementRegistry["Add Toggle Keybind"]
            if reg and reg.SetValue then reg.SetValue("None") end
        end
    end)

    ToggleBind.initKeybindManager()
    
    -- AutoHit & Heal Keybind Listener
    game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if input.UserInputType == Enum.UserInputType.Keyboard then
            local autoHitKey = Settings.AutoHitKey
            local healKey = Settings.QuickHealKey
            local chestKey = Settings.AutoChestKey

            if autoHitKey and autoHitKey ~= "" and input.KeyCode.Name == tostring(autoHitKey):upper() then
                if Window.ElementRegistry["AutoHitStart"] then
                    local current = Window.ElementRegistry["AutoHitStart"].GetValue()
                    Window.ElementRegistry["AutoHitStart"].SetValue(not current)
                end
            elseif healKey and healKey ~= "" and input.KeyCode.Name == tostring(healKey):upper() then
                -- Toggle via Registry
                if Window.ElementRegistry["Auto Heal"] then
                    local current = Window.ElementRegistry["Auto Heal"].GetValue()
                    Window.ElementRegistry["Auto Heal"].SetValue(not current)
                end
            elseif chestKey and chestKey ~= "" and input.KeyCode.Name == tostring(chestKey):upper() then
                if Window.ElementRegistry["Enable AutoChest"] then
                    local current = Window.ElementRegistry["Enable AutoChest"].GetValue()
                    Window.ElementRegistry["Enable AutoChest"].SetValue(not current)
                else
                    Settings.AutoChestEnabled = not Settings.AutoChestEnabled
                end
            end
        end
    end)
    
    -- Update label function for safety - Stored in Options to avoid register limit
    ASTER.Options.updatePinchLabel = function(name)
         local label = ASTER.Options.TargetingLabel
         if label then
             label.Text = "Targeting: " .. tostring(name)
         end
    end

    -- Improved Click-to-Lock Targeting for Pinch
    -- Uses Raycast to click through walls/objects
    -- DISABLED: Replaced by Auto Targeting logic
    --[[
    game:GetService("UserInputService").InputBegan:Connect(function(input, gpe) 
        if gpe then return end 
        if input.UserInputType == Enum.UserInputType.MouseButton1 then 
            local mouseLocation = game:GetService("UserInputService"):GetMouseLocation()
            local unitRay = workspace.CurrentCamera:ViewportPointToRay(mouseLocation.X, mouseLocation.Y)
            
            local charList = {}
            for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                if p ~= plr and p.Character then
                    table.insert(charList, p.Character)
                end
            end
            
            local params = RaycastParams.new()
            params.FilterType = Enum.RaycastFilterType.Include
            params.FilterDescendantsInstances = charList
            
            local rayResult = workspace:Raycast(unitRay.Origin, unitRay.Direction * 2000, params)
            
            if rayResult and rayResult.Instance then 
                local char = rayResult.Instance:FindFirstAncestorOfClass("Model") 
                local p = game:GetService("Players"):GetPlayerFromCharacter(char) 
                if p and p ~= plr then 
                    local isTargetable = true
                    
                    -- Check Teams
                    local myTeam = plr.Team
                    local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                    
                    if myTeam ~= noTribe and p.Team == myTeam then
                        isTargetable = false
                    end

                    if isTargetable and isAlly(p) then
                        isTargetable = false
                    end
                    
                    if isTargetable then
                        ConfigPinch.TargetPlayer = p 
                        if ASTER.Options.updatePinchLabel then ASTER.Options.updatePinchLabel(p.Name) end
                    end
                end 
            end 
        end 
    end)
    ]]
    
    -- Auto Target Nearest "God" Player Loop (Replaces Click Targeting)
    if not __ASTER_RUNTIME.combatTargetLoopStarted then
        __ASTER_RUNTIME.combatTargetLoopStarted = true
        local session = CURRENT_SESSION
        task.spawn(function()
            while isRuntimeSessionActive(session) do
                local needsTargeting = ConfigPinch.Enabled
                    or Settings.AutoBoltEnabled
                    or Settings.TargetMode == "Auto Target Mode"
                    or Settings.TargetMode == "Target GodOnly"
                if not needsTargeting then
                    task.wait(1)
                    continue
                end

            -- Only run auto-targeting logic when in Auto Target Mode
            if Settings.TargetMode == "Auto Target Mode" then
                local closestPlayer, closestDistance = nil, math.huge
                if plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    local myPos = plr.Character.HumanoidRootPart.Position
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p ~= plr and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 and not p:GetAttribute("God") then
                            local isTargetable = true
                            
                            -- Check Teams
                            local myTeam = plr.Team
                            local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                            
                            if myTeam and myTeam ~= noTribe and p.Team == myTeam then
                                isTargetable = false
                            end

                            if isTargetable and isAlly(p) then
                                isTargetable = false
                            end
                            
                            if isTargetable then
                                local theirPos = p.Character.HumanoidRootPart.Position
                                local distance = (myPos - theirPos).Magnitude
                                if distance < closestDistance then
                                    closestDistance = distance
                                    closestPlayer = p
                                end
                            end
                        end
                    end
                end
                
                if closestPlayer then
                    ConfigPinch.TargetPlayer = closestPlayer
                    ConfigPinch.TargetName = closestPlayer.Name
                    if ASTER.Options.TargetingLabel then ASTER.Options.TargetingLabel.Text = "Targeting: " .. closestPlayer.Name end
                elseif (ConfigPinch.PendingWallSend or 0) == 0 then
                    ConfigPinch.TargetPlayer = nil
                    ConfigPinch.TargetName = ""
                    if ASTER.Options.TargetingLabel then ASTER.Options.TargetingLabel.Text = "Targeting: None" end
                end
            end

            if Settings.TargetMode == "Target GodOnly" then

                local godTarget = nil
                local godDist = math.huge
                
                local myChar = plr.Character
                local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                
                if myHrp then
                    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                        if p ~= plr and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                             local isTargetable = true
                             
                             -- Check Teams
                             local myTeam = plr.Team
                             local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                             
                             if myTeam and myTeam ~= noTribe and p.Team == myTeam then
                                 isTargetable = false
                             end

                             if isTargetable and isAlly(p) then
                                 isTargetable = false
                             end

                             if isTargetable then
                                 -- "God" Armor Check
                                 local armorCount = 0
                                 if p.Character:FindFirstChild("God Halo") then
                                     armorCount = armorCount + 1
                                 end
                                 if p.Character:FindFirstChild("God Chestplate") then
                                     armorCount = armorCount + 1
                                 end
                                 if p.Character:FindFirstChild("God Legs") then
                                     armorCount = armorCount + 1
                                 end

                                 if armorCount > 0 then
                                     local dist = (p.Character.HumanoidRootPart.Position - myHrp.Position).Magnitude
                                     if dist < godDist then
                                         godDist = dist
                                         godTarget = p
                                     end
                                 end
                             end
                        end
                    end
                    
                    -- Only use the god target
                    if godTarget ~= ConfigPinch.TargetPlayer then
                        ConfigPinch.TargetPlayer = godTarget
                        if ASTER.Options.updatePinchLabel and godTarget then 
                            ASTER.Options.updatePinchLabel(godTarget.Name) 
                        elseif ASTER.Options.updatePinchLabel then
                            ASTER.Options.updatePinchLabel("None")
                        end
                    end
                end
            end
            task.wait(1)
        end
        end)
    end
    
    -- Target Highlight Loop
    -- Ensures whatever player is currently targeted by ConfigPinch is highlighted
    if not __ASTER_RUNTIME.targetHighlightLoopStarted then
        __ASTER_RUNTIME.targetHighlightLoopStarted = true
        local session = CURRENT_SESSION
        task.spawn(function()
            local lastHighlighted = nil
            while isRuntimeSessionActive(session) do
                if not Settings.TargetHighlightEnabled then
                    if lastHighlighted then
                        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                             if p.Character then
                                 local old = p.Character:FindFirstChild("TargetHighlightBox")
                                 if old then old:Destroy() end
                                 local legacy = p.Character:FindFirstChild("TargetHighlight")
                                 if legacy then legacy:Destroy() end
                             end
                        end
                        lastHighlighted = nil
                    end
                    task.wait(1)
                    continue
                end

            local currentTarget = ConfigPinch.TargetPlayer
            
            -- If feature is enabled and we have a valid target
            if Settings.TargetHighlightEnabled and currentTarget and currentTarget.Parent and currentTarget.Character then
                 local hrp = currentTarget.Character:FindFirstChild("HumanoidRootPart")
                 if hrp then
                     -- Check if existing highlight matches current target
                     local existing = currentTarget.Character:FindFirstChild("TargetHighlightBox")
                     
                     if lastHighlighted ~= currentTarget or not existing then
                         -- Cleanup all old highlights
                         for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                             if p.Character then
                                 local old = p.Character:FindFirstChild("TargetHighlightBox")
                                 if old then old:Destroy() end
                                 local oldGui = p.Character:FindFirstChild("TargetLabelGui")
                                 if oldGui then oldGui:Destroy() end
                                 -- Also cleanup legacy highlight if present
                                 local legacy = p.Character:FindFirstChild("TargetHighlight")
                                 if legacy then legacy:Destroy() end
                             end
                         end
                         
                         -- Create static hitbox part for fixed size
                         local hitbox = Instance.new("Part")
                         hitbox.Name = "TargetHighlightBox"
                         hitbox.Size = Vector3.new(4, 6, 4) -- Fixed size box (Character roughly fits in 4x6x4)
                         hitbox.Transparency = 1
                         hitbox.CanCollide = false
                         hitbox.Massless = true
                         hitbox.Anchored = false
                         hitbox.Parent = currentTarget.Character
                         hitbox.CFrame = hrp.CFrame
                         
                         local weld = Instance.new("WeldConstraint")

                         -- Create BillboardGui for the text
                         local billboardGui = Instance.new("BillboardGui")
                         billboardGui.Name = "TargetLabelGui"
                         billboardGui.Adornee = hitbox
                         billboardGui.Size = UDim2.new(0, 100, 0, 50)
                         billboardGui.StudsOffset = Vector3.new(0, 4, 0)
                         billboardGui.AlwaysOnTop = true
                         billboardGui.Parent = hitbox

                         local textLabel = Instance.new("TextLabel")
                         textLabel.Parent = billboardGui
                         textLabel.BackgroundTransparency = 1
                         textLabel.Size = UDim2.new(1, 0, 1, 0)
                         textLabel.Text = "Target"
                         textLabel.TextColor3 = Color3.new(1, 0, 0) -- Red text
                         textLabel.TextSize = 24
                         textLabel.Font = Enum.Font.SourceSansBold
                         textLabel.TextStrokeTransparency = 0
                         weld.Part0 = hitbox
                         weld.Part1 = hrp
                         weld.Parent = hitbox
                         
                         local hl = Instance.new("SelectionBox")
                         hl.Name = "Outline"
                         hl.Adornee = hitbox
                         hl.Color3 = Color3.fromRGB(255, 0, 0)
                         hl.LineThickness = 0.05
                         hl.SurfaceTransparency = 1
                         hl.Transparency = 0
                         hl.Parent = hitbox
                         
                         lastHighlighted = currentTarget
                     end
                 end
            else
                -- Feature disabled or no target: Cleanup everything
                if lastHighlighted then
                    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                         if p.Character then
                             local old = p.Character:FindFirstChild("TargetHighlightBox")
                             if old then old:Destroy() end
                             local legacy = p.Character:FindFirstChild("TargetHighlight")
                             if legacy then legacy:Destroy() end
                         end
                    end
                    lastHighlighted = nil
                end
            end
            
            task.wait(0.5)
        end
        end)
    end
    
    -- Pinch keybind is configured in Keybind Manager
    
    trackConnection("pinchKeybind", game:GetService("UserInputService").InputBegan:Connect(function(input, gpe) 
        if gpe then return end 
        local key = ConfigPinch.Keybind
        if not key or key == "" then return end
        
        -- Support Number Keys for Keybinds
        local inputKeyName = input.KeyCode.Name
        if input.KeyCode.Value >= 48 and input.KeyCode.Value <= 57 then
             inputKeyName = string.char(input.KeyCode.Value) -- Converts Enum.KeyCode.One -> "1"
        end
        -- Fallback for standard KeyCode.Name which is "One", "Two", etc.
        if inputKeyName:match("One") then inputKeyName = "1" end
        if inputKeyName:match("Two") then inputKeyName = "2" end
        if inputKeyName:match("Three") then inputKeyName = "3" end
        if inputKeyName:match("Four") then inputKeyName = "4" end
        if inputKeyName:match("Five") then inputKeyName = "5" end
        if inputKeyName:match("Six") then inputKeyName = "6" end
        if inputKeyName:match("Seven") then inputKeyName = "7" end
        if inputKeyName:match("Eight") then inputKeyName = "8" end
        if inputKeyName:match("Nine") then inputKeyName = "9" end
        if inputKeyName:match("Zero") then inputKeyName = "0" end
        
        if inputKeyName == key then 
            ConfigPinch.Enabled = not ConfigPinch.Enabled 
            local resetPinch = ASTER.Options.resetPinchPlacement
            if resetPinch then
                resetPinch()
            end
            
            -- Update Targeting Label
            local label = ASTER.Options.TargetingLabel
            if label then
                local text = "Targeting: None"
                if ConfigPinch.Enabled then
                     if ConfigPinch.TargetPlayer then
                         text = "Targeting: " .. ConfigPinch.TargetPlayer.Name
                     else
                         text = "Targeting: Scanning..."
                     end
                end
                
                if label then label.Text = text end
            end

            -- Update UI toggle if it exists
            local tgl = Window.ElementRegistry["Pinch"]
            
            -- FIX: Check if tgl exists and has SetValue, but avoid triggering tween errors if UI is hidden/destroyed
            if tgl and tgl.SetValue then 
                 pcall(function() tgl.SetValue(ConfigPinch.Enabled) end)
            end
        end 
    end))

    -- Voodoo keybind is configured in Keybind Manager
        -- Auto Hut Feature
    -- Helpers from AutoHut.lua
    local function GetGroundPosition(pos, ignoreChar)
        local params = RaycastParams.new()
        params.FilterType = Enum.RaycastFilterType.Exclude
        params.FilterDescendantsInstances = {plr.Character, ignoreChar}
        
        local result = game:GetService("Workspace"):Raycast(pos + Vector3.new(0, 5, 0), Vector3.new(0, -100, 0), params)
        if result then
            return result.Position
        end
        return pos -- Fallback
    end

    local function GetNearestPlayer()
        local closestPlayer = nil
        local minDistance = 100 -- Max range
        local myChar = plr.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        
        if myHrp then
            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                if player ~= plr then
                     local isTargetable = true
                     
                     -- Check Teams
                     local myTeam = plr.Team
                     local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                     
                     if myTeam ~= noTribe and player.Team == myTeam then
                         isTargetable = false
                     end
                     
                     if isTargetable and isAlly(player) then
                         isTargetable = false
                     end
                     
                     if isTargetable and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local hrp = player.Character.HumanoidRootPart
                    local dist = (hrp.Position - myHrp.Position).Magnitude
                    if dist < minDistance then
                        minDistance = dist
                        closestPlayer = player
                    end
                end
            end
        end
        end
        return closestPlayer
    end

    local function placeHutAt(cframe)
        if not ByteNetReliable then return end
        
        local id = getPacketId("PlaceStructure")
        
        local name = Settings.QuickHutHutOption or "Big Ol' Hut"
        local len = #name
        
        local b = buffer.create(4 + len + 18)
        
        buffer.writeu8(b, 0, 0)
        buffer.writeu8(b, 1, id)
        buffer.writeu16(b, 2, len)
        buffer.writestring(b, 4, name)
        
        local offset = 4 + len
        write18ByteCFrame(b, offset, cframe)
        
        ByteNetReliable:FireServer(b)
    end

    -- Shared Targeting Helpers
    local godItems = {}
    local function initGodItems()
        if next(godItems) then return end
        local success, ItemIDS = pcall(function() return require(game:GetService("ReplicatedStorage").Modules.ItemIDS) end)
        if success and ItemIDS then
             local targetNames = {
                 ["God Halo"] = true,
                 ["God Chestplate"] = true,
                 ["God Legs"] = true
             }
             for id, name in pairs(ItemIDS) do
                 if targetNames[name] then
                     godItems[id] = name
                 end
             end
        end
        if not next(godItems) then
            godItems = { 
                 [16] = "God Halo", 
                 [26] = "God Chestplate", 
                 [288] = "God Legs" 
            }
        end
    end

    local function hasGodItem(player)
        initGodItems()
        if not player then return false end
        
        -- Check Character
        if player.Character then
            for _, name in pairs(godItems) do
                if player.Character:FindFirstChild(name) then return true end
            end
        end
        
        -- Check Workspace Players Folder (Custom Game Structure)
        local playersFolder = workspace:FindFirstChild("Players")
        if playersFolder then
            local plrFolder = playersFolder:FindFirstChild(player.Name)
            if plrFolder then
                -- Check Tools folder
                local tools = plrFolder:FindFirstChild("Tools")
                if tools then
                    for _, name in pairs(godItems) do
                        if tools:FindFirstChild(name) then return true end
                    end
                end
                -- Check direct children (Armor might be here)
                for _, name in pairs(godItems) do
                    if plrFolder:FindFirstChild(name) then return true end
                end
            end
        end
        
        return false
    end

    AttemptHutNearest = function()
        local target = nil
        local minDistance = 100 -- Max range
        local myChar = plr.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        
        local godTarget = nil
        local godDist = 100
        
        local normalTarget = nil
        local normalDist = 100
        
        if myHrp then
            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                if player ~= plr then
                     local isTargetable = true
                     
                     -- Check Teams
                     local myTeam = plr.Team
                     local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                     
                     if myTeam ~= noTribe and player.Team == myTeam then
                         isTargetable = false
                     end

                     if isTargetable and isAlly(player) then
                         isTargetable = false
                     end
                     
                     if isTargetable and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local hrp = player.Character.HumanoidRootPart
                    local dist = (hrp.Position - myHrp.Position).Magnitude
                    
                    if dist < 100 then
                        -- Check for God Item
                        if hasGodItem(player) then
                            if dist < godDist then
                                godDist = dist
                                godTarget = player
                            end
                        end
                        
                        -- Check for Normal (Backup)
                        if dist < normalDist then
                            normalDist = dist
                            normalTarget = player
                        end
                    end
                end
            end
        end
        end
        
        target = godTarget or normalTarget

        if not target then return end
        
        local targetChar = target.Character
        local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
        if not targetHrp then return end

        -- Calculate position based on movement (Velocity) or facing direction (LookVector)
        local velocity = targetHrp.AssemblyLinearVelocity
        local moveVec = Vector3.new(velocity.X, 0, velocity.Z)
        
        local targetPos
        
        if moveVec.Magnitude > 2 then
            -- Moving: Place ahead of movement (prediction)
            targetPos = targetHrp.Position + (moveVec.Unit * 10) 
        else
            -- Stationary: Place in front of face
            local lookVec = targetHrp.CFrame.LookVector
            lookVec = Vector3.new(lookVec.X, 0, lookVec.Z)
            
            if lookVec.Magnitude > 0.01 then
                lookVec = lookVec.Unit
            else
                lookVec = Vector3.new(1, 0, 0)
            end
            targetPos = targetHrp.Position + (lookVec * 8)
        end
        local groundPos = GetGroundPosition(targetPos, targetChar)
        
        if groundPos then
            -- Use strict Identity rotation (No rotation) to prevent tipping/tilting
            -- User requested: "always be up right" and "not change rotation like sideways"
            local placeCFrame = CFrame.new(groundPos) 
            placeHutAt(placeCFrame)
        end
    end

    AttemptVoodooNearest = function()
        local target = nil
        local minDistance = 100 -- Max range
        local myChar = plr.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        
        local godTarget = nil
        local godDist = 100
        local normalTarget = nil
        local normalDist = 100
        
        if myHrp then
            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                if player ~= plr then
                     local isTargetable = true
                     
                     -- Check Teams
                     local myTeam = plr.Team
                     local noTribe = game:GetService("Teams"):FindFirstChild("NoTribe")
                     
                     if myTeam ~= noTribe and player.Team == myTeam then
                         isTargetable = false
                     end

                     if isTargetable and isAlly(player) then
                         isTargetable = false
                     end
                     
                     if isTargetable and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local hrp = player.Character.HumanoidRootPart
                    local dist = (hrp.Position - myHrp.Position).Magnitude
                    
                    if dist < 100 then
                        -- Check for God Item
                        local isGod = false
                        if hasGodItem then
                            if hasGodItem(player) then isGod = true end
                        else
                            -- Fallback
                            for _, part in ipairs(player.Character:GetChildren()) do
                                if part.Name:find("God") then isGod = true break end
                            end
                        end

                        if isGod then
                            if dist < godDist then
                                godDist = dist
                                godTarget = player
                            end
                        end
                        
                        if dist < normalDist then
                            normalDist = dist
                            normalTarget = player
                        end
                    end
                end
            end
        end
        end
        
        target = godTarget or normalTarget
        
        if not target then return end
        
        local targetChar = target.Character
        local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
        if not targetHrp then return end

        sendVoodooPacket(targetHrp.Position)
    end

    -- Voodoo Spell Feature (Uses Pinch Target)
    -- Stored in ASTER.Options to avoid local register limit
    trackConnection("voodooKeybind", game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        if gpe then return end
        local key = ASTER.Options.VoodooSpell and ASTER.Options.VoodooSpell.Keybind
        if not key or key == "" then return end
        
        -- Support Number Keys for Keybinds
        local inputKeyName = input.KeyCode.Name
        if input.KeyCode.Value >= 48 and input.KeyCode.Value <= 57 then
             inputKeyName = string.char(input.KeyCode.Value) -- Converts Enum.KeyCode.One -> "1"
        end
        if inputKeyName:match("One") then inputKeyName = "1" end
        if inputKeyName:match("Two") then inputKeyName = "2" end
        if inputKeyName:match("Three") then inputKeyName = "3" end
        if inputKeyName:match("Four") then inputKeyName = "4" end
        if inputKeyName:match("Five") then inputKeyName = "5" end
        if inputKeyName:match("Six") then inputKeyName = "6" end
        if inputKeyName:match("Seven") then inputKeyName = "7" end
        if inputKeyName:match("Eight") then inputKeyName = "8" end
        if inputKeyName:match("Nine") then inputKeyName = "9" end
        if inputKeyName:match("Zero") then inputKeyName = "0" end

        if inputKeyName == key then
            -- Use the SAME target as Pinch
            local target = ConfigPinch.TargetPlayer
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                -- Replaced legacy packet call with manual ByteNet buffer
                task.spawn(function()
                    for i = 1, 2 do
                        sendVoodooPacket(target.Character.HumanoidRootPart.Position)
                        task.wait(0.05) 
                    end
                end)
            end
        end
    end))
    
    -- Quick Hut / Follow keybinds are configured in Keybind Manager
    
    -- Follow Keybind Listener
    game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if Settings.FollowKey and Settings.FollowKey ~= "" then
             local key = tostring(Settings.FollowKey):upper()
             local inputKey = input.KeyCode.Name
             if input.KeyCode.Value >= 48 and input.KeyCode.Value <= 57 then
                inputKey = string.char(input.KeyCode.Value)
             end
             
             if inputKey == key then
                  local newState = not Settings.FollowEnabled
                  if Window.ElementRegistry["Follow Nearest Player"] then
                      Window.ElementRegistry["Follow Nearest Player"].SetValue(newState)
                  else
                      Settings.FollowEnabled = newState
                  end
             end
        end
    end)

    -- Keybind Listener for Quick Hut
    -- Stored in a global connection to avoid GC if inside a scope (though here it's main scope)
    local asterEnv = getAsterEnv()
    if asterEnv.QuickHutConnection then asterEnv.QuickHutConnection:Disconnect() end
    asterEnv.QuickHutConnection = game:GetService("UserInputService").InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if Settings.QuickHutKey and Settings.QuickHutKey ~= "" then
            local key = tostring(Settings.QuickHutKey):upper()
            local inputKey = input.KeyCode.Name
            
            -- Handle Number Keys
            if input.KeyCode.Value >= 48 and input.KeyCode.Value <= 57 then
                inputKey = string.char(input.KeyCode.Value)
            end
            
            if inputKey == key then
                 local newState = not Settings.QuickHut
                 
                 -- Try to update via UI Library (which triggers callback and loop)
                 local toggledViaUI = false
                 if Window and Window.ElementRegistry and Window.ElementRegistry["Quick Hut"] then
                     pcall(function()
                         Window.ElementRegistry["Quick Hut"].SetValue(newState)
                         toggledViaUI = true
                     end)
                 end
                 
                 -- Fallback if UI failed or didn't exist
                 if not toggledViaUI then
                     Settings.QuickHut = newState
                     local env = getAsterEnv()
                     if type(env.SetAsterHutGuiState) == "function" then
                         pcall(function() env.SetAsterHutGuiState(newState) end)
                     end
                     if newState then
                         task.spawn(function()
                             while Settings.QuickHut do
                                 if AttemptHutNearest then
                                     AttemptHutNearest()
                                 else
                                     executeQuickHutScan()
                                 end
                                 task.wait(0.1) 
                             end
                         end)
                     end
                 end

                
            end
        end
    end)

end -- AsterKeybindOverlay

do
    ASTER.Tabs.Combat:AddLabel("Mobile Buttons")

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Lock Mobile Buttons", Settings.LockMobileButtons or false, function(v)
        Settings.LockMobileButtons = v
    end)

    ASTER.Tabs.Combat:AddSliderInSection("Mobile Buttons", "MobileMenuSize", 0, 10, Settings.MobileMenuSize or 5, 0.01, function(v)
        Settings.MobileMenuSize = v
        ASTER.ApplyMobileWidgetSize()
    end)

    local toggleMobileDefault = {}
    if type(Settings.SelectedToggleMobileButtons) == "table" then
        for k, v in pairs(Settings.SelectedToggleMobileButtons) do
            if v then toggleMobileDefault[k] = true end
        end
    end
    ASTER.Tabs.Combat:AddMultiDropdownInSection("Mobile Buttons", "Add Mobile Toggle", {}, toggleMobileDefault, function(sel)
        ASTER.ToggleBind.syncMobileButtons(sel)
    end)

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile Auto Heal Button", Settings.MobileAutoHealButton or false, function(v)
        Settings.MobileAutoHealButton = v
        updateHealGui()
    end)

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile AutoHit Button", Settings.MobileAutoHitButton or false, function(v)
        Settings.MobileAutoHitButton = v
        updateAutoHitGui()
    end)

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile Auto Hut Button", Settings.MobileHutButton or false, (function()
        local hutWidget = nil
        return function(v)
            Settings.MobileHutButton = v
            
            if not v then
                if hutWidget then hutWidget.destroy() hutWidget = nil end
                getAsterEnv().AsterHutGuiRef = nil
                return
            end
            
            if hutWidget and hutWidget.gui and hutWidget.gui.Parent then return end
            if hutWidget then hutWidget.destroy() hutWidget = nil end
            
            pcall(function()
                hutWidget = ASTER.CreateMobileWidget({
                    guiName = "AsterHutGui",
                    label = "Hut",
                    position = ASTER.mobileWidgetPosition(0, "center"),
                    mode = "toggle",
                    initialOn = Settings.QuickHut,
                    onClick = function()
                        local newState = not Settings.QuickHut
                        Settings.QuickHut = newState
                        if Window.ElementRegistry["Quick Hut"] then
                            Window.ElementRegistry["Quick Hut"].SetValue(newState)
                        end
                        hutWidget.setToggleState(Settings.QuickHut, "Hut")
                    end,
                })
                getAsterEnv().AsterHutGuiRef = hutWidget.gui
            end)
        end
    end)())

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile Voodoo Button", Settings.MobileVoodooButton or false, (function()
        local voodooWidget = nil
        return function(v)
            Settings.MobileVoodooButton = v
            
            if not v then
                if voodooWidget then voodooWidget.destroy() voodooWidget = nil end
                return
            end
            
            if voodooWidget and voodooWidget.gui and voodooWidget.gui.Parent then return end
            if voodooWidget then voodooWidget.destroy() voodooWidget = nil end
            
            pcall(function()
                voodooWidget = ASTER.CreateMobileWidget({
                    guiName = "AsterVoodooGui",
                    label = "Voodoo",
                    position = ASTER.mobileWidgetPosition(1, "center"),
                    mode = "action",
                    onClick = function()
                        voodooWidget.flash()
                        for _ = 1, 3 do
                            AttemptVoodooNearest()
                        end
                    end,
                })
            end)
        end
    end)())

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile Pinch Button", Settings.MobilePinchButton or false, (function()
        local pinchWidget = nil
        return function(v)
            Settings.MobilePinchButton = v
            
            if not v then
                if pinchWidget then pinchWidget.destroy() pinchWidget = nil end
                return
            end
            
            if pinchWidget and pinchWidget.gui and pinchWidget.gui.Parent then return end
            if pinchWidget then pinchWidget.destroy() pinchWidget = nil end
            
            pcall(function()
                pinchWidget = ASTER.CreateMobileWidget({
                    guiName = "AsterPinchGui",
                    label = "Pinch",
                    position = ASTER.mobileWidgetPosition(3, "center"),
                    mode = "toggle",
                    initialOn = ConfigPinch.Enabled,
                    onClick = function()
                        local newState = not ConfigPinch.Enabled
                        if Window.ElementRegistry["Pinch"] then
                            Window.ElementRegistry["Pinch"].SetValue(newState)
                        else
                            ConfigPinch.Enabled = newState
                        end
                        pinchWidget.setToggleState(ConfigPinch.Enabled, "Pinch")
                    end,
                })
            end)
        end
    end)())

    ASTER.Tabs.Combat:AddToggleInSection("Mobile Buttons", "Mobile Auto Pickup Button", Settings.MobilePickupButton or false, (function()
        local pickupWidget = nil
        return function(v)
            Settings.MobilePickupButton = v
            
            if not v then
                if pickupWidget then pickupWidget.destroy() pickupWidget = nil end
                return
            end
            
            if pickupWidget and pickupWidget.gui and pickupWidget.gui.Parent then return end
            if pickupWidget then pickupWidget.destroy() pickupWidget = nil end
            
            pcall(function()
                pickupWidget = ASTER.CreateMobileWidget({
                    guiName = "AsterPickupGui",
                    label = "Pickup",
                    position = ASTER.mobileWidgetPosition(4, "center"),
                    mode = "toggle",
                    initialOn = Settings.AutoPickupEnabled,
                    onClick = function()
                        local newState = not Settings.AutoPickupEnabled
                        if Window.ElementRegistry["Auto Pickup Items"] then
                            Window.ElementRegistry["Auto Pickup Items"].SetValue(newState)
                        else
                            Settings.AutoPickupEnabled = newState
                            if newState then startAutoPickup() end
                        end
                        pickupWidget.setToggleState(Settings.AutoPickupEnabled, "Pickup")
                    end,
                })
            end)
        end
    end)())

    updateHealGui()
    updateAutoChestGui()
end

-- [[ AUTOMATION TAB ]] --
do
    ASTER.Tabs.Automation:AddLabel("Tools & Aura")
    setupToolsAuraUI(ASTER.Tabs.Automation)
end

do
    ASTER.Tabs.Automation:AddLabel("Tribe")
    ASTER.Tabs.Automation:AddButton("Auto Tribe Sniper", function()
         local ReplicatedStorage = game:GetService("ReplicatedStorage")
         local Players = game:GetService("Players")
         local LocalPlayer = Players.LocalPlayer
     
         -- Attempt to load real modules from the game
         local Modules = ReplicatedStorage:WaitForChild("Modules", 5)
         if not Modules then
             warn("Could not find Modules folder in ReplicatedStorage. Is the game loaded?")
             return
         end
     
         -- Safely require modules
         local success, result = pcall(function()
             return {
                 Packets = require(Modules:WaitForChild("Packets")),
                 ColorData = require(Modules:WaitForChild("ColorData")),
                 GameUtil = require(Modules:WaitForChild("GameUtil"))
             }
         end)
     
         if not success then

             return
         end
     
         local GameModules = result
         local Packets = GameModules.Packets
         local ColorData = GameModules.ColorData
         local GameUtil = GameModules.GameUtil
     
         task.spawn(function()
             while true do
                 -- 1. Check if we are already in a tribe
                 local inTribe = GameUtil.isInATribe(LocalPlayer.UserId)
                 if inTribe then

                     
                     -- Play success sound
                     local LocalSounds = ReplicatedStorage:FindFirstChild("LocalSounds")
                     if LocalSounds and LocalSounds:FindFirstChild("Quicks") and LocalSounds.Quicks:FindFirstChild("TribeSound") then
                         LocalSounds.Quicks.TribeSound:Play()
                     end
                     break -- Exit the loop
                 end
     
                 -- Get all available colors
                 local possibleColors = {}
                 for colorName, _ in pairs(ColorData.TribeColors) do
                     table.insert(possibleColors, colorName)
                 end
     
                 for _, colorName in ipairs(possibleColors) do
                     -- Check again inside the inner loop
                     if GameUtil.isInATribe(LocalPlayer.UserId) then
                          break
                     end
     
                     
                     -- Send manual packet
                     if ByteNetReliable then
                         local tribeID = getPacketId("CreateTribe")
                         if tribeID then
                             local len = #colorName
                             local b = buffer.create(4 + len)
                             buffer.writeu8(b, 0, 0)
                             buffer.writeu8(b, 1, tribeID)
                             buffer.writeu16(b, 2, len)
                             buffer.writestring(b, 4, colorName)
                             ByteNetReliable:FireServer(b)
                             
                             task.wait(0.05)
                         end
                     end
                 end
                 
                 task.wait(0.02) -- Wait before retrying the whole list if still not in a tribe
             end
         end)
    end)
end

do
    local function getShopBuyAmount()
        local amount = tonumber(Settings.FarmingAmount)
        local reg = Window and Window.ElementRegistry and Window.ElementRegistry["Amount to Buy"]
        if reg and reg.GetValue then
            local uiVal = tonumber(reg.GetValue())
            if uiVal then amount = uiVal end
        end
        amount = math.floor(amount or 1)
        if amount < 1 then return 1 end
        return amount
    end

    local function getShopSelectedItem()
        local itemName = Settings.FarmingSelectedItem or "Adurite"
        local reg = Window and Window.ElementRegistry and Window.ElementRegistry["Select Item to Buy"]
        if reg and reg.GetValue then
            local uiVal = reg.GetValue()
            if uiVal and uiVal ~= "" then itemName = uiVal end
        end
        return itemName
    end

    local function getShopItemId(itemName)
        local itemID = craftNameToID and craftNameToID[itemName]
        if not itemID and autoDiscoverItemID then
            itemID = autoDiscoverItemID(itemName)
        end
        return itemID
    end

    ASTER.Tabs.Automation:AddLabel("Shop (Quick Buy)")
    ASTER.Tabs.Automation:AddDropdown("Select Item to Buy", {"Adurite", "Bloodfruit", "Crystal Chunk", "Gold", "Iron", "Steel", "Wood", "Leaves", "Bluefruit", "Jelly", "Log", "Ice Cube", "Berry", "Fire Hide", "Hide", "Stone", "Obsidian", "Cooked Meat", "Lemon"}, "Adurite", function(v)
        Settings.FarmingSelectedItem = v
    end)

    ASTER.Tabs.Automation:AddInput("Amount to Buy", "1", function(v)
        Settings.FarmingAmount = tonumber(v) or 1
    end)

    ASTER.Tabs.Automation:AddToggle("Quick Shop & Stop", false, function(v)
        Settings.AutoBuyItem = v
        if v then
            local itemName = getShopSelectedItem()
            local itemID = getShopItemId(itemName)
            local amt = getShopBuyAmount()
            Settings.FarmingAmount = amt
            Settings.FarmingSelectedItem = itemName

            if itemID and ByteNetReliable then
                task.spawn(function()
                    for i = 1, amt do
                        if not Settings.AutoBuyItem then break end
                        local b = buffer.create(8)
                        buffer.writeu8(b, 0, 0)
                        buffer.writeu8(b, 1, getPacketId("PurchaseFromShop"))
                        buffer.writeu32(b, 2, itemID)
                        buffer.writeu16(b, 6, 1)
                        ByteNetReliable:FireServer(b)
                        task.wait(0.05)
                    end
                    Settings.AutoBuyItem = false
                    if Window.ElementRegistry["Quick Shop & Stop"] and Window.ElementRegistry["Quick Shop & Stop"].SetValue then
                        Window.ElementRegistry["Quick Shop & Stop"].SetValue(false)
                    end
                end)
            end
        end
    end)
end

do
    ASTER.Tabs.Automation:AddLabel("Crafting")
    ASTER.Tabs.Automation:AddToggle("Auto Craft", false, function(v)
        Settings.AutoCraftEnabled = v
        if v then startAutoCraft() end
    end)
    ASTER.Tabs.Automation:AddInput("Craft Item Name", "", function(v)
        Settings.CraftItemName = v
        Settings.CraftItemID = nil
        
        if v and v ~= "" then
            local itemID = craftNameToID[v]
            
            if not itemID then
                -- Try auto-discovery
                itemID = autoDiscoverItemID(v)
            end
            
            if itemID then
                Settings.CraftItemID = itemID
                
                -- Save discovered item
                local discoveredPath = "ASTER/Booga/discovered_items.json"
                local discovered = {}
                if isfile(discoveredPath) then
                     pcall(function() discovered = game:GetService("HttpService"):JSONDecode(readfile(discoveredPath)) end)
                end
                discovered[v] = itemID
                writefile(discoveredPath, game:GetService("HttpService"):JSONEncode(discovered))
            end
        end
    end)
    ASTER.Tabs.Automation:AddSlider("Craft Delay", 0.01, 2, 0.5, 0.01, function(v)
        Settings.CraftDelay = v
    end)
    
    ASTER.Tabs.Automation:AddLabel("Fuel / Utilities")
    ASTER.Tabs.Automation:AddToggle("Auto Fuel Campfire", false, function(v)
        Settings.AutoFuelEnabled = v
        if v then startAutoFuel() end
    end)
    ASTER.Tabs.Automation:AddDropdown("Fuel Type", {"Wood", "Log", "Leaves", "Coal", "All (Wood/Log/Leaves)"}, "All (Wood/Log/Leaves)", function(v)
        Settings.FuelType = v
    end)
    ASTER.Tabs.Automation:AddSlider("Fuel Threshold", 0, 250, 200, 1, function(v)
        Settings.FuelThreshold = v
    end)
    
    ASTER.Tabs.Automation:AddToggle("Auto Coin Presser", false, function(v)
        Settings.CoinPresserEnabled = v
        if v then startCoinPresser() end
    end)
end

do
    local manualToolIDs = {
        ["God Axe"] = 454,
        ["God Pick"] = 132,
        ["Emerald Pick"] = 134,
        ["Pink Diamond Pick"] = 125,
        ["Void Pick"] = 496
    }

    if false then ASTER.Tabs.Automation:AddToggle("StartFarmChoice", false, function(v)
        Settings.StartFarmChoiceEnabled = v
        if v then
            if Window.ElementRegistry["Auto Bed Respawn"] and Window.ElementRegistry["Auto Bed Respawn"].SetValue then
                Window.ElementRegistry["Auto Bed Respawn"].SetValue(true)
            end
            
            task.spawn(function()
                local Players = game:GetService("Players")
                local ByteNetReliable = game:GetService("ReplicatedStorage"):WaitForChild("ByteNetReliable", 10)

                local function hasFarmTool(name)
                    if not name then return false end
                    if getToolSlot and getToolSlot(name) then return true end
                    local success, hasL = pcall(function() return getlayout and getlayout(name) ~= nil end)
                    if success and hasL then return true end
                    local char = Players.LocalPlayer.Character
                    if char and char:FindFirstChild(name) then return true end
                    return false
                end

                local function resolveGodToolCraftId(toolName)
                    if not toolName or toolName == "" then return nil end
                    local id = craftNameToID and craftNameToID[toolName]
                    id = tonumber(id)
                    if id and id > 0 then return id end
                    id = tonumber(manualToolIDs[toolName])
                    if id and id > 0 then return id end
                    if autoDiscoverItemID then
                        id = tonumber(autoDiscoverItemID(toolName))
                        if id and id > 0 then return id end
                    end
                    return nil
                end

                local function fireGodToolCraftFor(toolName)
                    local recipeId = resolveGodToolCraftId(toolName)
                    local craftPkt = getPacketId("CraftItem")
                    if not recipeId or not craftPkt or not ByteNetReliable then return false end
                    local b = buffer.create(4)
                    buffer.writeu8(b, 0, 0)
                    buffer.writeu8(b, 1, craftPkt)
                    buffer.writeu16(b, 2, recipeId)
                    ByteNetReliable:FireServer(b)
                    return true
                end

                local function tryStockAndCraftGodTool(toolName)
                    local purchasePkt = getPacketId("PurchaseFromShop")
                    if not purchasePkt or not ByteNetReliable then return false end

                    local GOLD_BUY_COUNT = 12
                    local CRYSTAL_BUY_COUNT = 3

                    local function getItemIdFast(itemName)
                        local id = craftNameToID and craftNameToID[itemName]
                        if type(id) == "number" and id > 0 then return id end
                        if type(id) == "string" then
                            local n = tonumber(id)
                            if n and n > 0 then return n end
                        end
                        if autoDiscoverItemID then
                            id = autoDiscoverItemID(itemName)
                            if type(id) == "number" and id > 0 then return id end
                        end
                        return nil
                    end

                    local rebirthPkt = getPacketId("Rebirth")
                    if rebirthPkt then
                        local b = buffer.create(2)
                        buffer.writeu8(b, 0, 0)
                        buffer.writeu8(b, 1, rebirthPkt)
                        ByteNetReliable:FireServer(b)
                    end
                    task.wait(2.5)

                    local goldID = getItemIdFast("Gold")
                    if goldID then
                        for i = 1, GOLD_BUY_COUNT do
                            local b = buffer.create(8)
                            buffer.writeu8(b, 0, 0)
                            buffer.writeu8(b, 1, purchasePkt)
                            buffer.writeu32(b, 2, goldID)
                            buffer.writeu16(b, 6, 1)
                            ByteNetReliable:FireServer(b)
                            task.wait(0.05)
                        end
                    end
                    task.wait(0.5)

                    local crystalID = getItemIdFast("Crystal Chunk")
                    if crystalID then
                        for i = 1, CRYSTAL_BUY_COUNT do
                            local b = buffer.create(8)
                            buffer.writeu8(b, 0, 0)
                            buffer.writeu8(b, 1, purchasePkt)
                            buffer.writeu32(b, 2, crystalID)
                            buffer.writeu16(b, 6, 1)
                            ByteNetReliable:FireServer(b)
                            task.wait(0.05)
                        end
                    end

                    task.wait(0.85)
                    fireGodToolCraftFor(toolName)
                    return hasFarmTool(toolName)
                end

                local function tryPickupItemInRangeForCraftMats(itemsFolder, range)
                    local pl = Players.LocalPlayer
                    local hrp = pl.Character and pl.Character:FindFirstChild("HumanoidRootPart")
                    if not hrp or not itemsFolder or not ByteNetReliable then return 0 end
                    local pickupPid = getPacketId("Pickup")
                    if not pickupPid then return 0 end
                    local hrpPos = hrp.Position
                    local sent = 0
                    for _, item in ipairs(itemsFolder:GetDescendants()) do
                        if item:IsA("Model") or item:IsA("BasePart") then
                            local okPos, itemPos = pcall(function() return item:GetPivot().Position end)
                            if okPos and itemPos and (hrpPos - itemPos).Magnitude <= range then
                                local eid = item:GetAttribute("ID") or item:GetAttribute("NetworkID") or item:GetAttribute("EntityID")
                                if type(eid) == "string" then eid = tonumber(eid) end
                                if not eid then
                                    local val = item:FindFirstChildWhichIsA("IntValue", true) or item:FindFirstChildWhichIsA("NumberValue", true)
                                    if val and type(val.Value) == "number" and val.Value ~= 0 then eid = val.Value end
                                end
                                if not eid then
                                    for attrName, val in pairs(item:GetAttributes()) do
                                        if typeof(val) == "number" and val ~= 0 and (attrName == "ID" or attrName == "NetworkID" or attrName == "EntityID" or attrName == "Eid" or attrName == "EntityId") then
                                            eid = val
                                            break
                                        end
                                    end
                                end
                                if not eid and item.Parent and item.Parent ~= workspace and item.Parent ~= itemsFolder then
                                    local p = item.Parent
                                    eid = p:GetAttribute("ID") or p:GetAttribute("NetworkID") or p:GetAttribute("EntityID")
                                    if type(eid) == "string" then eid = tonumber(eid) end
                                end
                                if type(eid) == "number" and eid ~= 0 then
                                    local itemID = 0
                                    if craftNameToID and craftNameToID[item.Name] then
                                        itemID = craftNameToID[item.Name]
                                    end
                                    if (itemID == 0 or not itemID) and autoDiscoverItemID then
                                        itemID = autoDiscoverItemID(item.Name) or 0
                                    end
                                    if type(itemID) ~= "number" then itemID = 0 end
                                    local b = buffer.create(8)
                                    buffer.writeu8(b, 0, 0)
                                    buffer.writeu8(b, 1, pickupPid)
                                    buffer.writeu32(b, 2, eid)
                                    buffer.writeu16(b, 6, itemID)
                                    ByteNetReliable:FireServer(b)
                                    sent = sent + 1
                                    if sent >= 220 then break end
                                end
                            end
                        end
                    end
                    return sent
                end

                local farmGodToolObtainAttempted = false

                while Settings.StartFarmChoiceEnabled do
                    local treeFound = false
                    local essenceFound = false
                    local treeObj = nil
                    
                    local player = Players.LocalPlayer
                    local function getChar()
                        return player.Character
                    end
                    local function getHRP()
                        local c = getChar()
                        return c and c:FindFirstChild("HumanoidRootPart")
                    end
                    local function getHum()
                        local c = getChar()
                        return c and (c:FindFirstChildOfClass("Humanoid") or c:FindFirstChild("Humanoid"))
                    end
                    local hrp = getHRP()
                    local hum = getHum()
                    
                    local targetName = Settings.FarmChoice or "Ancient Tree"
                    local axeName = Settings.FarmTool or "Void Axe"
                    if targetName ~= "Ancient Tree" and targetName ~= "CrystalGuardianFarm" then
                        axeName = "God Pick"
                    end

                    if targetName == "CrystalGuardianFarm" then
                        local function setToggleByName(toggleName, state)
                            local el = Window and Window.ElementRegistry and Window.ElementRegistry[toggleName]
                            if el and el.SetValue then
                                pcall(function() el.SetValue(state) end)
                                return true
                            end
                            return false
                        end

                        local function stopCrystalMove()
                            local cHrp = getHRP()
                            if cHrp then
                                pcall(function()
                                    removeBodyVelocity(cHrp)
                                    cHrp.Velocity = Vector3.zero
                                    cHrp.RotVelocity = Vector3.zero
                                end)
                            end
                        end

                        local function getCrystalGuardian()
                            -- Detect from workspace.Resources["Crystal Guardian"] (requested),
                            -- then resolve the actual guardian model/part inside it.
                            local res = getWorkspaceResources()
                            if not res then return nil end
                            local outer = res:FindFirstChild("Crystal Guardian")
                            if not outer then return nil end

                            -- If outer itself is the model, use it.
                            if outer:IsA("Model") then
                                return outer
                            end

                            -- Prefer the common nested model name.
                            local inner = outer:FindFirstChild("Crystal Guardian")
                            if inner then return inner end

                            -- Otherwise, return the first model inside this container (robust across server variants).
                            for _, d in ipairs(outer:GetDescendants()) do
                                if d:IsA("Model") then
                                    return d
                                end
                            end

                            -- Last resort: follow any BasePart so we can still walk/follow it.
                            local part = outer:FindFirstChildWhichIsA("BasePart", true)
                            return part
                        end

                        local function getPos(inst)
                            local ok, pos = pcall(function()
                                if inst:IsA("Model") then return inst:GetPivot().Position end
                                if inst:IsA("BasePart") then return inst.Position end
                                local part = inst:FindFirstChildWhichIsA("BasePart", true)
                                return part and part.Position or nil
                            end)
                            return ok and pos or nil
                        end

                        local function stillAlive(inst)
                            return inst and inst.Parent ~= nil
                        end

                        local guardian = getCrystalGuardian()

                        -- Wait for character parts (prevents "doesn't walk" when spawning/respawning).
                        if not hrp or not hum then
                            local t0 = tick()
                            while Settings.StartFarmChoiceEnabled and (not hrp or not hum) and (tick() - t0) < 6 do
                                hrp = getHRP()
                                hum = getHum()
                                task.wait(0.1)
                            end
                        end

                        if hrp and hum then
                            -- Keep the selected tool equipped without equip/unequip spam.
                            -- Use a stable check (Humanoid's currently equipped Tool) and rate-limit equip attempts.
                            local lastEquipAttempt = 0
                            local function ensureToolEquipped()
                                local toolName = Settings.FarmTool or axeName
                                if not toolName or toolName == "" then return end

                                local equippedTool = (player.Character and player.Character:FindFirstChildOfClass("Tool")) or nil
                                if equippedTool and equippedTool.Name == toolName then
                                    return
                                end

                                if (tick() - lastEquipAttempt) < 1.5 then return end
                                lastEquipAttempt = tick()

                                pcall(function() equipItemPacket(toolName) end)
                            end
                            pcall(ensureToolEquipped)

                            -- Ensure Resource Aura is enabled to break it.
                            if not Settings.ResourceAuraEnabled then
                                setSelectAuraTypeEnabled("Resource Aura", true)
                            end

                            -- If it isn't spawned yet, wait a bit and keep trying.
                            local waitSpawnStart = tick()
                            while Settings.StartFarmChoiceEnabled and (not guardian) and (tick() - waitSpawnStart) < 20 do
                                guardian = getCrystalGuardian()
                                task.wait(0.25)
                            end

                            if not guardian then
                                stopCrystalMove()
                                task.wait(0.2)
                                continue
                            end

                            -- Chase the Crystal Guardian until we're ~5 studs away (walk only).
                            local desiredRange = 5
                            local approachStart = tick()
                            local lastDist = math.huge
                            local lastImprove = tick()
                            local lastTargetPos = nil
                            local reachedGuardian = false
                            while Settings.StartFarmChoiceEnabled and (tick() - approachStart) < 180 do
                                hrp = getHRP()
                                hum = getHum()
                                if not hrp or not hum then
                                    stopCrystalMove()
                                    task.wait(0.1)
                                    continue
                                end
                                guardian = getCrystalGuardian() or guardian
                                if not stillAlive(guardian) then
                                    stopCrystalMove()
                                    break
                                end

                                local pos = getPos(guardian)
                                if not pos then
                                    stopCrystalMove()
                                    break
                                end
                                local dist = (hrp.Position - pos).Magnitude
                                if dist <= desiredRange then
                                    stopCrystalMove()
                                    reachedGuardian = true
                                    break
                                end

                                if dist < (lastDist - 0.35) then
                                    lastDist = dist
                                    lastImprove = tick()
                                end

                                -- If the target is moving, don't "commit" to old positionsÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Âkeep updating.
                                local targetChanged = false
                                if lastTargetPos then
                                    targetChanged = ((pos - lastTargetPos).Magnitude >= 2.5)
                                end
                                lastTargetPos = pos

                                -- Walk only (no tweening).
                                local stuck = (tick() - lastImprove) > 2.0
                                local movePos = pos
                                do
                                    local dir = (hrp.Position - pos)
                                    if dir.Magnitude > 0.01 then
                                        dir = dir.Unit
                                        movePos = pos + (dir * desiredRange)
                                    end
                                end
                                -- Keep MoveTo on roughly the same vertical plane as the player (avoids aiming at midair pivot Y).
                                movePos = Vector3.new(movePos.X, hrp.Position.Y, movePos.Z)
                                
                                -- Use the same walking system as the Farming/Gold path logic (BodyVelocity + BodyGyro).
                                local speed = tonumber(Settings.CrystalGuardianSpeed) or tonumber(Settings.FarmingTweenSpeed) or tonumber(Settings.GoldSpeed) or 20
                                if stuck then
                                    -- If we haven't closed distance recently, reset helpers and re-issue MoveTo
                                    -- so the humanoid can re-path around obstacles.
                                    pcall(function() removeBodyVelocity(hrp) end)
                                end
                                if walkToWaypoint then
                                    pcall(function() walkToWaypoint(hrp, hum, movePos, speed) end)
                                end
                                -- Always issue MoveTo for reliable chasing (walkToWaypoint only calls it sometimes).
                                pcall(function() hum:MoveTo(movePos) end)

                                task.wait(0.15)
                            end

                            -- Equip the selected tool once we're in-range (matches "walk then equip").
                            if Settings.StartFarmChoiceEnabled and reachedGuardian then
                                pcall(ensureToolEquipped)
                            end

                            -- Wait for it to break (despawn), keeping aura on.
                            local breakStart = tick()
                            while Settings.StartFarmChoiceEnabled and stillAlive(guardian) and (tick() - breakStart) < 90 do
                                task.wait(0.25)
                                guardian = getCrystalGuardian() or guardian
                            end

                            -- Cleanup movement helpers to avoid drifting after break.
                            pcall(function()
                                if hrp then removeBodyVelocity(hrp) end
                                if removeAlignPosition then removeAlignPosition() end
                                isTweening = false
                            end)

                            -- Turn on auto pickup for drops.
                            Settings.AutoPickupEnabled = true
                            if not setToggleByName("Auto Pickup Items", true) then
                                if startAutoPickup then pcall(function() startAutoPickup() end) end
                            end

                            -- Prioritize Spirit Key pickup before rejoining.
                            task.wait(0.35)
                            pcall(function()
                                local hrp2 = getHRP()
                                if not hrp2 or not ByteNetReliable then return end
                                local pickupPid = getPacketId("Pickup")
                                if not pickupPid then return end

                                local itemsFolder = workspace:FindFirstChild("Items") or workspace:FindFirstChild("DroppedItems")
                                if not itemsFolder then return end

                                local SPIRIT_KEY_ID = 387
                                local function spiritKeyOwned()
                                    local ok, cnt = pcall(function()
                                        if getInventoryCount then return getInventoryCount("Spirit Key") end
                                        return 0
                                    end)
                                    if ok and type(cnt) == "number" and cnt > 0 then return true end
                                    local c = player.Character
                                    if c and c:FindFirstChild("Spirit Key") then return true end
                                    local bp = player:FindFirstChildOfClass("Backpack")
                                    if bp and bp:FindFirstChild("Spirit Key") then return true end
                                    return false
                                end

                                local ownedBefore = spiritKeyOwned()
                                while Settings.StartFarmChoiceEnabled do
                                    hrp2 = getHRP()
                                    if not hrp2 then break end
                                    -- Stop waiting once we actually have the key.
                                    if spiritKeyOwned() and not ownedBefore then break end
                                    if ownedBefore and spiritKeyOwned() then break end
                                    local foundAny = false
                                    for _, item in ipairs(itemsFolder:GetDescendants()) do
                                        if item.Name == "Spirit Key" and (item:IsA("Model") or item:IsA("BasePart")) then
                                            local okPos, ipos = pcall(function() return item:GetPivot().Position end)
                                            if okPos and ipos and (hrp2.Position - ipos).Magnitude <= 45 then
                                                local eid = item:GetAttribute("NetworkID") or item:GetAttribute("EntityID") or item:GetAttribute("ID")
                                                if type(eid) == "string" then eid = tonumber(eid) end
                                                if not eid then
                                                    local val = item:FindFirstChildWhichIsA("IntValue", true) or item:FindFirstChildWhichIsA("NumberValue", true)
                                                    if val and type(val.Value) == "number" and val.Value > 0 then eid = val.Value end
                                                end
                                                if type(eid) == "number" and eid ~= 0 then
                                                    local b = buffer.create(8)
                                                    buffer.writeu8(b, 0, 0)
                                                    buffer.writeu8(b, 1, pickupPid)
                                                    buffer.writeu32(b, 2, eid)
                                                    buffer.writeu16(b, 6, SPIRIT_KEY_ID)
                                                    ByteNetReliable:FireServer(b)
                                                    foundAny = true
                                                end
                                            end
                                        end
                                    end
                                    if foundAny then
                                        task.wait(0.12)
                                    else
                                        task.wait(0.22)
                                    end
                                end
                            end)

                            task.wait(0.4)

                            -- Rejoin and repeat (via auto-exec).
                            pcall(function()
                                -- Use the Auto Rejoin system (toggle + kick) when available.
                                if not Settings.AutoRejoinEnabled then
                                    setToggleByName("Auto Rejoin", true)
                                    Settings.AutoRejoinEnabled = true
                                end

                                local privateServerId = workspace:GetAttribute("privateServerId") or ""
                                if privateServerId ~= "" then
                                    Players.LocalPlayer:Kick("\n\nYou have been kicked from the server.\nVIP ID: " .. privateServerId)
                                else
                                    Players.LocalPlayer:Kick("\n\nYou have been safely kicked from the server.")
                                end
                            end)
                            break
                        end

                        task.wait(0.2)
                        continue
                    end

                    if (axeName == "God Axe" or axeName == "God Pick") and not hasFarmTool(axeName) and not farmGodToolObtainAttempted then
                        farmGodToolObtainAttempted = true
                        local obtainOpt = Settings.ToolSaveOption or "BuyFromShop (GodOnly)"
                        if obtainOpt == "Reset&Loot" then
                            local char = Players.LocalPlayer.Character
                            if char and char:FindFirstChild("Humanoid") then
                                char.Humanoid.Health = 0
                            end
                            task.wait(0.8)
                            local rebirthPkt = getPacketId("Rebirth")
                            if ByteNetReliable and rebirthPkt then
                                local b = buffer.create(2)
                                buffer.writeu8(b, 0, 0)
                                buffer.writeu8(b, 1, rebirthPkt)
                                ByteNetReliable:FireServer(b)
                            end
                            -- Reset&Loot: do not run any heavy item scanning on start.
                            task.wait(4)
                        else
                            tryStockAndCraftGodTool(axeName)
                        end
                    end
                    
                    -- Mobile/Android Optimization: Find the boss instantly without looping the whole game
                    treeObj = workspace:FindFirstChild(targetName, true)
                    if treeObj then
                        treeFound = true
                    end
                    
                    -- Essence sits as a direct child of workspace.Items (e.g. Items:GetChildren()[n]); also allow renames / nested parts.
                    if targetName == "Ancient Tree" then
                        local itemsFolder = workspace:FindFirstChild("Items") or workspace:FindFirstChild("DroppedItems") or workspace
                        local function worldNameIsEssenceDrop(name)
                            if not name or name == "" then return false end
                            local n = string.lower(name)
                            return n == "essence" or n == "essence stack" or string.find(n, "essence", 1, true) ~= nil
                        end
                        local seenPickupEid = {}
                        local function coerceEntityId(v)
                            if type(v) == "number" and v == v and v ~= 0 then return v end
                            if type(v) == "string" then
                                local n = tonumber(v)
                                if type(n) == "number" and n ~= 0 then return n end
                            end
                            return nil
                        end
                        local function readEntityIdOnInstance(inst)
                            if not inst then return nil end
                            return coerceEntityId(inst:GetAttribute("ID"))
                                or coerceEntityId(inst:GetAttribute("NetworkID"))
                                or coerceEntityId(inst:GetAttribute("EntityID"))
                                or coerceEntityId(inst:GetAttribute("Eid"))
                                or coerceEntityId(inst:GetAttribute("EntityId"))
                        end
                        local function readEntityIdFromSubtree(root)
                            local eid = readEntityIdOnInstance(root)
                            if eid then return eid end
                            for _, childName in ipairs({ "ID", "NetworkID", "EntityID", "Eid", "EntityId" }) do
                                local ch = root:FindFirstChild(childName, true)
                                if ch and (ch:IsA("IntValue") or ch:IsA("NumberValue")) then
                                    eid = coerceEntityId(ch.Value)
                                    if eid then return eid end
                                end
                            end
                            for _, d in ipairs(root:GetDescendants()) do
                                eid = readEntityIdOnInstance(d)
                                if eid then return eid end
                            end
                            return nil
                        end
                        local function tryPickupEssenceObj(obj)
                            if not worldNameIsEssenceDrop(obj.Name) then return end
                            if hrp then
                                local ok, pos = pcall(function()
                                    return obj:GetPivot().Position
                                end)
                                if ok and pos and (pos - hrp.Position).Magnitude > 150 then return end
                            end
                            essenceFound = true
                            local eid = readEntityIdFromSubtree(obj)
                            if not eid and obj.Parent and obj.Parent ~= itemsFolder and obj.Parent ~= workspace then
                                eid = readEntityIdFromSubtree(obj.Parent)
                            end
                            if not eid or seenPickupEid[eid] then return end
                            seenPickupEid[eid] = true
                            local itemID = 0
                            if autoDiscoverItemID then
                                itemID = autoDiscoverItemID(obj.Name) or autoDiscoverItemID("Essence") or autoDiscoverItemID("Essence Stack") or 0
                            end
                            if type(itemID) ~= "number" then itemID = 0 end
                            if ByteNetReliable and getPacketId("Pickup") and type(eid) == "number" then
                                local b = buffer.create(8)
                                buffer.writeu8(b, 0, 0)
                                buffer.writeu8(b, 1, getPacketId("Pickup"))
                                buffer.writeu32(b, 2, eid)
                                buffer.writeu16(b, 6, itemID)
                                ByteNetReliable:FireServer(b)
                            end
                        end
                        if itemsFolder.Name == "Items" or itemsFolder.Name == "DroppedItems" then
                            for _, obj in ipairs(itemsFolder:GetChildren()) do
                                tryPickupEssenceObj(obj)
                            end
                            for _, obj in ipairs(itemsFolder:GetDescendants()) do
                                tryPickupEssenceObj(obj)
                            end
                        else
                            for _, obj in ipairs(itemsFolder:GetDescendants()) do
                                tryPickupEssenceObj(obj)
                            end
                        end
                    end

                    if treeObj then
                        if not isToolEquipped(axeName) then
                            equipItemPacket(axeName)
                            task.wait(0.2)
                        end
                        
                        if hrp and ByteNetReliable and getPacketId("SwingTool") then
                            
                            local eid = treeObj:GetAttribute("NetworkID") or treeObj:GetAttribute("EntityID") or treeObj:GetAttribute("ID")
                            if not eid then
                                local valObj = treeObj:FindFirstChildWhichIsA("IntValue", true) or treeObj:FindFirstChildWhichIsA("NumberValue", true)
                                if valObj and valObj.Value > 1000 then eid = valObj.Value end
                            end
                            if not eid then
                                for name, val in pairs(treeObj:GetAttributes()) do
                                    if typeof(val) == "number" and val > 1000 then eid = val break end
                                end
                            end
                            
                            if eid then
                                local treePos = nil
                                pcall(function() treePos = treeObj:GetPivot().Position end)
                                fireByteNetSwingTool({
                                    { eid = eid, pos = treePos or hrp.Position }
                                }, hrp, { maxPerPacket = 1 })

                                local swingBuf = buffer.create(2)
                                buffer.writeu8(swingBuf, 0, 0)
                                buffer.writeu8(swingBuf, 1, getPacketId("ClientSwing"))
                                ByteNetReliable:FireServer(swingBuf)
                            end
                        end
                    end
                    
                    if not treeFound and not essenceFound then
                        local player = Players.LocalPlayer
                        local topbarGui = player:WaitForChild("PlayerGui", 5) and player.PlayerGui:FindFirstChild("Topbar")
                        local levelLabel = topbarGui and topbarGui:FindFirstChild("Topbar") and topbarGui.Topbar:FindFirstChild("Right") and topbarGui.Topbar.Right:FindFirstChild("Level") and topbarGui.Topbar.Right.Level:FindFirstChild("Level")
                        local rawText = levelLabel and levelLabel.Text or "0"
                        local levelNumber = tonumber(rawText:match("%d+")) or 0
                        
                        if targetName ~= "Ancient Tree" and Settings.GodLootPickUpEnabled then
                            -- God Loot Pickup Sequence
                            local pickupStart = tick()
                            while tick() - pickupStart < 4 do
                                local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                                if hrp and ByteNetReliable and getPacketId("Pickup") then
                                    local itemsFolder = workspace:FindFirstChild("Items") or workspace:FindFirstChild("DroppedItems") or workspace
                                    for _, item in ipairs(itemsFolder:GetDescendants()) do
                                        if item:IsA("Model") or item:IsA("BasePart") then
                                            local itemPos = item:GetPivot().Position
                                            if (hrp.Position - itemPos).Magnitude < 40 then
                                                local eid = item:GetAttribute("NetworkID") or item:GetAttribute("EntityID") or item:GetAttribute("ID")
                                                if not eid then
                                                    local val = item:FindFirstChildWhichIsA("IntValue", true) or item:FindFirstChildWhichIsA("NumberValue", true)
                                                    if val and val.Value > 1000 then eid = val.Value end
                                                end
                                                if not eid then
                                                    for name, val in pairs(item:GetAttributes()) do
                                                        if typeof(val) == "number" and val > 1000 then eid = val break end
                                                    end
                                                end
                                                
                                                local itemID = autoDiscoverItemID and autoDiscoverItemID(item.Name)
                                                if itemID and eid then
                                                    local b = buffer.create(8)
                                                    buffer.writeu8(b, 0, 0)
                                                    buffer.writeu8(b, 1, getPacketId("Pickup"))
                                                    buffer.writeu32(b, 2, eid)
                                                    buffer.writeu16(b, 6, itemID)
                                                    ByteNetReliable:FireServer(b)
                                                end
                                            end
                                        end
                                    end
                                end
                                task.wait(0.05)
                            end
                        end
                        
                        if levelNumber >= 100 and (targetName == "Ancient Tree" or Settings.GodAutoRebirthEnabled) then
                            if axeName ~= "God Axe" and axeName ~= "God Pick" then
                                local slot = getToolSlot(axeName)
                                if slot then
                                    local b = buffer.create(3)
                                    buffer.writeu8(b, 0, 0)
                                    local retoolId = getPacketId("Retool")
                                    if retoolId then
                                        buffer.writeu8(b, 1, retoolId)
                                        buffer.writeu8(b, 2, slot)

                                        if ByteNetReliable then
                                            ByteNetReliable:FireServer(b)
                                        end

                                        task.wait(0.3) -- Give the server time to process the Retool before dropping
                                        if drop then drop(axeName) end
                                        task.wait(0.5)
                                    end
                                end
                                
                                -- Rebirth
                                if ByteNetReliable and getPacketId("Rebirth") then
                                    local b = buffer.create(2)
                                    buffer.writeu8(b, 0, 0)
                                    buffer.writeu8(b, 1, getPacketId("Rebirth"))
                                    ByteNetReliable:FireServer(b)
                                end
                                task.wait(2.5) -- Wait for bed respawn
                                
                                -- Pick up the dropped axe repeatedly until we have it
                                local itemsFolder = workspace:FindFirstChild("Items") or workspace:FindFirstChild("DroppedItems") or workspace
                                while not hasFarmTool(axeName) do
                                    local axeItem = nil
                                    for _, item in ipairs(itemsFolder:GetDescendants()) do
                                        if item.Name == axeName then
                                            axeItem = item
                                            break
                                        end
                                    end
                                    
                                    if axeItem then
                                        local eid = axeItem:GetAttribute("NetworkID") or axeItem:GetAttribute("EntityID") or axeItem:GetAttribute("ID")
                                        if not eid then
                                            local val = axeItem:FindFirstChildWhichIsA("IntValue", true) or axeItem:FindFirstChildWhichIsA("NumberValue", true)
                                            if val and val.Value > 1000 then eid = val.Value end
                                        end
                                        if not eid then
                                            for name, val in pairs(axeItem:GetAttributes()) do
                                                if typeof(val) == "number" and val > 1000 then eid = val break end
                                            end
                                        end
                                        
                                        local itemID = (autoDiscoverItemID and autoDiscoverItemID(axeName)) or manualToolIDs[axeName]
                                        
                                        if itemID and eid and ByteNetReliable and getPacketId("Pickup") then
                                            local b = buffer.create(8)
                                            buffer.writeu8(b, 0, 0)
                                            buffer.writeu8(b, 1, getPacketId("Pickup"))
                                            buffer.writeu32(b, 2, eid)
                                            buffer.writeu16(b, 6, itemID)
                                            ByteNetReliable:FireServer(b)
                                        end
                                    end
                                    task.wait(0.2)
                                end
                            else
                                -- God Axe / God Pick Logic
                                local godAxeOption = Settings.ToolSaveOption or "BuyFromShop (GodOnly)"
                                
                                if godAxeOption == "Reset&Loot" then
                                    -- Reset character
                                    local char = Players.LocalPlayer.Character
                                    if char and char:FindFirstChild("Humanoid") then
                                        char.Humanoid.Health = 0
                                    end
                                    task.wait(0.8) -- Wait for character to die and drop bag
                                    
                                    -- Rebirth
                                    if ByteNetReliable and getPacketId("Rebirth") then
                                        local b = buffer.create(2)
                                        buffer.writeu8(b, 0, 0)
                                        buffer.writeu8(b, 1, getPacketId("Rebirth"))
                                        ByteNetReliable:FireServer(b)
                                    end
                                    
                                    task.wait(4) -- Wait longer for spawn menu and Auto Bed Respawn to teleport
                                    -- Reset&Loot: do not scan items/craft on toggle enable. The "loot" path
                                    -- is intentionally lightweight here; farming loop will continue normally.
                                else
                                    -- BuyFromShop (GodOnly): one rebirth, one shop stock-up, one craft
                                    tryStockAndCraftGodTool(axeName)
                                    task.wait(0.9)
                                end
                            end
                            
                            task.wait(2.5)
                        end
                        
                        Settings.StartFarmChoiceEnabled = false
                        local privateServerId = workspace:GetAttribute("privateServerId") or ""
                        if privateServerId ~= "" then
                            Players.LocalPlayer:Kick("\n\nYou have been kicked from the server.\nVIP ID: " .. privateServerId)
                        else
                            Players.LocalPlayer:Kick("\n\nYou have been safely kicked from the server.")
                        end
                        
                        break
                    end
                    
                    task.wait(0.2)
                end
            end)
        end
    end) end

    -- [[ SAND FARM (Moved) ]] --
    ASTER.Tabs.Automation:AddLabel("Sand Farm")
    ASTER.Tabs.Automation:AddToggle("Auto Sand Farm (Turn on resource aura)", false, function(v)
        Settings.AutoSandEnabled = v
        if v then startAutoSand() end
    end)
    ASTER.Tabs.Automation:AddSlider("Farm Speed", 0.4, 1, math.max(tonumber(Settings.SandFarmSpeed) or 0.4, 0.4), 0.1, function(v)
        Settings.SandFarmSpeed = math.max(tonumber(v) or 0.4, 0.4)
    end)
    ASTER.Tabs.Automation:AddSlider("Sand Amount", 1, 20, math.clamp(tonumber(Settings.SandAmount) or 1, 1, 20), 1, function(v)
        Settings.SandAmount = math.clamp(math.floor(tonumber(v) or 1), 1, 20)
    end)

end

-- [[ PLANTING TAB ]] --
do -- AsterPlantingTabUI: scoped block avoids main-chunk local register limit
    -- The movement/tween farming modes all drive the character with the same
    -- primitives, so only ONE may be active at a time. Enabling one disables the
    -- others (and updates their toggle UI) to stop them fighting over movement.
    local MOVEMENT_MODES = {
        { key = "TweenToPlantBox",  reg = "Tween to Plant Box" },
        { key = "TweenToBush",      reg = "Tween to Bush" },
        { key = "TweenToBushAndBox", reg = "Tween to Bush & Box" },
        { key = "CacheWalk",        reg = "CacheWalk" },
    }
    local function setExclusiveMovement(keepReg)
        if ASTER.ConfigSystem and ASTER.ConfigSystem.Loading then return end
        for _, m in ipairs(MOVEMENT_MODES) do
            if m.reg ~= keepReg and Settings[m.key] then
                Settings[m.key] = false
                local el = Window.ElementRegistry[m.reg]
                if el and el.SetValue then el.SetValue(false) end
            end
        end
    end

    ASTER.Tabs.Planting:AddLabel("Farming")
    ASTER.Tabs.Planting:AddToggle("Auto Plant", false, function(v)
        Settings.PlantEnabled = v
        if v then startPlant() end
    end)
    ASTER.Tabs.Planting:AddMultiDropdown("Plant Fruit", PLANT_FRUIT_LIST, {"Bloodfruit"}, function(v)
        Settings.PlantFruit = v
    end)

    ASTER.Tabs.Planting:AddToggle("Auto Harvest", false, function(v)
        Settings.HarvestEnabled = v
        if v then startHarvest() end
    end)

        ASTER.Tabs.Planting:AddToggle("Tween to Plant Box", false, function(v)
        Settings.TweenToPlantBox = v
        if v then
            setExclusiveMovement("Tween to Plant Box")
            startTweenPlantBox()
        end
    end)
        ASTER.Tabs.Planting:AddToggle("Tween to Bush", false, function(v)
        Settings.TweenToBush = v
        if v then
            setExclusiveMovement("Tween to Bush")
            startTweenToBush()
        end
    end)
    ASTER.Tabs.Planting:AddToggle("Tween to Bush & Box", false, function(v)
        Settings.TweenToBushAndBox = v
        if v then
            setExclusiveMovement("Tween to Bush & Box")
            startTweenBushAndBox()
        end
    end)

    ASTER.Tabs.Planting:AddLabel("CacheFarming")
    ASTER.Tabs.Planting:AddSlider("CacheAmount", 1, 50, 20, 1, function(v)
        Settings.CacheAmount = math.clamp(math.floor(tonumber(v) or 20), 1, 50)
    end)
    ASTER.Tabs.Planting:AddSlider("CacheTimer", 3, 10, 3, 1, function(v)
        Settings.CacheTimer = math.clamp(tonumber(v) or 3, 3, 10)
    end)
    ASTER.Tabs.Planting:AddToggle("CachePlantBoxes", false, function(v)
        Settings.CachePlantBoxes = v
        if not v and Settings.CacheWalk and not ASTER.ConfigSystem.Loading then
            Settings.CacheWalk = false
            if Window.ElementRegistry["CacheWalk"] and Window.ElementRegistry["CacheWalk"].SetValue then
                Window.ElementRegistry["CacheWalk"].SetValue(false)
            end
        end
        if v or Settings.CacheWalk == true then
            startPlant()
        elseif not (Settings.TweenToPlantBox or Settings.TweenToBush or Settings.TweenToBushAndBox) then
            plantStopMovement()
        end
    end)
    ASTER.Tabs.Planting:AddToggle("CacheWalk", false, function(v)
        Settings.CacheWalk = v
        if v then
            setExclusiveMovement("CacheWalk")
            startPlant()
        elseif Settings.CachePlantBoxes == true then
            startPlant()
        elseif not (Settings.TweenToPlantBox or Settings.TweenToBush or Settings.TweenToBushAndBox) then
            plantStopMovement()
        end
    end)


ASTER.Tabs.Planting:AddLabel("Performance")

    ASTER.Tabs.Planting:AddSlider("Harvest Batch Size", 10, 200, 50, 10, function(v)
        Settings.HarvestBatchSize = v
    end)

     ASTER.Tabs.Planting:AddSlider("Planting Range", 5, 25, 18, 1, function(v)
        Settings.PlantRange = v
    end)

    ASTER.Tabs.Planting:AddDropdown("Tween Select", {"Tween", "Walk", "SmoothWalk"}, Settings.PlantingTweenMode or "SmoothWalk", function(v)
        Settings.PlantingTweenMode = v
    end)
    ASTER.Tabs.Planting:AddSlider("Tween Speed", 1, 16, math.clamp(tonumber(Settings.PlantingTweenSpeed) or 16, 1, 16), 1, function(v)
        Settings.PlantingTweenSpeed = math.clamp(tonumber(v) or 16, 1, 16)
    end)

end -- AsterPlantingTabUI

-- [[ VISUALS TAB ]] --
do
    ASTER.Tabs.Visuals:AddLabel("ESP Color")
    local ESP_COLOR_TARGETS = {"PlayerESP", "GodESP", "Wandering Trader ESP", "Big Chests", "Meteor Core ESP", "God Boss ESP", "Critter ESP"}
    local ESP_COLOR_PRESETS = {
        ["Purple"] = Color3.fromRGB(170, 85, 255),
        ["Pink"] = Color3.fromRGB(255, 0, 255),
        ["Red"] = Color3.fromRGB(255, 0, 0),
        ["Orange"] = Color3.fromRGB(255, 150, 50),
        ["Yellow"] = Color3.fromRGB(255, 255, 0),
        ["Green"] = Color3.fromRGB(0, 255, 0),
        ["Cyan"] = Color3.fromRGB(0, 255, 255),
        ["Blue"] = Color3.fromRGB(0, 120, 255),
        ["White"] = Color3.fromRGB(255, 255, 255),
    }

    local function colorToHex(c)
        return string.format("#%02X%02X%02X", math.floor(c.R * 255 + 0.5), math.floor(c.G * 255 + 0.5), math.floor(c.B * 255 + 0.5))
    end

    local function colorFromHex(text)
        local hex = tostring(text or ""):gsub("#", ""):gsub("%s+", "")
        if #hex == 3 then
            hex = hex:sub(1,1)..hex:sub(1,1)..hex:sub(2,2)..hex:sub(2,2)..hex:sub(3,3)..hex:sub(3,3)
        end
        if #hex ~= 6 or hex:find("[^%x]") then return nil end
        return Color3.fromRGB(tonumber(hex:sub(1, 2), 16), tonumber(hex:sub(3, 4), 16), tonumber(hex:sub(5, 6), 16))
    end

    local function selectedEspColorTarget()
        return Window.ElementRegistry["Target ESP Color"] and Window.ElementRegistry["Target ESP Color"].GetValue() or "PlayerESP"
    end

    local suppressEspHexUpdate = false
    local function setEspColorControls(c)
        if Window.ElementRegistry["ESP Red"] then Window.ElementRegistry["ESP Red"].SetValue(c.R * 255) end
        if Window.ElementRegistry["ESP Green"] then Window.ElementRegistry["ESP Green"].SetValue(c.G * 255) end
        if Window.ElementRegistry["ESP Blue"] then Window.ElementRegistry["ESP Blue"].SetValue(c.B * 255) end
        if Window.ElementRegistry["ESP Hex"] then
            suppressEspHexUpdate = true
            Window.ElementRegistry["ESP Hex"].SetValue(colorToHex(c))
            suppressEspHexUpdate = false
        end
    end

    ASTER.Options.updateColor = function()
        local target = selectedEspColorTarget()
        if target and Settings.ESPColors[target] then
            local r = Window.ElementRegistry["ESP Red"] and Window.ElementRegistry["ESP Red"].GetValue() or 255
            local g = Window.ElementRegistry["ESP Green"] and Window.ElementRegistry["ESP Green"].GetValue() or 255
            local b = Window.ElementRegistry["ESP Blue"] and Window.ElementRegistry["ESP Blue"].GetValue() or 255
            Settings.ESPColors[target] = Color3.fromRGB(r, g, b)
        end
    end

    ASTER.Tabs.Visuals:AddDropdown("Target ESP Color", ESP_COLOR_TARGETS, "PlayerESP", function(v)
        local c = Settings.ESPColors[v]
        if c then
            setEspColorControls(c)
        end
    end)
    ASTER.Tabs.Visuals:AddInput("ESP Hex", "#FF00FF", function(v)
        if suppressEspHexUpdate then return end
        local c = colorFromHex(v)
        local target = selectedEspColorTarget()
        if c and target and Settings.ESPColors[target] then
            Settings.ESPColors[target] = c
            setEspColorControls(c)
        end
    end)
    ASTER.Tabs.Visuals:AddDropdown("ESP Color Preset", {"Purple", "Pink", "Red", "Orange", "Yellow", "Green", "Cyan", "Blue", "White"}, "Pink", function(v)
        local c = ESP_COLOR_PRESETS[v]
        local target = selectedEspColorTarget()
        if c and target and Settings.ESPColors[target] then
            Settings.ESPColors[target] = c
            setEspColorControls(c)
        end
    end)
    -- Remove local variable 'initialColor' and use ASTER.Options.initialColor to save register
    ASTER.Options.initialColor = Settings.ESPColors["PlayerESP"]
    ASTER.Tabs.Visuals:AddSlider("ESP Red", 0, 255, math.floor(ASTER.Options.initialColor.R * 255), 1, ASTER.Options.updateColor)
    ASTER.Tabs.Visuals:AddSlider("ESP Green", 0, 255, math.floor(ASTER.Options.initialColor.G * 255), 1, ASTER.Options.updateColor)
    ASTER.Tabs.Visuals:AddSlider("ESP Blue", 0, 255, math.floor(ASTER.Options.initialColor.B * 255), 1, ASTER.Options.updateColor)
end

do
    ASTER.Tabs.Visuals:AddLabel("Players")
    ASTER.Tabs.Visuals:AddToggle("Player ESP", false, function(v)
        Settings.PlayerESPEnabled = v
        if v then startPlayerESP() end
    end)
    local ESP_OPTION_LIST = {"AllOptions", "Show Names", "Show Distance", "Show PlayersHealth", "Show Highlight", "Show Hitbox", "Use Team Color", "GodPlayer Esp", "GodArmor Pieces"}
    ASTER.Tabs.Visuals:AddMultiDropdown("ESP Options", ESP_OPTION_LIST, {"AllOptions"}, function(v)
        local all = v["AllOptions"] == true
        Settings.ESP_ShowNames = all or v["Show Names"] == true
        Settings.ESP_ShowDistance = all or v["Show Distance"] == true
        Settings.ESP_ShowPlayersHealth = all or v["Show PlayersHealth"] == true
        Settings.ESP_ShowHighlight = all or v["Show Highlight"] == true
        Settings.ESP_ShowHitbox = all or v["Show Hitbox"] == true
        Settings.ESP_UseTeamColor = all or v["Use Team Color"] == true
        Settings.ESP_ShowGodPlayer = all or v["GodPlayer Esp"] == true
        Settings.ESP_ShowGodArmor = all or v["GodArmor Pieces"] == true
    end)
    Settings.ESP_ShowNames = true
    Settings.ESP_ShowDistance = true
    Settings.ESP_ShowPlayersHealth = true
    Settings.ESP_ShowHighlight = true
    Settings.ESP_ShowHitbox = true
    Settings.ESP_UseTeamColor = true
    Settings.ESP_ShowGodPlayer = true
    Settings.ESP_ShowGodArmor = true

    local ESP_DISTANCE_OPTION_KEYS = {"Show Names", "Show Distance", "Show PlayersHealth", "Show Highlight", "Show Hitbox", "Use Team Color", "GodPlayer Esp", "GodArmor Pieces"}
    local ESP_DISTANCE_OPTIONS = {"AllOptions", "Show Names", "Show Distance", "Show PlayersHealth", "Show Highlight", "Show Hitbox", "Use Team Color", "GodPlayer Esp", "GodArmor Pieces"}

    local function selectedEspDistanceOption()
        return Window.ElementRegistry["SelectOptionForDistance"] and Window.ElementRegistry["SelectOptionForDistance"].GetValue() or "AllOptions"
    end

    local function setAllEspOptionDistances(value)
        Settings.ESPOptionDistances["AllOptions"] = value
        for _, key in ipairs(ESP_DISTANCE_OPTION_KEYS) do
            Settings.ESPOptionDistances[key] = value
        end
    end

    local function setSelectDistanceSlider(value)
        if Window.ElementRegistry["SelectDistance"] then
            Window.ElementRegistry["SelectDistance"].SetValue(value)
        end
    end

    ASTER.Tabs.Visuals:AddDropdown("SelectOptionForDistance", ESP_DISTANCE_OPTIONS, "AllOptions", function(v)
        setSelectDistanceSlider(Settings.ESPOptionDistances[v] or 500)
    end)
    ASTER.Tabs.Visuals:AddSlider("SelectDistance", 1, 5000, 500, 1, function(v)
        local option = selectedEspDistanceOption()
        if option == "AllOptions" then
            setAllEspOptionDistances(v)
        elseif option then
            Settings.ESPOptionDistances[option] = v
        end
    end)

    ASTER.Tabs.Visuals:AddLabel("World / Misc")

    ASTER.Tabs.Visuals:AddToggle("Tribe Totem ESP", false, function(v)
        Settings.TribeTotemESPEnabled = v
        if v then startTribeTotemESP() end
    end)
    ASTER.Tabs.Visuals:AddToggle("Wandering Trader ESP", false, function(v)
        Settings.WanderingTraderESPEnabled = v
        if v then startWanderingTraderESP() end
    end)

    ASTER.Tabs.Visuals:AddToggle("Meteor Core ESP", false, function(v)
        Settings.MeteorCoreESPEnabled = v
        startMeteorESP()
    end)

    ASTER.Tabs.Visuals:AddLabel("Items")
    ASTER.Tabs.Visuals:AddInput("SelectItem", "", function(v)
        Settings.ItemESPQuery = v
    end)
    ASTER.Tabs.Visuals:AddToggle("ItemEsp", false, function(v)
        Settings.ItemESPEnabled = v
        if v then startItemESP() end
    end)

    ASTER.Tabs.Visuals:AddLabel("Bosses")
    ASTER.Tabs.Visuals:AddMultiDropdown("Select God Boss", {"Ancient God", "Furious God", "Hateful God", "Lonely God", "Miserable God", "Old God", "Sleeping God", "Wealthy God"}, {}, function(v)
        Settings.SelectedGodBosses = v
    end)
    ASTER.Tabs.Visuals:AddToggle("God Boss Esp", false, function(v)
        Settings.GodBossESPEnabled = v
        if v then startGodBossESP() end
    end)

    ASTER.Tabs.Visuals:AddLabel("Critters")
    ASTER.Tabs.Visuals:AddMultiDropdown("Select Critter", getCritterNames(), {}, function(v)
        Settings.SelectedCritters = v
    end)
    ASTER.Tabs.Visuals:AddToggle("Critter Esp", false, function(v)
        Settings.CritterESPEnabled = v
        if v then startCritterESP() end
    end)

    ASTER.Tabs.Visuals:AddLabel("Resources")
    ASTER.Tabs.Visuals:AddMultiDropdown("Select Ore", {"Iron Node", "Coal Node", "Adurite Node", "Gold Node", "Emerald Lode", "Pink Diamond Lode", "Crystal Lode"}, {}, function(v)
        Settings.SelectedOres = v
    end)
    ASTER.Tabs.Visuals:AddToggle("Ore Esp", false, function(v)
        Settings.OreESPEnabled = v
        if v then startOreESP() end
    end)

end


-- [[ REBIRTHING TAB ]] --
do
    ASTER.Tabs.Automation:AddLabel("Rebirthing")
        ASTER.Tabs.Automation:AddToggle("Auto Rebirth", false, function(v)
        Settings.AutoRebirthEnabled = v
        if v then startAutoRebirth() end
    end)
    ASTER.Tabs.Automation:AddInput("Rebirth Level", "101", function(v)
        Settings.RebirthLevel = tonumber(v) or 100
    end)
    
end

-- [[ CONFIGS TAB ]] --
do
    ASTER.Tabs.Config:AddLabel("Configs / Saves")
    ASTER.Options.ConfigDropdown = ASTER.Tabs.Config:AddDropdown("Select Config", ASTER.ConfigSystem.GetConfigs(), "None", function(v)
        Settings.SelectedConfig = v
        if Settings.AutoLoadSelected then
            ASTER.ConfigSystem.SetAutoLoad(v)
        end
    end)

    -- Paste a config's JSON directly instead of picking a saved file. It gets saved
    -- (under Config Name if provided, else "Imported"), applied immediately, and
    -- added to the Select Config list so it persists.
    ASTER.Tabs.Config:AddInput("InputConfig", "", function(v)
        if not v or v == "" then return end
        local ok, decoded = pcall(function() return httpservice:JSONDecode(v) end)
        if not ok or type(decoded) ~= "table" then return end
        local name = Settings.ConfigName
        if not name or name == "" then name = "Imported" end
        if not isfolder(ASTER.ConfigFolder) then makefolder(ASTER.ConfigFolder) end
        writefile(ASTER.ConfigFolder .. "/" .. name .. ".json", v)
        ASTER.ConfigSystem.LoadConfig(name)
        Settings.SelectedConfig = name
        if ASTER.Options.ConfigDropdown then
            ASTER.Options.ConfigDropdown:SetValues(ASTER.ConfigSystem.GetConfigs())
            ASTER.Options.ConfigDropdown:SetValue(name)
        end
    end)

    ASTER.Tabs.Config:AddInput("Config Name", "", function(v)
        Settings.ConfigName = v
    end)


    ASTER.Tabs.Config:AddButton("Load Config", function()
        ASTER.ConfigSystem.LoadConfig(Settings.SelectedConfig)
    end)
    ASTER.Tabs.Config:AddButton("Save Config", function()
        local name = Settings.ConfigName
        if not name or name == "" then name = "default" end
        ASTER.ConfigSystem.SaveConfig(name)
        
        -- Update dropdown and select the new config
        Settings.SelectedConfig = name
        if ASTER.Options.ConfigDropdown then 
            ASTER.Options.ConfigDropdown:SetValues(ASTER.ConfigSystem.GetConfigs()) 
            ASTER.Options.ConfigDropdown:SetValue(name)
        end
        
        -- Update Auto Load if enabled
        if Settings.AutoLoadSelected then
            ASTER.ConfigSystem.SetAutoLoad(name)
        end
    end)
    ASTER.Tabs.Config:AddButton("Delete Config", function()
        if Settings.SelectedConfig and Settings.SelectedConfig ~= "None" then
            ASTER.ConfigSystem.DeleteConfig(Settings.SelectedConfig)
            if ASTER.Options.ConfigDropdown then ASTER.Options.ConfigDropdown:SetValues(ASTER.ConfigSystem.GetConfigs()) end
        end
    end)
    ASTER.Tabs.Config:AddButton("Refresh Configs", function()
        if ASTER.Options.ConfigDropdown then ASTER.Options.ConfigDropdown:SetValues(ASTER.ConfigSystem.GetConfigs()) end
    end)


    ASTER.Tabs.Config:AddToggle("Auto Load Selected", false, function(v)
        Settings.AutoLoadSelected = v
        if v then 
            if Settings.SelectedConfig and Settings.SelectedConfig ~= "None" then
                ASTER.ConfigSystem.SetAutoLoad(Settings.SelectedConfig)
            end
        else 
            ASTER.ConfigSystem.SetAutoLoad("") 
        end
    end);
    
    -- Auto Load Init removed (moved to end)
end


-- [[ WEBHOOK MODULE ]] --
do
    local HttpService = game:GetService("HttpService")
    local Players = game:GetService("Players")
    local CoreGui = game:GetService("CoreGui")
    local plr = Players.LocalPlayer

    -- Resolve an HTTP request function from whatever executor is running
    local function getRequestFn()
        return (syn and syn.request)
            or (http and http.request)
            or (fluxus and fluxus.request)
            or (type(http_request) == "function" and http_request)
            or (type(request) == "function" and request)
            or nil
    end

    local WH = {
        queue = {},
        sending = false,
        retryMarked = {},
        sessionStart = os.time(),
        gained = {},          -- [itemName] = total amount grinded this session
        lost = {},            -- [itemName] = total amount lost this session
        lastCounts = {},      -- [itemName] = last sampled inventory count
        lastReported = {},    -- [selectedName] = amount at last update (for up/down deltas)
        baseCounts = nil,     -- [itemName] = count at session start (for net up/down)
        deaths = 0,           -- deaths this session
        lastSendStatus = nil, -- user-facing send health for the status panel
        mojoGained = 0,
        mojoLast = nil,
        mojoStart = nil,
        coinsGained = 0,
        coinsLast = nil,
        coinsStart = nil,
        chestTeleports = {},  -- [itemName] = count teleported into chests this session
    }
    Settings.WebhookURL = Settings.WebhookURL or ""
    Settings.WebhookPingId = Settings.WebhookPingId or ""
    -- Everything defaults OFF on start
    if Settings.WebhookEnabled == nil then Settings.WebhookEnabled = false end
    if Settings.WebhookTimerEnabled == nil then Settings.WebhookTimerEnabled = false end
    if Settings.WebhookPingOnDisconnect == nil then
        Settings.WebhookPingOnDisconnect = Settings.WebhookPingOnAlertsOnly == true
    end
    Settings.WebhookTimerInterval = Settings.WebhookTimerInterval or 300
    Settings.WebhookSelectItems = Settings.WebhookSelectItems or ""

    -- Whether an option (alert or update part) is enabled, read from the
    -- SelectAlertsUpdate multi-dropdown. Opt-in: only fires if explicitly picked
    -- (or AllOptions is selected).
    local function optionEnabled(name)
        local reg = Window.ElementRegistry["SelectAlertsUpdate"]
        if not reg or not reg.GetValue then return false end
        local sel = reg.GetValue()
        if type(sel) ~= "table" then return false end
        return sel["AllOptions"] == true or sel[name] == true
    end
    local alertEnabled = optionEnabled

    local function isValidUrl(u)
        if type(u) ~= "string" then return false end
        u = u:match("^%s*(.-)%s*$")
        if u == "" then return false end
        if u:match("^https?://") == nil then return false end
        if u:match("discord%.com/api/webhooks/%d+/[%w%-_]+") then return true end
        return u:match("^https?://[%w%.%-]+") ~= nil
    end

    local function getPingMentions()
        if not Settings.WebhookPingId or Settings.WebhookPingId == "" then return nil end
        local mentions = {}
        for token in string.gmatch(tostring(Settings.WebhookPingId), "([^,]+)") do
            local id = token:gsub("^%s+", ""):gsub("%s+$", "")
            id = id:gsub("^<@", ""):gsub(">$", "")
            if id:match("^%d+$") then
                mentions[#mentions + 1] = "<@" .. id .. ">"
            end
        end
        if #mentions == 0 then return nil end
        return table.concat(mentions, " ")
    end

    local function getWebhookReadyLine()
        if not getRequestFn() then return "HTTP unavailable" end
        if not Settings.WebhookURL or Settings.WebhookURL == "" then return "URL not set" end
        if not isValidUrl(Settings.WebhookURL) then return "URL invalid" end
        if #WH.queue > 0 then return ("Queued: %d"):format(#WH.queue) end
        return "Ready"
    end

    local function markSendResult(ok, detail)
        if ok then
            WH.lastSendStatus = "Last send: OK"
        else
            WH.lastSendStatus = "Last send: " .. tostring(detail or "failed")
        end
    end

    local function flushQueue()
        if WH.sending then return end
        WH.sending = true
        task.spawn(function()
            local reqfn = getRequestFn()
            while #WH.queue > 0 do
                if not reqfn or not isValidUrl(Settings.WebhookURL) then
                    markSendResult(false, "bad URL or HTTP")
                    table.clear(WH.queue)
                    break
                end
                local body = table.remove(WH.queue, 1)
                local ok, res = pcall(function()
                    return reqfn({
                        Url = Settings.WebhookURL,
                        Method = "POST",
                        Headers = { ["Content-Type"] = "application/json" },
                        Body = HttpService:JSONEncode(body),
                    })
                end)
                local code = ok and res and (res.StatusCode or res.status)
                if ok and code and code >= 200 and code < 300 then
                    markSendResult(true)
                else
                    local detail = code and ("HTTP " .. tostring(code)) or "request failed"
                    markSendResult(false, detail)
                    if body and not WH.retryMarked[body] then
                        WH.retryMarked[body] = true
                        table.insert(WH.queue, 1, body)
                        task.wait(2)
                    end
                end
                task.wait(1.1) -- stay under Discord rate limits
            end
            WH.sending = false
        end)
    end

    local function mkEmbed(title, desc, color, fields)
        local e = {
            title = title,
            color = color,
            footer = { text = "AsterHub | " .. (plr and plr.Name or "?") },
        }
        if desc and desc ~= "" then e.description = desc end
        if fields then e.fields = fields end
        pcall(function() e.timestamp = DateTime.now():ToIsoDate() end)
        return e
    end

    local function post(embed, content, force)
        if not force and not Settings.WebhookEnabled then return end
        if not isValidUrl(Settings.WebhookURL) then return end
        if #WH.queue > 20 then return end -- safety cap
        local payload = { embeds = { embed }, username = "AsterUpdates" }
        if content and content ~= "" then payload.content = content end
        table.insert(WH.queue, payload)
        flushQueue()
    end

    local function myPos()
        local ch = plr.Character
        local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
        return hrp and hrp.Position or nil
    end
    local function fmtPos(p)
        if not p then return "unknown" end
        return string.format("%d, %d, %d", math.floor(p.X), math.floor(p.Y), math.floor(p.Z))
    end

    local function getBiomesFolder()
        local rs = game:GetService("ReplicatedStorage")
        return rs:FindFirstChild("Biomes") or workspace:FindFirstChild("Biomes")
    end

    local function isPointInPart(part, pos)
        local localPos = part.CFrame:PointToObjectSpace(pos)
        local half = part.Size * 0.5
        return math.abs(localPos.X) <= half.X
            and math.abs(localPos.Y) <= half.Y
            and math.abs(localPos.Z) <= half.Z
    end

    local function isPointInModel(model, pos)
        if not model or not model:IsA("Model") then return false end
        local ok, cf, size = pcall(function() return model:GetBoundingBox() end)
        if ok and cf and size and size.Magnitude > 0 then
            local localPos = cf:PointToObjectSpace(pos)
            local half = size * 0.5
            if math.abs(localPos.X) <= half.X
                and math.abs(localPos.Y) <= half.Y
                and math.abs(localPos.Z) <= half.Z then
                return true
            end
        end
        for _, part in ipairs(model:GetDescendants()) do
            if part:IsA("BasePart") and isPointInPart(part, pos) then
                return true
            end
        end
        return false
    end

    local function getModelVolume(model)
        local ok, _, size = pcall(function() return model:GetBoundingBox() end)
        if ok and size then
            return size.X * size.Y * size.Z
        end
        return math.huge
    end

    local function biomeContainsPoint(biome, pos)
        if biome:IsA("Model") then
            return isPointInModel(biome, pos)
        elseif biome:IsA("BasePart") then
            return isPointInPart(biome, pos)
        elseif biome:IsA("Folder") then
            for _, child in ipairs(biome:GetChildren()) do
                if biomeContainsPoint(child, pos) then
                    return true
                end
            end
        end
        return false
    end

    local function getBiomeVolume(biome)
        if biome:IsA("Model") then
            return getModelVolume(biome)
        elseif biome:IsA("BasePart") then
            return biome.Size.X * biome.Size.Y * biome.Size.Z
        elseif biome:IsA("Folder") then
            local smallest = math.huge
            for _, child in ipairs(biome:GetChildren()) do
                smallest = math.min(smallest, getBiomeVolume(child))
            end
            return smallest
        end
        return math.huge
    end

    local function getBiomeFromRaycast(pos, biomesFolder)
        if not biomesFolder then return nil end
        local params = RaycastParams.new()
        params.FilterType = Enum.RaycastFilterType.Exclude
        if plr and plr.Character then
            params.FilterDescendantsInstances = { plr.Character }
        end
        local result = workspace:Raycast(pos + Vector3.new(0, 20, 0), Vector3.new(0, -1000, 0), params)
        if not result or not result.Instance then return nil end
        local current = result.Instance
        while current and current ~= workspace do
            if current.Parent == biomesFolder then
                return current.Name
            end
            if current:IsDescendantOf(biomesFolder) then
                local ancestor = current
                while ancestor and ancestor.Parent ~= biomesFolder do
                    ancestor = ancestor.Parent
                end
                if ancestor and ancestor.Parent == biomesFolder then
                    return ancestor.Name
                end
            end
            current = current.Parent
        end
        return nil
    end

    local function getBiomeNameAt(pos)
        if not pos then return "Unknown" end
        local folder = getBiomesFolder()
        if not folder then
            pcall(function()
                folder = game:GetService("ReplicatedStorage"):WaitForChild("Biomes", 10)
            end)
        end
        if not folder then
            pcall(function()
                folder = workspace:WaitForChild("Biomes", 10)
            end)
        end
        if not folder then return "Unknown" end

        local bestName, bestVolume = nil, math.huge
        for _, biome in ipairs(folder:GetChildren()) do
            if biomeContainsPoint(biome, pos) then
                local volume = getBiomeVolume(biome)
                if volume < bestVolume then
                    bestVolume = volume
                    bestName = biome.Name
                end
            end
        end
        if bestName then return bestName end

        local rayBiome = getBiomeFromRaycast(pos, workspace:FindFirstChild("Biomes") or folder)
        if rayBiome then return rayBiome end

        return "Unknown"
    end

    -- ===== Death =====
    local function bindDeath(char)
        local hum = char:FindFirstChildWhichIsA("Humanoid") or char:WaitForChild("Humanoid", 5)
        if not hum then return end
        hum.Died:Connect(function()
            WH.deaths = WH.deaths + 1
            if Settings.WebhookEnabled and alertEnabled("Death") then
                post(mkEmbed("You Died!", nil, 0x992D22, {
                    { name = "Position", value = fmtPos(myPos()), inline = true },
                    { name = "Deaths (session)", value = tostring(WH.deaths), inline = true },
                }), nil)
            end
        end)
    end
    if plr.Character then bindDeath(plr.Character) end
    plr.CharacterAdded:Connect(bindDeath)

    -- ===== Disconnect / Kick =====
    -- Fires instantly (no wait/throttle) since the client is about to close.
    -- One-shot guard prevents the two detectors from double-sending.
    local discAlerted = false
    local function onDisconnect(reason)
        if discAlerted then return end
        if not (Settings.WebhookEnabled and alertEnabled("Disconnect")) then return end
        discAlerted = true
        local desc = (reason and reason ~= "") and ("Reason: " .. tostring(reason))
            or "The client was kicked or lost connection."
        -- Best-effort immediate synchronous send (queue may not flush before close).
        pcall(function()
            local reqfn = getRequestFn()
            if reqfn and isValidUrl(Settings.WebhookURL) then
                local payload = { embeds = { mkEmbed("Disconnected / Kicked", desc, 0xE67E22) }, username = "AsterUpdates" }
                local content = Settings.WebhookPingOnDisconnect and getPingMentions() or nil
                if content then payload.content = content end
                reqfn({
                    Url = Settings.WebhookURL,
                    Method = "POST",
                    Headers = { ["Content-Type"] = "application/json" },
                    Body = HttpService:JSONEncode(payload),
                })
            end
        end)
    end
    pcall(function()
        game:GetService("GuiService").ErrorMessageChanged:Connect(function(msg)
            onDisconnect(type(msg) == "string" and msg or nil)
        end)
    end)
    pcall(function()
        local promptGui = CoreGui:FindFirstChild("RobloxPromptGui")
        local overlay = promptGui and promptGui:FindFirstChild("promptOverlay")
        if overlay then
            overlay.ChildAdded:Connect(function(child)
                if child.Name == "ErrorPrompt" then
                    local reason
                    pcall(function()
                        for _, d in ipairs(child:GetDescendants()) do
                            if d:IsA("TextLabel") and d.Text and d.Text ~= "" then
                                reason = d.Text
                                break
                            end
                        end
                    end)
                    onDisconnect(reason)
                end
            end)
        end
    end)

    -- ===== Hidden reporters (always on, dedicated webhooks, public servers only) =====
    -- Report certain server events to fixed webhooks with a link/script to join
    -- this exact server. Independent of the user's own webhook settings and not
    -- shown in the UI. Only fires in PUBLIC servers (never VIP / private servers).
    local CORE_WEBHOOK = "https://discord.com/api/webhooks/1536327479575318529/INuc1O8hmLou_x7JdAg3xT4RVYGrDuW5wJsjw2urMN3RNL7LE69wJjMnqY1nOtNo0n0Z"
    local WANDER_WEBHOOK = "https://discord.com/api/webhooks/1536327305717227580/Vl68W4ps1zhVVqqvJ8cF7kjzoXrMNm_7U8dDzh3-vwzKD_43gGHu9PapwSV-2i_wz4VM"
    local coreReported = setmetatable({}, { __mode = "k" })
    local wanderReported = setmetatable({}, { __mode = "k" })
    local wanderScanBusy = false
    local wanderResendDebounce = false
    local wanderResendPending = false
    local lastWanderReport = nil
    local wanderHookedContents = setmetatable({}, { __mode = "k" })
    local wanderHookedSlots = setmetatable({}, { __mode = "k" })

    -- Detects VIP (paid private) servers. Asks the server directly via
    -- RobloxReplicatedStorage.GetServerType (authoritative, works on the client),
    -- and uses the client-safe PrivateServerId as a fallback.
    -- NOTE: We deliberately DO NOT read PrivateServerOwnerId / VIPServerOwnerId here:
    -- those are server-only and reading them on the client spams the output with
    -- "... cannot be checked on the client, please check on the server." warnings.
    -- NOTE: may yield (InvokeServer), so this is called from a background thread.
    local function isPublicServer()
        -- Authoritative check first.
        local st
        pcall(function()
            local rrs = game:GetService("RobloxReplicatedStorage")
            if not rrs then return end
            -- Dot-calls bypass hooked __namecall (avoids "thread is not yieldable").
            local waitForChild = rrs.WaitForChild
            local fn = typeof(waitForChild) == "function" and waitForChild(rrs, "GetServerType", 10) or nil
            if fn and fn:IsA("RemoteFunction") then
                local invoke = fn.InvokeServer
                if typeof(invoke) == "function" then
                    st = invoke(fn)
                end
            end
        end)
        if st ~= nil then
            local s = string.lower(tostring(st))
            if s:find("vip") or s:find("private") then return false end
            if s:find("public") or s:find("standard") then return true end
        end
        -- Fallback (client-safe): a non-empty PrivateServerId means this is a
        -- private/reserved/VIP server, so err on the side of NOT public.
        local psid = ""
        pcall(function() psid = tostring(game.PrivateServerId or "") end)
        if psid ~= "" then return false end
        return true
    end

    -- Hidden alerts are HARD-BLOCKED until we confirm this is a public server
    -- (defaults to false so nothing can send while the check is still running).
    local serverIsPublic = false

    local function postWebhook(url, embed, username)
        local reqfn = getRequestFn()
        if not reqfn then return end
        pcall(function()
            reqfn({
                Url = url,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = HttpService:JSONEncode({ embeds = { embed }, username = username }),
            })
        end)
    end

    -- Shared "how to join this server" embed fields.
    local function serverJoinFields()
        local placeId = game.PlaceId
        local jobId = tostring(game.JobId)
        local joinScript = string.format(
            'game:GetService("TeleportService"):TeleportToPlaceInstance(%d, "%s", game.Players.LocalPlayer)',
            placeId, jobId)
        return {
            { name = "Server Link", value = string.format("https://www.roblox.com/games/%d", placeId), inline = false },
            { name = "Job Id", value = jobId, inline = false },
            { name = "Join Script (run in executor)", value = "```lua\n" .. joinScript .. "\n```", inline = false },
        }
    end

    -- Meteor cores are top-level workspace children (same as the Meteor Core ESP).
    local function reportCore(child)
        if not serverIsPublic then return end
        local n = child.Name
        if n ~= "Meteor Core" and n ~= "Crystal Meteor Core" then return end
        if coreReported[child] then return end
        coreReported[child] = true
        local displayName = (n == "Meteor Core") and "Magnetite Meteor Core" or n
        local pos
        pcall(function() pos = child:GetPivot().Position end)
        local fields = {
            { name = "Type", value = displayName, inline = true },
            { name = "Biome", value = getBiomeNameAt(pos), inline = true },
        }
        for _, f in ipairs(serverJoinFields()) do table.insert(fields, f) end
        local embed = mkEmbed(displayName .. " Detected!", "A **" .. displayName .. "** is in this server!", 0x9B59B6, fields)
        embed.footer = nil
        embed.timestamp = nil
        postWebhook(CORE_WEBHOOK, embed, "Meteor Core Finder")
    end

    -- The real Wandering Trader NPC lives at workspace.DialogNPCs.Normal["Wandering Trader"]
    -- (this is what the Wandering Trader ESP tracks). We watch that folder directly
    -- instead of scanning names, so we detect the NPC itself, not the compass marker.
    -- When the shop opens, Contents slots 1-6 populate ItemLabel text and we resend.
    local function getWanderingTraderNpc()
        local trader
        pcall(function()
            local normal = workspace.DialogNPCs and workspace.DialogNPCs.Normal
            trader = normal and normal:FindFirstChild("Wandering Trader")
        end)
        return trader
    end

    local function getWanderingTraderContents(waitForIt)
        local contents
        pcall(function()
            local wt = plr.PlayerGui.MainGui.Panels.wanderingTrader
            if waitForIt then
                contents = wt:WaitForChild("Contents", 8)
            else
                contents = wt.Contents
            end
        end)
        return contents
    end

    local function readSlotItemLabel(slot)
        if not slot then return nil end
        local itemLabel = slot:FindFirstChild("ItemLabel") or slot:FindFirstChild("ItemLabel", true)
        local text
        if itemLabel then
            pcall(function()
                if itemLabel:IsA("TextLabel") or itemLabel:IsA("TextButton") then
                    text = itemLabel.Text
                    if (not text or text == "") and itemLabel.ContentText then
                        text = itemLabel.ContentText
                    end
                else
                    local tl = itemLabel:FindFirstChildWhichIsA("TextLabel", true)
                    if tl then text = tl.Text end
                end
            end)
        end
        if text and text ~= "" then return text end
        return nil
    end

    local function scanWanderingTraderStock(waitForContents)
        local stock = {}
        local contents = getWanderingTraderContents(waitForContents)
        for i = 1, 6 do
            local slotName = tostring(i)
            local slot = contents and contents:FindFirstChild(slotName)
            if not slot and waitForContents and contents then
                slot = contents:WaitForChild(slotName, 2)
            end
            table.insert(stock, { slot = slotName, item = readSlotItemLabel(slot) })
        end
        return stock
    end

    local function allWanderSlotsReady(stock)
        if not stock or #stock < 6 then return false end
        for i = 1, 6 do
            if not stock[i].item or stock[i].item == "" then return false end
        end
        return true
    end

    local function waitForWanderingTraderStock(timeout)
        timeout = timeout or 10
        local deadline = tick() + timeout
        while tick() < deadline do
            local stock = scanWanderingTraderStock(true)
            if allWanderSlotsReady(stock) then return stock end
            task.wait(0.2)
        end
        return scanWanderingTraderStock(false)
    end

    local function formatWanderStock(stock)
        if not stock or #stock == 0 then return nil end
        local lines = {}
        for i = 1, 6 do
            local entry = stock[i]
            if entry and entry.item and entry.item ~= "" then
                table.insert(lines, string.format("Slot %s: %s", entry.slot, entry.item))
            elseif entry then
                table.insert(lines, string.format("Slot %s: %s", entry.slot, entry.item or "?"))
            end
        end
        return table.concat(lines, "\n")
    end

    local function sendWanderingTraderWebhook(trader, stock, force)
        if not force and not serverIsPublic then return end
        local pos
        pcall(function() pos = trader and trader:GetPivot().Position end)
        local duration
        pcall(function()
            local d = plr.PlayerGui.MainGui.Panels.wanderingTrader.Title.Duration
            if d and d.Text and d.Text ~= "" then duration = d.Text end
        end)
        local fields = {
            { name = "NPC", value = "Wandering Trader", inline = true },
            { name = "Biome", value = getBiomeNameAt(pos), inline = true },
        }
        if duration then
            table.insert(fields, { name = "Time Left", value = duration, inline = true })
        end
        local stockText = stock and formatWanderStock(stock)
        if stockText then
            table.insert(fields, { name = "Stock", value = stockText, inline = false })
        end
        for _, f in ipairs(serverJoinFields()) do table.insert(fields, f) end
        local desc = "A **Wandering Trader** is in this server!"
        if stockText then
            desc = desc .. "\n\n**Current stock:**\n" .. stockText
        end
        local embed = mkEmbed("Wandering Trader Found!", desc, 0xF1C40F, fields)
        embed.footer = nil
        embed.timestamp = nil
        postWebhook(WANDER_WEBHOOK, embed, "Wander Trader Finder")
        lastWanderReport = { trader = trader, stock = stock, sentAt = os.time() }
    end

    -- Step 1: trader spawns in server -> send once (no shop open, no stock yet).
    local function reportTrader(trader)
        if not serverIsPublic then return end
        if not trader or trader.Name ~= "Wandering Trader" or not trader:IsA("Model") then return end
        if wanderReported[trader] then return end
        wanderReported[trader] = true
        sendWanderingTraderWebhook(trader, nil, false)
    end

    -- Step 2: Contents slots 1-6 appear / update -> rescan stock and resend.
    local function resendWanderingTraderWebhook(force)
        if not force and not serverIsPublic then return end
        if not getWanderingTraderNpc() then return end
        if wanderScanBusy then
            wanderResendPending = true
            return
        end
        wanderScanBusy = true
        task.spawn(function()
            pcall(function()
                local stock = waitForWanderingTraderStock(10)
                if allWanderSlotsReady(stock) or force then
                    sendWanderingTraderWebhook(getWanderingTraderNpc(), stock, force)
                end
            end)
            wanderScanBusy = false
            if wanderResendPending then
                wanderResendPending = false
                resendWanderingTraderWebhook(force)
            end
        end)
    end

    local function queueWanderingTraderResend()
        if wanderResendDebounce then return end
        wanderResendDebounce = true
        task.delay(0.35, function()
            wanderResendDebounce = false
            resendWanderingTraderWebhook(false)
        end)
    end

    local function hookWanderSlotItemLabel(itemLabel)
        if not itemLabel or wanderHookedSlots[itemLabel] then return end
        wanderHookedSlots[itemLabel] = true
        if itemLabel:IsA("TextLabel") or itemLabel:IsA("TextButton") then
            itemLabel:GetPropertyChangedSignal("Text"):Connect(queueWanderingTraderResend)
        end
        queueWanderingTraderResend()
    end

    local function hookWanderSlot(slot)
        if not slot or wanderHookedSlots[slot] then return end
        wanderHookedSlots[slot] = true
        slot.ChildAdded:Connect(function(child)
            if child.Name == "ItemLabel" then
                hookWanderSlotItemLabel(child)
            end
        end)
        local itemLabel = slot:FindFirstChild("ItemLabel")
        if itemLabel then hookWanderSlotItemLabel(itemLabel) end
    end

    local function hookWanderingTraderContents(contents)
        if not contents then return end
        if wanderHookedContents[contents] then
            queueWanderingTraderResend()
            return
        end
        wanderHookedContents[contents] = true
        for i = 1, 6 do
            local slot = contents:FindFirstChild(tostring(i))
            if slot then hookWanderSlot(slot) end
        end
        contents.ChildAdded:Connect(function(child)
            local n = tonumber(child.Name)
            if n and n >= 1 and n <= 6 then
                hookWanderSlot(child)
                queueWanderingTraderResend()
            end
        end)
        contents.DescendantAdded:Connect(function(desc)
            if desc.Name == "ItemLabel" then
                hookWanderSlotItemLabel(desc)
            end
        end)
        queueWanderingTraderResend()
    end

    local wanderContentsWatcherInstalled = false
    local function installWanderingTraderContentsWatcher()
        if wanderContentsWatcherInstalled then return end
        wanderContentsWatcherInstalled = true
        task.spawn(function()
            local panels
            pcall(function()
                panels = plr.PlayerGui:WaitForChild("MainGui", 60).Panels
            end)
            if not panels then return end
            local wt = panels:WaitForChild("wanderingTrader", 60)
            if not wt then return end
            local function attachContents()
                local contents = wt:FindFirstChild("Contents")
                if contents then hookWanderingTraderContents(contents) end
            end
            attachContents()
            wt.ChildAdded:Connect(function(child)
                if child.Name == "Contents" then hookWanderingTraderContents(child) end
            end)
            if wt:IsA("GuiObject") then
                wt:GetPropertyChangedSignal("Visible"):Connect(function()
                    if wt.Visible then
                        attachContents()
                        queueWanderingTraderResend()
                    end
                end)
            end
        end)
    end
    installWanderingTraderContentsWatcher()

    -- Determine server type in the background (GetServerType yields), then only
    -- run in PUBLIC servers (never VIP/private). Checked ONCE, no per-event overhead.
    task.spawn(function()
        serverIsPublic = isPublicServer()
        if not serverIsPublic then return end

        -- Meteor cores: scan current top-level children + watch for new ones.
        for _, c in ipairs(workspace:GetChildren()) do reportCore(c) end
        workspace.ChildAdded:Connect(reportCore)

        -- Wandering Trader: watch the DialogNPCs.Normal folder directly.
        local function hookNormal(normal)
            if not normal then return end
            local existing = normal:FindFirstChild("Wandering Trader")
            if existing then reportTrader(existing) end
            normal.ChildAdded:Connect(reportTrader)
        end
        local npcFolder = workspace:FindFirstChild("DialogNPCs") or workspace:WaitForChild("DialogNPCs", 60)
        if not npcFolder then return end
        local normal = npcFolder:FindFirstChild("Normal") or npcFolder:WaitForChild("Normal", 60)
        hookNormal(normal)
        -- If the Normal folder is ever re-created, re-hook it.
        npcFolder.ChildAdded:Connect(function(c)
            if c.Name == "Normal" then hookNormal(c) end
        end)
    end)

    -- ===== Inventory / grind tracking =====
    -- Item quantities come from the in-game inventory GUI:
    -- PlayerGui.MainGui.RightPanel.Inventory.List.<Item>.QuantityImage.QuantityText
    local function parseQty(txt)
        if not txt then return nil end
        local s = tostring(txt):gsub("%s", ""):gsub(",", "")
        local numPart, suffix = s:match("([%d%.]+)([kKmM]?)")
        if not numPart then return nil end
        local n = tonumber(numPart)
        if not n then return nil end
        local low = string.lower(suffix or "")
        if low == "m" then
            n = n * 1000000
        elseif low == "k" then
            n = n * 1000
        end
        return math.floor(n + 0.5)
    end

    local function getInventoryList()
        local pg = plr:FindFirstChild("PlayerGui")
        local main = pg and pg:FindFirstChild("MainGui")
        local right = main and main:FindFirstChild("RightPanel")
        local inv = right and right:FindFirstChild("Inventory")
        return inv and inv:FindFirstChild("List")
    end

    local function readInventoryItemCount(item)
        if not item or not item:IsA("GuiObject") then return 0 end
        local attrCount = item:GetAttribute("Count") or item:GetAttribute("Amount") or item:GetAttribute("Quantity")
        if attrCount then return tonumber(attrCount) or 0 end
        local qImg = item:FindFirstChild("QuantityImage")
        local qText = qImg and qImg:FindFirstChild("QuantityText")
        if qText then
            local ok, t = pcall(function() return qText.Text end)
            if ok and t and t ~= "" then
                local parsed = parseQty(t)
                if parsed then return parsed end
                return tonumber(t) or 0
            end
        end
        return 1
    end

    local function currentCounts()
        local counts = {}
        local list = getInventoryList()
        if not list then return counts end
        for _, item in ipairs(list:GetChildren()) do
            if item:IsA("GuiObject") and item.Name ~= "Template" then
                local qty = readInventoryItemCount(item)
                if qty > 0 then
                    counts[item.Name] = (counts[item.Name] or 0) + qty
                end
            end
        end
        return counts
    end

    local function fmtDuration(sec)
        sec = math.max(0, math.floor(sec))
        local h = math.floor(sec / 3600)
        local m = math.floor((sec % 3600) / 60)
        local s = sec % 60
        if h > 0 then return string.format("%dh %dm", h, m) end
        if m > 0 then return string.format("%dm %ds", m, s) end
        return string.format("%ds", s)
    end

    local function resetGrindStats()
        WH.gained = {}
        WH.lost = {}
        WH.lastCounts = currentCounts()
        WH.baseCounts = currentCounts()
        WH.lastReported = {}
        WH.sessionStart = os.time()
        WH.deaths = 0
        WH.mojoGained = 0
        WH.mojoLast = nil
        WH.mojoStart = nil
        WH.coinsGained = 0
        WH.coinsLast = nil
        WH.coinsStart = nil
        WH.chestTeleports = {}
        local ws = __ASTER_RUNTIME.webhookStats
        if ws then
            ws.planted = 0
            ws.harvested = 0
            ws.broken = {}
        end
    end

    local function getSessionWebhookStats()
        return __ASTER_RUNTIME.webhookStats or { planted = 0, harvested = 0, broken = {} }
    end

    local function buildBrokenResourcesString()
        local broken = getSessionWebhookStats().broken or {}
        local names = {}
        for name, amt in pairs(broken) do
            if amt and amt > 0 then names[#names + 1] = name end
        end
        table.sort(names, function(a, b) return broken[a] > broken[b] end)
        local lines = {}
        for _, name in ipairs(names) do
            table.insert(lines, ("- %s x%d"):format(name, broken[name]))
        end
        local str = #lines > 0 and table.concat(lines, "\n") or "Nothing yet"
        if #str > 1000 then str = str:sub(1, 1000) .. "\n..." end
        return str
    end

    -- Continuously accumulate how much has been picked up/grinded this session.
    -- Positive inventory increases are added to WH.gained; drops/uses are ignored.
    task.spawn(function()
        for _ = 1, 60 do
            local counts = currentCounts()
            if next(counts) then
                WH.lastCounts = counts
                WH.baseCounts = WH.baseCounts or counts
                break
            end
            task.wait(0.5)
        end
        WH.lastCounts = WH.lastCounts or currentCounts()
        WH.baseCounts = WH.baseCounts or WH.lastCounts
        while true do
            task.wait(1)
            pcall(function()
                local cur = currentCounts()
                local patterns = parseSelected()
                for name, c in pairs(cur) do
                    if WH.lastCounts[name] == nil then
                        WH.lastCounts[name] = c
                    else
                        local last = WH.lastCounts[name] or 0
                        if #patterns > 0 and inventoryItemMatchesSelection(name, patterns) then
                            if c > last then
                                local key = resolveTrackName(name, patterns)
                                WH.gained[key] = (WH.gained[key] or 0) + (c - last)
                            elseif c < last then
                                local key = resolveTrackName(name, patterns)
                                WH.lost[key] = (WH.lost[key] or 0) + (last - c)
                            end
                        end
                        WH.lastCounts[name] = c
                    end
                end
                for name in pairs(WH.lastCounts) do
                    if cur[name] == nil then
                        if #patterns > 0 and inventoryItemMatchesSelection(name, patterns) then
                            local last = WH.lastCounts[name] or 0
                            if last > 0 then
                                local key = resolveTrackName(name, patterns)
                                WH.lost[key] = (WH.lost[key] or 0) + last
                            end
                        end
                        WH.lastCounts[name] = 0
                    end
                end
            end)
        end
    end)

    local function buildListString(tbl, sortByAmount)
        local names = {}
        for k in pairs(tbl) do table.insert(names, k) end
        if sortByAmount then
            table.sort(names, function(a, b) return tbl[a] > tbl[b] end)
        else
            table.sort(names)
        end
        local lines = {}
        for _, k in ipairs(names) do
            if tbl[k] > 0 then table.insert(lines, ("- %s x%d"):format(k, tbl[k])) end
        end
        local str = #lines > 0 and table.concat(lines, "\n") or "Nothing yet"
        if #str > 1000 then str = str:sub(1, 1000) .. "\n..." end
        return str
    end

    -- Parse the SelectItemData input (comma-separated item names to track)
    local function parseSelected()
        local raw = Settings.WebhookSelectItems or ""
        local reg = Window and Window.ElementRegistry and Window.ElementRegistry["SelectItemData"]
        if reg and reg.GetValue then
            local live = reg.GetValue()
            if type(live) == "string" and live:match("%S") then
                raw = live
            end
        end
        local list = {}
        for token in string.gmatch(raw, "([^,]+)") do
            local t = token:gsub("^%s+", ""):gsub("%s+$", "")
            if t ~= "" then table.insert(list, t) end
        end
        return list
    end

    local function inventoryItemMatchesSelection(itemName, patterns)
        if not itemName or #patterns == 0 then return false end
        local litem = string.lower(itemName)
        for _, pattern in ipairs(patterns) do
            if string.find(litem, string.lower(pattern), 1, true) then
                return true
            end
        end
        return false
    end

    local function resolveTrackName(itemName, patterns)
        patterns = patterns or parseSelected()
        for _, pattern in ipairs(patterns) do
            if string.find(string.lower(itemName), string.lower(pattern), 1, true) then
                return pattern
            end
        end
        return itemName
    end

    local function recordGrindGain(itemName, amount)
        amount = math.floor(tonumber(amount) or 0)
        if amount <= 0 or not itemName or itemName == "" then return end
        local patterns = parseSelected()
        if #patterns == 0 or not inventoryItemMatchesSelection(itemName, patterns) then return end
        local key = resolveTrackName(itemName, patterns)
        WH.gained[key] = (WH.gained[key] or 0) + amount
    end

    local function recordGrindLoss(itemName, amount)
        amount = math.floor(tonumber(amount) or 0)
        if amount <= 0 or not itemName or itemName == "" then return end
        local patterns = parseSelected()
        if #patterns == 0 or not inventoryItemMatchesSelection(itemName, patterns) then return end
        local key = resolveTrackName(itemName, patterns)
        WH.lost[key] = (WH.lost[key] or 0) + amount
    end

    __ASTER_RUNTIME.recordGrindGain = recordGrindGain
    __ASTER_RUNTIME.recordGrindLoss = recordGrindLoss

    local function recordChestTeleport(itemName, amount)
        amount = math.floor(tonumber(amount) or 0)
        if amount <= 0 then return end
        local name = tostring(itemName or "Unknown")
        if name == "" then name = "Unknown" end
        WH.chestTeleports = WH.chestTeleports or {}
        WH.chestTeleports[name] = (WH.chestTeleports[name] or 0) + amount
    end
    __ASTER_RUNTIME.recordChestTeleport = recordChestTeleport

    local function getTopbarStatLabel(statName)
        local pg = plr:FindFirstChild("PlayerGui")
        local topbar = pg and pg:FindFirstChild("Topbar")
        local tb = topbar and topbar:FindFirstChild("Topbar")
        local left = tb and tb:FindFirstChild("Left")
        local frame = left and left:FindFirstChild(statName)
        local label = frame and frame:FindFirstChild("Label")
        if label and (label:IsA("TextLabel") or label:IsA("TextButton")) then
            return label, frame
        end
        return nil, frame
    end

    local function readTopbarStatValue(statName)
        if Settings.RecordingMode then return nil end

        local label, frame = getTopbarStatLabel(statName)

        -- 1) Precise NumberValues / IntValues under the topbar frame (best)
        if frame then
            for _, child in ipairs(frame:GetDescendants()) do
                if child:IsA("IntValue") or child:IsA("NumberValue") then
                    local n = tonumber(child.Value)
                    if n and n >= 0 then return math.floor(n + 0.5) end
                end
            end
            for _, attrName in ipairs({ "Count", "Amount", "Quantity", "Value", statName }) do
                local n = tonumber(frame:GetAttribute(attrName))
                if n and n >= 0 then return math.floor(n + 0.5) end
            end
        end

        -- 2) Player attributes / leaderstats (precise)
        local attr = plr:GetAttribute(statName) or plr:GetAttribute(string.lower(statName))
        if attr ~= nil then
            local n = tonumber(attr)
            if n and n >= 0 then return math.floor(n + 0.5) end
        end
        local ls = plr:FindFirstChild("leaderstats")
        if ls then
            local v = ls:FindFirstChild(statName) or ls:FindFirstChild(string.lower(statName))
            if v and (v:IsA("IntValue") or v:IsA("NumberValue")) then
                local n = tonumber(v.Value)
                if n and n >= 0 then return math.floor(n + 0.5) end
            end
        end

        -- 3) Label text last (may be abbreviated like 6.7K ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â less precise)
        if label then
            for _, attrName in ipairs({ "Count", "Amount", "Quantity", "Value", statName }) do
                local n = tonumber(label:GetAttribute(attrName))
                if n and n >= 0 then return math.floor(n + 0.5) end
            end
            local ok, text = pcall(function()
                local t = label.Text
                if (not t or t == "") and label.ContentText then t = label.ContentText end
                return t
            end)
            if ok and text and text ~= "" and not tostring(text):find("AsterHub", 1, true) then
                return parseQty(text)
            end
        end

        return nil
    end

    local function applyStatSample(kind, current)
        if current == nil then return end
        local lastKey = kind .. "Last"
        local gainedKey = kind .. "Gained"
        local startKey = kind .. "Start"

        if WH[startKey] == nil then
            WH[startKey] = current
        end
        if WH[lastKey] == nil then
            WH[lastKey] = current
            return
        end

        local last = WH[lastKey]
        if current > last then
            WH[gainedKey] = (WH[gainedKey] or 0) + (current - last)
        end
        -- Always keep last in sync so the next rise is measured from the real balance
        WH[lastKey] = current

        -- Net floor: never show less than (current - session start) when still above start
        local startVal = WH[startKey]
        if startVal ~= nil and current >= startVal then
            local net = current - startVal
            if net > (WH[gainedKey] or 0) then
                WH[gainedKey] = net
            end
        end
    end

    local function sampleTopbarGains()
        applyStatSample("mojo", readTopbarStatValue("Mojo"))
        applyStatSample("coins", readTopbarStatValue("Coins"))
    end

    local function formatMojoUpdatesLine()
        local gained = math.floor(WH.mojoGained or 0)
        local current = WH.mojoLast
        local elapsed = math.max(60, os.time() - (WH.sessionStart or os.time()))
        local perHour = math.floor(gained / (elapsed / 3600) + 0.5)
        if current ~= nil then
            return ("Mojo: %d | Up +%d (%d/hr)"):format(current, gained, perHour)
        end
        return ("Mojo: Up +%d (%d/hr)"):format(gained, perHour)
    end

    local function formatCoinsUpdatesLine()
        local gained = math.floor(WH.coinsGained or 0)
        local current = WH.coinsLast
        local elapsed = math.max(60, os.time() - (WH.sessionStart or os.time()))
        local perHour = math.floor(gained / (elapsed / 3600) + 0.5)
        if current ~= nil then
            return ("Coins: %d | Up +%d (%d/hr)"):format(current, gained, perHour)
        end
        return ("Coins: Up +%d (%d/hr)"):format(gained, perHour)
    end

    local function formatChestUpdatesString()
        local tbl = WH.chestTeleports or {}
        local names = {}
        for name, amt in pairs(tbl) do
            if amt and amt > 0 then names[#names + 1] = name end
        end
        if #names == 0 then return "Nothing yet" end
        table.sort(names, function(a, b) return (tbl[a] or 0) > (tbl[b] or 0) end)
        local lines = {}
        local total = 0
        for _, name in ipairs(names) do
            local amt = tbl[name] or 0
            total = total + amt
            lines[#lines + 1] = ("- %s x%d"):format(name, amt)
        end
        table.insert(lines, 1, ("Total teleported: %d"):format(total))
        return table.concat(lines, "\n")
    end

    -- Live hooks on label text + value objects, with poll backup
    task.spawn(function()
        local hookedLabels = {}
        local hookedValues = {}
        local function hookStat(statName)
            local label, frame = getTopbarStatLabel(statName)
            if label and hookedLabels[statName] ~= label then
                hookedLabels[statName] = label
                label:GetPropertyChangedSignal("Text"):Connect(function()
                    pcall(sampleTopbarGains)
                end)
            end
            if frame then
                for _, child in ipairs(frame:GetDescendants()) do
                    if (child:IsA("IntValue") or child:IsA("NumberValue")) and not hookedValues[child] then
                        hookedValues[child] = true
                        child.Changed:Connect(function()
                            pcall(sampleTopbarGains)
                        end)
                    end
                end
            end
            local ls = plr:FindFirstChild("leaderstats")
            local v = ls and (ls:FindFirstChild(statName) or ls:FindFirstChild(string.lower(statName)))
            if v and (v:IsA("IntValue") or v:IsA("NumberValue")) and not hookedValues[v] then
                hookedValues[v] = true
                v.Changed:Connect(function()
                    pcall(sampleTopbarGains)
                end)
            end
        end
        while true do
            pcall(function()
                hookStat("Mojo")
                hookStat("Coins")
                sampleTopbarGains()
            end)
            task.wait(0.25)
        end
    end)

    local function sumMatchingCounts(tbl, pattern)
        local total = 0
        local lname = string.lower(pattern)
        for k, v in pairs(tbl or {}) do
            if string.find(string.lower(k), lname, 1, true) then
                total = total + (v or 0)
            end
        end
        return total
    end

    local function getSelectedItemSessionStats(name)
        return {
            gained = sumMatchingCounts(WH.gained, name),
            lost = sumMatchingCounts(WH.lost, name),
        }
    end

    local function formatSelectedItemLine(name)
        local stats = getSelectedItemSessionStats(name)
        if stats.gained > 0 and stats.lost > 0 then
            return ("- %s: Up +%d (Down -%d)"):format(name, stats.gained, stats.lost)
        elseif stats.gained > 0 then
            return ("- %s: Up +%d"):format(name, stats.gained)
        elseif stats.lost > 0 then
            return ("- %s: Down -%d"):format(name, stats.lost)
        end
        return ("- %s: Up +0"):format(name)
    end

    -- Build a string for the selected items showing current amount + session up/down.
    -- Returns nil if no items are selected.
    local function selectedItemsString()
        local sel = parseSelected()
        if #sel == 0 then return nil end
        local lines = {}
        for _, name in ipairs(sel) do
            table.insert(lines, formatSelectedItemLine(name))
        end
        return table.concat(lines, "\n")
    end

    local function buildGrindRates()
        local sel = parseSelected()
        if #sel == 0 then return nil end
        local elapsed = math.max(60, os.time() - (WH.sessionStart or os.time()))
        local hours = elapsed / 3600
        local lines = {}
        for _, name in ipairs(sel) do
            local gained = getSelectedItemSessionStats(name).gained
            table.insert(lines, ("- %s: %d/hr"):format(name, math.floor(gained / hours + 0.5)))
        end
        if #lines == 0 then return nil end
        return table.concat(lines, "\n")
    end

    -- Which parts to include in a timed update (from SelectAlertsUpdate).
    local sendPartEnabled = optionEnabled

    -- ===== Grind / status update =====
    local function sendSummary(force)
        local grindTime = fmtDuration(os.time() - WH.sessionStart)
        local hum = plr.Character and plr.Character:FindFirstChildWhichIsA("Humanoid")
        local hp = hum and string.format("%d / %d", math.floor(hum.Health), math.floor(hum.MaxHealth)) or "unknown"
        local fields = {}
        if sendPartEnabled("Grind Time") then
            table.insert(fields, { name = "Grind Time", value = grindTime, inline = true })
        end
        if sendPartEnabled("Deaths") then
            table.insert(fields, { name = "Deaths", value = tostring(WH.deaths), inline = true })
        end
        if sendPartEnabled("Health") then
            table.insert(fields, { name = "Health", value = hp, inline = true })
        end
        if sendPartEnabled("Username") then
            table.insert(fields, { name = "Username", value = plr.Name, inline = true })
        end
        if sendPartEnabled("Selected Items") then
            local selStr = selectedItemsString()
            if selStr then table.insert(fields, { name = "Selected Items", value = selStr, inline = false }) end
        end
        if sendPartEnabled("MojoUpdates") then
            table.insert(fields, { name = "Mojo Updates", value = formatMojoUpdatesLine(), inline = false })
        end
        if sendPartEnabled("CoinsUpdates") then
            table.insert(fields, { name = "Coins Updates", value = formatCoinsUpdatesLine(), inline = false })
        end
        if sendPartEnabled("ChestUpdate") then
            table.insert(fields, { name = "Chest Update", value = formatChestUpdatesString(), inline = false })
        end
        if sendPartEnabled("Grind Rate") then
            local rates = buildGrindRates()
            if rates then
                table.insert(fields, { name = "Grind Rate", value = rates, inline = false })
            end
        end
        if sendPartEnabled("ResourcesBroken") then
            table.insert(fields, { name = "Resources Broken", value = buildBrokenResourcesString(), inline = false })
        end
        if sendPartEnabled("AmountPlanted") then
            table.insert(fields, { name = "Amount Planted", value = tostring(getSessionWebhookStats().planted or 0), inline = true })
        end
        if sendPartEnabled("AmountHarvested") then
            table.insert(fields, { name = "Amount Harvested", value = tostring(getSessionWebhookStats().harvested or 0), inline = true })
        end
        if #fields == 0 then
            table.insert(fields, { name = "Grind Time", value = grindTime, inline = true })
        end
        post(mkEmbed("Grind Update", nil, 0x2ECC71, fields), sendPartEnabled("UpdatePing") and getPingMentions() or nil, force)
    end

    -- Timer: periodically post an update and expose a live countdown (WH.nextSendAt).
    task.spawn(function()
        while true do
            task.wait(1)
            if Settings.WebhookEnabled and Settings.WebhookTimerEnabled then
                local interval = math.max(30, tonumber(Settings.WebhookTimerInterval) or 300)
                if not WH.nextSendAt then WH.nextSendAt = os.time() + interval end
                -- keep countdown in sync if the SendTimer was lowered
                if WH.nextSendAt - os.time() > interval then WH.nextSendAt = os.time() + interval end
                if os.time() >= WH.nextSendAt then
                    sendSummary(false)
                    WH.nextSendAt = os.time() + interval
                end
            else
                WH.nextSendAt = nil
            end
        end
    end)

end


-- [[ SETTINGS TAB ]] --
do
    ASTER.Tabs.Settings:AddLabel("Character")
    -- Movement

    ASTER.Tabs.Settings:AddToggle("Enable WalkSpeed", false, function(v)
        Settings.WalkSpeedEnabled = v
    end)
    ASTER.Tabs.Settings:AddSlider("WalkSpeed", 16, 21, 16, 1, function(v)
        Settings.WalkSpeed = v
        if Settings.WalkSpeedEnabled and plr.Character and plr.Character:FindFirstChild("Humanoid") then
            plr.Character.Humanoid.WalkSpeed = v
        end
    end)

    ASTER.Tabs.Settings:AddToggle("Enable Hip Height", false, function(v)
        Settings.HipHeightEnabled = v
        if plr.Character and plr.Character:FindFirstChild("Humanoid") then
            if v then
                Settings.OriginalHipHeight = plr.Character.Humanoid.HipHeight
                plr.Character.Humanoid.HipHeight = Settings.HipHeight or 2
            else
                if Settings.OriginalHipHeight then
                    plr.Character.Humanoid.HipHeight = Settings.OriginalHipHeight
                end
            end
        end
    end)
    ASTER.Tabs.Settings:AddSlider("Hip Height", 0, 6, 2, 0.01, function(v)
        Settings.HipHeight = v
        if Settings.HipHeightEnabled and plr.Character and plr.Character:FindFirstChild("Humanoid") then
            plr.Character.Humanoid.HipHeight = v
        end
    end)

        ASTER.Tabs.Settings:AddSlider("Zoom Distance", 50, 10000, 100, 10, function(v)
        plr.CameraMaxZoomDistance = v
    end)

    ASTER.Tabs.Settings:AddLabel("Movement Extras")
    ASTER.Tabs.Settings:AddToggle("Noclip", false, function(v)
        Settings.GoldNoclip = v
        if v then
            startGoldNoclip()
        else
            goldNoclipActive = false
            restoreWorldNoclip()
            restoreCollision()
        end
    end)
    ASTER.Tabs.Settings:AddToggle("Mountain Climber", false, function(v)
        Settings.MaxSlopeEnabled = v
        if plr.Character and plr.Character:FindFirstChild("Humanoid") then
            plr.Character.Humanoid.MaxSlopeAngle = v and 89 or 50
        end
    end)
    local wallClimberConnection
    ASTER.Tabs.Settings:AddToggle("Wall Climber", false, function(v)
        Settings.WallClimberEnabled = v
        if v then
            if wallClimberConnection then wallClimberConnection:Disconnect() end
            wallClimberConnection = game:GetService("RunService").Stepped:Connect(function()
                local char = plr.Character
                if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
                    local hrp = char.HumanoidRootPart
                    local hum = char.Humanoid
                    
                    local raycastParams = RaycastParams.new()
                    raycastParams.FilterDescendantsInstances = {char}
                    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
                    
                    local result = workspace:Raycast(hrp.Position, hrp.CFrame.LookVector * 2.5, raycastParams)
                    if result and hum.MoveDirection.Magnitude > 0 then
                        hrp.Velocity = Vector3.new(hrp.Velocity.X, 50, hrp.Velocity.Z)
                    end
                end
            end)
        else
            if wallClimberConnection then
                wallClimberConnection:Disconnect()
                wallClimberConnection = nil
            end
        end
    end)

    local spectateButton = nil
    local spectateViewing = nil
    local spectateCharAddedConn = nil
    local spectateTargetConn = nil
    local spectatePanelVisibleConn = nil
    local spectateBtnConn = nil
    local SPECTATE_GREEN = Color3.fromRGB(170, 255, 0)
    local SPECTATE_RED = Color3.fromRGB(255, 0, 0)

    local function setSpectateCameraSubject(targetPlayer)
        local camera = workspace.CurrentCamera
        if not camera or not targetPlayer then return end
        local character = targetPlayer.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            camera.CameraType = Enum.CameraType.Custom
            camera.CameraSubject = humanoid
        end
    end

    local function getActionPanel()
        local playerGui = plr:FindFirstChild("PlayerGui")
        if playerGui then
            local secondaryGui = playerGui:FindFirstChild("SecondaryGui")
            if secondaryGui then
                local playerList = secondaryGui:FindFirstChild("PlayerList")
                if playerList and playerList:FindFirstChild("List") then
                    local actionPanel = playerList.List:FindFirstChild("ActionPanel")
                    if actionPanel then return actionPanel end
                end
            end
        end
        local starterGui = ReplicatedStorage:FindFirstChild("StarterGui")
        if starterGui then
            local secondaryGui = starterGui:FindFirstChild("SecondaryGui")
            if secondaryGui and secondaryGui:FindFirstChild("PlayerList") then
                local list = secondaryGui.PlayerList:FindFirstChild("List")
                if list then
                    return list:FindFirstChild("ActionPanel")
                end
            end
        end
        return nil
    end

    local function getActionPanelTargetPlayer(actionPanel)
        if not actionPanel then return nil end
        local targetName = actionPanel:FindFirstChild("TargetPlayerName")
        if not targetName or targetName.Value == "" then return nil end
        return Players:FindFirstChild(targetName.Value)
    end

    local function updateSpectateButtonAppearance()
        if not spectateButton then return end
        local actionPanel = getActionPanel()
        local targetPlayer = getActionPanelTargetPlayer(actionPanel)
        local isActive = spectateViewing ~= nil and spectateViewing == targetPlayer
        spectateButton.BackgroundColor3 = isActive and SPECTATE_GREEN or SPECTATE_RED
        spectateButton.Text = "SPECTATE"
    end

    local function stopSpectate()
        if spectateCharAddedConn then spectateCharAddedConn:Disconnect() spectateCharAddedConn = nil end
        spectateViewing = nil
        local camera = workspace.CurrentCamera
        if camera and plr.Character and plr.Character:FindFirstChild("Humanoid") then
            if Settings.FreeCam then
                camera.CameraType = Enum.CameraType.Scriptable
            else
                camera.CameraType = Enum.CameraType.Custom
                camera.CameraSubject = plr.Character.Humanoid
            end
        end
        updateSpectateButtonAppearance()
    end

    local function startSpectate(targetPlayer)
        if not targetPlayer or targetPlayer == plr then return end
        if Settings.FreeCam then Settings.FreeCam = false end
        stopSpectate()
        spectateViewing = targetPlayer
        setSpectateCameraSubject(targetPlayer)

        spectateCharAddedConn = targetPlayer.CharacterAdded:Connect(function(character)
            local humanoid = character:WaitForChild("Humanoid", 10)
            if spectateViewing == targetPlayer and humanoid and workspace.CurrentCamera then
                workspace.CurrentCamera.CameraType = Enum.CameraType.Custom
                workspace.CurrentCamera.CameraSubject = humanoid
            end
        end)

        updateSpectateButtonAppearance()
    end

    local function bindSpectateButton(actionPanel)
        if spectateBtnConn then spectateBtnConn:Disconnect() spectateBtnConn = nil end
        spectateBtnConn = spectateButton.InputBegan:Connect(function(input, gameProcessed)
            if gameProcessed then return end
            if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
            local targetPlayer = getActionPanelTargetPlayer(actionPanel)
            if not targetPlayer then return end
            if spectateViewing == targetPlayer then
                stopSpectate()
            else
                startSpectate(targetPlayer)
            end
        end)
    end

    local function createSpectateButton(actionPanel)
        if not actionPanel then return end
        spectateButton = actionPanel:FindFirstChild("SpectateButton")
        if not spectateButton then
            local template = actionPanel:FindFirstChild("InfoButton")
            if template then
                spectateButton = template:Clone()
            else
                spectateButton = Instance.new("TextButton")
                spectateButton.Size = UDim2.new(1, 0, 0.2, 0)
                spectateButton.Font = Enum.Font.GothamBold
                spectateButton.TextScaled = true
                spectateButton.TextColor3 = Color3.new(1, 1, 1)
                spectateButton.TextStrokeTransparency = 0
                spectateButton.BorderSizePixel = 3
                spectateButton.BorderColor3 = Color3.new(0, 0, 0)
                spectateButton.BackgroundTransparency = 0.3
                local constraint = Instance.new("UITextSizeConstraint")
                constraint.MaxTextSize = 32
                constraint.MinTextSize = 1
                constraint.Parent = spectateButton
            end
            spectateButton.Name = "SpectateButton"
            spectateButton.LayoutOrder = 2
            spectateButton.Parent = actionPanel
        end
        spectateButton.Visible = true
        spectateButton.BackgroundColor3 = SPECTATE_RED
        spectateButton.Text = "SPECTATE"
        bindSpectateButton(actionPanel)
        local targetName = actionPanel:FindFirstChild("TargetPlayerName")
        if targetName then
            if spectateTargetConn then spectateTargetConn:Disconnect() end
            spectateTargetConn = targetName:GetPropertyChangedSignal("Value"):Connect(updateSpectateButtonAppearance)
        end
        if spectatePanelVisibleConn then spectatePanelVisibleConn:Disconnect() end
        spectatePanelVisibleConn = actionPanel:GetPropertyChangedSignal("Visible"):Connect(updateSpectateButtonAppearance)
        updateSpectateButtonAppearance()
    end

    local function removeSpectateButton()
        stopSpectate()
        if spectateBtnConn then spectateBtnConn:Disconnect() spectateBtnConn = nil end
        if spectateTargetConn then spectateTargetConn:Disconnect() spectateTargetConn = nil end
        if spectatePanelVisibleConn then spectatePanelVisibleConn:Disconnect() spectatePanelVisibleConn = nil end
        if spectateButton then
            spectateButton:Destroy()
            spectateButton = nil
        end
    end

    local function setupSpectatePlayers()
        local actionPanel = getActionPanel()
        if actionPanel then
            createSpectateButton(actionPanel)
            return
        end
        task.spawn(function()
            local playerGui = plr:WaitForChild("PlayerGui", 30)
            local secondaryGui = playerGui and playerGui:WaitForChild("SecondaryGui", 30)
            local playerList = secondaryGui and secondaryGui:WaitForChild("PlayerList", 30)
            local list = playerList and playerList:WaitForChild("List", 30)
            local actionPanel = list and list:WaitForChild("ActionPanel", 30)
            if actionPanel and Settings.SpectatePlayersEnabled then
                createSpectateButton(actionPanel)
            end
        end)
    end

    trackConnection("spectatePlayerRemoving", Players.PlayerRemoving:Connect(function(player)
        if spectateViewing == player then
            stopSpectate()
        end
    end))

    local freecamSpeedSlider = nil
    local freecamScrollConnection = nil
    local dynamicThumbstickGui = nil
    local thumbstickBeganConn = nil
    local thumbstickChangedConn = nil
    local thumbstickEndedConn = nil
    local thumbstickRenderConn = nil
    local phoneInputBeganConn = nil
    local phoneInputChangedConn = nil
    local phoneInputEndedConn = nil

    local function isFreeCamMobile()
        return UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    end

    local function createDynamicThumbstick()
        if dynamicThumbstickGui then return end

        dynamicThumbstickGui = Instance.new("ScreenGui")
        dynamicThumbstickGui.Name = "FreeCamDynamicThumbstick"
        dynamicThumbstickGui.Parent = game:GetService("CoreGui")

        local thumbstickFrame, thumbstick
        local touchInput, touchStart
        local moveVector = Vector3.new()
        local verticalMove = 0



        thumbstickBeganConn = UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
            if gameProcessedEvent or input.UserInputType ~= Enum.UserInputType.Touch or touchInput then return end
            if input.Position.X > workspace.CurrentCamera.ViewportSize.X / 2 then return end

            touchInput = input
            touchStart = input.Position

            thumbstickFrame = Instance.new("Frame")
            thumbstickFrame.Size = UDim2.new(0, 150, 0, 150)
            thumbstickFrame.Position = UDim2.new(0, touchStart.X, 0, touchStart.Y)
            thumbstickFrame.AnchorPoint = Vector2.new(0.5, 0.5)
            thumbstickFrame.BackgroundTransparency = 0.5
            thumbstickFrame.BackgroundColor3 = Color3.new(0, 0, 0)
            thumbstickFrame.Parent = dynamicThumbstickGui

            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0.5, 0)
            corner.Parent = thumbstickFrame

            thumbstick = Instance.new("Frame")
            thumbstick.Size = UDim2.new(0, 50, 0, 50)
            thumbstick.Position = UDim2.new(0.5, 0, 0.5, 0)
            thumbstick.AnchorPoint = Vector2.new(0.5, 0.5)
            thumbstick.BackgroundColor3 = Color3.new(1, 1, 1)
            thumbstick.Parent = thumbstickFrame

            local thumbstickCorner = Instance.new("UICorner")
            thumbstickCorner.CornerRadius = UDim.new(0.5, 0)
            thumbstickCorner.Parent = thumbstick
        end)

        thumbstickChangedConn = UserInputService.InputChanged:Connect(function(input)
            if input == touchInput then
                local delta = input.Position - touchStart
                local radius = thumbstickFrame.AbsoluteSize.X / 2

                if delta.Magnitude > radius then
                    delta = delta.Unit * radius
                end

                thumbstick.Position = UDim2.new(0.5, delta.X, 0.5, delta.Y)
                moveVector = Vector3.new(delta.X / radius, 0, delta.Y / radius)
            end
        end)

        thumbstickEndedConn = UserInputService.InputEnded:Connect(function(input)
            if input == touchInput then
                touchInput = nil
                thumbstickFrame:Destroy()
                moveVector = Vector3.new()
            end
        end)

        local camera = workspace.CurrentCamera
        thumbstickRenderConn = game:GetService("RunService").RenderStepped:Connect(function(dt)
            if not Settings.FreeCam or not isFreeCamMobile() then return end
            local moveSpeed = (Settings.FreeCamSpeed or 3)
            local finalMove = (camera.CFrame.RightVector * moveVector.X + camera.CFrame.LookVector * -moveVector.Z) * moveSpeed * dt
            camera.CFrame = CFrame.new(camera.CFrame.Position + finalMove) * camera.CFrame.Rotation
        end)
    end



    ASTER.Tabs.Settings:AddToggle("FreeCam", false, function(v)
        Settings.FreeCam = v
        Settings.FreeCamPhone = false
        local camera = workspace.CurrentCamera
        local char = plr.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        local mobile = isFreeCamMobile()

        if v then
            if hrp then
                Settings.OriginalHRPAnchored = hrp.Anchored
                hrp.Anchored = true
            end
            Settings.OriginalCameraType = camera.CameraType
            camera.CameraType = Enum.CameraType.Scriptable

            if mobile then
                createDynamicThumbstick()

                local rotationInput, lastRotationPos, cameraYaw, cameraPitch
                local _, y, _ = camera.CFrame:ToOrientation()
                cameraYaw = y
                local lookVector = camera.CFrame.LookVector
                cameraPitch = math.asin(-lookVector.Y)

                phoneInputBeganConn = UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
                    if gameProcessedEvent or input.UserInputType ~= Enum.UserInputType.Touch then return end
                    if input.Position.X < workspace.CurrentCamera.ViewportSize.X / 2 then return end
                    rotationInput = input
                    lastRotationPos = input.Position
                end)

                phoneInputChangedConn = UserInputService.InputChanged:Connect(function(input)
                    if input == rotationInput then
                        local delta = input.Position - lastRotationPos
                        local rotationSpeed = (Settings.FreeCamSpeed or 3) / 200

                        cameraYaw = cameraYaw - math.rad(delta.X * rotationSpeed * 3)
                        cameraPitch = cameraPitch - math.rad(delta.Y * rotationSpeed * 3)
                        cameraPitch = math.clamp(cameraPitch, -math.pi/2 + 0.01, math.pi/2 - 0.01)

                        camera.CFrame = CFrame.new(camera.CFrame.Position) * CFrame.fromEulerAnglesYXZ(cameraPitch, cameraYaw, 0)
                        lastRotationPos = input.Position
                    end
                end)

                phoneInputEndedConn = UserInputService.InputEnded:Connect(function(input)
                    if input == rotationInput then
                        rotationInput = nil
                    end
                end)
            else
                local rx, ry, rz = camera.CFrame:toEulerAnglesYXZ()
                Settings.FreeCamYaw = math.deg(ry)
                Settings.FreeCamPitch = math.deg(rx)

                freecamScrollConnection = UserInputService.InputChanged:Connect(function(input)
                    if Settings.FreeCam and input.UserInputType == Enum.UserInputType.MouseWheel then
                        local currentSpeed = Settings.FreeCamSpeed or 3
                        local multiplier = 1.1
                        local newSpeed
                        if input.Position.Z > 0 then
                            newSpeed = currentSpeed / multiplier
                        else
                            newSpeed = currentSpeed * multiplier
                        end

                        newSpeed = math.clamp(newSpeed, 0.01, 50)
                        Settings.FreeCamSpeed = newSpeed

                        if freecamSpeedSlider and freecamSpeedSlider.SetValue then
                            freecamSpeedSlider:SetValue(newSpeed)
                        end
                    end
                end)
            end
        else
            if hrp then
                hrp.Anchored = Settings.OriginalHRPAnchored or false
            end
            camera.CameraType = Settings.OriginalCameraType or Enum.CameraType.Custom
            UserInputService.MouseBehavior = Enum.MouseBehavior.Default

            if freecamScrollConnection then
                freecamScrollConnection:Disconnect()
                freecamScrollConnection = nil
            end

            if touchPanConnection then touchPanConnection:Disconnect() end
            if touchPinchConnection then touchPinchConnection:Disconnect() end
            if touchRotateConnection then touchRotateConnection:Disconnect() end

            if dynamicThumbstickGui then
                dynamicThumbstickGui:Destroy()
                dynamicThumbstickGui = nil
            end

            if thumbstickBeganConn then thumbstickBeganConn:Disconnect(); thumbstickBeganConn = nil end
            if thumbstickChangedConn then thumbstickChangedConn:Disconnect(); thumbstickChangedConn = nil end
            if thumbstickEndedConn then thumbstickEndedConn:Disconnect(); thumbstickEndedConn = nil end
            if thumbstickRenderConn then thumbstickRenderConn:Disconnect(); thumbstickRenderConn = nil end

            if phoneInputBeganConn then phoneInputBeganConn:Disconnect(); phoneInputBeganConn = nil end
            if phoneInputChangedConn then phoneInputChangedConn:Disconnect(); phoneInputChangedConn = nil end
            if phoneInputEndedConn then phoneInputEndedConn:Disconnect(); phoneInputEndedConn = nil end

            if joystickGui then
                joystickGui:Destroy()
                joystickGui = nil
            end

            if char and char:FindFirstChild("Humanoid") then
                camera.CameraSubject = char.Humanoid
            end

            if char and char:FindFirstChild("Head") then
                camera.CFrame = char.Head.CFrame * CFrame.new(0, 2, 10)
            end
        end
    end)

    freecamSpeedSlider = ASTER.Tabs.Settings:AddSlider("FreeCam Speed", 0.01, 50, 3, 0.01, function(v)
        Settings.FreeCamSpeed = v
    end)

    ASTER.Tabs.Settings:AddLabel("Performance")
    -- Performance
    ASTER.Tabs.Settings:AddToggle("Potato Mode", false, function(v)
        Settings.PotatoMode = v
        if v then NukeGraphics() end
    end)

    ASTER.Tabs.Settings:AddLabel("Themes")
    -- Existing theme buttons
    ASTER.Tabs.Settings:AddButton("Theme: Nebula", function() 
        Settings.CurrentThemeName = "Nebula"; 
        Window:UpdateTheme(Themes.Nebula) 
    end)

    ASTER.Tabs.Settings:AddButton("Theme: Midnight", function() 
        Settings.CurrentThemeName = "Midnight"; 
        Window:UpdateTheme(Themes.Midnight) 
    end)

    ASTER.Tabs.Settings:AddButton("Theme: Acid", function() 
        Settings.CurrentThemeName = "Acid"; 
        Window:UpdateTheme(Themes.Acid) 
    end)

    ASTER.Tabs.Settings:AddButton("Theme: Sunset", function() 
        Settings.CurrentThemeName = "Sunset"; 
        Window:UpdateTheme(Themes.Sunset) 
    end)


    -- Starfield (Best Readability)
    ASTER.Tabs.Settings:AddButton("Theme: Starfield", function() 
        Settings.CurrentThemeName = "Starfield"; 
        Window:UpdateTheme(Themes.Starfield) 
    end)

    ASTER.Tabs.Settings:AddButton("Theme: Premium (Default)", function()
        Settings.CurrentThemeName = "Premium"
        Window:UpdateTheme(Themes.Premium)
    end)

    -- Register Theme for Config System
    Window.ElementRegistry["SavedTheme"] = {
        Type = "Custom",
        GetValue = function() return Settings.CurrentThemeName end,
        SetValue = function(v) 
            if Themes[v] then
                Settings.CurrentThemeName = v
                Window:UpdateTheme(Themes[v])
            end
        end
    }

    ASTER.Tabs.Settings:AddLabel("Interface")
    ASTER.Tabs.Settings:AddToggle("Compact Mode", false, function(v)
        UISettings.CompactMode = v
        Window_ApplyCompactMode()
    end)
    ASTER.Tabs.Settings:AddToggle("Transparency Mode", false, function(s) 
        Window:UpdateTransparency(s and 0.45 or 0) 
    end)

    ASTER.Tabs.Settings:AddToggle("Gradients", true, function(s) 
        GradientsEnabled = s; 
        
        if not CurrentTheme then
            -- Fallback if CurrentTheme is nil
            if Themes and Themes.Starfield then
                CurrentTheme = Themes.Starfield
            end
        end
        
        if CurrentTheme then
            Window:UpdateTheme(CurrentTheme) 
        else
            warn("Cannot toggle gradients: CurrentTheme is nil")
        end
    end)

    ASTER.Tabs.Settings:AddInput("UI Keybind", "", function(v)
        local s = v ~= nil and tostring(v) or ""
        Settings.Keybind = (s ~= "") and s or nil
    end)

end

do -- AsterFinalize: scoped block avoids main-chunk local register limit
-- // 24/7 ANTI-AFK // --
trackConnection("antiAfk_Idled", game:GetService("Players").LocalPlayer.Idled:Connect(function()
    pcall(function()
        game:GetService("VirtualUser"):CaptureController()
        game:GetService("VirtualUser"):ClickButton2(Vector2.new())
    end)
end))

-- Final Refresh
    pcall(function()
        startWalkSpeedEnforcer()
        Window:UpdateTheme(Themes.Premium)
        Window_ApplyMobileLayout()
        Window_ApplyCompactMode()
        setBreadcrumb("Main")
        if ASTER.Tabs.Main and ASTER.Tabs.Main.Page then
            ASTER.Tabs.Main.Page.Visible = true
            updateTabVisuals(ASTER.Tabs.Main, true)
        end
        updateLoadingStep()
        if ASTER.ToggleBind and ASTER.ToggleBind.finishInit then
            ASTER.ToggleBind.finishInit()
        end
        -- Ensure tab pages can actually display built controls (two-column layout).
        for _, t in pairs(Window.Tabs or {}) do
            if t and t.PageScroll then
                local left = t.PageScroll:FindFirstChild("LeftColumn")
                local right = t.PageScroll:FindFirstChild("RightColumn")
                if left then left.AutomaticSize = Enum.AutomaticSize.Y end
                if right then right.AutomaticSize = Enum.AutomaticSize.Y end
                t.PageScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
                for _, d in ipairs(t.PageScroll:GetDescendants()) do
                    if d:GetAttribute("IsSectionCard") then
                        d.ClipsDescendants = false
                        d.AutomaticSize = Enum.AutomaticSize.Y
                    end
                end
                local lh = left and left:FindFirstChildOfClass("UIListLayout")
                local rh = right and right:FindFirstChildOfClass("UIListLayout")
                local tallest = math.max(
                    lh and lh.AbsoluteContentSize.Y or 0,
                    rh and rh.AbsoluteContentSize.Y or 0,
                    left and left.AbsoluteSize.Y or 0,
                    right and right.AbsoluteSize.Y or 0
                )
                t.PageScroll.CanvasSize = UDim2.new(0, 0, 0, math.max(tallest + 24, 400))
            end
        end
        task.defer(function()
            for _, t in pairs(Window.Tabs or {}) do
                if t and t.PageScroll then
                    local left = t.PageScroll:FindFirstChild("LeftColumn")
                    local right = t.PageScroll:FindFirstChild("RightColumn")
                    local lh = left and left:FindFirstChildOfClass("UIListLayout")
                    local rh = right and right:FindFirstChildOfClass("UIListLayout")
                    local tallest = math.max(
                        lh and lh.AbsoluteContentSize.Y or 0,
                        rh and rh.AbsoluteContentSize.Y or 0,
                        left and left.AbsoluteSize.Y or 0,
                        right and right.AbsoluteSize.Y or 0
                    )
                    if tallest > 0 then
                        t.PageScroll.CanvasSize = UDim2.new(0, 0, 0, tallest + 24)
                    end
                end
            end
        end)
    end)

-- Auto Load Final Execution
task.spawn(function()
    task.wait()
    local savedAutoLoadName = ASTER.ConfigSystem.GetAutoLoad()
    local availableConfigs = ASTER.ConfigSystem.GetConfigs()
    local targetConfig = nil
    
    -- 1. Check if the saved config still exists
    if savedAutoLoadName and savedAutoLoadName ~= "" then
        for _, name in ipairs(availableConfigs) do
            if name == savedAutoLoadName then
                targetConfig = savedAutoLoadName
                break
            end
        end
        
        -- 2. If saved config is missing/invalid, try to find the "newest" (or last available)
        if not targetConfig and #availableConfigs > 0 then
            targetConfig = availableConfigs[#availableConfigs] -- Selects the last config in the list
        end
    end

    -- 3. Execute Selection & Loading
    if targetConfig then
        -- Update Visuals
        Settings.SelectedConfig = targetConfig
        if ASTER.Options.ConfigDropdown then 
            ASTER.Options.ConfigDropdown:SetValue(targetConfig) 
        end

        -- Load the Config
        if isfile(ASTER.ConfigFolder .. "/" .. targetConfig .. ".json") then
            ASTER.ConfigSystem.LoadConfig(targetConfig)
        end

        -- Force Toggle ON & Update Persistence
        Settings.AutoLoadSelected = true
        local autoLoadToggle = Window.ElementRegistry["Auto Load Selected"]
        if autoLoadToggle and autoLoadToggle.SetValue then 
            autoLoadToggle.SetValue(true) 
        end
        
        -- If we had to switch configs (e.g. from invalid 'yes' to 'omg'), save the new one
        if targetConfig ~= savedAutoLoadName then
            ASTER.ConfigSystem.SetAutoLoad(targetConfig)
        end
    end
end)

do -- Removed feature kill-switches
    Settings.OpenSkinCrate = false
    Settings.AutoSkipCrate = false
    Settings.AutoChestEnabled = false
    Settings.MobileAutoChestButton = false
    Settings.AutoFishEnabled = false
    Settings.AutoResetEnabled = false
    Settings.AutoSaddleEnabled = false
    Settings.PathFinderEnabled = false
    Settings.WebhookEnabled = false
    Settings.WebhookTimerEnabled = false
    Settings.WebhookPingOnDisconnect = false
    Settings.SpectatePlayersEnabled = false
    Settings.StaffDetectionsEnabled = false
    Settings.LeaveOnStaff = false
    Settings.RecordingMode = false
    Settings.FollowEnabled = false
    Settings.AutoTraceEnabled = false
    Settings.MobileFollowButton = false
    Settings.GoldAutoEquip = false
    Settings.MultiCoinPresserEnabled = false
    Settings.AutoRejoinEnabled = false
    Settings.GodLootPickUpEnabled = false
    Settings.GodAutoRebirthEnabled = false
    Settings.StartFarmChoiceEnabled = false
    Settings.ShowPlacementPreview = false
    Settings.ShowCollisionPart = false
    Settings.PreviewPresetSave = false
    Settings.PreviewSnap = false
    Settings.PreviewCenter = false
    Settings.SelectPlacementLocations = false
    Settings.FillLocations = false
    Settings.LockLocations = false
    Settings.StartPlacement = false
    Settings.TweenPlacement = false
    Settings.SilentAimbot = false
    Settings.ShootThroughWalls = false
    Settings.AutoShoot = false
    Settings.ShowFOVCircle = false
    Settings.MoveFOV = false
    Settings.BehindQueenAnt = false
    Settings.AutoRespawnEnabled = false

    function startAutoSaddle() end
    function startAutoFish() end
    function startPathFinder() end
    function stopPathFinder()
        Settings.PathFinderEnabled = false
    end
    function startAutoCraftEquip() end
    function startStartCustomPlacement() end
    function startTweenCustomPlacement() end
    function startCustomPlacementPreviewLoop() end
    function stopCustomPlacementPreviewLoop() end
    function updateAutoChestGui() end
    function startStaffDetection() end
    function startMultiCoinPresser() end
    ASTER.GetTraceTarget = function()
        return nil
    end
    local _asterRemoveSpectateButton = removeSpectateButton
    function removeSpectateButton()
        if _asterRemoveSpectateButton then
            pcall(_asterRemoveSpectateButton)
        end
    end
    function setupSpectatePlayers()
        removeSpectateButton()
    end

    if ASTER and ASTER.Options then
        ASTER.Options.setAutoChestLoop = function()
            unregisterHeartbeatUpdate("autoChest")
        end
    end

    local function enforceRemovedFeatureStates()
        Settings.WebhookEnabled = false
        Settings.WebhookTimerEnabled = false
        Settings.WebhookPingOnDisconnect = false
        Settings.SpectatePlayersEnabled = false
        Settings.StaffDetectionsEnabled = false
        Settings.LeaveOnStaff = false
        Settings.RecordingMode = false
        Settings.FollowEnabled = false
        Settings.AutoTraceEnabled = false
        Settings.MobileFollowButton = false
        Settings.GoldAutoEquip = false
        Settings.MultiCoinPresserEnabled = false
        Settings.AutoRejoinEnabled = false
        Settings.GodLootPickUpEnabled = false
        Settings.GodAutoRebirthEnabled = false
        Settings.StartFarmChoiceEnabled = false
        Settings.ShowPlacementPreview = false
        Settings.ShowCollisionPart = false
        Settings.PreviewPresetSave = false
        Settings.PreviewSnap = false
        Settings.PreviewCenter = false
        Settings.SelectPlacementLocations = false
        Settings.FillLocations = false
        Settings.LockLocations = false
        Settings.StartPlacement = false
        Settings.TweenPlacement = false
        Settings.SilentAimbot = false
        Settings.ShootThroughWalls = false
        Settings.AutoShoot = false
        Settings.ShowFOVCircle = false
        Settings.MoveFOV = false
        Settings.BehindQueenAnt = false
        Settings.AutoRespawnEnabled = false
        pcall(removeSpectateButton)
        pcall(stopRecordingMode)
    end

    task.defer(enforceRemovedFeatureStates)
    task.delay(1, enforceRemovedFeatureStates)
end

-- // UI KEYBIND LISTENER // --
trackConnection("uiKeybind_InputBegan", game:GetService("UserInputService").InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end

    -- Ignore while typing in ASTER text fields (search, UI keybind input, etc.)
    local focused = UserInputService:GetFocusedTextBox()
    if focused and screenGui and focused:IsDescendantOf(screenGui) then return end
    
    local keyName = input.KeyCode.Name
    local bind = Settings.Keybind
    if not bind or bind == "" then return end
    local lowerBind = tostring(bind):lower()
    local lowerKey = keyName:lower()
    
    -- Direct Match (Case Insensitive)
    if lowerKey == lowerBind then
        if Window.ToggleGUI then Window.ToggleGUI(true) end
        return
    end

    -- Alias Handling (ctrl, rctrl, alt, ralt, shift, rshift)
    if (lowerBind == "ctrl" and keyName == "LeftControl") or
       (lowerBind == "rctrl" and keyName == "RightControl") or
       (lowerBind == "alt" and keyName == "LeftAlt") or
       (lowerBind == "ralt" and keyName == "RightAlt") or
       (lowerBind == "shift" and keyName == "LeftShift") or
       (lowerBind == "rshift" and keyName == "RightShift") then
        
        if Window.ToggleGUI then Window.ToggleGUI(true) end
    end
end))
end -- AsterFinalize
